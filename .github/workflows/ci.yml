name: CI

on:
  push:
    branches:
      - main
  pull_request:

# H-4 (pre-0.6.0 security fix): scope workflow permissions to least privilege.
# Default GITHUB_TOKEN has broader access that a compromised third-party action
# could exploit.
permissions:
  contents: read

# A newer pull-request commit supersedes its older run, so those runs share a
# group and cancel promptly. GitHub cancels an older pending run in a shared
# group even when `cancel-in-progress` is false, so every non-PR run receives a
# unique group and must finish with an unambiguous result.
concurrency:
  group: ${{ github.event_name == 'pull_request' && format('{0}-{1}', github.workflow, github.ref) || format('{0}-{1}', github.workflow, github.run_id) }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # 0.8.23 Slice 50: reachable history is scanned independently of the
  # docs-only detector, because a path filter cannot be a security boundary.
  gitleaks:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Install pinned Gitleaks
        run: bash scripts/install-gitleaks.sh
      - name: Scan reachable Git history
        run: bash scripts/security/gitleaks-history.sh

  # 0.8.21 Slice 35 (SHELL-LINT-CI) — the shell gate, EARLY and INDEPENDENT.
  # Design of record: dev/design/ci-verify-robustness-review.md R2.1.
  #
  # THE MEASURED CASE. On the 2026-08-04 run the shell defect's verdict was in
  # the `verify` log at 00:45:39 and the job exited at 01:15:26 — 29m47s after
  # the answer was known. Slice 30 put the shell gate FIRST inside
  # agent-verify.sh (line 22), which moves this bug class from `step=test` to
  # `step=lint`, but `verify` still burns ~3 minutes of job setup (checkout,
  # python, node, the rust toolchain, rust-cache, a full bootstrap) before lint
  # begins — and `verify` is SKIPPED ENTIRELY on a docs-only PR. This job
  # reports the same verdict in about a minute, on every PR, as its own check.
  #
  # ALWAYS-ON: no `if:`, no `needs:`, NOT on the docs_only fast path. Same
  # reasoning as ledger-integrity / plan-anchors / release-state-views below, and
  # `scripts/tests/test_shell_lint_ci_job.sh` ENFORCES it rather than trusting
  # this comment. Two concrete reasons, not boilerplate: (1) a `needs: changes`
  # would make this gate CONTINGENT on another job — a `changes` failure, or a
  # paths-filter misread, would make the shell gate silently ABSENT rather than
  # red; (2) the gate is enforced over EVERY tracked `*.sh` plus two ratchet
  # files, so it can go red on a diff that touches no shell file at all (a
  # rename, or a listed file that has become clean and must leave its ratchet).
  # Running unconditionally also costs about a minute, which is cheaper than
  # reasoning about when it may be skipped.
  #
  # MINIMAL SETUP, on purpose: checkout + the pinned shellcheck, and nothing
  # else. No rust toolchain, no node, no python venv — the gate is bash, git and
  # shellcheck. Installing those would reintroduce the ~3 minutes this job
  # exists to avoid.
  #
  # ⛔ THIS GATES. No `continue-on-error`, no rc captured and discarded, no
  # advisory `exit 0`. (The `rust-workspace-race-report` job below IS
  # deliberately report-only; that is not the model here.)
  shell-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        # The shrink-only ratchets compare a PR to origin/$GITHUB_BASE_REF.
        # A shallow checkout cannot prove that base, so it must fail rather
        # than silently permit a new exemption.
        with:
          fetch-depth: 0
      - name: Cache the checksum/version-keyed ShellCheck archive
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/fathomdb/shellcheck
          # hashFiles covers the shared version pin AND every recorded platform
          # checksum. A hit still undergoes SHA-256 verification in the
          # installer; the cache saves time, never changes what is trusted.
          key: shellcheck-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('scripts/lib/shellcheck-version.sh', 'scripts/install-shellcheck.sh') }}
      # The ubuntu-latest image ships its OWN shellcheck, at whatever version
      # GitHub picked; it will not match the pin and would hard-fail the repo's
      # version preflight. Handled by installing the pinned binary into
      # ~/.local/bin, which find_shellcheck_bin prefers over PATH — NOT by
      # relaxing the pin. This is the same installer scripts/bootstrap.sh runs.
      - name: Install the pinned shellcheck (bootstrap's shellcheck leg only)
        run: bash scripts/install-shellcheck.sh
      - name: Shell lint (0.8.21 Slice 30 gate — the same script agent-verify runs)
        run: bash scripts/agent-lint-shell.sh
      - name: Recurrence guard for the job itself (fixture arms)
        run: bash scripts/tests/test_shell_lint_ci_job.sh

  # Docs-only fast-path detector. Computes whether the diff touches ANY
  # non-markdown file. A markdown/docs-only change then runs ONLY the lightweight
  # `markdownlint` job (plus the always-on `docs` mkdocs gate) and skips the heavy
  # verify / security / embedder / wheel jobs — so a trivial docs edit
  # no longer burns the full matrix (and a single md finding no longer reds main
  # via the heavy gate). The md gate itself is PRESERVED on the fast path; this is
  # not a `paths-ignore` drop. paths-filter handles base-ref resolution for both
  # push (before SHA) and pull_request (base branch).
  changes:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      # docs_only = true when NO changed file is outside markdown (i.e. every
      # changed path is *.md). `nonmd` is true when at least one non-md file
      # changed; docs_only is its negation. The bias is safe: a misread can only
      # make heavy jobs RUN on a docs change (slower), never SKIP on a code change.
      docs_only: ${{ steps.filter.outputs.nonmd == 'false' }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d # v4.0.1
        id: filter
        with:
          filters: |
            nonmd:
              - '!**/*.md'

  # The fast verifier owns every cheap diagnostic suite plus lint, typecheck,
  # and security. It runs alongside the heavy language-test tier below. The
  # tier-totality fixture makes the partition fail closed: a new registration
  # cannot be absent from both jobs, and no suite can be in both by accident.
  verify-fast:
    needs: changes
    if: needs.changes.outputs.docs_only != 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Configure per-session TMPDIR
        shell: bash
        run: |
          TMPDIR="$RUNNER_TEMP/fathomdb-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          mkdir -p "$TMPDIR"
          echo "TMPDIR=$TMPDIR" >> "$GITHUB_ENV"
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "25.9.0"
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # stable
        with:
          toolchain: "1.95.0"
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Bootstrap dev tooling
        run: bash scripts/bootstrap.sh
      - name: agent-verify fast (lint -> typecheck -> security -> cheap suites)
        env:
          AGENT_VERBOSE: "1"
        run: bash scripts/agent-verify.sh --tier=fast
      - name: Collect redacted fast verifier spill logs
        if: failure()
        shell: bash
        run: |
          set -euo pipefail
          spill_dir="${RUNNER_TEMP}/fathomdb-agent-spill-logs-fast"
          bash scripts/collect-agent-spill-logs.sh --destination "$spill_dir"
      - name: Upload redacted fast verifier spill logs
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: fathomdb-agent-spill-logs-fast-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/fathomdb-agent-spill-logs-fast
          if-no-files-found: error

  # Heavy language suites keep the established `verify` check name for the
  # release tooling, but it deliberately runs only the complementary tier.
  # Both jobs are gates; neither uses retries, quarantine, or path-based
  # language skips.
  verify:
    needs: changes
    if: needs.changes.outputs.docs_only != 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      # H-5 (pre-0.6.0 security fix): pin third-party actions to commit SHAs.
      # 0.8.9 Slice 1: fetch-depth 0 so the AC-050c removal-detect gate can
      # resolve its base ref (v0.6.1 tag); the default shallow checkout has no
      # tags, which made `git diff v0.6.1..HEAD` fail (`bad revision`) once the
      # bootstrap un-mask let agent-security.sh actually reach AC-050c.
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Configure per-session TMPDIR
        shell: bash
        run: |
          # Per-session TMPDIR root so cleanup is one rm -rf at job end (GH #40).
          # Set via $GITHUB_ENV — runner.* context isn't available at job-env level.
          TMPDIR="$RUNNER_TEMP/fathomdb-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          mkdir -p "$TMPDIR"
          echo "TMPDIR=$TMPDIR" >> "$GITHUB_ENV"
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "25.9.0"
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # stable
        with:
          toolchain: "1.95.0"
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Bootstrap dev tooling
        run: bash scripts/bootstrap.sh
      - name: agent-verify heavy (Rust -> Python -> TypeScript)
        env:
          # run_capped is normally quiet on success. CI needs per-suite timing
          # and status to expose a growing slow tail before it becomes a
          # failure; agent-suite-run also mirrors this table to the step
          # summary.
          AGENT_VERBOSE: "1"
        run: bash scripts/agent-verify.sh --tier=heavy
      # run_capped keeps complete diagnostics in /tmp, while the log exposes
      # only its first 200 lines. Collect redacted, regular spill files after
      # any verify failure so a long test/ruff failure remains root-causable
      # after this ephemeral runner exits. The collector deliberately rejects
      # symlinks and removes environment-transcript lines before artifacting.
      - name: Collect redacted agent verifier spill logs
        if: failure()
        shell: bash
        run: |
          set -euo pipefail
          spill_dir="${RUNNER_TEMP}/fathomdb-agent-spill-logs"
          bash scripts/collect-agent-spill-logs.sh --destination "$spill_dir"
      - name: Upload redacted agent verifier spill logs
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: fathomdb-agent-spill-logs-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/fathomdb-agent-spill-logs
          if-no-files-found: error

  # TC-74 diagnostic only: retain the parallel workspace run as an artifact
  # without allowing its known scheduling-sensitive failures to gate a release.
  rust-workspace-race-report:
    needs: changes
    if: needs.changes.outputs.docs_only != 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # stable
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Parallel Rust workspace race report
        shell: bash
        run: |
          set +e
          log="$RUNNER_TEMP/rust-workspace-parallel.log"
          artifact_name="rust-workspace-parallel-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          started_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          started_seconds="$(date +%s)"
          bash scripts/test-rust-workspace.sh --parallel-report >"$log" 2>&1
          rc=$?
          set -e
          ended_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          ended_seconds="$(date +%s)"
          duration_seconds=$((ended_seconds - started_seconds))
          {
            echo "mode=parallel-report"
            echo "sha=${GITHUB_SHA}"
            echo "run_attempt=${GITHUB_RUN_ATTEMPT}"
            echo "started_at=${started_at}"
            echo "ended_at=${ended_at}"
            echo "duration_seconds=${duration_seconds}"
            echo "log_name=$(basename "$log")"
            echo "artifact_name=${artifact_name}"
            echo "exit_code=${rc}"
          } >> "$GITHUB_STEP_SUMMARY"
          if [ "$rc" -ne 0 ]; then
            echo "::warning title=Rust workspace parallel report::parallel run exited ${rc}; inspect artifact=${artifact_name}"
          fi
          exit 0
      - name: Upload parallel Rust workspace report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: rust-workspace-parallel-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/rust-workspace-parallel.log


  # Gate (n) — agent-security battery (AC-036/037/038/050a/050c).
  #
  # 0.8.0 Slice 40 (Q3/META): the agent-verify job above already runs
  # STRICT=1 agent-security.sh, but on ubuntu-latest (24.04) AppArmor
  # restricts unprivileged user namespaces, so the AC-037 netns-deny-egress
  # fixture cannot actually create its userns there. This dedicated job pins
  # the security battery to ubuntu-22.04, which permits unprivileged userns
  # by default, so AC-037 genuinely exercises (the home of AC-037 per
  # acceptance.md). STRICT=1 promotes any toolchain blocker to a hard
  # failure — a blocker here means AC-037 did NOT run, which must fail the
  # gate rather than pass vacuously.
  security:
    needs: changes
    if: needs.changes.outputs.docs_only != 'true'
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    steps:
      # 0.8.9 Slice 1: fetch-depth 0 so AC-050c removal-detect can resolve its
      # v0.6.1 base tag (see the verify job above for the same rationale).
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Configure per-session TMPDIR
        shell: bash
        run: |
          TMPDIR="$RUNNER_TEMP/fathomdb-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          mkdir -p "$TMPDIR"
          echo "TMPDIR=$TMPDIR" >> "$GITHUB_ENV"
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # stable
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Bootstrap dev tooling (installs strace for AC-036/AC-037)
        run: bash scripts/bootstrap.sh
      - name: agent-security (AC-036/037/038/050a/050c)
        run: STRICT=1 bash scripts/agent-security.sh

  # EU-5c — pre-warm the BGE-small embedder weight cache (~133 MB from HF)
  # via actions/cache, then run the network-hitting EU-5b tests against the
  # warm cache. If the warm step fails (e.g. HF unreachable), the env-gate
  # `FATHOMDB_SKIP_NETWORK_TESTS=1` is exported and the gated tests skip
  # cleanly with a log line instead of failing the job. On the happy path
  # the env var stays unset and the tests run normally — fast on warm cache.
  default-embedder-tests:
    needs: changes
    if: needs.changes.outputs.docs_only != 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Configure per-session TMPDIR
        shell: bash
        run: |
          TMPDIR="$RUNNER_TEMP/fathomdb-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          mkdir -p "$TMPDIR"
          echo "TMPDIR=$TMPDIR" >> "$GITHUB_ENV"
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # stable
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      # H-5: pin third-party actions to commit SHAs. Cache key is
      # runner.os + model identity + HF revision (see
      # fathomdb-embedder/src/loader.rs::HF_REVISION). Bumping the
      # revision invalidates the cache cleanly.
      - name: Restore BGE embedder weight cache
        id: bge-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/fathomdb/embedders
          key: ${{ runner.os }}-fathomdb-embedder-bge-small-en-v1.5-5c38ec7c405ec4b44b94cc5a9bb96e735b38267a
          # Future revision bumps should still benefit from partial
          # restoration (the loader will sha-verify each file and refetch
          # only mismatched ones). Without this, a SHA bump = 100% cold
          # miss (~133 MB redownload from HF).
          restore-keys: |
            ${{ runner.os }}-fathomdb-embedder-bge-small-en-v1.5-
            ${{ runner.os }}-fathomdb-embedder-
      - name: Warm BGE embedder cache (no-op on cache hit)
        id: warm
        shell: bash
        run: |
          set +e
          # Surface cache-hit outcome as a GitHub Actions notice so it
          # shows up in the run summary without log-diving. A regression
          # that breaks the cache key (silently forcing cold downloads
          # on every run) becomes visible at a glance here.
          echo "::notice title=BGE cache::hit=${{ steps.bge-cache.outputs.cache-hit }}"
          cargo run --quiet -p fathomdb-cli --features default-embedder \
            --bin fathomdb -- doctor warm-cache --json
          rc=$?
          if [ "$rc" -ne 0 ]; then
            echo "warm-cache failed (rc=$rc); will skip network-hitting tests"
            echo "FATHOMDB_SKIP_NETWORK_TESTS=1" >> "$GITHUB_ENV"
          fi
          # Never fail the job on warm-cache failure — the env gate covers it.
          exit 0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
      - name: Install the Python binding from this checkout
        run: python -m pip install -e 'src/python[dev]'
      # The generic agent loop deliberately sets FATHOMDB_SKIP_NETWORK_TESTS=1
      # so developers can run its full prework without a live model download.
      # This cache-owning job runs the complementary live/release TypeScript
      # coverage and the one Python edge-body graph-arm fixture that requires a
      # live embedder. On a warm failure they inherit the conditional
      # GITHUB_ENV skip gate above, while non-network and release-surface
      # assertions still run.
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "25.9.0"
      - name: Install locked TypeScript test dependencies
        run: cd src/ts && npm ci
      - name: Build native debug TypeScript test binding
        run: cd src/ts && npm run build:native:debug
      - name: Emit TypeScript test files
        run: cd src/ts && npm exec -- tsc -p tsconfig.json
      - name: Default-embedder and release-surface TypeScript tests
        run: |
          cd src/ts
          # Each file starts a native engine and may load the BGE model. Run a
          # fresh Node test runner for each file: Node 25's multi-file runner
          # can retain an exited child's IPC handles, and concurrent children
          # contend for the same finite runner memory and CPU.
          for test_file in \
            dist/tests/embed-batch-cls.test.js \
            dist/tests/embedder-event-narrowing.test.js \
            dist/tests/functional-embed.test.js \
            dist/tests/release-surface.test.js \
            dist/tests/slice20c-flush-barrier.test.js \
            dist/tests/tc67-unsupported-vector-kind-report.test.js \
            dist/tests/use-default-embedder.test.js
          do
            RELEASE_SURFACE_TESTS=1 node --test "$test_file"
          done
      - name: Network-hitting EU-5b tests (engine)
        run: |
          cargo test -p fathomdb-engine --features default-embedder \
            --test eu5b_lockflip
      - name: Network-hitting Python SDK tests
        run: |
          python -m pytest \
            src/python/tests/test_use_default_embedder.py \
            src/python/tests/test_embed.py \
            src/python/tests/test_functional_graph_arm.py::test_edge_body_enables_edge_fact_seeding \
            src/python/tests/test_slice20c_flush_barrier.py::test_a_no_embedder_session_leaves_an_enrolled_kinds_write_recoverable
      - name: Network-hitting EU-5b tests (cli warm-cache)
        run: |
          cargo test -p fathomdb-cli --features default-embedder \
            --test cli_warm_cache

  # 0.8.23 Slice 60: a separate OS process holds a real SQLite WAL read
  # snapshot while `excise_source` attempts its truncating checkpoint. This is
  # deliberately a focused Windows x64 source test, not artifact validation:
  # its retained log records elapsed checkpoint time and the typed lock result.
  windows-wal-checkpoint-diagnosis:
    needs: changes
    if: needs.changes.outputs.docs_only != 'true'
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # stable
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Run cross-process WAL checkpoint diagnosis
        shell: pwsh
        run: |
          cargo test -p fathomdb-engine --features operator --test erasure_completeness erasure_busy_cross_process_windows_yields_typed_diagnostic -- --exact --nocapture 2>&1 | Tee-Object -FilePath "$env:RUNNER_TEMP\slice60-windows-wal-diagnostic.log"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - name: Upload cross-process WAL checkpoint diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: slice60-windows-wal-diagnostic-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/slice60-windows-wal-diagnostic.log
          if-no-files-found: error

  # Slice 65 extends the Slice 60 external-holder witness with private,
  # redacted ownership attribution. The managed-reader control is real SQLite
  # on a reader-worker connection; the child control remains the external
  # comparison. Its artifact contains role/index/phase and checkpoint counters
  # only—never a database path, SQL, query text, source id, or result data.
  windows-wal-attribution:
    needs: changes
    if: needs.changes.outputs.docs_only != 'true'
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.11"
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # stable
        with:
          toolchain: "1.95.0"
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Run WAL ownership attribution controls
        shell: pwsh
        run: |
          $env:FATHOMDB_WAL_ATTRIBUTION = "1"
          $artifact = "$env:RUNNER_TEMP\slice65-windows-wal-attribution.log"
          cargo test -p fathomdb-engine --features operator --lib tests::wal_attribution_owned_reader_records_exact_busy_and_idle_success -- --exact --nocapture 2>&1 | Tee-Object -FilePath "$env:RUNNER_TEMP\slice65-windows-wal-attribution.log"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (-not (Select-String -Path "$env:RUNNER_TEMP\slice65-windows-wal-attribution.log" -SimpleMatch "running 1 test" -Quiet)) {
            Write-Error "managed-reader attribution command selected zero tests"
            exit 1
          }
          cargo test -p fathomdb-engine --features operator --lib tests::wal_attribution_retained_materialized_result_is_idle_at_checkpoint -- --exact --nocapture 2>&1 | Tee-Object -FilePath $artifact -Append
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $readerHandoffLog = "$env:RUNNER_TEMP\slice65-windows-wal-reader-handoff.log"
          cargo test -p fathomdb-engine --features operator --lib tests::wal_attribution_reader_handoff_is_idle_before_materialized_reply -- --exact --nocapture 2>&1 | Tee-Object -FilePath $artifact -Append | Tee-Object -FilePath $readerHandoffLog
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (-not (Select-String -Path $readerHandoffLog -SimpleMatch 'running 1 test' -Quiet)) {
            Write-Error "reader-handoff attribution command selected zero tests"
            exit 1
          }
          if (-not (Select-String -Path $readerHandoffLog -SimpleMatch 'slice65_wal reader_handoff_idle_before_reply=passed' -Quiet)) {
            Write-Error "reader-handoff attribution artifact is missing success marker"
            exit 1
          }
          cargo test -p fathomdb-engine --features operator --lib tests::wal_attribution_reopen_recovery_reads_then_nested_erasure_are_idle -- --exact --nocapture 2>&1 | Tee-Object -FilePath $artifact -Append
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          cargo test -p fathomdb-engine --features operator --lib tests::wal_attribution_projection_worker_transaction_is_owned_then_idle -- --exact --nocapture 2>&1 | Tee-Object -FilePath $artifact -Append
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $postCommitLog = "$env:RUNNER_TEMP\slice65-windows-wal-post-commit.log"
          cargo test -p fathomdb-engine --features operator --lib tests::wal_attribution_post_commit_acknowledges_and_records_raw_checkpoint_diagnostic -- --exact --nocapture 2>&1 | Tee-Object -FilePath $artifact -Append | Tee-Object -FilePath $postCommitLog
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (-not (Select-String -Path $postCommitLog -SimpleMatch 'running 1 test' -Quiet)) {
            Write-Error "post-commit diagnostic command selected zero tests"
            exit 1
          }
          if (-not (Select-String -Path $postCommitLog -SimpleMatch 'slice65_wal post_commit_ack inventory=' -Quiet) -or -not (Select-String -Path $postCommitLog -SimpleMatch 'slice65_wal post_commit_raw case=pre_close' -Quiet) -or -not (Select-String -Path $postCommitLog -SimpleMatch 'slice65_wal post_commit_child_raw case=after_close' -Quiet) -or -not (Select-String -Path $postCommitLog -SimpleMatch 'slice65_wal post_commit_diagnostic=recorded' -Quiet)) {
            Write-Error "post-commit diagnostic artifact is incomplete"
            exit 1
          }
          $closeBoundaryDirectLog = "$env:RUNNER_TEMP\slice65-windows-wal-close-boundary-direct.log"
          cargo test -p fathomdb-engine --features operator --lib tests::wal_attribution_close_boundary_raw_checkpoint_is_clean -- --exact --nocapture 2>&1 | Tee-Object -FilePath $artifact -Append | Tee-Object -FilePath $closeBoundaryDirectLog
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (-not (Select-String -Path $closeBoundaryDirectLog -SimpleMatch 'running 1 test' -Quiet)) {
            Write-Error "close-boundary direct checkpoint control selected zero tests"
            exit 1
          }
          $closeBoundaryFreshLog = "$env:RUNNER_TEMP\slice65-windows-wal-close-boundary-fresh.log"
          cargo test -p fathomdb-engine --features operator --lib tests::wal_attribution_close_boundary_fresh_open_is_clean -- --exact --nocapture 2>&1 | Tee-Object -FilePath $artifact -Append | Tee-Object -FilePath $closeBoundaryFreshLog
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (-not (Select-String -Path $closeBoundaryFreshLog -SimpleMatch 'running 1 test' -Quiet)) {
            Write-Error "close-boundary fresh-open control selected zero tests"
            exit 1
          }
          $closeBoundaryReadGetLog = "$env:RUNNER_TEMP\slice65-windows-wal-close-boundary-read-get.log"
          cargo test -p fathomdb-engine --features operator --lib tests::wal_attribution_close_boundary_read_get_is_clean -- --exact --nocapture 2>&1 | Tee-Object -FilePath $artifact -Append | Tee-Object -FilePath $closeBoundaryReadGetLog
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (-not (Select-String -Path $closeBoundaryReadGetLog -SimpleMatch 'running 1 test' -Quiet)) {
            Write-Error "close-boundary read-get control selected zero tests"
            exit 1
          }
          $closeBoundaryNeighborsLog = "$env:RUNNER_TEMP\slice65-windows-wal-close-boundary-neighbors.log"
          cargo test -p fathomdb-engine --features operator --lib tests::wal_attribution_close_boundary_neighbors_is_clean -- --exact --nocapture 2>&1 | Tee-Object -FilePath $artifact -Append | Tee-Object -FilePath $closeBoundaryNeighborsLog
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (-not (Select-String -Path $closeBoundaryNeighborsLog -SimpleMatch 'running 1 test' -Quiet)) {
            Write-Error "close-boundary neighbors control selected zero tests"
            exit 1
          }
          if (@(Select-String -Path $artifact -SimpleMatch 'running 1 test').Count -lt 10) {
            Write-Error "one or more Slice 65 engine selectors selected zero tests"
            exit 1
          }
          cargo test -p fathomdb-engine --features operator --test erasure_completeness erasure_busy_cross_process_windows_yields_typed_diagnostic -- --exact --nocapture 2>&1 | Tee-Object -FilePath "$env:RUNNER_TEMP\slice65-windows-wal-attribution.log" -Append
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (@(Select-String -Path $artifact -SimpleMatch 'running 1 test').Count -lt 5) {
            Write-Error "external-reader attribution command selected zero tests"
            exit 1
          }
      - name: Build current disposable test-hooks wheel
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          args: --release --out dist-slice65-test -i python3.11 --features pyo3/extension-module,test-hooks
          working-directory: src/python
      - name: Run installed-wheel attribution controls
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $env:FATHOMDB_WAL_ATTRIBUTION = "1"
          $artifact = "$env:RUNNER_TEMP\slice65-windows-wal-attribution.log"
          $releasedEnv = "$env:RUNNER_TEMP\slice65-released-wheel"
          $currentEnv = "$env:RUNNER_TEMP\slice65-current-wheel"
          $bindingLog = "$env:RUNNER_TEMP\slice65-windows-python-binding.log"
          "slice65_wal current_head=$(git rev-parse HEAD)" | Tee-Object -FilePath $artifact -Append
          python -m venv $releasedEnv
          & "$releasedEnv\Scripts\python.exe" -m pip install --only-binary=:all: fathomdb==0.8.22 2>&1 | Tee-Object -FilePath $artifact -Append
          & "$releasedEnv\Scripts\python.exe" -m pip show fathomdb 2>&1 | Tee-Object -FilePath $artifact -Append
          $releasedNative = (Get-ChildItem "$releasedEnv\Lib\site-packages\fathomdb\_fathomdb*.pyd" | Select-Object -First 1).FullName
          "slice65_wal released_native_sha256=$((Get-FileHash $releasedNative -Algorithm SHA256).Hash)" | Tee-Object -FilePath $artifact -Append
          & "$releasedEnv\Scripts\python.exe" src/python/tests/test_slice65_wal_attribution_installed.py --control serial --wheel-version 0.8.22 --wheel-label released-0.8.22 --observe-baseline-first-erase 2>&1 | Tee-Object -FilePath $artifact -Append
          $releasedSerialExit = $LASTEXITCODE
          $releasedTypedObservations = @(Select-String -Path $artifact -Pattern 'slice65_wal BASELINE_FIRST_ERASE outcome=typed_erasure_incomplete type=ErasureIncompleteError stage=wal_checkpoint wal_frames=(?<frames>\d+)')
          $releasedCleanObservations = @(Select-String -Path $artifact -SimpleMatch 'slice65_wal BASELINE_FIRST_ERASE outcome=clean_completion type=none stage=none wal_frames=0')
          if ($releasedSerialExit -eq 65 -and $releasedTypedObservations.Count -eq 1 -and $releasedCleanObservations.Count -eq 0) {
            $releasedFrames = $releasedTypedObservations[0].Matches[0].Groups['frames'].Value
            "slice65_wal OBSERVED_EXPECTED_ERASURE_INCOMPLETE wheel_version=0.8.22 wheel_selector=released-0.8.22 exit=$releasedSerialExit type=ErasureIncompleteError stage=wal_checkpoint frames=$releasedFrames" | Tee-Object -FilePath $artifact -Append
          } elseif ($releasedSerialExit -eq 66 -and $releasedCleanObservations.Count -eq 1 -and $releasedTypedObservations.Count -eq 0) {
            "slice65_wal OBSERVED_CLEAN_SERIAL_COMPLETION wheel_version=0.8.22 wheel_selector=released-0.8.22 exit=$releasedSerialExit type=none stage=none frames=0" | Tee-Object -FilePath $artifact -Append
          } else {
            Write-Error "released serial baseline did not produce an accepted first-erase observation: exit=$releasedSerialExit typed=$($releasedTypedObservations.Count) clean=$($releasedCleanObservations.Count)"
            exit 1
          }
          python -m venv $currentEnv
          $currentWheelCandidates = @(Get-ChildItem -Path "dist-slice65-test" -Filter "*.whl" -File)
          if ($currentWheelCandidates.Count -ne 1) {
            Write-Error "expected exactly one disposable current test-hooks wheel, found $($currentWheelCandidates.Count)"
            exit 1
          }
          $currentWheel = (Resolve-Path -LiteralPath $currentWheelCandidates[0].FullName).Path
          "slice65_wal current_wheel_path=$currentWheel" | Tee-Object -FilePath $artifact -Append
          "slice65_wal current_wheel_sha256=$((Get-FileHash -LiteralPath $currentWheel -Algorithm SHA256).Hash)" | Tee-Object -FilePath $artifact -Append
          & "$currentEnv\Scripts\python.exe" -m pip install --no-index --no-deps $currentWheel 2>&1 | Tee-Object -FilePath $artifact -Append
          & "$currentEnv\Scripts\python.exe" src/python/tests/test_slice65_wal_attribution_installed.py --control serial --wheel-version 0.8.22 --wheel-label current-source-test-hooks --require-attribution 2>&1 | Tee-Object -FilePath $artifact -Append
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & "$currentEnv\Scripts\python.exe" src/python/tests/test_slice65_wal_attribution_installed.py --control binding --wheel-version 0.8.22 --wheel-label current-source-test-hooks --require-attribution 2>&1 | Tee-Object -FilePath $artifact -Append | Tee-Object -FilePath $bindingLog
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & "$currentEnv\Scripts\python.exe" src/python/tests/test_slice65_wal_attribution_installed.py --control retained --wheel-version 0.8.22 --wheel-label current-source-test-hooks --require-attribution 2>&1 | Tee-Object -FilePath $artifact -Append
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $bindingBusy = @(Select-String -Path $bindingLog -Pattern 'slice65_wal checkpoint_attempt=.*busy=1.*classification=owned_reader_snapshot.*active_roles=reader_worker:0').Count
          if ($bindingBusy -ne 5) {
            Write-Error "expected five installed-binding owned reader busy records, got $bindingBusy"
            exit 1
          }
          if (-not (Select-String -Path $bindingLog -Pattern 'slice65_wal checkpoint_attempt=.*busy=0.*classification=no_owned_snapshot.*active_roles=$' -Quiet)) {
            Write-Error "installed binding control did not retain the post-release no-owned checkpoint"
            exit 1
          }
          if (-not (Select-String -Path $artifact -SimpleMatch 'slice65_wal serial_current_attribution_expected=1' -Quiet)) {
            Write-Error "current serial wheel did not emit the attribution selector"
            exit 1
          }
          if (-not (Select-String -Path $artifact -SimpleMatch 'slice65_wal serial_idle_after_read_get=passed' -Quiet) -or -not (Select-String -Path $artifact -SimpleMatch 'slice65_wal serial_idle_after_neighbors=passed' -Quiet)) {
            Write-Error "current serial wheel did not prove immediate idle state after both recovery reads"
            exit 1
          }
          if (-not (Select-String -Path $artifact -SimpleMatch 'slice65_wal serial_wheel_selector=released-0.8.22' -Quiet) -or -not (Select-String -Path $artifact -SimpleMatch 'slice65_wal serial_wheel_selector=current-source-test-hooks' -Quiet)) {
            Write-Error "installed-wheel serial comparison did not retain both released and current selectors"
            exit 1
          }
          if (-not (Select-String -Path $artifact -SimpleMatch 'slice65_wal python_binding_snapshot_released' -Quiet)) {
            Write-Error "installed binding control did not reach its post-release checkpoint"
            exit 1
          }
          if (-not (Select-String -Path $artifact -SimpleMatch 'slice65_wal python_retained_materialized_idle=passed' -Quiet)) {
            Write-Error "installed retained-result control did not execute"
            exit 1
          }
          Write-Output "slice65_wal installed_wheel_controls=passed" | Tee-Object -FilePath $artifact -Append
      - name: Upload WAL ownership attribution diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: slice65-windows-wal-attribution-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/slice65-windows-wal-attribution.log
          if-no-files-found: error

   # EU-6 FIX-1 / ADR-0.6.0-default-embedder.md EMB-3 — wheel-size gate.
  #
  # Per `dev/design/0.7.1-EU-6-FIX-1-design.md` D2:
  #   * The SHIPPED wheel (with `default-embedder` feature ON, the
  #     surface a `pip install fathomdb` user actually consumes) is the
  #     GATED artifact. Each platform has its own `baseline_bytes`;
  #     regression > 20 MB blocks. This is the canary that matters.
  #   * The NO-FEATURE wheel is built informationally per matrix entry
  #     and recorded in the job summary so accidental dependency creep
  #     on the minimal-deps build path is still visible — but not gated.
  #
  # 0.8.22 validates the shipped Python wheel on Linux glibc x64/ARM64,
  # macOS x64/ARM64, and Windows x64. Musl and other targets remain unsupported.
  wheel-size-gate:
    needs: changes
    if: needs.changes.outputs.docs_only != 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            label: linux-x64
            manylinux: "2_28"
            # Measured on FIX-1 GREEN commit (Linux x86_64, abi3 cp310):
            # no-feature ≈ 3.06 MB, with-feature ≈ 6.16 MB. Baseline
            # = with-feature + ~20% headroom so Cargo.lock churn does
            # not whip the gate; the 20 MB regression budget on top of
            # this absorbs intentional dependency bumps.
            baseline_bytes: 7400000
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            label: linux-arm64
            manylinux: "2_28"
            baseline_bytes: 7400000
          - runner: macos-15-intel
            target: x86_64-apple-darwin
            label: darwin-x64
            manylinux: "auto"
            baseline_bytes: 7400000
          - runner: macos-14
            target: aarch64-apple-darwin
            label: darwin-arm64
            manylinux: "auto"
            baseline_bytes: 7400000
          - runner: windows-latest
            target: x86_64-pc-windows-msvc
            label: win32-x64
            manylinux: "auto"
            baseline_bytes: 7400000
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.11"
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # stable
        with:
          toolchain: "1.95.0"
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      # Use PyO3/maturin-action (the same SHA as release.yml's build-python)
      # so the tested wheel follows the release build path exactly.
      # Build the with-feature wheel first — this is the shipped
      # artifact and the GATED build. `--features` REPLACES the
      # pyproject list, so the explicit list here is what gets built
      # regardless of what pyproject.toml carries.
      - name: Build wheel (with default-embedder feature; shipped artifact)
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: ${{ matrix.target }}
          args: --release --out dist-feature -i python3.11 --features pyo3/extension-module,default-embedder
          manylinux: ${{ matrix.manylinux || 'auto' }}
          working-directory: src/python
      # No-feature wheel: informational only. Recorded to detect
      # accidental dependency creep on the minimal-deps tree.
      - name: Build wheel (no default-embedder feature; informational)
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: ${{ matrix.target }}
          args: --release --out dist-nofeature -i python3.11 --features pyo3/extension-module
          manylinux: ${{ matrix.manylinux || 'auto' }}
          working-directory: src/python
      - name: Assert wheel-size gate
        shell: bash
        working-directory: src/python
        run: |
          set -euo pipefail
          BASELINE=${{ matrix.baseline_bytes }}
          BUDGET=$((20 * 1024 * 1024)) # 20 MB, per EMB-3
          FEAT=$(stat -c%s dist-feature/*.whl 2>/dev/null || stat -f%z dist-feature/*.whl)
          NOFEAT=$(stat -c%s dist-nofeature/*.whl 2>/dev/null || stat -f%z dist-nofeature/*.whl)
          DELTA=$(( FEAT - BASELINE ))
          FEATDELTA=$(( FEAT - NOFEAT ))
          echo "::notice title=wheel-size (${{ matrix.label }})::with-feature=${FEAT}B baseline=${BASELINE}B delta=${DELTA}B; no-feature=${NOFEAT}B (informational)"
          {
            echo "## Wheel-size gate (${{ matrix.label }})"
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| with-feature wheel (shipped, GATED) | ${FEAT} B |"
            echo "| baseline | ${BASELINE} B |"
            echo "| delta vs baseline | ${DELTA} B |"
            echo "| budget (blocking) | ${BUDGET} B |"
            echo "| no-feature wheel | ${NOFEAT} B (informational only) |"
            echo "| with-feature delta vs no-feature | ${FEATDELTA} B (informational only) |"
          } >> "$GITHUB_STEP_SUMMARY"
          if [ "$DELTA" -gt "$BUDGET" ]; then
            echo "::error title=wheel-size regression::with-feature wheel grew ${DELTA} B vs baseline ${BASELINE} B (budget ${BUDGET} B)"
            exit 1
          fi

  # Slice 15 — native artifacts are not proven merely because they build. On
  # every release-ready host, build the shipped Python wheel and the matched
  # N-API platform package, install both from local files only, and exercise
  # open/write/search/close/process-exit. Registry-install smokes intentionally
  # remain the post-publish Slice 20 release gate.
  native-artifact-runtime-validation:
    needs: changes
    if: needs.changes.outputs.docs_only != 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            label: linux-x64-gnu
            manylinux: "2_28"
          - runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            label: linux-arm64-gnu
            manylinux: "2_28"
          - runner: macos-15-intel
            target: x86_64-apple-darwin
            label: darwin-x64
            manylinux: "auto"
          - runner: macos-14
            target: aarch64-apple-darwin
            label: darwin-arm64
            manylinux: "auto"
          - runner: windows-latest
            target: x86_64-pc-windows-msvc
            label: win32-x64-msvc
            manylinux: "auto"
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.11"
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "25.9.0"
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # stable
        with:
          toolchain: "1.95.0"
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - name: Build local Python wheel
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: ${{ matrix.target }}
          args: --release --out dist --features pyo3/extension-module,default-embedder -i python3.11
          manylinux: ${{ matrix.manylinux }}
          working-directory: src/python
      - name: Build local N-API artifact and TypeScript package
        working-directory: src/ts
        env:
          CARGO_BUILD_TARGET: ${{ matrix.target }}
        run: |
          npm ci
          npm run build:native
          npm exec -- tsc -p tsconfig.build.json
      - name: Validate local wheel and N-API package
        if: matrix.runner != 'windows-latest'
        shell: bash
        run: |
          bash scripts/release/smoke/smoke-local-native-artifacts.sh \
            "$PWD/src/python/dist" \
            "$PWD/src/ts" \
            "$PWD/src/ts/npm/${{ matrix.label }}" \
            "${{ matrix.label }}"
      - name: Validate local wheel and N-API package (Windows)
        if: matrix.runner == 'windows-latest'
        shell: pwsh
        run: ./scripts/release/smoke/smoke-local-native-artifacts.ps1 -WheelDirectory "$PWD/src/python/dist" -TsDirectory "$PWD/src/ts" -PlatformPackageDirectory "$PWD/src/ts/npm/${{ matrix.label }}" -NapiLabel "${{ matrix.label }}"

  # Docs-only fast-path md gate. Runs ONLY when the diff is markdown-only (the
  # heavy `verify` job — which runs scripts/agent-verify.sh -> agent-lint-md.sh —
  # is the md gate on every non-docs diff, so this avoids double-linting). Uses
  # the SAME .markdownlint-cli2.jsonc config as local + CI, so the real markdown
  # gate is preserved on the fast path (this is NOT a silent `paths-ignore` drop).
  markdownlint:
    needs: changes
    if: needs.changes.outputs.docs_only == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "25.9.0"
      - name: Install markdown tooling (markdownlint-cli2)
        run: npm ci
      - name: markdownlint-cli2 (repo markdown; same config as local + verify)
        run: ./node_modules/.bin/markdownlint-cli2
      - name: markdownlint docs/** (mirrors agent-lint-md.sh docs leg)
        run: bash scripts/agent-lint-docs.sh
      - name: dev/plans/*.md status frontmatter (T3/8+9, mirrors agent-lint-md.sh leg)
        run: bash scripts/lint-plans-status.sh
      - name: master §6 findings register — duplicate-id gate (T1c, mirrors agent-lint-md.sh leg)
        run: bash scripts/lint-findings.sh
      # NOTE: agent-lint-md.sh's `lint-plan-anchors` leg is deliberately NOT
      # mirrored here. This job is `if: docs_only == 'true'`, and the event that
      # breaks a symbol citation is a CODE push (a rename), which this job never
      # sees. It has its own always-on `plan-anchors` job below instead.

  # status-board-currency-enforcement item 3 — the non-bypassable CI backstop.
  # `preflight.sh --landing` (item 2) is the PREVENT gate at land time, but it is
  # a local, invocable check; this job is the DETECT gate that catches anything
  # that slipped preflight or predates it (the exact 4-day drift the incident
  # exhibited: a board that kept saying "not landed" after the merge commit was
  # already on origin/main). Reuses scripts/check-board-currency.sh — the SAME
  # script preflight.sh calls — so the two homes cannot diverge (see that
  # script's header for the full predicate). Intentionally NOT gated on
  # docs_only: a board can drift behind a non-docs-only push just as easily.
  # fetch-depth 0 is required: the predicate walks full reachable git history
  # for `merge(0.8.z): Slice N` commits, which a shallow (depth-1) checkout
  # would hide entirely.
  board-currency:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Board/git-ancestry drift check (status-board-currency-enforcement item 3)
        run: bash scripts/check-board-currency.sh --tip HEAD

  # DOC-HYGIENE-2 T1b — the always-on ledger-integrity backstop.
  # `preflight.sh --landing` is the PREVENT gate at land time; this job is the
  # DETECT gate for whatever slips it. Reuses scripts/check-ledgers.sh — the
  # SAME script preflight calls — so the two homes cannot diverge (see that
  # script's header for the predicate: sidecar == max(seq), and seq contiguous).
  #
  # Deliberately carries NO `if:` condition. A docs_only-gated job never fires on
  # a code push, and the measured incident (steward-ledger.jsonl.seq frozen at 80
  # against max(seq) 98 for 19 commits / four days) spanned both docs and code
  # pushes; the sidecar is tracked, so any clone taken in that window would have
  # minted colliding seq numbers. Default (depth-1) checkout is sufficient: the
  # predicate reads working-tree files only and walks no history.
  ledger-integrity:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Ledger sidecar/seq integrity (DOC-HYGIENE-2 T1b)
        run: bash scripts/check-ledgers.sh

  # DOC-HYGIENE-2 T1d — the always-on line-anchor / citation-integrity backstop.
  #
  # Deliberately carries NO `if:` and NO `needs:`. This is the whole point of the
  # job, not boilerplate. The `markdownlint` job above is gated on
  # `needs.changes.outputs.docs_only == 'true'`, so it fires only when a push
  # touches docs and nothing else — but the event that BREAKS a symbol citation
  # in an ACTIVE plan is a rename in `src/`, i.e. a code push, which docs_only
  # excludes by construction. A citation gate that cannot run on the push that
  # invalidates the citation is decorative. Same reasoning that put
  # `board-currency` and `ledger-integrity` outside the docs_only fast path.
  #
  # Default (depth-1) checkout is sufficient: the predicate reads working-tree
  # files only and walks no history.
  plan-anchors:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: ACTIVE-plan line-anchor ban + symbol-existence check (DOC-HYGIENE-2 T1d)
        run: bash scripts/lint-plan-anchors.sh
      - name: Recurrence guard for the gate itself (fixture arms)
        run: bash scripts/tests/test_lint_plan_anchors.sh

  # DOC-HYGIENE-2 T1e — the always-on governed-surface pin backstop.
  # `preflight.sh --landing` is the PREVENT gate at land time; this job is the
  # DETECT gate for whatever slips it. Reuses scripts/check-governed-surface-pin.sh
  # — the SAME script preflight calls — so the two homes cannot diverge (see that
  # script's header for the predicate: content hash + member lists + counts +
  # REQ-054, against scripts/governed-surface-pin.json).
  #
  # Deliberately carries NO `if:` condition and is NOT on the docs_only fast path:
  # a governed-surface change is BY DEFINITION a code change, so a docs_only gate
  # would make this job silently absent on exactly the pushes it exists to catch.
  # Default (depth-1) checkout is sufficient: the predicate reads working-tree
  # files only and walks no history.
  governed-surface-pin:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Governed-surface pin vs the AC-079 pre-sign (DOC-HYGIENE-2 T1e)
        run: bash scripts/check-governed-surface-pin.sh

  # 0.8.21 Slice 40 — dependency override rot is a fast static failure. It has
  # no `needs:` and no docs-only condition: a package manifest or advisory
  # snapshot can change on any push, and its checked-in evidence is deliberately
  # offline so an unavailable network cannot manufacture a green verdict.
  pinned-override-rot:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Dependency overrides remain justified and non-vulnerable
        run: bash scripts/check-pinned-override-rot.sh
      - name: Recurrence guard for vulnerable, obsolete, and unrationalized pins
        run: bash scripts/tests/test_check_pinned_override_rot.sh

  # R-20-H7 — the always-on RUBRIC-H7 `can-i-deploy` contract-conformance
  # backstop. `preflight.sh --landing` is the PREVENT gate at land time; this job
  # is the DETECT gate for whatever slips it. Reuses
  # scripts/check-c1-conformance.sh — the SAME script preflight calls — so the
  # two homes cannot diverge (see that script's header for the predicate, for the
  # five TC-37 evaporation paths, and for the explicit statement of what this
  # STATIC STRUCTURAL check does NOT prove).
  #
  # Deliberately carries NO `if:` condition and NO `needs:`, and is NOT on the
  # docs_only fast path. That combination is the whole point of the job. The
  # subject here straddles both fast paths: the ratified C-1 contract is a DESIGN
  # DOC (so a code-only push would skip a docs_only-gated job) while the code it
  # governs is SOURCE (so a docs-only push takes the fast path that skips the
  # heavy verify job). Either gate would make this job absent on exactly the
  # pushes that matter — a rename in src/ that breaks a contract obligation, or
  # an edit to the contract that moves it out from under its clause registry.
  #
  # An absent-or-failing gate HOLDS the 0.8.20 ↔ Memex breaking pair
  # (plan-0.8.20.md §3), so this job is a publish precondition, not decoration.
  # Default (depth-1) checkout is sufficient: the predicate reads working-tree
  # files only and walks no history.
  c1-contract-conformance:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: As-built code still satisfies the ratified OPP-12 C-1 contract (R-20-H7)
        run: bash scripts/check-c1-conformance.sh
      - name: Recurrence guard for the gate itself (fixture arms)
        run: bash scripts/tests/test_check_c1_conformance.sh

  # TC-86 — transcript hygiene. No tracked file in this PUBLIC repository may
  # carry a home-anchored path into a user's Claude Code state directory, because
  # such a line is raw agent SESSION TRANSCRIPT content.
  #
  # A codex §9 transcript arrived carrying 216 lines of raw session JSONL from
  # three OTHER projects: codex runs under
  # --dangerously-bypass-approvals-and-sandbox (which is what lets it read
  # outside the repo), it `rg`'d across ~/.claude, and TC-RUBRIC-7 then required
  # its stdout be persisted under a tracked path. Caught and redacted before
  # landing; reachability in history is ZERO.
  #
  # Deliberately carries NO `if:` and NO `needs:`, for the same reason as
  # c1-contract-conformance / board-currency / ledger-integrity above — and for
  # one more that is specific to this gate: CI IS ITS LOAD-BEARING HOME. A
  # pre-commit hook is bypassable with `--no-verify`, and a `preflight --landing`
  # gate only fires at a land, while a transcript is committed on an ordinary
  # push in the middle of a review loop. A transcript is a `.log` under
  # dev/plans/runs/**, i.e. a NON-markdown path, so `docs_only` would not have
  # excluded it — but making this job's existence contingent on another job is
  # exactly what "always-on" forbids.
  #
  # Default (depth-1) checkout is sufficient: the predicate reads working-tree
  # files only and walks no history.
  transcript-hygiene:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: No tracked file carries a Claude Code agent-state path (TC-86)
        run: bash scripts/check-transcript-hygiene.sh
      - name: Recurrence guard for the gate itself (fixture arms)
        run: bash scripts/tests/test_check_transcript_hygiene.sh

  # DOC-HYGIENE-2 T2a — the always-on single-writer release-state backstop.
  #
  # Deliberately carries NO `if:` and NO `needs:`, for the same reason as
  # board-currency / ledger-integrity / plan-anchors above. The push that
  # invalidates a generated release-state view is a LANDING push (a slice merges,
  # SCHEMA moves, the next slice changes) — i.e. a code push, which the
  # `markdownlint` job's `docs_only` gate excludes by construction. A currency
  # gate that cannot run on the push that makes the fact wrong is decorative.
  #
  # Full history is required: generated views claim slices are LANDED on
  # `origin/main`, and the gate verifies each claim by Git ancestry.
  release-state-views:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          # The generated views assert slices are "LANDED on `origin/main`", and
          # the gate verifies that with `merge-base --is-ancestor`. The default
          # `--depth=1` clone carries only the tip, so every historical landed
          # SHA is absent from the object store and the claim is UNVERIFIABLE —
          # the gate then correctly declines to check it and this job would vouch
          # for nothing. Full history is what makes the assertion real here.
          fetch-depth: 0
      - name: Fetch the scoped 0.8.23 release completion ref
        # `completion.ref` is deliberately an exact, release-scoped remote ref.
        # Checkout's refspec guarantees origin/main but does not guarantee this
        # sibling branch exists as a remote-tracking ref in every event shape.
        run: git fetch --no-tags origin +refs/heads/release/0.8.23:refs/remotes/origin/release/0.8.23
      - name: Release-state generated views are current (DOC-HYGIENE-2 T2a)
        run: bash scripts/check-release-state-views.sh
      - name: Recurrence guard for the gate itself (fixture arms)
        run: bash scripts/tests/test_check_release_state_views.sh

  # DOC-HYGIENE-2 T3b — the generated commission manifest. ALWAYS-ON (no `if:`,
  # no `needs:`) on purpose, even though the generator is an operator tool rather
  # than a repo invariant: what it guards is CITATION ROT, and a citation rots on
  # exactly the pushes the docs_only fast path excludes — a rename or a moved
  # design doc landing alongside code. `--verify-all` re-verifies the NEXT slice
  # of every live release-state file, so this job needs no release number of its
  # own and cannot go stale when the ladder advances. The recurrence-guard
  # fixture reads generator history, so this job checks out the full history.
  commission-manifest:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Every live release's next-slice manifest still resolves (T3b)
        run: bash scripts/commission-manifest.sh --verify-all
      - name: Recurrence guard for the generator itself (fixture arms)
        run: bash scripts/tests/test_commission_manifest.sh

  # DOC-HYGIENE-2 T2c — dev/design/**/*.md must carry a `status:` frontmatter key
  # (and `superseded_by:` when SUPERSEDED). Deliberately its OWN always-on job and
  # NOT a step in the `markdownlint` job above: that job is
  # `if: needs.changes.outputs.docs_only == 'true'`, so it never fires on a code
  # push — a design doc landing alongside code would slip the gate entirely. Same
  # reasoning as `board-currency` above (a doc tier can drift behind a non-docs-only
  # push just as easily). `verify` also runs it via agent-lint-md.sh; this job is
  # the backstop that does not depend on the verify matrix. No fetch-depth needed:
  # the check reads the working tree only.
  design-status:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: dev/design/**/*.md status frontmatter (T2c)
        run: bash scripts/lint-design-status.sh
      - name: Recurrence guard for the gate itself
        run: bash scripts/tests/test_design_status_frontmatter.sh

  # DOC-HYGIENE-2 T3a — the stateless Steward cold-start briefing.
  #
  # Deliberately carries NO `if:` and NO `needs:`. The briefing reads DOCS (the
  # live board, the release-state file, both ledgers, the hand-offs), and a
  # docs-only push takes the `docs_only` fast path that skips the heavy `verify`
  # job — i.e. the only job that would otherwise run this suite via agent-test.sh.
  # A guard that cannot run on the push that breaks it is decorative, so this
  # runs on every push. Cheap: shell + python3 stdlib, no build.
  #
  # `gh` is unauthenticated here, so the briefing's open-PR line degrades to its
  # documented `unavailable (...)` marker — an exempt section by design, which is
  # exactly why the exemption is written down in the script header.
  steward-orient:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
      - name: Steward cold-start briefing + its fixture arms (DOC-HYGIENE-2 T3a)
        run: bash scripts/tests/test_steward_orient.sh

  # mkdocs --strict is the broader CI gate beyond agent-verify (per AGENTS.md §3).
  # Intentionally NOT gated on docs_only: it must run for docs/** changes (the
  # fast path) AND alongside code changes, exactly as before.
  docs:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
      - run: python3 -m pip install --upgrade pip mkdocs
      - run: mkdocs build --strict
