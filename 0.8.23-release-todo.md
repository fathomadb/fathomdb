<!-- markdownlint-disable MD041 MD029 -->

Scoped 0.8.23 work is locally implemented and verified through the current
release-candidate PR, but **0.8.23 is neither completed nor published**.
Completion requires the remaining main-control-plane integration and the
candidate-bound external evidence below. No P0/P1 local product or lint defect
is currently open.

1. Finish the release-candidate integration.

- Merge the reviewed, fully green candidate PR into `release/0.8.23` and
  update the single-writer release state from Git.
- Keep `origin/main` integration explicitly `PENDING` until its reviewed
  control-plane and release integration PRs land. Do not call the release
  completed or published before then.
- Preserve source/evidence identity: a benchmark worktree based on a different
  release ancestry is not a source of 0.8.23 evidence. Use only an explicitly
  reviewed integration or a release-pinned external executor; never reuse
  cross-release receipts.

2. Prepare the GitHub dry-run correctly.

The branch is still Axis-W **0.8.22**, so a `0.8.23` dry-run cannot pass yet. After local defects are fixed:

- Make the intentional `0.8.23` version/changelog/Axis-W update on the candidate PR.
- Bring the reviewed Slice 10 control-plane changes to `main`; the dry-run is explicitly main-workflow-owned.
- Refresh the expired protection/runner/environment attestation.
- Open and approve the exact candidate PR to `main`.
- Merge a separate reviewed provenance-only main PR that authorizes exactly that candidate SHA in `cuda-unmerged-candidates.json`.
- Confirm an X64 runner with labels `[self-hosted, Linux, X64, gpu, cuda-12]`, protected `cuda-unmerged-preflight` environment, pinned model caches, and CUDA toolchain. The Jetson evidence is valuable AArch64 coverage, but it does not substitute for this Linux-x64 package-rehearsal route.
- Dispatch `release.yml` from `main` with `dry_run=true`, `release_version=0.8.23`, and the exact candidate SHA.

That run should produce and retain: Slice 10 preflight/witness, Slice 20 package
rehearsal, Slice 70 diagnostic evidence, and Slice 71 reranker-v3 evidence.
Independently review those artifacts before changing any release-state blocker.

3. Close the remaining external release blockers.

- Slice 72 is complete, including real local CUDA coexistence evidence.
- Slice 80.7 is complete, including hosted Jetson evidence.
- Slice 80.6.5 is approved, implemented, and has a retained pinned RTX 3090
  preflight witness; reconcile it to `LANDED` only after the candidate reaches
  `origin/release/0.8.23`.
- Slice 40 is out of scope and recorded in the repository backlog.
- Slice 10 remains blocked on the reviewed main control plane, approved
  candidate provenance, protected-runner attestation, and the GitHub dry-run
  receipt.
- Slice 20 remains blocked on one candidate-bound, non-publishing package
  rehearsal bundle and independent evidence review.
- Slice 70 remains blocked on its retained trusted-runner CUDA witness and
  installed-artifact rehearsal evidence. Its private TC-5 executor is not a
  substitute for product or cross-release evidence.
- Slice 71 remains blocked on compatible-GPU and visible-but-incompatible
  installed-artifact reranker receipts, independently reviewed against v3.
- After dry-run evidence passes review, update release-state/status, merge `release/0.8.23` to `main`, rerun the full main gate, and only then consider a separately authorized tag/publication. Publication remains held and is not implied by a dry-run.

<!-- markdownlint-enable MD041 MD029 -->

## Merge to `main` and push to `origin/main` constraints

- Use a fresh, clean linked worktree and current `origin/main`; rebase or merge
  `origin/main` deliberately, resolve conflicts from the source of truth, and
  never force-push, delete, or rewrite `main`.
- Preserve `main`'s deletion of the stale Tegra design file while bringing the
  Jetson workflow route forward. The file is required later when the completed
  release branch itself is merged, not as a reason to resurrect stale content
  in the control-plane PR.
- The active repository ruleset requires a pull request, forbids non-fast-forward
  updates and deletion, and requires CodeQL with no error-level alerts or
  high-or-higher security alerts. A classic branch-protection endpoint is absent;
  this active ruleset is the authoritative GitHub constraint.
- The ruleset currently requires zero formal approvals, but release policy still
  requires independent code/evidence review, resolved material review findings,
  and a clean, candidate-bound provenance trail before merge.
- Before a merge PR is marked ready, pass the full local release gate from the
  candidate bytes: `cargo fmt --check`; `cargo clippy --workspace --all-targets
  -- -D warnings -A missing-docs`; `cargo check --workspace --all-targets`;
  `scripts/agent-verify.sh --tier=all`; `scripts/check.sh`; Actionlint; release
  state/view, board-currency, public-doc-truth, security/Gitleaks, shell, and
  unconfined strace gates. Resolve red results; do not use `--no-verify` or
  waive a baseline merely because it predates this branch.
- The dry-run control plane must be merged to `main` through its own reviewed
  PR before dispatch. Its manifest/baseline/provenance entries must authorize
  exactly one reviewed, open candidate PR and expire safely; dispatch must use
  `release.yml` from `main` and the exact candidate SHA.
- Do not push a `v*` tag, dispatch a non-dry release, publish, or use release
  credentials. A successful dry-run is evidence only and is not authorization
  for publication.
- After the reviewed PR merge, verify the merged SHA and all required GitHub
  checks on `origin/main`, update the single-writer release state and generated
  views to `main_integration: COMPLETE`, and push only the reviewed fast-forward
  branch reference with explicit HITL authorization.

## Approved placement changes

- HITL `seq:253` approves and unblocks Slice 80.6.5. Its x86_64 scope is now
  part of the 0.8.23 completion path, subject to its separate hardware evidence
  and ordinary review gates.
- HITL `seq:254` moves Slice 40 out of 0.8.23. Its preserved V2 runner and
  validator design is recorded in the repository backlog as
  `TC-8ccd1cf1-8e7d-4b7c-8efa-176663aed553` (`seq:236`), and it is no longer a
  0.8.23 completion requirement.

## Reverification and completed local work

- The Slice 70 CPU `doctor gpu` side-effect allegation did not reproduce in
  fresh unconfined strict-strace runs at the release tip or the reported
  baseline: the CPU child accessed none of the HOME/XDG/HF/model/database
  canaries and the full CLI library suite passed. It is not an active P1.
- Local RED/GREEN fixes now cover README workspace-count truth, Python 3.10
  `timezone.utc` compatibility, all seven `embed-cuda` clippy findings, the
  IR-C CUDA type mismatch, Tegra wheel-discovery SC2312 masking, Slice 10's
  unmerged dry-run provenance contradiction, and Slice 71's stale
  control-plane-checkout expectation.
- Slice 80.6.5's local shared-witness merge and optional `GPU-` UUID
  normalization are complete. HITL ruled the former transfer scripts
  unnecessary because this session is already local on windchill3; the real
  pinned windchill3 RTX 3090 preflight passed and is retained. It does not
  replace the remaining candidate-bound GitHub dry-run evidence.
