---
status: UNREVIEWED
---

# 0.8.16 Slice-0 design package — F9 importance/confidence ranking + cross-vendor ONNX embedder

> **Status: SIGNED — HITL coreyt ratified all 5 Slice-0 decisions 2026-07-08 (via the Program Steward).**
> Authored by the 0.8.16 Release Orchestrator at Slice 0. Design/ADR authoring only; NO code, NO
> commit, NO migration until the Slice-0 gate is signed. Live tip at authoring = `36024585`.
> Plan of record: `dev/plans/plan-0.8.16.md`. ADRs: `dev/adr/ADR-0.8.16-f9-importance-confidence-ranking.md`,
> `dev/adr/ADR-0.8.16-onnx-embedder-backend.md`. Board: `dev/plans/runs/STATUS-0.8.16.md`.

## 0. Scope of this package

This is the Slice-0 deliverable for release **0.8.16 "Ranking signal & embedder reach"**: it (a) freezes
the release DoD, (b) records the two design decisions (F9 ranking/lifecycle; trait-local ONNX backend),
(c) maps F9 onto the OPP-12 `rankable` projection-role contract (the F-18/F-20 load-bearing constraint),
(d) specifies the candle↔ONNX equivalence-measurement plan that feeds the 0.8.18 #5 tolerance, and (e)
lists the new AC candidates for the HITL to mint at the Slice-0 gate. Implementation slices (5/10/15/40)
do NOT begin until this package is signed.

## 1. Verified baseline (from git @ `36024585`, not narration)

- **`canonical_edges.confidence REAL` already exists** — added by `fathomdb-schema/src/lib.rs` migration
  `step_id: 14` (G11 Slice-15). Nullable, **genuine SQL NULL** (no sentinel), populated by BYO-LLM ingest,
  validated to `[0.0, 1.0]` in `fathomdb-engine/src/lib.rs`.
- **No `importance` column exists anywhere** (grep: only doc-comment matches). **No `confidence` on
  `canonical_nodes`.** So F9 at 0.8.16 = *add the node-level importance column + integrate the existing
  edge confidence into ranking* — not re-add confidence.
- **`SCHEMA_VERSION = 17`** (`fathomdb-schema/src/lib.rs`). F9's importance column is a forward-only
  migration → **step_id 18, SCHEMA_VERSION 17→18** (engine/schema migration ⇒ HITL-gated).
- **The explain surface F9 plugs into: `struct PerHitExplain`** (`fathomdb-engine/src/lib.rs:1268`),
  `#[non_exhaustive]`, ratified in 0.8.8 EXP-OBS with the explicit note "leaf absorbs future arms / score
  components" — i.e. designed to grow to hold F9's contribution. Fields today: `arm, vector_rank,
  text_rank, graph_rank, fused_score, ce_score, blended`.
- **Ranking machinery:** `recency_reweight_enabled: AtomicBool` + `apply_recency_reweight` (G12-recency,
  OFF by default) is the precedent for an opt-in reweight; RRF fusion `fuse_rrf`/`rerank_fused`/
  `fuse_three_arms`; graph arm gated `use_graph_arm`; CE blend `ce_weight`/`pool_n`.
- **Embedder trait seam (originally zero-engine-change):** the `Embedder` trait lives in the independently
  semver'd `fathomdb-embedder-api` crate (`embed(&str)->Result<Vector,EmbedderError>`, `embed_batch`,
  `identity()`; `Vector = Vec<f32>`). The original injection used
  `EmbedderChoice::Caller(Arc<dyn Embedder>)`
  (`fathomdb-engine/src/lib.rs:2044`) plugged via `Engine::open_with_choice` (`:2443`/`:2449`) is the ONNX
  injection point — the engine never names a concrete embedder on that path. Device grammar parser
  `parse_device_request` (`fathomdb-embedder/src/device.rs`) is backend-agnostic and reusable; `resolve_device()`
  (`candle_bge.rs:121`, reads `FATHOMDB_EMBED_DEVICE`) is candle-specific and gets an ONNX sibling.
  Slice 70 makes a report-bearing ONNX caller use
  `CallerWithDeviceResolution` so the final session outcome is captured in
  `OpenReport`; the engine still does not name a concrete ONNX type.
  `fathomdb-embedder/Cargo.toml:64-68` already anticipates the ONNX path in comments; every ML dep is
  `optional` + feature-gated (thin `default = []` protected by the EMB-3 wheel-size gate).

## 2. Release DoD — FROZEN at Slice 0 (pre-registered)

Carried verbatim from `plan-0.8.16.md §2`; frozen here as the acceptance contract:

| ID | Requirement | Acceptance signal |
|----|-------------|-------------------|
| R-F9-1 | Importance column (REAL) + 3-way sentinel + edge confidence | step-18 migration lands; write/read round-trips importance + confidence; sentinel semantics tested |
| R-F9-2 | Importance/confidence influences ranking and is **observable** | a weighted query reorders vs unweighted on a fixture; `explain=True` (0.8.8 `PerHitExplain`) shows the importance/confidence contribution |
| R-F9-3 | Slice-35 ADR gate honored | the deferred-F9 ADR decision is satisfied; **no scope beyond it** (see ADR §5 supersession) |
| R-F9-4 (candidate) | F9 is `rankable`-forward-compatible (OPP-12 Q6a) | design-conformance mapping (§4) holds; absent-importance ranks identically to unweighted; no F9 field on the OPP-12 break-if-later list |
| R-ONNX-1 | `OrtBgeEmbedder` produces BGE-small vectors via the trait | fixture text → vector within documented Δ of candle CPU reference; a report-bearing caller uses `CallerWithDeviceResolution`, whose final session outcome is recorded once on `OpenReport` |
| R-ONNX-2 | Backend selected at `Engine::open` via config/env, not compile-only | `FATHOMDB_EMBED_DEVICE`/config selects ONNX; default build unaffected |
| R-ONNX-3 | Numeric-divergence documented, not yet enforced | candle↔ONNX Δ measured + recorded (§5); same-backend build-and-read discipline documented as the interim guard until 0.8.18 #5 |
| R-X-1 | Py + TS SDK parity for the F9 surface | X1 cross-binding harness green |
| R-GATE | eu7 ≥ 0.90 (one-sided CI) on any embedder/index change | `recall_gate.rs` PASS; breach BLOCKS→HITL. **Default paths are no-op** (F9 does not rewrite vectors; ONNX is behind the trait, default candle CPU unchanged) |

## 3. F9 design decision (detail in ADR-0.8.16-f9-...)

- **Importance column:** `canonical_nodes.importance REAL` (step-18). **3-way sentinel** (recommended,
  pending HITL ratification):
  1. **`NULL` = never assigned (graceful-absent)** → ranking treats the node as neutral; contribution ==
     the unweighted baseline. *This is the OPP-12 graceful-absent state — load-bearing for R-F9-4.*
  2. **`0.0` = explicitly de-weighted** → ranking applies the floor.
  3. **`(0.0, 1.0]` = explicit importance** → ranking scales the fused contribution.
  Symmetric with the existing genuine-NULL `canonical_edges.confidence` (NULL = neutral, defaults 1.0).
- **Ranking integration:** a new **opt-in, OFF-by-default** reweight (mirroring `recency_reweight_enabled`)
  — importance scales the node's fused RRF contribution; edge confidence scales the graph-arm contribution
  per the deferred ADR §4.1 sketch `graph_rrf_score(edge) = confidence × 1/(K + bfs_rank)`. **The exact
  weighting formula is an HITL sign-off item** (deferred-ADR §5 gate 3).
- **Observability (R-F9-2):** extend `PerHitExplain` (`#[non_exhaustive]`, so additive) with the
  importance/confidence contribution; `explain=True` surfaces it. This observability is *why 0.8.16 is the
  right slot* — the signal is only useful if a caller can see it act.
- **Ships OFF-by-default as a correct, observable, forward-compatible MECHANISM** — 0.8.16 does NOT claim
  an eval-quality win (that tuning stays gated on the deferred-ADR eval evidence; see ADR §5).

## 4. F9 → OPP-12 `rankable` mapping (the F-18/F-20 load-bearing constraint)

Per `OPP-12-C1-converged-contract.md` Q6a, F9's importance/confidence signal algebra **is** the ranking
mechanism the 0.8.20 projection registry's `rankable` role will graft. F9 must be shaped so 0.8.20's
idempotent `configure_projections` adopts it **without reshape**. Each contract clause → F9 mechanism:

| OPP-12 Q6a `rankable` clause | F9 (0.8.16) mechanism that makes it forward-compatible |
|---|---|
| "rankable = graceful-absent, **never blocking**" | `NULL` importance = neutral; never errors, never blocks a query (3-way sentinel state 1) |
| "engine **defers** a rankable projection it cannot yet honor — no error, no build" | F9 ranking is OFF-by-default and opt-in; declaring a `rankable` role stays inert until the weight is invoked |
| "grafts on the next idempotent `configure_projections` **once F9 exists**" | importance column + reweight are **add/drop idempotent**; 0.8.20 points a `rankable` role at them with no schema reshape / no data migration |
| "same idempotent build/drop mechanism as Q4" | the signal is a **projection-addressable per-record scalar** (`importance` on nodes, `confidence` on edges), NOT a query-only hardwired flag → a role can address it |
| C-1 "break-if-later fields frozen while the seam is fresh" | F9 schema shape carries **no field on the OPP-12 break-if-later list**; the column is named/typed so per-`IdSpace`/per-projection scoping is possible later (do NOT hardwire a single global ranking) |

**R-F9-4 (candidate AC):** *F9 is `rankable`-forward-compatible per OPP-12 Q6a — (a) importance/confidence
is a projection-addressable per-record scalar; (b) absent = graceful-neutral, never blocking; (c) ranking
integration is opt-in / idempotent add-drop so 0.8.20's `configure_projections` grafts a `rankable` role
without reshape or migration.* Acceptance: this mapping holds under review + a test asserting
absent-importance ranks identically to unweighted + no F9 field on the OPP-12 break-if-later list.

## 5. candle↔ONNX equivalence-measurement plan (R-ONNX-3 → feeds 0.8.18 #5)

**Goal:** measure and record the numeric Δ between the candle CPU reference (`CandleBgeEmbedder`,
BAAI/bge-small-en-v1.5, dim 384, CLS-corrected pooling) and `OrtBgeEmbedder` (ONNX Runtime), so 0.8.18 #5
can calibrate its vector-equivalence tolerance against a real number.

- **Probe set:** a fixed, deterministic, committed text probe set (order-stable), sized to exercise short/
  long/multilingual/edge-token inputs; re-embed on the 3090s (cuda:0/1, exclude K620) when there is GPU room.
- **Metrics (per vector, then distribution):**
  1. cosine similarity + L2 distance + max-abs element Δ (raw float divergence);
  2. **the load-bearing metric — 1-bit sign-quantization FLIP RATE.** The in-library query path is 1-bit
     Hamming; eu7 fidelity is quant-fidelity, so what matters downstream is whether ONNX vs candle produce
     the same sign bits. Record the per-vector and corpus flip rate — *this* is the number 0.8.18 #5 gates on.
- **Backend matrix:** candle-CPU (reference) × {ONNX-CPU (same-backend baseline), ONNX-CUDA, ONNX-ROCm/
  DirectML/OpenVINO as reach-hardware is available}. Report each Δ separately (cross-backend divergence is
  the hazard 0.8.18 exists to catch).
- **Interim guard (documented, not enforced at 0.8.16):** **same-backend build-and-read** — vectors WRITTEN
  by a given embedder backend must be READ/queried with the SAME backend until 0.8.18 #5 enforces a
  tolerance. Never mix candle-written vectors with ONNX-read queries. This mirrors the 0.8.14 eu7 policy
  (`649a8d45`: fidelity gates run CPU same-backend; a cross-backend run is an artifact, not a regression).
- **Output:** a recorded Δ table in the Slice-15 run log + a one-line hand-off to the 0.8.18 #5 tolerance.

## 6. ONNX design decision (detail in ADR-0.8.16-onnx-...)

- New `OrtBgeEmbedder` in `fathomdb-embedder` (`mod ort_bge`, sibling of `candle_bge`/`nomic`), `impl
  Embedder` from `fathomdb-embedder-api`. Its original `Caller` injection was
  zero-engine-diff; Slice 70 makes a report-bearing caller use
  `CallerWithDeviceResolution` so its final ONNX session outcome reaches
  `OpenReport` once without the engine naming an ONNX type.
- New `onnx-embedder` Cargo feature gating optional `ort` (+ tokenizer/loader deps), mirroring the
  `default-embedder` gating so the thin `default = []` build stays ML-free (EMB-3 wheel-size gate).
- Device selection: reuse `parse_device_request` for `FATHOMDB_EMBED_DEVICE` grammar parity; add an
  ONNX-specific `resolve_device()` sibling mapping `DeviceRequest → ORT execution provider` (CUDA / ROCm /
  DirectML / OpenVINO / CPU). Config/env selects at `Engine::open` (R-ONNX-2), not compile-only.
- The `Default` variant stays candle-only (no change to `open_default_embedder`) unless separately wired —
  ONNX is a caller-supplied backend, keeping the footprint invariant intact.

## 7. Slice fan-out (post-sign-off)

Off Slice 0: **F9 track Slice 5** (schema step-18 + ranking + explain) **∥ ONNX track Slice 10 → 15**
(backend, then equivalence measurement). Slice 40 = verification (X1/X2/X3 + R-F9/R-ONNX AC gate + eu7).
Canary the FIRST real launch (one implementer through the full cycle) before any parallelism; max 3
concurrent worktrees, each cut from live `main` + `scripts/preflight.sh --worktree` = pass.

## 8. Open HITL decisions (Slice-0 gate)

1. **Mint the candidate ACs?** R-F9-4 (rankable-forward), the F9 ranking-contract AC (R-F9-1/2 weighting
   formula), and the ONNX-equivalence measurement gate (R-ONNX-1/3). `dev/acceptance.md` is locked; new ACs
   only at gated slices, HITL-decided (plan §6).
2. **F9 weighting formula** (deferred-ADR §5 gate 3): ratify the OFF-by-default importance/confidence
   weighting algebra (multiplicative-on-fused recommended; §3).
3. **3-way sentinel encoding:** ratify `NULL = absent` (recommended, symmetric with edge confidence) vs a
   reserved NOT-NULL sentinel. Load-bearing for R-F9-4 graceful-absent.
4. **Deferred-ADR gate supersession:** confirm that the OPP-12 forward-compat obligation + 0.8.8
   observability is a **sufficient independent trigger** to land F9 as an OFF-by-default MECHANISM at 0.8.16,
   with eval-quality tuning (the original §5 gates 1/2: temporal-gap + ≥100 confidence edges) deferred to a
   later tuning slice. (This is how R-F9-3 "no scope beyond the ADR" is honored.)
5. **F9 schema migration (step-18) authorization** — engine/schema migration is HITL-gated; sign-off needed
   before Slice 5 runs it.
