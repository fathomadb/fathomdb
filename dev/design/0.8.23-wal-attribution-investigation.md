---
status: PROPOSED
target_release: 0.8.23
scope: windows-x64-wal-checkpoint-attribution
---

# Windows WAL checkpoint attribution investigation

## Design-review disposition

Slice 65 is an explicitly commissioned follow-on to Slice 60. It reviewed the
0.8.23 plan and its post-plan work; the completed Slice 30 no-embedder and
erasure repair; the completed Slice 60 Windows witness and retained artifact;
the Slice 3 draft allocation; the downstream Memex evidence note; the engine
checkpoint, erasure, reader-pool, projection-runtime, and Python-binding seams;
and the Python SDK's materialized result shapes.

- **Adjusted and approved:** the completed `N23-WINDOWS-WAL`,
  `R23-WINDOWS-WAL-DIAGNOSIS`, and `AC23-WINDOWS-WAL-DIAGNOSIS` establish the
  fail-closed cross-process witness only. Slice 65 adds the temporary planning
  labels `N23-WAL-ATTRIBUTION`, `R23-WAL-ATTRIBUTION`, and
  `AC23-WAL-ATTRIBUTION` for owner attribution and discriminating controls.
  They are not CRUD changes to the locked needs, requirements, or acceptance
  records.
- **Approved:** private, opt-in attribution instrumentation and the narrow
  real-SQLite Windows experiments below. The record must explain whether a
  known FathomDB reader snapshot, a Python-SDK concurrent request, a serial
  lifecycle, an external reader, or an unclassified checkpoint collision was
  present at the failure.
- **Rejected pending evidence:** changing the five-attempt retry policy,
  exposing a retry parameter, changing binding finalization or adding a
  context-manager API, reader-pool restructuring, public diagnostics, public
  error fields, or an OS/antivirus root-cause assertion.

The existing erasure contract remains unchanged: `purge`, `erase_source`, and
operator `excise_source` freeze projection work, delete/redact durably, then
call `complete_erasure_at_rest`; a failed truncating checkpoint remains typed
`ErasureIncomplete { stage: "wal_checkpoint", .. }`. Freezing projection work
does **not** presently quiesce reader-worker requests. Nor does the present
erasure boundary prove every projection dispatcher/worker connection is idle at
each checkpoint; both are hypotheses to measure rather than established defects.

## Why this is an attribution study

SQLite's WAL documentation says a reader fixes its end mark for the lifetime
of its transaction, and a checkpoint must stop before WAL pages used by a
reader. Long-lived or overlapping readers can therefore starve checkpoint
completion. It also says that `RESTART` and `TRUNCATE` may block readers.
([SQLite WAL](https://www.sqlite.org/wal.html)) The checkpoint result has a
separate busy/result contract; a busy outcome alone does not identify a reader
or exclude a concurrent checkpoint attempt.
([SQLite checkpoint pragma](https://www.sqlite.org/pragma.html#pragma_wal_checkpoint))

This explains the Slice 60 witness, but not the Memex failures. Slice 60
deliberately held an external child reader and observed the correct refusal. It
did not determine whether a normal FathomDB reader worker, a Python call,
another client connection, or an overlapping checkpoint caused a downstream
run. No conclusion about Windows itself, external scanners, or another OS is
licensed by this design.

### Incident-shaped downstream comparison

Memex's verified Windows run `31988695978`, job `95268355659`, used the
released FathomDB 0.8.22 wheel. Its document-first recovery/erasure nodes
exhausted all eight fresh-process attempts with fail-closed WAL-checkpoint
`ErasureIncompleteError`; process isolation and the downstream retry did not
make this incident disappear. The affected shape is not a generic serial
`read` then `purge`: it closes an old store, opens a fresh store that performs
startup recovery reads, then drives nested relation/source erasure or purge.

Slice 65 therefore adds an incident-shaped, real-SQLite control and requires a
two-version Windows comparison for every applicable control: the released
0.8.22 wheel and the current Slice 65 source/wheel. Each run must retain only
the private redacted attribution output and report the exact version/control
pair. A version difference is evidence to investigate, not proof of a remedy;
the comparison must not silently substitute a direct Rust call for the
installed Python binding path.

## Code-grounded baseline and hypotheses

`Engine` opens one writer connection and `READER_POOL_SIZE = 8` thread-affine
reader-worker connections. Each read request dispatches to one worker and its
`read_*_in_tx` routine opens a deferred SQLite transaction. `Engine::close`
joins those reader workers before dropping the writer connection. The current
checkpoint helper runs `PRAGMA wal_checkpoint(TRUNCATE)` on the writer under
the connection mutex, suppresses the SQLite busy timeout for erasure, and the
outer erasure loop retries five times with a 25 ms backoff.

The projection runtime also owns SQLite connections: `projection_dispatcher_loop`
opens one runtime connection and each `projection_worker_loop` opens its own.
The dispatcher scans durable projection work; workers perform projection
transactions and commits. `freeze_projection_for_erasure` freezes new scans and
waits for worker idleness, but that control is not ownership evidence at a
checkpoint attempt. Slice 65 must include these runtime roles before it may
classify an attempt as having no FathomDB-owned activity.

The Python binding clones the engine `Arc` and runs each engine call through
`py.detach`; Python threads can therefore issue a read and erasure concurrently.
The Python wrapper converts native search/read results into Python dataclasses
and lists before returning. A returned result has no SQLite cursor or live
reader transaction to finalize. Consequently, retaining a completed Python
result in a serial lifecycle is expected to be a negative control, not a
binding-lifetime explanation. It remains worth testing because the downstream
failure is process-sensitive.

The candidate explanations are deliberately distinguishable:

1. A FathomDB reader worker holds a snapshot while erasure checkpoints.
2. A concurrent Python request reaches such a worker; `py.detach` makes that
   possible even when a consumer believes its lifecycle is simple.
3. A projection dispatcher or worker has a transaction/snapshot that overlaps
   the checkpoint despite the erasure freeze boundary.
4. A completed Python result or serial Memex-shaped lifecycle holds no
   snapshot; an observed failure then has another owner.
5. A distinct external process holds the snapshot, as Slice 60 proves.
6. No owned connection is active, but a locally visible checkpoint overlap or
   an unattributed SQLite holder produces `BUSY`.

## Private opt-in instrumentation

The implementation must add a private diagnostic collector, disabled by
default and enabled only by test/controlled CI configuration. It is neither a
public SDK surface nor an `EngineError` shape change. It may retain only:

- a generated run/correlation token, monotonic elapsed times, attempt ordinal,
  `busy`, WAL log/checkpointed-frame counts, and outcome class;
- owned connection role (`writer`, `reader_worker`, `projection_dispatcher`, or
  `projection_worker`) and a stable role-local index;
- state and transaction/snapshot transitions for every owned role: opened,
  idle, request/scan/job admitted, transaction opened, snapshot acquired where
  applicable, commit/rollback, result materialized/responded, and completed;
  and
- local checkpoint-attempt ownership/overlap classification.

It must never emit a database path, SQL, query/body text, result contents,
source/logical identifiers, Python object addresses, or a new public error
field. At every checkpoint attempt it must list active owned roles before
classifying one of `owned_reader_snapshot`, `owned_runtime_transaction`,
`local_checkpoint_overlap`, `no_owned_snapshot`, or
`unclassified_external`. That internal diagnostic label means **unattributed**:
it does not establish an external process or a Windows cause. `no_owned_snapshot` is valid only after reader
workers **and** projection dispatcher/worker connections report no active
transaction or snapshot, and there is no local checkpoint overlap. A busy
attempt in that state may then be `unclassified_external`; its outcome is an
honest unattributed holder, not a guess. A test hook may pause a chosen reader only after
its snapshot-acquired transition, so it proves a real SQLite snapshot rather
than queue latency.

### Implementation review — Fix-N

The implementation must not infer a transaction from a queued reader request
or from an entire projection/embed work phase. An attributed reader records
`transaction_opened` only after `BEGIN DEFERRED` succeeds and
`snapshot_acquired` only after a collector-only canonical-table read succeeds;
`SELECT 1` is not sufficient because it need not touch the database/WAL.
Dispatcher
activity begins only when its actual query statement has started a read
snapshot; worker activity begins only after its `BEGIN IMMEDIATE` succeeds.
Reader completion must be recorded after every reader helper has returned and
dropped its transaction, but before the worker sends its materialized response.
Otherwise the collector reports channel scheduling rather than a live SQLite
snapshot. A private in-crate handoff barrier verifies that exact boundary: it
parks a completed `read_get` before the response send, requires the collector
to be idle, and proves a direct erasure checkpoint is nonbusy; it does not
change reader admission, retry timing, the SDK, or erasure behavior.
The managed-reader witness consumes a private retained-record buffer and
asserts the exact role/index for every busy attempt plus a no-owned post-release
success. The ordinary Rust control keeps its pause hook in-crate. The installed
binding control receives an opaque rendezvous only from a disposable
`test-hooks` wheel; that feature is non-default and absent from shipped wheels
and the production Rust surface. The Windows command selects the in-crate test
by its fully qualified path and inspects the retained output for `running 1
test`; a zero-test invocation is a hard CI failure rather than evidence.

### Local Windows execution evidence — non-closure

On 2026-08-16, an authorized local Windows guest executed the managed-reader
control from reviewed source `ed09e538` at `C:\projects\fathomdb`, without
guest Git operations. The guest was provisioned with Rustup 1.29.0, the
project's Cargo/Rustc 1.95.0 toolchain, and Visual Studio 2022 Build Tools
17.14.37 with the x64 C++ workload (compiler 19.44.35228). With
`FATHOMDB_WAL_ATTRIBUTION=1`, the fully qualified in-crate test passed and
retained its redacted output at
`C:\ci\fathomdb-slice65-wal-attribution-20260816.log` (24,632 bytes).

The record contains five `BUSY` attempts, each attributed to exactly
`reader_worker:0` as `owned_reader_snapshot`, followed by release and a
successful `no_owned_snapshot` attempt with no active owned roles. It validates
the narrow managed-reader discriminator on this local guest. It neither
reproduces a Memex client failure nor completes the Python, serial-lifecycle,
runtime-role, or external-reader controls. GitHub-hosted `windows-latest`
retained-artifact evidence remains the first-party closure requirement.

GitHub-hosted Windows job `95270599876` then passed that same focused control.
Independent inspection of its retained diagnostic log confirmed five busy
attempts classified `owned_reader_snapshot` for exactly `reader_worker:0`,
followed by a successful `no_owned_snapshot` record after release. This is
first-party validation that the diagnostic distinguishes the known Engine-owned
reader overlap; it is not an attribution of the Memex symptom. The Python
concurrent-reader, retained-result/idle-Engine, Memex-shaped serial-lifecycle,
and projection-runtime controls remain pending, so retry tuning, reader-pool
changes, and binding-lifetime changes remain unapproved.

### Slice 65 control implementation — hosted evidence pending

Commit `2583b908` wires the remaining discriminators without changing retry,
pool, finalizer, public diagnostics, or error behavior. It adds a real-DB
retained materialized-result/live-idle Engine negative control; a close-old,
fresh-open recovery-read, nested source-erasure plus deleted-root purge control;
and a projection-worker control paused only after `BEGIN IMMEDIATE` succeeds.
The last control checks `owned_runtime_transaction` while active, releases the
transaction, then requires an idle/no-owned checkpoint rather than calling
freeze-serialized normal erasure a collision.

Fix-N `819a281e`/`5be0346f` corrects an earlier pseudo-binding witness: the
installed Python thread now calls the public `read.get` API and the opaque,
non-shipping barrier fires only after that governed request has completed its
real `BEGIN DEFERRED` plus `canonical_nodes` snapshot read. It requires exactly
five `owned_reader_snapshot reader_worker:0` busy records and one post-release
`no_owned_snapshot` record. The same installed test-hooks wheel retains a
materialized public `read.get` result and its nested field without GC or
deletion, then proves both source erasure and deleted-root purge have only
nonbusy, empty-role, `no_owned_snapshot` checkpoints. The incident-shaped
script also runs once through
the released `0.8.22` wheel and once through the current source/test-hooks
wheel, retaining a nonsecret selector and first result for each. The current
wheel reads the private collector immediately after `read.get` and separately
immediately after `graph.neighbors`, requiring idle state before either erase
operation. It then requires nonempty, empty-role, nonbusy `no_owned_snapshot`
records for both source erase and purge. The old wheel cannot emit the newly
added private collector records; it is an observational baseline, not a
substitute for the current attribution witness. These controls passed on Linux
against the current disposable wheel. The projection control uses a direct
in-crate checkpoint while an exact captured projection worker is paused after
real `BEGIN IMMEDIATE`; it never labels freeze-serialized normal erasure as a
collision.

GitHub-hosted Windows run `31994135444`, job `95282754203`, passed the managed,
retained, incident-shaped serial, projection, and all four close-boundary Rust
controls. Its direct, fresh-open, `read_get`, and `neighbors` raw checkpoints
each retained `busy=0` and zero frame counters, with the fresh Engine live for
the applicable siblings. The job then failed only at the disposable current
test-hooks wheel installation: `maturin-action` built the wheel at the
repository-root `dist-slice65-test`, while the next PowerShell step looked for
`src/python/dist-slice65-test/*.whl`. The missing glob caused the install to
fail and the subsequent Python import to fail. Therefore that job supplies no
installed current-wheel Python binding, retained-result, or serial attribution
result; the pre-install digest line is not proof that the current wheel was
installed. This is a CI-path failure, not negative attribution evidence.

The workflow must discover exactly one root-level disposable wheel, record its
resolved path and SHA-256, install that exact path, and then rerun the current
installed Python controls on hosted Windows. Only after that rerun and the
three fresh repetitions required for a remedy decision may a remedy be
considered.

### Fix-N — released first-erase outcome is an observation, not a gate bypass

GitHub-hosted Windows job `95289111224` passed all selected Rust controls,
including the corrected reader-response handoff control and the live
close-boundary siblings, and built the disposable current test-hooks wheel. It
then ran the released `0.8.22` installed-wheel serial control first. That
control closed the old Engine, opened a fresh Engine, completed the recovery
reads, and received the typed `ErasureIncompleteError` at `wal_checkpoint` on
its first nested source erase: all five truncation attempts were busy and the
final report retained 25 WAL frames. The released wheel predates the collector,
so this is an observational baseline, not current attribution evidence.

GitHub-hosted Windows job `95291674186` shows why that harness cannot require
one preferred outcome: after the same close/fresh-open/recovery-read shape, its
released first nested source erase completed cleanly. The pre-refinement script
then entered its follow-on lifecycle before rejecting the unexpected clean
result, so that run is not an exact-one-first-erase witness and does not prove
an erasure remedy, no-holder state, or current behavior. It does establish that
the released baseline is variable and must be recorded, not selected for.

The refined harness executes exactly one released nested source erase after
close, fresh open, and recovery reads. It requires `nodes_excised == 1` for a
clean result and does not transition or purge afterwards. It reports either the
typed WAL refusal as `BASELINE_FIRST_ERASE outcome=typed_erasure_incomplete`
with `ErasureIncompleteError`, `wal_checkpoint`, WAL `BUSY`, and normalized
frames (exit 65), or the clean result as
`BASELINE_FIRST_ERASE outcome=clean_completion` (exit 66). The workflow accepts
only an exact matching marker/exit pair, retains respectively
`OBSERVED_EXPECTED_ERASURE_INCOMPLETE` or
`OBSERVED_CLEAN_SERIAL_COMPLETION` with only identity/selector/outcome facts,
and then continues once to the mandatory current test-hooks controls. Any other
exception, stage, detail, marker, or exit remains a CI failure. Neither outcome
is retried, an attribution/remedy pass, or a passing erasure result. The current
controls remain required to emit their exact selectors and collector records;
their final result, not the baseline observation, determines whether the hosted
job passes.

### Follow-on — post-COMMIT release discriminator

Hosted Windows job `95294107824` exposed a distinct ambiguity in the existing
positive control. Its held reader produced five expected busy attempts labelled
`owned_reader_snapshot reader_worker:0`. The test then signalled release; the
artifact later printed reader `completed`/`idle`, but the next existing erase
still produced five busy attempts labelled `unclassified_external`, with 198
frames. A release signal is not a proof that the reader's SQLite `COMMIT` had
completed before the next checkpoint, and an empty collector role list is not a
proof that every Engine-owned connection has been observed. The result is
`UNATTRIBUTED`.

The approved follow-on has no production effect and no retry behavior:

1. The held-reader test seam separates `release_signal` from a post-`COMMIT`
   acknowledgement. It may not report the acknowledgement before the actual
   commit returns and the reader collector is idle.
2. At acknowledgement, a private Engine inventory must account for exactly one
   writer, all `READER_POOL_SIZE` readers, one projection dispatcher, and all
   projection workers. Each entry records only role/index, connection-open, and
   actual SQLite transaction state; an absent or unregistered Engine connection
   is an incomplete diagnostic, never an external classification.
3. The control seeds and holds the reader, runs one existing fail-closed erase,
   signals it, waits for the acknowledgement, then issues a fixed small number
   of direct raw `wal_checkpoint(TRUNCATE)` samples. Every sample opens and
   closes its own independent connection. `TRUNCATE` may mutate the isolated
   test WAL; it never retries, relabels, or completes the original Engine
   erasure. It retains only case label, sample ordinal, monotonic elapsed time,
   busy/frame counters, collector state, and the complete inventory.
4. No sample retries `erase_source`, turns an incomplete erasure into success,
   or changes its outcome. A clean raw sample is diagnostic only, not an
   erasure/release pass. If any bounded pre-close sample remains busy, the same
   control MUST close and join the Engine, then run one fixed fresh-child raw
   probe. Its redacted outcome is required in the artifact; it is still not an
   erasure/release pass.

Implementation commits `4152b222`, Fix-N `a98474ef`, and Fix-N `5387a75a`
keep the diagnostic state test-only; their private open wrapper is
production-transparent: the
single held-reader erase remains a typed `ErasureIncomplete`; the separate
acknowledgement occurs after `COMMIT` and collector-idle; the inventory requires
exactly one writer, eight reader workers, one dispatcher, and two projection
workers, with direct `is_autocommit` checks on all of them. The test-only
factory is now the sole Engine-source `Connection::open` call: it classifies and
counts writer, reader-worker, dispatcher, worker, and temporary runtime-probe
opens. Independent raw test connections are deliberately outside Engine and are
labelled only as the raw diagnostic. The live registry detects duplicate/missing
long-lived roles; the factory count is expected-versus-actual in the artifact.
The artifact keeps those direct creation/transaction facts distinct from
collector roles. A missing reply, registry/count mismatch, non-autocommit handle,
or non-idle collector emits `post_commit_inventory=incomplete` and fails the
diagnostic rather than calling the holder external. It takes exactly two
independent pre-close samples.
On the local Linux focused run they were both clean, so the fixed fresh-child
branch truthfully emitted `outcome=not_required`. That is implementation smoke
evidence only: it neither explains the Windows `UNATTRIBUTED` job nor approves a
retry, pool, binding, or product remedy. Hosted Windows execution remains
required.

The Windows job selects this diagnostic by exact test path, rejects a zero-test
selection, and requires its redacted markers in the retained artifact before it
continues to current Python controls. The diagnostic does not pass a remedy or
Slice 65; it only allows those already required controls to execute.

Hosted Windows job `95301703507` is the first controlled execution of that
post-COMMIT diagnostic. Its retained artifact had the exact expected and actual
creation counts, direct autocommit facts for writer, eight readers, dispatcher,
and two workers, an idle collector, and two clean bounded pre-close raw samples;
the required child outcome was `not_required`. This is a controlled diagnostic
observation, not an erasure, release, attribution, or remedy pass. The job then
stopped before every current installed-wheel Python control: the action ran from
`src/python`, so its relative maturin output was beneath that directory, while
the PowerShell lookup resolved the same relative directory beneath the workspace
root. No current wheel was resolved, hashed, installed, or exercised. Fix-N
uses one explicit `${{ github.workspace }}/dist-slice65-test` build output and
derives that exact directory with `Join-Path $env:GITHUB_WORKSPACE`; it fails
closed if the directory is absent and accepts exactly one wheel through a
literal-path lookup before the existing resolved-path, SHA-256, no-index,
no-dependencies install. The hosted rerun is still required; the clean
post-COMMIT artifact does not approve any retry, pool, binding, or product
remedy.

### Fix-N — installed-binding transaction-completion discriminator

Hosted Windows job `95304898116` reached the current installed-wheel binding
control after its serial control passed. Its deliberately held public Python
read produced the intended initial evidence: one existing `erase_source`
failed closed with the typed WAL error and exactly five
`owned_reader_snapshot reader_worker:0` records. After snapshot release, the
old control joined the Python thread and issued a second erasure. That second
operation exhausted its normal checkpoint attempts with no active collector
role. This is an observation, not evidence for a finalizer, SDK, pool, retry,
Windows, or external-holder remedy: the old control did not acknowledge the
actual post-helper reader boundary and its second erasure was not a bounded
diagnostic sample.

The approved replacement is private to `cfg(test)` and the disposable
`test-hooks` wheel. It arms an opaque completion rendezvous on the same public
`read.get` request. The worker may acknowledge only after the read helper has
returned and dropped its transaction/statements, `finish_reader_request` has
set the collector idle, and that reader's direct `is_autocommit()` fact is
true. The public read result remains blocked until the diagnostic releases the
handoff; it then joins normally. The marker reports observable completion facts
rather than calling this a `COMMIT` or introducing SDK synchronization.

The control performs exactly one original Engine erasure while the public read
is held. It must remain the existing typed error and retain five exact owned
reader records. After release, completion acknowledgement, and public-thread
join, the diagnostic requires a complete direct inventory before drawing any
inference: exact live roles; creation tuple `writer:1, readers:8,
dispatcher:1, workers:2, probes:2`; autocommit facts from the writer, all eight
readers, dispatcher, and both workers; and a separately idle collector. A
missing reply, count/role mismatch, non-autocommit handle, or non-idle
collector fails closed as an incomplete inventory.

Only after that inventory may it record raw sample 1, exactly one test-only
Engine checkpoint-path sampler, then raw sample 2. The sampler may use its
ordinary bounded five-attempt checkpoint sequence, but may not invoke an
erasure verb, delete or purge data, complete/retry the failed erase, or
reclassify the original error. The two raw samples are independent SQLite
connections and retain only redacted timing and busy/frame facts. If either raw
sample is busy, the test closes/joins the Engine and runs exactly one native
fresh-child raw probe. Clean samples are diagnostic observations only. CI must
run current serial first, this exact binding diagnostic second, and retained
result third; every selector, zero-test guard, marker, and ordering assertion is
load-bearing. No production behavior, public API/error, retry, pool, or binding
lifetime change is authorized.

### Fix-N — retained installed-binding completion marker

GitHub-hosted Windows job `95320283953` reached and passed the current serial,
installed native binding, and installed retained-result controls. The binding
artifact contains the held-reader positive records, the direct completion
acknowledgement (`reader_autocommit=1`, `collector=idle`), complete direct
inventory, clean pre/post Engine-sampler raw observations, and the conditional
native child outcome `not_required`; the retained materialized-result control
also passed. The job then failed at a later workflow assertion that still
required the removed `python_binding_snapshot_released` marker. That is CI
contract drift after the controls, not a control failure and not attribution or
remedy evidence.

Fix-N RED `b821caa0` requires the completion acknowledgement specifically in
the retained artifact and rejects the removed marker. Its mutation deletes the
artifact-scoped completion requirement and must fail the static contract.
GREEN replaces the stale late assertion with the exact private completion
acknowledgement already required from the binding log. The same fact must be
retained in the artifact after the later retained-result control has run. This
does not select a retry, binding-lifetime, reader-pool, or Windows remedy;
fresh hosted repetitions and the remaining attribution decision remain
required.

### Fix-N — reader response-handoff attribution boundary

GitHub-hosted Windows job `95285220515` ran the managed-reader positive control
successfully, then failed the retained-result Rust control immediately after a
public `read_get`: its collector snapshot still named `reader_worker:0` as
`snapshot_acquired`. Independent review traced that result to the collector
boundary, not a retained SQLite reader: the read helper had materialized its
result and dropped its transaction before the worker sent the response, but the
pre-fix loop recorded `completed`/`idle` only after that response send. The
caller could therefore inspect an active diagnostic record in the narrow
handoff window even though no SQLite snapshot remained.

This invalidates that failure as a binding-lifetime, reader-pool, retry, or
Windows conclusion. It does not weaken the held-snapshot positive control:
that control keeps a real transaction open and still records five exact
`owned_reader_snapshot reader_worker:0` busy attempts. The repair applies one
private completion operation to every response-bearing reader variant and its
error result, before sending the response; the `HoldWalSnapshot` control
finishes only after its explicit SQLite `COMMIT`. A new in-crate handoff control
holds a materialized `read_get` result before response delivery and requires an
idle collector plus a successful no-owned erasure checkpoint. The hosted job
selects that control explicitly, rejects a zero-test selector, and requires
its fixed redacted success marker in the retained artifact. The raw and staged
controls remain partial observations; a repaired hosted rerun and fresh
repetitions are still required before attribution or a remedy decision.

### Fix-N — explicit close-boundary control

RED commit `5d2d3bd5` added four independently selected controls before the
raw checkpoint helper existed; the targeted engine build therefore failed on
the missing helper. Initial GREEN `baf8fb08` created a clean database in an old
Engine, wrote the fixed root/nested/edge fixture, explicitly closed it, then
opened an independent raw SQLite connection solely for one
`wal_checkpoint(TRUNCATE)` and explicitly closed that connection. Fix-N RED
`8c1d1993` caught that the three fresh siblings had also closed their fresh
Engine before the raw checkpoint. GREEN `4a63e561` keeps that fresh Engine open
for its raw checkpoint, then closes it afterwards.

The direct control's supporting facts are narrowly that the old Engine reports
closed, rejects a new read, and has an idle collector; they do not describe all
SQLite handles. Each live-fresh sibling proves immediately before the raw
checkpoint only that its `closed` flag is false, its collector is idle, and its
managed writer connection is present. The raw checkpoint itself has no
FathomDB collector role and must not be treated as a recovery-read attribution.

The direct close control was clean in the focused Linux source run
(`busy=0`, `log_frames=0`, `checkpointed_frames=0`). Therefore the staged
siblings also ran: fresh open, completed `read_get`, and completed
`graph_neighbors`; every raw checkpoint occurred while that fresh Engine and
its managed writer connection remained open and collector-idle, and each
returned the same redacted zero counters before the fresh Engine was closed.
This is a local source result, not Windows evidence and not a claim about the
primary incident-shaped recovery/reopen control, which is unchanged. The hosted
Windows job selects every sibling by fully qualified test path, stores only its
fixed case label plus managed fresh-connection state and busy/frame counters in
the existing artifact, and independently rejects zero selected tests for every
command.

On 2026-08-16, the follow-on local guest run stopped before copy or execution:
the guest's current SSH host key did not match a pinned host key in this host's
trust store. Strict verification refused the connection. No trust record was
changed or bypassed, no source was copied, and this is a local-witness gap only;
the hosted Windows controls remain required.

### Commissioned follow-on — direct-Rust incident checkpoint ladder

Hosted Windows job `95323862568` separates the controls that had previously
been conflated: the managed-reader, retained-result, reader-handoff, and
close-boundary controls completed, then the direct-Rust incident-shaped serial
fixture reached its single nested source erase and received the existing typed
`ErasureIncomplete { stage: "wal_checkpoint", .. }` after five BUSY attempts
with 25 WAL frames. That is a reproducing observation, not a failing test
expectation, a product fault assignment, or a reason to retry the erase.

This follow-on commissions only a private, test-only incident checkpoint
ladder. It must close the old Engine, open a fresh Engine, perform the real
`read_get` and `graph_neighbors` recovery reads, and retain ordered,
independent raw `wal_checkpoint(TRUNCATE)` facts at: old-close; after the fresh
reads with complete direct long-lived inventory and collector-idle state; and
after an erasure preflight that has an explicit RuntimeProbe actual-drop
acknowledgement but before the one destructive nested-source erase. The raw
samples do not retry, relabel, or complete that erase. The erase retains only
its typed outcome and is never repeated.

Every raw probe is a private audited RuntimeProbe lifecycle: open/live and
actual-drop acknowledgement are retained. An incomplete lifecycle, incomplete
long-lived inventory, or non-idle collector is a diagnostic failure and cannot
be labelled external. If a pre-erasure raw sample is BUSY, the ladder closes and
joins the fresh Engine, takes one same-process raw sample, then takes exactly
one native FathomDB/Rusqlite fresh-child raw sample. The output remains
redacted to fixed stage labels, frame counters, inventory/collector facts, and
old/fresh close and child facts. It changes no retry tuning, reader pool,
binding lifetime, public API, or error contract. The previous installed-Python
serial clean control and this direct-Rust typed incident result are distinct
observations; neither selects a remedy. The disposition remains
**UNATTRIBUTED**.

## RED-to-GREEN experiment matrix

All experiments use a real temporary SQLite database on Windows x64 and a
fresh database per case. Each RED test is committed or visibly staged before
the instrumentation/coordination code that turns it GREEN. The retained CI
artifact contains only the safe fields above plus test-case labels.

| Case | RED predicate | GREEN evidence / expected discriminator |
| --- | --- | --- |
| Managed reader paused after snapshot | A reader-worker test hook cannot yet report a snapshot-acquired state or hold it while erase begins. | The paused worker is recorded as active at each busy attempt; erasure returns typed `ErasureIncomplete`; release causes its request to complete and a later erase to finish. |
| Projection runtime role completeness | A controlled dispatcher/worker fixture lacks its indexed transaction/snapshot transition, so a busy attempt could be labelled `no_owned_snapshot`. | Implemented and passed once on hosted Windows: worker pause starts only after `BEGIN IMMEDIATE`, records `owned_runtime_transaction`, releases, then proves a no-owned checkpoint. Fresh repetitions remain pending. |
| Installed Python concurrent reader versus erase | A barrier-driven installed-binding Python thread case has no correlated reader/checkpoint record. | Implemented. Jobs `95289111224` and `95291674186` stopped before this current control—first after typed refusal, then after the harness rejected clean completion. Rerun required; no Windows binding result exists yet. |
| Completed retained materialized result/live Engine control | Before the collector exists, a contract fixture fails because it cannot prove the required post-return completed state and zero owned transaction/snapshot fact. | Rust control passed again in both jobs; its installed-Python counterpart did not run because the workflow stopped at the released observation. Rerun and fresh repetitions remain pending. |
| Incident-shaped recovery/reopen/nested erasure | The control does not close an old store, reopen a fresh one for startup recovery reads, then perform nested relation/source erasure or deleted-first purge with an explicit no-overlap assertion. | Rust control passed again. The released `0.8.22` installed baseline varied: job `95289111224` saw typed WAL refusal (five busy attempts; 25 frames); job `95291674186` saw a clean first source erase before its pre-refinement harness entered follow-on lifecycle and rejected the result. Neither run had a collector or reached current attribution. The refined exact-one harness rerun and fresh repetitions remain pending. |
| Explicit close boundary, then raw checkpoint | The test has no independent raw checkpoint immediately after old Engine close, so recovery reads could be blamed for a holder that already survived close. | Passed again in job `95289111224`: direct, fresh-open, `read_get`, and `graph_neighbors` retained zero busy and frame counters; fresh siblings kept their Engine live and collector-idle. Fresh repetitions remain pending; these cases do not alter the incident-shaped control. |
| Post-COMMIT reader release and complete inventory | A reader release signal can race its SQLite `COMMIT`, and an empty collector list can omit an Engine-owned connection. | Job `95294107824` is `UNATTRIBUTED`: the held-reader positive case was exact, but the next erase remained busy with no active collector role. Commit `4152b222` adds the private acknowledgement, exact inventory, and two bounded independent raw samples; local Linux smoke saw both clean and no child probe was required. Hosted Windows evidence remains pending and is not a remedy result. |
| External child reader control | The Slice 60 child witness has no attribution comparison. | The child remains an external-holder positive control: typed failure, zero active owned roles, external-holder classification, and release/reap before a successful later erase. |

The managed-reader and external-child cases preserve Slice 60's fail-closed
assertion. The Python cases must execute through the installed native binding
on Windows CI, not mock the database or call the Rust engine directly. A
Linux-runnable source/workflow contract may guard the required case names,
private-field redaction, and artifact upload, but cannot replace the Windows
execution witness. Each applicable Windows control runs against both the
released 0.8.22 wheel and the current source/wheel before a version-based
inference is allowed. No artificial prose-only test is added by this design.

### Fix-N — installed binding boundary and native-child fidelity

Review rejected the first binding-boundary implementation before integration.
Its completion rendezvous observed collector-idle after `finish_reader_request`,
but did not inspect the actual reader-worker `Connection` at that boundary; a
later whole-Engine inventory cannot replace that fact. The repair must capture
that exact `Connection::is_autocommit()` result after the helper-local
transaction and statement have dropped, store it in the private rendezvous,
and require it before acknowledging the blocked public `read.get` result.

The conditional fresh-child sample must also use the same native FathomDB /
Rusqlite SQLite path as the engine. Python's standard-library `sqlite3` is not
an acceptable substitute. If either pre-close raw sample is busy, the parent
must close and join its Engine, then start exactly one fresh installed
test-hooks child that calls a private native raw-checkpoint hook. The child
records only busy and frame counters. It never opens another Engine, retries
the original erasure, or changes the diagnostic outcome. The workflow contract
must name the completion marker rather than a removed post-release marker, and
must prove both serial→binding and binding→retained ordering by independent
mutations. These changes remain private/test-only and do not authorize any
production retry, reader-pool, or SDK lifetime change.

## Conditional remedy rules

No remedy is approved merely because the witness is timing-sensitive.

- **Retry tuning:** propose a bounded, parameterized policy only if repeated
  controlled Windows evidence attributes busy attempts to a known transient
  owner, measures its release distribution relative to the current budget, and
  shows that an expanded bounded wait completes after release without turning a
  still-pinned reader into success. The parameter's public/private placement,
  defaults, cancellation, and cross-platform behavior need a separate design.
- **Binding lifecycle work:** consider it only if the completed-result or
  serial control shows a live native request/snapshot after the Python call has
  returned, or a Python-owned engine/close path demonstrably retains one. The
  current materialized-result design is contrary evidence; GC/finalizer theory
  alone is insufficient.
- **Reader-pool/quiescence work:** consider a narrow erasure/read admission
  barrier before restructuring the pool, and only if attribution shows an
  owned worker snapshot survives beyond its request boundary or accepted
  concurrent SDK calls reproducibly conflict. Pool-size or architecture changes
  require a new design and concurrency/throughput regression evidence.
- **No owned role / checkpoint collision:** only after reader workers **and**
  projection runtime roles are demonstrably idle may the collector report no
  owned snapshot. If it then finds a local checkpoint overlap or an
  unclassified external holder, retain the typed refusal, report the
  classification, and investigate the coordinating caller or checkpoint
  ownership. Do not blame Windows or loosen erasure.

At least three independently fresh repetitions of the relevant positive and
negative control, with the same attribution class, are required before a
remedy proposal. Any ambiguous record is `UNATTRIBUTED`, not evidence for a
preferred fix.

## Acceptance and non-goals

`AC23-WAL-ATTRIBUTION` is satisfied only when a retained first-party Windows
x64 artifact demonstrates every matrix case, its safe correlation records,
the current typed fail-closed response under a still-held snapshot, and the
decision classification for each case. The Slice 65 status must say either
`ATTRIBUTED` with its evidence-supported next action, or `UNATTRIBUTED` and
retain all remedies as rejected pending data.

Non-goals: retry/pool/finalizer changes, a public diagnostics API, changing
the erasure error or success criteria, a general Memex test-suite import,
Windows-only blame, CUDA/artifact work, publication, or integration of this
release branch into `main`. Slices 30 and 60 remain completed on
`origin/release/0.8.23`; `origin/main` integration remains pending.
