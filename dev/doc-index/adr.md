# DOC-INDEX detail — `dev/adr/`

> Long-form per-doc notes for this area of the doc tree. The thin map at
> `dev/DOC-INDEX.md` links here; **every path listed below also has its own row in
> `dev/DOC-INDEX.md`** (path + a ≤120-char purpose clause) — this file carries the
> full slice-history / decision-record prose that DOC-INDEX.md compresses away.
> Update this file (not DOC-INDEX.md) when you want to add narrative detail; update
> BOTH files when you add/rename/materially change a doc (DOC-INDEX.md row +
> this file's row, same closing commit — mirrors DOC-INDEX.md's own rule).

## `dev/adr/` — architecture decision records

| Path | Purpose | Owning slice / AC | Last-touched |
|------|---------|-------------------|--------------|
| `dev/adr/README.md`, `ADR-0.6.0-decision-index.md` | ADR index | — | (tree) |
| `dev/adr/ADR-0.8.0-supersede-five-verb-surface-cap.md` | Supersede AC-057a's five-verb cap → governed surface; **status: SIGNED/accepted** (Q1–Q5 = A1/B1/amend/confirm/**BIND-RUST**; Rust pin → Slice 27) | advanced 0.b; signed 2026-06-03; executed at 25; gates 30 | 2026-06-03 |
| `dev/adr/ADR-0.8.0-canonical-identity-substrate.md` | NEW (0.a) — canonical identity substrate (logical_id+superseded_at, Option 2A); Decision 5 (Slice 31) re-scopes active uniqueness to `logical_id` alone | authored at 0.a; gates 15; amended by 31 | 2026-06-05 |
| `dev/adr/ADR-0.8.0-agent-memory-retrieval-and-identity.md` | Retrieval+identity ADR (Q1 table-stakes, Q3 RRF compat); gates Slice 10; Q2/Q4 amended by Slice 31 (`logical_id`-alone identity; edges DO supersede) | gates 10; amended by 31 | 2026-06-05 |
| `dev/adr/ADR-0.8.0-embedder-identity-change-workflow.md` | Embedder-identity change workflow | — | (tree) |
| `dev/adr/ADR-0.8.0-graph-model-and-edge-addressing.md` | NEW (Slice 32) — intended graph model: one **ontology-neutral** binary property-graph substrate first-classing **corpus (GraphRAG)** + **memory (Graphiti fact-on-edge)** ontologies; opaque-id addressing for 0.8.0 (hybrid future ADR); edge-enrichment/projectability/edge-inclusive-G7 **reserved-additive**; fact-on-node escape hatch. **status: ACCEPTED — H1 (neutral-both) + H3 (reserve edge-enrichment additive) HITL-SIGNED 2026-06-05**; H2/H4/H5/H6 deferred 0.8.x; **no 0.8.0 substrate change**. Foundation for the Slice 35 graph ADRs | Slice 32 (signed) | 2026-06-05 |
| `dev/adr/ADR-0.8.0-graph-traversal-scope.md` | NEW (Slice 35) — F1/G5/G6 graph-traversal SCOPE: SDK depth ceiling ≤3 + engine hard cap 50 (v0.5.6 `MAX_TRAVERSAL_DEPTH` port); 0.8.x walk filters `superseded_at IS NULL` only (edge valid-time G11 deferred); `canonical_edges(from_id)/(to_id)` already folded into G0 → **no new migration** (gap 36 not triggered); `G6 = G1+G4+G5+G9`, build G6 before standalone G5; v0.5.6 `WITH RECURSIVE` BFS re-targeted to `from_id`/`to_id`. **status: ACCEPTED as 0.8.1 roadmap direction** (HITL-signed 2026-06-06; revisable when 0.8.1 graph work opens — recorded in `dev/roadmap/0.8.1.md`; graph work retargeted 0.8.x→0.8.1). No 0.8.0 code/schema change | **35** produces; gates 0.8.1 Slice H (G5/G6) | 2026-06-06 |
| `dev/adr/ADR-0.8.0-filter-grammar.md` | NEW (Slice 35) — G4/F3 CLOSED typed filter enum `{JsonPathEq, JsonPathCompare{Gt/Gte/Lt/Lte}, ScalarValue{Text/Integer/Bool}}` on `read.list(kind, filter?, limit)`; EXCLUDES the fused predicates + all `*_unchecked` builders; coordinates with G10 `SearchFilter` (shared value vocab; **full unification = NEEDED future work, reserved-gap 37, affects both G4+G10**); compiles to parameterized `json_extract` over allowlisted paths (no DSL/no raw SQL). **status: ACCEPTED** (HITL-signed 2026-06-06). No 0.8.0 code/schema change | **35** produces; gates 0.8.x G4 | 2026-06-06 |
| `dev/roadmap/0.8.1.md` | NEW (Slice 35 close) — 0.8.1 roadmap direction (REVISABLE): the HITL-signed graph-traversal-scope decisions recorded as roadmap direction not a frozen G-gap contract (graph work retargeted 0.8.x→0.8.1); + the G4↔G10 unification (gap 37, needed) + the parked F9/F5 Slice 46 pointer | **35** close; informs 0.8.1 | 2026-06-06 |
| `dev/adr/ADR-0.8.1-deferred-f9-confidence-importance.md` | F9 confidence vs G12 importance — **DEFERRED 0.8.2+**; prerequisites: R2 eval (Slice 25), ≥100 confidence-bearing edges, HITL sign-off on formula; written as framing ADR by Slice 35 | **35** produces; gates 0.8.2+ | 2026-06-13 |
| `dev/adr/ADR-0.8.1-deferred-f5-fielded-fts-bm25f.md` | F5 fielded FTS / BM25F column model — **DEFERRED 0.8.2+**; prerequisites: R0 CDF (Slice 5), R2 eval (Slice 25), HITL sign-off; written as framing ADR by Slice 35 | **35** produces; gates 0.8.2+ | 2026-06-13 |
| `dev/adr/ADR-0.8.1-byo-llm-extraction-protocol.md` | BYO-LLM Extraction Provider Protocol ADR — `fathomdb.extract.v1` engine-side contract (spawn+handshake+ingest surface+invalidate-not-accumulate; footprint: no FathomDB network call); 5 additive pins from Memex ELPS consult 2026-06-12; pending HITL sign-off (Slice 0 gate) | Slice 0; Slice 15 implements | 2026-06-12 |
| `dev/adr/ADR-0.8.6-generalized-provider-protocol.md` | **OPP-8 generalized typed-task provider protocol** (ACCEPTED, HITL-signed 2026-06-26): generalizes `fathomdb.extract.v1` → `fathomdb.provider.v1` — same transport/handshake/error model, adds a `task` discriminator (extract-only default, back-compat) + `supported_tasks` negotiation + `provider_session` refactor; **Option A** = land the transport seam now, payloads deferred to 0.8.12; proven by **byte-identical** ELPS golden output; closes the I-5 (#8→#7/#6) rework edge | 0.8.6 Slice 0 gates; **Slice 5 implements** | 2026-06-26 |
| `dev/adr/ADR-0.8.6-governed-verb-coupling-hygiene.md` | **OPP-5 governed-verb coupling hygiene** (ACCEPTED, HITL-signed 2026-06-26): governed read surface (read.get/get_many/collection/mutations/list, graph.neighbors/search_expand) is already complete+LIVE in Py+TS; Slice 10 = **parity-harden** (cross-binding harness for ALL read verbs, not just `read.list`; consumer-boundary conformance assertion; reconcile interface docs), **not** build a new surface | 0.8.6 Slice 0 gates; **Slice 10 implements** | 2026-06-26 |
| `dev/adr/ADR-0.8.1-ir-measure-eval-design.md` | IR-measure/Eval Design ADR — R0 CDF spec (found@K for K∈{50..1000}, per-class, all arms; gates Slice 10 rerank depth) + R2 end-to-end parity eval design (identical-answerer, local Mem0-OSS baseline, per-class; Decision ①: AC-077 gate, R2 north-star, C3: gates R3 go/no-go); pending HITL sign-off | Slice 0; Slice 5/25 implements | 2026-06-12 |
| `dev/adr/ADR-0.8.1-graph-substrate-g11-migration.md` | G11 edge enrichment ADR — activates H3 reservation; step-14 SCHEMA_VERSION 13→14 (additive body/t_valid/t_invalid/confidence/extractor_model_id columns on canonical_edges); edge projectability capability (FTS+vector; exact mechanism = Slice 15 impl detail); invalidate-not-accumulate contract; pending HITL sign-off | Slice 0; Slice 15 implements | 2026-06-12 |
| `dev/adr/ADR-0.8.12-consolidation-recency-provider.md` | **OPP-2 consolidation/recency provider** (PROPOSED, 0.8.12 Slice-0): a new typed task `fathomdb.consolidate.v1` on the ONE OPP-8 transport (ADR-0.8.6) — additive `ProviderTask::Consolidate` + payload (candidate fact-cluster → keep/supersede/merge/invalidate verdict) + `EngineError::Consolidator` leaf; **no second transport** (closes I-5); post-retrieval recency/supersession metadata NOT a destructive content-rewrite (0.8.3 blind-merge caution); caller-side BYO-LLM, default-OFF until the Slice-20 value test clears | 0.8.12 Slice 0 gates; **Slice 15 implements**, Slice 20 value-gates | 2026-07-01 |
| `dev/adr/ADR-0.8.14-exp-s-kind-tagged-coexisting-index-substrate.md` | **EXP-S kind-tagged coexisting-index substrate migration (+ F5 co-land)** (RATIFIED, HITL-signed 2026-07-03): realizes the `ADR-0.8.1-deferred-f5` deferral + discharges ledger **TC-1** (OPP-12 projection-registry forward-compat). D1 separate `row_kind` column (leaf/coverage/graph); D2 per-kind index-target dispatch; D3 flush-then-byte-compare determinism check; D4 ONE coordinated migration (`SCHEMA_VERSION` 15→16 EXP-S, 16→17 F5); D5 discharges TC-1; **D6 eu7 no-op unless vec0 rewritten**; D7 KILL paths; **D8 = Option C** (F5 ships by conscious HITL override, NOT gate-clearance). Cross-links: `dev/plans/plan-0.8.14.md`, `runs/STATUS-0.8.14.md` | 0.8.14 Slice 0 gates; Slices 5/10 implement | 2026-07-03 |
| `dev/adr/ADR-0.8.16-f9-importance-confidence-ranking.md` | **F9 importance/confidence ranking — opens the deferred F9 signal** (SIGNED, HITL coreyt 2026-07-08 — Slice-0 gate; all 5 decisions ratified). Opens F9 as an **OFF-by-default, observable, OPP-12-`rankable`-forward-compatible MECHANISM**: `canonical_nodes.importance REAL` (step-18, `SCHEMA_VERSION` 17→18) with a **3-way sentinel** (`NULL`=absent/neutral · `0.0`=explicit floor · `(0,1]`=importance), the existing `canonical_edges.confidence` folded into ranking (multiplicative-on-fused; graph-arm `confidence × 1/(K+bfs_rank)`), surfaced additively through the 0.8.8 `PerHitExplain`. **Supersedes the open-condition** of `ADR-0.8.1-deferred-f9-confidence-importance.md` (mechanism now / eval-tuning later; §5 gates 1+2 deferred, gate 3 satisfied). eu7 no-op basis (no re-embed). Mints R-F9-4 + the F9 ranking-contract AC. Cross-links: `dev/design/0.8.16-slice-0-f9-onnx-design.md`, `dev/plans/plan-0.8.16.md` | 0.8.16 Slice 0 gates; **Slice 5 implements** | 2026-07-08 |
| `dev/adr/ADR-0.8.16-onnx-embedder-backend.md` | **Cross-vendor ONNX embedder backend (`OrtBgeEmbedder`)** (SIGNED, HITL coreyt 2026-07-08 — Slice-0 gate; Slice 70 report amendment). A new `impl Embedder` in `fathomdb-embedder` behind the `onnx-embedder` feature, whose report-bearing caller uses `EmbedderChoice::CallerWithDeviceResolution` so `OpenReport` records the final ONNX session outcome, reaching AMD ROCm / Intel OpenVINO / Windows DirectML that candle cannot. `ort` built `load-dynamic` (native lib dlopen'd at runtime via `ORT_DYLIB_PATH`; `cargo build`/`check` never download/link). Device/provider resolved from `FATHOMDB_EMBED_DEVICE` at construction (R-ONNX-2). The thin default build + in-library 1-bit query path are unchanged. Includes the candle↔ONNX equivalence-measurement plan (R-ONNX-3 → feeds 0.8.18 #5 tolerance); ONNX-equivalence measurement AC minted. Cross-links: `dev/design/0.8.16-slice-0-f9-onnx-design.md` §5-6, `dev/tools/onnx/README.md` | 0.8.16 Slice 0 gates; **Slices 10→15 implement** | 2026-08-17 |
| `dev/adr/ADR-0.6.0-cli-scope.md` | CLI scope = two-root operator surface (`recover` lossy / `doctor` bit-preserving); Option B (`search`/`get`/`list` application query) rejected. Amended 2026-06-06 (Slice 34): scopes the 0.8.0 `doctor dump-mutations` op-store read-back IN under `doctor` as a `dump-*` diagnostic (NOT Option B; application query verbs still require a re-open) | 34 (amendment); reference | 2026-06-06 |
| `dev/adr/ADR-0.7.0-vector-binary-quant.md` | Binary-quant + f32 rerank recall floor (0.90). **40/GA-2 amendment (AC-075, ◆ B-1):** floor now GATED on the real-embedder eu7 measured on the pre-fusion VECTOR STAGE (ANN-quantization fidelity in isolation), NOT the RRF-fused `search()`; synthetic `ac_013b` demoted to report-only; no numeric change; cites GA-1 | 40/GA-2 amends § 2 pt 4 + status | 2026-06-08 |
| `dev/adr/ADR-0.6.0-*.md`, `ADR-0.7.0-*.md`, `ADR-0.7.1-*.md` | Prior-release ADRs (typed-write boundary, CLI scope, error taxonomy, etc.) | reference (e.g. typed-write boundary preserved by 25) | (tree) |
