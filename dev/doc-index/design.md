# DOC-INDEX detail — `dev/design/`

> Long-form per-doc notes for this area of the doc tree. The thin map at
> `dev/DOC-INDEX.md` links here; **every path listed below also has its own row in
> `dev/DOC-INDEX.md`** (path + a ≤120-char purpose clause) — this file carries the
> full slice-history / decision-record prose that DOC-INDEX.md compresses away.
> Update this file (not DOC-INDEX.md) when you want to add narrative detail; update
> BOTH files when you add/rename/materially change a doc (DOC-INDEX.md row +
> this file's row, same closing commit — mirrors DOC-INDEX.md's own rule).

## `dev/design/` — design notes + ADR-adjacent specs

| Path | Purpose | Owning slice / AC | Last-touched |
|------|---------|-------------------|--------------|
| `dev/design/README.md` | Design-notes index | — | (tree) |
| `dev/design/chunking-strategy-and-test-guidance.md` | Verbatim research and experiment-design capture: competitor mechanisms, late chunking, and retrieval-specific test implications | performance experiments | 2026-08-15 |
| `dev/design/fathomdb-data-plane-architecture-v1.md` | **Superseded architecture v1.** Reviewed historical baseline retained with a pointer to v2.1. | 0.8.25 history | 2026-09-02 |
| `dev/design/fathomdb-data-plane-architecture-v2.md` | **Active architecture v2.1.** Defines the multi-release data-plane destination and narrowed 0.8.25 profile. | 0.8.25 Slice 7 | 2026-09-02 |
| `dev/design/gpu-eval-activities-policy.md` | **Policy — repo MUST use the 3090s for eval/embed activities when there is room** (standing HITL mandate, 2026-07-05): any GPU-acceleratable repo-internal activity (eu7 fidelity harness seed/re-embed, corpus re-embeds, eval sweeps, CE reranking, embedding/rerank-heavy dev/CI) MUST run on `cuda:0`/`cuda:1`; CPU is permitted ONLY for two narrow compatibility probes, not as a general fallback. Grounds the 0.8.14 Slice-20 eu7 policy correction (the fidelity gate itself must run **CPU same-backend** vs its 0.896 baseline — a GPU eu7 run cross-backends it) | 0.8.14 Slice 20 (eu7 policy) | 2026-07-05 |
| `dev/design/free-threaded-python-value-lift-and-experiments.md` | **Free-threaded Python (PEP 703) for FathomDB — value, lift, experiment plan** (`status: analysis/pre-decision`): raised out of the 0.8.8 pyo3 `0.24.1→0.29.0` bump (0.28+ defaults `#[pymodule]` to FT, forcing the `gil_used` declaration). 0.8.8 ships **`gil_used = true`** (safe); FT *support* (`gil_used = false`) is a separate later data-gated campaign. Key thesis: engine already runs GIL-free (every call `detach`/`allow_threads`; `ReaderWorkerPool` + real `Mutex`/`OnceLock`, `frozen` pyclasses) → **direct throughput value (V2) likely small; the real value (V1) = ecosystem non-poisoning** (a `gil_used=true` module re-enables the GIL **process-wide** on FT interpreters, killing FT for consumers Memex/Hermes/OpenClaw). Lift = MEDIUM, dominated by an FT CI lane + non-abi3 per-version `*t` wheel matrix, NOT engine code. Five experiments (EXP-FT-1 GIL-held fraction / -2 multi-thread scaling / -3 poisoning-cost / **-4 concurrency-safety HARD GATE** / -5 wheel feasibility) with objective data + eval criteria + a decision rule. **Appendix A — Survey** (web research): FathomDB ≈ HF `tokenizers` (Rust core + `RwLock` + GIL released → shipped FT cleanly), NOT polars (GIL-assumed bindings → crashes/deadlocks); benefits caveat confirmed (already-GIL-free native work gains ~0); pydantic-core's pre-fix segfault = why EXP-FT-4 is a hard gate. Cross-links: `dev/plans/plan-0.8.8.md` §1 (Migration approach) + R-SEC-1; scheduled as the **0.8.15** EXP-FT ladder (master §6 F-5; productization a 0.8.15-readout contingency → 0.8.16/0.8.17) | 0.8.15 ladder (pyo3 dep @ 0.8.8) | 2026-06-27 |
| `dev/design/0.8.18-slice-20-publish-pipeline.md` | **0.8.18 Slice 20 — #11-full full publish pipeline (implementation design)** (`status: IMPLEMENTED`). Reconciles `.github/workflows/release.yml` against the #11-full checklist. The scoped-platform-name portion is superseded by `dev/adr/ADR-0.8.20-unscoped-npm-platform-packages.md`; Linux AArch64 native artifacts are added by `dev/adr/ADR-0.8.20-linux-aarch64-native-artifacts.md`; the thin main package remains on a non-`latest` dist-tag while coverage is partial. | 0.8.18 Slice 20 | 2026-08-02 |
| `dev/adr/ADR-0.8.18-full-publish-pipeline.md` | **#11-full full publish pipeline** (ACCEPTED, HITL SIGNED 2026-07-09). Slice 20 = reconcile + gap-fill + harden the existing 549-line `release.yml` (NOT author); D5 critical path = `x86_64-unknown-linux-gnu`; harden for the 0.8.20 OPP-12 breaking-pair publish; drop the false atomicity claim; gate the tag matrix + npm platform contract. Implemented by `dev/design/0.8.18-slice-20-publish-pipeline.md`. | 0.8.18 Slice 0 gates; Slice 20 implements | 2026-07-09 |
| `dev/design/0.8.16-slice-0-f9-onnx-design.md` | **0.8.16 Slice-0 design package — F9 importance/confidence ranking + cross-vendor ONNX embedder** (`status: SIGNED` — HITL ratified all 5 Slice-0 decisions 2026-07-08). Freezes the release DoD (R-F9-1..4 / R-ONNX-1..3 / R-X-1 / R-GATE), records the two design decisions (F9 ranking/lifecycle honoring the Slice-35 deferred ADR; trait-local ONNX backend, amended by Slice 70 to use strict device policy and `EmbedderChoice::CallerWithDeviceResolution`), maps F9 onto the OPP-12 `rankable` projection-role contract (the F-18/F-20 forward-compat constraint), and specifies the candle↔ONNX equivalence-measurement plan (§5, feeds 0.8.18 #5 tolerance). Backing ADRs: `dev/adr/ADR-0.8.16-f9-importance-confidence-ranking.md`, `dev/adr/ADR-0.8.16-onnx-embedder-backend.md`. Cross-links: `dev/plans/plan-0.8.16.md`, `dev/plans/runs/STATUS-0.8.16.md` | 0.8.16 Slice 0 | 2026-08-17 |
| `dev/design/0.8.18-slice-5-vector-equivalence-probe.md` | **0.8.18 Slice 5 — #5 vector-equivalence probe (SHIPPED surface)** (`status: IMPLEMENTED`, RED/GREEN TDD; schema-migration slice 18→19 → **HITL-gated landing**). Open-time self-check that re-embeds the 45 committed probes with the LIVE backend and refuses dense retrieval on divergence beyond the FROZEN D4 floor (**P1** mean-centered `embedding_bin` sign-flip count = **0 exact**; **P2** un-centered L2 ε = **1e-5**; BOTH asserted). New `_fathomdb_embed_probe` table (schema step-19; **UN-centered f32 references ONLY — never persists P1 bits**, recomputed from the live mean). Divergence ⇒ **degraded-open** (`Engine::open` succeeds, `OpenReport.dense_disabled=true`) + a query-time **`EngineError::VectorEquivalenceMismatch`** at the single choke point `search_inner_with_stats` covering EVERY vector-dependent arm; the explicit `search_text_only` FTS-only path stays serviceable. Py/TS leaf classes (`VectorEquivalenceMismatchError`, code `FDB_VECTOR_EQUIVALENCE_MISMATCH`) + `dense_disabled` observability + refusal counter. ADDITIVE-ONLY (identity refusal unchanged, R-VEQ-5). Authority: `dev/design/0.8.18-slice-0-vector-equivalence-publish-design.md` §U1, `dev/adr/ADR-0.8.18-vector-equivalence-self-check.md` | 0.8.18 Slice 5 | 2026-07-09 |
| `dev/design/0.8.22-slice-19-join-index.md` | **Landed in PR #207 — Slice 19 canonical FTS join indexes.** Schema step 26 adds unconditional node/edge `write_cursor` indexes, proves planner shape with `EXPLAIN QUERY PLAN`, measures write cost without a host-specific performance guarantee, and rejects a semantically unsafe edge-FTS SQL `LIMIT`. | 0.8.22 Slice 19; integrated in `main` | 2026-08-08 |
| `dev/design/0.8.22-slice-21-projection-state.md` | **Landed in PR #207 — Slice 21 truthful projection runtime state.** Extends readiness with `unavailable` and specifies a safety-gated, atomic boot graft only after identity/equivalence checks; no per-projection progress fiction or failed-terminal reopens. | 0.8.22 Slice 21; integrated in `main` | 2026-08-08 |
| `dev/design/0.8.22-slice-22-projection-status.md` | **Landed in PR #207 — Slice 22 pure projection-status read.** Defines a distinct status result for safety-approved runtime availability, corpus-wide dense readiness per declared projection, and unsupported kinds, without configuration side effects. | 0.8.22 Slice 22; integrated in `main` | 2026-08-08 |
| `dev/design/0.8.22-slice-23-text-limit-prefix-stability.md` | **Landed in PR #209 — Slice 23 direct FTS result-prefix repair.** Defines the direct `search_text_only` prefix contract, fixed bounded candidate collection, real-database RED→GREEN proof, and the narrow boundary before the 0.8.23 hybrid semantics decision. | 0.8.22 Slice 23; integrated in `main` | 2026-08-08 |
| `dev/design/0.8.23-gpu-artifacts.md` | **0.8.23 Linux CUDA artifact design** (`status: PROPOSED`): defines the CPU-compatible CUDA binary contract, trusted GPU runner evidence, driverless CPU proof, and release workflow gates. | 0.8.23 Slices 0/5 | 2026-08-10 |
| `dev/design/0.8.23-embedding-configuration-feedback.md` | **0.8.23 embedding configuration feedback design** (`status: PROPOSED`): requires typed, actionable SDK feedback when a body-bearing graph write needs embedding but no usable embedder is configured; distinguishes that configuration condition from retryable scheduler work and records the Memex characterization matrix. | 0.8.23 Slice 10 | 2026-08-10 |
| `dev/design/0.8.23-memex-integration.md` | **0.8.23 Memex integration design** (`status: PROPOSED`): freezes typed `FDB_EMBEDDER_REQUIRED` and readiness-report feedback, client/engine responsibility boundaries, graph-on/vector-off query-policy disposition, privacy constraints, and the public-SDK controls needed to classify graph traversal and process lifecycle findings. | 0.8.23 Slice 10 | 2026-08-10 |
| `dev/design/earp.md` · `earp-slice-*-design.md` | **As-built EARP platform design.** Developer-side, off-wheel evaluation contract plus S0–S10 designs for strict inputs, real-SDK runners, honest metrics, projections, comparisons, and budget-guarded arms. | EARP developer harness | 2026-08-08 |
| `dev/design/0.8.2-m1-multihop-harness.md` | **0.8.2 / M1 multi-hop answer-accuracy harness — design + FROZEN pre-registration (AMENDED 2026-06-16; re-frozen 0-rev2 2026-06-19)** (`status: decision-ready`): MuSiQue-Ans 2/3/4-hop datasets, the 5 arms (bm25 / passage-dense / fused(RRF k=60) / fused+rerank / ppr-fusion), the **fixed comparator = `fused-RRF`** (re-frozen at 0-rev2 from the earlier `fused+rerank`, data-justified by the $0 bridge-vs-answer diagnostic; never per-hop max), **`MATERIAL_F1_LIFT = 0.04`** (re-frozen from 0.02/0.03), the **same-answerer** protocol, ablations + bias controls, the budget plan (flash-lite cheap-validate gate + answerer-call model), the dated frozen pre-registration block (primary endpoint = **paired pooled ≥3-hop ΔF1 vs fused-RRF** with bootstrap CI; per-hop = secondary feeding a **trend gate** that vetoes only on a significantly negative slope; EM CI-banded; unanswerable-set carries confident-wrong; decision rule frozen as `eval/m1_decision_rule.py::decide(material, em, trend, confident_wrong, power_ok)`; **whole-rule power-sim** plan) + the falsifiable Slice 5/15/20 AC list. **Applied at Slice 20 → verdict NO-GO.** Cross-links: `dev/plans/plan-0.8.2.md` §4, `dev/roadmap/0.8.2.md`, `runs/0.8.2-m1-FINDINGS.md`, `runs/0.8.2-slice-0-prereg-methodology-review.md` | 0.8.2 Slice 0-rev2 | 2026-06-19 |
| `dev/design/0.8.3-mem0-parity.md` | **0.8.3 Slice-0 design + FROZEN pre-registration (Mem0-parity resolution)** (`status: decision-ready`): the **two-level measurement** (external `FathomDB − {Mem0, Graphiti/Zep, naive-RAG}` gate decides *done*; internal corrected-fused = attribution only), the **near-parity band ε = 0.05** + power-sized per-class N (paired **MDE ≤ ε**), the **$0-probe pass/fail** criteria (15a embedder-ceiling PRIMARY; 15b D2 content-at-scale proxy), the **eu7-break fork** + **surpass-option protocol**, the **ADJ-2 F5/tunable-`b` scope ruling** (conditional promotion to Slice 25, pending HITL ratification), the aggregate ≤ \$30 three-priced-run budget, and the falsifiable S5/10/15/20/25/30 AC list. Rule frozen as `eval/decision_rule_083.py::decide_083(external_per_class, eu7_recall, latency_ok)`. Cross-links: `dev/plans/plan-0.8.3.md` §4, `dev/roadmap/0.8.3.md` §1c, `runs/0.8.x-capability-status-report.md`, `dev/adr/ADR-0.8.1-deferred-f5-fielded-fts-bm25f.md` | 0.8.3 Slice 0 | 2026-06-21 |
| `dev/design/0.8.5-ce-rerank-slice-design.md` · `dev/plans/0.8.5-ce-rerank-alpha-expose-slice.md` | **0.8.5 (EXP-0) — expose tuned CE-rerank α / pool_n / ce_score** (codex-reviewed design memo + slice spec): promote the measured-provisional Mem0-parity config (α≈1.0, narrow pool — previously reachable only via the eval `ReblendRerankAdapter` `score=0` trick) into the native `rerank()` + `Engine.search` surfaces as an **opt-in knob**; default behavior (α=0.3, pool_n=rerank_depth) is **ranking-field byte-identical** (C6 factoid guard preserved). Adds `ce_score = sigmoid(ce_logit)` per candidate (None outside the reranked pool). Locked decisions D1–D6; α=1.0 opt-in for the agentic-answer/memory path. Does NOT change the default or wire any caller to α=1.0. Cross-links: `dev/design/0.8.1-slice-10-reranker-design.md` Decision 5, `runs/0.8.3-rerank-tune-FINDINGS.md` | 0.8.5 (EXP-0) | 2026-06-25 |
| `dev/design/0.8.12-coverage-probe-and-value-test.md` | **0.8.12 Slice-0 pre-registration** (2026-07-01): §A the `$0` LLM-free coverage probe (Slice 5) that gates the Slice-10 priced extraction + folds in the OPP-6 EXP-COV academic/`$0` arms — frozen `personal.gold` slice (60 docs / 246 ent / 163 edges), arms C0-floor heuristic + current-ELPS-baseline scored `$0` from pre-computed `claude-haiku-4-5` outputs + C1-gliner(if installs) + ceiling re-measure; fact-coverage metric (entity/edge recall + precision guard, per doc-kind class) + GATE-OPEN/CLOSED decision rule; §B the consolidation lossiness-vs-latency value test (Slice 20) pre-registered SHIP-ON/STAY-OFF rule. C2/C3/C4 priced arms HELD (HITL go). Cross-links: Memex `OPP-6-experiments.md`, `ADR-0.8.12-consolidation-recency-provider.md`, `runs/EXP-COV-results.md` | 0.8.12 Slice 0 authors; Slice 5/20 execute | 2026-07-01 |
| `dev/design/0.8.8-explain-and-telemetry-adr.md` · `dev/plans/runs/0.8.8-explanation-fieldset-ratification.md` | **0.8.8 EXP-OBS — `Explanation` payload + telemetry/gold schema ADR** (`status: RATIFIED 2026-06-27`, 6-owner negotiation): the opt-in `search_explained` → `SearchResult.explanation: Option<Explanation>` **sidecar** carrier (HITL-ratified; `SearchHit` byte-identical, zero-cost when off), the ratified field set (`Explanation`/`QueryTrace`/`PerHitExplain`, all `#[non_exhaustive]`; raw `fused_score` not normalized; `ce_active`/`recency`; 3 arms), §A.4 Q1–Q3 closed, and the Part B telemetry/gold amendments (id-contract = stable `logical_id`; no `source_id`; `+query_id`/`+id_space`; gold-row-shape align) carried into Slice 15/20. Landed: Slice 5 engine keystone + R-OBS-1 golden / R-OBS-2-COV tests. Cross-links: `dev/plans/plan-0.8.8.md`, `runs/STATUS-0.8.8.md`, `runs/0.8.8-carrier-decision-hitl-brief.md` | 0.8.8 Slice 0/5 | 2026-06-27 |
| `dev/design/0.8.8-telemetry-design.md` | **0.8.8 Slice 15 — telemetry capture mechanism** (`status: PROPOSED`): the mechanism the §B schema didn't pin — opt-in `enable_telemetry` (off-by-default atomic fast-path → zero cost), the gated capture point in the search body, JSONL append-only sink (no egress), `last_telemetry_query_id()` surfacing (D1a), and the `record_feedback` API. Schema/id-contract/no-`source_id`/no-egress gates locked by the ratification artifact §B. Implemented in Slice 15 (engine + pyo3/napi/Py/TS); real-gold consumer = Slice 20 `eval/gold_capture.py` + `eval/frozen_candidate_scorer.py`. | 0.8.8 Slice 15/20 | 2026-06-28 |
| `dev/design/slice-40-gate-restructure-and-ga.md` | **Slice 40 / GA-2 gate-restructure + GA verification design memo** — the ◆ B-1 correction (eu7 → ANN+ vector-stage fidelity via a test-only engine seam), AC-075/076 designs, CI split, release-doc plan, the falsifiable bar | 40/GA-2 | 2026-06-08 |
| `dev/design/slice-0-adr-plan.md` | Slice 0 design memo — one-paragraph per ADR: BYO-LLM extraction protocol, IR-measure/eval design (R0+R2), G11 graph substrate migration | Slice 0 | 2026-06-12 |
| `dev/design/ir-recall-measure.md` | **IR/agentic evidence-recall MEASURE (definition + methodology)** — Phase-1 Claude↔codex consensus-signed: primary measure = Evidence Recall@K (strict all-of + graded), unit = atomic evidence unit (gold-set schema additive over eu8 `ground_truth_queries`), K-ladder @5/@10(headline)/@20/@50, per-class structure, retrieval-mode matrix (4/5 runnable; +reranker stub, +graph 0.8.1), pooled qrels methodology + pinned-versioned-corpus PRINCIPLE. Numbers all TBD; eu7/AC-075 fidelity gate untouched. Input to IR-1 Ph2–4 + IR-2 | IR-eval (IR-1 Phase 1) | 2026-06-08 |
| `dev/design/orchestration.md` | **Cross-release runbook** (release-independent METHOD) — roles (§1), state spine (§1.5), **preflight gate (§1.6)**, spawn/review/cherry-pick/fix-N (§2–9), worktree cleanup (§11), context discipline (§12), **failure & recovery catalog (§13)**, **reviewer topology & empirical resolution (§14)** | binds every slice | 2026-06-26 |
| `dev/agent-harness-bootstrap-prompt.md` | **Method on-ramp (portable distillation)** — the plan(to-do)-vs-runbook(method) split, 7 core principles mapped to repo files, artifact set, witness-state + release-DoD skeletons, kickoff-HITL set, the operating loop, bootstrap order, hard-won notes; single-agent degraded mode | method on-ramp (cross-release) | 2026-06-26 |
| `scripts/preflight.sh` | **Orchestrator preflight gate** — run before every worktree spawn: stale-base guard (HARD), dependency-CLOSED gate, mid-operation/disk checks; emits JSON, exits non-zero on HARD fail (`orchestration.md §1.6`) | binds every spawn | 2026-06-26 |
| `dev/design/bindings.md` | SDK bindings spec; §1 governed SDK surface invariant (allowlist + parity, AC-074); §10 recovery-unreachability (BYTE-FROZEN) | 25 rewrote §1/§13/§14; §10 preserved | 2026-06-04 |
| `dev/design/0.8.0-agent-memory-fit.md` | Agent-memory gap ladder (G0–G12) + §7 read-verb HITL questions | scope source for 0.8.0 | 2026-06-02 |
| `dev/design/0.8.0-v05-feature-triage.md` | v0.5.x feature triage (ship/defer/drop) | scope source of truth | 2026-06-02 |
| `dev/design/0.8.0-slice-5-G1-design.md` | Slice 5 design memo — structured `SearchHit` shape, per-branch score, dedup/order, step-11 tokenizer migration + re-tokenization, X1/X2/X3 plan | 5 (G1) | 2026-06-02 |
| `dev/design/slice-10-design.md` | Slice 10 design memo — G9 RRF fusion (formula/tiebreak, dropped-knob note) + rerank seam, G10 `SearchFilter` + 3-way shape-sentinel, G12-recency flag, score-comparability note, test plan | 10 (G9/G10/G12-recency) | 2026-06-03 |
| `dev/design/slice-15-g0-design.md` | Slice 15 design memo — G0 canonical-identity substrate: step-12 additive `ALTER` (exemption-marker rationale), tombstone-then-insert supersession + same-txn atomicity, NULL-on-legacy-rows rule, `row_cursors` semantics, op-store cascade, reserved Slice-16 shadow reconciliation, test plan (active-identity re-scoped to `logical_id` alone by Slice 31) | 15 (G0 keystone); amended by 31 | 2026-06-05 |
| `dev/design/slice-15-design.md` | Slice 15 design memo — G11 edge enrichment + BYO-LLM ingest + edge projectability: step-14 exact migration SQL (5 additive nullable cols on `canonical_edges` + `search_index_edges` FTS5), `fathomdb.extract.v1` NDJSON-over-stdio protocol (hello→ready→extract→result/error), invalidate-not-accumulate SQL keyed on `(from_id,to_id,kind)`, `derive_logical_id` sha256 formula, FTS Option B (`search_index_edges`), `SoftFallbackBranch::TextEdge`, Python/TS SDK API shapes, `ExtractorError` hierarchy, fixture harness | 15 (G11 BYO-LLM keystone) | 2026-06-13 |
| `dev/design/slice-31-identity-rescope-design.md` | Slice 31 design memo — re-scope active-row uniqueness to `logical_id` ALONE on both tables (Decision 5, HITL-SIGNED 2026-06-05): fixes the kind-change identity-fork, resolves Slice 30 read.get [P2], re-keys G8; amend step-12 in place (no SCHEMA_VERSION bump) | 31 (G0 re-scope) | 2026-06-05 |
| `dev/design/slice-20-g8-design.md` | Slice 20 design memo — G8 dangling-edge flag-and-count: cross-row post-row-insert EXISTS pass inside `commit_batch`'s open tx (why not `validate_write`), logical_id-alone probe + step-12 partial-index hit argument, legacy-NULL endpoint consequence, flag-and-count default (strict-mode deferred to band 22), test plan | 20 (G8/F10) | 2026-06-03 |
| `dev/design/slice-20-design.md` | Slice 20 design memo — G5/G6 graph traversal: BFS CTE SQL (ADR conflict resolution: t_invalid filter), visited-string cycle guard, direction parameterization (Outgoing/Incoming/Both), G6 compose (G1+G5), dedup strategy (search-score wins), hard cap 50 + depth >3 rejection, Python/TS API shapes, EXPLAIN gate strategy | 20 (0.8.1 G5/G6) | 2026-06-13 |
| `dev/design/slice-25-conformance-design.md` | Slice 25 design memo — governed-surface conformance rewrite: the allowlist (core 5 + `read.*` 4), the four falsifiable properties (P1 allowlist-membership · P2 cross-binding parity · P3 recovery-denylist empty-intersection · P4 no-raw-SQL), the honest-green plan for not-yet-live `read.*`, touch-points | 25 (AC-057a→AC-074) | 2026-06-04 |
| `dev/design/slice-27-rust-allowlist-design.md` | Slice 27 design memo — Rust-facade governed-surface allowlist/parity pin (Q5=BIND-RUST): the curated 17-type governed allowlist vs the 20 operator-seam re-exports (CLI-only), the three properties (P1 positive-allowlist · P2 parity-in-intent-not-identity · P3 recovery-denylist-absence exact-match), AC-074 Rust measurement, mirrors the best-effort `no_recovery_surface.rs` style (no Rust runtime introspection) | 27 (AC-074 Rust half) | 2026-06-05 |
| `dev/design/slice-27-fix1-operator-gate-design.md` | Slice 27 fix-1 design memo — feature-gate the operator/recovery seam off the default Rust facade (HITL Option B, codex [P1]): the `operator` cargo feature, the 12 gated methods + 6 private helpers + 20 re-exports, cfg-gated `compile_fail` method-absence doctests (feature-unification-safe), AC-050c green via scoping `tests/` out of the public-API scanner | 27 fix-1 (AC-074 method-level + AC-050c) | 2026-06-06 |
| `dev/design/slice-27-fix2-engine-test-gate-design.md` | Slice 27 fix-2 design memo — restore `cargo test -p fathomdb-engine` (default) under the operator gate (codex [P1]): 13 pure-operator engine test targets get `[[test]] required-features=["operator"]`; the mixed `pr2b_mean_recompute.rs` keeps its non-operator carve-out test via per-fn `#[cfg]`; `corpus_graph` is not-operator (doc-mention only); no method un-gated | 27 fix-2 (engine default test build) | 2026-06-06 |
| `dev/design/slice-30-design.md` | **0.8.1 Slice 30 design memo** — R3 graph-retrieval arm: ELPS conformance gate (8 golden cases, orphaned==0), fuse_three_arms() 3-arm RRF (RRF_WEIGHT_GRAPH=1.0), use_graph_arm bool flag threaded through Engine::search_reranked → search_inner → ReaderRequest::Search → read_search_in_tx, BFS over canonical_edges (depth≤3, cap 50, temporal filter t_invalid IS NULL OR t_invalid > now, synthesized-node penalty kind='unknown'→score*=0.3), SoftFallbackBranch::GraphArm, SDK parity (Python use_graph_arm=False + TypeScript useGraphArm?), t_valid/t_invalid in SDK write path; SCHEMA GATE: temporal_fallback col deferred to SCHEMA_VERSION 15 | 30 (R3) | 2026-06-13 |
| `dev/design/slice-33-cursor-hardening-design.md` | Slice 33 design memo — op-store `read.collection`/`read.mutations` cursor + limit hardening under a genuine ~1M-row log: EXPLAIN before (id-PK walk) / after (step-13 `(collection_name,id)` index, index-driven, no SCAN / no temp B-tree), clamp/cursor edge cases (`limit==0`, over-MAX clamp, negative/past-end `after_id`, unknown collection), accretion guard (index-only ⇒ no marker), test plan; no SDK signature change | 33 (G3/F4-READ) | 2026-06-05 |
| `dev/design/slice-34-cli-op-store-readback-design.md` | Slice 34 design memo — CLI-only `doctor dump-mutations` op-store read-back: the scope call (diagnostic dump over the mutation log, NOT the rejected Option-B application query surface; ADR amendment), verb shape + default `--limit` 1000, `--json` envelope (`verb`/`collection`/`after_id`/`limit`/`count`/`rows`/`next_after_id`), exit map {0,70,71} (empty page = 0), inline-serialize rationale (no `OpStoreRow` re-export ⇒ facade type-set unchanged), RED test plan; no engine/schema/SDK change | 34 (F4-READ / reserved-gap-34) | 2026-06-06 |
| `dev/design/slice-35-design.md` | Slice 35 design memo — G4 filter grammar: `read.list(kind, predicates?, limit)` with closed `Predicate` enum (`JsonPathEq`/`JsonPathCompare{Gt/Gte/Lt/Lte}`) and `ScalarValue{Text/Integer/Bool}`; path allowlist `[$.status, $.priority, $.tags, $.kind, $.created_at]`; injection-safe parameterized `json_extract` SQL; AND-composition; `InvalidFilterError` for non-allowlisted paths; `SearchFilter` unchanged; Python + TypeScript SDK wrappers; deferred F9/F5 ADR framing | 35 (G4 filter grammar) | 2026-06-13 |
| `dev/design/slice-5-design.md` | **0.8.1 Slice 5 design memo** — R0 CDF runner architecture: 4 arm implementations (bm25_text direct FTS5 / dense vector-stage-only / rrf_fused production RRF / oracle_union), found@K aggregation, K=1000 fanout override rationale, CE latency benchmark approach, ADR §2.3 schema alignment, latency-field list-vs-object discrepancy note | 0.8.1 Slice 5 | 2026-06-13 |
| `dev/design/slice-25-r2-design.md` | **0.8.1 Slice 25 design memo** — R2 end-to-end parity eval harness: the identical-answerer constraint (adapters expose only `retrieve()`, the harness owns the one `Answerer` + prompt template), query-set decision (frozen FathomDB gold = pinnable default; LongMemEval = source of the 4 memory classes, unavailable here), per-class scoring + abstention, replay-determinism, scoring function, and the environment-constraint findings (no answerer LLM / no local Mem0 backend / no memory-class gold in the frozen corpus → retrieval-only data-limited run) | 0.8.1 Slice 25 (R2) | 2026-06-14 |
| `dev/plans/prompts/0.8.1-graph-track-HANDOFF-2.md` | **0.8.1 graph-track hand-off #2 (CURRENT entry point)** — continues HANDOFF.md: the phase goal + FathomDB constraints, the tracking-file map, current state (main `0da87ab`: base e2e via airlock Batch API landed + TDD; readers re-anchored to `gemini-flash-lite`/`gemini-3.1-pro`; batch resolved = `gpt-5.4-nano` openai only), and the **outstanding-work backlog #5.1–5.7** (C1 seeding slice = budget-critical, C3 G1 AC, G0 Phase-2 + schema-gate, scale base test + judge, fused, scorer-unification, HITL decisions) + operating rules/traps | 0.8.1 graph track (entry point) | 2026-06-14 |
| `dev/plans/prompts/0.8.1-graph-track-HANDOFF.md` | **0.8.1 graph-track orchestrator continuation hand-off (#1, deep reference)** — instructs an adversarial review of the plan in FathomDB context, captures current state (G0 Phase 1 done + design-reviewed → seeding is the real blocker), the prior session's planning MISSES (tracer-first, stale-worktree trap, self-waiter death, vacuous-green tests, stats), open HITL decisions, the gated workflow, and references to **all** 0.8.1 graph files | 0.8.1 graph track (deep ref) | 2026-06-14 |
| `dev/plans/runs/0.8.1-g0-design-review.md` | **G0 Phase-1 adversarial design review** — BLOCK-1 (graph-arm seeding, not `source_id`, is the recall blocker → separate seeding slice), BLOCK-2 (traversed-edge source, not `LIMIT 1`), BLOCK-3 (measurement-accuracy tests), + CONCERNs (TS/conformance, `session_id_of`, PRE-1 product gap, node-stamping). Supersedes the G0 memo's E0a framing | 0.8.1 G0 | 2026-06-14 |
| `dev/design/slice-G0-design.md` · `dev/plans/runs/0.8.1-g0-capability-map-*.json` | **G0 Phase-1 design memo + capability map** — tracer T1–T4 results (dense via test-hook only; readers usable minus gpt-5/gemini-3-pro; embedding 4.7 sess/min); E0a/E0b/PRE-1/2/3 approach + step-16 SQL (memo partly superseded by the design review above) | 0.8.1 G0 | 2026-06-14 |
| `dev/design/0.8.1-graph-experiment-plan.md` | **0.8.1 graph/IR experiment plan (LME)** — prioritized, gated program to find the most valuable design choices: two-metrics/two-loops (LLM-free Recall@K iteration vs airlock end-to-end gates), match-mechanism-to-benchmark (community tier parked), fix-only-what-blinds-the-instrument sequencing; the **smallest-but-valid math** (per-question-haystack difficulty knob, paired-McNemar MDE table, KU=78 ceiling, ~11–17 pp per-class noise floor — **adversarially reviewed**); phases P0/P1 with deps/parallelism/gates/ACs; **cost forecast** (extraction $12–146, gate runs $7–16); G2 = HITL graph go/no-go on held-out questions | 0.8.1 graph track | 2026-06-14 |
| `dev/notes/elps-consult-3-provenance.md` | **ELPS consult #3 — `ready.provenance` (PRE-3) ANSWERED** (2026-06-14) — Memex confirms an additive OPTIONAL `ready.provenance` object is within `fathomdb.extract.v1` at `schema_version 1` (no bump/re-ratify; `ready.model` stays the authoritative cache-key; round-2 additive precedent). Also: **production ELPS does NOT truncate** (E0b is eval-harness-only); **E4 grounding-as-input-field WOULD warrant re-ratify**. Source: memex `dev/elps/FATHOMDB-CONSULT-3.md` | 0.8.1 graph track / G0 PRE-3 | 2026-06-14 |
| `dev/notes/longmemeval-leaderboard-reference.md` | **LongMemEval external leaderboard + reading notes** (original LME, NOT LME-V2) — reported end-to-end scores (Exabase 96.4, Mastra 94.87, Mem0 94.8, …) with the **reader-LLM-is-a-huge-variable** caveat (Mastra 84→95 by reader swap), the hard classes (temporal/knowledge-update/multi-session), and what the 90% target implies for our gate | 0.8.1 graph track (reference) | 2026-06-14 |
| `dev/design/fathomdb-graph-vs-mem0-zep-and-longmemeval-diagnosis.md` | **Graph implementation vs Mem0/GraphRAG/Zep + LongMemEval diagnosis** — code-grounded account of FathomDB's graph (SQLite property-graph, fact-on-EDGE, bi-temporal-aware, BYO-LLM); how Mem0 (vector+overwrite, graph removed), Microsoft GraphRAG (corpus sensemaking, Leiden community summaries, no temporal), and Zep/Graphiti (bi-temporal KG, 3 tiers, invalidate-not-delete) differ; the answer to "muddled node/edge?" (No — three separable causes: read-path `SearchHit.id=write_cursor` makes the graph arm invisible to the scorer; ~1% extraction coverage; base dense+FTS trails BM25 on conversational data) + a structural-completeness gap vs Zep; and an ordered experiment program (E0–E5) | 0.8.1 graph track (input to experiments) | 2026-06-14 |
| `dev/design/0.8.1-slice-10-reranker-design.md` | **0.8.1 Slice 10 design memo — IMPLEMENTED 0.8.2 Slice E1** — R1 CPU cross-encoder reranker: crate structure (no new crate; CE inference type lives in `fathomdb-embedder/candle_reranker.rs`, engine seam forwards the feature), pinned `cross-encoder/ms-marco-TinyBERT-L2-v2` rev `81d1926f` (2-layer BERT, hidden 128, sha256-pinned), Candle BERT+pooler+classifier `score`, lazy `OnceLock` load + ureq/sha256 cache loader, `rerank_fused` 3-arg signature, score-blend (α=0.3 × sigmoid(CE) + 0.7 × RRF-norm), soft-fallback (depth=0 or model absent → identity; feature-off = ML-free + no-network), determinism, `RECENCY_WEIGHT` fix (→0.002), `default-reranker` Cargo feature. **Slice E2** exposes the SAME CE over an ARBITRARY passage list (not engine-bound): engine helper `rerank_passages(query, Vec<(id,body,score)>, depth) -> Vec<(id,score)>` + governed pyo3 SDK fn `fathomdb.rerank(query, passages, rerank_depth)` (input `{"id","body","score"}` → reranked `[{"id","score"}]`; depth0/empty = byte-identical identity; allowlisted `rerank`) — unblocks Slice 5's `fused_rerank` comparator reranking its own fused(bm25+dense) pool | 0.8.1 Slice 10 (R1) → impl 0.8.2 Slice E1; standalone API Slice E2 | 2026-06-18 |
| `dev/design/agent-memory-impl-strategy.md` | Slice shapes / impl strategy for the gap ladder | 5/10/15/20/30 shapes | 2026-06-02 |
| `dev/design/retrieval.md` | Retrieval pipeline design; Slice 20 adds exact direct-text FTS rank-boundary collection, full-sort fallback, and writer durability fit. | 5/10; 0.8.24 Slice 20 | 2026-08-24 |
| `dev/design/projections.md` | Projection model | 5/15 | (tree) |
| `dev/design/migrations.md` | Migration model (forward-only, accretion guard; index-only additive steps need no marker) | 5/15/33 (schema 10→11→13) | 2026-06-05 |
| `dev/design/vector.md`, `ann-index-vec0.md` | Vector store / vec0 ANN index | 10/15 | (tree) |
| `dev/design/op-store.md` | Operational mutation store (incl. the Slice 30 `read.collection`/`read.mutations` read-back contract: reader-pool DEFERRED-tx, `ORDER BY id`, mandatory limit ≤ ~1M cap, after-id cursor; Slice 33 index-driven pagination via the step-13 `(collection_name,id)` index + clamp/cursor edge contract; Slice 34 notes the CLI `doctor dump-mutations` operator diagnostic over the same seam) | 30/33/34 (`read.collection`/`read.mutations`) | 2026-06-06 |
| `dev/design/engine.md`, `lifecycle.md`, `scheduler.md`, `recovery.md`, `errors.md`, `embedder.md`, `embedder-decision.md`, `release.md`, `perf-gates.md`, `perf-regression-detection.md`, `0.7.0-vector-quant-pack1.md`, `0.7.1-EU-6-FIX-*.md` | Engine/lifecycle/scheduler/recovery/error/embedder/release/perf design notes | per-slice as touched | (tree) |
