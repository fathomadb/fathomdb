# DOC-INDEX detail — `dev/plans/`

> Long-form per-doc notes for this area of the doc tree. The thin map at
> `dev/DOC-INDEX.md` links here; **every path listed below also has its own row in
> `dev/DOC-INDEX.md`** (path + a ≤120-char purpose clause) — this file carries the
> full slice-history / decision-record prose that DOC-INDEX.md compresses away.
> Update this file (not DOC-INDEX.md) when you want to add narrative detail; update
> BOTH files when you add/rename/materially change a doc (DOC-INDEX.md row +
> this file's row, same closing commit — mirrors DOC-INDEX.md's own rule).

## `dev/plans/` — plans + live state

| Path | Purpose | Owning slice / AC | Last-touched |
|------|---------|-------------------|--------------|
| `dev/plans/plan-0.8.23.md` | **Current 0.8.23 release plan.** Planning Slices 0–6 supply the hygiene/preparation workplan; later work remains explicitly commissioned. | 0.8.23 Slices 0–6 | 2026-08-15 |
| `dev/plans/release-state-0.8.23.json` | **Current single writer.** Names the live plan/board and owns the release ladder, landed witnesses, and next work. | 0.8.23 release state | 2026-08-15 |
| `dev/plans/runs/STATUS-0.8.23.md` | **Current live state board.** Read only after resolving it through the release-state JSON. | 0.8.23 release state | 2026-08-15 |
| `dev/plans/plan-0.8.24.md` | **Draft 0.8.24 release plan.** Defines prework Slices 0–7 and the proposed feature-slice ladder without creating a live release-state record or authorizing implementation. | 0.8.24 prework | 2026-08-23 |
| `dev/plans/0.8.24/prework/slice-0-environment-and-infrastructure.md` | **Slice 0 baseline.** Records release-worktree setup, CI/runner/registry/benchmark evidence, and owner decisions still required before platform work. | 0.8.24 Slice 0 | 2026-08-23 |
| `dev/plans/0.8.24/prework/slice-1-{design-review,library-sweep}.md` | **Slice 1 review and sweep.** Records feature allocation and dependency/dependabot evidence without changing manifests or automation. | 0.8.24 Slice 1 | 2026-08-23 |
| `dev/plans/0.8.24/prework/slice-2-{design-review,cruft-review}.md` | **Slice 2 review and inventory.** Classifies repository material and proposes narrow documentation/tooling follow-ups without modifying content. | 0.8.24 Slice 2 | 2026-08-23 |
| `dev/plans/0.8.24/prework/slice-3-{product-doc,architecture}-drafts.md` | **Slice 3 draft CRUD.** Disposes 0.8.24 planning labels and conditionally proposes product/architecture updates without editing canonical records. | 0.8.24 Slice 3 | 2026-08-23 |
| `dev/plans/0.8.24/prework/slice-5-verification-adequacy.md` | **Slice 5 verification review.** Maps every retained planning requirement to observed or proposed proof, distinguishes structural checks from target evidence, and assigns proof gaps without changing tests. | 0.8.24 Slice 5 | 2026-08-23 |
| `dev/plans/0.8.24/prework/slice-6-proposal-register.md` | **Slice 6 proposal register.** Scores every prework and feature proposal, preserves no-change findings, and awaits explicit HITL rulings before Slice 7 is finalized. | 0.8.24 Slice 6 | 2026-08-23 |
| `dev/plans/0.8.24/prework/slice-4-architecture-alignment.md` | **Slice 4 alignment.** Grounds each feature in current seams and routes only verified architecture/documentation gaps to its owning slice. | 0.8.24 Slice 4 | 2026-08-23 |
| `dev/plans/0.8.20-0.9.0-PROGRAM-SEQUENCING.md` | **Program schedule-of-record (THE master).** Current allocation, release sequencing, and safety rules from 0.8.20 through 0.9.0. | Program Steward (keeps true) | 2026-08-10 |
| `dev/plans/0.8.6-0.8.16-PROGRAM-SEQUENCING.md` | **Superseded historical sequencing record.** Retained decision evidence for the former 0.8.x master; no new allocation belongs here. | historical | 2026-08-10 |
| `dev/plans/prompts/0.8.x-STEWARD-HANDOFF.md` | **Program Steward hand-off — role/mandate (canonical).** Opening prompt for a program-scope Steward session: keep the master true, reconcile drift, place + sequence cross-cutting work, **commission** execution (do not implement, never hand-drive a release ladder), be the truthful propose-first HITL interface, trust-git-not-narration. | Program Steward (entry point) | 2026-06-27 |
| `dev/plans/prompts/0.8.x-RELEASE-ORCHESTRATOR-HANDOFF.md` | **Release Orchestrator hand-off — sibling role (NOT the Steward).** Per-release entry point (`/goal complete 0.8.z`) that gates one release's slice ladder; commissioned by the Steward. Renamed 2026-06-27 from `0.8.x-PROGRAM-STEWARD-HANDOFF.md`; generic mechanics valid, release-specific body partly 0.8.3/0.8.4-stale (pending a release-agnostic refresh). | Release Orchestrator (per-release) | 2026-06-27 |
| `dev/plans/0.8.0-implementation.md` | **Authoritative slice contracts** (objective/subagents/lifecycle/success per slice 0–40); Slice 15/20 active-identity language re-scoped to `logical_id` alone by Slice 31 | the plan itself | 2026-06-05 |
| `dev/plans/0.8.0-plan.md` | **Mod-5 ladder + reserved-gap policy + Immediate-Next-Slice pointer + Slice-0/5/10 CLOSED blocks** | 0 authors; every slice advances the pointer | 2026-06-03 |
| `dev/plans/runs/STATUS-0.8.0.md` | **Live state board** (nine §12.5 sections + X1/X2/X3 column + witness + harness contract) | 0 authors; every slice updates at close | 2026-06-03 |
| `dev/plans/prompts/PLAN-TEMPLATE.md` | **Per-release plan authoring template** — fill-in `plan-<release>.md` skeleton (mod-5 ladder, R-* acceptance, X1/X2/X3 DoD, override/duplication register, behavior-change register, decisions/open-Q) + the plan-authoring checklist that defeats plan-level under-spec | authors every plan-<release>.md | 2026-06-26 |
| `dev/plans/prompts/0.8.0-SLICE-TEMPLATE.md` | **Per-slice prompt template** — self-contained implementer prompt (worktree/stray-edit discipline, RED→GREEN→X1/X2/X3, closure output.json) + the anti-under-spec authoring checklist | authors every slice prompt | 2026-06-03 |
| `dev/plans/prompts/0.8.0-slice-*.md` | Self-contained per-slice subagent prompts | per slice | (per slice) |
| `dev/plans/prompts/0.8.22-slice-19-join-index.md` | **Landed in PR #207 — Slice 19 canonical FTS join-index plan.** Adds unconditional `canonical_nodes(write_cursor)` and `canonical_edges(write_cursor)` indexes in schema step 26, planner-shape tests, and measured write-cost evidence. It expressly excludes an edge-FTS SQL `LIMIT` because filtering/fusion occur after candidate selection. | 0.8.22 Slice 19; integrated in `main` | 2026-08-08 |
| `dev/plans/prompts/0.8.22-slice-21-projection-state.md` | **Landed in PR #207 — Slice 21 truthful projection-runtime-state plan.** Makes no-runtime dense readiness `unavailable` and adds a safety-gated, atomic boot graft for eligible stranded vector work, with RED→GREEN tests, all-SDK parity, and independent review. | 0.8.22 Slice 21; integrated in `main` | 2026-08-08 |
| `dev/plans/prompts/0.8.22-slice-22-projection-status.md` | **Landed in PR #207 — Slice 22 governed projection-status read plan.** Adds a pure read across Rust/Python/TypeScript for runtime availability, per-projection dense readiness, and unsupported kinds, separate from declaration and configuration delta. | 0.8.22 Slice 22; integrated in `main` | 2026-08-08 |
| `dev/plans/prompts/0.8.22-slice-23-text-limit-prefix-stability.md` | **Completed Slice 23 direct FTS result-prefix repair plan.** Requires a bounded, caller-limit-independent direct-text node candidate path, real-database Rust/Python/TypeScript RED→GREEN proof, public-contract updates, measurement, and FIX-n review closure. | 0.8.22 Slice 23; integrated in `main` | 2026-08-08 |
| `dev/plans/earp-foundation.md` | **As-built EARP foundation plan.** S0–S10 requirements, acceptance criteria, independent design review, and RED/GREEN execution record for the developer-only evaluation platform. | EARP developer harness | 2026-08-08 |
| `dev/plans/runs/0.8.22-slice-23-design-review-20260808.md` | **Approved Slice 23 design review.** Records three P2 findings, FIX-1, and an independent no-P1/P2 re-review; its completed implementation landed in PR #209. | 0.8.22 Slice 23; integrated in `main` | 2026-08-08 |
| `dev/plans/runs/0.8.22-slice-23-pickup-review-20260808.md` | **Approved Slice 23 pickup review.** Re-verifies engine/test anchors for the direct-FTS repair; the resulting implementation landed in PR #209. | 0.8.22 Slice 23; integrated in `main` | 2026-08-08 |
| `dev/plans/runs/0.8.22-slice-23-review-20260808.md` | **Slice 23 independent review.** Records the fallback review after two incomplete Codex transcripts and the two P2 items required for FIX-1. | 0.8.22 Slice 23; integrated in `main` | 2026-08-08 |
| `dev/plans/runs/0.8.22-slice-23-fix-1-review-20260808.md` | **Approved Slice 23 FIX-1 review.** Confirms all P1/P2 are closed after strengthened cardinality fixtures and corrected Rust wording. | 0.8.22 Slice 23; integrated in `main` | 2026-08-08 |
| `dev/plans/runs/0.8.22-slice-23-documentation-correctness-review-20260808.md` | **Approved Slice 23 repeat documentation-correctness review.** Confirms the landed repair's lifecycle/status documentation, generated views, developer indexes, and public contracts have no P1/P2 drift. | 0.8.22 Slice 23; integrated in `main` | 2026-08-08 |
| `dev/plans/runs/0.8.22-documentation-correctness-review-20260808.md` | **0.8.22 final documentation-correctness review.** Records the initial P1/P2 findings, FIX-1 through FIX-3, the final-FIX-1 review, and independent no-P1/P2 approvals; landed with PR #207. | 0.8.22 completion documentation gate | 2026-08-08 |
| `dev/plans/runs/0.8.0-slice-*-output.json` / `-review-*.md` | Per-slice closure artifacts + promoted codex verdicts | per slice | (per slice) |
| `dev/plans/runs/0.8.0-slice-6-tokenizer-experiment-*.md` | **Slice 6 (B2) FTS5 tokenizer latency experiment** — measured config×tier sweep + engine A/B proving the tokenizer is latency-neutral; recommends tiering AC-012 (10k binding) per the AC-072/073 precedent | Slice 6 (B2) | 2026-06-07 |
| `dev/plans/0.8.1-plan.md` | **0.8.1 mod-5 ladder** + reserved-gap policy + X1/X2/X3 DoD + Immediate-Next-Slice pointer (graph traversal + temporal-edge graph retrieval + reranker + eval + filter; R0–R4) | 0.8.1 Slice 0 authors; every slice advances the pointer | 2026-06-12 |
| `dev/plans/0.8.1-implementation.md` | **0.8.1 authoritative slice contracts** (Slices 0–40; critical path + parallelization; footprint invariant; BYO-LLM ingest) | the plan itself | 2026-06-12 |
| `dev/plans/runs/STATUS-0.8.1.md` | **0.8.1 live state board** (§12.5 sections + X1/X2/X3 column) | 0.8.1 Slice 0 authors; every slice updates at close | 2026-06-12 |
| `dev/plans/prompts/0.8.1-MASTER-ORCHESTRATOR-HANDOFF.md` | **0.8.1 orchestrator hand-off** — roles, ladder spawn order, the per-slice decision loop (TDD + codex §9 + board-in-one-commit), HITL gates, footprint invariant | orchestrator | 2026-06-12 |
| `dev/plans/prompts/IR-C-byo-llm-extraction-harness-memex.md` | **BYO-LLM extraction-harness brief** for Memex — the `fathomdb.extract.v1` Extraction Provider Protocol FathomDB proxies graph construction to (0.8.1 Slice 15 implements) | 0.8.1 Slice 15 | 2026-06-12 |
| `dev/plans/prompts/README.md` | **`dev/plans/prompts/` archive README** (DOC-HYGIENE-1 T2/7, new) — states the archive-in-place convention + why (~120 prompt paths cross-referenced from ~140 files, so relocation breaks references), that the great majority of the ~216-file directory is closed-line one-shot execution prompts, and lists the handful of live entry-point prompts an agent should actually read (`0.8.x-STEWARD-HANDOFF.md`, `0.8.x-RELEASE-ORCHESTRATOR-HANDOFF.md`, `DOC-HYGIENE-1-HANDOFF.md`, `LIBRARY-BUMP-STEWARD.md`, `LIBRARY-BUMP-ORCHESTRATOR-TEMPLATE.md`, `PLAN-TEMPLATE.md`) | DOC-HYGIENE-1 T2/7 | 2026-07-24 |
| `dev/roadmap/0.8.2.md` | **0.8.2 roadmap** — M1 multi-hop answer-accuracy harness; **✅ CLOSED, verdict NO-GO (HITL-signed 2026-06-19):** ppr_fusion ΔF1 −0.0405 vs fused-RRF (n=300), CI upper < +0.04; passage_dense strongest. Redirect 0.8.3 → index-key enrichment | 0.8.2 | 2026-06-19 |
| `dev/plans/plan-0.8.2.md` | **0.8.2 slice plan (as-built)** — M1 slice contracts (0 design+pre-register → 4 corpus → 5 baseline bar → 10 graph build → 15 PPR-fusion KEYSTONE → 20 verdict; +E1/E2 reranker engine slices); OUTCOME banner = NO-GO | 0.8.2 | 2026-06-19 |
| `dev/plans/plan-0.8.7.md` + `dev/plans/runs/STATUS-0.8.7.md` | **0.8.7 GPU embedder (OOB) — plan + live status (COMPLETE).** Historical device seam (cpu/cuda/cuda:N/metal) + `embed-cuda`/`embed-metal` feature chain ALREADY LANDED (`02297cb3`, in the 0.8.5 baseline); 0.8.7 added the pure parser + 7 env-grammar unit tests (R-GPU-1), validated `embed-cuda` on an RTX 3090, and recorded the measured speedup (per-doc **15.6×**, batched **93×**, best-path bulk **~46×**; CPU↔CUDA 1-bit codes identical, 0/6144 sign-bit diff). Slice 70 retires that grammar for embedding in favor of strict `auto`, `cpu`, or `cuda:N`; its parser remains reranker-only. Default CPU build byte-identical (R-GPU-2). Harness = `fathomdb-embedder/examples/gpu_speedup.rs` | 0.8.7 Slices 0/5/10/40 | 2026-08-17 |
| `dev/plans/plan-0.8.9.md` + `dev/plans/runs/STATUS-0.8.9.md` | **0.8.9 CI-integrity micro (OOB) — plan + live status.** **$0** CI/test-harness work (no library query-path change). Slice 0 audit found most of the plan's premises **already fixed** at 0.8.0 Slice 40+ → the deliverable is an **honest gate map** (`dev/design/perf-gates.md`), not a fabricated pass. Landed: **R-PG** ac_013b off the synthetic floor (report-only `RECALL_FIDELITY_INFO`; asserting verdict lives in `eu7_real_corpus_ac.rs`) + `recall_gate_predicate.rs` RED-confirmed catch; **R-037** AC-037 `netns-deny-egress` durably on the `ubuntu-22.04` `security` job + `check-netns-deny-egress-catch.sh` (offline catch + live-netns demonstrate, proven IN CI); **R-050c** `fetch-depth: 0` so removal-detect resolves `v0.6.1`; **R-DEP** npm overrides (markdown-it 14.2.0 / js-yaml 4.2.0, `npm audit`=0) + dependabot.yml npm `/` coverage; **R-BOOT** bootstrap un-mask (httpx `# type: ignore` + drop `--quiet`/`>/dev/null` masking). Two **external** blockers (NOT 0.8.9 scope): repo-wide markdownlint debt (`verify`/lint) escalated to steward, and the 0.8.8 pyo3 macOS/Windows link error. | 0.8.9 Slices 0/1/5/10/15/40 | 2026-06-28 |
| `dev/plans/runs/0.8.16-slice-15-candle-onnx-equivalence.md` | **0.8.16 Slice 15 — candle↔ONNX numeric-equivalence measurement (R-ONNX-3)** (`status: MEASURED + RECORDED`, not enforced). The candle↔ONNX Δ on a 45-probe set, CPU-vs-CPU, CLS pooling, both loading the same pinned `bge-small-en-v1.5` weights: **cosine mean/median = 1.000000000**, **sign-flip rate 0.0** (0 of 17280 bits), max-abs-delta ~1.2e-7. This is the input the 0.8.18 #5 vector-equivalence tolerance is calibrated against; documents the interim **same-backend build-and-read** discipline as the guard until then. Cross-links: `dev/adr/ADR-0.8.16-onnx-embedder-backend.md` §3, `dev/design/0.8.16-slice-0-f9-onnx-design.md` §5 | 0.8.16 Slice 15 | 2026-07-08 |
| `dev/plans/plan-0.8.14.md` + `dev/plans/runs/STATUS-0.8.14.md` | **0.8.14 Substrate & recall (the schema-migration release) — plan + live orchestrator board.** Spine EXP-S (#2 kind-tagged coexisting-index substrate) → F5 (#16 fielded BM25F), one coordinated `SCHEMA_VERSION` 15→17 migration; #17 filter-grammar struck (shipped 0.8.11). **Label-only** (manifests stay 0.8.9). Ladder `0 → 5 → 10 → 20 → 25 → 40` (15 void). Slices 5/10/20/25 CLOSED; Slice 40 = release readiness. **R-X-1 py-first deferral (intentional):** EXP-S (`row_kind`) + F5 (`search_index_v2`/BM25F) are engine-internal → NO new Py/TS SDK verbs (non-vacuously green). The module-level embedder helper `embed_batch_cls` is **Python-only** (`fathomdb-py` + `__init__.__all__` + `_fathomdb.pyi`; ABSENT from `fathomdb-napi`/TS); TS parity is a tracked deferral (the `Engine.embed` verb IS at Py↔TS parity). eu7 (R-GATE) satisfied on the D6 no-op basis; grown-corpus floor re-baseline → TC-5 | 0.8.14 Slice 0 authors; every slice updates | 2026-07-05 |
| `dev/plans/plan-0.8.12.md` + `dev/plans/runs/STATUS-0.8.12.md` | **0.8.12 Memory-quality plumbing — plan + live orchestrator board.** Mod-5 ladder `0 → 5 → 10 → 15 → 20 → 40`, tracks coverage (5→10) ∥ consolidation (15→20). #6 ELPS coverage (eval-gated by OPP-6 EXP-COV) + #7 consolidation/recency provider (OPP-2 on the OPP-8 seam). **Label-only** (manifests stay 0.8.9). Slice 5 folds in the OPP-6 EXP-COV academic/`$0` arms (HITL 2026-07-01). STATUS carries the frozen DoD (R-COV-1..3 / R-CON-1..3 / R-X-1), the `$` ledger, per-slice X + codex §9 columns. Renamed from `plan-0.8.10.md` at the 2026-07-01 forward renumber | 0.8.12 Slice 0 authors; every slice updates | 2026-07-01 |
| `dev/plans/runs/EXP-COV-results.md` | **OPP-6 EXP-COV Phase-A `$0` results (discharges the parked OPP-6 eval).** Per-class fact-coverage table (entity/edge recall + precision guard, per doc-kind) for C0-floor heuristic + current-ELPS-baseline (scored `$0` from pre-computed `claude-haiku-4-5` outputs) [+ C1-gliner if installed] on the frozen `personal.gold` slice; per-corpus relevance-ceiling re-measure; the GATE-OPEN/CLOSED recommendation for the Slice-10 priced run (HARD-STOP #1). No raw licensed payloads. Pre-registration: `dev/design/0.8.12-coverage-probe-and-value-test.md` §A | 0.8.12 Slice 5 | 2026-07-01 |
| `dev/plans/runs/0.8.2-m1-corpus-manifest.json` | **M1 corpus manifest artifact** — pinned `musique_hash` (sha256), per-hop counts (2/3/4), answerable vs unanswerable counts, materialized-corpus path, acquire command; consumed by Slices 5 ∥ 10 | 0.8.2 Slice 4 | 2026-06-17 |
| `src/python/eval/m1_graph_build.py` (+ `tests/test_m1_graph_build.py`) | **M1 per-question graph build** (Slice 10) — reproduces the Slice-4 corpus, reuses `graph_arm_recall.extract_graph` (Qwen3.6-27B Airlock vLLM batch, $0) to extract entities + fact-edges per MuSiQue **paragraph** (incremental cache), loads into a FathomDB engine with **per-question-namespaced** entities + **BODY-LESS** edges (dodges the `edge_fact` vector-projection scale bug), then **coverage-verifies** read-only (`verify_embed_db` pattern — every sampled question ≥1 entity node, tables populated, no edge body). PPR arm (Slice 15) seeds from this graph + the per-paragraph cache | 0.8.2 Slice 10 | 2026-06-17 |
| `dev/plans/runs/0.8.2-m1-graph-coverage-n300.json` | **M1 graph coverage artifact** (Slice 10) — pinned to `musique_hash 3cff37fd…` + extractor model id; per-question entity/edge counts, coverage = questions-with-non-empty-graph / total, median entities & edges, body-less-edge confirmation, `priced_api_calls=0` over the n=299 (300-requested, hop-stratified) answerable sample | 0.8.2 Slice 10 | 2026-06-17 |
| `src/python/eval/m1_baseline.py` (+ `tests/test_m1_baseline.py`) | **M1 strong-baseline harness — THE BAR** (Slice 5 + fix-2): `load_musique` (asserts pinned `musique_hash`), the 4 baseline arms (in-harness BM25 + in-harness **bge-small-en-v1.5** passage-dense (pure-numpy forward, **CLS-pooled** — the correct pooling) + RRF **k=60** fused + the **LIVE engine CE** via `fathomdb.rerank` = fixed comparator), SQuAD EM/F1 stratified per-hop(2/3/4) + pooled ≥3-hop + unanswerable confident-answer rate, **identical-answerer** protocol (one answerer, same top-K across arms; optional concurrent answer phase), per-question paired records for the Slice-20 bootstrap; `retrieval_recall()` returns both `recall_at_k` (per-passage) + `all_bridges_present_at_k` (multi-hop-correct binary, added fix-2). Dense/fused arms are in-harness (justified: canonical `.so` has `default-reranker` but not `test-hooks` + no production vector-kind surface). **Slice-15 prerequisite:** PPR arm MUST use the same CLS space | 0.8.2 Slice 5 + fix-2 | 2026-06-18 |
| `dev/notes/0.8.12-cpu-embedder-defect-blocks-dense-eval.md` | **Env finding (0.8.12 EXP-COV-1)** — the installed CPU embedder `.so` is pathologically slow/stalling (~13s/700-tok doc; stalls @1500% CPU) and has no `embed-cuda`/GPU build ⇒ CE-rerank + dense arms UNRUNNABLE ⇒ the coverage→outcome sufficiency read can't test the "above ~0.571 ceiling" question. Blocks ANY dense/CE eval in this env; fix = a GPU (`embed-cuda`) build; pairs with 0.8.14 GPU-rerank. Priced extraction + `$0` gold-scored census were unaffected | 0.8.12 EXP-COV-1; ties to 0.8.14 | 2026-07-02 |
| `dev/notes/0.8.2-bge-cls-mean-engine-bug.md` | **Engine bug note — CandleBgeEmbedder defaults to Mean pooling for a CLS model** (flagged Slice 5 fix-2, codex §9 [P2]): `CandleBgeEmbedder::new()` sets `pooling: Pooling::Mean`; `bge-small-en-v1.5` is a CLS-pooled model (`1_Pooling/config.json pooling_mode_cls_token:true`); BGE docs warn Mean causes "a significant decrease in performance". Fix NOT done here (changing the default touches stored-vector compat → embedder-identity change workflow → separate slice). The harness **CLS** pooling is CORRECT; stored engine vectors are in the wrong space. Includes Slice-15 prerequisite note | 0.8.2 Slice 5 fix-2 | 2026-06-18 |
| `src/python/eval/m1_power_sim.py` (+ `tests/test_m1_power_sim.py`) | **M1 whole-`decide()`-rule power simulation** (Slice 5, design §4 amendment 4): maps measured baseline F1/EM variance + effect shape (flat-positive+0.03 / monotonic / inverted-U) → **P(GO)** through the **real frozen `decide()`** (imported, not redefined); `required_n()` sizes the pooled ≥3-hop cell for **P(GO) ≥ 0.8 under flat-positive**. Paired-difference noise modelled from baseline variance via `rho` (logged); deterministic (seeded) | 0.8.2 Slice 5 | 2026-06-18 |
| `src/python/eval/m1_baseline_run.py` | **M1 baseline runner** (Slice 5) — `cheap`/`pilot` CLI: hop-stratified deterministic sample (pooled ≥3-hop answerable cell = power-sim variance source), `CostTrackingAnswerer` (captures proxy token usage → \$ = tokens × documented price table), runs the power sim from the measured variance, **HARD CAP N≤100** enforced, **never runs the full priced pass** (gated on HITL \$ go-ahead). Concurrent answerer phase (`--workers`) makes a 100-Q reasoning-model pilot tractable | 0.8.2 Slice 5 | 2026-06-18 |
| `dev/plans/runs/0.8.2-slice-10-graph-build.md` | **Slice 10 note** — what the graph build delivers (per-question, body-less edges), the granularity choice (per-paragraph for HippoRAG passage→entity membership), the sampling rule (hop-stratified, deterministic, `log()`-ed), the asset-divergence finding (asset already body-less at 92ed09e), and the Slice-15 hand-off (engine + cache paths, both gitignored) | 0.8.2 Slice 10 | 2026-06-17 |
| `src/python/eval/m1_ppr.py` (+ `tests/test_m1_ppr.py`) | **M1 PPR-fusion arm — the graph mechanism KEYSTONE** (Slice 15, $0): HippoRAG-style lexically-seeded Personalized-PageRank over the Slice-10 preserved per-question graph (`build_entity_graph` — nodes = entities ∪ relation endpoints, **body-less** edges, stable sorted order; membership = the `entities` list only so seeds/IDF/df are deterministic), **lexical IDF-weighted seeds** from the BM25 top-`K` (`seed_weights`, `log((N+1)/(df+0.5))` suppresses hub drift — NOT self-seeding), ONE deterministic `networkx.pagerank` pass (`α=0.85`, fixed `max_iter=1000`/`tol=1e-6` sized for near-bipartite oscillation), passages ranked by summed PPR mass then **RRF-fused with BM25 at k=60** (`retrieve_ppr` → `ppr_fusion`). Parallel arm — leaves `m1_baseline.retrieve_arms` byte-identical; `add_ppr_fusion_arm` is the Slice-20 hook. Lexical+graph only → CLS-pooling constraint N/A. Tests pin AC-M1-15a..d (determinism / restart→1.0 collapse / IDF-live / fusion no-regression) on provable constructed fixtures | 0.8.2 Slice 15 | 2026-06-19 |
| `dev/plans/runs/0.8.2-slice-15-ppr-arm.md` | **Slice 15 note** — what the PPR-fusion arm delivers (the 4 mechanism steps), the membership decision (`entities`-list only; relation-only phrase nodes relay mass but don't score), the convergence sizing (near-bipartite oscillation → `max_iter`/`tol`; the silent-fallback trap caught RED→GREEN), the integration seam (parallel arm; `add_ppr_fusion_arm`), the 4 load-bearing properties, and the Slice-20 hand-off (config: `α=0.85`, k=60, seed_k=10, IDF scheme) | 0.8.2 Slice 15 | 2026-06-19 |
| `dev/roadmap/0.8.3.md` | **0.8.3 roadmap** — **RE-ANCHORED 2026-06-21 on the Mem0-parity RESOLUTION** (§1c): the gate is `FathomDB − {Mem0, Graphiti/Zep}` near-parity-or-better on agentic memory; D1 dense/embedder + D2 fielded enrichment are *means*. §1a/§1b carry ADJ-1..10; §1c adds two-level measurement, D0-as-spine, embedder-primary, surpass-before-signoff | 0.8.3 | 2026-06-21 |
| `dev/plans/plan-0.8.3.md` | **0.8.3 slice plan** — resolution-driven ladder `0 → 5 D0a (gold re-pin + Mem0-OSS stand-up) → 10 D0b (measure the Mem0 gap = target) → 15 $0 triage (15a embedder PRIMARY ∥ 15b D2 proxy) → 20 D1 build (CLS + chosen embedder, one re-embed/eu7, PRF if gap) → 25 D2 build (conditional) → 30 verdict + surpass package`. Two-level measurement; stop-at-parity; eu7 ≥0.90 + latency hard gates | 0.8.3 | 2026-06-21 |
| `dev/roadmap/0.8.4.md` | **0.8.4 roadmap** — **RE-ANCHORED 2026-06-21 on the GraphRAG-parity RESOLUTION**: near-parity-or-better vs GraphRAG via **S1** (FathomDB's own community-summary build) PAIRED with **G-HH-2** (measured S1-vs-running-GraphRAG head-to-head on BenchmarkQED; HippoRAG-2 multi-hop secondary). Graphiti/Zep head-to-head moved to 0.8.3. Surpass-option before the gate | 0.8.4 | 2026-06-21 |
| `dev/roadmap/0.8.5.md` | **0.8.5 roadmap** — displaced/contingent items: R3b (gate now **S1-alone**; M1 already NO-GO), portable-DB vector guard, R4 whole-doc dense, tunable-b FTS5, EU7 perf | 0.8.5 | 2026-06-19 |
| `dev/archive/README.md` | **Archive manifest** — table of relocated historical docs (path · original path · release · date · what-it-was); records the ledger-prune relocations (interface-inventory study, context-research sets, psd, reports) | ledger-prune (`scripts/repo-prune/prompts/prune-docs.md`) | 2026-06-26 |
| `dev/archive/0.8.1-roadmap-direction-20260612.md` | **Archived** 0.8.1 roadmap-direction doc (graph/filter/eu7 + R0–R4 scope) — superseded by the implementation plan triad | reference | 2026-06-12 |
| `dev/plans/runs/IR-C-roadmap.md` (+ `-analysis-dossier`, `-deep-research`) | **IR-C retrieval roadmap** (Fable-5 review): R0–R4 + per-item P(success) + the C1–C8 ceiling corrections; the decision artifact behind 0.8.1's retrieval-quality track | reference / 0.8.1 source | 2026-06-12 |
| `dev/plans/runs/IR-C-recall-cdf.json` | **R0 candidate-recall CDF artifact** — found@K for K∈{50,100,200,500,1000} × 4 arms (bm25_text/dense/oracle_union/rrf_fused) × all gold query classes (exact_fact/exploratory/…), corpus_hash-pinned to fe973fcd…; CE latency section (TinyBERT-L-2 p50=1.54ms + MiniLM-L12 p50=16.82ms). Gates Slice 10 rerank depth choice | 0.8.1 Slice 5 produces; gates Slice 10 | 2026-06-13 |
| `dev/plans/runs/IR-C-r0-findings.md` | **R0 findings** — CDF curve interpretation: arm comparison at each K, C1/C2 corrections (depth-50 rrf_fused: exact_fact=0.950/exploratory=0.510 vs depth-1000), dense plateau at K=200, recommended rerank depth K=200 for Slice 10 (R1), CE latency budget analysis vs AC-013 | 0.8.1 Slice 5 | 2026-06-13 |
| `src/python/eval/r2_parity_eval.py` (+ `eval/__init__.py`) | **R2 parity eval harness** (test-infra; NOT in the shipped wheel) — identical-answerer protocol over FathomDB / Mem0-OSS / naive-RAG; `BaseAnswerer` (Stub/Recording/LLM/Null), `FathomDBAdapter`/`NaiveRAGAdapter`(pure-Python BM25)/`Mem0OSSAdapter`(lazy, local-only), `PerClassScorer` (5 R2 classes + abstention), `R2Harness` (COR-2 corpus-hash pin asserted before any number), `run_r2_eval()` + `python -m eval.r2_parity_eval` live runner. **0.8.3 Slice 5:** `load_repin_gold` + `R2Harness.from_repin_gold` (consume the LME re-pinned gold, bypassing the COR-2 pin by design); `Mem0OSSAdapter.try_build` now builds the footprint-safe LOCAL config (`eval.mem0_local`); `gold_gen.generate_class_targeted` added (class-targeted re-pin top-up). Unit tests: `src/python/tests/test_r2_harness.py` | 0.8.1 Slice 25 (R2) + 0.8.3 Slice 5 | 2026-06-21 |
| `src/python/eval/gold_repin.py` | **0.8.3 D0a power-sized gold re-pin builder** (Slice 5) — fixes the Slice-25 **N=0** memory-class defect. Build = **LongMemEval real labelled gold** (authoritative base; `load_lme` maps `question_type`→reporting class) **+** a **local-Qwen `gold_gen` top-up** (`augment_class`, class-targeted over the LME sessions of the deficit class) for any class below `n_min`; every query `_provenance`-tagged (`lme-real`/`qwen-aug:<model>`). Emits `variance_proxy` (per-class naive-RAG recall@k outcome variance + implied N for MDE≤ε — the Slice-10 power signal) + `corpus_hash` (sha256 over the sorted LME corpus). Resilient: per-class checkpoint + LLM backoff. `$0` OFFLINE-BUILD. `python -m eval.gold_repin` | 0.8.3 Slice 5 | 2026-06-21 |
| `src/python/eval/corpus_validity.py` | **0.8.3 D0a corpus-validity guard** (Slice 5) — `validate_repin(manifest, gold_queries)`: the non-vacuous guard MISSING in Slice 25 — flags any frozen `MEMORY_CLASS` that is absent / **N=0** / below `n_min`, plus manifest↔gold count drift. Pure stdlib. Demonstrated catching N=0/under-N in RED. Unit tests in `tests/test_r2_harness.py` | 0.8.3 Slice 5 | 2026-06-21 |
| `src/python/eval/mem0_local.py` | **0.8.3 Mem0-OSS footprint-safe LOCAL backend config** (Slice 5 de-risk output) — `build_local_mem0_config(api_key=…)`: airlock-local LLM (`qwen3.6-27b`) + local `bge-small` huggingface embedder + on-disk chroma vector store; **no Mem0 cloud** (ADR §3.6). Pins the D0b stand-up config as reviewable code; no install/network at import; wired into `Mem0OSSAdapter.try_build()` | 0.8.3 Slice 5 | 2026-06-21 |
| `dev/plans/runs/0.8.3-d0a-corpus-manifest.json` + `0.8.3-d0a-memory-gold.json` | **0.8.3 D0a re-pin artifacts** (Slice 5) — the manifest pins the new LME `corpus_hash`, `per_class_gold_counts` (all four MEMORY_CLASSES ≥ `n_min`=150), `synthetic_fraction`, `per_class_variance` proxy + `power_note` (MDE≤ε needs N≫150 → Slice-10 HITL power-check); the gold file carries the `_provenance`-tagged power-sized memory-class gold consumed by Slice 10 via `R2Harness.from_repin_gold` | 0.8.3 Slice 5 | 2026-06-21 |
| `dev/plans/runs/0.8.3-eu7-bisect-report.md` (+ `0.8.3-eu7-bisect.json`) | **0.8.3 eu7 0.937→0.896 root-cause / bisect** ($0, no-LLM, no-GPU, no-build offline archaeology) — **verdict: case A (vector-path/SUT), NOT case B (CLS/embedding); corpus ruled out.** Embedder src byte-identical v0.7.2→v0.8.0 (CLS pooling is option-only `c7afbfde`, default Mean, dated AFTER the 0.896 run); vector-stage ranking unchanged (stored bits byte-identical, KNN SQL ranking-invariant per codex §9 P2, f32 rerank unchanged) → no fidelity-loss commit; leading cause = the B-1 `vector_stage_only` SUT change (0.937 = pre-correction `search()` anchor, not comparable; true vector-stage = 0.896). **Slice-20 application:** decouple CLS from eu7 (CLS = eu8/relevance lever), judge eu7 fresh vs 0.90 floor after re-embed, quant-path fork (whitening → K>192 → 2-bit) if breach. Residual (engine A/B) blocked: no pre-embedded eu7 DB. De-risks `plan-0.8.3.md` §4 Slice 20 + `0.8.3-mem0-parity.md` §5 eu7-break fork | 0.8.3 Slice 20 (de-risk) | 2026-06-22 |
| `dev/plans/runs/0.8.3-resolution-verdict.md` (+ `.json`) | **0.8.3 RESOLUTION VERDICT (Slice 30) — CLOSED AS-IS** (HITL "take results as-is", 2026-06-23): **provisional parity-or-better with Mem0 via a retrieval-PRECISION lever** (in-footprint CE-rerank at tuned blend weight α=1.0). `decide_083` strict REACHED unavailable (eu7-blocked + the α=1.0 arm `ABORTED_INCOMPLETE` n=354/606 + just-underpowered MDE 0.078>ε). On the answered cells: reranked **0.525** vs mem0 **0.323** vs baseline **0.137** = +0.21; pooled margin +0.325 [+0.274,+0.381]; paired α=0.3→1.0 +0.241. **Honest subset-bias caveat:** unanswered 42% skew to retrieval-FAILURE cells where Mem0 wins → full-606 ≈ 0.33–0.35 vs 0.323 = marginal PARITY. Accuracy is **retrieval-gated**; surpass lives in retrieval **recall** (0.8.4). **Steward rec = SHIP-AT-PARITY.** Evidence chain: `…-d0b-parity-n606.json`, `…-gap-decomposition-n606.json`, `…-rerank-accuracy-n606.json`, `…-rerank-accuracy-reblend-a1-n606.json`, `…-rerank-tune-FINDINGS.md`. Final ledger ~$38.16/$50 | 0.8.3 Slice 30 | 2026-06-23 |
| `dev/plans/runs/0.8.3-parity-forward-plan.md` | **0.8.3→parity forward plan** — the Mem0 ladder (5a CE-rerank → distractor-suppression/MMR → PRF → D2) + the **0.8.4 GraphRAG framing** (§B: S1 community-summaries + G-HH-2 head-to-head; HippoRAG parked) + cross-cutting de-risks. Grounds: gap-decomposition = retrieval PRECISION is the lever | 0.8.3 / 0.8.4 framing | 2026-06-22 |
| `dev/design/0.8.3-slice-5-design.md` | **0.8.3 Slice 5 (D0a) design + de-risk findings** — Mem0-OSS root-cause (missing install + unconfigured backend) + footprint-safe local config + **GO** for D0b; LME-real-base + Qwen-top-up re-pin plan + n_min/power-rule rationale + HITL under-power escalation; answerer-seam wiring (`load_repin_gold`/`from_repin_gold`). (Namespaced — the generic `slice-5-design.md` is the unrelated 0.8.1 R0 doc) | 0.8.3 Slice 5 | 2026-06-21 |
| `src/python/eval/decision_rule_083.py` (+ `tests/test_decision_rule_083.py`) | **0.8.3 frozen RESOLUTION decision rule** (Slice 0) — the executable pre-registration extending `m1_decision_rule.py`'s style (pure stdlib, deterministic, consumes already-computed summary stats; **no `fathomdb`/`scipy`/`numpy`/`networkx`/RNG/I-O**): `EPS_NEAR_PARITY = 0.05`, `MEMORY_CLASSES`, `decide_083` (**BLOCK** on eu7 < 0.90 / latency first → per-class power guard `mde > ε` → **REACHED** iff every class `ci_lo ≥ −ε`; `surpass_candidates` = classes `ci_lo > 0`; `below_parity:`/`underpowered:` binding-constraint), `probe_15a_pass`/`probe_15b_pass` (the §5 $0-probe criteria), and `lint_preregistration_083` (asserts the design carries every dated `frozen-field:`). Tests run under plain `pytest` (no extension build; `FATHOMDB_TESTS_NO_REBUILD=1`) | 0.8.3 Slice 0 | 2026-06-21 |
| `dev/plans/runs/IR-C-r2-eval-results.md` | **R2 eval results doc** — per-class Evidence Recall@K for FathomDB vs naive-RAG on the frozen corpus (`fe973fcd…`), the FathomDB−Mem0 / FathomDB−naive delta table (the Slice 30 go/no-go input), N per class, answerer/version, and the blockers (no answerer LLM, no local Mem0 backend, no temporal/multi_hop/knowledge_update gold in the frozen corpus → data-limited go/no-go) | 0.8.1 Slice 25 (R2) | 2026-06-14 |
| `dev/plans/0.6.x/0.7.x-*.md`, `ci-deferred.md`, `README.md` | Prior-release plans + CI-deferral ledger | reference | (tree) |
| `dev/scripts/ir_c_ce_latency.py` | **CE latency benchmark** — CPU cross-encoder latency script for R0; benchmarks TinyBERT-L-2 (~4 MB, p50=1.54ms) and MiniLM-L12 (~22 MB, p50=16.82ms) on ≥1,000 random (query, passage) pairs; patches `latency` list into `IR-C-recall-cdf.json`; requires flashrank (exits 0 with null if unavailable) | 0.8.1 Slice 5 | 2026-06-13 |

---
