# GRAPH-01 TDD evidence

## RED

Command:

```text
PYTHONPATH=src/python:. .venv/bin/python -m pytest \
  src/python/tests/test_graph_01.py -q
```

Before implementation, collection failed with
`ModuleNotFoundError: No module named 'experiments.graph_01'`.

## GREEN

The same focused test command passes the initial nine tests. The tests pin relation
admission and exact provenance, type-conflict and generic-entity rejection,
native projection writes, protected bridge ranking, candidate-depth limits,
paired bootstrap and decision boundaries, paid checkpoint/cost behavior, and
strict model-output parsing.

The first execution attempt stopped before database build or paid calls when
the pinned extraction cache exposed two previously uncovered input shapes.
The added RED cases reproduced both failures: non-ASCII and currency-symbol
entities normalized empty, and one malformed extracted relation aborted the
cohort. GREEN now preserves Unicode letter/number/mark and symbol tokens while
counting and rejecting malformed relations. The focused suite passes all 12
tests. This does not repair or infer a relation; it only prevents invalid input
from erasing valid documents or terminating the registered run.

The next execution attempt reached the paid edge audit. Claude returned the
complete object inside one `json` code fence, so the strict parser's added RED
case failed. GREEN removes only an exact single fence before the existing JSON
schema checks. Prose outside the fence and every existing structural mismatch
remain errors. The three invalid attempts cost $0.011958 and are retained in
the atomic checkpoint; no answer cells ran before this correction.

The resumed audit then exposed Airlock PII redaction of a numeric substring in
one canonical MuSiQue edge ID. The added RED case proved the prompt leaked the
canonical ID; GREEN assigns deterministic batch-local IDs such as `audit-000`
and maps judgments back after strict completeness validation. The source edge
IDs and provenance in FathomDB are not changed. The focused suite passes all 13
tests.

The completed retrieval loop then exposed one three-hop and 24 two-hop cohort
rows with no labelled supporting passage. The inherited metric contract defines
recall and complete-bridge as undefined for those rows. RED reproduced the
`None` aggregation failure; GREEN symmetrically excludes only pairs where both
arms are undefined, reports the excluded count in bootstrap output, and fails
on asymmetric undefined values. Answer evaluation retains the registered
questions.

After every measurement and paid answer cell completed, receipt generation
failed because a relative CLI config path was passed directly to
`Path.relative_to`. The final RED case reproduces that closeout-only failure;
GREEN resolves and validates the path against the repository root before
serializing it. The focused suite passes all 14 tests.

Python compilation and focused Ruff checks also pass for the two GRAPH-01
modules and the test file. The repository Markdown structural gate passes for
the contract, design, review, and status changes.
