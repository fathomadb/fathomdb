# REASON-01 treatment eligibility plan

## Outcome

Determine whether an explicit caller-selected FathomDB relationship profile is
eligible for the native HippoRAG-2 comparison. This plan does not change A0,
start the native comparison, or authorize paid answer scoring.

The candidate is `protected_multiquery_v1`. Preserved development evidence
shows that the later deep-compact union added two correct answers over protected
multi-query but lost thirteen grounded answers. Deep compact is therefore a
rejected development offshoot, not the implementation candidate.

## Plan

1. Freeze the [requirements and acceptance criteria](2026-08-30-reason-01-requirements.md).
2. Write the [caller-side design](2026-08-30-reason-01-design.md) and obtain an
   independent read-only subagent design review.
3. Resolve every blocking design finding and preserve the
   [review disposition](2026-08-30-reason-01-design-review.md).
4. Add focused tests first. Use an importable skeleton and record behavioral
   RED assertions for every contract family before implementing behavior.
5. Implement the registry, explicit relationship selection, bounded query
   expansion, protected merge, and safe execution trace; make the focused tests
   GREEN.
6. Run the 282-question selection-equivalence check against the pinned
   development artifact. This is implementation evidence, not acceptance data.
7. Freeze and preflight all 109 untouched LongMemEval-S multi-session cases by
   excluding the 24 cases used in the prior directional run. Preflight is
   zero-spend and writes no model outputs.
8. Run scoped verification and the ptrace-capable security gate. Classify
   unrelated repository-wide failures as debt, then update PROGRAM and status.
9. Return with the frozen question count, models, estimated spend, and execution
   request. Run held-out answer scoring only after separate authorization.

## Implementation and preflight result

Steps 1–7 and the status updates are complete. The focused suite passed 17
tests after recorded RED failures for the profile behavior, historical
rewrite/merge equivalence, source-empty message handling, and exact runtime
identity. The live development gate reproduced all 282 accepted selected-ID
rows.

The frozen held-out cohort contains 109 untouched LongMemEval-S multi-session
cases. Its exact 0.8.23 environment prepared and doctored 109 fresh databases,
ingested 53,488 retained messages, and drained with CUDA embedding and reranking.
The final safe external receipts are:

- `data/performance-benchmarking/locomo-multihop/runs/reason01-equivalence-20260830-04/equivalence-observations.v1.json`
  (`sha256:9bae21c7a072603ee1644ec6ce11e0469f2e1296b99da1e4f392fa860f50a0b5`);
- `data/performance-benchmarking/locomo-multihop/runs/reason01-heldout-preflight-20260830-03/heldout-manifest.v1.json`
  (`sha256:ea2bcf1b93cd820dc88d4fb20c759cdbbbfdcabe4b271b46fb27a7b73393b9cb`);
  and
- `data/performance-benchmarking/locomo-multihop/runs/reason01-heldout-preflight-20260830-03/environment/environment-preflight.v1.json`
  (`sha256:637c06d6c67aa9357750f3d998600502356c941f2f01bf87e0ad5d95c6ae25fa`).

The ptrace-capable security gate passed with zero blockers. The authorized
paired run completed on 2026-08-30 with DeepSeek V4 Pro as answerer, Claude
Haiku as judge, a $5 hard cap, atomic per-cell checkpoints, and full
`Retry-After` handling. The [result](2026-08-30-reason-01-result.md) rejects the
profile: retrieval recall improved, but answer accuracy, groundedness, and
attribution failed the frozen boundaries. Stop before native HippoRAG-2.

## Known repository debt

The full repository gate exposes two pre-existing release-control failures that
do not overlap REASON-01 or its pinned 0.8.23 runtime:

- three v0.8.20 npm-recovery workflow-alignment assertions fail; and
- `dev/release/cuda-unmerged-candidates.json` expired on 2026-08-27.

These remain release-workflow debt. They are not REASON-01 execution blockers
and do not change the standing CUDA authorization for experiments.

## Stop

Stop on design-review rejection, inability to reproduce the accepted selection
artifact, any overlap with prior answer-scored inputs, incomplete GPU/reranker
attestation, a failed scoped/security gate, or a repository failure that
overlaps the runner or pinned runtime. Do not tune from held-out outcomes.
