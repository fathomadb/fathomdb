# LOCOMO-01 — LOCOMO retrieval self-characterization

**Status:** active; Phase-A plan-only preparation integrated. Execution remains
separately authorized.

## Decision

Which FathomDB retrieval configuration is eligible for answer scoring on
conversational personal memory?

## Preparation and contract

1. Amend the dated LOCOMO plan to use this identifier and add a typed
   `experiments/configs/locomo-01/` configuration for every grid cell.
2. Pin canonical A0, turn and session provenance manifests, corpus hash,
   bootstrap seed/resamples, and the pre-registered M1 margin.
3. Implement a grid runner that records FTS, hybrid, cross-encoder, and bounded
   neighbor treatments as separate CPU, GPU, cold, and steady-state cells.
4. Require M1/M2/M4-proxy/M6/M7 output plus factoid, temporal, and
   multi-session breakdowns; run a fixed-subset dry run first.

## Exit evidence

Every complete cell has a safe receipt and external metrics artifact. A survivor
passes the paired M1 and fast-local rules; selection records class regressions
rather than hiding them in an aggregate score.
