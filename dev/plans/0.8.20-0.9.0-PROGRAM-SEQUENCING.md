---
status: ACTIVE
supersedes: dev/plans/0.8.6-0.8.16-PROGRAM-SEQUENCING.md
---

# FathomDB 0.8.20 → 0.9.0 — program sequencing

> **Schedule of record from 2026-08-10.** This replaces the obsolete
> 0.8.6–0.8.16 filename and allocation narrative. Historical evidence remains
> in the prior file, which is retained in place and marked deprecated.

## Completed releases

| Release | Disposition |
| --- | --- |
| 0.8.20 | Published; retained historical state and records. |
| 0.8.21 | Published reliability and platform foundation. |
| 0.8.22 | Published cross-platform native delivery; npm latest promotion complete. |
| 0.8.23 | Published Linux CUDA, dual-runtime, Memex-readiness, and Tegra correctness release. |
| 0.8.24 | Merged to `main`; portable Tegra distribution, installed-package proof, and streamed FTS boundary behavior are complete. |

## Active release

| Release | Scope | Sequence |
| --- | --- | --- |
| 0.8.25 | Fold the completed performance program into FathomDB's governed data-plane contracts while keeping semantic policy external. | Slice 0 history import → Slice 5 architecture → Slice 7 executable measurement classification → Slice 10 source-complete evidence → Slices 20/30 constrained graph expansion and governed filtering/pagination → Slice 40 cross-workstream validation and release evidence. |

## Forward placement

| Release | Placement |
| --- | --- |
| 0.8.26 | Deferred Windows CUDA distribution and Windows Python-SDK WAL review from 0.8.24 (`seq-258`). |
| 0.9.0 | Post-0.8.x product identity and next major release planning; no implementation is scheduled here. |

## Dependency and safety rules

- GPU packaging is Linux x86_64 only until every additional platform has an
  independent reproducible build and hardware smoke.
- CPU remains the default library runtime; CUDA is explicit opt-in and must
  not make a driverless CPU installation unusable.
- The public-repository GPU runner must remain in a workflow-restricted
  organization or enterprise runner group. Labels alone are not authorization.
- A supported-live-embedder reproduction of the Memex scheduler reports, or a
  reproducible abnormal parent-process exit, is a release blocker.
- No-embedder body-bearing graph work receives a typed SDK outcome and readiness
  report; a client may not need to infer configuration from retry exhaustion.
- Embedder availability for graph-edge projection is independent of whether a
  consumer includes vector candidates in its own retrieval/ranking policy.
- Publishing always requires a distinct explicit HITL decision, even after the
  implementation and release rehearsal are complete.
