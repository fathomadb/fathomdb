---
status: SUPERSEDED
---

# FathomDB 0.8.6 → 0.8.24 — deprecated historical sequencing record

> **DEPRECATED 2026-08-10.** The active schedule of record is
> `dev/plans/0.8.20-0.9.0-PROGRAM-SEQUENCING.md`. This file remains in place as
> historical decision evidence; do not add new release allocation here.
>
> **Historical scope.** This retained record was the former master release plan.
> It sequences two historical bodies of work into the same release line:
>
> 1. the **17 "other important aspects"** (everything that is *not* the M1/M5/M6/M7 competitor-parity
>    experiments) — the even-release backbone plus the odd-release OOB drop-ins; and
> 2. the **planner-router track** — the experiment ladder (EXP-A/M4/B′/Fr-acc/AF) and the dispatcher
>    build (EXP-Fr), which rides the **odd** releases and interlocks with the even line at two points.
>
> This file is the **dependency graph + allocation + decision record**; each release's slice ladder lives
> in its own `plan-0.8.z.md`. The planner-router *design* lives in `planner-router-psd-0.8.x.md` +
> `initial-arch-planner-router-0.8.x.md`; its *sequencing* (formerly `planner-router-sequencing-0.8.x.md`)
> is now folded in here (§5) so there is one schedule of record.
>
> **The line now runs to 0.8.24, not 0.8.18** — findings F-19/F-20 pulled OPP-12 into 0.8.19/0.8.20,
> pushed free-threading/benchmark to 0.8.21 and the deferred dependency migrations to 0.8.22, and pulled
> the F-17 scale-bound work in as 0.8.23 (soft)/0.8.24 (stated). §4's table is the current allocation;
> §6's F-1…F-32 findings are the decision record for how the span grew. **The filename is historical and
> intentionally stays `0.8.6-0.8.16-PROGRAM-SEQUENCING.md`** — it does not track the H1's span; ~39 files
> reference this exact path, so the path is stable by design and is not renamed as the plan's horizon
> extends (see `dev/plans/README.md`'s archive-in-place rule).
>
> **Relationship to the M-work.** The M1/M5/M6/M7 competitor-parity experiments remain the **primary**
> priced-token spend and are tracked in their own roadmap/plan docs. This line is the **secondary,
> mostly-$0 plumbing/mechanism + planner-router-eval track** — deliberately sequenced to run *beside* the
> M-work without contending for priced-run budget. The odd releases carry the drop-in work and the $0
> planner-router experiments so neither blocks the M-work critical path. **(NB — see F-11: the "$0
> experiments ride the odd releases as float" mechanism FAILED and is RETIRED; the experiment ladder is
> now owned work folded into 0.8.11. The experiments are still mostly-$0, but no longer unowned float.)**
>
> **Footprint invariant (binds every item here).** Library query path = CPU-only, no network/LLM,
> 1-bit binary (Hamming), deterministic. The only LLM seam is **BYO-LLM in the caller**. GPU + frontier
> models are **OFFLINE-BUILD / EVAL-ONLY**. Every slice tags each technique
> IN-LIBRARY / CALLER-SIDE-BYO-LLM / OFFLINE-BUILD / EVAL-ONLY.

---

## 1. The tracked work

### 1a. The 17 non-measure items (source-of-record)

| # | Item | Source | Footprint class |
|---|------|--------|-----------------|
| 1 | Retrieval `EXPLAIN` / observability (EXP-OBS) | `initial-arch` §6 | IN-LIBRARY |
| 2 | Kind-tagged coexisting-index substrate (EXP-S) | tree EXP-S; `0.8.x-…-strategy` §Tier-D | IN-LIBRARY (schema) |
| 3 | Embedder GPU acceleration | `design/0.8.1-embedder-gpu-and-portability.md` §1 | OFFLINE-BUILD/EVAL |
| 4 | Cross-vendor embedder portability (ONNX) | same §2 | OFFLINE-BUILD/EVAL |
| 5 | Vector-equivalence self-check | same §3 | IN-LIBRARY (open-time) |
| 6 | ELPS extraction coverage (OPP-6) | Memex ledger OPP-6 | CALLER-SIDE-BYO-LLM |
| 7 | Consolidation/recency provider (OPP-2) | OPP-2 (SIGNED, F-13) | CALLER-SIDE-BYO-LLM |
| 8 | Generalized provider protocol (OPP-8) | OPP-8 (SIGNED, F-13) | CALLER-SIDE-BYO-LLM |
| 9 | Governed-verb coupling hygiene (OPP-5) | OPP-5 (SIGNED, F-13) | IN-LIBRARY |
| 10 | Telemetry + real-gold capture (OPP-9) | OPP-9 (SIGNED, F-13) | IN-LIBRARY + EVAL |
| 11 | Release pipeline + publish (Phase 11) | `ci-deferred.md` | CI/CD |
| 12 | Un-mask the perf gates | `perf-recall-gates-masked…` memory; slice-40 design | CI/CD |
| 13 | `benchmark-and-robustness.yml` restoration | `ci-deferred.md` | CI/CD |
| 14 | AC-037 CI wiring + AC-050c cleanup | slice-40 design; 0.8.0-impl | CI/CD |
| 15 | F9 importance / confidence | Slice-35 ADR gate | IN-LIBRARY (schema) |
| 16 | F5 fielded FTS / BM25F | `ADR-0.8.1-deferred-f5-fielded-fts-bm25f.md` | IN-LIBRARY (schema) |
| 17 | Filter-grammar G4↔G10 unification (gap-37) | Slice-35; reserved-gap 37 | IN-LIBRARY |

> **OPP status (HITL, Memex-side, 2026-06-29; F-13).** **OPP-2/4/5/7/8/9/10 are SIGNED** =
> **build-authorized (default-off); adoption still gated per item** (build ≠ adopt). **OPP-4 is split** —
> the **Depth-1 seam is SIGNED**, the **product claim is held**. The single source of truth for OPP status
> is the **Memex ledger**; this master only references it (the four OPP-tagged rows above are annotated
> for convenience, not as the authority).

### 1b. The planner-router track (scheduled on the odd line)

These are **not** among the 17; they are the experiment-tree + dispatcher work, sequenced here because
two of their prerequisites — **EXP-OBS (=#1)** and **EXP-S (=#2)** — are already in the even line, and
everything else is $0 eval that rides the odd releases around those two landings. **(Float model superseded — see F-11: this ride-the-odd-line $0 eval never executed; the ladder is now owned work folded into 0.8.11.)**

| Tag | Item | Cost | Footprint |
|---|---|---|---|
| Gate-0/Gate-2 | PSD Phase-1: golden-set re-scope + oracle-routing upper bound | $0 analysis | EVAL-ONLY |
| EXP-A | Recall generation (wider candidate-gen) | $0 | EVAL-ONLY |
| EXP-M4 | Embedder-ceiling escape (swap → re-whiten → eu7 re-clear → re-tune α; OD-7) | $0 / GPU | OFFLINE-BUILD/EVAL |
| EXP-B′ | 3-stage joint tuning (`candidate_k × pool_n × α × final_K`) | $0 + $ judge | EVAL-ONLY |
| EXP-Fr-acc | Intent-classifier accuracy + asymmetric mis-route cost matrix + **router-locus decision** | $0 + small $ | EVAL-ONLY |
| EXP-AF | Agent-feedback value test (exogenous relevance signal vs internal `ce_score`) | small $ | CALLER-SIDE-BYO-LLM/EVAL |
| **EXP-Fr** | **Build the in-library dispatcher** *(the only non-OOB piece)* | engine build | IN-LIBRARY |

---

## 2. Dependency graph + hard-dependency analysis

`A → B` = A must land (or be active) before B starts, to avoid bumping a missed dependency in flight.

```text
                 ┌─────────────────────────── enablers (no upstream among the 17) ──────────────────────────┐
                 │                                                                                           │
  (#3 GPU, OOB) ─┼─► accelerates every re-embed below (soft edge, not a hard gate)                          │
                 │                                                                                           │
  #8 provider-protocol ──► #7 consolidation                                                                  │
        └────────────────► #6 ELPS-coverage (refactor onto the one protocol)                                 │
  #9 coupling-hygiene ───► #7  (HITL: migrate onto governed verbs BEFORE layering OPP-2/4)                   │
  #11-min release ──────► (makes every micro-release DoD-shippable; gates nothing technical but is a         │
                          program prerequisite for "DoD at end of each release")                            │
                                                                                                            │
  #1 EXP-OBS ═══════════► agent-relevance-signal + reward-log + transparent router (HARD; see I-1 below)     │
  #10 telemetry ────────► better eval for all M-work; head of the real-gold loop; feeds EXP-AF              │
                                                                                                            │
  #2 EXP-S (kind-tag schema) ═══► EXP-Fr in-library dispatcher (HARD; long pole; see I-2 below)             │
        ├──► #16 F5 (coordinate the two schema migrations in one release)                                   │
        └──► coverage-node / graph row-kinds (router substrate)                                             │
  #16 F5 ◄── ADR already ratified (cond. on 0.8.3 15b-proxy + Mem0 gap)                                     │
  #17 filter-grammar ───► router typed-constraint input; touches shipped G10 SearchFilter                   │
  #15 F9 ◄── Slice-35 ADR; benefits from #1 EXP-OBS to surface the signal                                   │
                                                                                                            │
  #3 GPU ──► #4 ONNX (extends resolve_device(); rework-forcing — see I-3 below)                             │
  #3,#4 ──► #5 vector-equivalence (the guard only matters once ≥2 backends can read each other's index)     │
  #11-min ──► #11-full publish matrix                                                                        │
  #13 benchmark-harness ◄── needs perf/bench substrate (benches/, scale.rs, tracing feature) — author late  │
                                                                                                            │
  PLANNER-ROUTER eval chain:  EXP-A,EXP-M4 ──► EXP-B′ ──► EXP-Fr ;  EXP-Fr-acc ──► EXP-Fr ;                  │
                              EXP-OBS+#10 ══► EXP-AF / Fr-acc-agent-signal ;  EXP-S ══► locus decision        │
```

`══►` = hard gate (cannot cross until upstream lands). `──►` = feeds/rework-forcing.

### 2a. The binding edges — physically-hard vs rework-forcing

Integrated across both tracks, the schedule is rigid at exactly these edges. **Physically-hard** = the
downstream literally cannot be built/run earlier. **Rework-forcing** = you *can* go out of order, but you
throw work away — same management burden in practice.

| # | Edge | Direction | Class | Why |
|---|------|-----------|-------|-----|
| **I-1** | 0.8.8 **EXP-OBS** (per-arm provenance + score breakdown) → odd **agent-signal / EXP-AF** (0.8.9 half, 0.8.11) | even → **odd** | **physically hard** | The agent-relevance signal judges *against* the provenance/score-breakdown + telemetry; nothing agent-feedback can run before it exists. |
| **I-2** | 0.8.14 **EXP-S** substrate → 0.8.15 **EXP-Fr** in-library dispatcher (no 0.8.13) | even → **odd** | **physically hard** | The dispatcher is in-library code over the kind-tagged indexes — no substrate, no in-library router. **This is the long pole.** |
| **I-3** | 0.8.7 **GPU device seam (#3)** → 0.8.16 **ONNX (#4)** | **odd → even** | rework-forcing | ONNX extends `resolve_device()` from #3; build #3 first or build the seam twice. *This edge breaks the "evens are self-contained" model.* |
| **I-4** | 0.8.7 (#3) + 0.8.16 (#4) → 0.8.18 **vector-equivalence (#5)** | mixed → even | physically hard-ish | The guard is meaningless with one backend — it needs ≥2 backends that can read each other's index. |
| **I-5** | 0.8.6 **#8 provider protocol** → 0.8.12 **#7/#6** | even → even | rework-forcing | Build consolidation before the unified protocol → throwaway contract + rewrite (the one edge that "bites mid-implementation"). |
| **I-6** | 0.8.14 EXP-S (slice 5) → F5 (slice 10) | within-release | rework-forcing | F5 rides EXP-S's field columns; one coordinated migration. |
| **I-7** | 0.8.16 Slice-15 (candle↔ONNX Δ) → 0.8.18 Slice-0 (#5 tolerance calibration) | even → even | **physically hard** | The vector-equivalence guard's tolerance cannot be calibrated before the cross-backend Δ it must tolerate has been measured. **Recorded retrospectively** (both releases are ✅ COMPLETE, so this edge is historical, not scheduling): it was documented in the plans but never promoted to this table. Ruled 2026-07-03 (`c6d3449a`, **M18** in `dev/design/fathomdb-memex-overall-roadmap/00-priorities-and-misalignments.md`); the paired half of that ruling — renaming the duplicate `F-11` to `F-11a` — landed in DOC-HYGIENE-2 T1c (`f9654a79`). **M18 is now fully discharged.** |

**Everything else floats.** GPU just accelerates re-embeds everywhere (soft); Gate-0/Gate-2 / EXP-A /
EXP-M4 / EXP-B′ / EXP-Fr-acc are $0 eval that can land anywhere in their window *as long as they're done
by their consumer's deadline* (§5); CI-micro (0.8.9) is only "recommended-before" GA. **(Superseded — see
F-11: the "everything-else-floats" model FAILED — unowned float was silently dropped and never ran; the
ladder is RETIRED as float and folded into 0.8.11 as owned, gated work.)**

### 2b. Can the even line be batched ahead of the odd line? — No

Taking both tracks unedited and integrated, **neither parity can be completed as a batch before the
other**, because the hard edges run **both directions**:

- **All-even-first is impossible** — edge **I-3**: even 0.8.16 (ONNX) needs odd 0.8.7 (GPU seam).
- **All-odd-first is impossible** — edges **I-1, I-2**: odd 0.8.15 (dispatcher) needs even 0.8.14
  (EXP-S); the odd agent-signal track needs even 0.8.8 (EXP-OBS).

The correct mental model is **one interleaved DAG**, not two parallel tracks. **The even/odd split is a
*labeling*** (in-band mechanism vs out-of-band experiment), **not a dependency boundary.** What must be
managed is the **landing order of ~6 real builds** (EXP-OBS, EXP-S, EXP-Fr, and the GPU→ONNX→vec-equiv
embedder chain, plus the provider protocol); all the planner-router *experiments* are $0 float scheduled
against those landings. **(Float-scheduling RETIRED — see F-11: the experiments never ran as float and are
now owned work folded into 0.8.11.)** The forced minimum interleave is:
`0.8.6 → 0.8.7 → 0.8.8 → 0.8.12 → 0.8.14 → 0.8.15 → 0.8.16 → 0.8.18`, with the $0 experiments slotted
into the 0.8.7 / 0.8.9 / 0.8.11 windows.

---

## 3. Out-of-band proposal (odd micro numbers)

OOB = $0 eval/analysis or a flag-gated/byte-stable drop-in, landable into the repo anytime, runnable by a
parallel agent track. **The odd line carries two kinds of OOB work:** (a) the original non-measure
drop-ins (#3 GPU, #12/#14 CI), and (b) the **planner-router $0 experiment ladder**, which co-resides on
0.8.7/0.8.9 (**this co-residence is SUPERSEDED — see F-11: it never executed; the ladder is folded into 0.8.11 as owned work**) and extends the odd line to **net-new slots 0.8.11 / 0.8.15 / 0.8.17 / 0.8.19** (the dispatcher build
at 0.8.15 is the one piece that is *not* OOB — it is hard-gated by EXP-S; see I-2). The odd line is
therefore **mostly-parallel but pinned to the even line at I-1 and I-2**, and pins back via I-3.

| OOB release | Non-measure drop-in | Planner-router work co-hosted *(co-host SUPERSEDED — see F-11: none of this ran; folded into 0.8.11)* | Why here |
|---|---|---|---|
| **0.8.7** | **#3 GPU embedder** (default-CPU/opt-in, byte-stable; **first**, turns 27 h re-embed → minutes) | ~~**EXP-A**, **EXP-M4** (ride the GPU), **Gate-0/Gate-2** ($0)~~ — **did NOT run (F-11); folded into 0.8.11** | EXP-M4 needs the GPU for fast sweeps; Gate-0/2 are $0 analysis on existing corpora. *(0.8.7 closed on the GPU-embedder DoD only.)* |
| **0.8.9** | **#12 perf-gate unmask + #14 AC-037/AC-050c** (CI-integrity fixes) | ~~**EXP-B′** (joint tuning), **EXP-Fr-acc** (+ agent-signal half, now OBS exists @0.8.8)~~ — **did NOT run (F-11); folded into 0.8.11** | EXP-A/M4 done; OBS landed → agent-signal can start. *(0.8.9 closed on the CI-integrity DoD only — co-hosted eval dropped.)* |
| **0.8.11** *(net-new)* | **#17 filter-grammar** (shipped-G10 SearchFilter; router constraint input) | **EXP-AF** (agent-feedback value test) · finalize Fr-acc agent-signal/VoI · **agent-side L2 router prototype** (no substrate dep) · **EXP-Fr dispatcher pre-stage** (per-intent config tuples) | Real-gold (#10) + OBS both exist; an agent-side router needs no substrate → de-risks the EXP-S KILL path. |
| **0.8.15** *(net-new; NOT OOB)* | — | **EXP-Fr — build the in-library dispatcher** · finalize the **locus decision** | First odd slot after EXP-S (0.8.14), odd line skips 13. Hard-gated: EXP-B′ ∧ EXP-S ∧ EXP-Fr-acc ∧ EXP-OBS. |
| **0.8.17** *(net-new)* | — | Router hardening · per-feature config-tuple wiring · EXP-AF productization · F-fork experiments (EXP-C/D/E) as corpus lands | Hardening / overflow, built on the 0.8.15 dispatcher. |
| **0.8.19** *(net-new)* | ~~**#13 benchmark-and-robustness harness**~~ | ~~**EXP-FT free-threaded-Python ladder (FT-1…5; $0 eval, pyo3-0.29-gated)**~~ — **moved to 0.8.21 (F-19/F-20)** | **SUPERSEDED — see F-19/F-20: this row predates the resequence and no longer describes what landed at 0.8.19.** 0.8.19 shipped as **OPP-12 Phase-1 (COMPLETE, F-24)** instead; the #13/EXP-FT content in this row landed at **0.8.21** (was "end of 0.8.x" — it is not; see §4's 0.8.19 and 0.8.21 rows for the current state). |

Items that *look* OOB but are **not** scheduled OOB, and why:

- **#4 ONNX** is structurally drop-in (new `impl Embedder`, zero engine change) but (a) low urgency — no
  AMD/Intel hardware is in play — and (b) its productionization creates the numeric-divergence hazard
  that **#5** guards, and it has a rework-forcing dep on #3 (I-3). So it lands **0.8.16**, adjacent to #5.
- **#17 filter-grammar** touches the **shipped** G10 `SearchFilter` and feeds the router's
  typed-constraint surface — **moved to 0.8.11 (F-10)**: with no EXP-S substrate dependency it rides the
  odd line ahead of the dispatcher rather than waiting for the substrate/recall release (0.8.14).
- **#15 F9** is an engine/schema feature with ranking semantics, not a flag-gated drop-in.

---

## 4. Release allocation

Even line is a near-strict sequential chain (one internal rework pin, I-5); the odd line is mostly
parallel but pinned to the even line at I-1/I-2 and pins back at I-3 — see §2b. Interlocks are marked.

| Release | Theme | Items / planner-router work | Interlocks |
|---|---|---|---|
| **0.8.21 (current)** | Reliability, platform foundation, and nested projections | **Current authority:** `plan-0.8.21.md` and `release-state-0.8.21.json`. TC-91 precedes reproducible serial verification; the checked platform-capability manifest precedes Linux aarch64 packaging. HITL `seq-242` remaps nested-source projections to Slice 60 without reopening landed CI-observability Slice 45. The historical free-threading/benchmark row below is superseded as a release ladder. | Begins after published v0.8.20; no artifact publication without manifest, package, loader, and registry-smoke agreement. |
| **0.8.6** | Foundations & shippability | #8 provider-protocol · #9 coupling-hygiene · #11-min release-enablement | feeds I-5 |
| **0.8.7 (OOB)** | GPU embedder + recall/ceiling eval | #3 · EXP-A · EXP-M4 · Gate-0/Gate-2 | source of I-3, I-4 |
| **0.8.8** | Observability & telemetry | #1 EXP-OBS (per-arm provenance) · #10 telemetry + real-gold | **source of I-1** |
| **0.8.9 (OOB)** | CI integrity + joint-tuning eval | #12 · #14 · **pyo3 mac/win `cargo test` link fix (reserved-gap, F-9)** · EXP-B′ · EXP-Fr-acc (+agent-signal half) | consumes I-1 |
| **0.8.11 (OOB)** | Agent-feedback + agent router | EXP-AF · finalize Fr-acc/VoI · agent-side L2 router prototype · **#17 filter-grammar (moved from 0.8.14)** · **EXP-Fr dispatcher pre-stage (per-intent config tuples)** · **re-review \`record_feedback\` governance (F-8b)** · **upstream experiment ladder discharge — Gate-0/2 · EXP-A/M4 · EXP-B′ · EXP-Fr-acc (folded in, F-11; release grows)** · **Library Sweep #1 — transitory, label-only pico (no manifest bump / `v*` tag / publish): contained dependency bumps + `dependabot.yml` reconcile; ships as the normal pico `0.8.11.1` (a Library Sweep IS a pico; HITL 2026-06-29) (F-12, two-tier framing F-13)** | consumes I-1; feeds the 0.8.15 dispatcher; EXP-S-KILL hedge |
| **0.8.12** | Memory-quality plumbing | #6 ELPS coverage **(eval-gated by OPP-6 EXP-COV-0..3 — build≠adopt; flat per-corpus-ceiling curve ⇒ de-prioritize, real multi_session/temporal lift ⇒ invest; F-13. The OPP-6 EXP-COV eval now RUNS under the `0.8.11.2` pico umbrella (F-14), but its OUTPUT still gates this 0.8.12 #6 invest/de-prioritize decision — the dependency is intact; only the execution venue moved)** → **RESOLVED 2026-07-02: EXP-COV-1 GPU verdict = `CEILING-ABSORBED` ⇒ #6 DE-PRIORITIZED (coverage is not the retrieval lever; entity coverage solved, edge-lift ceiling-absorbed; do NOT fund the ~$340 full-corpus relation pass; F-15)** · #7 consolidation/recency provider | **consumes I-5** |
| **0.8.14 ✅ COMPLETE** (2026-07-05, label-only `465b43ac`) | Substrate & recall features | #2 EXP-S kind-tag substrate (**3-slice spine**, SCHEMA 15→16) · #16 F5 (SCHEMA 16→17; **ships per D8 Option-C HITL override** — the R-F5-2 pre-registered gate did not clear) · gpu-rerank merged (Slice 25) · gold-id remap (F-8a) | **source of I-2, I-6; DONE (label-only, manifests 0.8.9). eu7/R-GATE closed on the D6 no-op basis (codex-verified zero vec0 rewrite). NB: the eu7 fidelity gate MUST run CPU same-backend (policy `649a8d45`); 0.90-floor re-baseline for the grown 18,472-doc corpus → TC-5** |
| **0.8.15 (OOB) ⛔ RETIRED** *(was ⏸ PARKED)* | In-library dispatcher build — **RETIRED 2026-07-29** | **EXP-Fr** · locus decision finalized | **⛔ RETIRED (HITL 2026-07-29, steward `seq-184`)** — V-7 is **CLOSED BY DECISION** (CE-reranker default-OFF, cascade OFF, as both repos had already ratified); only **1 of the 7 V-gate items passed cleanly** (`TC-123`); the dispatcher would have built a mechanism 0.8.11's Gate-2 says buys ≈0. Per-item dispositions `TC-124`; **the four §11–§13 governed-verb candidates are RESCUED to `TC-126`**. Prior park rationale retained: **⏸ PARKED (HITL 2026-07-07, F-18)** — serves no top-5 Memex need, still V-7-gated, **ZERO OPP-12 dependency (verified)**; deferring is zero-OPP-12-risk. **Next commission = 0.8.16** (Memex-value critical path). Re-open on thin-consumer demand or after `0.8.16→0.8.18→0.8.19/0.8.20-OPP-12` *(corrected per F-19/F-20 — OPP-12 landed in 0.8.x, not 0.9.x)*. (Still: **consumes I-2**; `13` HITL-forbidden — odd line skips 13.) |
| **0.8.16 ✅ COMPLETE** (2026-07-08, label-only `8c6b92aa`) | Ranking signal & embedder reach | #15 F9 (OFF-by-default, OPP-12-`rankable`-forward; SCHEMA 17→18) · #4 cross-vendor ONNX (`OrtBgeEmbedder`, opt-in `onnx-embedder` feature, **zero engine diff**; candle↔ONNX cosine≡1.0 / 0-flip) | **consumes I-3**; feeds I-4; **F-18/F-20: F9 → OPP-12 `rankable` role @0.8.20** (was @0.9.1) **→ ON the Memex-value critical path**. eu7 no-op basis (default vector path byte-unchanged). Cross-backend ONNX Δ → 0.8.18 #5. **Label-only close (HITL): manifests stay 0.8.9; publish deferred** |
| **0.8.17 (odd) ⛔ RETIRED** *(was ⏸ PARKED)* | Router hardening / forks — **RETIRED 2026-07-29** | per-feature config wiring · EXP-AF productization · EXP-C/D/E forks | **⛔ RETIRED (HITL 2026-07-29, steward `seq-184`)** — Workstreams **A and B retired outright** (B is a closed KILL already executed in code; A exists as the validated `l2-router` artifact); **Workstream C (EXP-C/D/E) RESCUED to `TC-125`**, having zero dispatcher dependency. Prior: PARKED (F-19), built on the parked 0.8.15 dispatcher |
| **0.8.18 ✅ COMPLETE** (2026-07-09, label-only `bce032a0`) | Production-safety & CI capstone | #5 vector-equivalence self-check (two-stage P1 mean-centered flip=0 / P2 un-centered L2 ε=1e-5; degraded-open `dense_disabled` + query-time typed `VectorEquivalenceMismatch`; **SCHEMA 18→19**) · #11-full publish machinery (x86_64-linux critical path; matrix-tag-gate; resilience-not-atomicity; npm split-topology; **exercised via staging**) | **consumes I-4.** Design review CLEAN (4 codex rounds); D4 floor from the U3 canary (all same-identity legs 0/17280). **DEFECT #4 open-path baseline HITL-approved** (async write invariant). eu7 **no-op basis** (default vector path byte-unchanged; #5 is an additive open-time check). **#11-full is the publish PREREQUISITE for 0.8.20's OPP-12 breaking-pair publish (proven+exercised, not yet fired).** Cross-platform matrix + OOB D3 ONNX-GPU-EP Δ (2/17280, distinct `-onnx` identity) deferred-to-follow-on. **Label-only close (HITL): manifests stay 0.8.9; publish deferred to 0.8.20.** Carry: TC-10 (#5 open-latency), TC-9 (`ort::init` GPU-EP), cross-platform follow-on orchestrator |
| **0.8.19 ✅ COMPLETE** (2026-07-10, label-only) | **OPP-12 Phase-1 — lifecycle + id** | existence axis (`pending/active/deleted/purged`; SCHEMA **19→20** existence columns ONLY) · `transition`/`purge` verbs (codex §9 caught a real GDPR-erasure leak → `secure_delete`=ON every open) · **C-2 typed `SearchHit.id` swap** (`u64→IdSpace`; `write_cursor`+`stable_id` subsumed; `to_prefixed()` == prior `stable_id` byte-for-byte ⇒ **no gold-remap**; TC-8) · **X1 py/ts parity GREEN (26/26)** · **AC-074 governed-surface delta HITL-SIGNED** (recovery_denylist unchanged) | **F-19/F-24: OPP-12 Phase-1 DONE; ladder `plan-0.8.19.md` (F-23).** Breaking surface built **label-only** (odd). **Anonymous-node surrogate `logical_id` minting DEFERRED to 0.8.20 (F-23 ruling 1a):** doc-seeded/anonymous hits stay `h:`; C-2 swap total via `l:`/`h:`/`p:` without it → `SearchHit.id` values byte-unchanged. Prereqs met: EXP-S/Cause-A landed, C-1 RATIFIED. Slice-0 X0 design-review + adversarial audit terminal. **Label-only close (HITL Option-A): manifests stay `0.8.9`; publish deferred to 0.8.20 breaking-pair.** Carry: TC-11 (0.8.20 `h:` end-state pin). See **F-24** |
| **0.8.20 (even, publish)** | **OPP-12 Phase-2 + erasure completeness + breaking-pair publish** | `ReadView`/read-modes · node-validity (`valid_from`/`valid_until`) · **projection registry (C-1 co-land)** + EAV/property-FTS · `dense_readiness` · surrogate `logical_id` minting **for registry-admitted GOVERNED entities only (F-25)** · **erasure completeness (F-25: shared row-owned projection registry + total projector; `erase_source()` SDK verb; mandatory provenance; WAL/telemetry/op-store)** · X1 parity · **coordinated breaking-pair publish** (manifests **`0.8.9 → 0.8.20`** — first real bump in the line) | **F-19: OPP-12 DONE here** (Memex `0.5.x-successor` pairs). Needs 0.8.18 #11-full matrix ✓; F9 (0.8.16) for `rankable` ✓. Contract: `OPP-12-C1-converged-contract.md`. **✅ F-23 GUARDRAIL / TC-11 DISCHARGED (HITL 2026-07-12, F-25): `h:` end-state = pin A, TERMINAL-FOREVER.** Anonymous/doc-seeded nodes stay `h:<content-hash>` **permanently**; `structural-lifecycle-contract.md §2(ii)` is **OVERRULED**; the anonymous-surrogate leg is **CANCELLED, not deferred** — a surrogate serves ONLY registry-admitted governed entities. **`plan-0.8.20.md` DE-STALED + authored 2026-07-12** (dep-migration content removed → 0.8.22). **RUBRIC-H7 GATE (TC-RUBRIC-2, folded HITL 2026-07-10): the OPP-12 co-land carries a mechanical contract-conformance check — a Pact-style `can-i-deploy` gate verifying as-built code still satisfies the ratified `OPP-12-C1-converged-contract.md` at the co-land, not humans re-reading prose — as a precondition of the breaking-pair publish; an absent-or-failing gate HOLDS the pair. Tracked `dev/steward/agent-rubric-ledger.jsonl` TC-RUBRIC-2; incorporated as plan §3 R-20-H7 / Slice 30.** **BUILD-AUTHORIZED (F-21)**; publish = separate per-`x.y.z` HITL gate. **✅ SLICE-0 X0 SIGNED + LANDED 2026-07-19 (`403eb254`, F-26) — slices 5+ unblocked**; TC-RUBRIC-5 now ENFORCED by `scripts/preflight.sh --landing`, TC-RUBRIC-7 transcripts pinned at `dev/plans/runs/codex/0.8.20/`. **Scope deltas from Slice-0 (no slot/dependency change):** (a) F-27 — the erasure audit trail MUST be DURABLE (HITL: "there must be an auditable record of deletion event"); Slice 5 additionally exempts the erasure-audit collection from `enforce_provenance_retention` (the v4 §3.6 "retained permanently" claim was VERIFIED FALSE); (b) F-28 — R-20-EU7 is CLOSED BY DECISION, not by measurement: ZERO eu7 runs authorized on any backend at any N, so Slice 40 executes NO eu7 gate; (c) F-29 — new ACs mint from AC-079 (AC-077/078 are live IR-1/IR-2 reservations), so the governed-surface delta is AC-079, not "AC-078+"; (d) F-31 — Slice 5's `SourceId` newtype must sweep `PreparedWrite` constructions in `tests/` too. **F-30 (dead publish dry-run guard, red since 0.8.14) is PLACED at Slice 40 (HITL 2026-07-24, F-32).** **✅ LADDER PROGRESS (F-32, verified from git @ `fae276b9`): <!-- BEGIN GENERATED release-state:0.8.20:master-ladder-progress -->Slices 0 (`403eb254`) · 5 (`1f8ed8bf`) · 10 (`3cfb3cda`) · 15 (`a2022957`) · 20 (`841c307b`) · 25 (`83b1c818`) · 30 (`9b3ed0e3`) · 21 (`77be504b`) · 22 (`572475f2`) · 23 (`30102ecd`) · 31 (`d0287620`) · 32 (`31d33293`) · 33 (`f02dc5b4`) · 39 (`91db34d8`) · 40 (`833a2035`) are all LANDED on `origin/main`; SCHEMA is 24; remaining ladder = none.<!-- END GENERATED release-state:0.8.20:master-ladder-progress -->** **✅ RUN AUTHORIZED (F-34, HITL 2026-07-25):** the tail runs under a standing Steward mandate — `DOC-HYGIENE-2` → 20 → 25 (**sequential**) → 30 → *(batched surface decision)* → `DOC-HYGIENE-3` → 40. **✅ TAIL RE-SEQUENCED + SCOPED (F-35, HITL 2026-07-27 — steward `seq-119`):** the remainder is **30 → 21 → 22 → 31 → `DOC-HYGIENE-3` → *(batched surface decision)* → 40**, still **sequential**. **Slice 30 ran FIRST** of the remainder (publish precondition; likeliest source of a downstream-reshaping surprise) and **LANDED 2026-07-28 at `9b3ed0e3`** — the publish precondition is **SATISFIED**. **✅ RE-SEQUENCED AGAIN (F-36, HITL 2026-07-28 — steward `seq-129`), SUPERSEDING the F-35 order:** the remainder is now **⟨TC-86 transcript-hygiene fix⟩ → 21 → 22 → 31 → `DOC-HYGIENE-3` → ⟨batched governed-surface decision⟩ → 40**, still sequential. **TC-86 runs FIRST and is CROSS-CUTTING** — no slice number, **no `R-20-xx` id, no pico label** (the `DOC-HYGIENE-3` treatment, `seq-106`). It closes a *measured* near-miss in which a codex §9 transcript arrived carrying 216 lines of raw Claude Code session JSONL from **three other projects**, bound for a tracked path in this **PUBLIC** repo; caught and redacted before landing, with `git grep` proving **zero** already-committed files carry that shape. The mechanism is structural — every §9 review runs under `--dangerously-bypass-approvals-and-sandbox`, TC-RUBRIC-7 requires the transcript be persisted under a **tracked** path, and nothing between those two steps inspects the contents — so it is fixed in the **tooling**, not with guidance. Net-new reserved-gap slices: **21 = R-20-CR** (TC-57 characterize→fix · ac_002 oracle replacement · TC-71) and **22 = R-20-VC** (TC-67 option (c) — *report*, do NOT grow the `resolve_source_type` vocabulary, Pack-1 D3 lock untouched · TC-68 fingerprint-cached equivalence probe · Slice-15b decision #18 · sqlite-vec `#99` `vec0` DELETE probe); **31 = Library Sweep #3**, which **deliberately carries NO requirement id** (TC-76 — a recorded departure from F-12's pico precedent, not an overwrite of it). The batched surface decision is taken **AT THE CLOSE OF SLICE 23** (HITL `seq-141`, superseding `seq-134`/`seq-140`), where **Slice 23 = R-20-SV** is net-new (`seq-139` — the `fts`/`vector` reject ruled 2026-07-24, plus the TC-90/TC-91 characterization owed by `seq-136`). **23 is the LAST unit that can move the governed surface** — the allowlist is SDK *command names*, and neither Slice 31 (dependency sweep) nor DOC-HYGIENE-3 (docs/tooling) can add or remove one — so the delta is FINAL there. Non-blocking for 31 / DOC-HYGIENE-3; Slice 40 depends on it (AC-079 mints pinned to the allowlist). Also ruled: **TC-74** (Slice-40 workspace gate is **serialized**, with a **non-blocking** parallel reporting arm; race-hunting → 0.8.21), **TC-75** (engine fix-round cap 3 same-finding / **10** total / Steward check-in at 6), **TC-77** (§5 band-overflow tripwire kept, *conditional* on 21/22 not spawning ≥2 further slices), **TC-78** (Dependabot `#153` held to post-publish), and **`seq-118`** (Slice 40 makes the **TC-16 determination FIRST**, before the `#11`-full rehearsal, so the rehearsal is not run against a path already known broken). **AC-079 PRE-SIGNED** (pinned to the allowlist content; any diff re-opens it — T1e gate); **§7 prereq-5 Memex co-land readiness CLOSED BY DECISION** — Memex adapts to 0.8.20's surface, **no confirmation is to be sought**, push scope stays fathomdb-only, and the "one side alone is not a release" framing no longer gates the cut. **✅ RE-SEQUENCED AGAIN (F-37, HITL 2026-07-29 — steward `seq-153`), SUPERSEDING the F-36 order:** the remainder is **31 → 32 → 33 → `DOC-HYGIENE-3` → ⟨batched governed-surface decision⟩ → 40**, still sequential. **Library Sweep #3 is RE-SCOPED from an un-enumerated manual sweep to a TOOL** and split across **three net-new-in-effect legs, none carrying an `R-20-xx` id** (the TC-76 precedent, re-affirmed for all three): **31** = requirements + acceptance criteria + design + **RED tests, no code**; **32** = the code only; **33** = **runs** the tool and produces the survey. **NO leg applies a dependency bump and none edits a manifest or lockfile inside 0.8.20** — the survey is an **INPUT to 0.8.22**. Specified at `plan-0.8.20.md` **§3a**. **Also ruled 2026-07-29: TC-76** (`seq-151` — hold `sqlite-vec` at `=0.1.7`; the 0.1.9 bump lands at **0.8.22** with `rusqlite 0.31→0.40`, and **must move with the `tc76_vec0_long_metadata_delete.rs` tripwire**) and **TC-93** (`seq-152` — publish 0.8.20 with the six Dependabot advisories **open**, no pre-publish "Slice 4x"; the advisories land at **0.8.22**). Band: the **30 band** (§5 gaps 31–39, nine slots) goes to **three of nine**; **TC-77 NOT tripped** — its trigger is a spawn from Slice 21 or 22, which does not apply. **✅ THE BATCHED GOVERNED-SURFACE DECISION IS SIGNED (HITL 2026-07-29, `seq-157`) — cite it, never re-open it, never seek it again.** The signed delta is **7 net-new allowlist members = 4 logical verbs** (`erase_source`/`eraseSource` · `read.crossed_boundary_since`/`read.crossedBoundarySince` · `configure_projections`/`configureProjections` · `read.projections`) plus the non-command types `EraseReport`/`SourceId`/`ExciseReport`/`ReadView`/`BoundaryCrossing`/`ProjectionSpec`/`ProjectionDelta`/`ProjectionRole`/`ProjectionDestructiveError`; **`recovery_denylist` UNCHANGED at the five REQ-054 names** so AC-041 is unaffected, and `excise_source` stays CLI-only and un-allowlisted. **AC-079 still mints at Slice 40**, pinned to this content — any diff re-opens the gate (T1e). ⚠ **PUBLISH IS NOT AUTHORIZED BY THIS SIGNATURE.** **Provenance correction recorded with it:** the delta is authored by Slices **5d** (`3685fdb5`), **10b** (`4524ffd2`), **15b** (`35523156`) and **15d** (`0d887f85`) — **slices 20/25/30/21/22/23 each contributed ZERO allowlist bytes**, so the shorthand "the 20/25/30 delta" is imprecise; `plan-0.8.20.md` §11 attributes it correctly. **✅ THE `_comment` RE-PIN IS DISCHARGED (`seq-164`, `c239908b`).** The four `AWAITING HITL SIGN-OFF, NOT SIGNED` markers were corrected and `scripts/governed-surface-pin.json` re-issued in the same commit; `grep -c 'AWAITING HITL SIGN-OFF'` on the allowlist now returns **0** and `check-governed-surface-pin.sh` exits 0. ⚠ Do NOT brief any agent to preserve or verify that string — it no longer exists, and an agent reporting it intact would be reporting a green it could not have observed. **✅ TC-98 RULED (a) FOR NOW (`seq-156`)** — accepted, **not endorsed**; revisit at **0.8.22** as **TC-103**. **PUBLISH remains the hard HITL gate.** ⚠ It is the only decision that **halts the run**, but it is no longer the only unruled one: **`npm-dist-tag`** was surfaced 2026-07-30 (`halts_run: false`) — the pipeline defaults to `next` and the platform matrix is linux-x64-gnu only, so `latest` would serve an incomplete matrix. `decisions.unruled` in the state file is the authority. |
| **0.8.21 (odd, OOB)** *(was 0.8.19)* | Free-threading + benchmark harness + **the `#[non_exhaustive]` build** | **EXP-FT free-threaded-Python ladder (FT-1…5)** · **#13 benchmark-and-robustness harness** · **`#[non_exhaustive]` on the public facade enums — TC-103 part (b) + the TC-122 scoping study (HITL `seq-183`)**. Built here **label-only**, because odd = OOB and 0.8.21 does not publish, so the attribute lands breaking nothing; it **PUBLISHES at 0.8.22**, per the F-19 pattern *build label-only across odd micros → publish atomically at an even micro* (exactly how OPP-12 ran: built 0.8.19, published 0.8.20). Start from **TC-122**, which is a finished per-enum study: 4 attribute-only, 2 mechanical, `EngineError` + `EngineOpenError` involved, and **5 POSTPONED** whose signed ADR D-F1 / AC-035b / `errors.md` pins the attribute would **void** rather than merely cost · **`napi 2→3` — PULLED FORWARD FROM 0.8.22 (HITL `seq-203`, 2026-07-30)**, because napi generates the npm platform packages and 0.8.21 is where the expanded publish matrix is wired and greened. Validating that matrix on napi 2 and then migrating at 0.8.22 would make the validation stale the moment it shipped; 0.8.21 is odd ⇒ OOB label-only, so a breaking binding change lands here **breaking nothing** (F-19) · **PLATFORM MATRIX — wire and green the four remaining publish targets in CI, PUBLISH NOTHING (`seq-203`)**, in increasing-risk order: **`aarch64-apple-darwin`** (Apple Silicon is the dominant developer machine for the named consumers; pyo3-mac fixed at 0.8.8/0.8.9) → **`x86_64-apple-darwin`** (same runner, near-free) → **`x86_64-pc-windows-msvc`** (0.8.9 was the FIRST green Windows in this line, so it gets its own attention). `aarch64-unknown-linux-gnu` defers to **0.8.24 or later** — it needs cross-compilation or ARM runners and is the lowest-urgency target for a local-first library. ⚠ These are the **publish/wheel** matrix at `release.yml` lines 91–105; CI **already tests** Windows and macOS (`ci.yml` `rust-windows`, `rust-macos`) · **crates.io `categories` metadata — RE-ENTERED HERE (HITL `seq-198`, 2026-07-30)** after being **DROPPED from 0.8.20**, so the registry metadata is READY for the **0.8.22 breaking publish** (see the 0.8.22 row — `seq-183`). `keywords` **stay in 0.8.20**. ⚠ **RATIONALE CORRECTED (`seq-200`, HITL re-affirmed on measured facts) — the original justification recorded here was WRONG on both halves.** `cargo package` does **NOT** validate keywords locally: Slice 39 measured cargo 1.95.0 accepting **6 keywords, a 26-char keyword, and a keyword containing a space**, all `rc=0`. And the category taxonomy **IS** checkable ahead of any irreversible step — `GET https://crates.io/api/v1/categories` returns **http 200 unauthenticated with 58 slugs** (verified 2026-07-30). So the partial-publish risk is real in principle but **fully mitigable with one unauthenticated HTTP GET**. The placement is unchanged; the reasoning is replaced. Categories stay out of 0.8.20 because **Slice 39 was already finished and green at `91db34d8`** (reopening it costs a fresh codex round while blocking Slice 40), because **categories attach per-version and a crate page reflects its newest version** so 0.8.22 restores browse-discoverability with nothing permanently lost, and because 0.8.20 already carries an unrehearsed publish pipeline, four red test suites, post-publish smokes that fail on a missing `source_id`, and an undecided Axis-E version. **⚠ CATEGORIES MUST LAND VALIDATED, NOT HAND-WRITTEN (HITL `seq-200`): slug validation against the published crates.io taxonomy ships WITH them in 0.8.21** — a natural companion to TC-131 below, and the durable version of what `seq-198` was reaching for. ⚠ 0.8.21 is an **odd micro ⇒ OOB / label-only: re-entering the metadata here PUBLISHES NOTHING** — it rides the 0.8.22 cut, exactly like the `#[non_exhaustive]` build above · **TC-131 — the commission-brief FACT-CHECKER — is BUILT HERE (HITL `seq-198`)**: a checker that parses a commission brief for **(i) file:line citations, (ii) quoted source strings, (iii) claimed grep counts** and verifies each against `HEAD`, emitting a per-claim PASS/FAIL. Natural home `scripts/`, sibling to `check-design-refs.sh`. Deferred out of 0.8.20 because only two briefs remained there and the per-brief adversarial review had caught **100 %** of the defect class; 0.8.21 can use the **four recorded failure instances** (steward `seq-195`, `seq-196`, `seq-197`) as **test fixtures** · **FIVE ITEMS PLACED HERE 2026-07-30 (HITL, steward `seq-223`, F-38): TC-134** requirement-traceability per-note triage (`seq-212` ruled the *method*; only placement was open) · **TC-135** exclude `dev/plans/runs/codex/` from the orphan-marker scan — ⚠ **this UNBLOCKS TC-RUBRIC-7**, and until it lands §9 transcripts are held **out of the tree** and the rubric is **knowingly unmet** · **TC-141** add `--no-fail-fast` to `rust-macos` **and** give Linux a full-workspace `cargo test` in `ci.yml` — ⚠ until then the Linux Rust workspace suite runs in CI **only** inside `verify` and `rust-macos` aborts at its first failure, so the CI Rust signal stays narrower than it looks · **TC-142** pin `pyright` (today `>=1.1.380`, so CI can redden overnight with **no commit**) · **the tc57 anonymous-arm `repeat_embeds` assertion**, deferred out of Slice 40 because **no anonymous-arm number exists on any platform** to calibrate it against | bumped after OPP-12 (F-19); EXP-FT needs pyo3 0.29 (@0.8.8); #13 needs perf/bench substrate |
| **0.8.22 (even) — deferred major-dependency migrations** *(was 0.8.20)* | Library Sweep — owned engine slices | ⚠ **`napi 2→3` MOVED OUT to 0.8.21** (HITL `seq-203`) — see that row; it is validated there label-only and its platform packages publish HERE · **PUBLISHES THE EXPANDED PLATFORM MATRIX** (`x86_64-linux` + `aarch64-darwin` + `x86_64-darwin` + `win32-x64`) and **FLIPS the npm dist-tag to `latest`** once all four are green (`seq-203`) · **rusqlite 0.31→0.40 + sqlite-vec (coupled)** · **`sqlite-vec` `=0.1.7 → 0.1.9` (TC-76, HITL `seq-151`)** · **the six Dependabot advisories open at the 0.8.20 publish (TC-93, HITL `seq-152`)** · **the bundled ERROR-TAXONOMY REVIEW (TC-103, HITL `seq-156`)** | bumped after OPP-12 (F-19); deferred-with-trigger, LBO-executed (F-12). rusqlite/sqlite-vec affects the vector path → **precede the scale-bound (0.8.23/24)**. **Two items placed here by HITL 2026-07-29:** the `sqlite-vec` 0.1.9 bump was held out of 0.8.20 Slice 31 and rides the coupled `rusqlite` migration (`seq-151`) — ⚠ **it MUST move together with `src/rust/crates/fathomdb-engine/tests/tc76_vec0_long_metadata_delete.rs`, which asserts upstream `#274` STILL EXISTS, or a successful upgrade turns the suite red**; and the Dependabot advisories that 0.8.20 publishes with open land here rather than in a post-publish pico (`seq-152`). Neither overturns the standing "Dependabot stays out of the sequence" directive or TC-78. ⚠ **0.8.22 BECOMES A BREAKING PUBLISH (HITL `seq-183`).** The `#[non_exhaustive]` work is **built label-only at 0.8.21** and **published here**, so this release is no longer purely internal dependency migration: it needs a **CHANGELOG break notice**, `dev/interfaces/rust.md` updates, and **absorption by Memex**. Whoever commissions 0.8.22 must plan for that rather than discover it. **Fourth item placed here 2026-07-29 — `TC-103` parts (a) and (c) ONLY** (part (b) moved to 0.8.21 at `seq-183`); the original entry follows: **Third item placed here 2026-07-29 — `TC-103`, the bundled ERROR-TAXONOMY REVIEW (`seq-156`):** TC-98 was ruled option (a) **for now** and is **accepted, not endorsed**; the HITL requires a revisit here, together with **(a)** decision `#18`'s one named exception (`reject_unrenderable_edge_epoch` keeps `InvalidArgument { msg }` while the other six `validate_write` shape rejections use the message-less unit variant `WriteValidation`), **(b)** the fact that **`EngineError` is NOT `#[non_exhaustive]`** — see `pub enum EngineError` in `src/rust/crates/fathomdb-engine/src/lib.rs` — so a future variant is a **compile** break for a downstream exhaustive `match` and a **reclassification is a SILENT runtime break**, the hidden-break shape, which exists **only on the Rust facade** (Python/TS are single-rooted exception hierarchies where a leaf mismatch propagates loudly) — and **(c)** the sibling-condition asymmetry. ⚠ Adding `#[non_exhaustive]` is **itself a breaking change**, so it needs a breaking venue; 0.8.20 was one and was deliberately passed. ⚠ The **T1e byte-pin would probably NOT catch such a change** — it tracks allowlist *names*, not type *shapes* |
| **0.8.23 (odd)** | **F-17 scale-bound — *soft* (advisory)** | characterize + document the supported-scale envelope of the exact-search local-first stack (frozen eu7 fidelity + latency gates; ~<50k-row operating point) on the **complete 0.8.x surface** | **F-20: F-17 scale-bound pulled into 0.8.x** so the line is cohesive before 0.9.0 (**revises F-17's 0.9.x placement**). Advisory guidance |
| **0.8.24 (even)** | **F-17 scale-bound — *stated* (firm, tested)** — the 0.8.x-cohesive capstone | a firm, tested supported-scale limit for the 0.8.x exact-search envelope | **F-20**: publishable at an even micro (resolves the old 0.9.3-odd-publish tension). **0.9.0 opens only AFTER this** — 0.8.x is then cohesive (OPP-12 + scale-bound both done); **0.9.0's post-line identity is now OPEN** |
| **0.8.11.2 (pico umbrella, OOB)** | OPP-1 / OPP-3 / OPP-6 + Cause-A — calendar-decoupled, start now | **Scope = OPP-1 + OPP-3 + OPP-6 + Cause-A.** Label-only, NEVER-published pico (no manifest bump / `v*` tag / publish); a normal pico off the 0.8.11 parent, **distinct from `0.8.11.1`** (the Library Sweep, F-12 — untouched). **Sequencing PRESERVED** (landing vehicle, not a sequencing override): **Phase A now (OPP-6 EXP-COV + OPP-3 cascade evals + Cause-A size-it-first→additive code + OOB `margin`/distractor-rank-demotion eval-support) → V-1 live-CE re-validation → OPP-1 = V-3 (held behind V-1) → OPP-3 bears at V-7;** Cause-A parallel, gates only adoption arms. Memex consumes Cause-A via local build; publish = later HITL `x.y.z`. | F-14 + `plan-0.8.11.2.md`; cross-refs F-13 + Memex `OPP-1-experiments.md` / `OPP-3-experiments.md` |
| **Cause-A (pico, OOB — parallel slice; now under `0.8.11.2`, F-14)** | Stable hit-id for real-gold adoption | **size-it-first → cut**: additive stable-id field on `SearchHit` + 4 bindings + telemetry/gold keying; **may carry the OOB `margin`-measurement + distractor-injection / gold-rank-demotion eval-support** (or a tiny eval micro) | **Now lives UNDER the `0.8.11.2` pico umbrella (F-14)** alongside OPP-1/3/6 (scope unchanged). **Distinct from / lighter than the F-8a G0 `write_cursor`→`logical_id` swap** (additive, not a carrier reshape). **Gates only the real-gold *adoption* arms (+ OPP-9 join / graph), NOT the academic experiment arms.** A pico (label-only, never-published). Cross-refs Memex `dev/fathomdb/OPP-1-experiments.md` / `OPP-3-experiments.md` (F-13) |

Every one of the 17 items is placed exactly once (#11 split min/full across 0.8.6 / 0.8.18); the
planner-router track is placed across the odd line (experiments 0.8.7–0.8.11, dispatcher 0.8.15,
hardening 0.8.17, free-threading/bench 0.8.19). **`13` is HITL-forbidden as both a minor and a micro
(no `0.13.x`, no `0.8.13`, HITL 2026-06-29)** — the odd line skips 13 by hard constraint, not merely
because no work happened to land there.
Dependency hygiene is a separate, recurring program owned by the **Library Bump Steward** (F-12):
**Library Sweep #1** is a transitory, **label-only** pico (no manifest bump / `v*` tag / publish) of
contained bumps, shipping as the **normal pico `0.8.11.1`** (HITL 2026-06-29). Under the standing
**two-tier numbering model** (F-13): `x.y.z` = real, publishable releases (manifest bump + `v*` tag +
registry publish; publishing is always a separate explicit HITL call), and `x.y.z.p` **picos** =
label-only, never-published work-completion increments for OOB/transitory work — **a Library Sweep IS a
pico**, so `0.8.11.1` is a normal pico, not a one-time exception. `13` stays HITL-forbidden as both
minor and micro (no `0.13.x`, no `0.8.13`). Deferred major-dependency migrations are set out to net-new
**0.8.22** (timing to be re-reviewed before it proceeds).

---

## 5. Planner-router track integration (the by-when)

> **⚠ §5 status (T1/2 sweep, F-20's flagged residual): historical, pre-F-18/F-19/F-20.** This whole
> section narrates the dispatcher build (EXP-Fr) as a live, still-scheduled 0.8.15 landing. It is not —
> **0.8.15 (and its 0.8.17 overflow) were PARKED by HITL 2026-07-07 (F-18)**: zero OPP-12 dependency,
> serves no top-5 Memex need; next commission after 0.8.16 became OPP-12 instead (F-19/F-20), which is
> what actually shipped at 0.8.19/0.8.20. §5 is kept verbatim below as the decision record for *why* the
> dispatcher was sequenced the way it was before it was parked — read every "lands at 0.8.15" statement
> in that light, and see §4's 0.8.15/0.8.17 rows + finding F-18 for the current (parked) status.
>
> **Result in one box.** The planner-router work is **almost entirely OOB** ($0 eval/analysis, landable
> anytime) — with one exception: the **dispatcher build (EXP-Fr)**, real engine code **hard-gated on
> EXP-S (0.8.14)** (I-2). So the experiments run OOB across **0.8.7 → 0.8.11 and must be *done* by
> 0.8.14**, the dispatcher lands at **0.8.15** (by dependency; odd line skips 13, cap removed), and **EXP-S (0.8.14)
> is the long pole**. Two router prerequisites — **EXP-OBS (0.8.8)** and **EXP-S (0.8.14)** — already
> live in the even line; the odd planner-router work is sequenced *around* those two landings.
> **(SUPERSEDED — see F-11: the "experiments run OOB as float" model FAILED; none of the ladder ran. It is
> RETIRED as float and discharged as owned work IN 0.8.11. The §5b "by-when" deadlines below are recorded
> as MISSED history, not a live plan.)**

### 5a. The two interlocks that fix the timing

| Prerequisite | Lands (even) | What it gates |
|---|---|---|
| **EXP-OBS** (#1 — per-arm provenance + rrf/ce score breakdown + opt-in `explain=True`) | **0.8.8** | A *transparent* router; **and** the telemetry the agent-relevance-signal judges against + the reward log. Nothing agent-feedback runs before this (**I-1**). |
| **EXP-S** (#2 — kind-tagged coexisting indexes + multi-index write + determinism check) | **0.8.14** | The *in-library* dispatcher (EXP-Fr). Its **KILL path** decides the router's **locus** (in-library vs agent-side). The in-library build cannot precede it (**I-2**). |

### 5b. OOB items and the one dependency that gives each a deadline

> **SUPERSEDED — see F-11.** The "Land in" deadlines below were the float plan; **none of these items ran**
> and every deadline was missed. The whole ladder is now **discharged IN 0.8.11 as owned work** (the "Land
> in" column is retained as the original intent / dependency record, not a live schedule).

| Planner-router item | Cost | Blocked-to-START by | Consumed by → deadline | Land in |
|---|---|---|---|---|
| Gate-0 re-scope + Gate-2 oracle bound | $0 | — | foundational (informs all) | **0.8.7** |
| EXP-A (recall generation) | $0 | — | EXP-B′ | **by 0.8.9** |
| EXP-M4 (embedder ceiling) | $0 / GPU | wants GPU #3 (0.8.7) for speed | EXP-B′ (OD-7 serial chain) | **by 0.8.9** |
| EXP-B′ (joint tuning) | $0 + $ judge | EXP-A, EXP-M4 | EXP-Fr | **by 0.8.14** |
| EXP-Fr-acc (accuracy + mis-route matrix) | $0 + small $ | — | EXP-Fr | **by 0.8.14** |
| EXP-Fr-acc agent-signal + EXP-AF | small $ | **EXP-OBS + real-gold #10 (0.8.8)** | agent-loop productization | **by 0.8.15** |
| EXP-Fr-acc **locus decision** | $0 | **EXP-S outcome (0.8.14)** | the *form* of EXP-Fr | **resolves at 0.8.14** |
| **EXP-Fr — dispatcher build** *(NOT OOB)* | engine build | **EXP-B′ ∧ EXP-S ∧ Fr-acc ∧ EXP-OBS** | the router / product | **lands 0.8.15** |

### 5c. By-when (bottom line)

- **The dispatcher (EXP-Fr) lands at 0.8.15** (odd line skips 13) — by dependency (after EXP-S @0.8.14 + Fr-acc/pre-stage @0.8.11), NOT a cap. The earlier "no later than 0.8.15" bound is HITL-OVERRIDDEN (2026-06-28). EXP-S at 0.8.14 is still the long pole.
  Lands at **0.8.15** by dependency (cap removed).
- **All feeding experiments must be *done by 0.8.14*** so 0.8.15 builds with no idle wait and no stale
  re-runs. **(SUPERSEDED — see F-11: these float deadlines (EXP-A/M4 by 0.8.9 → EXP-B′ + Fr-acc by 0.8.11)
  were all MISSED — the float never ran. The ladder is now discharged IN 0.8.11 as owned work, then locus
  is finalized at 0.8.14 with EXP-S; 0.8.15's experiment base is produced by 0.8.11.)**
- **Long pole = EXP-S (0.8.14). If EXP-S slips, the router slips one-for-one.** The same one-for-one
  coupling holds for EXP-OBS@0.8.8 → the agent-signal track (I-1).

### 5d. Contingency decision points

- **DP-A — EXP-S KILL path (at 0.8.14): in-library vs agent-side locus.** If EXP-S's determinism/perf
  check fails, the in-library dispatcher is off the table — "router stays agent-side, indexes stay
  eval-side" — and 0.8.15 ships the **agent-side router + L1 hardening** instead. **Pre-mitigation:**
  the agent-side L2 prototype built at **0.8.11** has no substrate dependency, so **a router ships either
  way**; EXP-S only decides whether the *in-library* locus is also available (the "both-layered" stance
  as a schedule hedge). Owner: steward, at the 0.8.14 EXP-S readout, jointly with Fr-acc's locus call.
- **DP-B — EXP-B′ divergence (~0.8.9–0.8.11): does the router move onto the parity critical path?** If
  joint-tuning shows F1/F2 are Pareto-blocked and need different `(index, retrieval, α, pool_n, MMR,
  recency)` tuples, then per-intent **parity-or-better cannot ship without the router** — EXP-Fr stops
  being a capstone and becomes the **gate for the parity product claim**. It still **cannot precede
  EXP-S (0.8.14)**, so the effect is to **raise the cost of any router slip**, not pull the date earlier.
  Mitigation: have the 0.8.11 agent-side router carry the per-intent tuples so parity can be demonstrated
  agent-side before the in-library build. Owner: steward, at the EXP-B′ readout.

---

## 6. Integration findings (objective, carried into this plan)

### 6a. Findings index (in file order)

Every `F-n` is a stable citation target; each row links in-document to that
finding's heading. Read the renumber note below before reading any release
number in §6.

> **⚠ NO COUNT IS STATED HERE, DELIBERATELY.** This heading used to read *"all 35"* while the list
> carried 36 entries and three findings (F-36, F-37, F-38) had no entry at all — a hand-maintained
> total in a heading rots on the next finding, silently, and it did. **The invariant that replaces it
> is mechanical: every `### F-n` body heading has exactly one entry below, every entry names a heading
> that exists, and every entry's anchor is the one its heading generates.** Adding a finding means
> adding its row here in file order; the count takes care of itself.

- [F-1 — The EXP-S long-pole risk is understated by the raw planner-router schedule](#f-1--the-exp-s-long-pole-risk-is-understated-by-the-raw-planner-router-schedule)
- [F-2 — The EXP-OBS scope watch-item is already satisfied (confirm, don't re-litigate)](#f-2--the-exp-obs-scope-watch-item-is-already-satisfied-confirm-dont-re-litigate)
- [F-3 — The odd line grew; these net-new slots are now ratified by this plan](#f-3--the-odd-line-grew-these-net-new-slots-are-now-ratified-by-this-plan)
- [F-4 — 0.8.8 carries a pulled-forward pyo3 security exception (bounded; no I-1 risk)](#f-4--088-carries-a-pulled-forward-pyo3-security-exception-bounded-no-i-1-risk)
- [F-5 — Free-threaded-Python (EXP-FT) is folded into the 0.8.x line at 0.8.19 (experiment ladder); productization is a 0.8.19-readout contingency](#f-5--free-threaded-python-exp-ft-is-folded-into-the-08x-line-at-0819-experiment-ladder-productization-is-a-0819-readout-contingency)
- [F-6 — EXP-OBS keystone has LANDED and the field set is HITL-ratified (verified on `origin/main`)](#f-6--exp-obs-keystone-has-landed-and-the-field-set-is-hitl-ratified-verified-on-originmain)
- [F-7 — The repo-wide markdown lint/format gate is unsatisfiable; cleanup DONE at the transitory 0.8.9.1 (landed 2026-06-28, PR #115)](#f-7--the-repo-wide-markdown-lintformat-gate-is-unsatisfiable-cleanup-done-at-the-transitory-0891-landed-2026-06-28-pr-115)
- [F-8 — Two deferred contracts carried out of 0.8.8's telemetry/real-gold pipeline (HITL-approved push, 2026-06-28)](#f-8--two-deferred-contracts-carried-out-of-088s-telemetryreal-gold-pipeline-hitl-approved-push-2026-06-28)
- [F-9 — pyo3 macOS/Windows `cargo test` link failure: pre-existing CI rot, fixed in-band as a reserved-gap 0.8.9 slice (HITL 2026-06-28)](#f-9--pyo3-macoswindows-cargo-test-link-failure-pre-existing-ci-rot-fixed-in-band-as-a-reserved-gap-089-slice-hitl-2026-06-28)
- [F-10 — Remaining-release re-sizing + odd-line renumber (HITL 2026-06-28)](#f-10--remaining-release-re-sizing--odd-line-renumber-hitl-2026-06-28)
- [F-11 — The upstream planner-router experiment ladder never ran; the "`$0` float" model FAILED → folded into 0.8.11](#f-11--the-upstream-planner-router-experiment-ladder-never-ran-the-0-float-model-failed--folded-into-0811)
- [F-11a — (SUPERSEDED, historical — superseded by F-11) The upstream planner-router experiment ladder never ran; 0.8.11's "0.8.9 Fr-acc base complete" prerequisite is FALSE](#f-11a--superseded-historical--superseded-by-f-11-the-upstream-planner-router-experiment-ladder-never-ran-0811s-089-fr-acc-base-complete-prerequisite-is-false)
- [F-12 — Dependency hygiene becomes an owned, recurring program under a new Library Bump Steward](#f-12--dependency-hygiene-becomes-an-owned-recurring-program-under-a-new-library-bump-steward)
- [F-13 — Master reconciliation pass: two-tier numbering re-reconciled, the OPP-1/3/6 experiments recorded as one phased owned effort with a parallel Cause-A pico, and 7 OPP SIGNs reflected](#f-13--master-reconciliation-pass-two-tier-numbering-re-reconciled-the-opp-136-experiments-recorded-as-one-phased-owned-effort-with-a-parallel-cause-a-pico-and-7-opp-signs-reflected)
- [F-14 — The OPP-1 / OPP-3 / OPP-6 + Cause-A work is pulled into one label-only `0.8.11.2` pico umbrella to decouple it from the release calendar and start now](#f-14--the-opp-1--opp-3--opp-6--cause-a-work-is-pulled-into-one-label-only-08112-pico-umbrella-to-decouple-it-from-the-release-calendar-and-start-now)
- [F-15 — OPP-6 EXP-COV-1 downstream verdict = `CEILING-ABSORBED` ⇒ #6 (ELPS coverage-lift) DE-PRIORITIZED (HITL 2026-07-02; reconciled by the Steward)](#f-15--opp-6-exp-cov-1-downstream-verdict--ceiling-absorbed--6-elps-coverage-lift-de-prioritized-hitl-2026-07-02-reconciled-by-the-steward)
- [F-16 — ANN / HNSW is a 2.x item, explicitly NOT 0.8.x (HITL 2026-07-02; reconciled by the Steward)](#f-16--ann--hnsw-is-a-2x-item-explicitly-not-08x-hitl-2026-07-02-reconciled-by-the-steward)
- [F-17 — The DB scale-bound is a 0.9.x arc (0.9.0 *soft* → 0.9.3 *stated*), NOT a 0.8.18 GA claim (HITL 2026-07-02; reconciled by the Steward)](#f-17--the-db-scale-bound-is-a-09x-arc-090-soft--093-stated-not-a-0818-ga-claim-hitl-2026-07-02-reconciled-by-the-steward)
- [F-18 — Memex ranked-needs weighting: OPP-12 (≥0.9.x) is the single major FathomDB-side blocker for Memex](#f-18--memex-ranked-needs-weighting-opp-12-09x-is-the-single-major-fathomdb-side-blocker-for-memex)
- [F-19 — HITL RESEQUENCE (2026-07-07): OPP-12 is PULLED INTO THE 0.8.x LINE and must be DONE before 0.9.0](#f-19--hitl-resequence-2026-07-07-opp-12-is-pulled-into-the-08x-line-and-must-be-done-before-090)
- [F-20 — HITL CONFIRMED the F-19 allocation + pulled the F-17 scale-bound into 0.8.x so the line is cohesive before 0.9.0 (2026-07-07)](#f-20--hitl-confirmed-the-f-19-allocation--pulled-the-f-17-scale-bound-into-08x-so-the-line-is-cohesive-before-090-2026-07-07)
- [F-21 — HITL BUILD-AUTHORIZED OPP-12 (0.8.19 Phase-1 + 0.8.20 Phase-2); build ≠ adopt; publish stays a separate per-`x.y.z` gate (2026-07-08)](#f-21--hitl-build-authorized-opp-12-0819-phase-1--0820-phase-2-build--adopt-publish-stays-a-separate-per-xyz-gate-2026-07-08)
- [F-22 — Open-TC scheduling ratified (HITL 2026-07-09)](#f-22--open-tc-scheduling-ratified-hitl-2026-07-09)
- [F-23 — OPP-12 Phase-1 Slice-0 rulings: anonymous-surrogate minting DEFERRED to Phase-2 (0.8.20); gap-1 ruling "1a"](#f-23--opp-12-phase-1-slice-0-rulings-anonymous-surrogate-minting-deferred-to-phase-2-0820-gap-1-ruling-1a)
- [F-24 — 0.8.19 (OPP-12 record-lifecycle Phase-1) COMPLETE (2026-07-10, label-only close; HITL Option-A)](#f-24--0819-opp-12-record-lifecycle-phase-1-complete-2026-07-10-label-only-close-hitl-option-a)
- [F-25 — TC-11 DISCHARGED: the doc-seeded `h:` end-state is pin A (terminal-forever) by OVERRULE of `§2(ii)`](#f-25--tc-11-discharged-the-doc-seeded-h-end-state-is-pin-a-terminal-forever-by-overrule-of-2ii)
- [F-26 — 0.8.20 Slice-0 (X0 design gate) SIGNED + LANDED; the rubric guardrails are now MECHANICALLY ENFORCED (HITL 2026-07-19, `403eb254`)](#f-26--0820-slice-0-x0-design-gate-signed--landed-the-rubric-guardrails-are-now-mechanically-enforced-hitl-2026-07-19-403eb254)
- [F-27 — THE ERASURE AUDIT TRAIL IS DESTRUCTIBLE; HITL RULED IT MUST BE DURABLE (2026-07-19). Refutes the v4 design of record](#f-27--the-erasure-audit-trail-is-destructible-hitl-ruled-it-must-be-durable-2026-07-19-refutes-the-v4-design-of-record)
- [F-28 — R-20-EU7 is CLOSED BY HITL DECISION, NOT BY MEASUREMENT: ZERO eu7 runs are authorized, on ANY backend, at ANY N (2026-07-19)](#f-28--r-20-eu7-is-closed-by-hitl-decision-not-by-measurement-zero-eu7-runs-are-authorized-on-any-backend-at-any-n-2026-07-19)
- [F-29 — AC ids mint from AC-079; AC-077/078 are LIVE RESERVATIONS and "max AC id + 1" is an unsafe minting rule (HITL 2026-07-19)](#f-29--ac-ids-mint-from-ac-079-ac-077078-are-live-reservations-and-max-ac-id--1-is-an-unsafe-minting-rule-hitl-2026-07-19)
- [F-30 — THE PUBLISH DRY-RUN GUARD HAS BEEN DEAD SINCE 0.8.14, and 0.8.20 is the FIRST RELEASE THAT ACTUALLY PUBLISHES — currently UNOWNED, needs a home](#f-30--the-publish-dry-run-guard-has-been-dead-since-0814-and-0820-is-the-first-release-that-actually-publishes--currently-unowned-needs-a-home)
- [F-31 — Slice 5's `SourceId` newtype WILL BREAK the eu7 harness and every other in-tree `PreparedWrite` construction; the v4 design did not enumerate them](#f-31--slice-5s-sourceid-newtype-will-break-the-eu7-harness-and-every-other-in-tree-preparedwrite-construction-the-v4-design-did-not-enumerate-them)
- [F-32 — 0.8.20 ladder reconciliation: Slices 5/10/15 LANDED (SCHEMA 24), the eight-item HITL queue RULED, TC-16 and TC-45 PLACED](#f-32--0820-ladder-reconciliation-slices-51015-landed-schema-24-the-eight-item-hitl-queue-ruled-tc-16-and-tc-45-placed)
- [F-33 — DOC-HYGIENE-1 LANDED (2026-07-25): the cross-cutting docs/tooling effort that gated the 0.8.20 Slice-20 commission is DISCHARGED](#f-33--doc-hygiene-1-landed-2026-07-25-the-cross-cutting-docstooling-effort-that-gated-the-0820-slice-20-commission-is-discharged)
- [F-34 — RUN AUTHORIZATION (HITL 2026-07-25): the 0.8.20 tail runs to completion under a STANDING STEWARD MANDATE](#f-34--run-authorization-hitl-2026-07-25-the-0820-tail-runs-to-completion-under-a-standing-steward-mandate)
- [F-35 — TAIL RE-SEQUENCED (HITL 2026-07-27): Slice 30 runs FIRST, net-new slices 21, 22 and 31](#f-35--tail-re-sequenced-hitl-2026-07-27-slice-30-runs-first-net-new-slices-21-22-and-31)
- [F-36 — TAIL RE-SEQUENCED AGAIN (HITL 2026-07-28): TC-86 transcript hygiene runs FIRST, Slice 23 is net-new, and the batched governed-surface decision moves to the close of Slice 23](#f-36--tail-re-sequenced-again-hitl-2026-07-28-tc-86-transcript-hygiene-runs-first-slice-23-is-net-new-and-the-batched-governed-surface-decision-moves-to-the-close-of-slice-23)
- [F-37 — LIBRARY SWEEP #3 RE-SCOPED TO A TOOL (HITL 2026-07-29): Slice 31 becomes 31 → 32 → 33; TC-76 and TC-93 both land at 0.8.22](#f-37--library-sweep-3-re-scoped-to-a-tool-hitl-2026-07-29-slice-31-becomes-31--32--33-tc-76-and-tc-93-both-land-at-0822)
- [F-38 — `rust-windows` is a REAL ENGINE DEFECT, and CI's `verify`/`security` had been red ~3 weeks behind a skip-mask (HITL 2026-07-30, steward `seq-222`/`seq-223`)](#f-38--rust-windows-is-a-real-engine-defect-and-cis-verifysecurity-had-been-red-3-weeks-behind-a-skip-mask-hitl-2026-07-30-steward-seq-222seq-223)
- [F-39 — 0.8.20 is Linux-first; platform validation moves to 0.8.22 (HITL 2026-07-31, steward `seq-234`)](#f-39--0820-is-linux-first-platform-validation-moves-to-0822-hitl-2026-07-31-steward-seq-234)

> **⚠ Renumber note — read every number in §6 as the PRE-renumber scheme.** F-1..F-14 were written before the
> 2026-07-01 renumber and are kept verbatim as the historical decision record (especially the dated F-10 / F-11).
> The even slots were later REASSIGNED, so a bare number here is NOT the current release of that number:
> **old `0.8.12` = EXP-S → now `0.8.14`**, old `0.8.14` = ONNX → now `0.8.16`, **old `0.8.16` = #5/#11-full GA
> capstone → now `0.8.18`**; the *current* `0.8.12` is memory-quality. Authoritative map:
> `runs/0.8.x-renumber-reconciliation.md`.

Findings from reconciling the planner-router schedule against the even-line `plan-0.8.z.md` files, plus
landed-state reconciliations as releases close. Recorded here as standing risks/clarifications and the
verified record of what has actually shipped.

### F-1 — The EXP-S long-pole risk is understated by the raw planner-router schedule

That schedule treats `EXP-S@0.8.12` as if 0.8.12 *is* EXP-S and says "if EXP-S slips, the router slips one-for-one."
But in `plan-0.8.12`, **EXP-S shares the release with F5 (#16)** — one further schema migration (I-6);
**filter-grammar (#17) has moved to 0.8.11 (F-10)**. *Mitigation already in the plan:* EXP-S is the **slice-5 keystone**, so
the *capability* the router needs lands early-in-release, before the whole-release DoD closes — the
dispatcher can begin consuming it mid-0.8.12. *Residual risk:* EXP-S still competes for engine attention
with one co-resident migration (F5), and any EXP-S expansion (it is a schema migration with a determinism
check) pushes F5 and threatens the 0.8.15 router date. **Treat EXP-S as the program's single most
schedule-critical build and resist adding scope to 0.8.12.**

### F-2 — The EXP-OBS scope watch-item is already satisfied (confirm, don't re-litigate)

The planner-router schedule warns that if 0.8.8 ships "only `ce_score`, not per-arm provenance," the
agent-signal track slips. `plan-0.8.8` **R-OBS-1 already scopes per-hit arm-provenance + score
breakdown** as the EXP-OBS keystone (not a later increment), and places telemetry + real-gold (#10) at
its slices 15/20. So the two docs **agree**; I-1's upstream is correctly scoped. The standing action is
only to *hold* that scope at 0.8.8 Slice 0, not to expand it.

### F-3 — The odd line grew; these net-new slots are now ratified by this plan

The original non-measure sequencing defined only two OOB odd slots (0.8.7 GPU, 0.8.9 CI). Folding in the
planner-router track **adds 0.8.11 / 0.8.15 / 0.8.17 / 0.8.19 as net-new odd slots** and **co-hosts $0
planner-router eval on 0.8.7 / 0.8.9** alongside their original drop-ins (**this co-host SUPERSEDED — see
F-11: the 0.8.7/0.8.9 co-hosted eval never ran; folded into 0.8.11 as owned work**). This is deliberate (the odd
line is the program's OOB/eval channel), but it means the odd line now does substantially more than
"two drop-ins": 0.8.15 in particular carries a **real engine build** (EXP-Fr), not OOB work, and is
hard-gated (I-2). The steward should staff the odd line accordingly, not treat it as spare capacity.

### F-4 — 0.8.8 carries a pulled-forward pyo3 security exception (bounded; no I-1 risk)

A 2-HIGH + 2-moderate Dependabot pair (`GHSA-36hh-v3qg-5jq4` / `GHSA-chgr-c6px-7xpp`) on `pyo3 0.24.1` in the
shipped `fathomdb-py` binding is first patched at **pyo3 0.29.0**, so it is a five-minor jump, not a
lockfile bump. It is pulled forward into **0.8.8 as reserved-gap Slice 1** (ahead of the 0.8.9
CI-integrity backlog) so EXP-OBS's new Py-`explain` surface lands on a patched binding. Lift is
**LOW–MEDIUM**: a single-file (`fathomdb-py/src/lib.rs`) `Bound`-API rename wave (`with_gil`→`attach`,
`allow_threads`→`detach`, `downcast`→`cast`, `PyObject`→`Py<PyAny>`) plus the `#[pymodule(gil_used =
true)]` decision that preserves today's GIL semantics; free-threading is explicitly out of scope.
Because Slice 1 lands **first** and is small, it **does not threaten EXP-OBS@0.8.8 → the I-1
agent-signal upstream**. Detail in `plan-0.8.8.md` §1/§3; the rest of the Dependabot backlog (npm
markdown-it/js-yaml, pip idna/torch eval-only) → 0.8.9 Slice 15.

### F-5 — Free-threaded-Python (EXP-FT) is folded into the 0.8.x line at 0.8.19 (experiment ladder); productization is a 0.8.19-readout contingency

`dev/design/free-threaded-python-value-lift-and- experiments.md` proposes a 5-experiment ladder (EXP-FT-1 GIL-held-fraction profiling · FT-2 multi-thread
throughput · FT-3 GIL-poisoning cost · FT-4 no-GIL concurrency-safety **hard gate** · FT-5
wheel-matrix/CI-lane **feasibility**). The ladder is **$0 eval/analysis** whose only hard prerequisite is
**pyo3 0.29 + the `gil_used` seam (0.8.8 Slice 1)**, so it rides the **0.8.19** free-threading/benchmark
odd slot (the router-hardening + EXP-C/D/E forks sit at 0.8.17) — **kept inside 0.8.x, not deferred to 0.9+**.
**Productization** (flip `gil_used = false` + ship the non-abi3 `*t` wheels) is **conditional on FT-4
passing** and on the **#11-full publish matrix (0.8.16)** the wheel lane extends, so it is **not
pre-scheduled** — decided at the **0.8.19 EXP-FT readout** and, if greenlit, folded into a **net-new
post-0.8.19 publish slot** as the steward elects. **Dependencies:** (HARD, to start) pyo3 0.29
— 0.8.8 Slice 1; (HARD, to flip `gil_used=false`) the FT-4 concurrency-safety gate; (SOFT/packaging, to
ship) #11-full publish matrix — 0.8.16; (eval substrate) telemetry #10 — 0.8.8 aids FT-1/FT-2. The cheap
FT-1/2/3 arm is OOB-runnable any time after the 0.8.8 pyo3 0.29 landing if HITL pulls early-signal
forward.

### F-6 — EXP-OBS keystone has LANDED and the field set is HITL-ratified (verified on `origin/main`)

The `#1 EXP-OBS` keystone is on main: Slice 0 ADR (`170c5109`), Slice 1 pyo3 0.24.1→0.29.0 (`8c938bb7`,
see F-4), Slice 5 engine sidecar (`eec4ddb0`), and the field-set ratification (`5c7b9f31`). A 6-owner
negotiation ratified the *landed* `Explanation` field set as architecturally correct — **amend-ADR-to-
code, not revert** (`dev/plans/runs/0.8.8-explanation-fieldset-ratification.md`): ADR §A.3
PROPOSED→RATIFIED, §A.4 Q1–Q3 closed, the **only** engine delta being evolution-safety (`#[non_exhaustive]`
×4). The governed public surface expanded **to 29 types (AC-074, HITL-blessed)** — the three new EXP-OBS
sub-types `Explanation`/`QueryTrace`/`PerHitExplain` (the `SearchResult.explanation` sidecar; default
paths byte-unchanged, R-OBS-2-COV). **I-1 consequence:** the per-arm provenance + score-breakdown signal
the agent-relevance track judges against now *physically exists on main*, so the **0.8.9 agent-signal
half can begin** — the I-1 upstream is satisfied for the core signal. This **resolves F-2's standing
action** (scope was held at per-hit provenance, not expanded). **The full 0.8.8 release is proceeding:**
Slices 10 (SDK Py↔TS parity, R-OBS-4), 15 (telemetry), 20 (real-gold), 40 (verify) remain in flight —
authoritative per-slice status in `dev/plans/runs/STATUS-0.8.8.md`. *Verified-on-main; ratification
decided by HITL; reconciled into the master by the steward 2026-06-27.*

### F-7 — The repo-wide markdown lint/format gate is unsatisfiable; cleanup DONE at the transitory 0.8.9.1 (landed 2026-06-28, PR #115)

0.8.9's bootstrap un-mask (Slice 1, R-BOOT) exposed that the CI `verify` lint step
(`scripts/agent-lint-md.sh`) had been *masked*, not passing. With it un-masked the gate is now
**hard-red and currently un-passable**: `markdownlint-cli2` reports **7983 errors** (MD049 3266 ·
MD060 1857 · MD022 942 · MD032 935 · MD031 366 · MD004 283 · …) and — behind it (never reached in CI
because `set -e` aborts at the first check) — `prettier --check` would reformat **304 files**.
The debt is concentrated in agent-emitted working docs (`dev/plans` 3636 lint / 156 prettier,
`dev/design` 2375 / 61). **Verified by the steward on `origin/main` 2026-06-28.** The board's original
"1-file config relax (MD049→asterisk, MD060→repo style)" framing was **measured to be insufficient** —
it leaves ~2860 markdownlint errors *and* the entire prettier wall. **HITL decision (2026-06-28):
DEFER and DOCUMENT.** This is markdown, not code; the only real green path (bulk `prettier --write` +
`markdownlint --fix`) is a ~300-file diff that **must not run while release orchestrators are live**
(collision risk). It is therefore sequenced as a **one-shot cleanup at 0.8.16, after the 0.8.x release
work lands** (§4). 0.8.9 completes on its *own* gates (perf-gate honesty, AC-037 live-proven in CI,
dependency hygiene); the `verify` lint red is recorded as known pre-existing debt, **not** a 0.8.9
regression. *Decided by HITL; sequenced into the master by the steward.*  **UPDATE 2026-06-28 (HITL greenlight):** the 0.8.16 deferral is SUPERSEDED. The cleanup is pulled forward to a deliberately transitory, **label-only 0.8.9.1** (NO version bump / `v*` tag / publish) — run after 0.8.9 (closed) and before 0.8.10. Scope: bulk `markdownlint-cli2 --fix` + `prettier --write` + a residual/config pass under a semantic-neutrality guard, PLUS making every markdown-generating tool (incl. the `docs/` flow) emit gate-compliant output with a lint guard. Success = `scripts/agent-lint-md.sh` exits 0. Execution prompt: `dev/plans/prompts/0.8.9.1-markdownlint-cleanup-HANDOFF.md`; driven by the 0.8.9.1-orchestrator session. F-7 reconciliation done by the steward at greenlight (this edit), so the orchestrator does not re-edit it. **DONE 2026-06-28 (landed, PR #115):** markdown debt **8,054 → 0** (`scripts/agent-lint-md.sh` exits 0: markdownlint-cli2 + `docs/**` lint + lychee). The execution DIVERGED from the planned scope above (HITL-approved): the root cause was **prettier itself** — its non-configurable `*`→`_` emphasis reflow corrupts fragile spans (isolation test: prettier corrupted ~38 docs vs markdownlint-cli2 `--fix` only 5). So **prettier was REMOVED from the gate** (not used as a fixer), **MD049/MD050/MD060 DISABLED** (cosmetic; ~64% of findings; the corruption driver), and the sole fixer is markdownlint-cli2 via the AST-guarded `scripts/md-safe-fix.sh`. Anti-regression added: `dev/tools/md_neutrality_guard.py` (markdown-it-py AST neutrality check), a pre-commit hook (`scripts/install-hooks.sh --force` to activate), `docs/**` lint, and generator-compliance guards. Independently §9-reviewed (Claude; codex out of budget) — no P1; 2 P2s fixed. The earlier "`prettier --write`" / config-relax framing in this finding is **SUPERSEDED** by this approach.

### F-8 — Two deferred contracts carried out of 0.8.8's telemetry/real-gold pipeline (HITL-approved push, 2026-06-28)

The push was approved with two consciously-deferred constraints that downstream work **must** honor:

- **F-8a — gold/telemetry id ↔ identity-substrate.** Telemetry + real-gold records reference hits by the interim `SearchHit.id` (today the `write_cursor` value, identical to what `explain` returns): **within-session consistent** (the gold pipeline is valid), but **not cross-session-stable**. **CONSTRAINT:** at the *identity-substrate swap* — whatever work makes hit-level identity a stable `logical_id` — the gold id contract MUST be revisited: either provide a remap from interim ids → stable `logical_id`, or consciously accept pre-swap gold as session-scoped (documented data-loss). **This swap is currently UNSCHEDULED in this master and is NOT #2 EXP-S (0.8.12, the kind-tag *index* substrate).** 0.8.12 is the nearest substrate touchpoint and carries a pointer, but ownership falls to whoever lands hit-level `logical_id` stability. The swap trigger is the **G0 keystone** per `ADR-0.8.0-canonical-identity-substrate` (`SearchHit.id` swaps `write_cursor`→`logical_id` with no carrier reshape); the detailed steward action record + remediation surface (`GoldRecord.id_space`, `eval/gold_capture.py`, `eval/frozen_candidate_scorer.py`) is in `dev/plans/runs/NOTE-0.8.8-to-steward-id-contract.md`. Until then, accumulated gold is portable only within a single DB build/session.
- **F-8b — `record_feedback` governance re-classification.** 0.8.8 classed the three telemetry methods (`enable_telemetry`, `last_telemetry_query_id`, `record_feedback`) as **observability instrumentation** (like `counters()`), not governed commands — defensible for 0.8.8's local, opt-in, no-egress scope. `record_feedback` is the one that **ingests exogenous agent-supplied labels** and is load-bearing for the agent-signal / EXP-AF track (the I-1 consumer). **CONSTRAINT:** at **0.8.11 (EXP-AF)**, when the agent-feedback loop productizes, `record_feedback`'s classification MUST be re-reviewed — if it becomes an agent-facing input API it graduates to a **governed command** (allowlist + Py↔TS parity + conformance), a breaking change that must not be deferred past productization. The 0.8.11 reclassification checklist + target surfaces (`src/conformance/governed-surface-allowlist.json` + Rust facade allowlist + X1 surface suites) are in `dev/plans/runs/NOTE-0.8.8-to-orchestrator-record-feedback-reclassify.md`. *Decided by HITL; recorded into the master by the steward 2026-06-28.*

### F-9 — pyo3 macOS/Windows `cargo test` link failure: pre-existing CI rot, fixed in-band as a reserved-gap 0.8.9 slice (HITL 2026-06-28)

Verified from CI history: `rust-macos`/`rust-windows` have failed `cargo test --workspace` since **2026-05-21** (`80ffa6bd`, when the jobs were first added under their current names) — ~5 weeks **before** the pyo3 0.24→0.29 bump (`8c938bb7`); byte-identical link errors (`_PyDict_GetItemWithError`, `_PyExc_*`) before and after. **Not a 0.8.8 regression.** Cause: `src/rust/crates/fathomdb-py/Cargo.toml` hard-codes pyo3 `extension-module` **always-on**, so the `cargo test --workspace` binary links libpython-less and the strict macOS/Windows linkers fail (Linux tolerates it). Every release since 0.8.6 merged over this red — exactly the "lying gate" 0.8.9's CI-integrity charter exists to end. **HITL decision: fix in-band at 0.8.9 (version stays 0.8.9, NOT a 0.8.9.1 micro)** as a **reserved-gap slice** (target Slice 20). **Fix (verified safe):** drop `extension-module` from the always-on Cargo features (keep `abi3-py310`); the wheel builds already pass `--features pyo3/extension-module` explicitly (`release.yml`, `ci.yml`) and the documented `maturin develop` command already passes it, so wheels + dev flow are unaffected. Lands as its own 0.8.9 PR right after PR #93 merges (keeps #93's $0/CI-only footprint clean); does **not** block #93 (the red is orthogonal/pre-existing). *Decided by HITL; sequenced into the master by the steward 2026-06-28.*  **LANDED 2026-06-28:** PR #104 merged (origin/main `1cb1c7ac`); `rust-macos` + `rust-windows` GREEN — first-ever green Windows suite. The final fix grew to the link-fix + the 5 cross-platform test gaps the link exposed (2 principled `cfg(unix)` gates on POSIX-only mechanics — SIGKILL power-cut harness, bash-linter exec — plus benign portability fixes and a contract-preserving macOS de-flake). Steward adversarial honesty-review = coverage-preserving, no core-engine behavior gated off Windows, no masking (git-spot-checked: `cfg(unix)` confined to durability_soak/migrations/compatibility; no `continue-on-error` in ci.yml); lone caveat = `compatibility.rs` `holder_pid` diagnostic relaxed on Windows (LockFileEx), non-load-bearing. Detail: `dev/plans/runs/NOTE-0.8.9-slice20-pyo3-review-verdict.md`. **0.8.9 is fully complete.**

### F-10 — Remaining-release re-sizing + odd-line renumber (HITL 2026-06-28)

A git-grounded steward sizing review found 0.8.12 / 0.8.15 / 0.8.16 over-sized and the odd line (0.8.11 / 0.8.13 / 0.8.15) under-planned. HITL-approved rebalance: **Dispatcher (EXP-Fr) → 0.8.15** (was 0.8.13), real ~4-slice ladder, **pre-staged at 0.8.11** (agent-side prototype carries the per-intent config tuples, per DP-B). **0.8.12 long pole de-loaded** — EXP-S → 3-slice spine; **#17 filter-grammar moved to 0.8.11** (no EXP-S dep — touches shipped G10, feeds the router); **F5 (#16) kept** (I-6 real). **0.8.15 overflow split** — router hardening + config wiring + EXP-AF productization → **0.8.17** (net-new); **EXP-FT FT-1…5 → 0.8.19**; **EXP-C/D/E forks → 0.8.17** (both net-new). **#13 benchmark-harness kept in 0.8.x** (NOT 0.9.x) → **0.8.19**; **0.8.16** = coherent #5 + #11-full GA-safety capstone. **No x.y.13 release** — odd line skips 13; **`13` is subsequently confirmed HITL-forbidden as both minor and micro (no `0.13.x`, no `0.8.13`), HITL 2026-06-29** — so this is a hard versioning constraint, not merely a dependency-driven skip. The "no later than 0.8.15" cap is **HITL-OVERRIDDEN** (dispatcher lands at 0.8.15 by dependency). Net-new releases acceptable; odd numbers stay OOB. **Self-completion principle (HITL):** natural gaps (skipped 0.8.13; any not-currently-planned slot, even or odd) are slack only — **each release is planned to complete its own work, NOT to spill into a buffer.** Follow-up: ladders for the under-planned odd releases (0.8.11/0.8.15/0.8.17/0.8.19) authored as they approach. *Decided by HITL; re-sequenced by the steward 2026-06-28.*

### F-11 — The upstream planner-router experiment ladder never ran; the "`$0` float" model FAILED → folded into 0.8.11

**F-11 — The upstream planner-router experiment ladder never ran; the "`$0` float" model FAILED → folded into 0.8.11 (steward finding from git; HITL disposition 2026-06-28).** Verified from git: `dev/experiments-ledger.md` has **zero** planner-router rows, there are **no** `runs/` artifacts, STATUS-0.8.7 closed on its **GPU-embedder** DoD only, and 0.8.9 closed on its **CI-integrity** DoD only — so **none** of the experiments (Gate-0/2 · EXP-A · EXP-M4 · EXP-B′ · EXP-Fr-acc) scheduled as odd-line float ever executed. (These are **low-`$` — a ~`$20` ceiling, not strictly `$0`**; the master's "`$0` float" was the *scheduling label*, not the true cost. **The failure was ownership, not cost** — whether `$0` or ~`$20`, unowned + ungated float does not get done.) **Mechanism:** the "experiments float against the even-line landings" model assumed each odd release would pick up its co-hosted eval, but every odd release shipped on its own primary DoD and the unowned, ungated float was silently dropped — **float with no owning slot and no forcing gate does not get done.** Every §5b by-when deadline (EXP-A/M4 by 0.8.9 → EXP-B′/Fr-acc by 0.8.11) was already missed. **Consequence:** 0.8.11's experiment-leg prerequisite was FALSE (its I-1/EXP-OBS leg IS satisfied — F-6); 0.8.11 Slice 5 (Fr-acc finalize) had no base to extend (Slice 10/15 cascade), and 0.8.15 (dispatcher, gated by EXP-B′ ∧ Fr-acc) was likewise blocked. 0.8.11 Slice 0 + Slice 20 (#17 filter-grammar) were unaffected. **HITL DISPOSITION (2026-06-28):** the ladder (`Gate-0/2 → EXP-A/M4 → EXP-B′ → EXP-Fr-acc`) is **FOLDED INTO 0.8.11** — its orchestrator plans it in as owned work AND completes 0.8.11's originally-assigned slices. This **supersedes 0.8.11's earlier "well-sized" assessment** (it grows materially — HITL-accepted); 0.8.15's experiment base is now produced by 0.8.11. **The "`$0` float rides the odd line" assumption is RETIRED** — every experiment is OWNED by a consuming release with a DoD and a forcing gate. (No 0.8.10 in flight.) *Found by the steward from git; disposition by HITL; reconciled by the steward 2026-06-28.*

### F-11a — (SUPERSEDED, historical — superseded by F-11) The upstream planner-router experiment ladder never ran; 0.8.11's "0.8.9 Fr-acc base complete" prerequisite is FALSE

> **⚠ HISTORICAL — SUPERSEDED BY [F-11](#f-11--the-upstream-planner-router-experiment-ladder-never-ran-the-0-float-model-failed--folded-into-0811).** This is the earlier, longer first
> draft of the same finding, written before the HITL disposition landed (its own lead
> reads *"awaiting HITL on disposition"*) and amended in place afterwards; the
> reconciled record of that disposition is **F-11**. It was filed under the id `F-11`,
> which made that id ambiguous. Renaming it `F-11a` (historical, superseded-by `F-11`)
> was ruled 2026-07-03 (`c6d3449a`, M18 in
> `dev/design/fathomdb-memex-overall-roadmap/00-priorities-and-misalignments.md`) and is
> executed here (DOC-HYGIENE-2 T1c). The entry below is unchanged apart from that id.

**F-11a — The upstream planner-router experiment ladder never ran; 0.8.11's "0.8.9 Fr-acc base complete" prerequisite is FALSE (steward finding from git, 2026-06-28 — awaiting HITL on disposition).** Verified from git history, `dev/experiments-ledger.md`, and `dev/plans/runs/`: **none** of the planner-router $0 experiments scheduled as odd-line float (§3, §5b) has been executed. **0.8.7** closed on its **GPU-embedder** DoD only (R-GPU; `STATUS-0.8.7.md`) — its co-hosted **Gate-0/Gate-2 · EXP-A · EXP-M4** never ran. **0.8.9** closed on its **CI-integrity** DoD only (`9a4a5f1d`/`d5a68d17`) — its co-hosted **EXP-B′ · EXP-Fr-acc** never ran. The ledger has **zero** planner-router rows and there are **no** `runs/` artifacts for any of these; only the *design* docs exist (`planner-router-psd-0.8.x.md` + `initial-arch-…` + the folded sequencing, codex-verdict "aligned-enough-to-start"). **Mechanism — why the float silently slipped:** §2b's "the $0 experiments are float scheduled against the even-line landings" model assumed the odd releases would *pick up* the co-hosted eval, but each odd release shipped on its own primary DoD (GPU @0.8.7, CI @0.8.9) and the unowned float was dropped — float with no owning slot and no gate forcing it does not get done. Every by-when deadline in §5b (EXP-A/M4 by 0.8.9 → EXP-B′ + Fr-acc by 0.8.11) is therefore already missed. **Consequence for 0.8.11:** the I-1/EXP-OBS leg of 0.8.11's prerequisites **is** satisfied (F-6, verified on `origin/main`), but the experiment leg is **not** — `plan-0.8.11.md` §7 prereq #2 and §8 "upstream satisfied" are wrong. Concretely: Slice 5 ("EXP-Fr-acc/VoI *finalize*") has no 0.8.9 classifier base + mis-route matrix to extend; Slice 10 (EXP-AF) depends on Slice 5; Slice 15's per-intent config tuples lose their EXP-B′ basis (provisional/EXP-0-pinned only). 0.8.11's **Slice 0** (ADRs + EXP-AF pre-registration + F-8b decision) and **Slice 20** (#17 filter-grammar, IN-LIBRARY, no experiment dep) are unaffected and remain start-ready. **Disposition — HITL RULED 2026-06-28:** **fold the full missed ladder into 0.8.11** as owned, gated slices — 0.8.11 now discharges `Gate-0/2 → EXP-A/M4 → EXP-B′ → EXP-Fr-acc base` **and** its originally-allocated work (Fr-acc/VoI finalize · EXP-AF · L2 prototype + pre-stage · #17 filter-grammar · F-8b) in one release. The steward's "separate precursor" recommendation was **overridden**; the eval ladder is the new Track E of `plan-0.8.11.md` (Slices 5–35; rewritten to this shape, §7/§8 false prerequisites corrected, sizing tradeoff documented in its §8 — 0.8.11 is now the largest 0.8.x release, accepted deliberately because these are low-cost experiments where the failure was ownership, not cost; strict $0 is no longer required but the ladder is capped at a **~$20 priced-LLM ceiling**, HITL 2026-06-28). **The "$0 float rides the odd line" scheduling model is retired for these experiments** — each gets an explicit owning slice + DoD + gate. **Broader impact:** the same missing experiments hard-gate **0.8.15** (EXP-Fr is gated by EXP-B′ ∧ Fr-acc ∧ EXP-OBS) — folding them into 0.8.11 closes all but EXP-S (0.8.12) for the dispatcher. **Remaining steward reconciliation (not yet done in this doc):** update the §4 0.8.11 row + §5b by-when to point at the owned slices; retire the float language in §2a/§3/§5b; flag the now-satisfied prerequisite in `plan-0.8.15.md`. *Found by the steward from git 2026-06-28; disposition ruled by HITL 2026-06-28; `plan-0.8.11.md` rewritten by the steward the same day.*

### F-12 — Dependency hygiene becomes an owned, recurring program under a new Library Bump Steward

**F-12 — Dependency hygiene becomes an owned, recurring program under a new Library Bump Steward; the current Dependabot backlog is triaged and split across a transitory, label-only Library Sweep pico (`0.8.11.1`) and a net-new 0.8.20 (HITL 2026-06-29; pico framing re-reconciled to the standing two-tier model 2026-06-29, F-13).** A liaison-session triage of the 21 open Dependabot PRs (relevance test = target manifest tracked on `main`? direct dep or transitive-only? already at/past target in the lockfile? security-driven?) resolved them to: **4 merged** (CI-action + prettier patches — #49/#50/#100/#96), **7 closed as orphans** (stale targets not tracked on `main` — `/typescript`, `/tests/cross-language/typescript`, `python/uv.lock`, and the untracked `go/` module — #46/#61/#62/#63/#64/#65/#44), **1 closed as already-satisfied** (#59 `rand` — `rand 0.9.4` already resolved in `Cargo.lock`; transitive-only), and **9 left open**. The 2 open Dependabot **security** alerts (`idna` medium, `torch` low) are both against the **gitignored local eval env** (`python/uv.lock`), NOT shipped code (`src/python` declares `dependencies = []`) — so **none of the 9 are security-urgent.** **HITL DISPOSITION (2026-06-29):** dependency hygiene gets a standing owner — a second Steward archetype, the **Library Bump Steward (LBS)**, an orchestrator-of-orchestrators that spawns per-library **Library Bump Orchestrators (LBOs)** (charter: `dev/plans/prompts/LIBRARY-BUMP-STEWARD.md`; template: `dev/plans/prompts/LIBRARY-BUMP-ORCHESTRATOR-TEMPLATE.md`). Sweeps run as **recurring transitory OOB micros between even releases** — owned, with a DoD and a forcing gate (**NOT float** — would repeat F-11; **NOT a buffer** — respects F-10 self-completion). **The 9 open PRs split by blast radius:** (a) **contained → Library Sweep #1, a transitory, label-only micro** (label-only mechanics — NO version bump / `v*` tag / publish; the manifests do not move): `sha2` 0.10→0.11 (escalate to (b) if the LBO's blast rating comes back wide), `typescript` 5→6 + `@types/node` 25→26, `actions/checkout` 6→7, `action-gh-release` 2→3 (needs a release dry-run), plus a `dependabot.yml` reconciliation. **Sweep #1 ships as a pico `0.8.11.1` (HITL 2026-06-29). Under the standing TWO-TIER numbering model (re-reconciled 2026-06-29, F-13 — supersedes this morning's "pico decanonized / one-time exception" wording): `x.y.z` are real releases (planned even/odd line; publishable — even micros w/ HITL approval, odd not expected — carrying a manifest bump + `v*` tag + registry publish); `x.y.z.p` picos are label-only, NEVER-published work-completion increments (no manifest bump / `v*` tag / registry publish) for OOB/transitory work that lands on `main` without consuming a real slot. A Library Sweep IS a pico, and `0.8.11.1` is a NORMAL pico, NOT a one-time exception (`0.8.9.1`, `0.8.9.2`, `0.8.11.1`, and Cause-A are all picos). Publishing is always a separate explicit HITL call on a real `x.y.z` — pico work that must reach registries gets a later HITL-cut publishable `x.y.z`. `13` is still HITL-forbidden as both minor and micro (no `0.13.x`, no `0.8.13`).** (b) **migration/wide → net-new 0.8.20, owned engine slices**: `napi` 2→3 (`napi` + `napi-derive` + `@napi-rs/cli`, matched set) and `rusqlite` 0.31→0.40 + `sqlite-vec` (coupled via bundled SQLite). Each 0.8.20 migration is **deferred-with-trigger** (a feature needs the new API, a security advisory, or toolchain deprecation). **0.8.20's timing is NOT yet confirmed — it MUST be strongly reviewed for timing-correctness before it proceeds** (a heavy migration must not be forced adjacent to an already-heavy even release; self-completion, F-10). Merges stay HITL/Steward-gated; build ≠ adopt. *Proposed by the FathomDB liaison session from a live triage; disposition by HITL 2026-06-29; reconciled into the master by the steward.*

### F-13 — Master reconciliation pass: two-tier numbering re-reconciled, the OPP-1/3/6 experiments recorded as one phased owned effort with a parallel Cause-A pico, and 7 OPP SIGNs reflected

**F-13 — Master reconciliation pass: two-tier numbering re-reconciled, the OPP-1/3/6 experiments recorded as one phased owned effort with a parallel Cause-A pico, and 7 OPP SIGNs reflected (HITL 2026-06-29; reconciled by the Steward).** Three reconciliations land together:

- **(1) Two-tier numbering — SUPERSEDES this morning's "pico decanonized / one-time exception" wording.** Two standing tiers: **`x.y.z` = real releases** (planned even/odd line; publishable — even micros with HITL approval, odd not expected — carrying a manifest bump + `v*` tag + registry publish) and **`x.y.z.p` picos = label-only, NEVER-published** work-completion increments (no manifest bump / `v*` tag / registry publish) for OOB/transitory work that lands on `main` **without consuming a real slot**. `0.8.9.1`, `0.8.9.2`, `0.8.11.1` (Library Sweep #1), and the **Cause-A** standalone work are **all picos**; **a Library Sweep IS a pico** (this supersedes the morning "sweeps must be non-pico" carve-out), and **`0.8.11.1` is a NORMAL pico, NOT a one-time exception.** **Publishing is ALWAYS a separate explicit HITL call on a real `x.y.z`** — pico work that must reach registries (e.g. Memex consuming Cause-A) gets a later HITL-cut publishable `x.y.z`. **`13` stays HITL-forbidden** as both minor and micro (no `0.13.x`, no `0.8.13`). This **only** re-frames the pico numbering — the F-12 / 0.8.20 / Dependabot-triage *disposition* is unchanged. Applied in F-12, the §4 0.8.11 row, the §4 post-table prose, the LBS charter (`LIBRARY-BUMP-STEWARD.md`), and `plan-0.8.11.1.md`.
- **(2) The OPP-1 / OPP-3 / OPP-6 experiments are ONE OWNED EFFORT with phased slices (not "two efforts").** The **Pre-0.8.15 Validation Gate is the single owning effort** (F-11 discipline: owned, with a definition-of-done — not float). A **Phase A** (recall/headroom evals — **OPP-6 EXP-COV-0..3** + **OPP-3 cascade evals**, runnable now) is **prepended** to the existing **V-1 → V-7** sequence. **OPP-1 sits at V-3**, held strictly behind V-1 per the hand-off §8c — **NOT pulled forward**. **OPP-3 bears at V-7.** The **Cause-A pico runs as a parallel slice.** **(UPDATE — see F-14: all four items now LAND under the `0.8.11.2` pico umbrella to decouple from the release calendar; OPP-1 still RUNS at its V-3 gate position and OPP-3 at V-7 — the sequencing is unchanged, only the landing vehicle moved.)** Concrete master deltas already applied: §4 0.8.10 row marks **#6 ELPS coverage as eval-gated by OPP-6 EXP-COV-0..3** (build≠adopt; flat per-corpus-ceiling curve ⇒ de-prioritize, real multi_session/temporal lift ⇒ invest); a **Cause-A standalone OOB pico row** is added to §4 (**size-it-first → cut**; **distinct from / lighter than the F-8a G0 `write_cursor`→`logical_id` swap** — additive: a stable-id field on `SearchHit` + 4 bindings + telemetry/gold keying; it **gates only the real-gold *adoption* arms (+ OPP-9 join / graph), NOT the academic experiment arms**). The concrete V-3 / V-7 designs live in the **Memex repo** (`dev/fathomdb/OPP-1-experiments.md` / `OPP-3-experiments.md`); the small **OOB `margin`-measurement + distractor-injection / gold-rank-demotion eval-support** deliverable can **ride the Cause-A pico or a tiny eval micro**. Full placement rationale: `dev/plans/runs/experiment-roadmap-placement-proposal.md`.
- **(3) Seven OPP SIGNs reflected.** **OPP-2/4/5/7/8/9/10 are SIGNED** (HITL, Memex-side, 2026-06-29) = **build-authorized (default-off); adoption still gated per item** (build ≠ adopt). **OPP-4 is split** — Depth-1 seam SIGNED / product claim held. The **source of truth for OPP status is the Memex ledger**; this master only references it (§1a OPP rows annotated for convenience).
*Disposition by HITL 2026-06-29; reconciled into the master by the Steward; supporting artifact `dev/plans/runs/experiment-roadmap-placement-proposal.md`. A separate session owns the Memex-ledger side.*

### F-14 — The OPP-1 / OPP-3 / OPP-6 + Cause-A work is pulled into one label-only `0.8.11.2` pico umbrella to decouple it from the release calendar and start now

**F-14 — The OPP-1 / OPP-3 / OPP-6 + Cause-A work is pulled into one label-only `0.8.11.2` pico umbrella to decouple it from the release calendar and start now; sequencing is PRESERVED (HITL 2026-06-29; reconciled by the Steward).** F-13 recorded the OPP-1/3/6 experiments as one owned, phased effort and Cause-A as a parallel pico, but left them pinned to the release cadence (Phase A around 0.8.10, V-3 after 0.8.12, V-7 before 0.8.15). **HITL DISPOSITION (2026-06-29):** decouple the four items — **OPP-1 + OPP-3 + OPP-6 + Cause-A** — from the release calendar by folding them into a single **`0.8.11.2` pico umbrella** so each can start the moment its OWN Phase-0 prerequisites are met (`$` ceilings, §8b ownership split, MuSiQue re-pull), not when 0.8.10 / 0.8.12 / the V-gate calendar arrives. **The pico is a LANDING VEHICLE, NOT a sequencing override** — the hard dependency order is UNCHANGED: **Phase A (runnable now: OPP-6 EXP-COV + OPP-3 cascade evals + Cause-A size-it-first→additive code + the OOB `margin`-measurement / distractor-rank-demotion eval-support) → V-1 (live-CE re-validation, keystone) → OPP-1 = V-3 (held strictly behind V-1, NOT pulled forward) → OPP-3 bears at V-7;** Cause-A runs parallel and gates only the real-gold *adoption* arms (+ OPP-9 join / graph), not the academic arms. **`0.8.11.2` is a normal pico under the two-tier model (F-13): label-only, NEVER-published** (no manifest bump / `v*` tag / registry publish); Cause-A's code lands but Memex consumes it via **local build**, and a publishable `x.y.z` is a separate later HITL call. **`0.8.11.2` is DISTINCT from `0.8.11.1`** (the dependency Library Sweep, F-12) — different pico off the same 0.8.11 parent; `0.8.11.1`'s scope is untouched. `13` stays HITL-forbidden as minor and micro. **The acceleration is purely calendar-decoupling** — experiments still produce verdicts/eval artifacts (not shipped features), and the V-1 → V-3 → V-7 gate semantics survive intact. Cross-refs F-13, `dev/plans/plan-0.8.11.2.md`, and `dev/plans/runs/experiment-roadmap-placement-proposal.md`. *Disposition by HITL 2026-06-29; reconciled into the master by the Steward.*

### F-15 — OPP-6 EXP-COV-1 downstream verdict = `CEILING-ABSORBED` ⇒ #6 (ELPS coverage-lift) DE-PRIORITIZED (HITL 2026-07-02; reconciled by the Steward)

The EXP-COV-1 downstream sufficiency read — full held-fixed GPU stack (dense edge_facts + FTS + CE-rerank@50, α=1.0), same-stack C-none recomputed in-sweep, paired-bootstrap B=2000 — ran to a decisive verdict under the FIXED pre-registered rule (SUFFICIENT iff CI-lo of Δgold-in-pool@10 or ΔMRR > +0.04 on ≥1 powered class; no goalposts moved). **Every powered Δ is NEGATIVE:** multi_session Δgip@10 **−0.123** [−0.167,−0.078] / ΔMRR −0.227; temporal **−0.069** [−0.097,−0.044] / ΔMRR −0.244 — added relation coverage **degrades** retrieval (the graph-arm pulls topically-adjacent-but-wrong sessions into the pool, diluting precision). **The binding constraint is the embedder/retrieval ceiling, not extraction coverage** — entity coverage was already solved (local GLiNER matches the frontier); the residual edge-strict-recall 0.227 gap is real but ceiling-absorbed. 1-bit CPU↔CUDA parity **held** (max abs 1.1e-7); `$0` (preserved 272/272 cache reused, no re-extraction); commit `813d9a22` on `0.8.12-expcov1-sweep` (derived metrics only — LOCOMO CC-BY-NC, no payloads). **HITL DISPOSITION (2026-07-02): #6 DE-PRIORITIZED** — coverage-lift is not funded as a retrieval lever; specifically **do NOT fund the ~$340 full-corpus relation-extraction pass**. **R-COV-3 resolves NEGATIVE; Slice 10 CLOSED.** 0.8.12 ships #7 (consolidation/recency) as its remaining feature; productization of relation extraction is explicitly OUT of 0.8.12 (a separate later HITL call). Secondary OPP-6 D-5 datum: graph-arm query cost scales ~10× with coverage density (63→640 ms/q), in the CPU/SQLite BFS not the embedder. Cross-refs F-13/F-14, `dev/plans/plan-0.8.12.md`, `dev/plans/runs/EXP-COV-1-results.md`. *Verdict computed by the commissioned orchestrator; verified from git by the Steward; disposition by HITL 2026-07-02; reconciled by the Steward.*

### F-16 — ANN / HNSW is a 2.x item, explicitly NOT 0.8.x (HITL 2026-07-02; reconciled by the Steward)

The vector stage is brute-force (two-phase bit-KNN: Hamming scan to `TOP_K_BIT_CANDIDATES`=192 → exact-f32 rerank), O(N) — it meets the 10k-row p50 gate but misses the 100k/1M budgets. A doc drift had `runs/0.8.x-remaining-todos.md` aspirationally noting "0.8.20 evaluate HNSW" while `plan-0.8.20.md` is **deps-only** (napi + `rusqlite`/`sqlite-vec` majors, F-12) and the authoritative design `dev/design/ann-index-vec0.md` targets post-1.0/pre-2.1. **HITL RULED 2026-07-02: replacing the sqlite-vec O(N) scan with an HNSW/ANN index (usearch/libSQL) is a 2.x item — NOT scheduled anywhere in the 0.8.x line, NOT 0.8.20; 0.8.20 stays deps-only.** Rationale unchanged: target consumers run <~50k rows where the 10k p50 gate is met, and GPU is not the retrieval lever (`[[gpu-not-retrieval-lever-hnsw-at-0.8.20]]`, timing corrected). Reconciled: `remaining-todos.md` §4 corrected; steward memory updated. *Surfaced by the steward readiness audit; ruled by HITL 2026-07-02; reconciled by the Steward.*

### F-17 — The DB scale-bound is a 0.9.x arc (0.9.0 *soft* → 0.9.3 *stated*), NOT a 0.8.18 GA claim (HITL 2026-07-02; reconciled by the Steward)

**⚠ REVISED-IN-PART by F-20 (2026-07-07): the scale-bound WORK is pulled INTO the 0.8.x line (0.8.23 *soft* / 0.8.24 *stated*) so 0.8.x is cohesive before 0.9.0; the lower rungs (1.0.0 exit-beta, 1.1.0 hard) still stand, and 0.9.x's role is now OPEN.** The steward readiness/sequencing audit flagged that 0.8.18 "GA-grade shippable" is unbounded on scale while the vector path is O(N) brute-force (HNSW/ANN is 2.x, F-16), and proposed labelling a scale envelope on the 0.8.18 GA DoD. **HITL RULED 2026-07-02: 0.8.18 GA makes NO scale claim; the scale-bound is a staged maturity ladder.** Pre-1.0.0 is **beta** — so 0.8.18's "GA capstone" is *release-engineering* GA (publish machinery + safety gates complete), NOT a maturity/stability/scale guarantee. The ladder: **0.9.0 = *soft* (advisory) scale bound · 0.9.3 = *stated* (firm, tested) scale bound · 1.0.0 = exit beta / first stable · 1.1.0 = *hard* scale bound (a supportable promise for the local-first envelope)** — with **1.0.0 → 1.1.0 deliberately left as break/fix room** before the hard commitment ("we will build our way there"). The 1.1.0 hard bound commits the current exact-search local-first envelope; the **2.x HNSW/ANN work (F-16) later raises the bound** to large-scale. So the "label the GA scale envelope at 0.8.18" option is **declined** — 0.8.18 stays scoped to #5 vector-equivalence + #11-full publish (F-1), and rests on the frozen eu7 + latency gates for its operating point. This is beyond the 0.8.x master horizon; a future 0.9.x roadmap doc will carry the 0.9.0 / 0.9.3 slots when authored (`remaining-todos.md` §4 seeds them). *Ruled by HITL 2026-07-02; reconciled by the Steward.*

### F-18 — Memex ranked-needs weighting: OPP-12 (≥0.9.x) is the single major FathomDB-side blocker for Memex

**F-18 — Memex ranked-needs weighting: OPP-12 (≥0.9.x) is the single major FathomDB-side blocker for Memex; the 0.8.15 dispatcher serves no top-5 Memex need and has ZERO OPP-12 dependency (Steward analysis 2026-07-07; the 0.8.15 resequence is HITL-pending).** Memex supplied a ranked "key needs from FathomDB" (memex `6f7456c` context). **Both P0 items are the OPP-12 record-lifecycle pair:** #1 the **Cause-A typed `SearchHit.id` round-trip** (C-2 / F-8a — typed `IdSpace` + `write_cursor→logical_id`; **verified NOT started** — `SearchHit.id` is `u64`, `stable_id` additive only; **TC-8**), and #2 the **OPP-12 net-new lifecycle build** (existence axis · node-validity · projection registry + EAV/property-FTS · `dense_readiness` · surrogate `logical_id`). #3 (`margin`/V-7) is P1 but **low Memex value** (gap ~0.03; cascade stays OFF regardless); #4 (adjudicator signals) is P2 enhancement (core signals landed); #5 (OPP-1 follow-on) is P3 (ball is Memex-side). **So essentially all remaining Memex need from FathomDB = OPP-12 @≥0.9.x** — a *forward-capability* blocker (Memex runs today on shipped FathomDB), gated behind the 0.8.x tail. **Memex-value critical path:** `0.8.16 (F9) → 0.8.18 (#11-full publish matrix) → open 0.9.0 (C-2 swap + existence axis) → 0.9.1 (projection registry + node-validity + C-1 co-land) → 0.9.2 (breaking-pair publish; Memex consumes)`. **Three consequences:** **(a) New forward edge — F9 (0.8.16) → OPP-12 `rankable` projection role (0.9.1):** F9's signal algebra is the `rankable` ranking contract (0.9.x draft §3), so **0.8.16's F9 must be specced with it in mind**, and 0.8.16 is thereby ON the Memex critical path. **(b) 0.8.15 (in-library dispatcher, OPP-10) is OFF the Memex critical path** — it appears in **none** of the top-5 needs (Memex is the sophisticated L1 driver; the in-library L2 is "batteries for thin consumers"), it is still gated on the **unrun V-7 leg**, and it has **ZERO OPP-12 dependency** (verified: the only "dispatcher" in the OPP-12 contract is the *async projection worker* (`fn projection_dispatcher_loop` / `fn projection_worker_loop` in `fathomdb-engine/src/lib.rs`), already shipped — a naming collision with the 0.8.15 *routing* dispatcher; `route_recommend`/`search_routed`/`RouteDecision` appear nowhere in the OPP-12 design docs, and the 0.8.15 plan references OPP-12 nowhere). **HITL DECIDED 2026-07-07: PARK 0.8.15** (§4 row marked ⏸ PARKED) — next commission = **0.8.16**; re-open the dispatcher on a thin-consumer demand signal or after the Memex-critical releases. Parking it is zero-OPP-12-risk (and avoids a future `search_routed`↔`ReadView` retrofit). Focus pivots to **OPP-12**, starting with the **C-1 co-design (TC-7)** run as a bounded two-shot loop with Memex on `dev/design/record-lifecycle-protocol/OPP-12-sub-ledger.jsonl`. **(c) New near-term coordination — C-1 co-design timing (TC-7):** Memex's **Commission C (entity-schema-registry) is live 0.5.x work** that builds toward the hard C-1 `EntityTypeSpec→ProjectionSpec` seam; the co-design ADR should be agreed **before** Memex locks Commission C's registry shape, even though FathomDB's build is 0.9.1. **UPDATE 2026-07-07: C-1 CONVERGED** (bounded 2-shot loop; `dev/design/record-lifecycle-protocol/OPP-12-C1-converged-contract.md`) — the break-if-late `AttributeSpec.index` role hint is already **frozen inert in Memex Commission C** (memex `95ed450`); TC-7 → converged-pending-HITL; **C-1 timing risk retired**. Cross-refs TC-7/TC-8, steward-ledger `seq 36`, memex `6f7456c`, `dev/plans/0.9.x-roadmap-DRAFT.md §3/§5`. *Steward analysis from the Memex ranked-needs list, verified from git; the 0.8.15 resequence + C-1-timing are HITL-pending.*

### F-19 — HITL RESEQUENCE (2026-07-07): OPP-12 is PULLED INTO THE 0.8.x LINE and must be DONE before 0.9.0

**F-19 — HITL RESEQUENCE (2026-07-07): OPP-12 is PULLED INTO THE 0.8.x LINE and must be DONE before 0.9.0; the 0.8.x micros keep counting up (odd/even); 0.9.0 decouples to the F-17 scale-bound arc only.** **Supersedes the "OPP-12 breaking ⇒ ≥0.9.x" placement** (0.9.x-roadmap-DRAFT + F-18's critical-path line). **Direction (HITL-decided):** (1) **ALL OPP-12 work** — existence axis · node-validity · `ReadView`/read-modes · projection registry + EAV/property-FTS · `dense_readiness` · surrogate `logical_id` · the **C-2 typed `SearchHit.id` swap (TC-8)** — **lands in 0.8.x**, built to DONE before 0.9.0; (2) the 0.8.x micro numbers **keep counting up** under the two-tier **odd/even** rule (even = publishable w/ HITL · odd = OOB label-only · `13` still forbidden); (3) **0.9.0 no longer carries OPP-12** — it reverts to the **F-17 scale-bound arc** (0.9.0 *soft* → 0.9.3 *stated*), opening only **after** OPP-12 is DONE in 0.8.x. This cleanly removes the old OPP-12-vs-scale-bound tension at 0.9.0. **Rationale:** pre-1.0 is beta (F-17) so a breaking OPP-12 surface may publish *within* 0.8.x (build label-only across odd micros → publish atomically at an even micro after 0.8.18's #11-full matrix); "get OPP-12 DONE" is the priority (it is Memex's entire remaining need, F-18). **Prereqs (landed / in-line):** EXP-S substrate (0.8.14 ✓) · Cause-A base (✓) · F9 for `rankable` (0.8.16) · #11-full publish matrix (0.8.18) · C-1 RATIFIED (✓). **CONCRETE ALLOCATION = PROPOSED, pending HITL confirm (Steward 2026-07-07):** OPP-12 slots right after 0.8.18 as **0.8.19 (odd, label-only) = Phase-1** [existence axis · `transition`/`purge` · C-2 swap · schema migration · X1] and **0.8.20 (even, publishable) = Phase-2 + breaking-pair publish** [read-modes/`ReadView` · node-validity · projection registry (C-1 co-land) · `dense_readiness`; pairs with a Memex 0.5.x-successor], **bumping** former 0.8.19 (free-threading/#13-bench)→**0.8.21** and former 0.8.20 (dep-migrations)→**0.8.22**. *(Alt: append OPP-12 at 0.8.21/0.8.22, keeping free-threading/deps at 0.8.19/0.8.20 — but that delays the priority.)* **Knock-ons to reconcile on confirm:** §4/§5 allocation rewrite; C-1 converged-contract landing ref (`0.9.1 P2·S-F` → the new 0.8.x slot); 0.9.x-roadmap-DRAFT (OPP-12 removed, 0.9.0 = scale-bound only, §7 decisions 1/2/4/5/6/8 fold into this resequence). *Direction by HITL 2026-07-07; concrete allocation proposed by the Steward, pending confirm.*

### F-20 — HITL CONFIRMED the F-19 allocation + pulled the F-17 scale-bound into 0.8.x so the line is cohesive before 0.9.0 (2026-07-07)

**Confirmed:** OPP-12 = **0.8.19 (odd, label-only) Phase-1** + **0.8.20 (even, publish) Phase-2 + breaking-pair publish** (position = 0.8.20); former free-threading → **0.8.21**, deps → **0.8.22**. **Added (HITL):** the **F-17 scale-bound work is pulled into 0.8.x** — **0.8.23 (soft/advisory)** + **0.8.24 (stated/firm-tested)** — *"F-17 also needs to be completed before 0.9.0 for 0.8.x to be a cohesive release."* This **revises F-17** (scale-bound soft/stated move from the 0.9.x arc into the 0.8.x line). **Add further micros (0.8.25+) if the scale-bound needs them.** **Publish-rule clarified (HITL):** even/odd is a **default expectation, NOT a hard publish gate** — an **odd micro may publish by HITL exception** (publish is always a per-`x.y.z` HITL call). **Consequence for 0.9.0:** with **both** OPP-12 and the scale-bound delivered in 0.8.x, **0.9.0 carries neither** — its post-line identity is now **OPEN** (the F-17 `0.9.0`-soft/`0.9.3`-stated markers are consumed by 0.8.23/24; 0.9.x scope is TBD, likely the 1.0 exit-beta runway). §4 table rewritten to this allocation. **Residual sweep (flagged, follow-up):** §5/§9 narrative prose + `0.9.x-roadmap-DRAFT.md` still carry pre-F-19 sequencing (`OPP-12@0.9.x` · `0.8.19=free-threading` · `0.8.18=end-of-line`) — banner-reconciled, full prose sweep pending. *Direction + confirmation by HITL 2026-07-07; §4 reconciled by the Steward.*

### F-21 — HITL BUILD-AUTHORIZED OPP-12 (0.8.19 Phase-1 + 0.8.20 Phase-2); build ≠ adopt; publish stays a separate per-`x.y.z` gate (2026-07-08)

With C-1 RATIFIED both sides and the F-19/F-20 slotting confirmed, HITL **cleared the OPP-12 build gate** for both slots. **What this authorizes:** *building* the full OPP-12 surface — Phase-1 (existence axis · `transition`/`purge` · **C-2 typed `SearchHit.id` swap / TC-8** · surrogate `logical_id` · `SCHEMA_VERSION` migration · X1) and Phase-2 (`ReadView`/read-modes · node-validity · **projection registry, C-1 co-land** · EAV/property-FTS · `dense_readiness` · X1). **What it does NOT do:** (a) **build ≠ adopt** — per-item *adoption* arms (e.g. anything that changes shipped default behavior) remain separately gated; (b) **publish is still a separate explicit HITL call** on the 0.8.20 `x.y.z` (Phase-1 @0.8.19 is label-only regardless); (c) it does **not** reorder the line — **OPP-12 stays in-sequence after 0.8.18** (0.8.16 → 0.8.18 → 0.8.19 → 0.8.20). **Sequencing note (Steward):** building 0.8.19 Phase-1 OOB *now* (parallel to the in-flight 0.8.16) is technically prereq-clear (EXP-S/Cause-A/C-1 all met) BUT would run **two concurrent `SCHEMA_VERSION` migrations** (F9 step-18 @0.8.16 ∥ OPP-12 @0.8.19) on the shared tree — a real collision hazard — so the Steward recommends **in-sequence** unless HITL elects to parallelize with explicit migration-ordering. **Ladder drift flagged (follow-up, NOT physically-hard — slots are 2 releases out):** `plan-0.8.19.md` still holds the **pre-F-19 "free-threading + benchmark harness"** content and `plan-0.8.20.md` the **"Library Sweep / dep-migrations"** content (both now allocated to **0.8.21 / 0.8.22** by F-19/F-20). The **OPP-12 Phase-1/Phase-2 ladders must be authored** (as the slot approaches, per F-10) and the free-threading/deps ladders **re-homed to `plan-0.8.21.md` / `plan-0.8.22.md`**; until then those two filenames mis-describe their slot. *Build-authorization by HITL 2026-07-08; recorded + drift flagged by the Steward.*

### F-22 — Open-TC scheduling ratified (HITL 2026-07-09)

The Steward's proposed slot for each open todo/consideration (todos ledger) was confirmed: **TC-8** (OPP-12 C-2 `SearchHit.id` swap) = **0.8.19** (it *is* Phase-1 work); **embed_batch_cls TS-binding parity** decision (add-TS vs ratify-py-first) = **0.8.19/0.8.20** (folded into their X1 parity work); **eu7 basis decision** (no-op vs real) = **0.8.20** (the real-publish gate — bounded re-baseline if OPP-12 touches retrieval/gold-keying); **TC-5** (full eu7 0.90 floor re-baseline, grown 18,472-doc corpus) = **0.8.23** (scale-bound *soft* — its natural home); **TC-10** (#5 open-latency) = **measure at 0.8.19**, optimize (identity-gated probe caching) **≥0.8.21 only if warranted**; **bm25f_search perf gate** = **0.8.21** (#13 bench harness); **TC-9** (`ort` 2.0-stable bump + the `ort::init()` GPU-EP fix) = **0.8.22** (dep-migrations) or an opportunistic pico when ort 2.0-stable ships; **typescript-6 / Dependabot #67** = next **Library Sweep pico** (or 0.8.22); **GPU-eval-mandate tooling enforcement** = opportunistic tooling task, pair with **0.8.23/24** (heavy eval). *Near-term (0.8.19/0.8.20) touches only TC-8, the embed_batch_cls decision, and the eu7 basis; the perf/dep/scale items cluster into 0.8.21–0.8.24 where those releases already live.* Slots recorded in the todos ledger. *Proposal by the Steward; ratified by HITL 2026-07-09.*

### F-23 — OPP-12 Phase-1 Slice-0 rulings: anonymous-surrogate minting DEFERRED to Phase-2 (0.8.20); gap-1 ruling "1a"

**F-23 — OPP-12 Phase-1 Slice-0 rulings: anonymous-surrogate minting DEFERRED to Phase-2 (0.8.20); gap-1 ruling "1a" (HITL-approved 2026-07-09, adversarially audited).** At the 0.8.19 Slice-0 design gate, the load-bearing question — *does minting a surrogate `logical_id` for anonymous doc-seeded nodes migrate the dominant hit class `h:`→`l:`?* — was ruled **1a: doc-seeded/anonymous `SearchHit`s stay `h:<content-hash>`; the anonymous-surrogate mechanism is deferred IN FULL to Phase-2 (0.8.20).** **Rationale (grounded):** Memex's Phase-1 need is closed by the C-2 swap alone (`structural-lifecycle-contract.md §2(ii)` — its records already carry `logical_id`s); `h:` is a *first-class terminal `IdSpace` variant*, not a placeholder (`api-surface.md §SearchHit`); a Phase-1 surrogate has no stable meaning absent the Phase-2 natural-key/projection registry (C-1 Q6b). **Consequence:** `SearchHit.id` values stay byte-identical to today's `stable_id` output → **no gold-remap, eu7 keying a true no-op**; the C-2 swap is total via `l:`/`h:`/`p:` without the surrogate. **Independent adversarial audit (Steward-commissioned) confirmed 1a is correct and creates NO 0.8.20 unwind risk** (label-only ⇒ no published-surface lock-in / no "second break"; two migrations = correct I-6 discipline; mixed `l:`+`h:` is the designed end-state) — but found the ADR under-scoped the reconciliation. **Reconciliations carried (this finding):** surrogate moved to the 0.8.20 row (above); **R-ID-3** narrowed (governed nodes `l:`-addressable, anonymous stay `h:`, surrogate → Phase-2), **R-MIG-1** narrowed (19→20 migration = **existence columns only**, no surrogate backfill); the "lands-together GATING" co-requisite in the OPP-12 authority docs is **split** (swap total via `h:` without the surrogate); the **Slice-5/Slice-15 ladder** re-derived (Slice 5 builds no surrogate; **Slice 15 parallelizable off Slice 0**). **The other five Slice-0 gaps ratified (HITL 2026-07-09, unbundled):** purge edge-cascade = **cascade-remove** (edge-stubs are a non-breaking Phase-2 addition if ever wanted); `secure_delete` = standing PRAGMA + documented pre-20 freelist residual; read-without-`ReadView` = complete surface (undelete via `transition`); `PreparedWrite::Node` gains create-time `state`/`reason`; AC-074 governed-surface delta minus the deferred surrogate API. **0.8.20 guardrail (see §4 0.8.20 row):** `plan-0.8.20.md` (currently stale) must pin the doc-seeded `h:` end-state before Phase-2 proceeds (TC logged). *Ruling by HITL 2026-07-09 on a Steward recommendation + independent audit; reconciled by the Steward.*

### F-24 — 0.8.19 (OPP-12 record-lifecycle Phase-1) COMPLETE (2026-07-10, label-only close; HITL Option-A)

All mod-5 slices landed + verified from git: **Slice 5** existence axis + **SCHEMA 19→20** (existence columns only);
**Slice 15** C-2 typed `SearchHit.id` `u64→IdSpace` (`write_cursor`+`stable_id` subsumed; `to_prefixed()`
reproduces the prior `stable_id` byte-for-byte ⇒ **no gold-remap, eu7 keying a true no-op**, per F-23/1a);
**Slice 10** `transition`/`purge` verbs (codex §9 caught a real **GDPR-erasure leak** — `secure_delete` was
writer-only → fixed to every-open); **Slice 40** verification GREEN. **X1 py/ts parity re-confirmed GREEN
(26/26)** via a fresh isolated-clone rebuild (maturin-develop wheel + napi `.node` from `origin/main @
d4b5cd90`; durable log `fathomdb-x1-verify/x1-rerun-2026-07-10.log`) after the shared-checkout contention
forced the isolated-worktree/fresh-clone landing discipline (owed guardrail-fix proposal: release
orchestration in its own dedicated checkout). **AC-074 governed-surface delta HITL-SIGNED 2026-07-10**
(`transition`/`purge` + the non-command lifecycle/id types; **`recovery_denylist` UNCHANGED — five names**;
Phase-2 read-view/validity/projection types excluded); recorded at the JSON-allowlist `_comment` audit trail
per the JSON-governs precedent (AC-074 prose is a frozen illustrative subset, not mirrored). eu7 **no-op basis**
(default vector/ranking path byte-unchanged). **Label-only: manifests stay `0.8.9`; NO `v*` tag / publish** —
OPP-12's first real publish is 0.8.20's coordinated breaking-pair (#11-full @0.8.18 the proven prerequisite).
**Next = 0.8.20 Phase-2**; the F-23 GUARDRAIL / **TC-11** (pin the doc-seeded `h:` end-state + de-stale
`plan-0.8.20.md`) is the first Phase-2 step. *Close driven by the Steward under HITL directive; X1 re-run +
AC-074 sign-off + close all HITL-authorized 2026-07-10.* **→ TC-11 DISCHARGED by F-25 (2026-07-12).**

### F-25 — TC-11 DISCHARGED: the doc-seeded `h:` end-state is pin A (terminal-forever) by OVERRULE of `§2(ii)`

**F-25 — TC-11 DISCHARGED: the doc-seeded `h:` end-state is pin A (terminal-forever) by OVERRULE of
`§2(ii)`; the REQ-037 lawful-erasure carve-out is approved; and an erasure-completeness defect class is
opened (HITL 2026-07-12; codex §9 ×3 rounds; `plan-0.8.20.md` de-staled + authored).**
**(1) TC-11 / F-23 guardrail — CLOSED.** `structural-lifecycle-contract.md §2(ii)` mandates an opaque
auto-assigned surrogate `logical_id` for *every anonymous node* — doc-chunks **named** (`:97-107`; restated
at `engine-minted` in `dev/design/record-lifecycle-protocol/README.md`). It is **OVERRULED for the anonymous class.** Anonymous/doc-seeded nodes stay
`h:<content-hash>` **PERMANENTLY** — no backfill, no forward-mint, no split. The surrogate leg of the
"lands-together GATING" co-requisite (`api-surface.md:64`) is **CANCELLED, not deferred**; a surrogate serves
**only registry-admitted governed entities**, decided **at write time**, and a stored row's id-space is
**never re-derived** (invariant: no migration/backfill/verb may populate `logical_id` on an existing canonical
row — RED-testable as `NULL → NOT NULL` transitions == 0). **Grounds (code-verified):** an opaque surrogate is
**not re-derivable from content**, so re-ingest would mint a *different* id, destroying the re-ingest-stable
content-addressed identity `h:` provides (`same bytes ⇒ same handle` — the basis of gold keying, telemetry
`result_stable_ids`, explain-correlation); `derive_logical_id = SHA256("{kind}:{name}")` (`fn derive_logical_id` in `fathomdb-engine/src/lib.rs`) is
**hashed**, so `:54`'s opaque-id precondition *definitionally excludes* it as "the surrogate mechanism" (§2:98
names it a **separate** mechanism); §2(ii) has **no consumer** (the contract itself states Memex's need is
closed by **(i) alone**, `:106`); and its goal is **already met** — the shipped C-2 `IdSpace` is **total**
(`l:`/`h:`/`p:`, non-null) ⇒ **`h:` IS an address**. **Accepted corollary:** an anonymous row and a later
governed row for the same thing **both stay active and both surface in search** (supersession keys on
`logical_id`; NULL never collides with the partial-unique index) — the engine does not dedupe them; supplying
a `logical_id` at write time is what makes a record governed. **(2) The erasure axis is PROVENANCE, not the
`l:`/`h:` id-space** — so the pin is **orthogonal to GDPR** and costs nothing there. **(3) REQ-037
lawful-erasure carve-out — APPROVED.** The project's real policy is *"**recovery-NAMED** verbs are CLI-only"*,
**not** "destructive ⇒ CLI-only": **`purge(logical_id)` is already an SDK verb** (`fn purge` in `fathomdb-py/src/lib.rs`,
`pub async fn purge` in `fathomdb-napi/src/lib.rs`, 0.8.19) *despite being named in REQ-037's forbidden list*, because **AC-041 tests only the REQ-054 five-name
denylist**. The defect is an **asymmetry** — the `l:` axis has an application erasure verb, the `h:` axis (the
dominant corpus class) has none — compounded by the fact that **no CLI ships to SDK consumers** (the wheel
declares no `[project.scripts]`; npm no `"bin"`), so `excise_source` is **unreachable**. **Ruling:**
`excise_source` stays CLI-only (it is the *recovery* seam, REQ-026 — built to excise a **corrupt ingest**);
**`erase_source(source_id)` ships as a first-class SDK lifecycle verb** alongside `transition`/`purge`, on
**one shared engine path**; it is **not** a recovery name ⇒ **AC-041 stays GREEN and the denylist stays five**;
REQ-037's prose gains the carve-out and `purge_logical_id` is **struck** from its forbidden list (shipped code
already contradicted it — *the amendment records reality rather than changing it*). **(4) ERASURE-COMPLETENESS
DEFECT CLASS (root cause: the row-owned projection table list is IMPLICIT and duplicated FIVE times).**
`search_index_v2` is a **content-storing** FTS5 (`CREATE VIRTUAL TABLE IF NOT EXISTS search_index_v2` in
`fathomdb-schema/src/lib.rs`) holding the **body verbatim**, yet it is written
by the write path (`fn project_canonical_node_row` in `fathomdb-engine/src/lib.rs`) and deleted **only by `purge`** (`:6229`) —
`excise_source_inner` (`:6398`), `rebuild_shadow_state` (`:6515`), and
`reproject_search_index_after_tokenizer_upgrade` (`:9515`) **never touch it**. Consequences: erased bodies
**survive on disk** (`secure_delete=ON` cannot help — it only zeroes pages freed by a real `DELETE`); the table
**monotonically accumulates every body ever written**; and after a tokenizer upgrade **v1 and v2 diverge** (BM25F
scores on a stale tokenizer). **It is invisible to every functional test** — both FTS read paths gate on
`canonical_nodes`, so *a test that searches for the excised text passes on the broken code* ⇒ **RED tests MUST
assert on raw table contents.** There is also **no edge projector** (edge projection is inlined in
`commit_batch`), so any projector-replay rebuild must cover edges or it silently drops edge FTS + vectors.
Further verified gaps: ingest takes provenance from the **model's JSON echo** (`:3644`, `:3771`) rather than the
caller's `ExtractDocument.source_doc_id` (`:1934`, already in the prompt at `:3598`); `source_id` is
`Option<String>` (`:2024`, `:2051`) ⇒ silently un-erasable rows; `truncate_wal()` **exists** (`:6379`, typed
`Busy`) but **`purge`/`excise` never call it**; the telemetry sink retains `l:` ids post-erasure (`:4462`) and
`logical_id` is a hash of a **low-entropy natural key** (dictionary-attackable); op-store payloads have **no
record-level delete** (`:10083` is a cap sweep only). **All folded into `plan-0.8.20.md` §3 (R-20-E1…E8) /
Slice 5.** Design of record: `dev/design/0.8.20-erasure-and-h-end-state-v4.md` (codex §9 rounds 1–3).
**Until 0.8.20 ships, FathomDB MUST NOT be represented as GDPR-erasure-capable.** **(5) `plan-0.8.20.md`
DE-STALED** — the Library-Sweep/dep-migration runbook it carried is removed and **owed to `plan-0.8.22.md`**
(F-19/F-20). *Ruling by HITL 2026-07-12 on a Steward recommendation + 3 adversarial codex rounds; reconciled by
the Steward.*

### F-26 — 0.8.20 Slice-0 (X0 design gate) SIGNED + LANDED; the rubric guardrails are now MECHANICALLY ENFORCED (HITL 2026-07-19, `403eb254`)

The X0 package — frozen §3 requirements + RED-testable acceptance signals, the
Slice-0 erasure design (`dev/design/0.8.20-slice0-erasure-design.md`, a v5 addendum on the v4 design of record),
and the codex §9 review — was **HITL-SIGNED and merged to `main` at `403eb254`**; codex's terminal verdict was
**PASS** (after one fix round). **Slices 5+ are unblocked.** Two process guardrails moved from *adopted prose* to
*enforced mechanism*: **TC-RUBRIC-5** (dedicated-checkout-per-orchestration) is now enforced by
**`scripts/preflight.sh --landing`, which HARD-fails on the primary checkout** — per the standing
*guardrail-failures-fix-the-tooling* rule, the guardrail is code, not a reminder; and **TC-RUBRIC-7**
(persist every codex §9 transcript to a durable release-namespaced path) is now **pinned at
`dev/plans/runs/codex/0.8.20/`**, `.log` (not `.md`) to match the `0.8.16-slice-*-codex-review-*.log` convention
and stay out of markdownlint scope. **Process caveat carried (TC-18):** committing a transcript *into the
reviewed range* pollutes the next review's diff — at Slice-0 the fix-1 re-review re-read codex's own prior
findings verbatim off the unchanged `--base`, which reads as "the finding was not fixed" when it was.
**Mitigation for every later slice: commit transcripts only AFTER the slice's final review round.** Slice 0
surfaced **nine** findings handed up for reconciliation (`dev/plans/runs/STATUS-0.8.20.md` §10; ledger
**TC-12…TC-19**) — the load-bearing ones are **F-27** (audit durability), **F-28** (eu7 closure), **F-29**
(AC minting floor), **F-30** (dead publish guard), **F-31** (`SourceId` breakage). *Signed by HITL 2026-07-19;
reconciled by the Steward.*

### F-27 — THE ERASURE AUDIT TRAIL IS DESTRUCTIBLE; HITL RULED IT MUST BE DURABLE (2026-07-19). Refutes the v4 design of record

`dev/design/0.8.20-erasure-and-h-end-state-v4.md` §3.6 asserted that `excise_source_audit`
*"retains `source_id` **permanently** (`:6479`) — by design; that is the audit trail"*, and `plan-0.8.20.md` §8
repeated it as the **justification for the non-PII `source_id` rule**. **VERIFIED FALSE at `4ca70ba6` (TC-15):**
the audit row is written into **`operational_mutations`** (`engine lib.rs:6479`), and
**`enforce_provenance_retention` (`:10070`) sweeps that same table** cap-first, oldest-id-first, with **NO
collection filter** (`:10083-10089`, `DELETE FROM operational_mutations WHERE id IN (SELECT id … ORDER BY id
LIMIT ?1)`). Erasure-audit rows are swept like any other op-store row. **Two consequences:** (1) an
**accountability gap** — demonstrating **that** an erasure occurred is an obligation **distinct** from
performing it, and a retention sweep can silently destroy the proof; (2) a **collision with erasure work-item
9** — `excise_collection_record` operates on the **same table**, so **the erasure record and the payloads it
must prove erased share one retention pool**. **HITL RULING 2026-07-19: *"there must be an auditable record of
deletion event."*** The erasure audit trail **MUST be durable** ⇒ **Slice 5 EXEMPTS the erasure-audit collection
from `enforce_provenance_retention`** (filter the sweep by collection) and **states the audit-durability
guarantee explicitly**. This is a **retention-policy change, not a pure defect repair** — hence HITL-decided.
**The non-PII `source_id` rule STILL STANDS** (an unswept audit row still carries it, and under this ruling it
is now *intended* to outlive the erased payload) **but its stated rationale was argued from a false premise and
is corrected.** Reconciled: v4 §3.6 **struck-and-annotated as SUPERSEDED** (original text retained, not deleted),
v4 header banner, `plan-0.8.20.md` §8 + §10. *Ruled by HITL 2026-07-19 on a Slice-0 code-verified finding;
reconciled by the Steward.*

### F-28 — R-20-EU7 is CLOSED BY HITL DECISION, NOT BY MEASUREMENT: ZERO eu7 runs are authorized, on ANY backend, at ANY N (2026-07-19)

The 0.8.20 eu7 basis gate (F-22) is **closed on existing evidence**. **Not GPU, not
CPU, not reduced-N, not a smoke run, not a confirmation run.** HITL verbatim: *"no one run. it always turns into
10 runs and 3 days and token burn only for it to become a HITL decision anyway. I did already."* **Basis:** CPU
and GPU are **equivalent** — 0.8.7 proved the CPU↔CUDA embed path **bit-identical** — so the **0.8330** GPU eu7
figure (`dev/plans/runs/0.8.14-slice-20-eu7-gpu-run-20260705T205222Z.log:73`) compared against the **0.8960 CPU**
reference (CI [0.8640, 0.9250] ⇒ PASS on the one-sided gate) was a **cross-backend comparison artifact — NOT a
regression and NOT a backend defect**. Pin A independently keeps `SearchHit.id` byte-identical
(`to_prefixed()` == prior `stable_id`) ⇒ **no gold-remap**, so the no-op basis holds on the argument. **Running
eu7 to "confirm" this is FORBIDDEN, not merely unnecessary** — proposing a cheap confirming run on a question the
HITL has already decided **re-opens a closed decision** under the cosmetic framing of confirming it, and is
exactly the measurement-spiral this ruling guards against (**a Steward error, corrected in-flight**: the
withdrawn "one fast run at Slice 40" clause had already reached the Slice-5 orchestrator's brief).
**Consequently:** **TC-19** — the eu7 harness **cannot complete on CPU on this box** (`BATCH = 256` cannot drain
inside its hardcoded 600 s at the measured **0.179 docs/s**, ~7.3× below the documented rate, so it fails on the
**first batch at any N**) — is **CLOSED BY ROUTING, not by a fix**; the `BATCH 256→64` proposal is **WITHDRAWN**;
and **investigating the CPU throughput shortfall is EXPLICITLY FORBIDDEN** (HITL: *"we understand it"*).
**TC-12**'s "capture on CPU only" recommendation is **OVERRULED on the merits** — it was correct that
*cross-backend* comparison is invalid, but backend equivalence makes the capture itself moot. **TC-13**'s harness
hazards are **retained as knowledge, NOT scheduled work**: the documented run command is wrong (needs
`--features default-embedder,operator`, else exit 101) and the gitignored `data/corpus-data/` makes a
**worktree run a vacuous skip-with-exit-0**. *Ruled by HITL 2026-07-19 (correcting an earlier same-day
"capture same-backend on GPU" routing, now withdrawn); reconciled by the Steward.*

### F-29 — AC ids mint from AC-079; AC-077/078 are LIVE RESERVATIONS and "max AC id + 1" is an unsafe minting rule (HITL 2026-07-19)

`plan-0.8.20.md` §3 stated *"AC ceiling today = **AC-077**"* and that new ACs *"continue
from"* it — **wrong, and a collision with a live reservation.** The highest **defined, non-reserved** AC is
**AC-076** (`dev/acceptance.md:1147`). **AC-077** (`:1286`) is a **RESERVED PLACEHOLDER** for the agentic-IR
**IR-1/IR-2** initiative (*"not yet a gate; no fabricated numbers"*, maps to REQ-067, all thresholds TBD), and
**AC-078 is CONDITIONALLY reserved to the same initiative** (`:1297` — *"+ AC-078… only if the consensus splits
the measure"*). **0.8.20 mints from `AC-079`.** **The trap (TC-14):** a naive grep for the maximum `AC-0NN`
returns **AC-078 out of that reservation's prose**, so an agent applying "max + 1" mints a colliding id while
believing it verified the ceiling — any future release minting ACs **must read `dev/acceptance.md:1286-1300`
first**. Per the **locked-`acceptance.md` policy**, the standing preference remains **tracking by
requirement id + TDD test name over minting per-feature ACs at all**. Reconciled into `plan-0.8.20.md` §3 (inline warning),
the R-20-AC row, and the Slice-40 row. *Ratified by HITL 2026-07-19 on a Slice-0 finding; reconciled by the
Steward.*

### F-30 — THE PUBLISH DRY-RUN GUARD HAS BEEN DEAD SINCE 0.8.14, and 0.8.20 is the FIRST RELEASE THAT ACTUALLY PUBLISHES — currently UNOWNED, needs a home

`scripts/tests/test_actionlint_fixture.sh:53-56` asserts that
every `publish-rust-<tier>` job block in `.github/workflows/release.yml` contains the literal
`cargo publish --dry-run -p` (with a trailing space). It fails on the **first** tier (`t1-embedder-api`), which makes
**`./scripts/agent-test.sh` exit 1 wholesale**. **The workflow is FINE; the TEST is STALE** (verified at
`4ca70ba6`): the `publish-rust-t1-embedder-api` job in `.github/workflows/release.yml` no longer inlines the command — it delegates to
`bash scripts/release/cargo-publish-if-new.sh --dry-run fathomdb-embedder-api`, and that helper **does** forward
to `cargo publish --dry-run -p <crate>` (`cargo-publish-if-new.sh:14`). **So publish BEHAVIOR is intact — this is
not a publish defect. The harm is the MASKED GATE:** a permanently-red assertion is a gate nobody reads, so a
**genuine** publish-wiring regression is now invisible — and **0.8.20 is the first real firing** of the publish
path (manifests **`0.8.9 → 0.8.20`**, cargo order **embedder → engine**, and a pushed `v*` tag **auto-fires REAL
crates/PyPI/npm**). **Same class as the `perf-recall-gates-masked` trap.** `dev/plans/runs/0.8.14-slice-40-output.json:23`
already recorded it as *"the only remaining agent-verify red"* — it has been **carried unfixed across 0.8.14 /
0.8.16 / 0.8.18 / 0.8.19 into 0.8.20**. **Fix shape (TC-16):** re-point the assertion at the delegation — assert
the job invokes `cargo-publish-if-new.sh` **with `--dry-run`** AND that the helper forwards to
`cargo publish --dry-run -p` — then confirm `./scripts/agent-test.sh` is **GREEN before the publish gate**.
**⚠ OWNERSHIP: this item has NO assigned slice or owner as of 2026-07-19** — it is a natural fit for the 0.8.20
Slice-40 release-readiness step (alongside R-20-PUB), but **that placement is NOT yet made**; the Steward owes a
placement call. **✅ PLACED 2026-07-24 (HITL): TC-16 folds into 0.8.20 Slice 40**, alongside the `#11`-full
tag→publish rehearsal — the slot the Steward owed is now made. See **F-32**.
*Found at 0.8.20 Slice-0; recorded by the Steward — placement CLOSED 2026-07-24.*

### F-31 — Slice 5's `SourceId` newtype WILL BREAK the eu7 harness and every other in-tree `PreparedWrite` construction; the v4 design did not enumerate them

R-20-E3 / v4 §3.2 replaces `source_id: Option<String>` with a **mandatory `SourceId` newtype** on the public `PreparedWrite`, making "no provenance" **inexpressible**
— which is the *point* of the change, but it means **every** existing in-tree construction using
`source_id: None` **stops compiling**. **v4 §3.2 anticipated only two internal callers** (`:5186`, `:5209`, the
`doc(hidden)` poison fixture) **and missed the TEST-side callers entirely. Confirmed instance:**
`src/rust/crates/fathomdb-engine/tests/eu7_real_corpus_ac.rs:405` seeds the real-corpus haystack with
`source_id: None, logical_id: None`. **Action for Slice 5 (TC-17): sweep ALL `PreparedWrite` constructions
across `src/` AND `tests/` — not just the two names v4 lists — give each a real `SourceId`, and re-verify the
affected test targets still BUILD before landing.** Cheap with the change, expensive to discover later.
**Note the interaction with F-28:** eu7 is now **closed by decision and will not be run**, so a broken eu7
harness would **no longer be caught by executing it** — the build must be verified directly (the
full-workspace `cargo check --workspace --all-targets` gate covers this, which is precisely why that gate is
release-DoD). *Found at 0.8.20 Slice-0; recorded by the Steward.*

### F-32 — 0.8.20 ladder reconciliation: Slices 5/10/15 LANDED (SCHEMA 24), the eight-item HITL queue RULED, TC-16 and TC-45 PLACED

**F-32 — 0.8.20 ladder reconciliation: Slices 5/10/15 LANDED (SCHEMA 24), the eight-item HITL queue RULED,
TC-16 and TC-45 PLACED (HITL 2026-07-24; reconciled by the Steward).** The master had fallen three slices and
eight decisions behind the repo — exactly the "board lies about git" failure the status-board-currency work
exists to end. **Verified from git @ `origin/main` `fae276b9`:** **Slice 5** `1f8ed8bf` (erasure completeness,
R-20-E1…E8 — the `search_index_v2` body-leak fixed at the mechanism) · **Slice 10** `3cfb3cda` (`ReadView` /
read-modes + node-validity, SCHEMA 21→22) · **Slice 15 keystone** `a2022957` (**R-20-PR** row-owned projection
registry = the C-1 co-land · **R-20-EAV** EAV/property-FTS · `filterable` **pre-KNN** vec0 routing, TC-46
Option 1 **non-destructive** reshape · **TC-33** temporal model harmonised to INTEGER epoch with an engine-side
hard-reject ISO round-trip, **TC-47** · **TC-34** · **Finding-1 = (A)** attribute-filter drops edge hits, (D)
reserved · `#[non_exhaustive] SearchFilter`), **SCHEMA now 24**; codex §9 terminal-clean, gates re-verified
(clippy 0, check 0, (A) pin 1/1, AC-041 3/3, recovery denylist unchanged at five). **Remaining ladder:
20 → 25 → 30 → 40**; Slices 20 and 25 are UNBLOCKED (they needed R-20-PR). **HITL rulings 2026-07-24 (plan §11
carries the full text):** (1) sign **AC-079 once at Slice 40** — it is still **PROPOSED / NOT SIGNED**, and
publish is blocked until it is; (2) **adoption arms** — Phase-2 surface **OPT-IN**, erasure fixes **ON**;
(3) **publish at Slice 40**, subject to H7-green + the `#11`-full tag→publish rehearsal + Memex readiness;
(4) an `fts`/`vector` sub-object without the `searchable` role is an **invalid spec — REJECT** (fail-fast), at
the next `configure_projections` slice; (5) **add the `embed_batch_cls` TS binding** (F-22 closed, X1 / Slice
40); (6) **TC-16** (the dead publish dry-run guard, red since 0.8.14) **→ Slice 40** — F-30's ownership gap is
CLOSED; (7) **TC-45 → FIXED IN 0.8.20, folded into Slice 20** (below); (8) the Hermes consult on the future
**(D)** endpoint-node filter widening is pending input and blocks nothing. **TC-45 placement (HITL, overriding
the Steward's earlier "0.8.21 own fixup" recommendation):** `record_projection_terminal` writes
`state='superseded'` under `INSERT OR IGNORE` while the terminal CHECK admits only `('failed','up_to_date')`,
so the terminal is **silently dropped** and `advance_projection_cursor` can **stall** (todos ledger seq 65;
pre-existing from fix-32, NOT a 0.8.20 regression). It is fixed **in-release** because 0.8.20 is the first real
publish **and it publishes the projection registry** — shipping a known silent cursor-stall in that machinery
to the registries turns an internal defect into a published one, its twin (TC-33 fix-4) was already fixed in
this release, and "carried unfixed across releases" is the F-30 trap. **Implementation note (open micro-call
for the implementer + codex):** prefer `'up_to_date'` at both call sites (**no migration**) over widening the
CHECK to admit `'superseded'` (**a schema step + migration test**); escalate only if the terminal's semantics
demand the distinct token. **Also reconciled:** `plan-0.8.20.md` §11 + `STATUS-0.8.20.md` §2 carry the same
placements, so master / plan / board now agree. *Landings verified from git by the Steward; decisions by HITL
2026-07-24; reconciled by the Steward.*

### F-33 — DOC-HYGIENE-1 LANDED (2026-07-25): the cross-cutting docs/tooling effort that gated the 0.8.20 Slice-20 commission is DISCHARGED

**F-33 — DOC-HYGIENE-1 LANDED (2026-07-25): the cross-cutting docs/tooling effort that gated the 0.8.20
Slice-20 commission is DISCHARGED, and it left two STANDING GATES behind.** Ruled by HITL 2026-07-24 (todos
**TC-48**) as *all* cleanup items run as **one dedicated effort BEFORE** Slice 20 — a wide docs diff must not
run while an orchestrator is live (**F-7** collision rule), and the between-slices window was the only safe
one. **Verified from git @ `origin/main` `f53b8c64..597738d9` (fast-forward), steward ledger seq-101; carries
NO pico label (HITL 2026-07-25)** — it is cross-cutting hygiene, not a release increment, so the two-tier
numbering model is untouched. **Four tranches:** **T1** — the markdown gate had been **RED on `main` since
`c366d6e0` (2026-07-02)** and unseen for three weeks (**TC-49**), because **TC-37** made `agent-lint-md.sh`
exit 0 *vacuously* in a worktree with no `node_modules` — so every orchestrator read a green that wasn't; also
**retitled the master `0.8.6-0.8.24` in place** (the path stays `0.8.6-0.8.16-PROGRAM-SEQUENCING.md` by the
archive-in-place rule — ~39 files reference it) and swept §5/§9. **T2** — `DOC-INDEX` split into a thin map +
per-area detail, **83.6 KB → 33 KB with all 199 paths preserved**, closed-line giants bannered, `prompts/README`
added; this is context cost, which is the Steward's scarce resource. **T3** — TC-37 now **hard-fails** instead
of skipping, `status:` frontmatter + lint, closed-release run-artifact prune (**2 read-input artifacts were
wrongly deleted and were RESTORED** per codex §9 [P2]). **T4** — status-board-currency items 1–3 (todos
seq-70): the **seam-ownership contract** (the Steward owns a board's **LANDED** row + next-slice pointer, in
the landing action itself), a board-currency **gate in `scripts/preflight.sh --landing`**, and a **CI drift
detector** (`scripts/check-board-currency.sh`) that goes red when a live board contradicts git ancestry, plus a
**vacuous-pass guard** (a live board matching zero rows HARD-fails). codex §9 terminal: **1 P2 fixed, no P1.**
**Both gates verified GREEN on `main` by the Steward** (`check-board-currency.sh` exit 0; `agent-lint-md.sh`
exit 0 via `PIPESTATUS`). **Why it is a finding and not just a chore:** it closes the *exact* failure this
program keeps re-hitting — a gate that reports green without running (**TC-37**, same class as
`perf-recall-gates-masked` and **F-30**'s dead publish assertion) and a board that lied about git for four days
post-land. Per `[[guardrail-failures-fix-tooling-not-people]]`, both are now mechanical, not advisory.
**Executed by the Steward as orchestrator (an explicit HITL-directed role change for this effort, not a
precedent that dissolves the §9 commission-and-verify separation).** *Landing verified from git by the Steward;
scope + no-pico ruling by HITL 2026-07-24/25; reconciled by the Steward 2026-07-25.*

### F-34 — RUN AUTHORIZATION (HITL 2026-07-25): the 0.8.20 tail runs to completion under a STANDING STEWARD MANDATE

**F-34 — RUN AUTHORIZATION (HITL 2026-07-25): the 0.8.20 tail runs to completion under a STANDING STEWARD
MANDATE, preceded by DOC-HYGIENE-2, with exactly TWO stops reaching the HITL.** A cold-start retrospective
(multi-agent audit, 2026-07-25) measured *why* stewardship keeps re-paying for the same orientation and found
the cause is **narrated state with a 5–12 file write-side fan-out** — verified: one reconciliation commit
(`b70629e5`) touched **7 files**, and the master is **129,631 B (2.3× the readable cap)** with **60.6%** of it
an unanchored append-only findings log. The remediation is sequenced ahead of the remaining ladder.
**Authorized sequence:** `DOC-HYGIENE-2` → Slice 20 → Slice 25 → Slice 30 → *(batched surface decision)* →
`DOC-HYGIENE-3` → Slice 40 *(publish gate)* → `N6`. **Slices 20/25 run SEQUENTIALLY** (HITL; the parallel
option with serialized `lib.rs` merges was declined). Both hygiene efforts are **cross-cutting, NOT release
increments ⇒ NO pico label**, per the DOC-HYGIENE-1 precedent (F-33); the two-tier numbering model is
untouched. **DOC-HYGIENE-2 scope:** the four verified drift items (18 backticked line-anchors in
`plan-0.8.20.md` — *counted, not estimated*; the 4th `/goal` instruction site at
`STEWARD-SESSION-HANDOFF-2026-07-24-A.md:15`; the **duplicate `F-11` at L416/L418** with the stale copy
second, whose rename was ruled 2026-07-03 and never executed; the §4 **L238-vs-L219** dep-migration
contradiction) · a ledger-invocation fix, ledger-integrity gate, findings anchors + duplicate-ID lint, a
line-anchor ban, and the **T1e allowlist-pin gate** · then the structural change: **single-writer release
state with generated views**, the **ledger as ruling registry**, and **frontmatter governance over
`dev/design/**`** (108 docs, **only 23 carry `status:`** — the largest unlinted surface, and the tier a
Steward must cite to an orchestrator) · then a generated orient script + commission manifest.
**Five HITL rulings fold in:** (1) **X0 per-slice HITL sign-off is NOT required** — resolution is codex §9 +
fix-N iteration, escalating only when unresolvable; (2) the **fix-N circuit-breaker is now a STANDING RULE
and quantified** — 3 rounds on one finding / 6 per slice, escalating to the **Steward** first
(`orchestration.md` §6 + §1.5 invariant 3, which previously said only "a small bound" and routed to the
HITL); (3) **reviewer substitution** — local `/code-review` when codex is unavailable, **never an
ultra-level/billed review**, TC-RUBRIC-7 persistence binding either way; (4) **AC-079 PRE-SIGNED**, pinned to
the allowlist content, mechanically re-opened by any diff (minting still occurs at Slice 40); (5) **§7
prereq-5 Memex co-land readiness CLOSED BY DECISION** — Memex adapts to 0.8.20's surface, no confirmation is
to be sought, and **push scope stays fathomdb-only**. **The two remaining stops: the batched governed-surface
decision (Slice 30 → 40) and PUBLISH (Slice 40, hard).** Band overflow (plan §5) still halts.
**Deferred by ruling:** `dev/design/**` frontmatter backfill defaults to `status: UNREVIEWED` for anything
not confidently classifiable — deliberately provisional, with a **dedicated post-0.8.20 slice** owed to do
the real classification (todos ledger). *Retrospective + plan by the Steward; every ruling by HITL
2026-07-25; recorded by the Steward.*

> **⚠ SUPERSEDED IN PART, 2026-07-30 — "exactly TWO stops" IS STALE. Everything above is kept verbatim
> as the 2026-07-25 record; this note corrects the STOP COUNT only, and nothing else.** Following F-35's
> precedent — which adjusts F-34's sequence without rewriting it — the run authorization and the standing
> mandate both stand; only F-34's arithmetic does not.
>
> **The first of the two named stops is CLOSED.** The **batched governed-surface decision** was **SIGNED**
> by the HITL at steward `seq-157`; cite it, and do not re-open or re-seek it (§4 `0.8.20` row). **PUBLISH
> remains open and is still the only decision that HALTS THE RUN** — but it is no longer the only one
> outstanding, and further HITL calls have surfaced that F-34 could not have contemplated.
>
> ⛔ **This note states NO count, on purpose.** A number written into prose here is precisely what rotted.
> **The single authority for the live set is the `unruled` array of `dev/plans/release-state-0.8.20.json`**,
> whose per-entry `halts_run` flag is what distinguishes a run-halting gate from a decision that merely
> needs an answer. Read it at the moment you need it; do not restate it, here or in a commission brief.
>
> ⚠ **Known gap at the time of writing, so the registry is authoritative but not provably complete:** the
> **Axis-E version for `fathomdb-embedder-api`** is an open HITL call carried by the Slice 40 commission
> brief (PHASE 1 — *determine and recommend, do NOT pick*; `set-version.sh` deliberately skips Axis E) and
> it is **not registered in that array**. Register it there rather than adding a second prose list.
> *Verified against the state file and recorded by the Steward 2026-07-30.*
>
> ✅ **CLOSED 2026-07-31 — and the closing itself demonstrated the defect this note warns about.** Axis-E was
> registered at `c73c367a`; it is **RULED** as of `seq-229`. But that registration updated the single writer
> **and nothing else**: the generated numeral followed TWO → THREE automatically, while **four hand-written
> copies did not** — the board's §1 cell (which `steward-orient.sh` prints verbatim to every steward), the
> board's §4 enumeration, the session hand-off's §4, and *this note*. `check-release-state-views.sh` stayed
> rc=0 throughout, because the generated fence covers the **count**, not the **list**. Worse, the numeral
> landed on THREE — the number a stale clause had *already* claimed since `80f83bf7`, for a different and
> also-wrong reason (`platform-publish-schedule`, which `2159fb03` had moved to `ruled_tracking`). **The one
> control that fired was invisible because the prose beside it was wrong in a way that made the correct new
> value look like no change.** ⛔ **Do not re-transcribe the open set into prose anywhere.** The durable fix
> — a generated fence over the unruled *ids*, not just their count, proven by a control that FAILS first —
> is owed and is deferred to immediately after the 0.8.20 publish. *Steward, HITL-confirmed 2026-07-31.*

### F-35 — TAIL RE-SEQUENCED (HITL 2026-07-27): Slice 30 runs FIRST, net-new slices 21, 22 and 31

**F-35 — THE 0.8.20 TAIL IS RE-SEQUENCED AND RE-SCOPED (HITL 2026-07-27; steward ledger `seq-119`,
todos `TC-74`…`TC-78`).** **Amends F-34's authorized sequence; the standing mandate itself is unchanged.**
**New order, still SEQUENTIAL:** **Slice 30 → 21 → 22 → 31 → `DOC-HYGIENE-3` → *(batched governed-surface
decision)* → Slice 40 *(publish gate)*.** **Slice 30 moves to the FRONT of the remainder** — it is the
publish precondition, it is fully specified (§3 `R-20-H7`), all four dependencies are landed, and the C-1
Q6(b) amendment (`efa8d584`) removed the hazard that would have made the gate permanently red; it is also
the slice likeliest to surface a contract-conformance failure that reshapes what follows, so discovering
that *before* three slices of hygiene is worth more than numeric tidiness.

**Net-new reserved-gap slices (plan §5 pre-authorizes the gaps; no mod-5 spine change, no I-edge change, no
release-slot change).** **21 = `R-20-CR`** — TC-57 (the governed-write vs projection-worker race)
characterize-**then**-fix per `seq-111`, the **ac_002** oracle replaced by *isolation* rather than a third
patch of its substring scan, and **TC-71** (`vector_projection_declared` must require the `searchable`
role) folded in rather than carried past publish. **22 = `R-20-VC`** — **TC-67 option (c)**: declaring a
vector projection over a kind the vector writer cannot commit must **REPORT**; the `resolve_source_type`
vocabulary is **NOT** grown and the **Pack-1 D3 partition-key lock is untouched** · **TC-68** equivalence
probe **fingerprint-cached** against embedder identity (preserving the 0.8.18 guarantee rather than
weakening it) · Slice-15b **decision #18** settled before the surface is signed · the **sqlite-vec `#99`**
`vec0` DELETE probe. **31 = Library Sweep #3**, which **deliberately carries NO requirement id (`TC-76`)** —
dependency hygiene is not a release requirement, and **F-12 treats a Library Sweep as a pico**; placing this
one in-ladder before publish is a **recorded departure**, not a silent overwrite of that precedent.

**Consequence for the two remaining stops.** The **batched governed-surface decision now sits after Slice
22**, not strictly at the 30 → 40 boundary, because 22 may add surface (TC-67, #18). A pin trip mid-slice
means **HALT AND ESCALATE TO THE STEWARD** (`seq-113`), never a workaround. **PUBLISH remains the hard HITL
gate at Slice 40**, unchanged. **Also ruled the same day:** **TC-74** — the Slice-40 workspace test gate is
**serialized**, with a **NON-BLOCKING parallel reporting arm** so the races stay visible, and race-hunting
defers to 0.8.21 · **TC-75** — the fix-round cap for engine slices becomes 3 same-finding / **10** total
with a mandatory **Steward check-in at 6** (amends F-34 ruling 2 and `orchestration.md` §6 for engine
slices only) · **TC-77** — plan §5's band-overflow tripwire is **kept**, *conditionally*: it returns to the
HITL if Slice 21 or 22 spawns **two or more** further slices · **TC-78** — Dependabot `#153`
(`setup-node` 6→7) is held to post-publish, monitored for re-opening if the Slice-40 rehearsal fails on npm
auth · **`seq-118`** — Slice 40 makes the **TC-16 determination FIRST**, before the `#11`-full rehearsal,
because a rehearsal run against a path already known broken proves nothing. *Every ruling by HITL
2026-07-27; proposed, verified and recorded by the Steward.*

### F-36 — TAIL RE-SEQUENCED AGAIN (HITL 2026-07-28): TC-86 transcript hygiene runs FIRST, Slice 23 is net-new, and the batched governed-surface decision moves to the close of Slice 23

*(**Body section recorded 2026-07-30.** The ruling itself is HITL 2026-07-28 and is unchanged. Until now
F-36 existed ONLY inline in the §4 `0.8.20` row — so F-37, which supersedes "the F-36 order", cited a
number with nothing behind it, and the one finding in this file that could not be cited was a
re-sequencing of the live tail. **The §4 row is deliberately left exactly as written**; this section is
the citable record. Content lifted, not re-decided.)*

**F-36 — THE 0.8.20 TAIL IS RE-SEQUENCED AGAIN (HITL 2026-07-28; steward ledger `seq-129`).**
**Supersedes the F-35 order; the F-34 standing mandate is unchanged.** **New order, still SEQUENTIAL:**
**⟨TC-86 transcript-hygiene fix⟩ → 21 → 22 → 31 → `DOC-HYGIENE-3` → ⟨batched governed-surface decision⟩
→ 40.**

**TC-86 runs FIRST and is CROSS-CUTTING** — no slice number, **no `R-20-xx` id, no pico label** (the
`DOC-HYGIENE-3` treatment, `seq-106`). It closes a *measured* near-miss: a codex §9 transcript arrived
carrying **216 lines of raw Claude Code session JSONL from three other projects**, bound for a tracked
path in this **PUBLIC** repo. It was caught and redacted before landing, and `git grep` proved **zero**
already-committed files carry that shape. The mechanism is structural — every §9 review runs under
`--dangerously-bypass-approvals-and-sandbox`, TC-RUBRIC-7 requires the transcript be persisted under a
**tracked** path, and nothing between those two steps inspects the contents — so it is fixed in the
**tooling**, not with guidance.

**Net-new reserved-gap slices** (plan §5 pre-authorises the gaps; no mod-5 spine change, no I-edge
change, no release-slot change). **21 = `R-20-CR`** — TC-57 characterize-**then**-fix · the **ac_002**
oracle replaced by isolation rather than re-patched · **TC-71**. **22 = `R-20-VC`** — **TC-67 option
(c)**: *report*, and do **NOT** grow the `resolve_source_type` vocabulary, Pack-1 D3 lock untouched ·
**TC-68** fingerprint-cached equivalence probe · Slice-15b decision #18 · the sqlite-vec `#99` `vec0`
DELETE probe. **31 = Library Sweep #3**, which **deliberately carries NO requirement id** (**TC-76** — a
recorded departure from F-12's pico precedent, not an overwrite of it). *(21/22/31 are as F-35 scoped
them; what F-36 changes is their ORDER relative to TC-86, not their contents.)*

**THE BATCHED SURFACE DECISION MOVES TO THE CLOSE OF SLICE 23** (HITL `seq-141`, superseding `seq-134`
and `seq-140`), where **Slice 23 = `R-20-SV` is NET-NEW** (`seq-139` — the `fts`/`vector` reject ruled
2026-07-24, plus the TC-90/TC-91 characterization owed by `seq-136`). **23 is the LAST unit that can
move the governed surface**: the allowlist is SDK *command names*, and neither Slice 31 (a dependency
sweep) nor `DOC-HYGIENE-3` (docs and tooling) can add or remove one — so the delta is **FINAL** there.
Non-blocking for 31 and `DOC-HYGIENE-3`; **Slice 40 depends on it** (AC-079 mints pinned to the
allowlist).

⚠ **Do not read this finding as the live sequence.** The F-36 order was itself superseded by **F-37**
(HITL 2026-07-29), and the batched governed-surface decision it placed has since been **SIGNED** at
steward `seq-157` — see **F-37** and the §4 `0.8.20` row. F-36 is the record of the 2026-07-28 ruling.
*Ruled by HITL 2026-07-28; lifted into a body section and reconciled by the Steward 2026-07-30.*

### F-37 — LIBRARY SWEEP #3 RE-SCOPED TO A TOOL (HITL 2026-07-29): Slice 31 becomes 31 → 32 → 33; TC-76 and TC-93 both land at 0.8.22

*(**F-36 now carries its own body section**, immediately above — added 2026-07-30. The note that stood
here, saying F-36 was recorded only inline in the §4 `0.8.20` row, is superseded: the §4 row is unchanged
and still carries the as-written text, but the citable record is now F-36's own section. F-37 follows
F-35's shape.)*

**F-37 — THE 0.8.20 TAIL IS RE-SEQUENCED AGAIN AND LIBRARY SWEEP #3 IS RE-SCOPED (HITL 2026-07-29; steward
ledger `seq-151`, `seq-152`, `seq-153`).** **Supersedes the F-36 order; the F-34 standing mandate is
unchanged.** **New order, still SEQUENTIAL: `31 → 32 → 33 → DOC-HYGIENE-3 → ⟨batched governed-surface
decision⟩ → 40`.**

**What Slice 31 was, and why the re-scope is not a scope cut.** Slice 31's contents were **never enumerated
in any document** — it inherited the `LIBRARY-BUMP-STEWARD.md` §1–2 manual loop by reference (union of open
Dependabot PRs + a fresh `cargo upgrade --dry-run` / `npm outdated` + `gh api …/dependabot/alerts`, then a
five-question triage per candidate). The only item ever *named* against 31 was **TC-76**, which moved to
0.8.22 at `seq-151`. The HITL replaced that manual sweep with a **tool**, expressly so the exercise is
**~90% mechanical** rather than a token-burning search → review → reason loop: a small, isolated
mini-project under `scripts/` with its own req/AC/design/tests/code, emitting a **CycloneDX-JSON SBOM** over
every tracked manifest, enumerating **library↔library** dependencies, and **diffing used-versus-published**
versions. Python; external libraries permitted and **named in the design**; the mini-project's manifests
stay **isolated**; generated reports **gitignored**; **recurring by design but NOT CI-gating** (informational).

**Three legs, no `R-20-xx` id on any of them** (the TC-76 precedent, re-affirmed): **31** = requirements +
acceptance criteria + design + **RED tests, no code** (**codex §9 reviews the req/AC/design, FIX-*n* complete
before close**) · **32** = the code only · **33** = **runs** the tool and writes the survey. **33 is tightly
scoped to ASCERTAIN, NOT IMPLEMENT** whether a **surgical ~1–5 SLOC** change would likely land each upgrade;
anything larger is **merely NOTED for the project to handle**. **No X0-style HITL sign-off on any of the
three** — codex §9 terminal-clean is the gate. Full spec: `plan-0.8.20.md` **§3a**.

**The binding guardrail.** **No leg applies a dependency bump, and none edits a manifest or lockfile inside
0.8.20.** The survey is an **INPUT to 0.8.22**.

**Two decisions closed the same day, both placing work at 0.8.22.** **TC-76** (`seq-151`) — hold `sqlite-vec`
at `=0.1.7` for 0.8.20 and ship the engine-side workaround; take the **0.1.9** bump at **0.8.22** with
`rusqlite 0.31→0.40`, which §4 already couples. ⚠ **The bump MUST move together with
`src/rust/crates/fathomdb-engine/tests/tc76_vec0_long_metadata_delete.rs`, which asserts upstream `#274`
STILL EXISTS — or a *successful* upgrade turns the suite red.** **TC-93** (`seq-152`) — **publish 0.8.20 with
the six Dependabot advisories OPEN**, no pre-publish "Slice 4x"; the advisories land at **0.8.22**. Basis
verified live: 6 open / exactly 2 HIGH, **both HIGH `scope=development`** in the root `package-lock.json`,
and the shipped Python wheel declares **`dependencies = []`** (`python/pyproject.toml:15`,
`src/python/pyproject.toml:12`) — **no open alert reaches a registry consumer of a published artifact.**
Neither ruling overturns the standing "Dependabot stays out of the sequence" directive or **TC-78**.

**Band (raised unprompted, as TC-77 requires): NOT TRIPPED.** §5 allocates gaps **31–39** to the 30 band —
nine slots — which goes to **three of nine**. TC-77's trigger is narrower still: it fires only if **Slice 21
or Slice 22** spawns two or more further slices, and 32/33 were spawned by an HITL scope ruling on 31.
*Every ruling by HITL 2026-07-29; proposed, verified and recorded by the Steward.*

### F-38 — `rust-windows` is a REAL ENGINE DEFECT, and CI's `verify`/`security` had been red ~3 weeks behind a skip-mask (HITL 2026-07-30, steward `seq-222`/`seq-223`)

A Steward-commissioned three-agent read-only diagnosis pass, run **before** commissioning Slice 40 rather
than discovering mid-flight. Every load-bearing claim was re-verified by the Steward from source, `ci.yml`
or the CI logs — two agent inferences were checked and one was overturned.

1. **`rust-windows` is not a flake — it is `TC-91`, bucket (c).** `TransactionBehavior::Immediate` occurs
   **exactly once** in the engine (`commit_batch`, TC-57's fix); `commit_projection_outcomes` opens plain
   **DEFERRED** with the same read-then-upgrade shape TC-57 characterised and deliberately left, and its
   error is **discarded**, so failed commits silently re-embed. **83 failures / 92 executed Windows runs =
   90.2 %**; it failed the first Windows run that ever reached it. ⚠ **The defect ships on Linux** —
   105–119 repeat-embeds over 200 rows, i.e. roughly every other row embedded twice, on the only platform
   0.8.20 publishes. That is **TC-91(a)**, open and unowned since Slice 21. **Ruled into Slice 40**; bar is
   **N=5 consecutive green `rust-windows` runs** — Slice 40's long pole, and unverifiable before pushing.
2. **A ~3-week masked red.** The most recent *successful* `ci.yml` run had `verify` and `security`
   **skipped** — a docs-only push. Every executed `verify` in the retained window **failed**; the stub
   drift dates to `f2f843ed` (2026-07-09). **The jobs were red the whole time and the greens were
   vacuous.** ⇒ **publish gate (i) is amended to be measured on the CI run of Slice 40's own LANDING
   commit** (`release-state-0.8.20.json`). This is the durable lesson: *a skipped job is not a passed job,
   and a release gate must name the run it is measured on.*
3. **Clearing the 7 pyright errors makes `verify` RUNNABLE, not GREEN** — `security` is predicted green,
   but `verify` moves its failure to the **test** stage. Its first CI run is therefore a **discovery
   event**, built into Slice 40's checkpoint BASE as the gate BASE exits on.
4. **Nine placement rulings, all HITL 2026-07-30.** **Slice 40:** TC-138 (both halves — `graph.py`
   silently dropped `source_id`, the field the erasure axis runs through) · TC-139 · TC-140 · OOS-18
   (with a fix-shape probe first, after the Steward flagged that it had recommended undiagnosed work into
   an un-splittable slice) · the tc57 fix + stale comment · the gate (i) amendment. **0.8.21:** TC-134 ·
   TC-135 · TC-141 · TC-142 · the tc57 anonymous-arm assertion. **No `plan-0.8.21.md` is opened** — the
   allocation lives here in §4; the ladder is written when the release opens.
5. ✅ **TC-136 RESOLVED.** `test-python`'s real state is **rc=0, 863 passed, 6 skipped**. It was never 40
   failures — one unrunnable prerequisite, correctly reported as a fourth state rather than as red.

*Proposed, verified and recorded by the Steward; every placement decided by the HITL.*

### F-39 — 0.8.20 is Linux-first; platform validation moves to 0.8.22 (HITL 2026-07-31, steward `seq-234`)

This supersedes only F-38's `rust-windows` accrual and the B4 macOS lock-holder disposition. 0.8.20 supports
and publishes **Linux x86_64 native artifacts only**. macOS/Windows CI, validation and native-artifact generation
are deferred to **0.8.22**; this is a native-support boundary, not a claim that Cargo source crates cannot compile
elsewhere. **B4 is cancelled and deferred to 0.8.22**, and its private patch must not land in 0.8.20.

TC-91 still ships on Linux, so it remains Slice 40 work. Its acceptance is **five consecutive relevant Linux CI
TC-91 greens**: each qualifying run must execute the non-ignored B3 TC-91 gate, rather than merely be a green,
skipped, or bootstrap-only Linux job. B5's canonical test-hooks binding route remains governed separately by
`seq-233`; the final PUBLISH gate (ii) remains explicit HITL-only.

---

## 7. Answers to the three evaluation questions

### Q1 — Can #3 and #4 be done out-of-band / in parallel?

- **#3 GPU — yes, strongly, and early.** It is the textbook OOB item: default build stays CPU, GPU is
  opt-in via a cargo feature + `FATHOMDB_EMBED_DEVICE`, so every existing test and byte-stability gate
  is unchanged. It has no upstream dependency. Scheduled **0.8.7, parallel to 0.8.6**, because
  single-stream GPU is ≈100–300× CPU (27 h embed → minutes) and accelerates every re-embed-heavy slice
  and experiment (incl. EXP-M4). **Do this first.** *Note the one downstream edge:* it feeds #4 (I-3) and
  the vec-equiv calibration (I-4).
- **#4 ONNX — structurally yes, but not early.** A new `impl Embedder` behind `EmbedderChoice::Caller` is
  a zero-engine-change drop-in, but it is **low urgency** (NVIDIA hardware in play; ONNX targets AMD/Intel
  we don't have), its productionization makes silent cross-backend numeric divergence likely (needs #5),
  and it has a rework-forcing dep on #3's device seam (I-3). So it lands **0.8.16, adjacent to #5**.

### Q2 — #5 vector-equivalence: parallel, deprecate, defer to 2.0.0, or essential early?

**Verdict: defer to late-0.8.x (0.8.18). Do not deprecate. Do not push to 2.0.0. Not essential early.**

- **Not essential early — and actively counter-productive early.** 0.8.x is the high-velocity
  experimentation phase: many re-embeds, algorithm changes, possibly a full embedder swap (EXP-M4).
  Every experiment **rebuilds a fresh index and reads it with the same backend**. The guard protects
  against *moving a DB across machines/backends* — which experiments do not do. Landing it early adds
  `Engine::open` friction (re-embed an N≈16–32 probe set on every open) plus a real
  tolerance-calibration burden (the check must be done at the **post-1-bit-quantization** representation
  against the binary-quant floor) — i.e. cost and churn during exactly the phase with the most index
  rebuilds, for ~zero protection.
- **Not 2.0.0 either.** The hazard is created *inside* 0.8.x the moment #3 (CUDA) and #4 (ONNX) exist:
  a GPU- or ONNX-built index read by a CPU reader silently degrades the dense arm. The design doc is
  explicit — §3 *must* land before FathomDB advertises portable DBs or runtime backend-swap, and before
  any production GPU-built index is moved to a CPU reader. That promise is a 0.8.x-era one.
- **So: build it as the capstone of 0.8.18, gated as the prerequisite to any "portable DB / runtime
  backend-swap" claim, bundled with #4's productionization** (cross-vendor is when divergence actually
  bites — ONNX vs candle numerics differ more than CUDA vs CPU of the same candle model; this is I-4).
- **Interim mitigation (cheap, ships with #3 in 0.8.7):** a documented *same-backend build-and-read*
  discipline + the existing `EmbedderIdentity` open-time pre-filter (catches model/revision/dim
  mismatch). #5 upgrades that from discipline to *enforcement* when #4 makes divergence probable.
- **Parallel?** It can be *authored* in parallel (localized embedder-crate + open-time change), but it
  should **activate** only at 0.8.18 to avoid open-time friction during experimentation.

### Q3 — How to handle #11–#14 (CI/CD) without starving the experiments?

**Principle: spend the minimum CI/CD that (a) makes every micro-release genuinely DoD-shippable and
(b) stops the gates from lying — defer everything heavy or net-new until its substrate exists or GA
nears. Run all CI/CD as a separate, mostly-$0, agent-parallelizable track (foundation slices + OOB
odd releases) so it never competes with experiment tokens.**

- **#11 Release — SPLIT.** The **minimal viable publish path** (rewrite `set-version.sh` to the two
  version axes, restore `release.yml` to the 8-tier topological order from `design/release.md`,
  dry-run, and push the 178-commit backlog) lands **early in 0.8.6** — without it, "DoD-shippable per
  micro-release" is hollow. The **heavy publish matrix** (multi-OS napi prebuilds, full cross-ecosystem
  gate, index-propagation orchestration) defers to the **0.8.18** GA-hardening capstone.
- **#12 perf-gate unmask — early but cheap, OOB 0.8.9.** It is a *correctness* bug: `ac_012/013/013b/
  019/020` are `AGENT_LONG`-gated so they never run per-push (vacuously green), and `ac_013b` asserts
  the 0.90 floor against a ~0.73 synthetic embedder. Fixing the gate *semantics* — run them where they
  can run, or honestly re-scope + relabel so the board stops implying a floor CI never checks — is
  small and high-value. OOB so it doesn't draw experiment tokens.
- **#14 AC-037 CI wiring + AC-050c cleanup — small, OOB 0.8.9 with #12.** Durably wire the
  `netns-deny-egress` security gate on a userns-permissive runner (it was confirmed green *once*,
  manually); clean up the pre-existing AC-050c removal-detect baseline failure.
- **#13 benchmark-and-robustness harness — defer to 0.8.19 (kept in 0.8.x).** It is **net-new
  authorship** (no `benches/`, no `scale.rs`, no `tracing` cargo feature, no stress suites), not a
  restoration — low near-term ROI. Some substrate (criterion benches) accretes naturally from the perf
  experiments, so authoring it late costs less. Build it only when GA-hardening needs continuous
  regression coverage.
- **Net spend: ≈1.5 releases of CI/CD total** (0.8.6 release-min + 0.8.9 OOB micro + 0.8.18 capstone),
  all off the experiment critical path.

---

## 8. How each release runs — the orchestrator contract (`/goal complete 0.8.z`)

Every release in this line is executed by an **orchestrator session** (the release-orchestrator role,
`dev/plans/prompts/0.8.x-RELEASE-ORCHESTRATOR-HANDOFF.md`), commissioned by the **Program Steward**
(`0.8.x-STEWARD-HANDOFF.md`), not a single implementer. Open the session with
`/goal` on that handoff (or this file's release pointer) and drive the release's `plan-0.8.z.md` ladder.
Per `dev/design/orchestration.md`:

1. **Requirements + acceptance criteria** are frozen at the release's **Slice 0** (design-ADR slice):
   the DoD table in `plan-0.8.z.md` becomes pre-registered, and where a gated slice mints a new AC it
   is added then (acceptance.md stays locked otherwise — `acceptance-md-locked-no-feature-acs`).
2. **Design → design-review** per slice: the implementer (or steward) writes a slice design doc; an
   independent **codex** pass reviews the design *before* code (`codex exec --dangerously-bypass-
   approvals-and-sandbox`, data+goals, no leaning).
3. **TDD implementation** in a pre-created worktree off `$(git rev-parse main)`: RED tests first, then
   GREEN; `implementer` subagent commits on the slice branch and writes `output.json`.
4. **codex §9 review** of the diff → PASS / CONCERN(+override) / BLOCK(→fix-N).
5. **Closure**: board (`runs/STATUS-0.8.z.md`) + one docs commit per transition; worktree cleaned.
6. **Release DoD** (Fathom is DoD at the end of each micro-release): all mod-5 slices CLOSED · X1/X2/X3
   satisfied (SDK parity + `mkdocs build` green + DOC-INDEX maintained) · the release's AC gate green ·
   **and the artifact is shippable** through the release machinery stood up in 0.8.6 (`/goal` may
   dry-run the publish as the final verification step).

**Cross-cutting DoD (X1/X2/X3), carried verbatim from 0.8.0/0.8.1, binds every slice in every release
here** — restated in each `plan-0.8.z.md`. Odd releases that are pure $0 eval (e.g. EXP-A/M4/B′) run the
same gate-and-close discipline but their "shippable" DoD is the **landed result doc + reproducible
script**, not a published artifact. **(Per F-11 the experiment ladder is no longer a free-standing "pure
$0 eval" odd release — it is owned work folded into 0.8.11; this DoD discipline (result doc + repro script
as the shippable) now binds those experiment slices WITHIN 0.8.11.)**

---

## 9. Sequencing rationale (one line per release)

> **⚠ §9 status (T1/2 sweep, F-20's flagged residual).** The 0.8.6→0.8.14 bullets below are unaffected
> and still accurate. From **0.8.15 on**, this section predates the F-18 park + F-19/F-20 resequence and
> the 0.9.x-era F-17 scale-bound pull-in — see the inline annotations on each affected bullet, and §4's
> table (the current allocation) for the authoritative row per release. Bullets for **0.8.20–0.8.24** are
> appended below to close the gap; they did not exist when §9 was first written.

- **0.8.6 first** — nothing in this line should layer onto un-migrated coupling or an un-unified
  provider transport, and no release can be "DoD-shippable" until the publish path exists. Foundations.
- **0.8.7 OOB GPU + recall/ceiling eval** — bank the 100–300× re-embed speedup before the re-embed-heavy
  releases (0.8.12/14) and run EXP-A/M4 + Gate-0/2 on it ($0). **(SUPERSEDED — see F-11: 0.8.7 closed on the
  GPU-embedder DoD only; EXP-A/M4 + Gate-0/2 did NOT run and are folded into 0.8.11.)**
- **0.8.8 obs+telemetry** — surface *why a result ranked* and *what actually happened* before building
  features (F9/F5) whose value you'll want to observe; **this is I-1**, the agent-signal upstream.
- **0.8.9 OOB CI + joint tuning** — stop the vacuous-green gates lying, cheaply; run EXP-B′ + Fr-acc
  (agent-signal half now possible after 0.8.8).
- **0.8.11 OOB agent-feedback** — EXP-AF + the **agent-side router prototype** (no substrate dep) — the
  EXP-S-KILL hedge and the EXP-B′-divergence parity vehicle (DP-A/DP-B).
- **0.8.12 memory plumbing** — now that #8/#9 exist (I-5), build the consolidation/recency provider and
  lift ELPS coverage (the head of the virtuous loop that feeds every graph/coverage measure).
- **0.8.14 substrate+recall** — coordinate the two schema migrations (EXP-S + F5) in one release; **this
  is the long pole (I-2) — protect EXP-S's schedule (F-1) and resist scope creep.**
- **0.8.15 dispatcher** — first odd slot after EXP-S (odd line skips 13); build EXP-Fr to the §5 contract;
  finalize the locus decision. **Cannot precede 0.8.14.** **(SUPERSEDED — see F-18: PARKED by HITL
  2026-07-07, zero OPP-12 dependency, serves no top-5 Memex need; next commission after 0.8.16 was OPP-12,
  not this. §4's 0.8.15 row is the current status.)**
- **0.8.16 ranking+reach** — F9 ranking signal (now observable via 0.8.8) + the ONNX backend (consumes
  I-3, feeds I-4). **(F-18/F-20 add-on: F9 also feeds the OPP-12 `rankable` role @0.8.20, so 0.8.16 is ON
  the Memex-value critical path — not just this bullet's original I-3/I-4 rationale.)**
- **0.8.17 router hardening / forks** — per-feature config wiring, EXP-AF productization, and the
  EXP-C/D/E forks; built on the 0.8.15 dispatcher. **(SUPERSEDED — see F-19: PARKED alongside 0.8.15;
  re-opens together with it, not on this schedule.)**
- **0.8.18 capstone** — make portable DBs/backend-swap *safe* (#5, now that #3/#4 exist — I-4) and
  complete the full publish pipeline (#11-full) for a clean GA-grade release. **(SUPERSEDED — see F-19/
  F-20: 0.8.18 was NOT the end of the 0.8.x line; OPP-12 (0.8.19/0.8.20) and the F-17 scale-bound
  (0.8.23/0.8.24) were pulled in after it. #11-full's real payoff is that it is the publish PREREQUISITE
  for 0.8.20's breaking-pair publish.)**
- **0.8.19 free-threading + bench** — the **EXP-FT free-threaded-Python ladder** ($0 eval, pyo3-0.29-gated;
  productization is a readout contingency) + the **#13 benchmark-and-robustness harness**. **(SUPERSEDED —
  see F-19/F-20: this content BUMPED to 0.8.21 (`plan-0.8.21.md`); 0.8.19 was NOT "end of the 0.8.x line"
  — it is now OPP-12 Phase-1, below.)**
- **0.8.19 OPP-12 Phase-1 ✅ COMPLETE** — existence axis (`pending/active/deleted/purged`) · `transition`/
  `purge` verbs · the C-2 typed `SearchHit.id` swap (`u64→IdSpace`, byte-unchanged; TC-8) · X1 py/ts
  parity. HITL-resequenced ahead of free-threading/bench (F-19/F-20) because OPP-12 is Memex's entire
  remaining FathomDB-side need (F-18). See F-24 for the close.
- **0.8.20 (even, publish) OPP-12 Phase-2 + erasure completeness** — `ReadView`/read-modes · node-validity
  · the projection registry (C-1 co-land) + EAV/property-FTS · `dense_readiness` · surrogate `logical_id`
  minting for registry-admitted governed entities only (F-25) · `erase_source()` erasure completeness ·
  the coordinated breaking-pair publish (manifests `0.8.9 → 0.8.20`, first real bump in the line).
  Needs 0.8.18's #11-full matrix and 0.8.16's F9 `rankable` role. See F-19/F-20/F-25/F-32.
- **0.8.21 (odd, OOB) free-threading + bench** *(was 0.8.19)* — the EXP-FT free-threaded-Python ladder
  (FT-1…5) and the #13 benchmark-and-robustness harness; bumped after OPP-12 (F-19/F-20).
- **0.8.22 (even) deferred major-dependency migrations** *(was 0.8.20)* — rusqlite 0.31→0.40 (⚠ `napi 2→3` moved to **0.8.21**, `seq-203`)
  with sqlite-vec (coupled), LBO-executed (F-12); precedes the scale-bound work since rusqlite/sqlite-vec
  touch the vector path.
- **0.8.23 (odd) F-17 scale-bound — soft/advisory** — characterize + document the supported-scale envelope
  of the exact-search local-first stack on the complete 0.8.x surface. Pulled into 0.8.x from the old
  0.9.0 slot so the line is cohesive before 0.9.0 opens (F-20, revises F-17).
  Before its architecture documentation is finalized, also decide and document
  hybrid/vector candidate-fanout versus caller-limit semantics: whether hybrid
  result-prefix stability is a product contract, a fixed internal fanout
  policy, or an explicit non-guarantee. This is distinct from the narrow
  0.8.22 direct-FTS-only P2 repair (HITL seq-248).
- **0.8.24 (even) F-17 scale-bound — stated/firm, the 0.8.x-cohesive capstone** — a firm, tested
  supported-scale limit; publishable at an even micro. **0.9.0 opens only after this** — with OPP-12 and
  the scale-bound both done, 0.8.x is cohesive and 0.9.0's post-line identity is OPEN (F-20).

> **This file is the durable decision record and the sole 0.8.x release-plan schedule.** When a release
> closes, reconcile any re-sequencing into §4/§5 here in the same docs commit that closes it
> (orchestration.md §12.4). The companion design docs (`planner-router-psd-0.8.x.md`,
> `initial-arch-planner-router-0.8.x.md`) remain the *design* inputs; `planner-router-sequencing-0.8.x.md`
> is **superseded by this file** for scheduling.
