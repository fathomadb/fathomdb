# FathomDB 0.8.25 — Slice 75 and release-ready closure (no publish)

> **Superseded for commissioning on 2026-09-08.** The owner inserted Slices
> 71–73 before Slice 75. Use the active release-state writer, the
> [scope adjustment](../0.8.25/scope-adjustment-2026-09-08-slices-71-73.md),
> and the slice-local plans. This file remains historical input and must not
> cause the reallocated work to be duplicated in Slice 75.

When the active release state reaches Slice 75, continue FathomDB 0.8.25
directly from the durable release worktree, obtain local and hosted CI evidence,
and leave the release in a truthful **release-ready / CI-complete /
publication-pending** state. Do not publish.

## Role and execution model

- Work directly. Do not invoke, spawn, adopt, or imitate the FathomDB Steward
  or Release Orchestrator roles, skills, launchers, or acknowledgement
  handshakes. The owner ruled direct execution for 0.8.25 on 2026-08-31.
- The main session is the sole writer in the durable release worktree.
- Use independent read-only subagents for design review, implementation review,
  and final verification. Do not let two agents mutate the same checkout.
- Verify claims from Git, retained receipts, commands, installed artifacts, and
  CI state rather than agent narration.
- CUDA, NVIDIA tools including `nvidia-smi`, and ptrace are standing-authorized,
  including unconfined execution when the unchanged applicable command needs
  them.
- The owner granted standing approval on 2026-09-07 for this and future
  equivalent transfers of disposable exact-source verification fixtures to
  the standing Windows VM. Hash both sides and remove exact owned paths after
  the evidence is durable.

## Repository and exact inherited state

- Canonical checkout: `/home/coreyt/projects/fathomdb`
- Durable release worktree:
  `/home/coreyt/projects/fathomdb-worktrees/release-0.8.25`
- Local branch: `release/0.8.25`
- Remote branch: `origin/release/0.8.25`
- Verified and pushed Slice 60 closure:
  `53f8c46b53441c8b537717f539779af11e82b792`
- Reviewed Slice 60 code candidate:
  `ce59e65b64945bb389c010117eab8a7a10d54b62`
- Final Slice 60 implementation-review candidate:
  `59208028da2f6d403df0c2ec1397e439782b41ea`
- Release-state writer: `dev/plans/release-state-0.8.25.json`
- Board: `dev/plans/runs/STATUS-0.8.25.md`
- Master plan: `dev/plans/plan-0.8.25.md`
- Slice 75 plan/design:
  `dev/plans/0.8.25/features/slice-75/{plan,design}.md`
- Scope authority:
  `dev/plans/0.8.25/scope-adjustment-2026-09-02.md`
- Architecture authority: `dev/design/fathomdb-data-plane-architecture-v2.md`

The handoff commit is a documentation-only descendant of the recorded Slice
60 closure. Do not assume the hard-coded SHA is still `HEAD`; verify that the
current local and remote release refs agree and that the tree is clean:

```bash
cd /home/coreyt/projects/fathomdb-worktrees/release-0.8.25
test "$(git rev-parse --show-toplevel)" = \
  /home/coreyt/projects/fathomdb-worktrees/release-0.8.25
test "$(git branch --show-current)" = release/0.8.25
git status --short --branch
git rev-parse HEAD
git rev-parse origin/release/0.8.25
git merge-base --is-ancestor 53f8c46b HEAD
```

At the 2026-09-08 handoff, the tree and refs agreed, `next_slice` was 75,
Slice 60 was `COMPLETE_ON_RELEASE_BRANCH`, the worktree `target/` was about 35
GiB, and the filesystem had about 138 GiB free. The proven disposable
`target/debug/incremental` cache had already been removed; retain the remaining
dependency/release cache unless ownership and disk pressure justify a narrower
cleanup.

`gh run list --branch release/0.8.25` returned no runs. This is expected:
`.github/workflows/ci.yml` runs on `main` pushes and pull requests, not direct
release-branch pushes. Do not mistake an absent run for green CI.

## Required orientation

Read before editing:

1. `AGENTS.md` in full.
2. The external memory `MEMORY.md`, especially:
   - `release-0825-direct-execution-no-steward-orchestrator.md`;
   - `0.8.25-slice60-constrained-graph-lessons.md`;
   - `release-dod-requires-full-workspace-gate.md`;
   - `release-publish-gotchas.md`;
   - `windows-vm-for-fathomdb-testing.md`; and
   - `orchestration-execution-traps.md` for Git-witness and one-writer rules,
     not for adopting the prohibited orchestrator role.
3. The release-state JSON, board, master plan, scope adjustment, architecture
   v2, and `dev/design/0.8.x-after-0.8.25-design-notes.md`.
4. Slice 75's complete plan and design, all retained R25/AC25-75 and A25
   contracts they cite, accepted ADRs, public interfaces, and the final status
   and verification records for Slices 10–60.
5. `dev/design/release.md`, `.github/workflows/ci.yml`,
   `.github/workflows/release.yml`, `scripts/check.sh`, and the sized selectors
   in `scripts/agent-verify.sh --help`.

Accepted ADRs and public interfaces remain contracts. `dev/progress/` is
historical and read-only. Never hand-edit release-state generated blocks.

## Current remaining ladder

This historical handoff must not be commissioned until the active release
state reaches Slice 75. The current ladder is:

1. **Slice 71 — finish AC-072 while preserving completed 71B.**
2. **Slice 72 — generic preflight and installed CE CPU/CUDA profile.**
3. **Slice 73 — focused Windows Node/N-API coverage.**
4. **Slice 75 — integrated release verification and CI closure.**
5. **Nonpublishing release-ready close.** Record all slices complete, exact
   local/hosted CI evidence, publication pending, and the next owner decision.

Do not reopen a closed feature slice merely because Slice 75 audits it. A
genuine defect returns to its owning contract through a focused committed
RED/GREEN correction and independent review.

## Hard boundary: release-ready, not published

For this handoff, “release work, not publish work” means completing Slice 75,
local source-independent verification, the full local CI-equivalent gate,
release-branch push, pull-request CI, evidence/status closure, and a truthful
release-ready state. It does **not** authorize package production or any
publish-capable action.

Allowed:

- source, test, documentation, workflow, and verification-harness changes
  required by the reviewed Slice 75 design;
- local disposable wheels, N-API modules, archives, and isolated consumers used
  only as verification fixtures;
- pushes of the explicit `release/0.8.25` branch ref;
- opening or updating a pull request from `release/0.8.25` to `main` solely to
  run ordinary PR CI, then inspecting every relevant job to terminal state;
  and
- recording `release-ready`, `CI complete`, `publication pending`, or the
  closest truthful existing repository vocabulary.

Forbidden without a later explicit owner instruction:

- `scripts/set-version.sh`, release-version or changelog cut commits;
- `scripts/release/local-dry-run.sh` or any `release.yml` dispatch, including a
  nominal dry run, because those are release-packaging workflows;
- creating, signing, moving, or pushing any `v*` tag;
- registry staging, upload, publication, promotion, or package reservation;
- PyPI/npm/crates.io installation claimed as post-publication smoke;
- creating or publishing a GitHub Release;
- merging the pull request or otherwise updating `main`; and
- marking publication, post-publication verification, or the public release
  complete.

A `v*` tag push fires the real multi-registry publish. Never use a tag as a CI
trigger or release-ready marker.

## Slice 75 design reconciliation and review

The inherited plan is `DRAFT` and the design header is stale:
`DRAFT_SCOPE_RECONCILED_BLOCKED_ON_SLICE_7`. Slice 7 and every Slice 10–60
predecessor are now complete. First reconcile the plan/design from current Git:

- remove the stale Slice 7 blocker;
- bind every workload to the owner-approved trimmed 0.8.25 scope;
- separate CI/release-ready evidence from later packaging/publication evidence;
- name exact manifest/receipt paths, schemas, required raw evidence, agreement
  rules, timeouts, reset/isolation policy, and truthful N/A routes;
- define which tests and commands prove each requirement without relying on a
  future registry artifact;
- preserve measurement-layer truth and make no answer-system or semantic
  quality claim;
- consume exact refs and receipts from Slices 71–73 without rerunning their
  historical baselines or focused investigations; and
- keep the matrix representative rather than restoring the rejected exhaustive
  scale-by-feature-by-CUDA envelope.

Obtain an independent read-only design review before implementation. Allow at
most four documented design-review/FIX-n cycles. Slice 60's exceptional cycle
authorizations do not carry forward. A remaining implementation-shaping P1/P2
means the design is not READY and requires owner direction.

## Required Slice 75 work

### 1. Consume prior-slice receipts

Require closed, exact-commit receipts from Slices 71–73. Validate their source
identity, manifest/receipt agreement, required platform or performance outcome,
and applicability to the selected integrated candidate. Missing, stale, or
contradictory evidence stops Slice 75; it does not authorize repeating a prior
slice's historical investigation.

### 2. Consume Slice 71 performance closure

Consume AC-072 and 71B receipts from the exact closed Slice 71 candidate.
Slice 75 may run only candidate-side performance checks naturally present in
its integrated final matrix. It must not rerun the AC-072 branch comparison,
the write baselines, attribution arms, or historical investigations.

### 3. Consume Slice 72 and 73 closure

Consume the generic preflight and installed CE CPU/CUDA receipts from Slice 72
and focused Windows Node/N-API evidence from Slice 73. Do not redesign or
repeat those profiles and platform investigations. Apply their implemented
gates normally when the integrated exact candidate reaches them.

### 4. Integrated representative verification

Implement the reviewed strict manifest/receipt harness with committed RED/GREEN
tests. Run the selected installed cross-SDK/wire, concurrency, lifecycle,
evidence/page/dependency/graph, projection/rebuild, overhead/resource, and
packaged-native retrieval witness cells. Use real SQLite databases and exact
source-independent local artifacts. Missing evidence is never zero or pass.

Linux and Windows x64 CPU/native Rust/Python/Node evidence is mandatory.
Windows CUDA, live-model, registry-installed, post-publication, and exhaustive
scale-by-feature-by-CUDA routes are N/A. Preserve the Windows transfer and
cleanup rules from memory.

## TDD, review, and retry discipline

- Commit or stage the failing test before implementation. Record the command,
  exit, diagnostic, and frozen test/fixture hashes.
- Test files are frozen during GREEN except through a separately justified and
  independently reviewed oracle correction.
- No database mocks and no agent-generated oracle regeneration.
- Run the smallest relevant selector first. If the same failure repeats twice,
  stop, reread the test/design/memory, and change the hypothesis before another
  attempt.
- After GREEN/refactor, use an independent read-only implementation reviewer on
  the exact commit. Allow at most seven documented implementation-review/FIX-n
  cycles. Additional Slice 55/60 authorizations do not carry to Slice 75.
- Use a separate fresh read-only verifier for final acceptance. A discovered
  defect returns to committed RED/GREEN and independent re-review.
- Preserve already-green unaffected-path evidence across docs-only or
  test-only corrections. Do not restart a complete matrix merely because a
  later commit cannot affect it.

## Verification order and anti-loop rule

Use latency order and stop on the first actionable failure:

1. exact changed-file/fixture tests;
2. affected lint/typecheck and feature checks;
3. Slice 75 focused workloads and installed consumers;
4. `./scripts/agent-verify.sh --tier=fast`;
5. selected heavy/all/applicable-feature and platform routes;
6. full workspace strict Clippy/check;
7. one final `./scripts/check.sh` CI-equivalent run, including the exact
   `AGENT_LONG=1` selectors sealed by the READY design; and
8. targeted metadata gates:
   `./scripts/check-release-state-views.sh`, changed-file Markdown lint with
   `markdownlint-cli2 --no-globs`, and `git diff --check`.

Do not enter an evergreen verification cycle. Once a full route is green at an
exact production candidate, a later narrow finding reruns only the affected
selector, its contract dependencies, and any final exact-candidate identity
check required by the READY design. A docs-only close does not invalidate
compiled/runtime evidence. Normal Git hooks remain enabled and are recorded
separately from deliberate verification commands.

AC-036 requires ptrace. If the sandbox denies `PTRACE_TRACEME`, rerun the
unchanged strict command unconfined. Use RTX 3090 CUDA as required by the CE
profile; do not silently substitute CPU or the display GPU.

## Hosted CI without publication

After the final local candidate and records are committed:

1. Push the explicit ref:

   ```bash
   git push origin \
     refs/heads/release/0.8.25:refs/heads/release/0.8.25
   ```

2. Open or update a pull request from `release/0.8.25` to `main` solely to run
   ordinary PR CI. Do not merge it.
3. Bind the evidence to the exact PR head SHA. Inspect every relevant CI job to
   a terminal state. Running, cancelled, unexpectedly skipped, or superseded is
   not green.
4. Correct an actual failure through focused TDD and review, push the exact
   branch again, and rerun only affected local evidence plus the ordinary PR CI
   required for the new head.
5. Do not dispatch `release.yml`; do not create a tag; do not use a registry.

## Durable closure

When every selected Slice 75 workload and ordinary PR CI job passes:

- write the Slice 75 plan/design review, TDD chronology, implementation-review,
  verification, evidence manifest/receipt, raw-run index, and `status.md`;
- edit only `dev/plans/release-state-0.8.25.json` for release facts, then run
  `./scripts/check-release-state-views.sh --write`;
- mark Slice 75 `COMPLETE_ON_RELEASE_BRANCH`, set `remaining_ladder` empty, and
  use the existing truthful representation for no next slice;
- record the exact CI head/run/jobs, all selected local/platform evidence,
  N/A routes, and cleanup;
- describe the release as `release-ready; CI complete; publication pending`, or
  the closest existing vocabulary that does not claim main reachability,
  package production, a tag, registry state, post-publication smoke, or public
  release completion;
- keep `landed` empty unless the owner separately authorizes and completes main
  integration;
- commit and push the explicit release branch ref, then recheck local/remote
  equality;
- capture reusable Slice 75/release-close lessons in the external memory store
  and update `MEMORY.md`; and
- remove only proven verifier-owned temporary artifacts and reclaim only
  explicitly owned rebuildable caches.

Then stop. Present the owner with the exact release-ready commit, CI run/PR,
remaining publication/main-integration decisions, and a concise statement that
no package, registry, tag, publication, or main merge occurred.

## Stop conditions

Stop and request owner direction for:

- an unresolved P1/P2 after the authorized design or implementation cycles;
- missing, stale, contradictory, or non-passing required receipts from Slices
  71–73;
- an integrated candidate-side performance failure that contradicts a closed
  prior-slice receipt;
- a new public-contract or accepted-ADR decision;
- unavailable mandatory Linux/Windows/CUDA evidence after safe environment
  recovery is exhausted;
- merge to `main`, version/changelog cut, release packaging/rehearsal, tag,
  registry mutation, upload, publication, promotion, or post-publication smoke.

## Immediate task

Do not begin Slice 75 until the release-state writer names it as `next_slice`.
Once Slices 71–73 are closed:

1. Reconfirm the exact clean synchronized release worktree and Slices 71–73
   closure.
2. Read and reconcile the Slice 75 plan/design, sealing the trimmed integrated
   manifest and receipt contract around the prior-slice receipts.
3. Obtain independent design review, with at most four documented cycles.
4. Implement the preflight and verification harness through committed TDD
   RED/GREEN, then obtain independent implementation review with at most seven
   documented cycles.
5. Use a separate fresh verifier for the exact integrated local, platform,
   performance, installed-consumer, and CI routes selected by the READY design.
6. Close Slice 75 and the release-ready state durably, push the branch, compact
   memory, clean owned artifacts, and stop before every packaging/publication or
   main-integration action.
