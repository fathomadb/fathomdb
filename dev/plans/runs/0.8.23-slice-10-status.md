---
title: 0.8.23 Slice 10 status
status: PENDING_EXTERNAL
target_release: 0.8.23
---

# Slice 10 — CUDA contract external release gate

## Landed static contract work

`8a9e05b5` landed the independently reviewed safe static implementation:

- Driverless Python and npm preflight smokes remove device-selection variables
  rather than explicitly forcing CPU.
- A versioned, SHA-bound CUDA preflight-witness schema and fail-closed verifier
  reject missing, malformed, incomplete, mismatched, and symlinked evidence.
- The self-hosted CUDA preflight requires a separate GitHub-hosted,
  default-branch-owned trusted-route verifier. That verifier has read-only
  permissions, a pinned checkout without persisted credentials, exact workflow
  identity and candidate-SHA/main-ancestry checks, and an exact whole-job
  allowlist that rejects extra candidate-controlled steps or actions.

`8814b374` additionally lands the independently reviewed local exact-SHA
control-plane preparation:

- A strict candidate manifest, protection-baseline manifest, hosted verifier,
  same-run receipt verifier, and static mutation suite are present. The checked
  in candidate manifest is empty and the baseline is expired, so this branch
  authorizes no candidate or runner dispatch.
- Candidate-consuming dry-run jobs are read-only and credentialless; finalizer
  and publisher jobs reject a nonempty candidate SHA. The only
  `github.workflow_sha` checkouts are the two main-owned control-plane uses.
- Provenance separately binds a reviewed PR head and its merged `main` commit;
  timestamp ties fail closed. The focused provenance/CUDA/release-contract
  suites, `actionlint`, and `scripts/agent-verify.sh` passed locally.

The implementation followed committed RED→GREEN tests and passed independent
design/code review and `scripts/agent-verify.sh`. No runner dispatch, runner
configuration change, release-package claim, registry operation, tag, or
publication occurred.

## Pending external release closure

Slice 10's local contract work is complete, but its release/publication closure
is pending external evidence:

1. A separately reviewed `main` control-plane PR, followed by a provenance-only
   `main` PR for one exact candidate SHA. The candidate manifest/baseline now
   on `release/0.8.23` are local preparation, not a `main` authorization.
2. An externally reviewed protection-baseline/environment attestation naming
   the runner-group ID, selected repository/workflow restriction, unbypassable
   `main` protection, and the independent no-self-approval environment.
3. One retained, schema-valid, non-publishing trusted-runner witness for a
   protected-default-branch candidate, or for the single exact SHA admitted by
   the separately reviewed provenance route. It must bind candidate SHA,
   build/link facts, driverless Python/npm CPU-default smokes, and selected-GPU
   PID/device observations.

The current `release/0.8.23` commit is not protected-default-branch reachable.
The authorized exact-SHA route is under local design/test preparation, but its
protection-baseline and environment evidence cannot be inferred locally. These
are not replaced by local static test passes.

## Local-sequencing decision

HITL `seq:249` rules that this external condition blocks release completion and
publication, but does **not** block local Slice 20 or Slice 70 work. Slice 20's
own real rehearsal remains independently gated by a valid Slice 10 receipt and
witness; no evidence requirement is waived.

## External next action

Move the reviewed control-plane code to its separate `main` PR/review path.
Then obtain the external protection-baseline/environment attestation, create
the separately reviewed provenance record, dispatch exactly one non-publishing
trusted preflight, independently review the retained witness, and only then
consider Slice 10 closure and Slice 20 start.
