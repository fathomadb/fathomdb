---
title: 0.8.23 Slice 20 status
status: PENDING_EXTERNAL
target_release: 0.8.23
---

# Slice 20 — CUDA package rehearsal and installed-artifact smokes

## Landed local control plane

`91a5ddd8` lands the reviewed local CUDA package-rehearsal control plane.

- The candidate route accepts exactly one Linux wheel, thin npm main tarball,
  and Linux x64 platform tarball; the versioned bundle schema/verifier rejects
  missing, extra, substituted, noncanonical, or symlinked inputs.
- The trusted `control-plane` checkout owns sealing, verification, and smoke
  scripts. Candidate checkout content is package input only.
- The retained bundle binds the exact candidate SHA, preflight witness digest,
  full validated Slice 10 witness, package-byte digests, and CPU/GPU smoke
  evidence. CPU smokes are driverless and network-isolated; GPU smokes use the
  runtime PID and selected UUID correlation.
- Candidate Linux x64 byte construction is limited to the nonpublishing
  preflight/rehearsal route. Normal CPU Linux x64 producers are rejected.
  Canonical tag/non-dry runs instead encounter a credentialless explicit
  failure before aggregate or publisher eligibility until a separately
  commissioned, tag-bound CUDA delivery route exists.

The work used committed RED-to-GREEN tests and independent design/code/verify
reviews. Canonical `scripts/agent-verify.sh`, workspace clippy/check,
Actionlint, and the CUDA package, release-contract-truth, CUDA-contract, and
workflow-scope mutation suites passed. No GitHub request, registry action,
tag, publication, or real package rehearsal occurred.

## Pending external release closure

Slice 20 cannot close its release/publication evidence locally. It requires:

1. A candidate-bound, schema-valid Slice 10 route receipt and preflight
   witness.
2. One retained, valid, nonpublishing package-rehearsal bundle for that exact
   candidate, including the three package bytes and installed CPU/GPU smokes.
3. Independent review of the retained evidence.

Those conditions block release completion and publication. They do not waive
any gate and do not block local Slice 70 work. Canonical tag CUDA delivery and
publication remain separately commissioned and unauthorized.

## Local next action

Begin Slice 70 using its reviewed TC-5 vector-stage design. Do not run a real
Slice 20 rehearsal unless the required Slice 10 receipt and witness exist.
