---
release: 0.8.23
slice: 3
status: LANDED
scope: planning-only
baseline: 31c315be
---

# 0.8.23 Slice 3 — draft needs, requirements, and AC allocation

## Outcome

This is a draft allocation, not a CRUD edit to `dev/needs.md`,
`dev/requirements.md`, `dev/acceptance.md`, an interface contract, or an ADR.
Those existing documents remain authoritative until an owning feature is
expressly commissioned and its contract change is approved. IDs below are
temporary planning labels to avoid minting
apparently approved `REQ-*` or `AC-*` identifiers prematurely.

## Draft allocation

| Draft need | Draft requirement / acceptance evidence | Allocated feature slice | Source and boundary |
| --- | --- | --- | --- |
| N23-CUDA-SAFE — an operator can distribute Linux CUDA-capable bindings without making CPU-default installs require a GPU. | R23-CUDA-CONTRACT — trusted environment/artifact policy is explicit; AC23-CUDA-CONTRACT — protected dry-run preflight records the required toolchain, build/link, driverless-CPU, and GPU-PID witnesses. | 10 | `0.8.23-gpu-artifacts.md`; Linux x86_64 only, CPU default, no publish. |
| N23-CUDA-REHEARSAL — a release candidate is validated as the installed Python and npm artifact rather than as a source build. | R23-CUDA-PACKAGE — release-equivalent artifacts retain CPU usability and prove explicit CUDA engagement; AC23-CUDA-PACKAGE — installed-artifact CPU/GPU smokes and rehearsal gate have retained evidence. | 20 | `0.8.23-gpu-artifacts.md`; depends on Slice 10 and does not authorize publication. |
| N23-EMBEDDER-FEEDBACK — SDK users can distinguish missing embedding configuration from a real worker/runtime failure. | R23-EMBEDDER-OUTCOME — Rust, Python, and TypeScript expose a typed non-retryable outcome, operation, and remediation; AC23-EMBEDDER-PARITY — real-engine cross-SDK tests preserve that shape and do not consume retry backoff. | 30 | `0.8.23-embedding-configuration-feedback.md`; no automatic model download, CUDA selection, or failure masking. |
| N23-MEMEX-CONTRACT — graph integration has a documented, tested relation and graph-on/vector-off query policy. | R23-MEMEX-CHARACTERIZATION — characterize supported input/embedding states and one explicit query policy; AC23-MEMEX-CHARACTERIZATION — public-SDK tests prove the selected policy, not a promise inferred from one fixture. | 30 | `0.8.23-memex-integration.md`; any configured-embedder reliability defect becomes a separately proposed slice. |
| N23-SCALE-READOUT — maintainers can obtain a reproducible, bounded characterization of the shipped vector path. | R23-SCALE-PROTOCOL — preserve fixture, candidate, dependency, host, isolation, and raw-log provenance; AC23-SCALE-PROTOCOL — result status follows the design's `DEFERRED` through `CHARACTERIZED` rules and never implies a supported-scale promise. | 40 | `0.8.23-scale-characterization-protocol.md`; no benchmark runs or performance thresholds are drafted here. |
| N23-SECRET-GUARD — a contributor and CI can detect a staged or historical credential without exposing it in logs. | R23-GITLEAKS — a pinned scanner checks staged index and reachable history, fails closed, and redacts output; AC23-GITLEAKS — synthetic-secret regression tests prove hook/CI wiring and unavailable-scanner behavior. | 50 | `0.8.23-gitleaks-guards.md`; no history rewrite, credential rotation, or collector. |
| N23-WINDOWS-WAL — FathomDB can confirm or refute the Windows WAL checkpoint hypothesis with first-party evidence. | R23-WINDOWS-WAL-DIAGNOSIS — reproduce cross-process locking, collect attempts/timing, and preserve fail-closed erasure; AC23-WINDOWS-WAL-DIAGNOSIS — a Windows-native repro distinguishes `ErasureIncomplete` from false success and carries the required diagnostic evidence. | 60 | `0.8.23-windows-wal-checkpoint-reader-conflict.md`; requires first-party Windows evidence path and may not tune retry constants before measured data. |
| N23-WAL-ATTRIBUTION — FathomDB can distinguish an owned reader, Python-SDK concurrency, serial materialized result, external holder, and locally visible checkpoint collision in Memex-like Windows WAL failures. | R23-WAL-ATTRIBUTION — private opt-in diagnostics record only owned connection role/state and safe checkpoint facts; AC23-WAL-ATTRIBUTION — real-SQLite Windows controls classify every outcome while retaining typed fail-closed erasure. | 65 | `0.8.23-wal-attribution-investigation.md`; explicitly commissioned after Slice 60. No retry, binding, reader-pool, public-surface, or OS-cause change before discriminating evidence. |

## Proposed CRUD after HITL approval

| Authoritative record | Proposed operation | Scope rule |
| --- | --- | --- |
| `dev/needs.md` | Create the seven approved N23 need statements, or reject/defer them individually. | Do not rewrite existing personas or historical needs. |
| `dev/requirements.md` | Create only the corresponding approved R23 requirements. | A rejected/deferred need creates no requirement. |
| `dev/acceptance.md` | Create only approved AC23 criteria, one observable assertion per criterion, with real test/fixture ownership. | Do not mint numbers, thresholds, or tests before the owning feature-slice design review. |
| `dev/interfaces/` and ADRs | Update only if the approved Slice 30 public typed outcome or another approved feature changes the contract/decision. | No interface or ADR edit is authorized by this draft alone. |

## Completeness and deferments

Every candidate in the 0.8.23 ladder (10, 20, 30, 40, 50, 60, and 65) has an
explicit draft allocation. Slices 0–6 themselves add no product need or AC:
they are planning and decision work. Dependency hygiene and document cleanup
remain disposition proposals, not hidden feature requirements.

This allocation landed in `91e162c2`.

## Slice 70 extension — 2026-08-17

The HITL subsequently commissioned Slice 70. This dated extension is a
temporary planning allocation only; it does not revise the historical Slice 3
landing or mint authoritative needs, requirements, or acceptance identifiers.

| Draft need | Draft requirement / acceptance evidence | Allocated feature slice | Source and boundary |
| --- | --- | --- | --- |
| N23-TC5-VECTOR-STAGE — experiment tooling can measure the exact pre-fusion vector stage without hybrid-search substitution. | R23-TC5-VECTOR-STAGE — a feature-gated benchmark executable accepts only qualified manifest pins plus allowlisted K/top-k/execution controls, executes one-snapshot bit-KNN → exact-f32 rerank → exact ground truth, and attests device/timing/provenance; AC23-TC5-VECTOR-STAGE — RED→GREEN tests reject public-search/FTS/fusion/graph/CE paths, ambient device/config controls, output races, incomplete results, and device/provenance drift. | 70 | `0.8.23-slice-70-tc5-vector-stage-hypothesis.md`; benchmark-only Rust surface, no SDK/CLI expansion, no external TC-5 GPU run or publication. |
| N23-DUAL-RUNTIME — supported artifacts use CPU or compatible CUDA without an end-user rebuild and make GPU policy/availability visible. | R23-DUAL-RUNTIME — one typed cross-surface `auto`/`cpu`/`cuda:N` policy, provider report, and read-only doctor diagnostic govern supported CUDA-capable artifacts; AC23-DUAL-RUNTIME — RED→GREEN tests reject silent forced-CUDA fallback, artifact feature drift, and diagnostic side effects. | 70 | `0.8.23-slice-70-dual-runtime-device-policy.md`; product surface precedes and does not weaken private TC-5 isolation. |
| N23-RERANK-DUAL-RUNTIME — supported artifacts can run cross-encoder reranking on CPU or a compatible GPU without an end-user rebuild and make the selected execution path visible. | R23-RERANK-DUAL-RUNTIME — after an audit of the actual reranker/provider/artifact seams, one strict cross-surface `auto`/`cpu`/`cuda:N` reranker policy, provider report, and diagnostic/configuration path must govern the supported artifact; AC23-RERANK-DUAL-RUNTIME — RED→GREEN tests reject a forced GPU reranker silently using CPU, artifact feature drift, ambiguous SDK reports, and diagnostic side effects. | 71 | `0.8.23-slice-71-draft-plan.md`; draft only. CPU database retrieval is out of scope: a GPU-bound reranker means cross-encoder inference uses the selected GPU. |

## Slice 65 extension — 2026-08-16

The HITL expressly commissioned this post-Slice-60 reliability investigation.
The added row is a temporary planning allocation only; it does not revise the
historical Slice 3 landing or mint an authoritative need, requirement, or
acceptance identifier. Its owning design and Slice status record the required
review, evidence boundary, and future RED-to-GREEN work.
