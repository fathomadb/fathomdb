---
release: 0.8.23
slice: 30
status: COMPLETE
baseline: f88dc65f7f6cffec4eefa6f3a010db7743d404b4
---

# 0.8.23 Slice 30 — Memex embedding readiness and graph integration

## Review and decisions

Reviewed `plan-0.8.23.md`, release state, Slice 0–6 records/workplan/HITL
package, Slice 50, and the baseline commit range. The prepared real-engine
fixture is available; no release artifact or hardware evidence is claimed.
Reviewed the two proposed designs, the N23/R23/AC23 Slice-30 allocations,
authoritative interface documents, and the accepted ADR index.

- Approved N23-EMBEDDER-FEEDBACK / R23-EMBEDDER-OUTCOME and
  N23-MEMEX-CONTRACT / R23-MEMEX-CHARACTERIZATION as this slice's bounded
  contract. AC23 labels remain planning labels; `acceptance.md` is locked for
  per-feature IDs.
- No accepted ADR is contradicted, so no successor ADR is required.
- `EmbeddingReadiness` is Rust-owned and pure: `ready`, `processing`,
  `deferred`, or `blocked`, usable-runtime fact, pending count, affected kinds,
  and an optional stable blocked payload.
- `drain` returns `EngineError::EmbedderRequired` immediately for pending work
  in a no-embedder session. Binding code is `FDB_EMBEDDER_REQUIRED`; the
  payload identifies operation, state, remediations, and documentation URL.
- Body-bearing graph edges project as `edge_fact` and require an embedder.
  Body-less structural edges do not. `graph.neighbors` supports known-root
  traversal without an embedder; query-seeded graph expansion remains tied to
  ordinary vector-seeded search. There is no graph-on/vector-off query mode.
- Worker failures and vector-equivalence refusal remain operational outcomes;
  this slice does not reclassify or hide them. No network, LLM, CUDA, or
  ranking-policy behavior was added.

The design review and disposition are in
`dev/design/0.8.23-memex-integration.md`.

## TDD evidence

RED first: `cargo test -p fathomdb-engine --test slice30_embedding_readiness`
exited 101 before implementation, with unresolved
`EmbeddingOperation`/`EmbeddingReadinessState`, missing
`read_embedding_readiness`, and missing `EngineError::EmbedderRequired`.

GREEN: the same real-SQLite test exited 0. It writes a body-bearing edge into a
no-embedder engine, proves the typed blocked report, and proves a 30-second
drain request returns the typed outcome in under one second.

## Verification

- `git diff --check` — exit 0.
- `cargo check -p fathomdb-py -p fathomdb-napi -p fathomdb` — exit 0.
- `cargo test -p fathomdb-engine --test slice30_embedding_readiness` — exit 0.
- TypeScript native build could not run: `napi` is absent in this worktree;
  borrowing the main checkout's `tsc` also fails because its `@types/node`
  installation is incomplete. No TypeScript GREEN claim is made.

## Fix-N 1 — CLI taxonomy completeness

The independent workspace verifier found the new `EngineError::EmbedderRequired`
variant missing from the CLI's exhaustive stable-code mapper. A focused unit
test was added first; its initial command exited 101 on that non-exhaustive
match. The CLI now maps it to `EmbedderRequiredError`.

- `cargo test -p fathomdb-cli embedder_required_uses_the_stable_cli_error_code` — exit 0.
- `cargo check --workspace --all-targets` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings -A missing-docs` — exit 0.

## Fix-N 2 — adversarial contract review

The review found that `EmbeddingReadiness` consulted the scheduler-owned
embedder handle. Equivalence refusal intentionally clears that handle even
though an embedder remains attached to `Engine`, so a refused runtime was
incorrectly exposed as `Blocked` and `FDB_EMBEDDER_REQUIRED`. The fixed gate
uses `Engine::runtime_embedder.is_none()`: only actual configuration absence is
the typed caller-remediable outcome. Equivalence refusal leaves outstanding
work operationally `Deferred`; an exhausted worker records a durable `failed`
terminal, after which `drain` may return once idle. Neither is configuration
feedback.

- RED: `cargo test -p fathomdb-engine --test slice30_embedding_readiness
  equivalence_refused_embedder_defers_work_without_relabelling_it_as_missing_configuration
  --no-fail-fast` — exit 101 before the gate fix; the real SQLite fixture
  observed `Blocked` where the contract requires `Deferred`.
- GREEN: the same focused command — exit 0 after the gate fix.
- `cargo test -p fathomdb-engine --test slice30_embedding_readiness --no-fail-fast`
  — exit 0 (two real-SQLite tests: absent configuration and refused runtime).
- `cargo test -p fathomdb-cli embedder_required_uses_the_stable_cli_error_code
  --no-fail-fast` — exit 0.
- `scripts/check-governed-surface-pin.sh` — exit 0. The pin now records the
  explicit Slice-30 HITL commission for the additive `read.embedding_readiness`
  / `read.embeddingReadiness` contract (35 allowlist members; core and
  recovery denylist unchanged).
- `cargo check --workspace --all-targets` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings -A missing-docs` — exit
  0.
- `python3 -m py_compile src/python/tests/test_slice30_embedding_readiness.py
  src/python/fathomdb/read.py src/python/fathomdb/types.py` — exit 0. The
  following real-engine `pytest` command exited 2 at collection because this
  worktree has no built `_fathomdb` PyO3 extension; no Python runtime pass is
  claimed.
- `npm run build` in `src/ts` — exit 127 because `napi` is not installed in this
  worktree; no TypeScript runtime or static pass is claimed.
- `scripts/agent-lint-md.sh` — exit 1 because the worktree has no local
  `markdownlint-cli2`; no markdown-lint pass is claimed.

The public Python and TypeScript real-engine tests now assert the readiness
fields, immediate typed drain error and payload, and that the edge body is not
exposed in either report or error. `dev/interfaces/{rust,python,typescript}.md`
and `dev/design/errors.md` now define the Rust/Python/TypeScript mapping,
including the distinct `SchedulerError` operational path. The stale Slice-10
reference and two-remediation claim in the configuration-feedback input design
were corrected.

## Fix-N 3 — documentation-only verification repair

The governed-surface pin's active fields already named blob
`ade9a9daf5869462d7d197058432639860e7832e` and SHA-256
`6aedeca5006810d53a47e430400a086d58a877b1cbae316511f584083b3e01b2`, but
the `_comment` reproduction instructions still named their obsolete values.
Only those instructions were corrected. The Python interface also now says the
high-level `read.embedding_readiness` wrapper returns
`affected_kinds: tuple[str, ...]`; this is intentionally distinct from the
native transport's list-like field and matches the frozen public dataclass and
real-engine Python test.

- `scripts/check-governed-surface-pin.sh` — exit 0.
- `git diff --check` — exit 0.

## Fix-N 4 — worker-terminal documentation consistency

The public Python/TypeScript readiness sections had overstated that every
worker failure remains `deferred`, and the error matrix had overstated that
`SchedulerError` is the ordinary result for all operational failures. The
contract now distinguishes equivalence refusal (outstanding work is deferred
and a bounded drain can time out) from an embedder worker that exhausts retries
(a durable `failed` terminal is recorded and `drain` may return once idle).
Neither operational result is `FDB_EMBEDDER_REQUIRED`.

- `git diff --check` — exit 0.
- Markdown structural lint remains unavailable in this worktree because
  `markdownlint-cli2` is not installed; no lint pass is claimed.

## Fix-N 5 — TypeScript strict-property initialization

The canonical TypeScript build found that `EmbedderRequiredError` declared four
readonly payload fields without definite constructor initialization.
`Object.assign` does not satisfy `strictPropertyInitialization`, so the
constructor now assigns `operation`, `state`, `remediations`, and
`documentationUrl` explicitly from its typed payload. A focused TypeScript unit
test constructs the error and asserts every typed field and its `EmbedderError`
base class.

- RED evidence is the canonical build's `TS2564` diagnostics for the four
  uninitialized fields before this repair.
- `/home/coreyt/projects/fathomdb/typescript/node_modules/.bin/tsc --noEmit -p
  tsconfig.json --typeRoots
  /home/coreyt/projects/fathomdb/typescript/node_modules/@types` — exit 0. The
  explicit type-root is required only because this isolated worktree has no
  local TypeScript dependency tree.
- `npm run build` in `src/ts` — exit 127 before TypeScript compilation because
  `napi` is absent; no native build or runtime-test pass is claimed.
- `git diff --check` — exit 0.

## Fix-N 6 — C1 Rust URL comment lexing

The C1 static reader removed `//` with a regex before recognizing Rust string
literals. The Slice-30 documentation URL therefore lost its closing quote in
the comment-stripped view, and the literal blanker treated the remainder of
`fathomdb-engine/src/lib.rs` as an unterminated string. C1 could no longer parse
the subsequent `ProjectionSpec`, `ProjectionDelta`, and `ProjectionVector`
subjects.

- RED: `scripts/tests/test_check_c1_conformance.sh` — exit 1 before the lexer
  repair. The real-engine checker reported the affected Projection structures
  unparseable; the new fixture puts an `https://` literal immediately before
  `ProjectionSpec` and requires the C1 structural check to still see it.
- GREEN: `scripts/check-c1-conformance.sh &&
  scripts/tests/test_check_c1_conformance.sh` — exit 0. `blank_comments` now
  preserves normal, byte, raw, and byte-raw string contents while blanking
  comments length- and line-preservingly (including nested Rust block comments).
- `bash -n scripts/check-c1-conformance.sh
  scripts/tests/test_check_c1_conformance.sh` — exit 0.
- `git diff --check` — exit 0.

## Fix-N 7 — Python readiness type narrowing

The native PyO3 readiness transport intentionally exposes raw `str` values, but
the public frozen dataclass promises closed literal unions. Pyright correctly
rejected passing those raw `state`, `code`, and `operation` values through
unchanged. The wrapper now validates each native spelling before returning the
public shape, rejects inconsistent blocked payload presence, and does not use a
cast to conceal an unexpected native value. The native exception stub now
declares the typed `EmbedderRequiredError` payload attributes; the public test
assigns each to its exact static type as a regression seam.

- RED: focused pyright reported the three raw-to-literal errors in
  `fathomdb/read.py`; the untyped exception payload was also not visible to
  static callers.
- GREEN: `/home/coreyt/projects/fathomdb/.venv/bin/pyright --venvpath
  /home/coreyt/projects/fathomdb --project src/python/pyproject.toml
  src/python/fathomdb/read.py src/python/fathomdb/_fathomdb.pyi
  src/python/tests/test_slice30_embedding_readiness.py` — exit 0 (0 errors,
  0 warnings).
- `/home/coreyt/projects/fathomdb/.venv/bin/ruff check` on the changed Python
  files — exit 0.
- `PYTHONPATH=src/python python3 -m pytest
  src/python/tests/test_slice30_embedding_readiness.py -q` — exit 2 at
  collection because this worktree has no built `_fathomdb` extension; no
  runtime test pass is claimed.
- `git diff --check` — exit 0.

## Fix-N 8 — Python readiness payload completeness

The public Python decoder had validated only the presence of the blocked
`code` and `operation`. A malformed native transport could therefore expose a
blocked result with no remediations or no documentation URL. Its non-blocked
branch also used URL truthiness, accepting an empty-string blocked payload.
The decoder now requires a non-empty remediation list and documentation URL for
`blocked`, while every non-`None` documentation URL is rejected outside that
state. This is validation of the native boundary, not a change to the public
readiness shape.

- RED: a focused dynamic execution of the added public Slice 30 regression
  raised `AssertionError: malformed readiness was accepted: {'remediations':
  []}` against the prior decoder (exit 1). It loads the worktree wrapper with
  the canonical native extension because this isolated worktree has no built
  extension.
- GREEN: the same public regression executed its four malformed payload cases
  through `read.embedding_readiness` with the worktree wrapper — exit 0. It
  covers empty remediations, `None`/empty blocked documentation URLs, and an
  empty non-blocked documentation URL.
- `/home/coreyt/projects/fathomdb/.venv/bin/pyright --venvpath
  /home/coreyt/projects/fathomdb --project src/python/pyproject.toml
  src/python/fathomdb/read.py src/python/fathomdb/_fathomdb.pyi
  src/python/tests/test_slice30_embedding_readiness.py` — exit 0 (0 errors,
  0 warnings).
- `/home/coreyt/projects/fathomdb/.venv/bin/ruff check` on the changed Python
  files — exit 0.
- `PYTHONPATH=src/python /home/coreyt/projects/fathomdb/.venv/bin/python -m
  pytest src/python/tests/test_slice30_embedding_readiness.py -q` — exit 2 at
  collection because the isolated worktree still has no built `_fathomdb`
  extension; the dynamic focused execution above is the available worktree
  runtime evidence.
- `git diff --check` — exit 0.

## Closure

The independent adversarial re-review passed after Fix-N 5. The canonical
environment then rebuilt the Python and N-API bindings: the real-engine Python
test, TypeScript typecheck, TypeScript readiness/privacy test, and typed-error
test all passed. Slice 30 is complete on `origin/release/0.8.23` at `776d2c20`.
Its release-state completion reference verifies that branch and explicitly
records that integration into `origin/main` remains pending; this record does
not claim an `origin/main` landing or publication. The next slice is 60.

## Canonical SDK verification

- `.venv/bin/python -m pytest src/python/tests/test_slice30_embedding_readiness.py -q`
  — exit 0 (one real-engine test passed).
- `npm run typecheck` — exit 0 in `src/ts`.
- `node --test dist/tests/slice30-embedding-readiness.test.js` — exit 0.
- `node --test dist/tests/typed-errors.test.js` — exit 0 (nine tests passed).
- The final independent adversarial review — PASS; its durable terminal record
  is `dev/plans/runs/codex/0.8.23/slice-30-adversarial-rereview-20260816.md`.
- `scripts/check-release-state-views.sh` — exit 0: its completion reference
  verifies every recorded landed SHA against `origin/release/0.8.23`.
  `origin/main` integration remains PENDING, but is not a Slice 30 blocker.

## Fix-N 9 — no-embedder pending-work regression

Independent review found that the immediate `FDB_EMBEDDER_REQUIRED` drain
contract had not been applied consistently to the projection dispatcher and
internal drain callers. A no-embedder body edge could still enter the retry
ladder and record a `failed` terminal; governed erasure then returned the
drain-only error before deleting pending work. The disposition preserves the
signed contract: direct `drain` reports immediate typed feedback, no-runtime
sessions dispatch no embedding jobs, pending rows remain recoverable, and
`erase_source`, `excise_source`, and deleted-first `purge` retain their
fail-closed at-rest erasure path.

- RED: source and operator erasure of a pending body edge each returned
  `EmbedderRequired`; a collapsed-retry body-edge fixture recorded terminal
  `failed`.
- GREEN: `993036cc` adds targeted real-SQLite coverage for those two erasure
  spellings, terminal-free no-runtime work, and deleted-first purge. It also
  ratifies legacy tests whose direct-drain `Scheduler` expectation was
  superseded by Slice 30, while keeping operational equivalence-refusal
  `Scheduler` behavior distinct.
- Verification: focused Slice 30/erasure/provenance/registration/TC-31 suites,
  the default engine suite, workspace check, workspace clippy, fmt, and diff
  checks passed. `--all-features` remains host-unavailable because this Linux
  seat lacks Apple and CUDA toolchains.

This repair is pending integration onto `origin/release/0.8.23`; it does not
change Slice 30's completed-on-release completion reference or the separately
pending `origin/main` integration state.

## Fix-N 10 — TypeScript flush-barrier stale assertion (RED staged)

Hosted CI run `31978698709`, default-embedder job `95242016822`, exposes a
stale TypeScript fixture in `slice20c-flush-barrier`: it still expects a
body-bearing edge behind more than 32 no-runtime nodes to enter the retry
ladder, reach a `failed` terminal, and make `drain` time out. That expectation
contradicts the integrated Slice 30 contract and the analogous Rust regression:
an absent runtime dispatches neither node nor edge embedding jobs; direct
`drain` immediately returns typed `FDB_EMBEDDER_REQUIRED`; pending work remains
recoverable. The RED evidence is the hosted job above. The pending test repair
will preserve the concrete `graph_edge_body_projection` error payload/readiness
assertions and replace only the superseded terminal/retry expectation; no
product behavior or public interface is being changed.

- GREEN: the replacement requires the exact blocked readiness payload for the
  oversubscribed node-plus-edge fixture, immediate typed
  `graph_edge_body_projection` feedback from `drain`, and a terminal-free edge
  after a scheduler opportunity. `npm run build:debug`, `npm run typecheck`,
  and `node --test --test-concurrency=1 --test-name-pattern "no-embedder
pending edge" dist/tests/slice20c-flush-barrier.test.js` passed in the
isolated worktree; the focused native case completed in 368 ms. Contract and
release-state checks passed, as did the analogous Rust
`no_embedder_body_edge_remains_pending_behind_many_enrolled_nodes` regression,
before release integration.
