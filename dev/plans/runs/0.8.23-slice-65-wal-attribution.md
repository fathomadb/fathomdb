---
release: 0.8.23
slice: 65
status: IMPLEMENTED_PENDING_WINDOWS_EVIDENCE
baseline: bf826c67
implementation: 0583a323
---

# 0.8.23 Slice 65 — WAL root-cause attribution

## Review and draft disposition

Slice 65 reviewed the 0.8.23 release plan and the work since its planning
baseline; Slice 30's no-embedder/erasure repair; Slice 60's first-party Windows
reader-holder witness; its draft allocation; the supporting Memex note; the
engine checkpoint, eight-reader-worker, and projection-runtime implementation;
and the Python SDK binding/wrapper result lifetime.

- Adjusted `N23-WINDOWS-WAL` / `R23-WINDOWS-WAL-DIAGNOSIS` /
  `AC23-WINDOWS-WAL-DIAGNOSIS`: they are complete evidence labels for Slice
  60, not a root-cause conclusion.
- Added temporary planning labels `N23-WAL-ATTRIBUTION`,
  `R23-WAL-ATTRIBUTION`, and `AC23-WAL-ATTRIBUTION`. They require owner
  classification and controls; they do not create authoritative IDs.
- Approved private opt-in, redacted attribution for every Engine-owned role:
  reader worker, projection dispatcher, projection worker, and writer/checkpoint.
  The real-SQLite Windows cases cover paused Engine reader, runtime-role
  completeness, concurrent Python reader, retained completed Python result,
  Memex-shaped serial lifecycle, and external child reader.
- Rejected pending evidence: retry parameter/tuning, binding finalizer or
  context-manager changes, reader-pool restructuring, public diagnostics/error
  changes, and any Windows/AV root-cause assertion.

## Design and TDD handoff

The design of record is
`dev/design/0.8.23-wal-attribution-investigation.md`. It preserves typed,
fail-closed `ErasureIncomplete`, requires each experiment's RED assertion to
be committed or visibly staged before its GREEN instrumentation/coordination
implementation, and makes all remedies conditional on discriminating evidence.
The future implementation must use the installed Python native binding on
Windows CI for Python cases; mocked SQLite or direct-Rust substitutions do not
meet this record.

No product code, test oracle, public contract, retry value, or CI execution
was performed by this design slice. No design-only lint guard was added, so a
prose-only synthetic RED test would not be meaningful.

## Fix-N — runtime ownership and truthful retained-result RED

Independent review found two gaps in the original design. A projection
dispatcher and every projection worker own runtime SQLite connections, so they
must report role-local index plus transaction/snapshot transitions. A busy
attempt cannot be `no_owned_snapshot` until those roles as well as every reader
worker are idle and there is no local checkpoint overlap. The collector records
`owned_runtime_transaction` separately from an owned reader snapshot.

The retained-result control now has the correct production expectation: a
materialized Python result retained after its call returns, with a live but idle
Engine, has zero owned transaction/snapshot and `erase_source` succeeds. Its
RED is the absence or misclassification of that required collector fact before
GREEN instrumentation, never a fabricated expectation that retention itself
causes `BUSY`.

## Release truth

## Implementation record — pending first-party Windows evidence

`0583a323` implements the approved diagnostic scope only; `a5254b83` and
`ae3b4b00` are Fix-Ns for reviewer-found lifecycle and evidence gaps. A private collector
is disabled unless controlled CI sets `FATHOMDB_WAL_ATTRIBUTION=1`; it emits
only Engine-owned role/index/phase, monotonic timing, checkpoint attempt,
busy, and frame counters. It registers the writer, each reader worker, the
projection dispatcher, and each projection worker. A `no_owned_snapshot`
fact requires all reader/runtime roles idle and no overlapping local checkpoint;
a busy checkpoint in that state is reported as `unclassified_external`, not
blamed on Windows or an unnamed client.

The new in-crate real-SQLite control pauses an Engine reader worker only after
its deferred transaction has selected and acquired a WAL snapshot. It parses
the collector's retained records, requiring all five busy attempts to name the
exact paused `reader_worker:0` as `owned_reader_snapshot`, then requiring the
post-release success to record `no_owned_snapshot`. Normal reader transitions
now begin only after actual `BEGIN DEFERRED` plus the collector-only snapshot
read; dispatcher and worker transitions begin at their actual SQLite statement
and `BEGIN IMMEDIATE` boundaries. The reader pause seam is crate-private, so
it is unavailable in published Rust artifacts. The retained Windows job runs
this record-parsing managed-reader control alongside the Slice 60
external-child control, with a nonsecret artifact and a load-bearing workflow
mutation test. Its exact lib-test filter is fully qualified, and the job scans
the retained output for `running 1 test` before it may continue; zero selected
tests are a failure. The normal reader probe is
`SELECT COUNT(*) FROM canonical_nodes`, not `SELECT 1`, so it establishes a
database/WAL snapshot before the collector labels it acquired.

The local Windows environment design was consulted. It documents manual,
guest-local NTFS staging over authenticated SSH/SCP and PowerShell or Git Bash
execution. This checkout has no automation launcher and intentionally contains
no guest credentials or address, so no local guest run was attempted or
claimed. This is an environment-access limitation only; hosted
`windows-latest` remains the required first-party closure witness.

Not implemented: retry tuning or parameterization, binding finalizer/context
manager behavior, reader-pool restructuring, a public diagnostic API, public
error fields, or an OS/external-reader attribution claim. The completed-result,
serial-lifecycle, and Python-SDK concurrent controls remain pending the first
Windows diagnostic run and binding-specific test seam review.

This slice is **IMPLEMENTED_PENDING_WINDOWS_EVIDENCE**, not complete. Slices
30 and 60 are complete on `origin/release/0.8.23`; their integration to
`origin/main` remains pending. Per-slice work is retained on the release
branch.
