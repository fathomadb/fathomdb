---
title: 0.8.23 Slice 70 status
status: PENDING_EXTERNAL
target_release: 0.8.23
---

# Slice 70 — dual runtime and private TC-5

## Landed local implementation

`d6fdcd6f..c3990aa6` is integrated on the local `release/0.8.23` branch.
It completes the reviewed strict embedding CPU/CUDA policy, the read-only
`doctor gpu` classifier and process matrix, witness v2, package rehearsal v2,
and the feature-gated private TC-5 vector-stage benchmark. Forced CUDA is
typed and fail-closed; `auto` reports a classified CPU fallback; neither path
labels SQLite/vector retrieval or TC-5 database work as GPU work.

The range contains committed behavioral RED-to-GREEN increments. Independent
design, code, and verification gates passed after the final doctor and TC-5
corrections. The release-branch integration preserved the TC-5 request-scoped
traps at Search, FTS, fusion, graph, and cross-encoder seams and the direct
Candle/cudarc UUID proof for a selected CUDA device.

Focused release-branch verification passed: the doctor subprocess/strace
matrix, witness and package rehearsal mutation suites, CUDA release contract,
TC-5 benchmark/engine/cache-factory tests, workspace checks, and clippy. CUDA
feature compilation was also checked where the local toolchain permitted it.

## Pending external release closure

No real CUDA device, trusted runner witness, installed-artifact GPU rehearsal,
or TC-5 characterization was run or claimed. Those retained evidence routes
remain `PENDING_EXTERNAL`; they block release completion and publication, not
the completed local implementation. No tag, push, registry operation, or
publication occurred.
