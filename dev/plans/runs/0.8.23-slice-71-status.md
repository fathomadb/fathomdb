---
title: 0.8.23 Slice 71 status
status: PENDING_EXTERNAL
target_release: 0.8.23
---

# Slice 71 — cross-encoder dual runtime

## Landed local implementation

`d7b45cce..a49ac989` is integrated on the local `release/0.8.23` branch.
It adds the independent strict `FATHOMDB_RERANK_DEVICE` policy
(`auto`, `cpu`, or `cuda:N`), typed forced-CUDA failure propagation, separate
cross-SDK reranker resolution reporting, and the model-free `doctor
reranker-gpu` diagnostic. `auto` probes visible devices in deterministic order
until it finds a compatible GPU; `cpu` does not initialize CUDA; forced CUDA
never becomes a CPU rerank.

The candidate-only, credentialless reranker CUDA v3 preflight/rehearsal route
binds the exact Python, N-API, and CLI feature tuples, a verified TinyBERT
cache, installed-artifact rerank smokes, forced-policy captures, and the
diagnostic evidence. The final producer correction mounts the cache root the
loader actually resolves and disables the default embedder for forced SDK
captures, so their exact typed reranker refusal cannot be masked by BGE model
loading.

The range followed committed RED-to-GREEN changes and passed independent final
review. Focused reranker policy, engine/report, CLI diagnostic, producer,
witness, rehearsal, release-contract, shell-lint, actionlint, and CUDA-feature
checks passed on the integrated release branch.

## Pending external release closure

Compatible-GPU and incompatible-classifier installed-artifact receipts remain
`PENDING_EXTERNAL`. No real GPU execution, trusted runner dispatch, tag, push,
registry operation, or publication occurred. The local v3 route rejects any
attempt to substitute feature labels or synthetic evidence for those receipts.
