---
title: 0.8.23 Slice 72 status
status: REVIEWED_PENDING_INTEGRATION
target_release: 0.8.23
---

# Slice 72 — concurrent embedding and CE GPU coexistence

## Landed local implementation

`77c460c9..824b1d4b` is integrated on the local `release/0.8.23` branch.
It adds a feature-gated, test-only real-engine characterization harness for
one process that uses independently selected CUDA embedding and TinyBERT CE
reranking on the same process-visible device. It does not add a GPU scheduler,
reservation, memory cap, or public telemetry API.

The harness preflights immutable BGE and TinyBERT caches before engine open,
requires exactly one process-visible selected CUDA device for each model, and
uses the resolver UUID as telemetry authority. Its basic and bounded
low/moderate tiers are designed to assert real BGE and CE forward overlap when
run on an authorized CUDA runner; its ignored stress tier is authenticated to a
parent watchdog, has a hard 120-second child-process bound, and retains timeout
evidence. Receipts distinguish process CPU/RSS from
device-wide GPU utilization and VRAM, bind compute-app PID evidence, retain
sensor provenance, and fail closed on malformed, wrong-device, missing, or
out-of-order evidence.

The range contains committed RED-to-GREEN increments, including review-driven
contracts for cache preflight, receipt schema, active-overlap sampling, stress
containment, and failure evidence. Independent design review, code review,
verification, and semantic public/project/developer documentation review
passed. Focused deterministic tests, `agent-verify`, workspace check/clippy,
markdown/documentation lint, and strict MkDocs passed.

The TC-5 decision is **no Slice 72 GPU path**: its private vector-stage
workload is pre-fusion and rejects CE; the external TC-5 execution contract
remains CPU-only pending a separate amendment. SQLite bit-KNN, exact-f32
database reranking, FTS, fusion, graph, and storage remain CPU work.

## Authorized-host CUDA evidence

The authorized Linux host provided CUDA 12.6.68 at
`/usr/local/cuda-12.6/bin/nvcc` and NVIDIA driver `580.173.02`. The test ran
on process-visible `cuda:0`, an RTX 3090 (UUID
`GPU-5f9cfc90-2be1-06a7-ce39-5a6d294b209b`, compute capability 8.6, 24 GiB).
The harness used `CUDA_VISIBLE_DEVICES=0`, `CUDARC_CUDA_VERSION=12060`,
`CUDA_COMPUTE_CAP=86`, and only the already pinned local BGE and TinyBERT
assets. The CUDA toolkit required explicit `PATH`, `LIBRARY_PATH`, and
`LD_LIBRARY_PATH` entries for `/usr/local/cuda-12.6/{bin,lib64}`; that is a
host invocation detail, not a product fallback.

An initial real attempt failed the harness's receipt timestamp contract and is
retained as failure evidence. The committed behavioral correction
`3193744b..be6deb8c`, merged at `824b1d4b`, puts the forward rendezvous and
receipt samples on one clock and recovers the test-only lock after a retained
failure. Independent review passed. The corrected basic, bounded low/moderate,
and explicit 60-second stress tiers then passed, including an independent
cache-only verifier rerun. Retained receipt hashes on the authorized host are:

- Initial failure: `/tmp/fathomdb-slice72-receipts.H9v0gi/slice72-basic-2890592.json`
  — `19b9aef34e25bf0b49b4293551aa3e7234d3ffa7f9c1d17296b960e316d1ec6f`.
  It records the typed panic `receipt timestamps are not strictly increasing`
  with intentionally null phase, summary, and timing fields.

- Candidate basic: `/tmp/fathomdb-slice72-receipts.H9v0gi/slice72-basic-2899332.json`
  — `1c2692b165e87e995ff7c9bb9b612e19fb395c67a139fc61a64cd31f5ead0c3a`.
- Candidate moderate: `/tmp/fathomdb-slice72-receipts.H9v0gi/slice72-moderate-2899535.json`
  — `10fc459ca118998823e99f2cdc7e74a4b579863cefbcef898942cbeb31cac427`.
- Candidate stress: `/tmp/fathomdb-slice72-stress-receipts.uLienX/slice72-stress-2900373.json`
  — `e177eb34eba15ac9c316da449344b49048ee57de5f670e62f6427647015521a7`.
- Independent verifier basic/moderate:
  `/tmp/fathomdb-slice72-verifier-receipts.3X2Vjl/` —
  `f0f3d7b6c98b25464050805a93c8c7ebfef7d0ac44c02e891a18ca933146fda8`
  and `157cf89af49d5b396506ef73cc912e82475a79732d0f64a67b754f38c8683e4f`.
- Independent verifier stress:
  `/tmp/fathomdb-slice72-verifier-stress-receipts.KCxbVL/slice72-stress-2914824.json`
  — `974fdd7e73bdc840ff73efa606f7deec92530c2e4203f8aa12b68dbb9d6e4e7a`.

The independent stress run recorded 3,234 iterations, 60 seconds, concurrency
3, UUID/PID-bound compute-app evidence, a 945,815,552-byte VRAM peak, and
device-wide utilization samples. An unrelated compute PID appeared in its
final stress sample; it is retained as a confounder. These are coexistence and
resource observations, not a performance threshold or allocation claim. No
dispatch, tag, push, registry operation, or publication occurred.
