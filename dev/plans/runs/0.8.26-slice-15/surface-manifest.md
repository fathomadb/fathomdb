# Slice 15 graph surface manifest

The baseline text inventory found 23 Rust, 9 Python, 5 TypeScript, and 7 public
documentation/interface files containing maintained graph-expansion names.
These are conservative file counts, not promised edit counts. Option B would
touch the ordinary result codecs and fixtures across all three bindings;
option A can isolate the carrier but still requires new binding methods and
documentation.

Final-tree verification must return the public API, codecs, schemas, and
bindings to the Slice 15 baseline.
