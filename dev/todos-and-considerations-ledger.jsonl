{"ts": "2026-07-03T20:52:05.224Z", "seq": 1, "kind": "caveat", "summary": "EXP-S (#2, 0.8.14) coexisting-index / multi-index-write substrate must not foreclose the OPP-12 projection registry that will later sit on it — design the multi-index write so a declarative filterable/rankable/searchable registry can wrap it without a substrate reshape", "area": "engine/substrate", "epistemic": "verified", "id": "TC-1", "owner": "orchestrator", "priority": "p2", "status": "open", "refs": ["opp:OPP-12", "plan:dev/plans/plan-0.8.14.md", "file:dev/design/record-lifecycle-protocol/projection-registry-and-async-embed.md", "git:83ef13fc"], "body": "Grounded caveat for the 0.8.14 EXP-S build (Slice-0 ADR + Slice-5 keystone). OPP-12's engine-owned projection registry (filterable/rankable/searchable declarations; same-txn FTS/filter, async vector via the existing worker + the atomic dense_readiness flip) is architecturally a DECLARATIVE FRONT-END over exactly the coexisting-index / incremental multi-index write that EXP-S builds -- EXP-S is the substrate the projection registry will sit on (OPP-12 lands >=0.9.x). No dependency edge forces ordering, and EXP-S touches NONE of OPP-12's surface (verified: plan-0.8.14 has no SearchHit / lifecycle / projection scope). But the Slice-0 substrate ADR should design the multi-index write so a later projection registry can WRAP it without a substrate reshape (don't foreclose per-kind declarative projection specs or the async-vector dense_readiness flip). Re-check at OPP-12 scheduling. See [[opp12-record-lifecycle-protocol]]."}
{"ts": "2026-07-03T21:00:49.077Z", "seq": 2, "kind": "observation", "summary": "typescript-6 major looks PARTIALLY APPLIED: src/ts/package.json already pins typescript ^6.0.3 (matches its lockfile) while Dependabot PR #67 (typescript 6.0.3) is still OPEN — the typescript LBO should reconcile (verify + close #67, or complete the bump), not assume it's unstarted", "area": "src/ts/build", "epistemic": "verified", "id": "TC-2", "owner": "hitl", "priority": "p3", "status": "open", "refs": ["git:8bec917e", "file:src/ts/package.json", "url:https://github.com/coreyt/fathomdb/pull/67"], "body": "Surfaced during Library Sweep #2 (typescript is a MAJOR, out of the 6-minor scope, left untouched). State is ambiguous: the src/ts manifest+lockfile are internally consistent at typescript ^6.0.3, yet PR #67 was never merged/closed. So the typescript-6 upgrade is either already effectively in place (PR stale, just needs closing) or half-applied. The typescript LBO (or the 0.8.20 major sweep) must verify the actual tsc build under ^6.0.3 and reconcile the open PR rather than treat typescript-6 as greenfield. Ref src/ts/package.json + PR #67."}
{"ts": "2026-07-04T00:19:41.019Z", "seq": 3, "kind": "caveat", "summary": "DESIGN-ADDRESSED: the 0.8.14 Slice-0 ADR (dff4830c, D5) DISCHARGES this caveat — the D2 per-kind index-target set is the seam a later OPP-12 projection registry wraps; FTS/filter same-txn + vector async-via-worker preserved; terminal-cursor readiness kept per-kind-extensible. Now WATCHING: re-check the Slice-5 keystone actually honors the seam, and again at OPP-12 scheduling", "area": "engine/substrate", "epistemic": "verified", "id": "TC-1", "owner": "orchestrator", "priority": "p2", "status": "watching", "refs": ["seq:1", "git:dff4830c", "opp:OPP-12"], "body": "Advanced from open->watching. The ratified Slice-0 substrate ADR explicitly discharges TC-1 in §D5 (verified from git dff4830c): the engine already owns the async substrate a future OPP-12 projection registry needs (projection workers + _fathomdb_projection_terminal readiness = durable dense_readiness equivalent; per-kind kind_is_vector_indexed), and the ADR obligates EXP-S to (a) keep the D2 per-kind index-target set a wrappable seam, (b) preserve FTS/filter same-txn + vector async-via-worker (no collapse to sync), (c) keep terminal-cursor readiness per-kind-extensible. Trigger to re-check: the Slice-5 keystone implementation honors these; then close/re-open at OPP-12 scheduling (>=0.9.x)."}
{"ts": "2026-07-04T01:29:26.727Z", "seq": 4, "kind": "consideration", "summary": "Engine::bm25f_search scores by re-tokenizing each field via a temp-FTS5 round-trip (fts5_tokenize) per field/candidate — correctness-first, but there is NO perf gate on bm25f_search. A future perf pass / Slice 40 / the 1-2M scaling work should weigh this before F5 rides a hot path", "area": "engine/fts-bm25f", "epistemic": "verified", "id": "TC-3", "owner": "hitl", "priority": "p3", "status": "open", "refs": ["git:c57e4e99", "plan:dev/plans/plan-0.8.14.md"], "body": "Surfaced at 0.8.14 Slice 10 (F5 BM25F, c57e4e99); flagged by BOTH codex §9 and the implementer. The in-engine BM25F scorer (justified ADR-0.8.1 deviation — FTS5 bm25() pins k1/b, so scoring is done in-engine to honor R-F5-1's tunable b) computes per-field term stats by re-tokenizing each field through fts5_tokenize per field per candidate. Correct + tokenization-faithful (the fix-1 that resolved codex's medium finding), but O(fields x candidates) tokenizer round-trips with no perf gate on the path. Engine-internal / experimental today, so NOT blocking. Re-weigh before F5 is put on a hot/large query path: a perf gate at Slice 40 (release readiness) or during the 1-2M scaling work, or a caching/precompute optimization. Ref c57e4e99 (bm25f_search)."}
{"ts": "2026-07-05T20:13:03.419Z", "seq": 5, "kind": "todo", "summary": "ENFORCE the HITL mandate in tooling: the eu7/eval harness + eval runners MUST DEFAULT to GPU embedding (embed-cuda + FATHOMDB_EMBED_DEVICE=cuda:0, rerank-cuda for rerank) when the 3090s have compute+memory room, CPU fallback only -- so no agent has to remember the flags. Exclude the K620 (FATHOMDB_GPU_EXCLUDE); check nvidia-smi room before dispatch; verify GPU engaged", "area": "eval/gpu", "epistemic": "verified", "id": "TC-4", "owner": "hitl", "priority": "p1", "status": "open", "refs": ["file:dev/design/gpu-eval-activities-policy.md", "opp:none"], "body": "Standing HITL mandate 2026-07-05 (policy: dev/design/gpu-eval-activities-policy.md; memory repo-must-use-gpu-for-eval-when-3090-room). Root cause: a fresh 0.8.14 eu7 run crawled on the CPU embedder (~0.43 docs/sec, seed-timeout-prone) with both 3090s at 0% idle. GPU is 15-93x faster (0.8.7) and CPU<->CUDA is 1-bit identical (fidelity-safe). Per fix-the-tooling-not-people: make GPU-when-room the DEFAULT in the eu7 harness (eu7_real_corpus_ac.rs currently builds --features default-embedder = CPU) + the eval runners (agent-build.sh / eval scripts), with a room-check (nvidia-smi util+free-mem) and CPU fallback only when the 3090s are busy/full. Watch the GPU-eval build traps (libcuda bundling / stale .so shadowing, eval-environment-operational-notes). SCOPE: internal eval/dev/CI only; shipped-library default (CPU/opt-in) unchanged. PLACEMENT TBD (HITL): fold into 0.8.14 Slice 40, or a fast-follow pico. Interim: pass the env/flags explicitly per run (the current eu7 re-run already switched to GPU)."}
{"ts": "2026-07-05T20:16:50.031Z", "seq": 6, "kind": "todo", "summary": "REFINED (HITL 2025-07-05): CPU is NOT a general fallback. Full eval/embed/rerank suites ALWAYS run on GPU; CPU is permitted ONLY for two narrow probes -- (1) simple CPU-compatibility testing, (2) CPU-library repeatability/compatibility checks. 'Feature compatibility' = check compat with CPU libraries, NOT run full suites on both CPU+GPU. When 3090s busy: wait/queue or use the other 3090, never a full CPU run. Harness/eval-runner default = GPU for full runs", "area": "eval/gpu", "epistemic": "verified", "id": "TC-4", "owner": "hitl", "priority": "p1", "status": "open", "refs": ["seq:5", "file:dev/design/gpu-eval-activities-policy.md"], "body": "Tightens the enforcement scope. The eu7/eval harness + eval runners MUST default to GPU for full runs; there is NO CPU full-suite fallback. CPU is reserved for minimal, targeted compatibility probes only: a CPU-code-path build/run smoke, and a CPU-library repeatability/compatibility check -- never a mirror of the full GPU suite. On a busy GPU, wait/queue or use cuda:1, don't run the full suite on CPU. Policy: dev/design/gpu-eval-activities-policy.md (updated). Placement of the tooling change still TBD (HITL): 0.8.14 Slice 40 or a fast-follow pico."}
{"ts": "2026-07-06T01:25:40.538Z", "seq": 7, "kind": "todo", "summary": "Re-baseline the eu7 0.90 recall floor for the GROWN 18,472-doc cross-source corpus. The floor + the 0.896 CPU baseline (ci_hi 0.925) predate the musique (Jun 16) + compmix (Jul 3) cross-source additions, which mechanically lower recall@10 at fixed N. Needs a fresh CPU same-backend eu7 N=7667 measurement on the current corpus + an HITL threshold ruling on a corpus-appropriate floor. Separate from 0.8.14 (D6 zero vec0 rewrite excludes a regression)", "area": "eval/eu7", "epistemic": "verified", "id": "TC-5", "owner": "hitl", "priority": "p1", "status": "open", "refs": ["file:dev/design/gpu-eval-activities-policy.md", "plan:dev/plans/plan-0.8.14.md"], "body": "Surfaced at 0.8.14 Slice 20: a GPU eu7 run read vector-stage recall@10 N=7667=0.833 (ci_hi 0.864 < 0.90) vs the historical CPU baseline 0.896 (ci_hi 0.925 PASS); N=1000=0.950 PASS. Two verified confounds: (1) cross-backend (GPU embed vs CPU baseline; near-threshold 1-bit-quant flips at large N) -- fixed forward: eu7/fidelity gates now run CPU same-backend per gpu-eval-activities-policy.md; (2) corpus growth (musique+compmix added post-baseline). D6 (codex-verified zero vec0 rewrite @ Slice 5) excludes a 0.8.14 regression, so Slice 20 closed on the D6 no-op basis (HITL). OPEN: is 0.90 still right for the grown corpus? Resolve via a fresh CPU same-backend eu7 N=7667 on the current corpus, then an HITL floor ruling (keep 0.90 or re-baseline). Placement TBD."}
{"ts": "2026-07-06T02:21:27.885Z", "seq": 8, "kind": "consideration", "summary": "embed_batch_cls is py-only (no TS binding) — a py-first X1 asymmetry surfaced+deferred at 0.8.14 Slice 40. A future binding-parity release should DECIDE: add the TS embed_batch_cls, or ratify the py-first asymmetry as intended. Currently guarded by the new drift-guard test (src/python/tests/test_surface.py, a8e28ff2) + board R-X-1 so it can't silently regress", "area": "sdk/parity", "epistemic": "verified", "id": "TC-6", "owner": "hitl", "priority": "p3", "status": "open", "refs": ["git:a8e28ff2", "plan:dev/plans/plan-0.8.14.md"], "body": "At 0.8.14 Slice 40 X1, the embed_batch_cls helper (CLS-pooled GPU batch embed, shipped in Slice 25 for eval) is exposed py-only with a .pyi stub; there is no TS binding. Previously a silent harness blind spot; now non-vacuously ASSERTED as a known py-only asymmetry (test a8e28ff2). This is not a 0.8.14 gap (embed_batch_cls is an eval/GPU helper, not a governed verb) but a binding-parity decision for a future release: (a) add the TS embed_batch_cls for full X1 symmetry, or (b) ratify it as a deliberate py-first asymmetry (eval-only helper). Distinct from TC-5 (eu7 floor). Placement TBD (a future SDK-parity or 0.8.16+ release)."}
{"ts": "2026-07-08T00:18:18.773Z", "seq": 9, "kind": "consideration", "summary": "C-1 co-design TIMING (EntityTypeSpec -> ProjectionSpec): Memex's Commission C / entity-schema-registry is its ACTUAL 0.5.x work and is LIVE NOW; it builds toward the C-1 seam. C-1 is HARD (one declarative registration flow -- app never syncs two registries). FathomDB's projection registry is 0.9.1, but the EntityTypeSpec->ProjectionSpec co-design ADR should be AGREED before Memex locks Commission C's registry shape, or we recreate the two-registry-sync drift C-1 exists to prevent. Near-term cross-repo coordination watch (raise to HITL) even though the FathomDB build is >=0.9.x.", "area": "cross-repo/co-design", "epistemic": "verified", "id": "TC-7", "owner": "hitl", "priority": "p1", "status": "open", "refs": ["opp:OPP-12", "file:dev/plans/0.9.x-roadmap-DRAFT.md", "git:6f7456c"]}
{"ts": "2026-07-08T00:18:18.800Z", "seq": 10, "kind": "consideration", "summary": "Cause-A TYPED SearchHit.id round-trip (OPP-12 C-2 / F-8a): typed IdSpace {space,value} + write_cursor->logical_id swap. VERIFIED NOT started on main -- SearchHit.id is u64 (lib.rs:1109), stable_id is additive Option<String> (1132), no IdSpace type. Breaking => >=0.9.x (0.9.0 P1-S-C in the draft). This is MEMEX's #1 P0 need: gates the whole OPP-12 Memex-side pair, graph-arm hit->goal/WMAction reconnection (OPP-1 graph path), and typed hit-level data-fitness beyond the string stable_id base. Standing cross-repo blocker; lands-together with the C-2 contract + anonymous-node surrogate minting.", "area": "engine/identity", "epistemic": "verified", "id": "TC-8", "owner": "hitl", "priority": "p0", "status": "open", "refs": ["opp:OPP-12", "opp:OPP-1", "file:dev/plans/0.9.x-roadmap-DRAFT.md", "git:main"]}
{"ts": "2026-07-08T00:44:21.803Z", "seq": 11, "kind": "consideration", "summary": "TC-7 ADVANCED open->in_progress: C-1 co-design two-shot loop KICKED OFF. Fresh sub-ledger dev/design/record-lifecycle-protocol/OPP-12-sub-ledger.jsonl created (protocol + SHOT-1 FATHOM posted; full text OPP-12-C1-shot1.md). SHOT 1 forces 6 bounded questions; the load-bearing near-term catch = Q1 (add optional AttributeSpec role hint) + Q5 (engine-as-derived-cache) must be agreed BEFORE Memex Commission C locks AttributeSpec (breaking re-serialization otherwise). Awaiting HITL review of SHOT-1 content before Memex is routed to answer (SHOT 2).", "area": "cross-repo/co-design", "epistemic": "verified", "id": "TC-7", "owner": "hitl", "priority": "p1", "status": "in_progress", "refs": ["opp:OPP-12", "file:dev/design/record-lifecycle-protocol/OPP-12-sub-ledger.jsonl"]}
{"ts": "2026-07-08T01:27:02.200Z", "seq": 12, "kind": "consideration", "summary": "TC-7 ADVANCED in_progress->converged-pending-hitl: OPP-12 C-1 two-shot co-design CONVERGED in 2 rounds. Memex agreed Q1(field landed @95ed450)/Q3/Q5; Q2/Q4/Q6 accepted with FathomDB refinements (Q4 engine-needs-no-provisional-concept; Q2 explicit no-migration; over-build restated). Converged contract OPP-12-C1-converged-contract.md (seed for joint 0.9.1 P2.S0 ADR). Deferred to P2.S0: apply-atomicity + tokenizer/embedder defaults. Co-lands 0.9.1 / Memex 0.5.x-succ; build!=adopt. NEXT: HITL ratification of the converged C-1 contract.", "area": "cross-repo/co-design", "epistemic": "verified", "id": "TC-7", "owner": "hitl", "priority": "p1", "status": "converged-pending-hitl", "refs": ["file:dev/design/record-lifecycle-protocol/OPP-12-C1-converged-contract.md", "opp:OPP-12"]}
{"ts": "2026-07-08T02:11:07.100Z", "seq": 13, "kind": "consideration", "summary": "TC-7 RESOLVED (converged-pending-hitl -> ratified): HITL ratified the C-1 converged contract 2026-07-07. C-1 co-design is closed + agreed (OPP-12-C1-converged-contract.md); the near-term timing risk is retired (break-if-late AttributeSpec.index field frozen inert in Memex Commission C @95ed450). Remaining C-1 work is the 0.9.1 P2.S-F engine build (a future-release item, separately build-authorized at 0.9.x §7.8), NOT a standing todo watch. Closing TC-7.", "area": "cross-repo/co-design", "epistemic": "verified", "id": "TC-7", "owner": "hitl", "priority": "p1", "status": "resolved", "refs": ["file:dev/design/record-lifecycle-protocol/OPP-12-C1-converged-contract.md", "opp:OPP-12"]}
{"ts": "2026-07-08T03:14:33.152Z", "seq": 14, "kind": "reconcile", "summary": "TC-8 PLACEMENT reconciled to plans (HITL 'ledgers secondary -> reflect new release planning'): the OPP-12 C-2 typed SearchHit.id swap (write_cursor->logical_id + typed IdSpace) is NO LONGER >=0.9.x. Per master F-19/F-20 it lands IN the 0.8.x line at 0.8.19 (odd, label-only) OPP-12 Phase-1 [existence axis . transition/purge . C-2 swap . schema mig . X1]. Still VERIFIED-NOT-STARTED on main (SearchHit.id u64, stable_id additive). BUILD-AUTHORIZATION for 0.8.19/0.8.20 remains a SEPARATE pending HITL gate (build != adopt); this entry reconciles PLACEMENT only. Memex's #1 P0 need.", "area": "cross-repo/lifecycle", "epistemic": "verified", "id": "TC-8", "owner": "hitl", "priority": "p0", "status": "open", "refs": ["file:dev/plans/0.8.6-0.8.16-PROGRAM-SEQUENCING.md", "opp:OPP-12", "seq:10", "git:f3ccf67a"]}
{"ts": "2026-07-08T03:14:33.179Z", "seq": 15, "kind": "reconcile", "summary": "TC-7 SLOT CORRECTION (stays RESOLVED): the C-1 EntityTypeSpec->ProjectionSpec co-land is NO LONGER '0.9.1 P2.S-F'. Per master F-19/F-20 (and OPP-12 sub-ledger seq 8) C-1 co-lands at FathomDB 0.8.20 (OPP-12 Phase-2 + breaking-pair publish), DESIGN UNCHANGED. Read every '0.9.1'/'P2.S-F' in seq 13 + the converged contract as 0.8.20. No re-ratification (scheduling change, not contract change).", "area": "cross-repo/co-design", "epistemic": "verified", "id": "TC-7", "owner": "hitl", "priority": "p1", "status": "resolved", "refs": ["file:dev/design/record-lifecycle-protocol/OPP-12-C1-converged-contract.md", "opp:OPP-12", "seq:13", "git:f3ccf67a"]}
{"ts": "2026-07-08T11:55:09.115Z", "seq": 16, "kind": "decision", "summary": "TC-8 BUILD-AUTHORIZED (HITL 2026-07-08, master F-21): the OPP-12 C-2 typed SearchHit.id swap (write_cursor->logical_id + typed IdSpace) build gate is CLEARED at its 0.8.19 Phase-1 slot. Build != adopt (adoption arms still per-item gated); publish stays a separate per-x.y.z HITL call. Stays OPEN as a tracked build item (VERIFIED-NOT-STARTED on main); state advances open->build-authorized. In-sequence after 0.8.18 (not reordered); Steward flagged a concurrent-schema-migration hazard vs parallelizing into 0.8.16. Ladder plan-0.8.19.md still holds pre-F-19 free-threading content -> OPP-12 Phase-1 ladder TBA when the slot approaches.", "area": "cross-repo/lifecycle", "epistemic": "verified", "id": "TC-8", "owner": "hitl", "priority": "p0", "status": "build-authorized", "refs": ["plan:dev/plans/0.8.6-0.8.16-PROGRAM-SEQUENCING.md", "opp:OPP-12", "seq:14"]}
{"ts": "2026-07-08T21:00:45.325Z", "seq": 17, "kind": "todo", "summary": "TC-9 (NEW): bump ort =2.0.0-rc.10 -> 2.0 STABLE when it releases. HITL accepted the RC pin (2026-07-08) for 0.8.16 ONNX behind the optional non-default onnx-embedder feature (EVAL-only; default build/wheels pull zero new deps). rc.12 currently fails to build (VitisAI EP mismatch); rc.10 is newest-that-builds; 1.x is EOL. Watch for ort 2.0 stable and bump the pin (Library-Bump-Steward candidate). Low urgency (opt-in EVAL feature).", "area": "deps/onnx", "epistemic": "verified", "id": "TC-9", "owner": "steward", "priority": "p3", "status": "open", "refs": ["plan:dev/plans/plan-0.8.16.md", "git:146eecca"]}
{"ts": "2026-07-09T12:27:12.326Z", "seq": 18, "kind": "todo", "summary": "TC-9 EXTENSION (D3/L3 finding, 2026-07-09, Steward-verified): ONNX-GPU-EP cross-backend Delta measured OOB (durable doc git:76568975). candle-CPU(Cls) <-> ONNX-CUDA-EP(Cls; ORT 1.26.0 GPU + cuDNN 9.5 + RTX3090 cuda:1) = 2/17280 raw sign flips (1/17280 proxy-mean-centered), cosine 0.99999996, L2 <= 5.4e-4, deterministic x3, EFFECTIVE PROVIDER VERIFIED CUDA -- the FIRST non-byte-identical cross-backend leg (both prior CPU legs = 0 flips; pure GPU FP divergence, not a broken export). FINDING: as-shipped OrtBgeEmbedder under ort=2.0.0-rc.10 (default-features=false) CANNOT instantiate the CUDA EP -- ort::init() is never called (Steward-verified ABSENT in ort_bge.rs; the EP-dispatch + loud-fallback code IS present) -> ORT 'DefaultLogger not registered' -> LOUD CPU fallback. Proven binding-not-host: same native stack runs CUDA via Python + via a standalone rc.10 probe WITH a one-time ort::init().commit(). NOT a shipped hazard (ONNX = opt-in non-default EVAL feature; fallback is LOUD not silent; default library path = candle-CPU, unaffected). ACTION folded into TC-9 (ort 2.0-stable bump): (a) add a one-time ORT env init so ONNX GPU/cross-vendor EPs are reachable, (b) re-verify CUDA-EP session creation on 2.0-stable. #5/D4 BEARING: none now -- the D4 same-identity floor stays tight (0 flips; same-identity candle CPU<->CUDA was 0/6144 @0.8.7), and the 2-flip GPU-EP leg is a DISTINCT '-onnx' identity already refused by identity + R-VEQ-5 additive-only; a FUTURE cross-vendor portability ADR must budget a NON-ZERO GPU-EP flip rate.", "decider": "steward", "extends": "TC-9", "refs": ["git:76568975", "file:dev/plans/runs/0.8.18-slice-0-onnx-gpu-ep-calibration.md", "file:src/rust/crates/fathomdb-embedder/src/ort_bge.rs"]}
{"ts": "2026-07-09T18:27:35.304Z", "seq": 19, "kind": "todo", "summary": "TC-10 (NEW, HITL-directed 2026-07-09): #5 vector-equivalence probe OPEN-LATENCY. The #5 keystone re-embeds the 45-probe set at EVERY open of a vector-kind DB (the cross-open backend-drift check) -> a new OPEN-time overhead. NOT a query-latency / AC-012/013/020 breach (the read path adds only an open-time AtomicBool dense_disabled check; representation unchanged). ACTION: (a) MEASURE the open-latency overhead (45 CPU embeds at open); (b) consider IDENTITY-GATED probe caching -- re-run the probe only when the embedder identity/revision CHANGES since last open, not every open (the EmbedderIdentity pre-filter already tracks identity) -- as a future-micro optimization; (c) optionally a GA open-latency AC/measured note. Surfaced at 0.8.18 Slice-40 GA verification (orchestrator); HITL directed TC-track, NOT a GA blocker (0.8.18 closed label-only). Owner: future micro (convenient slot >=0.8.21 or fold into an OPP-12 slice). Correctness-first: the every-open probe is correct; caching is an optimization.", "decider": "hitl", "kind_detail": "open-latency", "release": "0.8.18", "refs": ["file:dev/design/0.8.18-slice-0-vector-equivalence-publish-design.md"]}
{"ts": "2026-07-09T18:36:37.571Z", "seq": 20, "kind": "reconcile", "summary": "OPEN-TC SCHEDULE RATIFIED (HITL 2026-07-09; master F-22). Slot per open item: TC-8 (OPP-12 C-2 SearchHit.id swap write_cursor->logical_id+IdSpace) = 0.8.19 Phase-1 (it IS the work; build-auth F-21). embed_batch_cls TS-binding parity (py-only X1 asymmetry, seq8) = 0.8.19/0.8.20 (decide add-TS vs ratify-py-first, folded into their X1 parity). eu7 BASIS decision (no-op vs real) = 0.8.20 (real-publish gate; bounded re-baseline if OPP-12 touches retrieval/gold-keying). TC-5 (full eu7 0.90 floor re-baseline, grown 18,472-doc corpus, seq7) = 0.8.23 scale-bound soft (natural home). TC-10 (#5 open-latency, seq19) = MEASURE at 0.8.19, optimize (identity-gated probe caching) >=0.8.21 only if warranted. bm25f_search perf gate (seq4) = 0.8.21 (#13 bench harness). TC-9 (ort 2.0-stable bump + ort::init GPU-EP fix, seq17/18) = 0.8.22 dep-migrations or opportunistic pico when ort 2.0-stable ships. typescript-6/Dependabot #67 (seq2) = next Library Sweep pico / 0.8.22. GPU-eval-mandate tooling enforcement (seq5/6) = opportunistic tooling task, pair with 0.8.23/24 heavy eval. RESOLVED items (EXP-S/projection caveat seq1/3, TC-7 C-1 seq9-13) need no slot.", "decider": "hitl", "kind_detail": "tc-schedule-ratified", "refs": ["plan:dev/plans/0.8.6-0.8.16-PROGRAM-SEQUENCING.md"]}
{"ts": "2026-07-09T23:19:10.270Z", "seq": 21, "kind": "todo", "summary": "TC-11 (NEW, HITL 2026-07-09; master F-23 GUARDRAIL): 0.8.20 (OPP-12 Phase-2) MUST pin the doc-seeded 'h:' end-state BEFORE Phase-2 proceeds -- is doc-seeded/anonymous SearchHit.id 'h:<content-hash>' TERMINAL-FOREVER, FORWARD-MINT-ONLY (new anonymous nodes get l: surrogates, old stay h:), or BACKFILLED (h:->l: remap of the existing ~18k doc-seeded rows + real-gold remap)? The genuine long-run risk (surfaced by the 0.8.19 Slice-0 adversarial audit) is a PERMANENT SPLIT: pre-0.8.20 doc-seeded h: vs post-0.8.20 anonymous l: for identical re-ingested content. This exists INDEPENDENT of 1a's timing (1a correctly deferred the decision to where the natural-key/projection registry lives). LEADING HYPOTHESIS (audit inference, confirm at Phase-2 design): 'h:' is TERMINAL -- doc-chunks are NOT EntityTypeSpec entities, so the C-1 anonymous-entity surrogate path may not apply to them -> h: terminal -> 1a fully safe, no backfill ever. ACTION: de-stale plan-0.8.20.md (currently holds pre-F-19 dep-migrations content) + author OPP-12 Phase-2 ladder including this end-state decision record. Owner: 0.8.20 Slice-0.", "decider": "hitl", "kind_detail": "phase2-docseeded-endstate", "release": "0.8.20", "refs": ["plan:dev/plans/0.8.6-0.8.16-PROGRAM-SEQUENCING.md"]}
{"ts": "2026-07-10T21:02:42.994Z", "seq": 22, "kind": "reconcile", "summary": "TC-8 (OPP-12 C-2 SearchHit.id swap: write_cursor+stable_id -> typed IdSpace) RESOLVED — shipped in 0.8.19 Phase-1 (label-only, origin/main 578fe20b; master F-24; OPP-12 sub-ledger seq 9). u64->IdSpace {space,value}; to_prefixed() == prior stable_id byte-for-byte => real-gold keying a true no-op (no gold-remap); breaking surface built label-only, PUBLISHES at 0.8.20. TC-11 (0.8.20 doc-seeded h: end-state pin + de-stale plan-0.8.20) REMAINS OPEN and is the FIRST Phase-2 step. NEW carries for the 0.8.20 commission, from the rubric-eval v3 pilot findings on 0.8.19 (HARD GATE PASS 12/12, 84.4%): (a) G6 — run Py/TS parity BEFORE land as one unit on any surface-touching slice (0.8.19 ran X1 green AFTER land due to a native-import env trap; treat an env trap as a landing blocker, not a follow-up); (b) TC-RUBRIC-7 — persist every codex §9 review transcript to a durable release-namespaced path (e.g. scratchpad/codex/0.8.20/slice-N-review.txt) so B1/B2 independent re-verification is possible after the fact; (c) TC-RUBRIC-5 / A6 — dedicated checkout/worktree per orchestration (the shared-checkout guard; see the steward guardrail-fix proposal). Fold (a)+(b) into the 0.8.20 X0 process gate.", "decider": "steward", "release": "0.8.20", "refs": ["git:578fe20b", "seq:21", "file:dev/design/record-lifecycle-protocol/OPP-12-sub-ledger.jsonl"]}
{"ts": "2026-07-11T15:40:59.056Z", "seq": 23, "kind": "consideration", "summary": "0.8.20 Slice-0 ERASURE PRIMITIVE inputs (HITL steer 2026-07-11, reframes the GDPR/purge question). KEY REFRAME: the erasure axis is PROVENANCE (source_id / excise_source), NOT the l:/h: id-space -> pin A is ORTHOGONAL to erasure and survives regardless. Slice-0 must ask 'is excise_source a COMPLETE erasure primitive, and is provenance MANDATORY?' with THREE concrete work-items (defects, not design debates): (1) ONE shared row-owned delete-set consumed by BOTH purge_inner and excise_source_inner -- the bug is two erasure verbs maintaining the target-table list INDEPENDENTLY, so the next projection table reopens the hole (per memory: excise_source misses the content-storing search_index_v2 => body survives erasure); the RED test asserts on RAW TABLE CONTENTS, not search results (leak is data-at-rest, never surfaces in results). (2) Fix ingest provenance -- use the caller's d.source_doc_id, NOT the model's echo (straight bug fix). (3) source_id MANDATORY (String, not Option) on PreparedWrite::Node/Edge -- this is where the breaking-changes lever pays, converting erase-by-provenance from best-effort to TOTAL; PLUS expose excise_source in the napi/py SDKs (else Memex has NO erasure capability at all). THE ONE HITL DECISION (not a fix): pre-change NULL-provenance rows get a BACKFILL or a documented DATED BOUNDARY. See memory [[erasure-axis-is-provenance-excise-source-gap]].", "decider": "hitl", "release": "0.8.20", "slice": "0", "refs": ["seq:22"]}
{"ts": "2026-07-12T00:00:00.000Z", "seq": 24, "kind": "decision", "voice": "FATHOM", "status": "RATIFIED", "summary": "TC-11 CLOSED + seq-23 CORRECTION (HITL 2026-07-12; master F-25). (1) TC-11 RATIFIED = pin A, TERMINAL-FOREVER: anonymous/doc-seeded nodes stay h:<content-hash> PERMANENTLY; structural-lifecycle-contract.md 2(ii) is OVERRULED; the anonymous-surrogate leg is CANCELLED, not deferred (a surrogate serves ONLY registry-admitted governed entities, decided at write time; a stored row's id-space is never re-derived). Accepted corollary: an anonymous row + a later governed row for the same thing BOTH stay active and BOTH surface in search (NULL never collides with the partial-unique index) -- the engine does not dedupe them. (2) CORRECTION TO seq 23 -- its work-item (3) said 'source_id mandatory + excise_source IN THE SDKs'. The excise_source-in-SDK half is WRONG and is RETRACTED: excise_source is the RECOVERY seam (REQ-026, built to excise a corrupt ingest) and is named in REQ-037 as SDK-forbidden. But 'CLI-only' is ALSO wrong as a resolution: NO CLI SHIPS to SDK consumers (the wheel declares no [project.scripts]; npm no 'bin'), so erasure is UNREACHABLE. Real policy = 'RECOVERY-NAMED verbs are CLI-only', NOT 'destructive => CLI-only' -- proof: purge(logical_id) is ALREADY an SDK verb (py:1297, napi:1102) despite being named in REQ-037, because AC-041 tests only the REQ-054 five-name denylist. RULING: excise_source stays CLI-only; ADD erase_source(source_id) as a first-class SDK LIFECYCLE verb (not a recovery name => AC-041 stays GREEN, denylist stays five), one shared engine path. REQ-037 amended with a lawful-erasure carve-out; purge_logical_id struck from its forbidden list (shipped code already contradicted it). (3) ERASURE DEFECT CLASS WIDENED: the row-owned projection table list is implicit and duplicated FIVE times, not two. search_index_v2 (content-storing FTS5, holds the body verbatim) is written by project_canonical_node_row:11806 and deleted ONLY by purge:6229 -- excise_source_inner:6398, rebuild_shadow_state:6515, and reproject_search_index_after_tokenizer_upgrade:9515 NEVER touch it => erased bodies survive on disk; the table accumulates every body ever written; and v1/v2 diverge after a tokenizer upgrade. Also: NO edge projector exists (edge projection is inlined in commit_batch) so projector-replay rebuild must cover edges; truncate_wal() EXISTS (:6379, typed Busy) but purge/excise never call it; telemetry retains l: ids post-erasure (:4462); op-store has no record-level delete (:10083 is a cap sweep). RED tests MUST assert on RAW TABLE CONTENTS -- both FTS read paths gate on canonical_nodes, so a test that SEARCHES for the excised text PASSES on the broken code. (4) plan-0.8.20.md DE-STALED + AUTHORED (dep-migration runbook removed -> owed to plan-0.8.22.md). Design of record: dev/design/0.8.20-erasure-and-h-end-state-v4.md (codex 9 rounds 1-3).", "refs": ["file:dev/plans/plan-0.8.20.md", "file:dev/design/0.8.20-erasure-and-h-end-state-v4.md", "file:dev/plans/0.8.6-0.8.16-PROGRAM-SEQUENCING.md#F-25", "seq:23"], "opp": "OPP-12", "cx": "TC-11"}
{"ts": "2026-07-19T03:35:38.066Z", "seq": 25, "kind": "observation", "summary": "eu7 is a CPU SAME-BACKEND gate: a GPU-run eu7 number is NOT comparable to the 0.90 floor and manufactures a FALSE REGRESSION. Verified at 0.8.20 Slice-0 from run artifacts: CPU (GA-signoff-eu7-remeasure-20260608T172804Z.json) n=7667 vector-stage recall@10 = 0.8960 CI [0.8640, 0.9250] => PASS on the one-sided gate (ci_hi >= 0.90). GPU (dev/plans/runs/0.8.14-slice-20-eu7-gpu-run-20260705T205222Z.log:73) n=7667 = 0.8330 CI [0.8010, 0.8640] => FAIL. The delta is a cross-backend quantization artifact, NOT a code regression (it is the TC-5 floor re-baseline driver, scheduled 0.8.23). CONSEQUENCE: the standing HITL GPU-eval mandate (repo-must-use-gpu-for-eval-when-3090-room) DOES NOT APPLY to the eu7 recall gate -- by that mandate's own 'fidelity-safe' caveat. Any release taking an eu7 basis decision (0.8.20 R-20-EU7 at Slice 40; TC-5 at 0.8.23) MUST capture and compare on CPU only, and must compare against a CPU reference. Running eu7 on the 3090s to 'honor the mandate' would wrongly force a re-baseline.", "cx": "TC-12", "release": "0.8.20", "slice": "0", "status": "OPEN", "refs": ["file:dev/design/0.8.20-slice0-erasure-design.md#8", "file:dev/plans/runs/STATUS-0.8.20.md#6", "seq:24"]}
{"ts": "2026-07-19T03:35:38.096Z", "seq": 26, "kind": "observation", "summary": "eu7 harness has TWO vacuous-green hazards; any release running eu7 must guard both. (1) THE DOCUMENTED RUN COMMAND DOES NOT WORK. src/rust/crates/fathomdb-engine/tests/eu7_real_corpus_ac.rs:85-86 documents 'AGENT_LONG=1 cargo test -p fathomdb-engine --features default-embedder --test eu7_real_corpus_ac'. That fails with EXIT 101: 'target eu7_real_corpus_ac in package fathomdb-engine requires the features: operator'. Working invocation: --features default-embedder,operator. Fix the docstring (0.8.20 Slice 5 owns it). (2) THE CORPUS IS UNREACHABLE FROM A LINKED WORKTREE. data/corpus-data/ is gitignored (.gitignore:9) and exists ONLY in the primary checkout (2.1 GB). From a worktree, load_subset_or_skip finds nothing, the test prints [skip] and returns EXIT 0 -- a full vacuous green having measured nothing. Workaround used at 0.8.20 Slice-0: symlink data/corpus-data from the primary checkout into the worktree and exclude it via .git/info/exclude (no tracked file changes). GUARD REQUIRED: assert a NON-SKIP WITNESS (e.g. the EU7_SETUP real_docs=N line) rather than trusting exit 0. Compounded by the known trap background-exit-masks-real-exit: a trailing echo around the cargo invocation reported exit 0 while the real exit was 101 -- this recurred live during Slice-0.", "cx": "TC-13", "release": "0.8.20", "slice": "0", "status": "OPEN", "refs": ["file:src/rust/crates/fathomdb-engine/tests/eu7_real_corpus_ac.rs", "file:dev/plans/runs/STATUS-0.8.20.md#6.2"]}
{"ts": "2026-07-19T03:36:01.033Z", "seq": 27, "kind": "caveat", "summary": "AC ID-SPACE ABOVE AC-076 IS RESERVED -- do not mint new ACs by taking 'max AC id + 1'. Verified at 0.8.20 Slice-0: the highest DEFINED, NON-RESERVED acceptance criterion is AC-076 (dev/acceptance.md:1147). AC-077 (:1286) is a RESERVED PLACEHOLDER, explicitly 'not yet a gate; no fabricated numbers', held for the IR-eval IR-1/IR-2 initiative (agentic IR / evidence-recall, maps to REQ-067, all thresholds TBD). AC-078 is CONDITIONALLY reserved to the SAME initiative -- :1297 reads '+ AC-078... only if the consensus splits the measure'. TRAP: a naive AC-036\nAC-037\nAC-019\nAC-077\nAC-077\nAC-075\nAC-076\nAC-075\nAC-077\nAC-078 returns AC-078 from that PROSE and will mislead an agent into minting a colliding id. plan-0.8.20.md section 3 states 'AC ceiling today = AC-077' and that new ACs 'continue from' it -- following that literally collides with a live reservation. 0.8.20 Slice-0 RECOMMENDS minting from AC-079 (HITL decision owed). Any future release minting ACs must read the reservation note at dev/acceptance.md:1286-1300 first, and per the locked-acceptance.md policy should prefer tracking by requirement id + TDD test name over minting per-feature ACs at all.", "cx": "TC-14", "release": "0.8.20", "slice": "0", "status": "OPEN", "refs": ["file:dev/acceptance.md", "file:dev/design/0.8.20-slice0-erasure-design.md#5"]}
{"ts": "2026-07-19T03:36:01.065Z", "seq": 28, "kind": "consideration", "summary": "THE ERASURE AUDIT TRAIL IS DESTRUCTIBLE -- refutes the v4 design of record. dev/design/0.8.20-erasure-and-h-end-state-v4.md section 3.6 asserts excise_source_audit 'retains source_id PERMANENTLY (:6479) -- by design; that is the audit trail', and plan-0.8.20.md section 8 repeats it as the justification for the non-PII source_id rule. VERIFIED FALSE at 4ca70ba6: the audit row is written into operational_mutations (engine lib.rs:6479), and enforce_provenance_retention (:10070) sweeps THAT SAME TABLE cap-first, oldest-id-first, with NO COLLECTION FILTER (:10083-10089: DELETE FROM operational_mutations WHERE id IN (SELECT id FROM operational_mutations ORDER BY id LIMIT ?1)). Erasure audit rows are swept like any other op-store row. TWO CONSEQUENCES: (1) ACCOUNTABILITY GAP -- demonstrating THAT an erasure occurred is a distinct obligation from performing it; a retention sweep can silently destroy the proof. (2) COLLISION WITH WORK ITEM 9 -- excise_collection_record operates on the op-store, the SAME table the audit rows live in, so the erasure record and the payloads being erased share one retention pool. The non-PII source_id rule STILL STANDS (an unswept audit row may still hold it) but its stated rationale is argued from a false premise and must be corrected. RECOMMENDED (HITL, decision owed): exempt the erasure-audit collection from enforce_provenance_retention by filtering the sweep by collection, and state the audit-durability guarantee explicitly. This is a RETENTION-POLICY change, not a pure defect repair, hence HITL-gated.", "cx": "TC-15", "release": "0.8.20", "slice": "5", "status": "OPEN", "refs": ["file:dev/design/0.8.20-slice0-erasure-design.md#2", "seq:24"]}
{"ts": "2026-07-19T03:44:59.060Z", "seq": 29, "kind": "todo", "summary": "THE PUBLISH DRY-RUN GUARD IS DEAD AND HAS BEEN RED SINCE 0.8.14 -- fix before the first real publish. scripts/tests/test_actionlint_fixture.sh:53-56 asserts that every publish-rust-<tier> job block in .github/workflows/release.yml contains the literal 'cargo publish --dry-run -p '. It FAILS on the FIRST tier (t1-embedder-api), which makes ./scripts/agent-test.sh exit 1 wholesale. dev/plans/runs/0.8.14-slice-40-output.json:23 already recorded this as 'the only remaining agent-verify red' -- it has now been carried unfixed across 0.8.14/0.8.16/0.8.18/0.8.19 into 0.8.20. DIAGNOSIS (verified at 4ca70ba6): the WORKFLOW IS FINE, THE TEST IS STALE. release.yml:248 no longer inlines the cargo command; it delegates to 'bash scripts/release/cargo-publish-if-new.sh --dry-run fathomdb-embedder-api', and that helper does forward to 'cargo publish --dry-run -p <crate>' (cargo-publish-if-new.sh:14). So publish BEHAVIOR is intact and this is not a publish defect. THE REAL HARM IS THE MASKED GATE: a permanently-red assertion is a gate nobody reads, so any GENUINE publish-wiring regression would now be invisible -- and 0.8.20 is the FIRST REAL PUBLISH (manifests 0.8.9 -> 0.8.20, cargo order embedder -> engine, and a pushed v* tag auto-fires REAL crates/PyPI/npm). Same class as perf-recall-gates-masked. FIX (Slice 40, R-20-PUB): re-point the assertion at the delegation -- assert the job invokes cargo-publish-if-new.sh with --dry-run AND that the helper forwards to cargo publish --dry-run -p -- so the guard is live again, then confirm ./scripts/agent-test.sh is GREEN before the publish gate.", "cx": "TC-16", "release": "0.8.20", "slice": "40", "status": "OPEN", "refs": ["file:scripts/tests/test_actionlint_fixture.sh", "file:.github/workflows/release.yml", "file:dev/plans/runs/0.8.14-slice-40-output.json"]}
{"ts": "2026-07-19T03:45:24.246Z", "seq": 30, "kind": "caveat", "summary": "SLICE 5's SourceId NEWTYPE WILL BREAK THE eu7 HARNESS (and every other in-tree PreparedWrite construction) -- budget for it. R-20-E3 / v4 section 3.2 replaces 'source_id: Option<String>' with a mandatory SourceId newtype on the public PreparedWrite, making 'no provenance' INEXPRESSIBLE. That is the point of the change -- but it means EVERY existing in-tree construction of PreparedWrite with 'source_id: None' stops compiling. CONFIRMED INSTANCE: src/rust/crates/fathomdb-engine/tests/eu7_real_corpus_ac.rs:405 seeds the real-corpus haystack with 'source_id: None, logical_id: None'. eu7 is the ANN-fidelity gate feeding R-20-EU7 at Slice 40, so if Slice 5 lands the newtype without updating this harness, the Slice-40 eu7 basis decision cannot even be measured -- the test will not build. v4 section 3.2 only anticipated 'two internal callers (:5186, :5209, the doc(hidden) poison fixture)'; the TEST-side callers were not enumerated. ACTION for Slice 5: sweep ALL constructions of PreparedWrite across src/ AND tests/ (not just the two v4 names), give each a real SourceId, and re-verify the eu7 harness still BUILDS before landing. Cheap to do with the change, expensive to discover at Slice 40.", "cx": "TC-17", "release": "0.8.20", "slice": "5", "status": "OPEN", "refs": ["file:src/rust/crates/fathomdb-engine/tests/eu7_real_corpus_ac.rs", "file:dev/design/0.8.20-erasure-and-h-end-state-v4.md#3.2"]}
{"ts": "2026-07-19T03:45:24.280Z", "seq": 31, "kind": "observation", "summary": "PROCESS: committing a codex transcript into the reviewed range pollutes the NEXT review's diff. TC-RUBRIC-7 requires persisting every codex section-9 transcript to a durable release-namespaced path. At 0.8.20 Slice-0 the transcript was committed to dev/plans/runs/codex/0.8.20/ and the fix-1 RE-review was then run with the same '--base 4ca70ba6'. Because the base was unchanged, the re-review's diff INCLUDED the previously-committed transcript file, so codex re-read its own prior findings as though they were part of the change under review -- the old [P2] finding text reappeared verbatim in the new transcript, which is easy to misread as 'the finding was not fixed'. It was fixed (verified independently: pre-fix exit 128, post-fix exit 0 under codex's own reproduction), and codex's actual terminal verdict was PASS. MITIGATIONS (pick one, for every later slice): (a) re-review against a base that already contains the prior transcript, (b) persist transcripts on a path excluded from the review scope, or (c) commit transcripts only AFTER the slice's final review round. Recommend (c) as the simplest: it keeps the reviewed diff to real code+docs and still satisfies TC-RUBRIC-7 at slice close. Also note the transcript path was pinned as .log (not .md) to match the existing 0.8.16-slice-*-codex-review-*.log convention and stay out of markdownlint scope.", "cx": "TC-18", "release": "0.8.20", "slice": "0", "status": "OPEN", "refs": ["file:dev/plans/runs/STATUS-0.8.20.md#5.1"]}
{"ts": "2026-07-19T04:09:21.079Z", "seq": 32, "kind": "observation", "summary": "ROOT CAUSE: the eu7 harness CANNOT COMPLETE ON CPU on this box -- BATCH=256 exceeds its own hardcoded 600s drain timeout at the measured embed rate. Measured at 4ca70ba6 by bisecting N (three runs): n=7667 FAILED, n=200 FAILED, n=20 PASSED. All three failures are identical: panic at eu7_real_corpus_ac.rs:414 'seed drain (batch): Scheduler' == engine.drain(600_000) -> wait_for_idle timed out -> EngineError::Scheduler. THE WORKER IS NOT WEDGED: n=20 passed cleanly (exit 0, vector_stage recall@10 = 1.0000, 258.54s) so the embed/projection path is FUNCTIONALLY CORRECT. It is THROUGHPUT. The n=20 run reports seed_ms=111670 for 20 docs = 0.179 docs/sec, which is ~7.3x SLOWER than the 1.3 docs/sec the harness docstring assumes (:97-99). At 0.179 docs/s a BATCH of 256 needs ~1430s, so it can NEVER drain inside the hardcoded 600_000 ms -- the harness is STRUCTURALLY unable to run here regardless of N, because failure happens on the FIRST batch. Full 7667-doc seed would need ~11.9 HOURS even if the timeout were raised. NOT a weights problem (cache complete: config.json + tokenizer.json + model.safetensors present) and not CPU contention (load 4.5 of 24 cores). OPTIONS for Slice 40: (a) reduce BATCH 256 -> 64 (64/0.179 = 358s, fits inside 600s) or make BATCH/timeout env-tunable -- minimal, surgical, does NOT change measurement semantics; (b) raise the drain timeout and accept a ~12h CPU run; (c) investigate the 7x throughput shortfall vs the documented rate, which may itself be a real CPU-embed regression worth catching. RECOMMEND (a)+(c). NOTE THE TENSION: TC-12 forbids running eu7 on GPU for comparability, and CPU cannot finish -- so R-20-EU7 has NO runnable path until this is fixed. Slice 40 must not treat this as optional. SIDE-EFFECT HAZARD: the harness WRITES dev/plans/runs/eu7-latest-measurements.json into the repo on every run, so a reduced-N scouting run silently produces a file that LOOKS authoritative (the n=20 run wrote recall=1.0000 there). Do not commit it from a scouting run.", "cx": "TC-19", "release": "0.8.20", "slice": "40", "status": "OPEN", "refs": ["file:src/rust/crates/fathomdb-engine/tests/eu7_real_corpus_ac.rs", "seq:26"]}
{"ts": "2026-07-19T23:49:04.707Z", "seq": 33, "kind": "observation", "summary": "R-20-EU7 is CLOSED BY HITL (zero runs authorized) but the prohibition is UNENFORCEABLE: eu7_real_corpus_ac has no #[ignore] and no env gate, so a bare 'cargo test --workspace' or 'cargo test -p fathomdb-engine --features operator' WILL execute it. Slice 5 complied only by running targets individually. Per fix-the-tooling-not-the-actor this wants a mechanical guard -- but a naive #[ignore] creates the OPPOSITE hazard at Slice 40, where eu7 IS wanted and would then silently never run (TC-13 vacuous-green class). Needs a Steward/HITL decision on the guard's shape, not a unilateral fix.", "id": "TC-20", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:00b46b84"]}
{"ts": "2026-07-19T23:49:04.735Z", "seq": 34, "kind": "observation", "summary": "PRE-EXISTING (not 0.8.20): pr_g10_reranker_ce has NOT COMPILED under --features default-reranker since 0.8.19's SearchHit.id u64->IdSpace swap; file verified byte-identical to baseline, so this is not a Slice-5 regression. Root cause of its survival: the release DoD full-workspace gate (cargo clippy/check --workspace --all-targets) does NOT cover feature-gated targets, so default-reranker targets are invisible to it. Two gaps: fix the compile break, and decide whether the DoD gate must fan out over feature combinations.", "id": "TC-21", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:00b46b84"]}
{"ts": "2026-07-19T23:49:04.758Z", "seq": 35, "kind": "caveat", "summary": "STRUCTURAL: a BREAKING change to the write path cannot be X1-verified from an orchestration worktree. maturin develop / pip install -e are prohibited there (they rebind the shared .venv), so the Python native extension cannot be built or executed -- Slice 5c's SourceId newtype broke ~50 Python fixtures that could only be statically checked (py_compile + ruff + AST audit), never run. TypeScript is NOT subject to this: 'npm install' in src/ts is directory-local, creates no shared state, and gave a real tsc + node --test green (170 passing). Rule of thumb for future breaking slices: TS is worktree-verifiable, Python is NOT, and a main-tree pytest run is owed before land. 0.8.19 already taught that an env trap is a landing blocker, not a follow-up.", "id": "TC-22", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:875017a2"]}
{"ts": "2026-07-19T23:49:04.781Z", "seq": 36, "kind": "observation", "summary": "TOOLING: implementer closure output.json artifacts left UNTRACKED (orchestration.md section 8 permits this, orchestrator promotes later) are destroyed by routine git hygiene. Happened TWICE in Slice 5: one implementer ran 'git checkout --' on uncommitted lib.rs and lost a whole implementation; another ran 'git stash push -u' + 'git stash drop' and swept away three prior sub-slices' untracked output.json (recovered byte-identical from the dropped stash's third parent, verified via git hash-object). Fix the tooling: have implementers COMMIT output.json as their final commit rather than leaving it untracked, so the closure witness is not destructible by ordinary git commands.", "id": "TC-23", "release": "0.8.20", "slice": "5", "status": "open"}
{"ts": "2026-07-19T23:49:04.804Z", "seq": 37, "kind": "caveat", "summary": "BEHAVIOR CHANGE to a shipped knob: enforce_provenance_retention's 'cap' now bounds SWEEPABLE rows, not physical rows in operational_mutations. Slice 5 exempts two collections from the sweep -- the erasure-audit collection (per HITL ruling 'there must be an auditable record of deletion event', defect D-A) and the new pending-redaction intent queue (sweeping it would re-create the P2 false-success defect via cap pressure). Consequence: an operator who sized 'cap' against physical row count will see the table grow beyond it by the exempt collections. Deliberate, but it needs a changelog line and any operator-facing retention docs updated.", "id": "TC-24", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:18197495"]}
{"ts": "2026-07-19T23:49:04.827Z", "seq": 38, "kind": "observation", "summary": "VACUOUS-GREEN HAZARD: src/rust/crates/fathomdb/tests/sdk_only_erasure.rs is '#![cfg(not(feature = \"operator\"))]' by design -- it proves an SDK-ONLY consumer with no CLI/operator surface can erase (R-20-E4). Consequence: any feature-unified run (cargo test --workspace, or anything enabling 'operator') compiles it to ZERO tests and reports success having asserted nothing. It requires the explicit invocation 'cargo test -p fathomdb --test sdk_only_erasure'. CI must carry that invocation explicitly or the R-20-E4 guarantee is untested.", "id": "TC-25", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:4b78658d"]}
{"ts": "2026-07-19T23:49:04.851Z", "seq": 39, "kind": "todo", "summary": "DESIGN TEXT CORRECTION OWED: plan-0.8.20 R-20-E8 and the v4/v5 designs state the legacy provenance backfill is gated on 'logical_id IS NULL ONLY', rationale being that governed rows stay purge-addressable by logical_id. codex section 9 proved that rationale is TRUE FOR NODES and FALSE FOR EDGES -- purge_inner resolves targets exclusively via 'SELECT state FROM canonical_nodes WHERE logical_id = ?1', so an edge's logical_id is only a supersession identity and confers NO purge-addressability. A legacy edge with source_id NULL and logical_id NOT NULL was therefore erasable by no verb at all. Shipped fix (step 21): nodes keep the logical_id IS NULL gate; edges backfill on source_id IS NULL alone. TC-11 pin NOT affected (only source_id is written, never logical_id). The design/plan prose still states the unqualified rule and must be corrected.", "id": "TC-26", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:989fd7ef"]}
{"ts": "2026-07-20T00:51:27.551Z", "seq": 40, "kind": "observation", "summary": "HARD-RULE TRAP: 'maturin develop' can fire AUTONOMOUSLY from a worktree without any agent issuing it. src/python/tests/conftest.py::_ensure_test_hooks_binding (line ~118) shells out to 'maturin develop' to build the test-hooks extension. So merely running the Python suite from an orchestration worktree attempts the one thing the build-isolation rule forbids (it rebinds the SHARED .venv). Observed live in Slice 5 fix-3. It failed harmlessly ONLY because the interpreter was not an activated virtualenv (conftest guards on that); with an activated venv it would have rebound the shared .venv to the worktree. Orchestrator verified no damage: /home/coreyt/projects/fathomdb/.venv/.../fathomdb.pth mtime still 2026-07-09, still pointing at the MAIN repo. Per fix-the-tooling-not-the-actor, conftest needs an explicit guard that refuses to run maturin when CWD is a linked worktree.", "id": "TC-27", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:8e09b950"]}
{"ts": "2026-07-20T00:51:27.581Z", "seq": 41, "kind": "observation", "summary": "UPDATE (raised on THREE consecutive codex rounds in Slice 5): the eu7 no-run prohibition is still unenforceable, and scripts/agent-test.sh contains a bare 'cargo test --workspace' that WOULD execute eu7_real_corpus_ac. Slice 5 complied only by every implementer remembering to pass '-- --skip eu7_real_corpus_ac_validation' or to run targets individually. Original entry stands: the guard's shape needs a Steward/HITL decision because a naive #[ignore] creates the opposite (vacuous-green) hazard at Slice 40 where eu7 IS wanted.", "id": "TC-20", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:8e09b950"]}
{"ts": "2026-07-20T00:51:27.604Z", "seq": 42, "kind": "caveat", "summary": "DESIGN HARDENING OWED (FU-FIX3-3): the pending-redaction queue introduced in Slice 5 is a transactional outbox stored as ordinary operational_mutations rows (collection_name='erasure_pending_redaction'). Its 'a row is removed ONLY when the obligation is discharged' invariant is upheld by THREE separate correct call sites (retention sweep exemption, excise_collection_record refusal, the NotFound arm) rather than enforced structurally -- and it shipped with three ways to remove a row of which only one proved anything. Every known path is now closed, but NOTHING PREVENTS A FIFTH. Codex found a defect in this mechanism on each of rounds 1, 2 and 3. Structural options: give it its own table with no generic DELETE verb, or a trigger. Deliberately NOT done inside a fix round.", "id": "TC-28", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:8e09b950"]}
{"ts": "2026-07-20T00:51:27.627Z", "seq": 43, "kind": "observation", "summary": "RESIDUAL PATTERN (FU-FIX2-2): 'freeze the projection scanner, then wait on a DATABASE-derived idleness probe' is the shape behind a Slice-5 P2 where erase_source/excise_source could strand unqueued work and time out with EngineError::Scheduler after the full 30s LIFECYCLE_DRAIN_TIMEOUT_MS. purge and (now) the source-erasure path drain BEFORE freezing. run_rebuild is the LAST remaining instance that still freezes before its only drain -- likely benign (operator action, not a write-then-act sequence) but unaudited. Also logged: operator_cli::t_s34_dump_mutations_lock_held_exits_71 is FLAKY under cross-binary lock contention (it spawns its own lock holder); 25/25 in isolation, flaked once in a full-suite run. Touches no erasure path.", "id": "TC-29", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:7be20ec3"]}
{"ts": "2026-07-20T01:09:57.581Z", "seq": 44, "kind": "observation", "summary": "VACUOUS-GREEN HAZARD (found closing Slice 5): scripts/agent-lint-md.sh exits 0 SILENTLY from any linked worktree because node_modules/.bin/markdownlint-cli2 is absent there and the structural lint self-skips -- same failure class as TC-13 (eu7 corpus skip) and TC-25 (cfg-gated target compiles to zero tests). Running the real linter (npx --yes markdownlint-cli2) gives exit 1: 9 PRE-EXISTING MD025/MD001 issues in dev/research/personal-agent-database-market-2026-07-02.md, untouched since c366d6e0 (2026-07-02). Two implications: (a) the markdown gate is unreliable from every orchestration worktree, and (b) agent-lint-md.sh is likely RED in the main tree right now on that research file, independent of 0.8.20. Note also STATUS boards sit inside the config's dev/plans/runs/** ignore and are never linted.", "id": "TC-30", "release": "0.8.20", "slice": "5", "status": "open"}
{"ts": "2026-07-20T02:14:53.190Z", "seq": 45, "kind": "observation", "summary": "WRITE/READ PROVENANCE ASYMMETRY -- the 'NO SDK EXPOSURE' erasure gap, now measured. 0.8.20 makes provenance structurally MANDATORY on write (SourceId newtype), but PySearchHit.source_id is populated ONLY for graph-arm hits and is None for every two-arm (text/vector) hit -- documented at src/rust/crates/fathomdb-py/src/lib.rs:537-539. So a caller can be forced to supply provenance yet cannot read it back on an ordinary search hit. Measured consequence: BOTH remaining Python suite failures (test_option2_elps_pipeline::test_build_fathomdb_elps_path_uses_ingest_with_extractor and test_verify_embed_db::test_complete_embed_is_queryable) are caused by this -- _doc_id_of falls through the 'if sid:' branch because source_id is None, reaching int(sh.id) which has raised TypeError since 0.8.19 made SearchHit.id an IdSpace (C-2). ONE read-side fix (expose source_id on all arms) likely closes BOTH. Verified pre-existing: identical failures on origin/main f22e4947 in an isolated A/B clone; _doc_id_of is byte-identical on main.", "id": "TC-31", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:9c87d758"]}
{"ts": "2026-07-20T02:14:53.219Z", "seq": 46, "kind": "observation", "summary": "ERASURE GAP adjacent to R-20-E2 (found during fix-4, NOT fixed -- entity-identity design, out of slice scope): ingest_with_extractor dedupes entities within a batch by logical_id derived from (kind, name). Two documents naming the SAME entity collapse to ONE canonical row carrying the FIRST document's provenance. Consequence for erasure completeness: erasing the second document leaves that entity row behind, still attributed to the first document -- the row survives an erasure that should have removed the second document's contribution. Visible in the ELPS stub path (both docs yield 'TestEntity' => 1 node). Interacts with the multi-doc per-entity attribution contract added in 0.8.20: attribution is now required per entity, but dedup silently discards all but the first attribution.", "id": "TC-32", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:9c87d758"]}
{"ts": "2026-07-20T02:14:53.242Z", "seq": 47, "kind": "observation", "summary": "UPDATE -- GUARD SHIPPED, and the hazard was confirmed LIVE not hypothetical. src/python/tests/conftest.py now REFUSES to shell out to 'maturin develop' unless FATHOMDB_TESTS_ALLOW_REBUILD=1 is set (0.8.20 Slice 5, commit 265c54c0). During verification the probe showed the orchestration worktree's binding LACKS test-hooks, meaning the pre-guard code WOULD have rebuilt from that worktree into the shared .venv given an activated virtualenv. Downstream effect to know about: scripts/agent-test.sh's pytest step now FAILS LOUDLY instead of silently auto-rebuilding -- any isolated/throwaway verification environment must set FATHOMDB_TESTS_ALLOW_REBUILD=1 explicitly (the fresh-clone verification run did exactly this).", "id": "TC-27", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:265c54c0"]}
{"ts": "2026-07-20T03:11:17.565Z", "seq": 48, "kind": "observation", "summary": "RESOLVED and REWORKED in fix-6 -- SUPERSEDES the seq-47 update, which is now STALE (it said agent-test.sh 'FAILS LOUDLY' and told verification runners to export FATHOMDB_TESTS_ALLOW_REBUILD; both no longer accurate). codex flagged the fix-4 env-var guard as a P2: it turned a silent-rebuild hazard into a PERMANENTLY RED default pytest path (a clean 'pip install -e src/python[dev]' checkout raised at import time before collection). fix-6 restates TC-27 POSITIVELY: the policy is a pure function (src/python/tests/_test_hooks_gate.py) returning PROCEED/REBUILD/DEGRADED/CONTRADICTORY, and the load-bearing check is venv_belongs_to_source_tree() -- maturin develop may run ONLY when the venv prefix lies inside the repo root. The opt-in env var CANNOT override the ownership check, so the shared .venv is protected structurally rather than by an env var. agent-test.sh now sets the opt-in itself when it selected the in-tree .venv. Verified by execution in an isolated clone: default path collects (775 items, no raise); ownership check returns False for the shared venv and for a worktree venv and degrades instead of rebuilding; degraded mode skips EXACTLY the two hook-dependent tests with a visible banner (not a session-wide self-skip).", "id": "TC-27", "release": "0.8.20", "slice": "5", "status": "resolved", "refs": ["git:d710721a"]}
{"ts": "2026-07-20T03:11:17.593Z", "seq": 49, "kind": "observation", "summary": "CORRECTION to TC-16's attribution, found by isolated-clone verification. The dead publish dry-run assertion that makes scripts/agent-test.sh exit 1 is NOT in test_pypi_publish_roundtrip.sh (that script passes cleanly). It is in scripts/tests/test_actionlint_fixture.sh, invoked at agent-test.sh line 63: 'FAIL publish-rust-t1-embedder-api dry-run branch is not cargo publish --dry-run -p'. Confirmed pre-existing -- both that script and .github/workflows/release.yml are byte-identical to origin/main. CONSEQUENCE THAT MATTERS: because the failure is at line 63 and agent-test.sh runs under 'set -euo pipefail', the script ABORTS THERE and NEVER REACHES the Rust or Python steps at all. So agent-test.sh's aggregate exit code tells you NOTHING about whether the test suites pass -- any agent reading it as a suite signal is reading a vacuous result. Run the steps individually until this is fixed.", "id": "TC-16", "release": "0.8.20", "slice": "5", "status": "open", "refs": ["git:d710721a"]}
{"ts": "2026-07-20T18:15:47.518Z", "seq": 50, "kind": "observation", "summary": "TC-31 RESOLVED — source_id readable on every search-hit path (Py+TS), erase_source round-trip closed", "id": "TC-31", "release": "0.8.20", "slice": "10", "status": "resolved", "refs": ["git:93a57b10", "plan:dev/plans/plan-0.8.20.md"], "body": "Engine populated source_id at the 5 caller-reachable SearchHit sites (vector node, vector edge_fact, text/BM25F, pre-step-12 legacy text tiered, edge FTS) by adding one column to an existing SELECT/JOIN — no N+1 (there is no index on canonical_nodes(write_cursor)). Node hits carry canonical_nodes.source_id; edge hits carry canonical_edges.source_id; graph-arm semantics unchanged. rerank_passages left None deliberately: pure fn, no DB handle, hit never escapes to a caller — populating would fabricate provenance. Verified by execution in fresh clones: Python 787 passed/12 skipped, TS 186/186. codex §9 terminal PASS."}
{"ts": "2026-07-20T18:15:47.545Z", "seq": 51, "kind": "caveat", "summary": "TC-32 entity dedupe: HITL-ACCEPTED as-is; erasure guarantee must NOT be stated unconditionally to users", "id": "TC-32", "release": "0.8.20", "slice": "10", "status": "accepted", "refs": ["git:93a57b10"], "body": "HITL ruling 2026-07-20: same-name entity dedupe collapsing onto one logical_id carrying the FIRST document's provenance is INTENTIONAL and ACCEPTED; annotate only, no behavior change, no multi-source-provenance API, no further analysis (explicitly time-boxed). Annotated at dedup_prepared_by_logical_id + derive_logical_id in engine/src/lib.rs and in the design doc. CARRY-FORWARD CAVEAT: the erasure guarantee 'erase every row owned by this source' does NOT hold for a co-named entity spanning documents, so user-facing docs and any GDPR/erasure claim must stay conditional until TC-32 is revisited."}
{"ts": "2026-07-20T18:15:47.568Z", "seq": 52, "kind": "open_question", "summary": "Node validity is INTEGER epoch while edge validity is ISO-8601 TEXT — temporal model now internally inconsistent", "id": "TC-33", "release": "0.8.20", "slice": "10", "status": "open", "refs": ["git:93a57b10"], "body": "R-20-NV shipped canonical_nodes.valid_from/valid_until as INTEGER epoch seconds (sargable, and lets the instant be a BOUND :now parameter — the seam that makes validity deterministically testable). canonical_edges.t_valid/t_invalid remain ISO-8601 TEXT compared via datetime(), and the edge path still inlines datetime('now'). Edges deliberately untouched (unifying would re-litigate shipped semantics or force a data rewrite); divergence pinned by s22_edge_temporal_columns_are_untouched_text + r_20_nv_edge_temporal_filter_is_unchanged. HITL decision owed: accept the node/edge split long-term, or schedule a slice unifying edges onto INTEGER."}
{"ts": "2026-07-20T18:16:10.764Z", "seq": 53, "kind": "todo", "summary": "Node validity has NO write-side authoring verb — queryable but not settable from any SDK", "id": "TC-34", "release": "0.8.20", "slice": "10", "status": "open", "refs": ["git:93a57b10"], "body": "R-20-NV as written specifies columns + read-side selector (valid_as_of) + crossed_boundary_since hook, but NOT a write verb, so Slice 10 shipped none. Consequence: a consumer can QUERY by validity window but cannot SET one — validity is not end-to-end usable from Python/TS/Rust. Tests author windows via direct SQL on a closed DB. This is the single most likely follow-up. HITL decision owed: is R-20-NV considered met without it, or does a write verb land before 0.8.20 publishes? Note a write verb GROWS the governed surface and would need to join the AC-079 delta."}
{"ts": "2026-07-20T18:16:10.792Z", "seq": 54, "kind": "observation", "summary": "napi #[napi(object)] Option::None OMITS the property — it is not null; ToNapiValue governs bare returns only", "id": "TC-35", "release": "0.8.20", "slice": "10", "status": "open", "refs": ["git:9a6e4896"], "body": "MEASURED, after two wrong theory-driven type claims in one slice. napi-derive-backend 1.0.75 codegen/struct.rs obj_field_setters emits 'if field_.is_some() { obj.set(..) }' for use_nullable==false — None SKIPS the set, so the JS property is ABSENT (not null, not present-and-undefined). The else-branch emitting Null only exists under use_nullable=true, which appears ZERO times in fathomdb-napi. napi 2.16.17 ToNapiValue for Option<T> DOES map None->null, but #[napi(object)] fields never reach that impl (it governs bare returns / nested positions) — conflating the two is the trap. Convention in this repo: the public TS layer MANUFACTURES null via '?? null' (index.ts ceScore); a bare passthrough leaks undefined. Any future #[napi(object)] Option field must normalize with '?? null' or declare the field optional. STALE PROSE NOT FIXED: Rust doc comments on SearchHit.ce_score (fathomdb-napi/src/lib.rs:540-541) and PerHitExplain still say 'null' where native truth is 'absent'."}
{"ts": "2026-07-20T18:16:10.818Z", "seq": 55, "kind": "todo", "summary": "Published API docs still declare SearchHit id as int/number 'write_cursor' — stale since 0.8.19 C-2 IdSpace retype", "id": "TC-36", "release": "0.8.20", "slice": "10", "status": "open", "refs": ["git:93a57b10"], "body": "docs/reference/python-api.md:380 and docs/reference/typescript-api.md:348 declare 'id: int' / 'id: number' with the comment 'canonical row write_cursor (interim identity carrier)'. Stale since 0.8.19 C-2 retyped SearchHit.id to IdSpace and stopped surfacing write_cursor to the SDKs entirely. Found during the TC-31 doc sweep and deliberately left out of scope (different staleness class). Small doc-only fix; no owning slice. Related: the same sweep found the docs stated source_id was graph-arm-only, which was the user-facing face of TC-31."}
{"ts": "2026-07-20T18:26:27.550Z", "seq": 56, "kind": "observation", "summary": "scripts/agent-lint-md.sh exits 0 VACUOUSLY in any worktree — self-skips both checks when node_modules/lychee absent", "id": "TC-37", "release": "0.8.20", "slice": "10", "status": "open", "refs": ["git:9d387ede"], "body": "MEASURED: in a linked worktree there is no node_modules/.bin/markdownlint-cli2 and lychee is not on PATH, so agent-lint-md.sh takes its skip_notice branches (lines 12-15 and 34-41) for BOTH checks and exits 0 having linted NOTHING. An orchestrator/implementer that reports 'md-lint exit 0' from a worktree is reporting a vacuous green — this actually happened in Slice 10 and I passed it into a report before catching it. Same class as the known agent-test.sh TC-16 early-abort trap. Real result when run with the primary checkout's binary: plan-0.8.20.md 0 errors; STATUS-0.8.20.md is OUT OF SCOPE by config (.markdownlint-cli2.jsonc ignores dev/plans/runs/**); exit 1 comes from 9 PRE-EXISTING MD025/MD001 errors in dev/research/personal-agent-database-market-2026-07-02.md, a file untouched by this slice. FIX THE TOOLING: agent-lint-md.sh should exit non-zero (or loudly WARN) when it skips every check, so absence-of-tooling cannot masquerade as pass."}
{"ts": "2026-07-20T20:08:58.363Z", "seq": 57, "kind": "observation", "summary": "search is NOT one of the five read verbs — every ReadView visibility axis must be separately applied to Engine::search or it leaks", "id": "TC-38", "release": "0.8.20", "slice": "15", "status": "open", "refs": ["git:ab790880"], "body": "MEASURED by codex §9 on Slice 15b. Slice 10 scoped ReadView to FIVE read verbs (read_get, read_get_many, read_list, read_list_filter, graph_neighbors). Engine::search is NOT among them: read_search_in_tx hydrates hits from search_index/vector rows using ONLY 'superseded_at IS NULL AND state = active', with no validity predicate. This was LATENT and unreachable while no SDK caller could author a validity window; TC-34 (Slice 15b) made authoring reachable from Py/TS and thereby turned it into a LIVE defect — an SDK-authored out-of-window node still came back from search while read_get/read_list correctly hid it. Note dev/design/record-lifecycle-protocol/api-surface.md:50 always specified ReadView on 'search' too, so the five-verb scope was a narrowing, not the contract. THE DURABLE LESSON, which outlives this fix: the five-read-verb ReadView plumbing and the search path are SEPARATE code paths, so ANY future visibility axis (a new relax-flag, a new lifecycle state, a new temporal dimension) added to ReadView will silently NOT apply to search unless separately wired. Slices 20 and 25 and any later release adding a visibility axis must check both. A search-based test CANNOT catch this class — assert on the read verbs AND on search."}
{"ts": "2026-07-20T20:08:58.387Z", "seq": 58, "kind": "todo", "summary": "AGENTS.md:25 mandates a dev/interfaces/* or ADR update in the same PR for any pub-Rust/SDK contract change — implementers keep missing it; DOC-INDEX rows are also incomplete", "id": "TC-39", "release": "0.8.20", "slice": "15", "status": "open", "refs": ["git:ab790880"], "body": "codex §9 fired a [P2] on Slice 15b for shipping a public PreparedWrite::Node contract change plus new Py/TS write-item keys with NO dev/interfaces/* update. The rule is verbatim at AGENTS.md:25: 'Public surface is contract. Anything in dev/interfaces/ or pub Rust APIs is a contract; changes need an ADR or interface-doc update in the same PR.' It is a DISJUNCTION — an interface-doc update alone satisfies it; no ADR is separately required (searched AGENTS.md, CLAUDE.md, dev/interfaces/README.md, plan-0.8.20.md for an overriding convention; none found). TWO DURABLE ITEMS. (1) This is a recurring miss, not a one-off — the natural fix-the-tooling answer is a pre-commit/CI check that fails when a diff touches a pub engine type or a binding surface without touching dev/interfaces/*. Worth considering before more of 0.8.20's net-new surface lands unminuted. (2) MEASURED GAP: DOC-INDEX does NOT uniformly track dev/interfaces/* — only rust.md and cli.md had rows; python.md, typescript.md, wire.md and README.md had NONE. Slice 15b fix-1 added rows for python.md and typescript.md and refreshed rust.md, but DELIBERATELY left wire.md and README.md unrowed. DOC-INDEX's own header rule says a stale-or-missing row is an X3 gap that FAILS Slice 40 gate m — so wire.md and README.md are outstanding gate-m debt for 0.8.20 Slice 40 and are NOT attributable to any slice that touched them."}
{"ts": "2026-07-20T20:09:31.141Z", "seq": 59, "kind": "consideration", "summary": "R-20-PR registry shape: plan's roles:{filterable,rankable,searchable} as three booleans CANNOT express the ratified C-1 contract — must be a SET of typed roles", "id": "TC-40", "release": "0.8.20", "slice": "15", "status": "open", "refs": ["plan:dev/design/record-lifecycle-protocol/OPP-12-C1-converged-contract.md"], "body": "ORCHESTRATOR DESIGN FINDING, code-grounded, for whoever builds R-20-PR/R-20-EAV. plan-0.8.20.md §1/§3 summarises the registry as 'ProjectionSpec {name, roles: {filterable, rankable, searchable}}'. Read as three booleans that is UNBUILDABLE against the RATIFIED C-1 contract, for three independent reasons. (1) C-1 Q4's whole tiering mechanism is 'declare the cheap roles now, ADD the vector role at promote(), drop on reject' — that REQUIRES searchable->FTS and searchable->vector to be SEPARATELY declarable; one 'searchable' boolean cannot express it. (2) The staleness contract (projection-registry-and-async-embed.md §3) splits them by guarantee: filterable + searchable->FTS are SAME-TRANSACTION, searchable->vector is ASYNC carrying dense_readiness. (3) Slice 20 (R-20-DR, dense_readiness + atomic flip) attaches ONLY to searchable->vector — a boolean gives Slice 20 nothing to attach to. C-1 Q1 also fixes the wire shape: Memex ALREADY LANDED 'AttributeSpec.index: Set[ProjectionRole]' inert at memex 95ed450, so the engine must accept a SET or the ratified cross-repo seam does not connect. Recommended: enum ProjectionRole { Filterable, SearchableFts{tokenizer:Option<String>}, SearchableVector{embedder:Option<String>}, Rankable } with roles as a set; absent tokenizer/embedder => engine default (C-1 'Tokenizer/embedder defaults' section). This matters beyond Slice 15 because Slice 30's RUBRIC-H7 can-i-deploy gate mechanically checks as-built code against OPP-12-C1-converged-contract.md — a three-boolean shape would FAIL Slice 30 and HOLD the breaking pair."}
{"ts": "2026-07-20T20:09:31.164Z", "seq": 60, "kind": "consideration", "summary": "R-20-PR: 'filterable' has TWO incompatible backends — vec0 pre-KNN filtering cannot be extended without a full re-embed; ADR-0.8.11 D3 forbids the post-KNN fallback", "id": "TC-41", "release": "0.8.20", "slice": "15", "status": "open", "refs": ["git:29eba153"], "body": "ORCHESTRATOR DESIGN FINDING, code-grounded. The 'filterable' role resolves to storage through TWO different, incompatible engines today. (A) SEARCH/vector backend: vector_filter_clause (engine lib.rs:8685-8710) is a HARDCODED four-column map onto vec0 metadata columns {source_type, kind, created_at, status}. vec0 is a virtual table with NO ALTER, so adding a fifth filterable field on the pre-KNN path means DROP+RECREATE vector_default and RE-EMBED THE WHOLE CORPUS — per registration. Unacceptable. (B) read.list backend: Predicate::to_sql_clause emits json_extract(body, path) gated by the COMPILE-TIME const PREDICATE_PATH_ALLOWLIST (lib.rs:1808) of exactly six paths. The obvious escape — join EAV after the KNN — is EXPLICITLY FORBIDDEN by ADR-0.8.11 D3 no-demotion, which is enforced in the type system: a Json filter term is TYPE-REJECTED on the vector backend (lib.rs:2038-2044). So all three options are constrained and the tension is REAL, not incidental. NOTE the C-1 contract describes filterable as a 'pre-KNN constraint' (projection-registry-and-async-embed.md §2), i.e. backend (A) — the expensive one. ORCHESTRATOR RECOMMENDATION (NOT ratified, owed to the HITL): scope filterable to backend (B) for 0.8.20 — the registry supersedes the compile-time PREDICATE_PATH_ALLOWLIST with a runtime-registered set, and the vec0 metadata filter set stays FROZEN at its four columns. This is a DEVIATION from the contract's pre-KNN wording and must be ratified, not assumed, because Slice 30's H7 can-i-deploy gate checks as-built against that contract."}
{"ts": "2026-07-20T20:09:58.062Z", "seq": 61, "kind": "open_question", "summary": "TC-33 migration story worked out: node/edge temporal split CONFIRMS the INTEGER recommendation; blast radius is small in schema but crosses an external BYO-LLM wire format", "id": "TC-33", "release": "0.8.20", "slice": "15", "status": "open", "refs": ["git:29eba153"], "body": "ORCHESTRATOR DESIGN WORK on the HITL-directed harmonisation. CONCLUSION: converge on INTEGER epoch seconds, i.e. the Steward recommendation STANDS — no escalation of the direction call is required on the merits. SCHEMA BLAST RADIUS IS SMALLER THAN FEARED: exactly ONE table, TWO columns (canonical_edges.t_valid/t_invalid, TEXT, added by step 14), and ZERO indexes reference either column, so no index rebuild is forced. BUT the real cost is NOT the schema, it is FIVE surfaces: (1) 8 SQL predicate sites that today inline datetime('now') (engine lib.rs:7806, 9553, 9685, 10074, 10112, 10903, 10970, 4539) must switch to a BOUND :now — which is a strict improvement, it is what makes edge validity deterministically testable the way node validity already is; (2) PreparedWrite::Edge t_valid/t_invalid Option<String> -> Option<i64>, a breaking binding-coordination change; (3) ConsolidateCandidateEdge AND its harness JSON contract (lib.rs:4405-4406) which is an EXTERNAL BYO-LLM wire format; (4) temporal_fallback is derived by STRING EQUALITY comparing t_valid against warnings[].substituted_t_valid (lib.rs:4302-4305, 4123-4141) and BREAKS SILENTLY under a type change — this is the sharpest edge and needs its own RED test; (5) two tests that deliberately PIN the TEXT representation (s22_edge_temporal_columns_are_untouched_text in schema/tests/step22_migration.rs:211, r_20_nv_edge_temporal_filter_is_unchanged in engine/tests/slice10_node_validity.rs:624) must be rewritten, not deleted — they were the drift guard and must be replaced by an INTEGER equivalent. LAYERING CALL OWED TO THE HITL: storage and the governed SDK surface should both become INTEGER, but the BYO-LLM EXTRACTOR JSON should KEEP ISO-8601 with engine-side normalisation on ingest — an LLM cannot reliably emit epoch seconds, and that boundary already has a normalisation layer. This is a layering boundary, not splitting the difference on the representation; flagging it explicitly because it is adjacent enough to the direction call that the HITL should confirm it. MIGRATION MECHANICS: SQLite has no ALTER COLUMN TYPE, and the pinning test asserts declared type == TEXT, so true harmonisation requires a 12-step TABLE REBUILD of canonical_edges (create-new / copy-with-conversion / drop / rename), not an UPDATE in place. HAZARD: strftime('%s', x) returns NULL on an unparseable value, and a NULL t_invalid READS AS 'still valid' — so a junk value silently RESURRECTS an invalidated edge. That must be a RED test and a migration-time hard failure, never a silent NULL. SCHEDULING NOTE: this needs schema step 24 (23 being the EAV/registry step), making FOUR migrations in 0.8.20. I-6 'one migration per release' is GUIDANCE, not enforced — there is no test counting steps per release, and 0.8.20 already ships two (21 and 22), which dev/plans/0.8.6-0.8.16-PROGRAM-SEQUENCING.md:417 explicitly blesses. So four is precedented but should be surfaced, not slipped through."}
{"ts": "2026-07-20T21:35:56.822Z", "seq": 62, "kind": "observation", "summary": "GATE-INTEGRITY: R-20-NV was closed at Slice 10 while one of its three Slice-0-named RED tests (valid_as_of_binds_now_once) was NEVER WRITTEN — and that exact property then regressed", "id": "TC-42", "release": "0.8.20", "slice": "15", "status": "open", "refs": ["git:a8087dfb"], "body": "MEASURED, and the failure it predicted actually happened. dev/design/0.8.20-slice0-erasure-design.md:308 names THREE falsifiable RED tests for R-20-NV: validity_window_matrix, valid_as_of_binds_now_once, crossed_boundary_since_hook. A grep across src/ finds ZERO hits for valid_as_of_binds_now_once — it exists ONLY in that design table. R-20-NV was nevertheless declared CLOSED at Slice 10. The property it was supposed to pin (':now' binds ONCE PER QUERY, not per row/arm — a per-arm bind makes results non-deterministic across a validity boundary) then REGRESSED in Slice 15b fix-2, where the graph arm re-read current_epoch_seconds independently of the instant already resolved for the text/vector arms. codex §9 caught it; the never-written test would have caught it first. TWO DURABLE CONSEQUENCES. (1) TRACEABILITY IS UNENFORCED: a Slice-0 design can name a RED test, the slice can close without writing it, and nothing detects the gap — the acceptance table is prose, not a gate. Slice 40's DoD should mechanically check that every test NAMED in the Slice-0 acceptance tables actually EXISTS in src/, for every requirement, not just this one. Other named-but-unverified tests may exist across R-20-RV/PR/EAV/DR/SUR — this was found by accident, so assume it is not the only one. (2) NAMING: the property is now pinned for the first time by default_view_search_reads_the_clock_once_per_query (Slice 15b fix-3), which does NOT match the design's name. Either rename it or cross-reference it, or a traceability sweep will still report R-20-NV as missing a test."}
{"ts": "2026-07-21T07:15:33.052Z", "seq": 63, "kind": "question", "summary": "Extractor timestamp rejection uses InvalidArgument while the parallel consolidation path uses Consolidator (after TC-33 fix-3) — should the extractor leaf be Extractor?", "area": "fathomdb-engine/error-taxonomy", "epistemic": "proposed", "id": "TC-43", "owner": "hitl", "priority": "p3", "status": "open", "refs": ["file:src/rust/crates/fathomdb-engine/src/lib.rs:1950", "file:src/rust/crates/fathomdb-engine/src/lib.rs:4995", "file:src/rust/crates/fathomdb-engine/tests/tc33_edge_temporal_epoch.rs"], "body": "TC-33 fix-3 remaps a malformed/non-string consolidation-verdict t_invalid from EngineError::InvalidArgument to EngineError::Consolidator, because consolidation is a BYO-LLM PROVIDER boundary whose documented leaf is Consolidator (matching the two sibling failure modes: missing key, null/unparseable-to-None). But normalize_extractor_timestamp is ALSO called on the EXTRACTOR ingest boundary (lib.rs:4665-4684), which is likewise a BYO-LLM provider boundary and has its OWN leaf EngineError::Extractor at the enum. One could argue a malformed EXTRACTOR timestamp is symmetrically a provider fault (Extractor), not a user InvalidArgument. This is a broader taxonomy call the HITL/steward should weigh. NOT resolved here and deliberately OUT OF SCOPE for fix-3: the 15c GREEN tests tc33_edge_temporal_epoch.rs PIN that the extractor rejection is InvalidArgument{msg} and that msg names the offending field (t_valid/t_invalid); changing the extractor leaf would break those pinned tests. Deferred."}
{"ts": "2026-07-21T07:57:03.726Z", "seq": 64, "kind": "observation", "summary": "strftime('%s',?) used as an ISO-8601 validator but SQLite's date parser is MORE LENIENT than the declared contract — codex found 3 leniencies across 2 rounds; safety-critical no-NULL property intact (wrong-instant, not resurrection)", "area": "fathomdb-engine/tc33-timestamp-normalisation", "id": "TC-44", "status": "open", "refs": ["git:7ca664f7", "plan:dev/plans/plan-0.8.20.md"], "body": "TC-33 normalises edge timestamps by delegating to SQLite strftime/date fns, leakier than strict ISO-8601: (1) too-large epochs [fixed fix-1]; (2) pre-year-0000 epochs render non-NULL '-001-12-31T23:59:59Z' so reject_unrenderable_edge_epoch (engine:1892-1894) passes them despite outside declared 0000..=9999; (3) bare numeric strings '0'/'2451545.0' treated as JULIAN-DAY -> epoch not NULL (engine:1824-1827), so non-ISO wire input stored as unrelated instant. SAFETY: none yield NULL t_invalid, so HITL Note1/P3 'no resurrection via NULL' INTACT — wrong-instant not fail-open. Root fix: strict ISO-8601 shape gate (regex, no dep) before SQLite, plus numeric MIN/MAX range check on governed i64 path. HALTED at fix-N bound (5th codex round) pending HITL/steward: consolidated root-cause fix vs accept-as-pre-1.0. Distinct from TC-43."}
{"ts": "2026-07-21T07:57:03.753Z", "seq": 65, "kind": "observation", "summary": "PRE-EXISTING (not TC-33): fix-32 supersession writes projection terminals with state='superseded' but the terminal-table CHECK allows only ('failed','up_to_date') and the writer uses INSERT OR IGNORE — terminals SILENTLY DROPPED, supersession path can stall the projection cursor", "area": "fathomdb-engine/projection-cursor", "id": "TC-45", "status": "open", "refs": ["git:7ca664f7"], "body": "record_projection_terminal (INSERT OR IGNORE) called engine:14867/:14890 with state='superseded' by fix-32 supersession. _fathomdb_projection_terminal (schema step7) CHECK(state IN ('failed','up_to_date')). INSERT OR IGNORE skips CHECK-violating rows silently, so the terminal never persists; advance_projection_cursor can stall at a superseded cursor with no terminal — same class TC-33 fix-4 fixed for migration. PRE-EXISTING (fix-32, prior slice), NOT introduced by TC-33, left untouched (out of scope). Surfaced by the fix-4 implementer while proving the 'up_to_date' token. Needs its own fix; likely 'up_to_date' at both sites."}
{"ts": "2026-07-21T08:26:06.514Z", "seq": 66, "kind": "observation", "summary": "TC-44 RESOLVED by TC-33 fix-5: strict ISO-8601 shape gate before strftime (rejects Julian-day/bare-numeric wire input) + numeric MIN/MAX epoch range check replacing renderability (rejects pre-year-0000 governed i64) — whole strftime-leniency class closed", "area": "fathomdb-engine/tc33-timestamp-normalisation", "id": "TC-44", "status": "resolved", "refs": ["seq:64", "git:895b9df0", "plan:dev/plans/plan-0.8.20.md"], "body": "fix-5 (GREEN 895b9df0): (a) is_iso8601_shape() hand-rolled ASCII-byte shape gate (YYYY-MM-DD[(T|space)HH:MM:SS[.fff][Z|+-HH:MM|+-HHMM]]) runs BEFORE strftime in iso8601_to_epoch_seconds; '2451545.0' (Julian day) and '0' now return None -> existing hard-reject fires, ZERO rows written (raw canonical_edges count asserted). NO new dependency: no chrono/time in workspace, regex only transitive via jsonschema; hand-rolled. Shape-valid-but-impossible dates (2025-13-45) still None via strftime. (b) reject_unrenderable_edge_epoch() now tests numeric [MIN_RENDERABLE_EPOCH=-62167219200, MAX_RENDERABLE_EPOCH=253402300799] bounds directly, not renderability; pre-year-0000 epoch -62167219201 (renders NON-NULL -001-12-31T23:59:59Z) now rejected. Consts verified vs SQLite = 0000-01-01T00:00:00Z .. 9999-12-31T23:59:59Z exactly. No NULL path opened; safety-critical no-resurrection property intact. RED 3-fail/5-pass then GREEN 8/8 in tc33_fix5_iso8601_shape_gate.rs; full engine+schema suite 588 passed/0 failed/13 ignored. Positive controls (Z, +00:00, date-only, space-separated) still accepted at correct epoch."}
{"ts": "2026-07-21T13:07:58.417Z", "seq": 67, "kind": "question", "summary": "Slice 15e gated vec0 filterable reshape: the ProjectionDestructive drop-list acknowledgement channel is defined for REMOVING a filterable but UNDEFINED for ADDING a net-new filterable that (per decision-4) still wipes all embeddings — needs a total ack channel; Steward direction owed", "area": "fathomdb-engine/projection-registry-vec0-reshape", "id": "TC-46", "status": "open", "refs": ["git:282a03f7", "plan:dev/plans/plan-0.8.20.md"], "body": "15e must reshape vec0 to carry a registry-declared filterable attribute as a PRE-KNN metadata column (decision 2, ADR D3). Decision 4: the reshape does NOT preserve embedding blobs, so ANY filterable-set change (ADD or REMOVE) is destructive to the vector index and must be gated. Slice 15d's gate is ProjectionDestructive + an explicit drop-list — which names a projection to REMOVE. When ADDING a net-new filterable attribute there is nothing to put in the drop-list, yet the ADD still triggers a full embedding wipe, so it must be acknowledged. The drop-list channel is therefore undefined for the ADD case. Implementer recommendation (sound): a typed reshape-acknowledgement parameter on configure_projections (e.g. allow_reshape / a ReshapeAck), the only channel total across ADD and REMOVE — a GOVERNED-SURFACE addition needing sign-off. Also verified: vec0 rejects quoted column identifiers (attr cols need byte-safe encoding e.g. attr_<hex-of-utf8>); vec0 rejects partial-column INSERT (all ~6 INSERT sites must bind every attr col — inherently multi-site); the async projection worker carries no node body (write-path population is net-new); run_pin_and_requantize_pass (engine:8973) is a SAME-SHAPE re-quantize that must PRESERVE attr cols — decision-4 no-preservation applies ONLY to the filterable-set-change reshape, NOT to same-shape re-quantize (conflating would wipe attr values on every mean-pin). Proposed decomposition 15e-1 (shape param + gated reshape + pre-KNN compile, attr cols ship '' sentinel, population deferred — mirrors how Slice 10 shipped status) / 15e-2 (write-path + rebuild_vec0 population) / 15e-3 (governed filter-predicate + reshape-ack across Py/TS). 15e-1 is BLOCKED on this ack-channel decision."}
{"ts": "2026-07-21T23:22:53.452Z", "seq": 68, "kind": "decision", "summary": "RESOLVED — HITL chose Option 1: NON-DESTRUCTIVE vec0 reshape. Ack-channel gap DISSOLVES (no new governed surface). NO-DATA-MIGRATION is scoped to the cross-version schema step, NOT a runtime reshape of a live DB. Follow migrate_vector_partition_pack1_to_pack2 (engine:13492).", "id": "TC-46", "status": "resolved", "refs": ["git:cf2f155c"], "body": "The premise 'a reshape must wipe embeddings' was over-applying NO-DATA-MIGRATION (which governs the 0.8.9->0.8.20 cross-version schema step) to a RUNTIME reconfiguration. A non-destructive re-insert keeps old+new rows aligned/coherent/queryable, and the tree already ships exactly that op (pack1->pack2). So a filterable ADD reshape is NON-destructive => no acknowledgement parameter needed, no ProjectionDestructive for the ADD case; idempotent re-registration is a no-op; a shape change re-inserts non-destructively. FOUR load-bearing conditions (all must hold or results go SILENTLY wrong): (1) list rowid EXPLICITLY in the re-insert (rowid==write_cursor, engine:7086/:10005; auto-assign decouples embedding from node); (2) new attr column is plain metadata OR partition key, NEVER a vec0 aux/'+' column (aux hard-errors filtered KNN, engine:13460-13464 — this is what makes pre-KNN D3 work); (3) back-fill old rows with '' sentinel (vec0 TEXT metadata NOT-NULL-able, engine:7091) so they fail-to-match cleanly; (4) copy embedding_bin VERBATIM via vec_bit(...), do NOT re-quantize (re-deriving bits from raw embedding leaves old rows un-centered vs new mean-centered => incomparable Hamming, silent recall corruption; anti-pattern at engine:13584-13599). run_pin_and_requantize_pass is a separate same-shape op — leave alone. Scope: filterable already works via 15d EAV (canonical_attributes); 15e adds ONLY pre-KNN vector-path routing (D3, no post-KNN json_extract demotion)."}
{"ts": "2026-07-22T22:50:10.094Z", "seq": 69, "kind": "question", "summary": "SURVIVING TEMPORAL P2 (calendar-rollover sub-class of TC-44): strftime silently ROLLS OVER a shape-valid impossible DAY (2025-02-30 -> 2025-03-02) instead of NULL; the is_iso8601_shape gate (fix-5) checks format, not calendar validity. ESCALATED per the ratified sixth-round rule (no reflexive temporal fix).", "area": "fathomdb-engine/tc33-timestamp-normalisation", "id": "TC-47", "status": "open", "refs": ["git:0b47349c", "seq:64"], "body": "Verified vs SQLite: strftime('%s','2025-02-30T00:00:00Z') -> epoch that round-trips to 2025-03-02; '2025-04-31'->'2025-05-01'; but '2025-13-01' (month 13) -> NULL. So SQLite rejects impossible MONTHS but silently rolls over impossible DAYS. TC-44's fix-5 shape gate accepts 2025-02-30 (well-formatted YYYY-MM-DD), so a BYO-LLM edge timestamp of Feb 30 stores a DIFFERENT instant than the provider supplied — bypassing the hard-reject contract. SAFETY: the stored value is a valid instant, not NULL, so the 'no resurrection via NULL' property (HITL Note 1 / P3) HOLDS — this is wrong-instant, not fail-open. Clean complete fix available: a CALENDAR ROUND-TRIP (strftime the epoch back to a date and compare the date component to the input; reject on mismatch) closes the whole rollover sub-class, not another partial patch. Options for HITL/steward: (a) calendar round-trip validation [recommended]; (b) accept as documented pre-1.0 (safety intact); (c) rethink strftime-as-validator (no chrono/time in workspace by design). Per the ratified rule, this is a design call, not a reflexive fix. Distinct, non-temporal same-round finding: TS configure_projections wrapper skips validateFfiString (lone UTF-16 surrogate -> U+FFFD persists) — bounded AC-068 gate fix, folded into the closeout."}
{"ts": "2026-07-24T12:56:20.969Z", "seq": 70, "kind": "todo", "summary": "Status-board currency enforcement — design dev/design/status-board-currency-enforcement.md; first effort scope = items 1-3 (seam-ownership contract + preflight land-gate + CI drift detector); commission an orchestrator via the next steward", "detail": "HITL-directed 2026-07-24. Motivation: the 0.8.20 STATUS board lied for four days after Slices 10/15 landed (said Slice 15 PARTIAL/unlanded) because the LANDED board state falls in an ownership seam between the orchestrator (owns in-flight board) and the steward (does the land, reconciles master+ledger, skipped the board). Fix the mechanism, not the actor. FIRST EFFORT = items 1-3 of the design: (1) contract line assigning the steward the LANDED row + next-slice pointer, updated IN the landing merge (edit .claude/agents/steward.md + orchestration.md section 12.5); (2) board-currency GATE in scripts/preflight.sh --landing that refuses a land leaving STATUS-0.8.z.md stale, RED-first test; (3) CI drift detector on main, red when board disagrees with git ancestry. PLACEMENT ANSWER (HITL asked): NOT precommit (board legitimately reads in-flight during a build => false positives; precommit can only check well-formedness). Prevent at land-time in preflight (mandatory land gate, blocks before stale board reaches main); DETECT via CI on main (non-bypassable backstop for what slips preflight or predates it); prepush is optional shift-left, not a substitute (--no-verify-able, local-config-dependent). Item 4 (machine-derived LANDED table from git ancestry) is LATER, only if drift persists despite 1-3; when built its check is 'regenerate + assert no diff' in the same two homes (preflight + CI). Applies to EVERY release board + the master, not just 0.8.20. TDD-able, docs+scripts+one CI job, no engine code. Aligns with guardrail-failures-fix-tooling-not-people. Proposed to the next steward in STEWARD-SESSION-HANDOFF-2026-07-24-A.md (commission alongside Slice 20 or as its own micro-effort).", "owner": "next-steward-to-commission-orchestrator", "status": "OPEN"}
{"ts": "2026-07-24T14:55:11.588Z", "seq": 71, "kind": "decision", "summary": "TC-45 PLACED: fix IN 0.8.20, folded into Slice 20 (HITL 2026-07-24). Prefer 'up_to_date' at both record_projection_terminal call sites (no migration) over widening the terminal CHECK (schema step); RED-first.", "decider": "hitl", "id": "TC-45", "status": "placed", "refs": ["seq:65", "plan:dev/plans/plan-0.8.20.md"]}
{"ts": "2026-07-24T14:55:11.615Z", "seq": 72, "kind": "decision", "summary": "HITL 2026-07-24: cross-cutting docs/tooling hygiene effort = ALL cleanup items, run FIRST as one dedicated effort BEFORE commissioning 0.8.20 Slice 20; absorbs the status-board-currency items 1-3 (todos seq 70) as its tranche T4.", "decider": "hitl", "id": "TC-48", "status": "open", "refs": ["seq:70", "design:dev/design/status-board-currency-enforcement.md"]}
{"ts": "2026-07-24T14:59:05.997Z", "seq": 73, "kind": "observation", "summary": "MARKDOWN GATE IS RED ON MAIN since c366d6e0 (2026-07-02): 9 errors (MD001 x1, MD025 x8) in dev/research/personal-agent-database-market-2026-07-02.md, appendix headings are # instead of ##. Unseen for 3 weeks because TC-37 makes agent-lint-md.sh exit 0 vacuously in a worktree (no node_modules) - every orchestrator read a vacuous green. Regression vs the 0.8.9.1 debt-to-zero achievement. Placed: DOC-HYGIENE-1 tranche T1/0 (fix headings) + T3/9b (make a missing linter a HARD failure).", "decider": "steward", "id": "TC-49", "status": "open", "refs": ["git:c366d6e0", "prompt:dev/plans/prompts/DOC-HYGIENE-1-HANDOFF.md"]}
{"ts": "2026-07-25T16:52:43.808Z", "seq": 74, "kind": "decision", "summary": "TC-48 CLOSED — DOC-HYGIENE-1 landed on origin/main f53b8c64..597738d9 (ff) 2026-07-25, no pico label (HITL). All 4 tranches T1-T4 delivered; codex §9 terminal 1 P2 fixed / no P1. The Slice-20 sequencing prerequisite is discharged. Reconciled to master §6 as F-33.", "decider": "steward", "id": "TC-48", "status": "closed", "refs": ["seq:72", "git:597738d9", "plan:dev/plans/0.8.6-0.8.16-PROGRAM-SEQUENCING.md"]}
{"ts": "2026-07-25T16:52:43.836Z", "seq": 75, "kind": "observation", "summary": "TC-49 CLOSED — the markdown gate red on main since c366d6e0 (2026-07-02) is REMEDIATED by DOC-HYGIENE-1 T1, and the TC-37 vacuous-skip that hid it for 3 weeks now HARD-FAILS (T3). Steward re-verified on main: agent-lint-md.sh exit 0 (read via PIPESTATUS, not a trailing echo).", "decider": "steward", "id": "TC-49", "status": "closed", "refs": ["seq:73", "git:597738d9"]}
{"ts": "2026-07-25T18:52:35.955Z", "seq": 76, "kind": "todo", "summary": "POST-0.8.20 SLICE OWED — dev/design/** status: frontmatter backfill is DELIBERATELY PROVISIONAL and must be remediated in its own slice. DOC-HYGIENE-2 T2c introduces a frontmatter gate over dev/design/** (108 docs, only 23 carry status: today) and backfills the remaining 85. Per HITL 2026-07-25 the backfill DEFAULTS TO status: UNREVIEWED for anything not confidently classifiable, because a doc wrongly marked ACTIVE is WORSE than one with no marker - a falsely-current design doc is exactly the failure mode this program keeps re-hitting (see the 0.6.0-era dev/architecture.md still frontmattered target_release 0.6.0 / locked, and README.md still calling FathomDB a rewrite-in-progress). Only docs with clear evidence - an existing supersession banner, a closed-release tie - get ACTIVE or SUPERSEDED at backfill time. OWED: a dedicated post-0.8.20 slice that does the real classification, converting UNREVIEWED to a truthful status per doc. Until then the gate proves PRESENCE of a status field, never its TRUTH.", "decider": "hitl", "id": "TC-50", "status": "open", "refs": ["plan:dev/plans/0.8.6-0.8.16-PROGRAM-SEQUENCING.md", "seq:106"]}
{"ts": "2026-07-25T22:22:46.422Z", "seq": 77, "kind": "todo", "summary": "DOC-HYGIENE-3 BACKLOG (from DOC-HYGIENE-2 escalations; placed by HITL O3 into the Slice 30 -> Slice 40 window). Four carried items, none blocking Slice 20. (a) ~55 FURTHER ROTTEN LINE-ANCHORS in plan-0.8.20.md in shapes T1d's narrow ANCHOR_RE does not match - the section 0.1 ROOT-CAUSE table driving Slice 5 is measurably wrong (offsets +4680, +2502, -8160); symbols exist so the findings hold, but nobody can navigate by the numbers. Widening ANCHOR_RE is one line but reddens ~80 sites, so it needs its own budget. (b) LEDGER VOCABULARY CONTRADICTS ITS OWN README: TC-7 was ruled but carries no kind=decision so it is invisible to the T2b ruling registry; kind is declared immutable yet TC-8 walks consideration->decision; the declared terminal vocabulary matches ZERO real entries; todos seq-24 rules on TC-11 with id=null. Adjudicating means appending to a ledger, so it needs a decision first. (c) UNKNOWN vs UNREVIEWED are near-synonyms across two vocabularies introduced by T2c - a single vocabulary is owed once TC-50 drains. (d) 46 pre-existing free-form dev/design status values (locked, decision-ready, ...) grandfathered behind a frozen LEGACY_RE + one-way budget ratchet; rewriting them IS the TC-50 classification and must not be done piecemeal.", "decider": "steward", "id": "TC-51", "status": "open", "refs": ["seq:106", "git:986dd912"]}
{"ts": "2026-07-25T22:22:46.450Z", "seq": 78, "kind": "todo", "summary": "ALLOWLIST _comment PROSE CONTRADICTS THE PRE-SIGN, and correcting it TRIPS THE T1e PIN. src/conformance/governed-surface-allowlist.json's _comment still reads 'AWAITING HITL SIGN-OFF, NOT SIGNED' for slices 5d/10b/15b/15d, which is false as of 427d2712 (master F-34 - the HITL pre-signed the accumulated delta). The T1e pin binds to that file's EXACT CONTENT (hash 93ef3a86...), so a prose-only correction re-opens the gate and requires a deliberate re-pin under HITL sign-off - it is NOT a docs fix. DELIBERATELY DEFERRED to the batched governed-surface decision at the Slice 30 -> Slice 40 boundary, where a re-pin happens anyway (Slice 20 is expected to trip the pin) and where AC-079 mints. Until then the TRUTH is carried by the generated status-unblocks view (STATUS section 1), the corrected section 4 row 7, plan section 11's 2026-07-25 rulings block, and the Slice-20 commission brief - so no implementer should be misled. DO NOT hand-edit the allowlist prose before that decision.", "decider": "steward", "id": "TC-52", "status": "open", "refs": ["seq:106", "git:7b6a67ba"]}
{"ts": "2026-07-25T22:23:55.824Z", "seq": 79, "kind": "todo", "summary": "LEDGERWRITE HAS NO GUARD AGAINST SHELL-SUBSTITUTION CORRUPTION AT WRITE TIME, and it has now silently mangled two entries. seq-95/96 (a backtick substitution dropped one word) and seq-108/109 (an exit-status variable expanded to 0 and a command substitution expanded to empty, its stderr showing \"basename: missing operand\"). Both were caught only because a human/Steward re-read the written record; a malformed summary is otherwise indistinguishable from an intended one, and the ledger is APPEND-ONLY so the damage can only be corrected by a follow-up entry, never repaired in place. Per the standing rule that guardrail failures are fixed in TOOLING and not by telling people to be careful: ledgerwrite should refuse-or-warn when a --summary contains characters that indicate an unintended expansion already happened or is likely (a lone $ , an empty $() residue, backticks), and/or dev/steward/README.md should mandate single-quoted summaries with a worked example. Note the failure is at the CALLER (the shell expands before ledgerwrite ever sees the string), so a pure in-tool check can only catch residue, not intent - the README half of the fix is load-bearing. Placed in the DOC-HYGIENE-3 window alongside TC-51.", "decider": "steward", "id": "TC-53", "status": "open", "refs": ["seq:109", "seq:96"]}
{"ts": "2026-07-26T00:23:51.952Z", "seq": 80, "kind": "observation", "summary": "RESOLVED BY HITL 2026-07-25: .markdownlint-cli2.jsonc ignoring .claude/** is INTENTIONAL and stays. A subagent flagged it as a coverage gap while adding the /decisions command, on the grounds that .claude/commands/*.md ARE git-tracked while the config comment claimed they were gitignored. The comment was factually wrong; the IGNORE is correct. Reason recorded in the config: these files are prompt text read by an agent, not published prose, so markdownlint structural rules police a shape that carries no meaning and a lint failure would block a command edit for style. The stale justification has been corrected in place so no future agent removes the ignore on the strength of a false premise. NO gate change; NO coverage added. Do not re-open.", "decider": "hitl", "id": "TC-54", "status": "closed", "refs": ["file:.markdownlint-cli2.jsonc"]}
{"ts": "2026-07-26T00:33:28.289Z", "seq": 81, "kind": "caveat", "summary": "BLOCKER FOR SLICES 20c/25/30 - the governed-surface pin gate makes 'record the delta as a proposal AND land' mechanically impossible. plan-0.8.20.md section 11 ruling 2 (HITL 2026-07-25) says new governed surface at 20/25/30 is recorded as a proposal, the branch stays green, and the accumulated delta goes to the HITL once at the Slice 30 to Slice 40 boundary. But check-governed-surface-pin.sh compares a sha256 AND git-blob-sha1 over the RAW BYTES of src/conformance/governed-surface-allowlist.json, so ANY diff - including the _comment prose edit that IS the proposal convention used by Slices 5d/10b/15b/15d - hard-fails the gate. preflight.sh --landing invokes that gate as a HARD fail, so the slice cannot land. The gate header asserts that tripping it is 'precisely what lets Slices 20/25/30 proceed WITHOUT stopping for a per-slice sign-off'; as wired that claim is FALSE - tripping it blocks the land. Slice 20 was split so part b (dense_readiness) adds ZERO governed commands and lands clean; part c (flush_embeddings) is held. Options: (a) FIX THE TOOLING - give the pin a pending_delta block so the gate passes iff allowlist == pinned union pending_delta, loudly reporting the unsigned set, preserving every guarantee incl. the locked recovery_denylist; (b) fresh HITL sign-off plus re-pin per slice, which contradicts the batching ruling; (c) reclassify. Recommend (a).", "area": "release/0.8.20/governed-surface", "decider": "orchestrator", "id": "TC-54", "priority": "p1", "status": "open", "refs": ["plan:dev/plans/plan-0.8.20.md", "file:scripts/check-governed-surface-pin.sh", "file:scripts/preflight.sh"]}
{"ts": "2026-07-26T00:33:28.317Z", "seq": 82, "kind": "open-question", "summary": "UNRESOLVED CLASSIFICATION that decides whether Slice 20c trips the surface pin at all - is flush_embeddings a governed APPLICATION COMMAND or an INSTRUMENTATION/CONTROL method? plan-0.8.20.md section 11 ruling 2 states it 'reads as a net-new command' and expects the pin to trip. But dev/design/record-lifecycle-protocol/api-surface.md C4 says the opposite - reuse the shipped drain(timeout_ms) barrier INSTEAD of adding flush_embeddings - and projection-registry-and-async-embed.md section 4 repeats it. Both design docs are status UNREVIEWED, so the plan is the contract and the plan wins; recorded because the docs will otherwise keep misleading readers, and TC-50 owes them a classification. Load-bearing mechanical detail: the shipped drain is NOT in the JSON allowlist - it sits in the _INSTRUMENTATION exclusion set hardcoded in BOTH src/python/tests/test_surface.py and src/ts/tests/surface.test.ts ('observability, NOT application commands'). So classifying flush_embeddings as instrumentation would leave the pinned JSON untouched and let 20c land clean, while classifying it as a command forces an allowlist edit and hits TC-54. That makes the classification a real design question with a large mechanical consequence - it must NOT be settled by whichever answer is easier to land. Design rider that survives either way: drain is a BARRIER (wait-for-idle), flush must also be a TRIGGER, so deferred/backfill rows have to be enqueued on the same projection runtime drain waits on.", "area": "release/0.8.20/api-surface", "decider": "orchestrator", "id": "TC-55", "priority": "p1", "status": "open", "refs": ["plan:dev/plans/plan-0.8.20.md", "file:dev/design/record-lifecycle-protocol/api-surface.md", "tc:TC-54"]}
{"ts": "2026-07-26T00:33:28.342Z", "seq": 83, "kind": "observation", "summary": "Slice 20 fix-1 CLOSED A SHIPPED Engine::drain DEFECT as a side effect - a behaviour change on an already-published surface that the 0.8.20 changelog and the Slice 40 publish narrative must reflect. The pending-work probe (database_has_pending_projection_work, which backs ProjectionRuntime::wait_for_idle and therefore the public Engine::drain) omitted the scheduler's JOIN against _fathomdb_vector_kinds on the edge arm, while next_pending_projection_jobs carried it. Consequence: a live canonical edge WITH A BODY whose edge_fact kind is not registered counts as outstanding work that the scheduler will never schedule, so it never gains a projection terminal - phantom-pending forever, and drain burns its whole timeout and returns Err(Scheduler). Confirmed PRE-EXISTING empirically, not by inspection: a detached checkout at baseline 9db32765 using only baseline surfaces returned BASELINE-PROBE drain result = Err(Scheduler) after the full timeout, with zero Slice-20 code in the build. Found because dense_readiness derives from that same predicate, which is what surfaced it. Fix shares ONE definition (pending_edge_projection_from_where) between probe and scheduler so they cannot drift again. Deliberate preserved asymmetry: the scheduler filters write_cursor > cursor and the probe does not, per the Slice-15 G11 fix-1 note, so the probe still sees edge bodies left un-projected below the watermark after a mid-flight close.", "area": "engine/projection-runtime", "decider": "orchestrator", "id": "TC-56", "priority": "p2", "status": "open", "refs": ["git:2b1f62d0", "slice:0.8.20-20b-fix-1"]}
{"ts": "2026-07-26T00:33:28.366Z", "seq": 84, "kind": "observation", "summary": "PRE-EXISTING ENGINE DEFECT, reproducible at baseline, no owning plan - a loop of GOVERNED writes racing the async projection worker intermittently returns EngineError::Storage. Shape: PreparedWrite::Node with logical_id Some(..) in a loop while the projection worker runs; reproduced at baseline 9db32765 with ZERO Slice-20 code in the build, failing 7 of 8 runs (e.g. 'governed write 4 failed: Storage'). NEVER reproducible with logical_id None, which points at the supersession read-then-write upgrade on a deferred transaction - most likely SQLITE_BUSY_SNAPSHOT, which busy_timeout cannot retry. This matters beyond a flaky test: governed writes are exactly the Phase-2 surface 0.8.20 publishes, so a host doing bulk governed ingest can hit it. Slice 20 deliberately routed around it (its concurrency test uses anonymous writes, documented in a comment) rather than dragging an unrelated red into R-20-DR. Needs placement - candidate for Slice 40 triage or a 0.8.21 fixup.", "area": "engine/write-path", "decider": "orchestrator", "id": "TC-57", "priority": "p2", "status": "open", "refs": ["git:9db32765", "slice:0.8.20-20b"]}
{"ts": "2026-07-26T00:33:28.389Z", "seq": 85, "kind": "observation", "summary": "TEST-FRAGILITY THAT WILL WASTE ANOTHER AGENTS TIME - lifecycle_observability::ac_002_no_log_files_without_subscriber scans HOME-rooted measurement roots and false-positives on ANY concurrent write anywhere under HOME whose relative path merely CONTAINS the string fathomdb, including files in SIBLING repositories. Observed during Slice 20: it failed once claiming 'engine created a fathomdb-named artifact outside the DB path' and named /home/coreyt/projects/agent-aware/method/fathomdb-reference-census.md - a file in a different repo written by a concurrent process during the test window. Passed 3 of 3 on re-run and passes at baseline, so it is environmental, not a code defect. Because this machine routinely runs several agents across sibling repos at once, the false-positive rate is not negligible. Fix direction: scope the scan to the test's own temp/DB root rather than to HOME, or match the engine artifact naming precisely instead of a substring.", "area": "tooling/tests", "decider": "orchestrator", "id": "TC-58", "priority": "p3", "status": "open", "refs": ["slice:0.8.20-20b"]}
{"ts": "2026-07-26T00:46:17.646Z", "seq": 86, "kind": "caveat", "summary": "BLOCKER FOR SLICES 20c/25/30 - the governed-surface pin gate makes 'record the delta as a proposal AND land' mechanically impossible. REISSUE of the entry mis-filed as TC-54 at seq 81 (id race - TC-54 was already taken by the markdownlint observation at seq 80). plan-0.8.20.md section 11 ruling 2 (HITL 2026-07-25) says new governed surface at 20/25/30 is recorded as a proposal, the branch stays green, and the accumulated delta goes to the HITL once at the Slice 30 to Slice 40 boundary. But check-governed-surface-pin.sh compares a sha256 AND a git-blob-sha1 over the RAW BYTES of src/conformance/governed-surface-allowlist.json, so ANY diff - including the _comment prose edit that IS the proposal convention used by Slices 5d/10b/15b/15d - hard-fails the gate. preflight.sh --landing invokes that gate as a HARD fail, so the slice cannot land. The gate header asserts that tripping it is 'precisely what lets Slices 20/25/30 proceed WITHOUT stopping for a per-slice sign-off'; as wired that claim is FALSE - tripping it blocks the land. Slice 20 was therefore SPLIT so part b (dense_readiness) adds ZERO governed commands and landed clean at 26b237c0; part c (flush_embeddings) is HELD. Options: (a) FIX THE TOOLING - give the pin a pending_delta block so the gate passes iff allowlist equals pinned union pending_delta, loudly reporting the unsigned set, preserving every guarantee including the locked recovery_denylist; (b) fresh HITL sign-off plus re-pin per slice, which contradicts the batching ruling; (c) reclassify per TC-55. Recommend (a). Escalated to the Steward by the Slice-20 orchestrator.", "area": "release/0.8.20/governed-surface", "decider": "orchestrator", "id": "TC-59", "priority": "p1", "status": "open", "refs": ["seq:81", "tc:TC-55", "git:26b237c0", "file:scripts/check-governed-surface-pin.sh"]}
{"ts": "2026-07-26T00:46:17.674Z", "seq": 87, "kind": "observation", "summary": "RESTORED STATE after an id collision - this id belongs to the markdownlint-cli2.jsonc item, NOT to the governed-surface pin blocker. Original TC-54 (seq 80) recorded: .markdownlint-cli2.jsonc ignoring .claude/** is INTENTIONAL and stays, RESOLVED BY HITL 2026-07-25; that remains the state of TC-54 and it stays CLOSED. The Slice-20 orchestrator appended a different item under this same id at seq 81 after reading the ledger tail when TC-53 was the newest id and main then advanced with a new TC-54 before the write. Because state is the fold-to-latest per id, seq 81 had silently overwritten this item. That content is REISSUED as TC-59; this entry restores TC-54. Root cause worth fixing in TOOLING per the standing rule: ledgerwrite mints no id and cannot detect that a caller-supplied id already exists, so a read-then-write race across concurrent agents reuses ids silently - ledgerwrite should reject a NEW id that is already present unless an explicit update flag is passed. Related to TC-53, which records the adjacent shell-substitution corruption hazard in the same tool.", "area": "tooling/markdown", "decider": "orchestrator", "id": "TC-54", "priority": "p3", "status": "closed", "refs": ["seq:80", "seq:81", "tc:TC-59", "tc:TC-53"]}
{"ts": "2026-07-26T16:49:43.234Z", "seq": 88, "kind": "observation", "summary": "A third-party READ-WRITE SQLite connection opened against a LIVE engine database wedges the projection pipeline. Measured during Slice 20c X1 with node:sqlite DatabaseSync(path) in its default read-write mode: with an RW observer opened and closed between write and configure_projections, the declaration enrolment and backfill never became visible or effective, the subsequent drain timed out into SchedulerError, and _fathomdb_vector_kinds was still empty. Opening the same observer with readOnly true reproduces NONE of it. IMPORTANT and load-bearing: the R-20-DR barrier invariant HELD throughout - drain returned an ERROR, never a false Ok - so this is a robustness and WAL-interaction question, not a correctness violation of the flush barrier. Nothing in the Slice 20c fix causes it; the harness merely surfaced it. Any future test harness or operator tool that inspects a live engine DB must open READ-ONLY. Consider whether the engine should detect or document this.", "area": "engine/projection-runtime", "decider": "orchestrator", "id": "TC-60", "priority": "p2", "status": "open", "refs": ["git:7de05aae", "plan:dev/plans/plan-0.8.20.md"]}
{"ts": "2026-07-26T16:49:59.982Z", "seq": 89, "kind": "caveat", "summary": "CAVEAT for every future path that enqueues projection work: drain never calls notify_new_work, so an enqueue that does not itself notify leaves the dispatcher asleep on pending_scan false and drain burns its ENTIRE timeout into EngineError::Scheduler. Confirmed live during Slice 20c - an early cut enrolled the vector kind AFTER collect_projection_jobs had already decided whether to wake the dispatcher, and the row sat enqueued in the database with the dispatcher asleep for the full 120s. The api-surface.md C4 rider deliberately forbids making drain a trigger - drain is a barrier, wait-for-idle - so the fix ALWAYS belongs on the enqueue side. Slice 20c fixed the paths it owns (configure_projections notifies after commit; late enrolment runs before collect_projection_jobs). ANY OTHER enqueue path that does not notify retains this defect. Rule of thumb for future slices: enqueue and notify are one operation, never two.", "area": "engine/projection-runtime", "decider": "orchestrator", "id": "TC-61", "priority": "p2", "status": "open", "refs": ["git:7de05aae", "tc:TC-56"]}
{"ts": "2026-07-26T16:50:00.006Z", "seq": 90, "kind": "observation", "summary": "drain spins to its full timeout while ProjectionRuntime state.frozen is true. The dispatcher will not schedule while frozen, so a drain issued during a purge, erase, or rebuild window cannot make progress and returns EngineError::Scheduler rather than flushing. Freeze sites are in fathomdb-engine/src/lib.rs around the purge and erase paths and in run_rebuild. Found during Slice 20c and deliberately NOT fixed there - drain passivity is protected by the api-surface.md C4 rider and the internal callers already drain BEFORE freezing. Worth deciding whether drain should return a distinct typed error while frozen instead of an indistinguishable timeout, since a caller cannot currently tell a frozen engine from genuinely slow embedding.", "area": "engine/projection-runtime", "decider": "orchestrator", "id": "TC-62", "priority": "p2", "status": "open", "refs": ["git:7de05aae"]}
{"ts": "2026-07-26T16:50:10.059Z", "seq": 91, "kind": "observation", "summary": "run_rebuild can return with re-enqueued embed work still outstanding. It drains BEFORE rebuild_shadow_state rewinds the projection cursor, and never drains after, so the rebuild replays work onto the runtime and then returns while that work is still in flight. It relies on set_frozen(false) setting pending_scan to get the dispatcher moving. Found during Slice 20c and not fixed there (out of slice scope). Consequence for callers: an operator rebuild is not a synchronous flush - a caller that needs vectors materialised must issue its own drain AFTER run_rebuild returns. Related to TC-61 (enqueue and notify coupling).", "area": "engine/projection-runtime", "decider": "orchestrator", "id": "TC-63", "priority": "p2", "status": "open", "refs": ["git:7de05aae", "tc:TC-61"]}
{"ts": "2026-07-26T17:35:43.760Z", "seq": 92, "kind": "observation", "summary": "ProjectionRuntime::wait_for_idle has a lost-wakeup window that causes spurious slow drains and is a spurious-timeout risk for callers passing a short timeout. It releases the state lock to run database_has_pending_projection_work; a worker that finishes the last job inside that window notifies BEFORE the waiter re-acquires the lock, so the waiter misses the notification and sleeps the full remaining timeout before returning Ok. Measured during Slice 20c fix-1: the flush-barrier suite reported roughly 30s wall time with all assertions passing in about 40 percent of parallel runs (30.19s / 0.25s / 0.26s / 30.15s for the same test). Mechanism was confirmed by capping the cvar wait at 25ms - 6 of 6 runs then fast - and that one-line change was REVERTED because drain passivity is protected by the api-surface.md C4 rider and was out of the fix scope. PRE-EXISTING, not introduced by 20c; 20c only widened exposure by adding engines to the suite. A caller passing a short drain timeout can get a spurious Err(Scheduler) purely from this race. Fixing it means re-checking the predicate under the lock or bounding the cvar wait.", "area": "engine/projection-runtime", "decider": "orchestrator", "id": "TC-64", "priority": "p2", "status": "open", "refs": ["git:0759715a", "tc:TC-61"]}
{"ts": "2026-07-26T17:35:43.787Z", "seq": 93, "kind": "caveat", "summary": "CAVEAT for the slice that lands PER-ATTRIBUTE embedding: ProjectionSpec has NO kind axis, so Slice 20c had to guess the enrolment scope and the guess is load-bearing. The spec is name plus roles plus optional fts and vector sub-objects - per-ATTRIBUTE, with no kind - while derive_dense_readiness is documented as CORPUS-WIDE because 15d defers per-attribute embedding. Slice 20c therefore read a searchable-to-vector declaration as the corpus dense arm is live and enrolled every vector-eligible node kind present in canonical_nodes (its decision D1). That breadth directly produced BOTH codex round-2 findings: a P1 permanent wedge when a corpus contains a kind outside the resolve_source_type locked list, and a P2 false-ready on the late-enrolment path. Both were fixed in 20c fix-2, but the underlying seam remains: enrolment scope is inferred, not declared. When per-attribute embedding lands, the enrolment scoping and the readiness scoping must move together through the SAME seam, and this inference should be replaced by an explicit kind axis rather than re-derived.", "area": "engine/projection-registry", "decider": "orchestrator", "id": "TC-65", "priority": "p2", "status": "open", "refs": ["git:0759715a", "tc:TC-55"]}
{"ts": "2026-07-26T17:48:42.336Z", "seq": 94, "kind": "observation", "summary": "TC-11 logical_id pin: the STATIC migration guard is a lexical scanner, not a SQL parser — known-open evasion shapes remain; the DYNAMIC whole-ladder test (D2) is the load-bearing, shape-independent proof", "area": "schema-migrations", "epistemic": "verified", "id": "TC-66", "owner": "unassigned", "priority": "P3", "status": "open", "refs": ["git:83b1c818", "plan:dev/plans/plan-0.8.20.md"], "body": "Landed at 0.8.20 Slice 25 (R-20-SUR). check_migration_logical_id_pin in src/rust/crates/fathomdb-schema/src/lib.rs statically rejects any migration step that would populate logical_id on an existing canonical row. It is a LEXICAL scanner (comment-strip, string-literal elision, quoted-identifier unwrap, schema-qualifier normalisation, statement split on \";\") and therefore cannot be complete against SQL grammar. codex §9 produced FOUR same-family [P2] evasions across three fix rounds — schema-qualified names, UPDATE OR <conflict-action>, leading-CTE, and subquery-in-an-earlier-SET-expression. The first three are CLOSED; the fourth and the shapes below are KNOWN-OPEN and recorded in the function doc comment under \"# Known limits\": subquery in an earlier SET expression truncating the SET clause, a rename round-trip, and a CREATE TABLE AS SELECT swap. The run halted at the orchestration circuit-breaker (3 fix rounds on one finding) — a 4th round buys a 5th [P2]; Steward ruled ACCEPT the residual. WHY THAT IS SAFE: the requirement is NOT proven by the static guard. It is proven by the dynamic guard in fathomdb-schema/tests/slice25_logical_id_pin.rs — migrating_to_head_never_populates_logical_id_on_an_existing_node/_edge snapshot identity ROWS before and after migrating a real mixed anonymous/governed corpus through the whole ladder to head, and assert via fn null_to_not_null that NULL -> NOT NULL transitions == 0 plus byte-identical IdSpace::to_prefixed(). That compares rows, not SQL text, so it catches EVERY evasion the lexical guard misses, whatever its shape. D1 is defence-in-depth and a fast authoring-time fail; D2 is the proof. DURABLE CAVEAT FOR A FUTURE SLICE/RELEASE: do not treat a green static guard as evidence the pin holds, and do not delete or weaken the D2 whole-ladder tests to speed up the schema suite — they are the only shape-independent enforcement. If anyone wants the static guard to be actually complete, the durable fix is to parse the migration SQL with a real SQLite grammar rather than extend the lexical scanner; that is a net-new dependency and was explicitly out of scope for 0.8.20."}
{"ts": "2026-07-26T18:35:30.715Z", "seq": 95, "kind": "open-question", "summary": "OPEN QUESTION for the HITL and for consumer adoption: the dense arm is now explicitly scoped to the resolve_source_type LOCKED vocabulary - email, article, paper, meeting, note, todo, doc, plus edge_fact. Slice 20c fix-2 closed a codex P1 by restricting vector-kind enrolment to commit-able kinds (kind_is_vector_committable delegates to resolve_source_type(kind).is_ok(), so the two cannot drift). The alternative - teaching the vector writer arbitrary node kinds - was NOT taken because it reaches into Pack-1 partition-key semantics locked by the HITL in dev/design/0.7.0-vector-quant-pack1.md D3. CONSEQUENCE a consumer must know: a workspace using entity kinds outside that list (Memex, for example) gets FTS and lexical search for them but SILENTLY NO VECTORS - no typed error, no failure row, and readiness still reports ready, which is correct because an unenrolled kind is not outstanding work. This is that kind pre-slice status quo restored, not a new regression, but 20c is the first release where declaring a vector projection makes the boundary observable. The question to settle: should the source-type vocabulary grow, should it become consumer-extensible, or should declaring a vector projection over an unsupported kind report something rather than be silent? Growing it is a Pack-1 decision under HITL lock, so a fix round may not take it.", "area": "engine/vector-projection", "decider": "orchestrator", "id": "TC-66", "priority": "p2", "status": "open", "refs": ["git:1b0cf514", "tc:TC-65"]}
{"ts": "2026-07-26T18:36:45.159Z", "seq": 96, "kind": "open-question", "summary": "REISSUE of the item mis-filed as TC-66 at seq 95 (id race with a concurrent Slice-25 orchestrator - TC-66 was already taken by the TC-11 migration-guard observation at seq 94). OPEN QUESTION for the HITL and for consumer adoption: the dense arm is now explicitly scoped to the resolve_source_type LOCKED vocabulary - email, article, paper, meeting, note, todo, doc, plus edge_fact. Slice 20c fix-2 closed a codex P1 by restricting vector-kind enrolment to commit-able kinds (kind_is_vector_committable delegates to resolve_source_type(kind).is_ok(), so the two cannot drift). The alternative - teaching the vector writer arbitrary node kinds - was NOT taken because it reaches into Pack-1 partition-key semantics locked by the HITL in dev/design/0.7.0-vector-quant-pack1.md D3. CONSEQUENCE a consumer must know: a workspace using entity kinds outside that list (Memex, for example) gets FTS and lexical search for them but SILENTLY NO VECTORS - no typed error, no failure row, and readiness still reports ready, which is correct because an unenrolled kind is not outstanding work. This is that kind pre-slice status quo restored, not a new regression, but 20c is the first release where declaring a vector projection makes the boundary observable. The question to settle: should the source-type vocabulary grow, should it become consumer-extensible, or should declaring a vector projection over an unsupported kind report something rather than be silent? Growing it is a Pack-1 decision under HITL lock, so a fix round may not take it.", "area": "engine/vector-projection", "decider": "orchestrator", "id": "TC-67", "priority": "p2", "status": "open", "refs": ["git:1b0cf514", "tc:TC-65", "seq:95"]}
{"ts": "2026-07-26T18:37:08.517Z", "seq": 97, "kind": "observation", "summary": "RESTORED STATE after an id collision - this id belongs to the TC-11 static-migration-guard observation from Slice 25 (R-20-SUR), NOT to the vector-kind vocabulary question. That content is REISSUED as TC-67. Root cause: the Slice-20c orchestrator verified TC-66 was free, and a CONCURRENT Slice-25 orchestrator appended this item at seq 94 before the 20c write at seq 95; because state is the fold-to-latest per id, seq 95 silently overwrote it. Same failure shape as the TC-54 collision at seq 81/87, now demonstrated to recur across CONCURRENT orchestrators rather than a stale tail read - re-checking immediately before the write is NOT sufficient. Tooling fix stands (TC-53/TC-54): ledgerwrite should reject a NEW caller-supplied id that already exists unless an explicit update flag is passed. Original state below, unchanged and still OPEN. TC-11 logical_id pin: the STATIC migration guard is a lexical scanner, not a SQL parser — known-open evasion shapes remain; the DYNAMIC whole-ladder test (D2) is the load-bearing, shape-independent proof", "area": "schema-migrations", "epistemic": "verified", "id": "TC-66", "owner": "unassigned", "priority": "P3", "status": "open", "refs": ["git:83b1c818", "plan:dev/plans/plan-0.8.20.md", "seq:94", "seq:95", "tc:TC-67"], "body": "Landed at 0.8.20 Slice 25 (R-20-SUR). check_migration_logical_id_pin in src/rust/crates/fathomdb-schema/src/lib.rs statically rejects any migration step that would populate logical_id on an existing canonical row. It is a LEXICAL scanner (comment-strip, string-literal elision, quoted-identifier unwrap, schema-qualifier normalisation, statement split on \";\") and therefore cannot be complete against SQL grammar. codex §9 produced FOUR same-family [P2] evasions across three fix rounds — schema-qualified names, UPDATE OR <conflict-action>, leading-CTE, and subquery-in-an-earlier-SET-expression. The first three are CLOSED; the fourth and the shapes below are KNOWN-OPEN and recorded in the function doc comment under \"# Known limits\": subquery in an earlier SET expression truncating the SET clause, a rename round-trip, and a CREATE TABLE AS SELECT swap. The run halted at the orchestration circuit-breaker (3 fix rounds on one finding) — a 4th round buys a 5th [P2]; Steward ruled ACCEPT the residual. WHY THAT IS SAFE: the requirement is NOT proven by the static guard. It is proven by the dynamic guard in fathomdb-schema/tests/slice25_logical_id_pin.rs — migrating_to_head_never_populates_logical_id_on_an_existing_node/_edge snapshot identity ROWS before and after migrating a real mixed anonymous/governed corpus through the whole ladder to head, and assert via fn null_to_not_null that NULL -> NOT NULL transitions == 0 plus byte-identical IdSpace::to_prefixed(). That compares rows, not SQL text, so it catches EVERY evasion the lexical guard misses, whatever its shape. D1 is defence-in-depth and a fast authoring-time fail; D2 is the proof. DURABLE CAVEAT FOR A FUTURE SLICE/RELEASE: do not treat a green static guard as evidence the pin holds, and do not delete or weaken the D2 whole-ladder tests to speed up the schema suite — they are the only shape-independent enforcement. If anyone wants the static guard to be actually complete, the durable fix is to parse the migration SQL with a real SQLite grammar rather than extend the lexical scanner; that is a net-new dependency and was explicitly out of scope for 0.8.20."}
{"ts": "2026-07-26T19:52:57.605Z", "seq": 98, "kind": "observation", "summary": "MEASURED: reopening a database that has ANY enrolled vector kind costs 90 SYNCHRONOUS embeds inside Engine::open - the 0.8.18 vector-equivalence probe. This was previously unreachable in practice because nothing enrolled a node kind in production; Slice 20c gave _fathomdb_vector_kinds its FIRST production enrolment path (configure_projections declaring searchable-to-vector), so from 0.8.20 onward a normal consumer workspace can hit it on every open. Found while investigating the no-embedder enqueue question in 20c fix-3 and NOT fixed there (out of slice scope). Consequence: open latency becomes proportional to the equivalence-probe batch rather than constant, on a path consumers hit at startup. Worth deciding whether the probe should be sampled, made async, cached against a fingerprint, or skipped when the embedder identity is unchanged since the last open.", "area": "engine/open-path", "decider": "orchestrator", "id": "TC-68", "priority": "p2", "status": "open", "refs": ["git:2b162287", "tc:TC-65"]}
{"ts": "2026-07-26T19:52:57.633Z", "seq": 99, "kind": "observation", "summary": "The EDGE projection path has the same unconditional-enqueue shape as the node path that Slice 20c fixed, and the un-stranding helper does not reach it. project_canonical_edge_row enqueues unconditionally, while reenqueue_stranded_vector_rows joins canonical_nodes ONLY - so gating edges alone would merely trade one permanent terminal for another rather than making edge embed work recoverable. PRE-EXISTING since G11; NOT introduced by 20c and deliberately not fixed there (20c scoped itself to the node path its own enrolment change created). A future slice that wants edge bodies to be recoverable after a no-embedder or failed session must extend the stranded-row query to canonical_edges at the same time as it gates the edge enqueue - the two must land together or the fix is a no-op.", "area": "engine/projection-runtime", "decider": "orchestrator", "id": "TC-69", "priority": "p3", "status": "open", "refs": ["git:2b162287", "tc:TC-61"]}
{"ts": "2026-07-26T21:47:56.012Z", "seq": 100, "kind": "observation", "summary": "PRE-EXISTING SQLITE_BUSY_SNAPSHOT race between a projection worker committing its terminal and a caller open write, already documented in slice20_dense_readiness.rs. MEASURED during 20c fix-4 while probing whether the embed retry ladder could be shortened for edges: shortening ONLY the edge ladder moved the worker terminal-commit into the window of a caller write and took consolidate_provider from 0 of 48 failures to 8 of 48 under 6-way concurrency (origin-main baseline is 0 of 48). The retry ladder was therefore LEFT AS SHIPPED and the race was not fixed - 20c deliberately did not touch it. CAVEAT for any future slice that wants to shorten, remove, or make adaptive the projection retry ladder (DEFAULT_PROJECTION_RETRY_DELAYS_MS): the ladder is currently acting as an accidental backoff that masks this race, so changing it WILL surface SQLITE_BUSY_SNAPSHOT failures under concurrency. Fix the race first, then the ladder - not the other way round.", "area": "engine/concurrency", "decider": "orchestrator", "id": "TC-70", "priority": "p2", "status": "open", "refs": ["git:1f46438b", "tc:TC-64"]}
{"ts": "2026-07-26T22:43:48.228Z", "seq": 101, "kind": "todo", "summary": "OPEN codex section-9 [P2] from Slice 20c round 5, DELIBERATELY NOT FIXED - the orchestration circuit-breaker was reached at 5 fix rounds. vector_projection_declared keys off the stored vector sub-object ALONE (vector_declared = 1) rather than requiring the searchable role, so declaring {roles: [filterable], vector: true} in a session WITH a live embedder will enrol vector kinds, backfill, and make later writes enqueue embeddings - even though that combination is documented as inert-but-round-trippable (the shipped Slice-15d contract pinned by vector_subobject_is_stored_not_built). The shipped inert test does not catch it because it runs without a live embedder. FIX: make the predicate require the searchable role, i.e. use the same active searchable-to-vector predicate everywhere. NOTE the blast radius - vector_projection_declared gates all three of the forward backfill path, the drop inverse, and late enrolment, so the change must be re-verified across all of them together. IMPACT is wasted embed work and unexpected vectors at rest for a projection meant to be inert; it is NOT a false-ready and NOT data loss, which is why it was judged lower severity than the four findings closed in rounds 1-4. Transcript: dev/plans/runs/codex/0.8.20/slice-20c-fix-5-rereview-20260726T224040Z.log", "area": "engine/projection-registry", "decider": "orchestrator", "id": "TC-71", "priority": "p2", "status": "open", "refs": ["git:30ab62df", "tc:TC-65"]}
{"ts": "2026-07-27T00:00:11.891Z", "seq": 102, "kind": "observation", "summary": "MEASURED: cargo test --workspace --no-fail-fast is NOT reliably green on this machine, on main as well as on a slice branch, so a single green workspace run is WEAK evidence. Controlled comparison run during the Slice 20c landing gate, same script, sequential, same load: the merged 20c tree failed 1 of 3 runs and plain main failed 1 of 3 runs. Each failure was a DIFFERENT concurrency-sensitive test - slice15e undeclared_after_concurrent_drop_is_typed_invalidfilter_not_storage_race on the merged side, t_s34_dump_mutations_lock_held_exits_71 on main, and lifecycle_observability in an earlier ad-hoc run - and EVERY one of them passes reliably in isolation (slice15e 6 of 6, lifecycle_observability 3 of 3). Conclusion: this is parallel-execution contention in the full-workspace run, NOT a regression from any one slice, and it is the same family as TC-70. CONSEQUENCE for future slices: do not treat a single red full-workspace run as a blocker without re-running the failing suite in isolation and comparing against a main control, and do not treat a single green run as proof. If the workspace gate is to be load-bearing for release DoD it needs the underlying races fixed or the run serialised.", "area": "testing/ci", "decider": "orchestrator", "id": "TC-72", "priority": "p2", "status": "open", "refs": ["git:841c307b", "tc:TC-70"]}
{"ts": "2026-07-27T00:03:17.543Z", "seq": 103, "kind": "todo", "summary": "SLICE 21 SCOPE PLACED (HITL 2026-07-26) — a reserved-gap slice in the 20 band, off the critical path to Slice 30, to land BEFORE publish. Two items, both engine/test hygiene. (1) TC-57 characterize-then-fix per the ruling at steward seq-111: governed writes (logical_id Some) racing the projection worker return EngineError::Storage, reported 7 of 8 baseline runs, never with logical_id None, suspected SQLITE_BUSY_SNAPSHOT; reproduce and characterize FIRST, then fix in-release. (2) ac_002 ORACLE REPLACEMENT, not an engine fix and not a new test. Triage verdict: ac_002 and lifecycle_observability.rs are the SAME finding (the file contains the test) so this was two flakes not three, and both flakes are class (A) harness artifacts with NO engine defect and NO TC-57 corroboration — ac_029 writes logical_id None while TC-57 is logical_id Some, a different failure mode. ac_002 Part 1 (allowlist inside the DB parent) is hermetic and good, KEEP IT. Part 2 scans PWD/HOME/XDG_*/TMPDIR to depth 6 for any relative path containing the substring fathomdb: it is simultaneously TOO BROAD (fails on files other processes create; already false-positived twice, once on a sibling repo file, TC-58) and TOO NARROW (misses a log at ~/.cache/engine.log or anything without fathomdb in its path or deeper than 6 levels) — so the AC protecting users from a littered filesystem has a false-negative hole. Fix by isolation not detection: redirect HOME/XDG_*/TMPDIR/CWD into a per-test sandbox and assert the sandbox holds nothing but the allowlist, which also closes the false-negative. Prefer a child process with a scrubbed env since std::env::set_var is racy across parallel test threads. DO NOT patch the substring/root-stripping logic a third time — it has already been patched twice (0.8.9 Slice 20 F-9, then the longest-matching-root fix) and that is the Slice-25 lexical-guard lesson: when an oracle keeps producing new evasions, change the oracle.", "decider": "hitl", "id": "TC-73", "status": "open", "refs": ["seq:111", "seq:114"]}
{"ts": "2026-07-27T14:29:49.663Z", "seq": 104, "kind": "decision", "summary": "WORKSPACE-GATE EVIDENCE RULE RULED (HITL 2026-07-27): option (a) - SERIALIZE the Slice-40 workspace test gate AND keep a NON-BLOCKING parallel run that reports without gating. Race-hunting itself defers to 0.8.21. Chosen over (b) serialize-only because (b) buys the same determinism while destroying the only signal that these races exist, and TC-57 already proves at least one race in this family is a REAL engine defect rather than a harness artifact. Basis at seq-116: the full-workspace run failed 1 of 3 on merged-20c AND 1 of 3 on plain main, a different concurrency test each time, every one green in isolation - so today a single green proves nothing and a single red blocks nothing. Serializing buys DETERMINISM, not correctness: the races stop being exercised rather than being fixed, which is why the non-blocking arm is load-bearing and not decoration. WORK OWED at Slice 40. UNMEASURED and worth measuring before committing: serialized wall-clock. NOT COVERED by this ruling and still unclassified: OOS-12 slice15e undeclared-after-concurrent-drop has NOT been triaged harness-vs-real and sits on the registry path 0.8.20 publishes.", "decider": "hitl", "id": "TC-74", "priority": "p2", "release": "0.8.20", "slice": "40", "status": "open", "refs": ["tc:TC-72", "tc:TC-16", "seq:116"]}
{"ts": "2026-07-27T14:29:49.691Z", "seq": 105, "kind": "decision", "summary": "ENGINE FIX-ROUND CAP RULED (HITL 2026-07-27): option (a) - the numbers are CONFIRMED as same-finding bound STAYS AT 3, per-slice TOTAL rises 6 to 10 for slices touching fathomdb-engine/src, with a MANDATORY STEWARD CHECK-IN AT 6 at which the orchestrator reports what each round found and the Steward decides continue / re-scope / escalate. Chosen over (c) no-fixed-total-with-judgement because an unbounded total removes the runaway backstop that caught TC-47, and the check-in already supplies the judgement (c) was reaching for. STEWARD DISCLOSURE MADE BEFORE THE RULING: these numbers were the Stewards invention, agreed to rather than authored by the HITL, and the disclosure was made while overturning was still cheap. Basis: in Slice 20c the same-finding rule NEVER fired - every round found a new distinct defect, each RED-first - so the binding constraint was the total and the breaker fired on a productive loop. WORK OWED: this rule is NOT YET IN FORCE. The durable homes still read 3 and 6 - dev/design/orchestration.md section 6 and dev/plans/prompts/0.8.x-RELEASE-ORCHESTRATOR-HANDOFF.md lines 159 to 163 - so every orchestrator commissioned before that lands gets the OLD bound. Amends seq-107 for engine slices only; supersedes nothing else in that rule.", "decider": "hitl", "id": "TC-75", "priority": "p2", "status": "open", "refs": ["seq:107", "seq:117"]}
{"ts": "2026-07-27T14:29:49.714Z", "seq": 106, "kind": "decision", "summary": "LIBRARY SLICE REQUIREMENT-ID RULED (HITL 2026-07-27): option (a) - Slice 31 Library Sweep 3 carries NO R-20-xx requirement id and is annotated explicitly as dependency hygiene and as a DELIBERATE DEPARTURE from the master F-12 pico precedent. Chosen over (b) mint R-20-LIB because (b) would convert a RECURRING hygiene programme into a one-off release requirement of 0.8.20, which is the category error F-12 was created to fix; the annotation preserves the precedent for the next Steward instead of silently overwriting it. Context: F-12 and the two-tier numbering model treat a Library Sweep as a PICO, label-only and never published, so placing this one in-ladder before publish is the departure being recorded. RE-OPEN TRIGGER: if the sqlite-vec PR 99 probe promotes that dependency into this slice, it stops being pure hygiene and option (b) becomes correct.", "decider": "hitl", "id": "TC-76", "release": "0.8.20", "slice": "31", "status": "open", "refs": ["tc:TC-73"]}
{"ts": "2026-07-27T14:29:49.736Z", "seq": 107, "kind": "decision", "summary": "RESERVED-GAP OVERFLOW TRIPWIRE RULED (HITL 2026-07-27): option (a) FOR NOW - keep the plan section 5 default, which halts to the HITL only on band overflow. Explicitly CONDITIONAL: the HITL ruled a for now and stated it DEPENDS ON WHETHER SLICES 21 AND 22 TURN OUT TO NEED MORE SLICES. Chosen over (b) halt-at-the-next-new-slice because the 20 band was sized as slack deliberately and (b) spends HITL attention on precisely what section 5 exists to handle without them; the growth so far came from HITL rulings, not from drift, so the tripwire is not the mechanism that has been failing. STATE AT RULING: the 20 band gaps 21 to 24 now hold 21 and 22, leaving TWO slots. RE-OPEN TRIGGER, and the Steward is to raise it unprompted: if Slice 21 or Slice 22 spawns two or more further slices, option (b) becomes correct and this returns to the HITL.", "decider": "hitl", "id": "TC-77", "priority": "p3", "release": "0.8.20", "status": "open", "refs": ["plan:dev/plans/plan-0.8.20.md"]}
{"ts": "2026-07-27T14:29:49.758Z", "seq": 108, "kind": "decision", "summary": "DEPENDABOT PR 153 setup-node 6 to 7 RULED (HITL 2026-07-27): option (a) FOR NOW - terminal hold, revisit after publish with the rest of the held set, with the HITL directing MONITOR FOR OPTION (c). No analysis is scheduled before publish. Chosen over (c) fold-into-the-Slice-40-publish-rehearsal because the npm leg published successfully at v0.8.9 on the CURRENT action version, so there is no evidence we are broken today, and (c) would add an unmeasured variable to the one rehearsal that most needs to be clean. UNRESOLVED AND DELIBERATELY LEFT SO: a subagent reads v7 as plausibly FIXING our npm OIDC publish path rather than threatening it, because v6 exports a placeholder NODE_AUTH_TOKEN that can stop npm attempting the OIDC exchange - that reading is ANALYSIS, NOT MEASUREMENT, on a path that has never run. MONITOR TRIGGER per the HITL: if the Slice-40 publish rehearsal fails on npm auth, option (c) becomes the diagnosis rather than a precaution, and this re-opens immediately.", "decider": "hitl", "id": "TC-78", "priority": "p3", "release": "0.8.20", "status": "open"}
{"ts": "2026-07-27T15:28:44.350Z", "seq": 109, "kind": "observation", "summary": "test_commission_manifest.sh arm 9d is RED on main today, and commission-manifest is an ALWAYS-ON CI job", "area": "scripts/tests", "epistemic": "verified", "id": "TC-79", "priority": "p2", "status": "open", "refs": ["git:23a436dc", "slice:0.8.20-30"], "body": "MEASURED during Slice 30, twice, independently. scripts/tests/test_commission_manifest.sh exits 1 on arm 9d with: FAIL arm 9d (real base line): base=[ base sha a2022957 (Slice 15 - the newest LANDED slice STRICTLY BEFORE Slice 20)]. Reproduced by the implementer in a throwaway clone at baseline 23a436dc, and reproduced again by the orchestrator in the PRIMARY checkout with main at 23a436dc and zero slice-30 changes applied. Slice 30 does not touch scripts/commission-manifest.sh or its test (git diff --stat over the slice range for both paths is empty), so this is PRE-EXISTING and not slice-caused. WHY IT MATTERS: commission-manifest is one of the always-on CI jobs in .github/workflows/ci.yml (no if:, no needs:), and it runs BOTH the generator with --verify-all AND this fixture suite as a recurrence guard. So CI on main is presumably red on this job right now. The arm expects the manifest generator to name the newest LANDED slice strictly before the next slice; the ladder has since been re-sequenced (30 runs first of the remainder, with 21/22/31 net-new), so the likely cause is that the re-sequence moved what next-slice and base resolve to and the fixture arm was not updated with it. NEEDS A HOME: it is not in any slice scope today. Natural candidates are the Slice 40 verification sweep (alongside the TC-16 determination, which is the same category of dead or stale release-gate assertion) or a DOC-HYGIENE pass. DO NOT let a green Slice-30 landing be read as evidence this is fixed."}
{"ts": "2026-07-27T15:28:58.568Z", "seq": 110, "kind": "caveat", "summary": "C-1 contract Q6(b) carries a stale parenthetical its OWN amendment banner contradicts - fix it at the NEXT amendment, never on its own", "area": "dev/design/record-lifecycle-protocol", "epistemic": "verified", "id": "TC-80", "priority": "p3", "status": "open", "refs": ["git:efa8d584", "slice:0.8.20-30", "file:dev/design/record-lifecycle-protocol/OPP-12-C1-converged-contract.md"], "body": "Found while deriving the R-20-H7 clause registry. Inside clause Q6(b) of OPP-12-C1-converged-contract.md the parenthetical says the C-2 typed carrier had its additive stable_id base landed but its typed swap NOT STARTED. Four lines below, the TC-11 amendment banner added at efa8d584 says the C-2 swap SHIPPED ALONE in 0.8.19 and is total without a surrogate. As-built agrees with the BANNER: SearchHit.id is a non-Option IdSpace (fathomdb-engine/src/lib.rs around line 1575) and IdSpaceKind is a closed three-variant enum. So the parenthetical is a point-in-time status note that its own clause has since superseded. IT IS NOT A GATE FAILURE and was deliberately registered in scripts/c1-conformance-pin.json as category PROSE, id C1-Q6B-C2-CARRIER-STATUS, with that reason written into the pin. Pinning it CHECKABLE would have held the breaking-pair publish on an already-superseded sentence - the exact hazard the efa8d584 amendment was made to remove. THE CAVEAT FOR WHOEVER FIXES IT: the R-20-H7 gate pins the sha256 AND git blob sha1 of this contract file. ANY edit to it, including a one-word prose correction, trips the gate and REQUIRES re-deriving the whole 45-clause registry against the new text and re-pinning under review. That is correct-by-design, not a bug. Therefore do NOT fix this parenthetical as a standalone docs commit - fold it into the next substantive contract amendment, where the re-derivation has to happen anyway. Same shape as the TC-52 allowlist _comment deferral."}
{"ts": "2026-07-27T15:29:19.891Z", "seq": 111, "kind": "caveat", "summary": "R-20-H7 gate maintenance contract: what it does NOT cover, the permanently-red assertion hazard, and the fixture coupling it introduced", "area": "scripts", "epistemic": "verified", "id": "TC-81", "priority": "p2", "status": "open", "refs": ["slice:0.8.20-30", "file:scripts/check-c1-conformance.sh", "file:scripts/c1-conformance-pin.json"], "body": "Three durable properties of the can-i-deploy gate landed at Slice 30 that a later agent must not rediscover the hard way. (1) THE PERMANENTLY-RED ASSERTION HAZARD IS A CLASS, not a one-off. A can-i-deploy assertion keyed to a release action that has NOT HAPPENED YET holds the release forever. It bit this contract once already: pre-efa8d584 clause Q6(b) mandated anonymous surrogate minting that the ratified TC-11 pin forbids ever implementing. Slice 30 avoided three MORE instances of the same class: (a) the contract Landing section says C-1 co-lands at 0.8.20, but the manifests still read 0.8.9 until the Slice-40 bump, so the gate asserts C-1 CODE PRESENT ON THE 0.8.20 LINE and deliberately NOT a manifest version equality; (b) the per-projection vector embedder override is RECORDED BUT INERT (one engine-wide pipeline serves all vector projections), so the gate asserts only absent-implies-engine-default; (c) fts tokenizer override likewise recorded but deliberately not honoured, pinned to the 0.9.x multi-field FTS work. DO NOT TIGHTEN ANY OF THESE THREE into the stronger nearby statement. (2) THE GATE IS STATIC-STRUCTURAL AND SAYS SO. It asserts symbols, tables, negative-space greps, and for behavioural clauses that the NAMED TEST still exists - it does NOT re-execute those suites. Consequence worth naming: TC-71 (vector_projection_declared ignores the searchable role) breaks NO verbatim C-1 clause, so when Slice 21 fixes it this gate will neither notice nor object. Do not read a green H7 as evidence about TC-71 either way. (3) FIXTURE COUPLING INTRODUCED DELIBERATELY. Wiring the gate into preflight --landing turned four sibling suites red (test_preflight_landing, test_check_ledgers, test_check_board_currency, test_check_governed_surface_pin) because their throwaway fixture repos carry no C-1 subject and the gate correctly exited 2 under anti-fail-open. Repaired with a seeder scripts/tests/lib/c1-conformance-fixture.sh that COPIES the real artifacts, unlike its synthetic sibling lib/governed-surface-fixture.sh. That departure is forced, not lazy: the implemented-assertion set is a constant inside the gate and the registry bijection is rigid in both directions, so any pin not registering exactly the 26 CHECKABLE ids reports MALFORMED. If the clause registry ever changes, those four suites inherit the change through the seeder - expect it and do not weaken the gate to decouple them."}
{"ts": "2026-07-27T18:36:23.835Z", "seq": 112, "kind": "consideration", "summary": "Slice 30 hit the 6-round fix cap on a NON-engine slice with every round finding a new distinct defect — the TC-75 argument, outside TC-75 scope", "area": "dev/design/orchestration.md", "epistemic": "verified", "id": "TC-82", "priority": "p1", "status": "open", "refs": ["slice:0.8.20-30", "tc:TC-75", "git:e8e35260"], "body": "ESCALATION CONTENT for the Steward, recorded durably so it survives the orchestrator returning. Slice 30 (R-20-H7 can-i-deploy gate) ran SIX codex section-9 rounds and halted at the per-slice total cap in orchestration.md section 6. The 3-same-finding bound NEVER fired. Each round found a NEW and DISTINCT false-negative class in the gate: round 1 regex shape plus open vocabulary; round 2 refinements of both (schema qualifiers, spellings outside a match arm); round 3 FILE SCOPE (negative probes read one lib.rs, missing a literal violation in a sibling module); round 4 SUBJECT BINDING (a probe bound to a file rather than to the thing the clause names); round 5 WRONG SUBJECT plus INACTIVE PROOF (fn_defined accepted a function with #[test] removed or #[ignore] added); round 6 a receiver-less fn_sig false green plus a nested-inner-attribute false RED. THE YIELD WAS REAL AND LARGE, which is why this matters: fix-4 alone found that EIGHTEEN OF TWENTY-SIX clauses had a demonstrable false green, every one closed with its own RED-first divergent fixture. The gate as first written would have been substantially decorative. Two weak probe kinds were deleted OUTRIGHT rather than left unused (file-scoped absent in fix-3, the count-based min in fix-4) and a third (fn_defined) was made unreachable for test paths. Fixture assertions grew 106 to 276. THE POINT FOR THE STEWARD: this is precisely the argument that produced TC-75 for engine slices - the total was cutting off a PRODUCTIVE loop rather than an unproductive one - but Slice 30 touches ZERO engine source, so the engine amendment does not reach it and the non-engine bound of 6 applied. The question to put to the HITL is whether the TC-75 reasoning should be re-expressed as a rule keyed to WHETHER ROUNDS ARE PRODUCTIVE (each finding new and distinct) rather than to WHICH DIRECTORY the slice touches. A gate/tooling slice that audits a whole contract surface has the same round-productivity profile as an engine slice. VERBATIM ROUND-6 FINDING (2), whose commit-message copy at e8e35260 was truncated by shell backtick substitution and is therefore recorded here in full: a pinned test file that later gains a nested module of the form - mod helpers open-brace hash-bang bracket cfg(feature = x) bracket ... close-brace - would be reported by fix-5 new file-level cfg check as wholly conditionally compiled, blocking CI and preflight even though the top-level pinned proofs still run. No such module exists today so nothing is red now; the fix is to scan only depth-0 header attributes. NEITHER round-6 finding makes the gate red on the current tree."}
{"ts": "2026-07-27T18:52:43.532Z", "seq": 113, "kind": "observation", "summary": "ORCHESTRATION TRAP, self-disclosed by the Slice-30 orchestrator and reusable across every future run: an until-loop waiter written as until ! pgrep -f \"codex exec review\" CANNOT EVER SATISFY ITS EXIT CONDITION, because pgrep -f matches against full command lines and the waiter subshell CONTAINS that pattern in its own command line - so it matches itself and spins forever. Consequence in this run was wasted wall-clock only; the orchestrator disclosed that every green it reported was taken from direct git and gate polling rather than from those loops, and the Steward independently re-ran all eight gates in the worktree and reproduced exit 0 on each, so NO conclusion depended on the broken waiter. The reason to record it is that the pattern would SILENTLY HANG any future orchestrator that copies it, and a hung orchestrator is exactly the 36-hour silent-stall failure class this program already carries a memory about. FIX SHAPE: a pgrep guard must exclude self - match the binary rather than the command line, e.g. pgrep -x codex or ps -C codex, or add a negative pattern. Per the standing rule that guardrail failures are fixed in TOOLING rather than by telling agents to be careful, the durable fix is a documented wait-for-codex helper in the orchestration playbook rather than a warning; every orchestrator hand-rolls this loop today. Adjacent to TC-53, the ledgerwrite shell-substitution hazard, in that both are caller-side shell defects that a tool or helper could eliminate outright.", "area": "tooling", "decider": "steward", "id": "TC-83", "priority": "p2", "status": "open", "refs": ["git:e8e35260"]}
{"ts": "2026-07-28T01:35:33.938Z", "seq": 114, "kind": "decision", "summary": "TC-79 RULED (HITL 2026-07-28): option A - fix NOW as cross-cutting CI hygiene, state-derived, NOT deferred to Slice 40", "area": "scripts/tests", "decider": "hitl", "epistemic": "verified", "id": "TC-79", "priority": "p2", "status": "resolved", "refs": ["seq:109", "seq:125", "git:10bb0e51", "tc:TC-81"], "body": "RULING: option A. Fix immediately as cross-cutting CI hygiene in a form that cannot expire again; do NOT defer to the Slice 40 sweep. TC-16 stays at Slice 40 per seq-118 - it is a different red and this ruling does not move it. Basis for fixing now rather than deferring: commission-manifest is an always-on CI job, so main is red through the Slice 30 landing plus 21/22/31 if deferred, and Slice 30 is the slice whose entire purpose is stopping a false green from reaching a first real breaking publish. The change is test-only, touches no engine source, no governed surface and not agent-test.sh, so it cannot collide with either unlanded branch. DIAGNOSIS CORRECTED - the cause recorded at seq-109 is WRONG and this supersedes it. seq-109 hypothesised the re-sequence moved what next-slice and base resolve to. It did not. The Steward reproduced arm 9d on main at 10bb0e51 and read the generated manifest directly: the base line is CORRECT and both of its assertions hold - base sha a2022957 and the label Slice 15 are present exactly as expected. The clause that actually fails is the THIRD one, the negative assertion that the whole manifest does not match HISTORICAL. Slice 20 has since LANDED at 841c307b, so the generator now correctly emits a regeneration banner reading HISTORICAL Slice 20 is ITSELF LANDED. THE TOOL IS RIGHT AND THE TEST IS STALE. The arm hardcodes Slice 20 as its live in-flight example and its own inline comment says Slice 20 is not yet landed so predecessor and max landed agree TODAY - it documented its own expiry date and shipped anyway. This is the TC-81 permanently-red-assertion hazard class recurring, of which efa8d584 was the first instance, not a one-off. REQUIRED FORM OF THE FIX: state-derived, not a retarget. Retargeting the hardcoded 20 to 30 was considered and REJECTED as option B - it is the same bomb with a later fuse and would re-red the day Slice 30 lands. Instead take the target slice from release-state next_slice, and assert that the HISTORICAL banner presence MATCHES the target slice landed state rather than forbidding the banner unconditionally. That is strictly stronger than today, checks both directions, and cannot expire. Quarantining or marking the arm known-red was not offered as an option: that is the vacuous-green pattern this repo keeps getting bitten by."}
{"ts": "2026-07-28T01:35:33.966Z", "seq": 115, "kind": "decision", "summary": "TC-82 RULED (HITL 2026-07-28): AMEND - the fix-N cap keys on ROUND PRODUCTIVITY, not on which directory a slice touches", "area": "dev/design/orchestration.md", "decider": "hitl", "epistemic": "verified", "id": "TC-82", "priority": "p1", "status": "resolved", "refs": ["seq:112", "seq:125", "tc:TC-75"], "body": "RULING: amend. The TC-75 engine amendment is re-expressed as a rule keyed to whether rounds are PRODUCTIVE - each round finding a new and distinct defect - rather than to whether a slice touches src/rust/crates/fathomdb-engine/src. Basis, measured not argued: Slice 30 touches ZERO engine source and nonetheless ran seven rounds in which every single round found a new and distinct false-negative class in the gate, the 3-same-finding anti-thrash bound never fired once, and fix-4 alone showed eighteen of twenty-six clauses had a demonstrable false green. Directory is a poor proxy for risk; a gate or tooling slice auditing a whole contract surface has the same round-productivity profile as an engine slice. WHAT MOVES AND WHAT DOES NOT: the same-finding bound stays at 3 and is unchanged - it is the anti-thrash rule and three attempts at one defect means the design is wrong, not the patch. The per-slice default total stays 6. What changes is the predicate that permits extension to 10: formerly the slice path, now round productivity, with the mandatory Steward check-in at 6 becoming UNIVERSAL rather than engine-only. Beyond 10 remains an HITL halt. This codifies what actually happened on Slice 30, where the Steward check-in at 6 occurred at seq-124 and authorised a bounded seventh round."}
{"ts": "2026-07-28T01:35:33.989Z", "seq": 116, "kind": "decision", "summary": "TC-84 RULED (HITL 2026-07-28): a mid-flight SendMessage steer does NOT count against the fix-N round cap; the residual is ACCEPTED-KNOWN", "area": "dev/design/orchestration.md", "decider": "hitl", "epistemic": "verified", "id": "TC-84", "priority": "p2", "status": "resolved", "refs": ["seq:125", "tc:TC-75", "tc:TC-82"], "body": "The step-7 question carried by the 2026-07-27-A hand-off as open decision 3, minted its own id here so the durable rule homes can cite it. RULING: a mid-flight SendMessage steer to an in-progress fix-N round does NOT count as a round and does not advance the cap counter. RECORDED IN THE OPEN - this is the OPPOSITE of the Steward recommendation, which was that drip-feeding inside one round should count. The Steward accepts the ruling and does not re-litigate it. ACCEPTED-KNOWN RESIDUAL, recorded so no future reader mistakes it for an oversight: because the counter advances only on rounds, an orchestrator can serially steer inside a single round and never trip the breaker. The cap therefore bounds ROUNDS, not correction volume. Anyone reasoning about the breaker should read it as a bound on review-verdict cycles, not on how much correction flows in one cycle. CONSEQUENCE: Option B Phase 2 agent-seat hardening is UNBLOCKED - it was blocked on exactly this semantics question."}
{"ts": "2026-07-28T01:50:42.839Z", "seq": 117, "kind": "observation", "summary": "TC-79 CLOSED - arm 9d fixed state-derived and LANDED on main; CI red on main is cleared", "area": "scripts/tests", "decider": "steward", "epistemic": "verified", "id": "TC-79", "priority": "p2", "status": "closed", "refs": ["git:d39a1219", "git:430c5cfe", "seq:125", "tc:TC-81"], "body": "Landed d39a1219 (fix 430c5cfe + codex low fix d39a1219), fast-forwarded to main from worktree branch fix-tc79-commission-manifest cut at 2824d270. WHAT SHIPPED: arm 9d now derives the target slice from release-state next_slice, derives the expected base as the greatest landed slice strictly below it and that slice's landing sha, and asserts the HISTORICAL banner as an EQUIVALENCE - present if and only if the target is itself landed. Strictly stronger than the old unconditional prohibition because it now also catches a banner that fails to appear. NO SKIP PATH: null next_slice, absent predecessor, missing sha and unreadable state file all route to fail with a remedy message, never to a silent pass. STEWARD-VERIFIED INDEPENDENTLY, not taken from the subagent report. Diff scope confirmed one file. Suite rc=0 with 42 PASS and zero FAIL. I ran my own adversarial probes and one of MY probes was initially wrong and I am recording that: removing slice 20 from the landed set did NOT turn the arm red, because the generator reads the same state file, so both the expectation and the tool moved together. That is correct behaviour, not a false green, and it defines the arm's true scope - it catches TOOL drift from the state-derived contract, not a state file that is itself wrong. Corrected probe perturbing only the tool: with a landed target and the banner emission neutered, suite rc=1 with the expected want-present got-absent message; restored, rc=0. CODEX SECTION 9 RAN AND WAS CLEAN: no P1, no P2, one low - the banner was matched by its literal warning glyph, so a cosmetic ASCII-ification would have turned the arm red against a correct tool, the very TC-81 class this arm exists to stop. FIXED rather than overridden, because carrying a latent permanently-red assertion inside the commit that removes one is self-defeating. Now matches semantically on HISTORICAL plus the slice-specific claim; verified by probe that a glyph-only change stays green rc=0 while a semantic suppression still goes red rc=1. Transcript dev/plans/runs/codex/0.8.20/tc79-arm9d-20260728T014653Z.log. Gates on main after landing, each captured separately: test_commission_manifest 0, commission-manifest --verify-all 0, check-board-currency 0, check-release-state-views 0, agent-lint-md 0. RESIDUAL CARRIED, not blocking: when the 0.8.20 ladder completes and next_slice goes null, this arm goes RED LOUDLY telling the operator to re-point it. That is the mandated behaviour over a silent skip, but the release literal 0.8.20 is the last literal in the arm and a Steward decision is owed at release close - re-point it, or teach it to fall back to the greatest landed slice."}
{"ts": "2026-07-28T03:26:59.727Z", "seq": 118, "kind": "decision", "summary": "TC-85 RULED (HITL 2026-07-28): Option B Phase 2 AUTHORIZED - option (a), wire the agent-seat guard now via agent frontmatter, not settings.json", "area": ".claude", "decider": "hitl", "epistemic": "verified", "id": "TC-85", "priority": "p2", "status": "resolved", "refs": ["seq:127", "tc:TC-84"], "body": "Minted at close time for a decision that was raised in session and had no repo home - it existed only in the 2026-07-27-A hand-off section 4 as open decision 3s consequence and in this conversation. RULING: option (a). Rebase agent-seat-hardening onto the landed Slice 30 and wire the hook now, as cross-cutting work, rather than deferring it into DOC-HYGIENE-3. WIRING MECHANISM IS PART OF THE RULING: agent frontmatter, NOT project-global .claude/settings.json. That was the Phase-1 design finding - a PreToolUse hook can identify a spawned subagent because agent_type ships with agent_id, but cannot identify a main-thread /steward or /orchestrate session, and frontmatter scopes the guard to the seats it can actually identify without turning it into a project-global hook. The Phase-1 hard boundary that settings.json stays untouched therefore HOLDS. BASIS FOR NOW RATHER THAN LATER, in the HITLs decision: the branch at 07311772 is not merged and already conflicts with landed Slice 30 in scripts/agent-test.sh, where both sides insert a run_capped block at line 49; the conflict only widens as 21, 22 and 31 land, and until it is wired the 113 assertions in scripts/tests/test_seat_path_guard.sh are dead code. Conflict resolution is to KEEP BOTH blocks. UNBLOCKED BY TC-84 - the steer-is-not-a-round ruling was the only thing Phase 2 was waiting on. Options rejected: defer to DOC-HYGIENE-3, land Phase 1 only, abandon."}
{"ts": "2026-07-28T04:28:42.904Z", "seq": 119, "kind": "observation", "summary": "CODEX TRANSCRIPTS CAN CARRY FOREIGN SESSION DATA INTO A PUBLIC REPO - caught before landing, no prior exposure, needs a tooling guard", "area": "dev/plans/runs/codex", "decider": "steward", "epistemic": "verified", "id": "TC-86", "priority": "p1", "status": "open", "refs": ["git:2810fe94", "git:c58ce2fb", "tc:TC-85"], "body": "MEASURED, NOT HYPOTHETICAL. The ASH Phase-2 round-1 codex transcript arrived carrying 216 lines of raw Claude Code session JSONL. codex, running under --dangerously-bypass-approvals-and-sandbox, had run rg across ~/.claude and slurped the results into its own stdout, which the Steward then tee-d to a file destined for dev/plans/runs/codex/ - a tracked path. Those lines held conversation content from THREE PROJECTS OTHER THAN THIS ONE: agent-aware, local-unifi-openwrt, memex. github.com/coreyt/fathomdb is PUBLIC (gh repo view --json visibility -> PUBLIC), so committing them would have published another projects sessions, including a home-network project. CAUGHT BEFORE LANDING and redacted, with an in-file banner naming the count, the projects and the reason; the review verdict and all reasoning were preserved. NO PRIOR EXPOSURE, measured rather than assumed: git grep -l for lines beginning /home/coreyt/.claude/projects/ over dev/plans/runs/codex/** returned ZERO already-committed files. This was a first occurrence. WHY IT WILL RECUR WITHOUT A GUARD: the mechanism is structural, not a one-off mistake. Every codex section 9 review in this repo is invoked with --dangerously-bypass-approvals-and-sandbox, which is what lets it read outside the repo at all; TC-RUBRIC-7 then requires the terminal transcript to be persisted under a TRACKED path; and nothing between those two steps inspects what the transcript contains. Any reviewer prompt that makes codex search broadly for context can reproduce this, and the person committing is looking at a verdict at the end of a multi-megabyte file. Per the standing principle that a guardrail failure is fixed in the tooling and not with a be-careful note, the disposition should be a mechanical check rather than guidance. PROPOSED, NOT DECIDED - the Steward is bringing this to the HITL: a pre-commit or agent-lint check that HARD-fails when any file under dev/plans/runs/codex/ contains lines matching the home-slash-.claude-slash-projects shape, or more generally any absolute path into a users Claude Code state directory. Cheap, mechanical, and it fails closed. Natural home is DOC-HYGIENE-3, which is already sequenced after Slice 31 and already carries the TC-53 ledgerwrite id fix. NOT placed by the Steward - placement is an HITL call."}
{"ts": "2026-07-28T15:45:26.345Z", "seq": 120, "kind": "decision", "summary": "TC-86 PLACED (HITL 2026-07-28): approach approved as proposed; sequenced FIRST, ahead of Slice 21, not deferred to DOC-HYGIENE-3", "area": "dev/plans/runs/codex", "decider": "hitl", "epistemic": "verified", "id": "TC-86", "priority": "p1", "status": "build-authorized", "refs": ["seq:129", "git:fcaef280"], "body": "The Steward raised TC-86 and proposed but did not place it; the HITL has now approved the approach as proposed and ruled it runs FIRST. New order supersedes F-35: TC-86 then 21 then 22 then 31 then DOC-HYGIENE-3 then batched governed-surface then 40, sequential. Cross-cutting, so no slice number, no R-20-xx id and no pico label - the DOC-HYGIENE-3 treatment. Two layers approved: capture-time redaction wrapper around the codex exec tee, so the foreign lines never enter the artifact; plus scripts/check-transcript-hygiene.sh as the landing gate, dual-homed into agent-test.sh, preflight --landing and an always-on CI job. Detect home-anchored absolute paths into a users Claude Code state directory, not relative .claude/ references. On trip, fail loudly and offer --redact that preserves the transcript with an audit banner - never delete, because TC-RUBRIC-7 needs the evidence. RED-first with a positive control so the detector cannot go vacuous. Out of scope by ruling: gitignoring transcripts, dropping the codex bypass flag, and history rewriting."}
{"ts": "2026-07-28T17:39:55.849Z", "seq": 121, "kind": "observation", "summary": "TC-86 CLOSED and LANDED - two-predicate gate dual-homed, capture-time filter wired, and a LIVE exposure remediated: 33 private project names removed from 7 committed transcripts", "area": "scripts", "decider": "steward", "epistemic": "verified", "id": "TC-86", "priority": "p1", "status": "closed", "refs": ["seq:130", "tc:TC-86"], "body": "Landed on main. What began as a near-miss uncovered a real, already-public exposure. Seven committed transcripts carried directory inventories slurped by bypass-sandboxed codex reviewers; one was ls of the users whole projects directory, which had published 33 PRIVATE PROJECT NAMES including a client-named one to a PUBLIC repo for roughly three weeks. All removed via the tools own --redact, preserving each transcript as evidence per TC-RUBRIC-7 and stating counts and reasons without re-typing the names. STEWARD-VERIFIED ACROSS ALL TRACKED FILES, not the edited blocks: ten representative names each git grep to ZERO; ordinary words bosun wake memex local were checked individually and legitimate prose left untouched; verdicts and review reasoning survive in every redacted file. THE GATE HAS TWO PREDICATES sharing one lib so filter and gate cannot drift - agent-state paths, where FOREIGN hard-fails including the tool-results shape and OWN-PROJECT warns with the self-exemption PRINTED ON EVERY RUN rather than hidden in a regex; and out-of-repo directory inventories, hard-fail with no own/foreign split. Dual-homed into agent-test.sh, preflight --landing and an always-on CI job. Capture-time layer folded into the already-mandated codex-nostdin.sh wrapper with the exit code preserved via PIPESTATUS. A SUBAGENT REPORTING ERROR IS RECORDED: fix-3 claimed the foreign names were all at zero; that did NOT reproduce - it had verified against the block it edited rather than the whole file, and the Steward caught it, which is what surfaced the far larger ls-projects exposure underneath. HONEST LIMITS: redaction does NOT unpublish, the names were public for three weeks and history forks and caches retain them, so this stops propagation only and a history rewrite was considered and NOT taken; the gate cannot see a bare directory listing with no output block, only --redact aimed by a human; preflight does not relay WARN lines on a clean tree. CODEX SECTION 9 WAS NOT RUN on this branch - disclosed, not omitted - under an explicit HITL directive to finish, the acceptance criterion being mechanical and Steward-verified directly. Gates all 0: transcript-hygiene, its 223-assertion suite, preflight --landing, seat-path-guard, c1 fixtures, lint-md."}
{"ts": "2026-07-28T19:26:55.933Z", "seq": 122, "kind": "observation", "summary": "Do NOT triage a codex review by grepping [P1]/[P2] markers over the transcript - the tally is dominated by the CODEBASE'S OWN provenance comments, which use the identical format. Measured: fathomdb-engine/src/lib.rs alone carries 21 [P1] and 101 [P2] markers.", "area": "process", "decider": "steward", "epistemic": "verified", "id": "TC-87", "priority": "p2", "status": "open", "refs": ["tc:TC-71", "seq:131", "git:d3157113"], "body": "MEASURED, on the Slice 21 round-1 review (dev/plans/runs/codex/0.8.20/slice-21-review-20260728T184947Z.log, 228,792 bytes, CODEX_RC=0). The Steward tallied markers over the transcript as an independent cross-check on the orchestrator's triage and reported a floor of 3 [P1] / 25 [P2]. That interpretation was WRONG and the orchestrator corrected it. The true distinct finding set was 0 [P1] / 1 [P2]. TWO compounding causes, and the second is the durable one: (1) codex double-emitted its own finding block verbatim, so even its real output over-counts; (2) THE DOMINANT CAUSE - codex cat/rg'd lib.rs into the transcript, and this repo deliberately annotates fixes with their review provenance in the SAME syntax ('// 0.8.20 Slice 21 fix-1 (codex section 9 round 1 [P2], ledger TC-71)', '// fix-5 (codex section 9 round 4 [P2])'). Verified independently by the Steward against the branch: git show orch-0.8.20-s21:src/rust/crates/fathomdb-engine/src/lib.rs | grep -oE '\\[P[12]\\]' yields 21 [P1] and 101 [P2] IN THE SOURCE FILE ITSELF, before any review runs. Regions codex reads twice are counted twice. CONSEQUENCE: a marker tally over a transcript measures how much source codex echoed, NOT how many findings it raised, and it fails in the DANGEROUS direction - it inflates, so it manufactures phantom P1s and can push a slice toward a fix round it does not need or make a clean review look alarming. It also cannot be fixed by de-duplicating near-identical lines, because the noise is legitimately distinct source lines. WHAT TO DO INSTEAD: read codex's own verdict/'Review comment:' blocks, which are structurally distinguishable from echoed source, and reconcile any count against the orchestrator's triage rather than asserting it over the top. DO NOT 'fix' this by stripping provenance markers from source comments - they are load-bearing (they tell the next agent which review round produced a line, which is exactly how the TC-57 stale-517 comment trap was caught). The tallying method is what is broken, not the annotation convention. Applies to any future agent building an automated review-triage or a marker-count gate."}
{"ts": "2026-07-28T20:45:34.814Z", "seq": 123, "kind": "todo", "summary": "ledgerwrite updates a .seq SIDECAR that nothing makes you commit - stage the .jsonl alone and you strand it, which breaks preflight --landing on main for the NEXT agent, not yourself. Happened twice in one session. Place the fix in DOC-HYGIENE-3 beside TC-53.", "area": "tooling", "decider": "steward", "epistemic": "verified", "id": "TC-88", "priority": "p1", "status": "open", "refs": ["tc:TC-53", "tc:TC-87", "seq:131"], "body": "MEASURED, twice in the same session, by the same actor (the Steward), which is the point - discipline did not prevent the second occurrence twenty minutes after the first. MECHANISM: dev/agent-tools/ledgerwrite/ledgerwrite.py appends to <ledger>.jsonl AND writes the max seq to a sibling <ledger>.jsonl.seq sidecar. The natural git incantation after a ledger append is 'git add dev/steward/steward-ledger.jsonl' - the sidecar is a separate path, is not implied by it, and nothing warns. Both commits 41a81c17 (steward seq-131) and 3e660f95 (todos TC-87) staged the .jsonl and left the .seq behind. BLAST RADIUS, and why this is worse than a private mistake: the ledger-integrity check in scripts/preflight.sh --landing HARD-fails when a sidecar disagrees with max(seq), so a stranded sidecar breaks the LANDING GATE ON MAIN FOR EVERY SUBSEQUENT AGENT while the actor who caused it sees nothing wrong. It surfaced here only because the Slice 21 orchestrator hit exit 1 at its push and correctly refused to clear a ledger it had been told was not its own - a well-behaved halt is the ONLY reason this was caught rather than compounding. Verified at the time: working tree sidecars 131/122, committed 130/121, actual max(seq) 131/122. REMEDY OPTIONS, cheapest first, none implemented: (a) a pre-commit hook that refuses any commit staging a ledger .jsonl without its .seq sidecar - mechanical, catches it at the moment of the mistake, mirrors how the repo fixed other guardrail failures; (b) ledgerwrite itself runs 'git add' on both files after a successful append, so the pair can never separate; (c) drop the sidecar and have preflight derive max(seq) from the .jsonl directly, removing the duplicated state that can drift at all - the cleanest, but changes the integrity check's contract and needs a look at what else reads the sidecar. PLACEMENT: DOC-HYGIENE-3, beside TC-53 and the ledgerwrite duplicate-id hole - same file, same class of missing guard, one visit. DO NOT fix this by telling agents to remember the sidecar; that is the failure mode this repo has already ruled against (guardrail failures are fixed in tooling, not with a be-careful note)."}
{"ts": "2026-07-28T20:51:48.917Z", "seq": 124, "kind": "observation", "summary": "TC-57 CLOSED and LANDED in Slice 21 (77be504b). The race is fixed by BEGIN IMMEDIATE on the governed arm of commit_batch - AND the characterization CORRECTED this entry's own mechanism claim: it is plain SQLITE_BUSY (5), NOT SQLITE_BUSY_SNAPSHOT (517), at 10/10 not 7-of-8.", "area": "engine", "decider": "steward", "epistemic": "verified", "id": "TC-57", "priority": "p1", "status": "closed", "refs": ["git:77be504b", "seq:111", "tc:TC-72"], "body": "The ruled characterize-first ordering (steward seq-111, HITL 2026-07-26) paid for itself here and this is the durable lesson. THE CORRECTION: every prior write-up - this TC-57 entry, the anon_node comment in slice20_dense_readiness.rs, and the OOS-17 note in lib.rs - named SQLITE_BUSY_SNAPSHOT (517) as the mechanism. Measured, that is only ONE of two un-retryable exits from a lock promotion and it is NOT the one the engine takes. The engine returns plain SQLITE_BUSY (extended code 5) IMMEDIATELY, with the busy handler invoked ZERO times. A fix scoped to 'retry on 517' would have shipped INERT - passing review, closing the ticket, fixing nothing. Both code comments were corrected in-slice; this entry is the ledger half. MEASURED at 41a81c17: repro arm (logical_id Some) 10/10 failures, control arm (logical_id None) 0/10, failure message byte-identical across all ten runs - deterministic at this shape, not intermittent, and always the third write of a governed ingest loop. Prior claim was '7 of 8'. ROOT CAUSE: the governed branch of commit_batch READS BEFORE IT WRITES inside a BEGIN DEFERRED transaction, so it must promote a read lock to a write lock while the async projection worker is active; busy_timeout cannot save a promotion. FIX: R1 - BEGIN IMMEDIATE for the governed arm, taking the write lock up front. Verified to the characterization's own acceptance bar (doc section 1.4), not one lucky run: repro 0/10, control 0/10, three tc57_mechanism_* pins green, OOS-17 regression guard rc 0. That bar matters because of TC-72 - cargo test --workspace is not a stable signal in this repo, so a single green proves nothing on a concurrency fix. FULL CHARACTERIZATION: dev/design/0.8.20-tc57-write-race-characterization.md (519 lines, proven/not-proven labelled, five remedies enumerated with NONE implemented before the ruling). FOUR ITEMS CARRIED FORWARD, unfixed, recorded in STATUS-0.8.20.md section 18 and NOT part of this close: the ~50 percent baseline duplicate-embed rate (present in the anonymous arm too, so unrelated to TC-57), silently-discarded worker commit failures, Engine::transition's still-DEFERRED read-then-upgrade transaction, and R2 bounded retry pending an error-path restructure. Placement of those is a Steward call, pending."}
{"ts": "2026-07-28T20:51:48.945Z", "seq": 125, "kind": "observation", "summary": "TC-71 CLOSED and LANDED in Slice 21 (77be504b). vector_projection_declared now requires the searchable role - and codex round 1 caught that gating the DECISION without reconciling ALREADY-ENROLLED state left the entire harm intact on upgrade.", "area": "engine", "decider": "steward", "epistemic": "verified", "id": "TC-71", "priority": "p1", "status": "closed", "refs": ["git:77be504b", "git:3349344b", "tc:TC-87"], "body": "Left OPEN at the Slice 20c five-round circuit breaker, closed here. THE BASE FIX: the predicate keyed off the stored vector sub-object alone, so {roles:[filterable], vector:true} activated the dense arm instead of staying inert. It now answers through the typed registry (roles is persisted as a comma-joined sorted string and is NOT a trustworthy SQL predicate - a LIKE would match a forward-compat token that merely CONTAINS a role spelling). Re-verified across all three paths the predicate gates: forward backfill, drop inverse, late enrolment. The RED test runs with a LIVE embedder, because the shipped inertness test passes VACUOUSLY without one. THE FINDING THAT MATTERED (codex section 9 round 1, the single distinct finding of the review): the base fix corrected the DECISION to enrol but not the ALREADY-ENROLLED STATE. lib.rs:17550 gates the enqueue solely on _fathomdb_vector_kinds membership, and vector_kind_needs_enrolment tests kind_is_vector_indexed BEFORE reaching the new predicate, so an already-enrolled kind never sees the fix; the only un-enrol path fires on a before-and-not-after transition. Net: a database that had already run the old code KEPT EMBEDDING under inert declarations - TC-71's entire stated harm surviving its own fix, for exactly the users who already had it. Fixed at open time (RED f70f00bf, GREEN 3349344b), not overridden. TWO IMPLEMENTER CORRECTIONS TO THE ORCHESTRATOR'S OWN BRIEF, both load-bearing: (1) the legacy-database safety argument was WRONG - schema step 24 CREATES the registry table on every open, so a legacy DB boots with the table present but EMPTY and the table-exists probe never protects it; EXISTS(vector_declared = 1) is the sole protection, audited against the four at-risk suites; (2) reconciliation in apply_projection_config had to be 'before OR predicate', not a replacement - the predicate alone would have REGRESSED the shipped drop case, since dropping the last projection carrying a vector sub-object leaves the EXISTS condition false. Neither implies the other. ALSO: absolute embed counts are unusable as an oracle across a reopen, because run_vector_equivalence_probe re-embeds 45 references on any open with a non-empty _fathomdb_vector_kinds; every embed assertion is now a DELTA from a post-open snapshot."}
{"ts": "2026-07-28T20:51:48.967Z", "seq": 126, "kind": "observation", "summary": "TC-83 WIDENED: the self-matching-process trap is NOT pgrep-specific. pkill -f <pattern> matches its OWN command line too and kills the calling shell - observed live (exit 144) in the Slice 21 orchestrator, which had been briefed against the pgrep form and still hit the pkill form.", "area": "tooling", "decider": "steward", "epistemic": "verified", "id": "TC-83", "priority": "p2", "status": "open", "refs": ["tc:TC-83", "git:77be504b"], "body": "ORIGINAL TC-83: 'until ! pgrep -f \"codex exec review\"' can never exit, because pgrep -f matches the command line of the shell running the loop, so the wait hangs forever and silently stalls any orchestrator that copies it. WIDENING, measured during Slice 21: the same defect in pkill is WORSE than a hang. The Slice 21 orchestrator ran pkill -f \"waiter_exited\" to clean up its own bounded waiter; the pattern matched the pkill invocation's own command line and it killed its own shell (exit 144). It had been explicitly briefed against TC-83 in the pgrep form by the Steward and still hit it, because the entry named pgrep and the agent reached for pkill - so the write-up, not the agent, is what failed. THE GENERAL RULE, which is what should be carried forward: any -f / full-command-line process matcher (pgrep -f, pkill -f, ps | grep, killall against a pattern) matches the matching process ITSELF. Match the BINARY instead (pgrep -x codex, ps -C codex), or exclude self by pid. The Steward's own waiter in the same session used pgrep -x codex and was unaffected, which is the working pattern to copy. RELATED, same session, same class: 'rc=$?' placed after a pipe reports the LAST element's status, not the command's - the orchestrator read tail's exit as a merge's and a conflicted merge briefly registered as rc 0. Capture rc on the very next line, before any pipe or command substitution."}
{"ts": "2026-07-28T22:13:33.458Z", "seq": 127, "kind": "todo", "summary": "plan-0.8.20.md section 9's 'IMMEDIATE NEXT' pointer is HAND-WRITTEN while release-state-<release>.json already owns next_slice - it went stale at two consecutive landings in one session, and commission-manifest.sh cites that section as a brief's MANDATE anchor, so the staleness is COPIED INTO THE NEXT COMMISSION. Make it a generated view. DOC-HYGIENE-3.", "area": "docs", "decider": "steward", "epistemic": "verified", "id": "TC-89", "priority": "p2", "status": "open", "refs": ["tc:TC-88", "git:62486a01", "git:77be504b"], "body": "MEASURED TWICE IN ONE SESSION. (1) At the Slice 21 commission the section still said 'IMMEDIATE NEXT: Slice 30' although Slice 30 had landed at 9b3ed0e3; caught and fixed at 62486a01 BEFORE briefing, but only because the Steward happened to read the anchor the manifest cited. (2) Immediately after the Slice 21 landing it said 'IMMEDIATE NEXT: Slice 21' - stale again, within hours. WHY IT MATTERS MORE THAN ORDINARY DOC ROT: scripts/commission-manifest.sh resolves '## 9. Immediate next slice' as the {{MANDATE}} anchor for an orchestrator brief. The manifest's own CHECK 2 verifies that the anchor EXISTS, not that its CONTENT is current - a heading cannot rot, but the prose under it can, and does. So the generated, path-verified brief hands an orchestrator a hand-written pointer at the wrong slice, with the manifest's authority behind it. DOC-HYGIENE-2 already solved exactly this class for four other regions: release-state-<release>.json is the single writer and scripts/check-release-state-views.sh renders marker-delimited blocks from it, HARD-failing on any drift or hand-edit. next_slice is ALREADY a field in that file and is ALREADY kept true at every landing. This pointer is simply not yet one of the rendered views. REMEDY: add a generated_views entry rendering the 'IMMEDIATE NEXT' line (and ideally the remaining-ladder line beside it) from next_slice + remaining_ladder + the ladder titles, then the drift becomes mechanically impossible rather than a thing each Steward must remember at land time. INTERIM MITIGATION, already applied: a visible warning blockquote sits above the pointer telling the reader to re-verify against next_slice before commissioning, and the STATUS board's next-action cell now carries the same instruction. Those are notes, not controls - the generated view is the control. Same class as TC-88: a fact duplicated in two places with nothing checking they agree."}
{"ts": "2026-07-28T22:13:33.486Z", "seq": 128, "kind": "observation", "summary": "Engine::transition still runs a read-then-upgrade transaction under BEGIN DEFERRED - the SAME defect class TC-57 just fixed in commit_batch, in a second location, UNFIXED. Found by the Slice 21 orchestrator, carried forward in board section 18, no owning plan.", "area": "engine", "decider": "steward", "epistemic": "verified", "id": "TC-90", "priority": "p1", "status": "open", "refs": ["tc:TC-57", "git:77be504b"], "body": "TC-57's root cause was that the governed branch of commit_batch READ BEFORE IT WROTE inside a BEGIN DEFERRED transaction, forcing a read-to-write lock promotion while the async projection worker held the write lock - which SQLite refuses with plain SQLITE_BUSY (5), immediately, with the busy handler never invoked. Slice 21 fixed that ONE call site with BEGIN IMMEDIATE. Engine::transition has the SAME shape and was NOT touched. WHY IT WAS NOT FIXED IN SLICE 21: correctly out of scope - the slice's ruled legs were TC-57, ac_002 and TC-71, and widening an engine slice mid-flight is exactly the scope creep the repo forbids. It is recorded here rather than left in the board's close record because board section 18 is a CLOSE RECORD for a landed slice; an agent scoping 0.8.21 or a later concurrency slice will read the todos ledger, not the section 18 of a slice that closed months earlier. STATUS: not characterized - no repro exists for the transition path, and TC-57's own lesson is that the mechanism must be MEASURED before a fix is scoped (the prior write-ups named SQLITE_BUSY_SNAPSHOT/517 and were wrong, which would have made an obvious fix inert). Do NOT assume the commit_batch remedy transfers unexamined. PLACEMENT IS AN OPEN STEWARD/HITL CALL: it is a real defect on the concurrency path of a publishing release, but 0.8.20's remaining ladder is 22 -> 31 -> DOC-HYGIENE-3 -> 40 and none of those own it; the natural candidates are a 0.8.21 concurrency slice or a scoped addition, and adding scope to a ruled slice is an HITL decision, not a Steward one."}
{"ts": "2026-07-28T22:13:33.510Z", "seq": 129, "kind": "observation", "summary": "Two further vector/worker defects carried forward from Slice 21, both unfixed and unowned: (a) the ~50 percent BASELINE duplicate-embed rate, present in the ANONYMOUS arm too so NOT TC-57's doing, and (b) projection-worker commit failures silently discarded via 'let _ = commit_projection_outcomes(..)'.", "area": "engine", "decider": "steward", "epistemic": "verified", "id": "TC-91", "priority": "p2", "status": "open", "refs": ["tc:TC-57", "tc:TC-68", "git:77be504b"], "body": "Both surfaced during the Slice 21 investigation, both correctly left out of scope, both recorded in board section 18 - which is a close record, not a work queue, so they are lifted here. (a) DUPLICATE EMBEDS: roughly half of embeds at BASELINE are duplicates. Critically this reproduces in the ANONYMOUS arm as well, so it is NOT caused by the governed-write race TC-57 fixed and did not go away with it. Impact is wasted embed work - the same harm class as TC-71, at a different layer - and it is invisible to correctness tests because a duplicate embed produces the right answer, just twice. Note the interaction with TC-68 (Slice 22): run_vector_equivalence_probe already re-embeds 45 references on ANY open with a non-empty _fathomdb_vector_kinds, which is a separate known source of repeat embedding that TC-68 is scheduled to cache away; whoever takes this must separate the two before attributing a rate. (b) SILENTLY DISCARDED WORKER COMMIT FAILURES: the projection worker's commit path is invoked as 'let _ = commit_projection_outcomes(..)', so a failure to commit projection outcomes is dropped on the floor with no error, no log and no telemetry. Consequence: the worker can fail to persist its work and nothing anywhere reports it - and, as the Slice 21 orchestrator found the hard way, it also means measuring worker risk via 'failed' terminal states is STRUCTURALLY INCAPABLE of seeing this class, because the failure never reaches a terminal state at all. That second point is the durable one: any future attempt to quantify worker reliability by counting failed terminals will report a falsely clean number until this is fixed. (c) ALSO CARRIED, lower priority: TC-57 remedy R2 (bounded retry on the busy codes) remains unimplemented pending an error-path restructure; R1/BEGIN IMMEDIATE shipped instead and closed the measured defect, so R2 is an option not an obligation - see the five-remedy enumeration in dev/design/0.8.20-tc57-write-race-characterization.md section 7."}
{"ts": "2026-07-28T22:17:27.929Z", "seq": 130, "kind": "observation", "summary": "Requirement ids are never BACK-LINKED into the design docs they depend on, so commission-manifest.sh HARD-FAILED on Slice 22 (zero design docs matched TC-67/TC-68/R-20-VC) even though the design of record exists for every leg. Fixed for 22 by annotating 5 docs; the discipline gap is unfixed.", "area": "docs", "decider": "steward", "epistemic": "verified", "id": "TC-92", "priority": "p2", "status": "open", "refs": ["tc:TC-89", "tc:TC-50", "git:77be504b"], "body": "MEASURED at the Slice 22 commission. scripts/commission-manifest.sh 0.8.20 22 exited 1: 'ZERO design docs matched 0.8.20 Slice 22 (tokens: TC-67, TC-68, R-20-VC)' - its TC-37 vacuous-pass guard, working exactly as designed, refusing to emit a brief whose required-reading list would be empty. THE GUARD WAS RIGHT AND THE ALARM WAS REAL, but the underlying diagnosis is NOT 'the design does not exist'. It exists for all four legs and was found in minutes: TC-67 -> record-lifecycle-protocol/projection-registry-and-async-embed.md (registry/declaration semantics) plus 0.7.0-vector-quant-pack1.md (the home of resolve_source_type and of the D3 partition-key lock the leg must NOT touch); TC-68 -> 0.8.18-slice-5-vector-equivalence-probe.md (the probe itself); decision #18 -> errors.md (canonical error-type ownership); sqlite-vec #99 -> vector.md (vec0 storage). NONE of them mentioned TC-67, TC-68 or R-20-VC, because the manifest selects design docs by LITERAL TOKEN MATCH on TC/requirement ids and those ids were minted 2026-07-27, long after the docs were written. So the scan cannot see a dependency that runs BACKWARD IN TIME - which is the normal direction for a reserved-gap slice consuming older design. CONSEQUENCE, and it is bimodal: a slice with zero matches HARD-FAILS and cannot be commissioned at all (loud, recoverable - this case); a slice with ONE weak incidental match emits a brief whose required reading is thin but LOOKS complete (quiet, and worse). Slice 21 was the second shape: it matched exactly one doc, orchestration.md, on the token TC-71, and the manifest itself printed 'NO design doc mentions: ac_002, TC-57, R-20-CR'. That line was correct and was easy to read past. FIXED FOR SLICE 22, bookkeeping only: a 'Requirement traceability' blockquote added to each of the five docs naming the requirement/TC it serves, explicitly marked as added by the Steward with no design change. Verified: manifest now exits 0 with 5 docs, 3 classified / 2 unclassified, statuses reported honestly (locked / draft / UNREVIEWED with the TC-50 pointer) - nothing laundered as design-of-record that is not classified. THE DISCIPLINE GAP IS UNFIXED: nothing requires a back-link when a requirement id is minted. REMEDY OPTIONS: (a) at requirement-mint time (the plan's reserved-gap section), require the minting agent to back-link each new id into the design docs the leg depends on - cheapest, and it is a checklist item at exactly the moment the knowledge exists; (b) let the state file carry an explicit design_refs list per slice, so the manifest reads a curated set instead of guessing by token match - removes the guessing entirely; (c) leave selection by token but make a slice with FEWER THAN N matches WARN loudly rather than pass quietly, closing the dangerous quiet mode. (b) plus (c) is the strongest pair. Natural home: DOC-HYGIENE-3 beside TC-89, which is the same failure shape - a fact the manifest depends on, maintained by hand, with nothing checking it."}
{"ts": "2026-07-29T00:47:20.473Z", "seq": 131, "kind": "todo", "summary": "HITL IDEA (2026-07-28, steward seq-135): consider a \"Slice 4x\" that RESOLVES THE DEPENDABOT ITEMS BEFORE the 0.8.20 publish, rather than publishing to three public registries with 6 open advisories (2 HIGH) on the default branch. Slot, scope and existence UNDECIDED — an HITL call, not scheduled by this entry.", "area": "release", "decider": "hitl", "epistemic": "verified", "id": "TC-93", "priority": "p2", "status": "open", "refs": ["tc:TC-78", "seq:135"], "body": "RAISED BY THE HITL when ruling on the publish decision, in the words \"it might be correct to do a Slice 4x and resolve the dependabot items\". Recorded so the idea is not lost in a transcript. STATUS: NOT a ruling and NOT a schedule — the publish decision itself remains UNRULED (halts_run true) and the HITL explicitly said \"lets see where we are then\". CONTEXT: GitHub reports 6 vulnerabilities on the default branch (2 high, 2 moderate, 2 low), surfaced on every push this session as a remote notice. There are ~18 origin/dependabot/* branches open. The standing directive for this session is that Dependabot is NOT addressed outside the sequence, and TC-78 separately holds PR #153 (setup-node 6->7) to post-publish. NEITHER IS OVERTURNED by this entry. WHY IT MATTERS AT EXACTLY THIS POINT: 0.8.20 is the first REAL manifest bump in the line (0.8.9 -> 0.8.20) and Slice 40 publishes a coordinated breaking pair to crates.io, PyPI and npm. A pushed v* tag fires real publishes and is IRREVERSIBLE. So the publish gate is the moment the \"park Dependabot\" decision is actually tested: parking an advisory on an unpublished repo and shipping it to three public registries are different risk positions. WHAT WOULD HAVE TO BE DECIDED IF IT PROCEEDS: (1) does a 4x exist at all, or is triage folded into Slice 40 beside the TC-16 determination; (2) does it cover all 6 advisories or only the 2 HIGH; (3) whether the majors already deferred (napi-3, rusqlite+sqlite-vec — see the library-bump-steward memory) are in or out, since those are breaking-change work, not patch bumps; (4) whether TC-78s post-publish hold for #153 survives, since a 4x before publish would be the natural place to take it. NOTE the interaction with Slice 22: the sqlite-vec #99 probe may itself be a TC-76 re-open trigger for the sqlite-vec placement, so its finding should be read BEFORE scoping any dependency work."}
{"ts": "2026-07-29T00:57:04.177Z", "seq": 132, "kind": "todo", "summary": "commission-manifest.sh CANNOT CITE dev/adr/** or dev/interfaces/** AT ALL - DESIGN_ROOT is dev/design only, so ruling ADRs are unreachable by construction at any status, no matter what is back-linked. It also cannot see bare-number legs (#18, #99), and a full token match SUPPRESSES its own honest \"NO design doc mentions\" warning. DOC-HYGIENE-3 candidate.", "area": "tooling", "decider": "steward", "epistemic": "verified", "id": "TC-94", "priority": "p1", "status": "open", "refs": ["tc:TC-92", "tc:TC-89", "git:793e3295"], "body": "FOUND by an independent HITL-directed audit of the Slice 22 design-of-record set (2026-07-28), and verified. THREE DISTINCT DEFECTS in the same selector, all worse than TC-92s back-link gap because no amount of back-linking fixes them. (1) UNREACHABLE TIERS: commission-manifest.sh sets DESIGN_ROOT=dev/design and scan_design walks only that tree, so dev/adr/** and dev/interfaces/** can never appear in a brief. For Slice 22 alone that permanently excludes the RULING ADR for decision #18 (ADR-0.6.0-error-taxonomy.md, status accepted, whose per-module-vs-direct-variant rule decides the family split), the HITL-SIGNED authority for TC-68 (ADR-0.8.18-vector-equivalence-self-check.md), the identity definition TC-68s fingerprint must key on (ADR-0.6.0-vector-identity-embedder-owned.md, accepted), and the canonical vec0 metadata-column table for #99 (ADR-0.8.11-filter-grammar-unification.md). It also excludes dev/interfaces/*.md, which carries the LITERAL #18 statement at rust.md:213-215 and :352 - and AGENTS.md:25 obliges those files to be updated when an error surface changes, an obligation TC-39 already records as routinely missed. An ADR is a HIGHER tier of authority than a design memo, so the manifest is blind to exactly the strongest evidence. (2) BARE-NUMBER LEGS ARE INVISIBLE: slice_tokens derives tokens by regex from the state-file title; the shapes #18 and #99 match none of its patterns, so two of Slice 22s four legs contributed NOTHING to selection and no doc could ever be matched for them. (3) THE HONEST WARNING IS SUPPRESSED BY SUCCESS: the manifest prints NO design doc mentions: <tokens> only for tokens with zero matches. Slice 21 got that line and it was correct and load-bearing. Once every derived token matches at least one doc, the line disappears - so a brief whose coverage is THIN but nonzero looks identical to one that is complete. This is precisely the dangerous quiet mode TC-92 named, and the Steward walked straight into it: the TC-92 back-link fix made all three Slice-22 tokens match and thereby SILENCED the warning. Self-inflicted, recorded so it is not repeated. REMEDY: TC-92 option (b) - an explicit design_refs list per slice in release-state-<release>.json - closes all three at once, because a curated list is tier-agnostic, needs no token shape, and makes coverage explicit rather than inferred. TC-92 options (a) and (c) close NONE of them. Recommend (b) as the primary remedy for both TC-92 and this entry, with a warn-below-N as a cheap secondary. PLACEMENT: recommend DOC-HYGIENE-3 alongside TC-53/88/89/92 (HITL ruled that scope at steward seq-137) - but that ruling named four ids, so adding a fifth is an HITL call and is NOT assumed here."}
{"ts": "2026-07-29T02:27:01.451Z", "seq": 133, "kind": "observation", "summary": "TC-67 CLOSED in Slice 22 (572475f2). ProjectionDelta.vector_unsupported_kinds - sorted, deduped, empty-not-absent, OUTPUT-ONLY, additive and NON-ERRORING. Vocabulary NOT grown, Pack-1 D3 lock untouched, readiness semantics pinned in all three languages. The C-1 graceful-graft contract (Q4/Q6a) is preserved - verified byte-level by the Steward.", "area": "engine", "decider": "steward", "epistemic": "verified", "id": "TC-67", "priority": "p1", "status": "closed", "refs": ["git:572475f2", "tc:TC-92"], "body": "The Steward steered this leg mid-flight (not a fix round, TC-84) after an independent audit found the commission manifest had omitted a load-bearing constraint: the ratified OPP-12 C-1 contract Q4/Q6(a) requires a projection the engine cannot honor to be DEFERRED with NO ERROR and NO BUILD, and that clause is byte-pinned by scripts/c1-conformance-pin.json and mechanically asserted by check-c1-conformance.sh (ProjectionDelta.deferred: Vec<String> plus two named tests) as R-20-H7, a PUBLISH PRECONDITION. A report implemented as an ERROR, or by restructuring deferred, would have turned that gate RED. The reconciliation held: \"report, not silent\" and \"no error, no build\" are compatible, and the delivered report is additive and non-erroring. STEWARD-VERIFIED AT THE LANDING, mechanically rather than on the orchestrators word: the contract file, c1-conformance-pin.json and check-c1-conformance.sh are all BYTE-UNTOUCHED between 0cf2923c and b32e38ca, and check-c1-conformance.sh exits 0. DESIGN CHOICE WORTH KEEPING: the report is emitted OUTSIDE the dense_arm_live gate, because the unsupported-kind vocabulary is a COMPILE-TIME CONSTANT and gating it would hide a permanent fact behind a transient one."}
{"ts": "2026-07-29T02:27:01.478Z", "seq": 134, "kind": "observation", "summary": "TC-68 CLOSED in Slice 22 (572475f2) - and the commissions OWN DoD CLAUSE WAS VACUOUS. \"Open cost independent of enrolled-kind count\" was ALREADY TRUE before the slice (the gate is SELECT EXISTS, not a count). The real cost was measured instead: 90 embeds on the population open, 45 on EVERY open thereafter, forever.", "area": "engine", "decider": "steward", "epistemic": "verified", "id": "TC-68", "priority": "p2", "status": "closed", "refs": ["git:572475f2", "tc:TC-10"], "body": "A DoD clause that is already satisfied at baseline is a vacuous gate - the TC-37 class, and it was written by the Steward into the R-20-VC row and carried into the commission unchallenged. The orchestrator caught it and re-derived a real measurement rather than passing a test that could not fail. RECORD THE REAL NUMBERS: both figures previously in the ledger were true, but of DIFFERENT opens - 90 embeds on the population open, 45 on every subsequent open, permanently, because run_vector_equivalence_probe re-embeds 45 reference probes on ANY open with a non-empty _fathomdb_vector_kinds. THE FIX: a fingerprint keyed on the embedder identity triple PLUS the live mean_vec bytes AND the stored baseline blobs - deliberately not the identity alone, and never the embedder NAME alone, which would silently fail to re-fire on a revision or dimension change and defeat the guarantee TC-68 was ruled to PRESERVE. Stored in the EXISTING _fathomdb_open_state; NO schema bump, SCHEMA stays 24. NOTE A GAP THE SLICE COULD NOT CLOSE: EmbedderIdentity carries NO normalization field, so mean_vec covers that axis in practice rather than by construction. A backend/device descriptor on EmbedderIdentity is the only thing that RETIRES this residual - recorded separately."}
{"ts": "2026-07-29T02:27:01.500Z", "seq": 135, "kind": "observation", "summary": "sqlite-vec vec0 DELETE defect: REPRODUCED AND LIVE - erase_source AND purge both returned Err(Storage) and LEFT THE ROW AT REST. A GDPR-ERASURE VERB, FAILING, IN THE RELEASE THAT PUBLISHES. Fixed engine-side in Slice 22 (572475f2) across all six by-rowid delete sites. Upstream is #274, NOT #99 - a mis-citation this repo carried for months.", "area": "engine", "decider": "steward", "epistemic": "verified", "id": "TC-99-VEC0", "priority": "p1", "status": "closed", "refs": ["git:572475f2", "git:b604fc77", "tc:TC-76"], "body": "COMMISSIONED AS A PROBE - \"record a written finding either way\". It came back red against two shipped erasure verbs, so the orchestrator kept the fix rather than shipping a finding; the Steward RATIFIES that deviation, because publishing knowingly-broken erasure to three public registries is not a defensible alternative. MECHANISM, pinned to upstream source: sqlite-vec 0.1.7 vec0Update_Delete_ClearMetadata (sqlite-vec.c:8888) deletes the _metadatatext<NN> shadow row for a TEXT metadata value longer than the 12-byte inline view, then returns sqlite3_step s SQLITE_DONE (101) VERBATIM - it never resets rc to SQLITE_OK. vec0Update_Delete (:9027) reads 101 as an error and aborts the whole DELETE. The INSERT/UPDATE twin is saved by an unconditional rc = sqlite3_blob_close(..) after its switch, so ONLY DELETE carries it. FATHOMDB EXPOSURE: the 0.8.20 Slice-15e attr_<hex> filterable columns - values are CALLER-SUPPLIED and stored marker-encoded (\\\\x01 || V), so any raw attribute value of 12+ bytes breaks every by-rowid DELETE FROM vector_default. MEASURED BOUNDARY on the real engine: raw 11 -> stored 12 -> Ok; raw 12 -> stored 13 -> Err(Storage), with the vec0 row AND its shadow value left at rest. That is data that survives an erasure request. FIXED across all six by-rowid delete sites with a tripwire test asserting the UPSTREAM defect still exists, so the workaround is removed deliberately rather than rotting. THE CITATION CORRECTION MATTERS FOR THE BUMP DECISION: the repo has tracked this as \"sqlite-vec #99\" throughout; the real upstream issue is #274. 0.1.8 STILL CARRIES IT; 0.1.9 FIXES IT. No dependency bump was taken in Slice 22 - that is an open HITL call, and the TC-76 re-open trigger has FIRED."}
{"ts": "2026-07-29T02:27:41.246Z", "seq": 136, "kind": "todo", "summary": "Message-carrying WriteValidation { msg }: the #18 settlement collapsed the family onto WriteValidation but the variant carries NO message, so the window-refusal lost its bounds and tests now ASSERT THE ABSENCE. Restoring it is ~14 engine + ~44 binding sites across both payload shapes - its own slice, not a fold-in.", "area": "engine", "decider": "steward", "epistemic": "verified", "id": "TC-95", "priority": "p2", "status": "open", "refs": ["git:572475f2", "tc:TC-39"], "body": "Consequence of the Slice 22 decision-#18 settlement (collapse InvalidArgument onto WriteValidation). BREAKING AND USER-VISIBLE: the window refusal no longer carries its bounds in the error, and the shipped tests were updated to assert that ABSENCE - so the regression is now pinned as intended behaviour and will not surface as a failure. SIZE: ~14 engine sites and ~44 binding sites, and it must handle BOTH payload shapes. That is why it is not a fold-in. Interface docs (rust/python/typescript) were updated for the collapse itself (AGENTS.md:25, the TC-39 obligation), so a later message-carrying change must sweep them AGAIN."}
{"ts": "2026-07-29T02:27:41.272Z", "seq": 137, "kind": "todo", "summary": "EmbedderIdentity has NO normalization (or backend/device) field. TC-68 fingerprints cover that axis via the live mean_vec bytes IN PRACTICE, not BY CONSTRUCTION. A backend/device descriptor on EmbedderIdentity is the only thing that RETIRES the residual.", "area": "embedder", "decider": "steward", "epistemic": "verified", "id": "TC-96", "priority": "p2", "status": "open", "refs": ["tc:TC-68", "git:572475f2"], "body": "ADR-0.6.0-vector-identity-embedder-owned defines identity as (model_identity, model_version, dimension, normalization), but the shipped EmbedderIdentity struct has no normalization field - so a change on that axis is not structurally part of the identity. TC-68s cache compensates by including the live mean_vec bytes and the stored baseline blobs, which detects such a drift EMPIRICALLY. That is a sound mitigation and it is what shipped, but it is a probe, not a guarantee: it detects a change that has already altered the vectors rather than declaring the identity that produced them. Adding a backend/device descriptor to EmbedderIdentity closes it structurally. Relevant to the CPU/CUDA story: CPU and CUDA are 1-bit identical today, so a device descriptor would NOT currently change any fingerprint - it would make the invariant explicit rather than accidental."}
{"ts": "2026-07-29T02:27:41.294Z", "seq": 138, "kind": "todo", "summary": "THREE test-harness traps measured in Slice 22, each of which makes a green or a red LIE: ac_029 is a WALL-CLOCK RATIO assertion that fails when a reviewer runs concurrently; src/python pythonpath=[\".\"] SHADOWS an installed wheel and kills the X1 Python route with a misleading circular import; six engine targets exit 101 WITHOUT RUNNING absent --features operator.", "area": "ci", "decider": "steward", "epistemic": "verified", "id": "TC-97", "priority": "p2", "status": "open", "refs": ["tc:TC-72", "git:572475f2"], "body": "All three measured during the Slice 22 build; recorded because each produces a signal that reads as the opposite of the truth. (1) ac_029 IS A WALL-CLOCK RATIO ASSERTION. It failed once at baseline=899ms stalled=4.24s WHILE CODEX WAS RUNNING CONCURRENTLY, then passed 3/3 quiet. RULE: never run the engine suite alongside a reviewer or another heavy job. Same family as TC-72 (cargo test --workspace unstable ~1 run in 3) - a red here is not evidence of a defect and a green under load is not evidence of health. (2) src/python/pyproject sets pythonpath = [\".\"], which SHADOWS an installed wheel: the X1 Python verification route dies at COLLECTION with a misleading circular import that looks like a code defect and is not. Workaround that works: -o pythonpath= run from a neutral cwd. This is the background-exit-masks-real-exit class - a collection/import error is not a code defect, and here it actively misdirects. (3) SIX ENGINE TEST TARGETS EXIT 101 WITHOUT RUNNING when --features operator is absent - a vacuous non-run that an aggregate runner reports as a failure, or worse, that a naive harness could treat as coverage. Related: --all-features clippy is IMPOSSIBLE in this workspace (objc2), so any instruction to \"just run --all-features\" is wrong here."}
{"ts": "2026-07-29T02:27:41.316Z", "seq": 139, "kind": "observation", "summary": "TC-76 RE-OPEN TRIGGER HAS FIRED (Slice 22, 572475f2). The sqlite-vec vec0 DELETE defect is upstream #274 (NOT #99 as this repo cited for months); 0.1.7 AND 0.1.8 both carry it; 0.1.9 FIXES it. FathomDB now ships an engine-side workaround. Whether the 0.1.9 bump is pulled OUT of Slice 31 and taken before publish is an OPEN HITL CALL.", "area": "deps", "decider": "steward", "epistemic": "verified", "id": "TC-76", "priority": "p1", "status": "open", "refs": ["tc:TC-99-VEC0", "git:572475f2", "seq:142"], "body": "TC-76 recorded that the sqlite-vec #99 probe finding \"informs whether sqlite-vec should move out of the 0.8.22 placement\" and named itself the re-open trigger. IT HAS FIRED, and with a stronger result than anticipated: the defect is not theoretical, it BROKE erase_source and purge on real data, and the fix is already known and released upstream. WHAT IS NOW KNOWN THAT WAS NOT: (a) the correct upstream id is #274, so any search or changelog check against \"#99\" will come back empty and look reassuring - a mis-citation the repo carried for months; (b) 0.1.8 does NOT fix it, so a routine \"bump to latest patch\" would have left the defect in place while appearing to address it; (c) 0.1.9 does fix it. THE DECISION IS NOT WHETHER FATHOMDB IS SAFE - the engine-side workaround shipped in Slice 22 and is tripwired. It is whether to carry a workaround into the FIRST REAL PUBLISH when the upstream fix exists, versus taking a dependency bump before Slice 40. INTERACTS WITH: Slice 31 (Library Sweep #3, where sqlite-vec would otherwise be handled), TC-93 (the possible pre-publish Slice 4x for Dependabot), and the standing directive that Dependabot stays out of the sequence. A bump also re-runs the tripwire test, which ASSERTS THE UPSTREAM DEFECT STILL EXISTS - so the bump and the tripwire must be taken together or the suite goes red on a successful upgrade."}
{"ts": "2026-07-29T05:00:14.005Z", "seq": 140, "kind": "todo", "summary": "DEFERRED BY THE HITL UNTIL AFTER SLICE 23 (2026-07-28): does R-20-VCs acceptance signal \"one family, tests updated\" stand with ONE documented, test-pinned exception? decision #18 collapsed the write-shape family onto WriteValidation except reject_unrenderable_edge_epoch, which still returns InvalidArgument { msg }. RETROSPECTIVE - about the already-LANDED Slice 22, not a gate on anything forward.", "area": "engine", "decider": "hitl", "epistemic": "verified", "id": "TC-98", "priority": "p2", "status": "open", "refs": ["tc:TC-95", "tc:TC-33", "git:572475f2"], "body": "THE EXCEPTION IS DELIBERATE, DOCUMENTED AND PINNED BY A TEST so it cannot drift silently - see src/rust/crates/fathomdb-engine/tests/error_taxonomy.rs:214-227 and the dev/design/errors.md 2026-07-28 amendment. THE REASON IT EXISTS: EngineError::WriteValidation is MESSAGE-LESS. reject_unrenderable_edge_epoch (lib.rs:2180) returns InvalidArgument { msg } naming the offending field and bound, and that message is a REQUIRED CONTRACT from TC-33 fix-1, itself a codex section 9 finding - an unrenderable epoch renders to null on the consolidation wire and SILENTLY RESURRECTS AN INVALIDATED EDGE, so the caller must be told WHICH field. Collapsing it onto the message-less variant would destroy the diagnostic. THE THREE OPTIONS, unchanged and none taken: (a) accept the exception, and let TC-95 (message-carrying WriteValidation { msg }, ~14 engine + ~44 binding sites) retire it in its own slice at 0.8.21; (b) collapse it now, satisfying \"one family\" literally and DESTROYING the safety diagnostic; (c) do TC-95 inside 0.8.20 and then collapse - the same end state as (a) but paying ~58 sites of breaking churn in the release that publishes. Steward recommendation on record was (a). WHAT WOULD SETTLE IT CHEAPLY: whether dev/interfaces/{rust,python,typescript}.md state \"one family\" WITHOUT documenting the exception. If they do, code and docs disagree and one of them must move regardless of which option is chosen. NOTE the deferral is to AFTER Slice 23, not to a release - it does not gate Slice 23, Slice 31, DOC-HYGIENE-3 or the batched surface decision."}
{"ts": "2026-07-29T05:25:59.970Z", "seq": 141, "kind": "observation", "summary": "TC-94 CORRECTION: defect (3) as originally written is FALSE and is WITHDRAWN. The manifest does NOT suppress its coverage warning on a full token match - it reports unmatched tokens PER TOKEN and always has. Defects (1) unreachable dev/adr/** and dev/interfaces/**, and (2) bare-number legs invisible to the regex, STAND and are unaffected. A REPLACEMENT third defect, worse and real, is recorded separately as TC-100.", "area": "tooling", "decider": "steward", "epistemic": "verified", "id": "TC-94", "priority": "p1", "status": "open", "refs": ["tc:TC-100", "tc:TC-92", "seq:138"], "body": "WITHDRAWN CLAIM, in the Stewards own words as originally filed: \"a FULL token match SUPPRESSES the manifests own honest NO design doc mentions warning - the Steward triggered this personally, because the TC-92 back-link fix made all three Slice-22 tokens match and thereby SILENCED the one signal Slice 21 correctly received.\" THAT IS FALSE. MEASURED at HEAD on the current main: Slices 15, 20, 21 AND 31 all emit \"NO design doc mentions: <token>\" WHILE OTHER TOKENS MATCHED - so the report is per-token, not all-or-nothing. git log -S(NO design doc mentions) -- scripts/commission-manifest.sh returns exactly ONE commit, 1b9cf9a3, the original implementation: the block has never regressed and there was nothing to restore. The generator computes matched_tokens as a set union over the hits and reports tokens not in it. HOW THE STEWARD GOT IT WRONG: after back-linking, the Slice 22 manifest printed no such line, and the Steward inferred suppression. In fact ALL THREE Slice-22 tokens (TC-67, TC-68, R-20-VC) genuinely matched, so there was nothing to report - correct behaviour observed and misread as a regression. The error was then written into this entry, into commit messages, and into a commission brief. FOUND BY the implementer commissioned to build the remedy, which measured the premise instead of implementing it, and escalated rather than widening scope. THE ONLY REAL GAP IN THAT AREA is narrow: full coverage prints NOTHING, so a reader cannot distinguish \"checked, all tokens covered\" from \"coverage not reported\". Closing it needs an always-on line, which would break the byte-identity guarantee that makes design_refs safely additive for the eight slices with no curation. THAT TRADE WAS RULED AGAINST BY THE STEWARD: byte-identity is worth more, and the always-on line is NOT being built. What WAS built instead, gated on design_refs so byte-identity holds: when tokens are unmatched AND curated refs exist, the report notes the curated docs are not token-matched and must be read before concluding a token is undesigned."}
{"ts": "2026-07-29T05:25:59.994Z", "seq": 142, "kind": "todo", "summary": "commission-manifest.sh matches design-doc tokens by BARE SUBSTRING with no word boundary, so \"C-1\" matches inside \"TC-15\" and Slice 15 currently cites EIGHTEEN docs on that token - including 0.8.4-graphrag-sensemaking.md and four fathomdb-memex-overall-roadmap files that have nothing to do with the projection registry. A padded required-reading list that LOOKS like thorough coverage. Needs word-boundary matching; that changes which docs existing slices cite, so it needs its own slice.", "area": "tooling", "decider": "steward", "epistemic": "verified", "id": "TC-100", "priority": "p1", "status": "open", "refs": ["tc:TC-94", "tc:TC-92"], "body": "FOUND by the implementer building the design_refs remedy; INDEPENDENTLY VERIFIED by the Steward: python3 -c \"print(\\\"C-1\\\" in \\\"TC-15\\\")\" -> True, and commission-manifest.sh 0.8.20 15 emits 18 rows carrying C-1 in their matched: list. THIS IS THE REAL VERSION OF THE DEFECT THE STEWARD MIS-DIAGNOSED AS WARNING SUPPRESSION (see the TC-94 correction). The failure is not that coverage goes unreported - it is that coverage is OVERSTATED by spurious matches, which is the same \"thin brief that looks complete\" outcome reached by the opposite mechanism. WHY IT MATTERS MORE THAN IT LOOKS: an orchestrator brief is a REQUIRED-READING list. Padding it with a GraphRAG sensemaking memo and four Memex roadmap drafts costs the orchestrator context on documents that cannot inform the slice, and - worse - makes a slice with genuinely thin design coverage look thoroughly supported, which is precisely the condition TC-92s vacuous-pass guard exists to surface. It also interacts with the status tier: spurious hits carry their own recorded status, so an UNREVIEWED or PROPOSAL-stage doc can be presented as part of a slices design of record purely because a two-character token appeared inside a longer id. SHORT TOKENS ARE THE HAZARD: C-1, C-2, and any AC-NN or TC-NN that is a prefix of a longer id. REMEDY: word-boundary matching (re.search with \\\\b or an explicit token regex per pattern) rather than the `t in text` substring test. NOT DONE IN THE design_refs FIX, deliberately and correctly: it changes WHICH docs existing slices cite, so it breaks the byte-identity guarantee that makes design_refs safely additive, and it needs a review of the resulting citation changes for every slice. RECOMMEND placement in DOC-HYGIENE-3 alongside TC-53/88/89/92/94 - but that scope was ruled by the HITL at seq-137/seq-138 naming five ids, so adding a sixth is an HITL call and is NOT assumed here."}
{"ts": "2026-07-29T05:32:49.571Z", "seq": 143, "kind": "todo", "summary": "OPP-12-C1-converged-contract.md records status: UNREVIEWED in its own frontmatter, so a commission brief now cites the RATIFIED contract as \"⚠ unclassified (TC-50)\". It CANNOT be fixed by editing: c1-conformance-pin.json pins its sha256 AND git blob sha1, so any frontmatter correction breaks the R-20-H7 publish-precondition gate. Needs a PIN RE-ISSUE, not an edit.", "area": "docs", "decider": "steward", "epistemic": "verified", "id": "TC-101", "priority": "p2", "status": "open", "refs": ["tc:TC-50", "tc:TC-80", "git:d30ef52f"], "body": "SURFACED by the implementer that built design_refs, when the byte-pinned C-1 contract first appeared in a manifest section 4 as: \"design contract  CURATED [UNREVIEWED] unclassified (TC-50) dev/design/record-lifecycle-protocol/OPP-12-C1-converged-contract.md\". THE MANIFEST IS BEHAVING CORRECTLY - it reports status AS RECORDED and never as a currency claim, and lint-design-status.sh proves the PRESENCE of a status, never its truth. The document is the one that is wrong about itself: it is the RATIFIED converged contract, the authority for the R-20-H7 can-i-deploy gate, and it records UNREVIEWED, which means \"nobody has classified this yet\". THE UNUSUAL CONSTRAINT: scripts/c1-conformance-pin.json pins the file by sha256 AND git_blob_sha1, and check-c1-conformance.sh hashes the ON-DISK bytes and fails on any difference - the pins own comment states a whitespace-only change still fails DELIBERATELY. So the ordinary TC-50 remedy (edit the frontmatter to the right status) would turn a PUBLISH PRECONDITION red. Any correction must be a coordinated PIN RE-ISSUE: change the frontmatter and re-derive sha256 + git_blob_sha1 + the 45-clause classification in the same reviewed change. RELATED AND SAME SHAPE: TC-80 already records a stale Q6(b) parenthetical in that file, deferred for exactly this reason - \"fix only at the next substantive amendment; the gate pins contract bytes and any edit forces a 45-clause re-derivation\". THESE TWO SHOULD BE TAKEN TOGETHER at that next amendment, so the pin is re-issued once rather than twice. DO NOT let an agent \"helpfully\" fix the frontmatter in isolation - that is the failure this entry exists to prevent."}
{"ts": "2026-07-29T08:47:20.808Z", "seq": 144, "kind": "observation", "summary": "TC-68 FIGURE CORRECTED. My close entry recorded \"90 embeds on the population open, 45 on EVERY open thereafter, forever\". The 45-per-open half is STALE. Measured at HEAD during Slice 23: 0 / 90 / 0, with 20/20 ZERO variance. Subtract 90 on the population open and NOTHING thereafter - the fingerprint cache works better than the number I recorded for it.", "area": "engine", "decider": "steward", "epistemic": "verified", "id": "TC-68", "priority": "p2", "status": "closed", "refs": ["tc:TC-91", "git:b79e22f0"], "body": "CORRECTION to the TC-68 close, event-sourced (the prior entry stands as written; this supersedes its figure). ORIGIN OF THE STALE NUMBER: the 45-per-open figure described run_vector_equivalence_probe re-embedding 45 reference probes on ANY open with a non-empty _fathomdb_vector_kinds - true BEFORE TC-68 shipped its fingerprint cache in Slice 22. I recorded it in the close entry as if it still held afterwards. It does not. MEASURED at HEAD by the Slice 23 orchestrator while separating TC-91(a)s duplicate-embed rate from probe re-embeds: 0 / 90 / 0 across the three open classes, 20/20 runs, zero variance, with probe_calls == 0 ASSERTED rather than assumed. WHY IT MATTERS BEYOND TIDINESS: TC-91(a) had to be measured against a baseline of probe activity, and a stale 45-per-open would have been subtracted from every open - which would have corrupted the duplicate-embed attribution in exactly the direction that makes a real defect look smaller. The correct baseline is what let TC-91(a) come out at governed 52.0 percent vs anonymous 52.5 percent, indistinguishable, and therefore NOT the TC-57 race. A figure carried forward without re-measurement is the same class as TC-57s 517-vs-5 and the TC-94 defect (3) I withdrew this session: a plausible number, never re-checked, load-bearing for a later inference."}
{"ts": "2026-07-29T08:47:20.835Z", "seq": 145, "kind": "observation", "summary": "TC-90 REPRODUCES (10/10 stress) - and the TC-57-shaped protocol ALONE would have filed a FALSE NEGATIVE (paced arm 0/10). Mechanism measured, not assumed: plain SQLITE_BUSY (5), NOT 517, busy handler invoked ZERO times, 0 ms against a 5000 ms timeout. Engine::transition is WORSE than commit_batch: it never calls emit_sqlite_internal_error, so the code is never produced at all. Characterization only - the FIX is 0.8.21 per seq-136.", "area": "engine", "decider": "steward", "epistemic": "verified", "id": "TC-90", "priority": "p1", "status": "open", "refs": ["tc:TC-57", "tc:TC-72", "git:b79e22f0", "seq:136"], "body": "THE METHODOLOGICAL FINDING IS THE MOST TRANSFERABLE PART. TC-57s repro protocol - a paced loop with a 2 ms embed - returns 0/10 against transition. A stress arm (2 threads x 24 burst, 0 ms embed) returns 10/10, with its control at 0/10. So an agent instructed to \"follow the TC-57 template\", which is what the Steward wrote into the Slice 23 commission, would have concluded transition is CLEAN and filed a false negative on a real defect. THE TEMPLATE NEEDED A STRESS ARM AND THE COMMISSION DID NOT SAY SO. Recorded so the next characterization inherits the corrected protocol rather than the one that would have missed this. REPRODUCTION IS THE BAR, NOT RATE: the failure RATE does not replicate across sessions on the same commit and a quiet machine (5.9/40 vs 3.8/40), while REPRODUCTION held 10/10 in both. Any future acceptance criterion that gates on a rate will flake; gate on reproduction. Consistent with TC-72 (cargo test --workspace unstable ~1 run in 3) and TC-97 (ac_029 is a wall-clock ratio) - this repo cannot gate on timing-derived numbers. MECHANISM, measured: primary DatabaseBusy, extended code 5 - plain SQLITE_BUSY, NOT the SQLITE_BUSY_SNAPSHOT (517) that the pre-TC-57 write-ups all named - busy handler invoked 0 times, failing in 0 ms against a 5000 ms busy_timeout. Identical in kind to what TC-57 measured in commit_batch, which is why the classes are the same. A SECOND, DISTINCT DEFECT: transition never calls emit_sqlite_internal_error, so the SQLite extended code is NEVER PRODUCED - the race is not merely unfixed, it is undiagnosable from the outside, which makes the next investigation harder than TC-57s was. BEGIN IMMEDIATE TRANSFER MEASURED, NOT ASSUMED, exactly as the commission required: transition -> Err(Storage) in 0 ms; the already-IMMEDIATE Engine::write -> Ok after waiting ~935 ms. So the remedy does transfer - but it was proven rather than inherited. FIX IS OUT OF SCOPE HERE by HITL ruling seq-136 (characterize in 0.8.20, fix at 0.8.21); lib.rs verified byte-identical across leg 2 by the Steward."}
{"ts": "2026-07-29T08:47:20.857Z", "seq": 146, "kind": "todo", "summary": "PRESUMED, NOT MEASURED: at saturating load Engine::transition drain never reaches idle - 5 calls in 181 s, 4 Scheduler errors after burning the full 30 s timeout. configure_projections, purge and lib.rs:8646 share the shape. Surfaced during Slice 23; deliberately kept as a hypothesis in the characterization doc rather than promoted to a finding.", "area": "engine", "decider": "steward", "epistemic": "assumed", "id": "TC-102", "priority": "p2", "status": "open", "refs": ["tc:TC-90", "tc:TC-91", "git:b79e22f0"], "body": "OBSERVED during the TC-90 stress arm, NOT characterized - and the epistemic label on this entry is assumed, deliberately. The Slice 23 orchestrator recorded it as PRESUMED-NOT-MEASURED in the characterization document and refused to promote it, which is the correct handling and the reason it is trustworthy enough to record at all. WHAT WAS SEEN: under saturating write load, drain on the transition path never reaches idle - 5 calls in 181 seconds, 4 of them returning Scheduler after burning the full 30 s timeout. THE SHAPE IS SHARED by configure_projections, purge and the site at lib.rs:8646, which is why this is filed as potentially structural rather than as a transition quirk. WHAT IS NOT ESTABLISHED: whether this is starvation (the worker never gets scheduled), livelock (it runs but never converges), or simply correct back-pressure under a load no real consumer generates. Those have different fixes and one of them is not a defect at all. DO NOT SCOPE A FIX FROM THIS ENTRY. It needs the same treatment TC-57 and TC-90 got - a written characterization with a repro protocol - and TC-57s lesson is that a fix scoped before measurement ships inert. Related: TC-91(b) proves projection-worker commit failures are discarded via let _ = commit_projection_outcomes(..), so terminal-state counting is structurally blind - meaning any attempt to quantify this by counting failed terminals will report a falsely clean number."}
{"ts": "2026-07-29T14:23:44.465Z", "seq": 147, "kind": "todo", "summary": "ERROR-TAXONOMY REVIEW, BUNDLED, REVISIT AT 0.8.22 (HITL 2026-07-29). Three findings from the TC-98 discussion, deferred TOGETHER by the HITL rather than settled in 0.8.20. (1) TC-98 itself is ruled option (a) FOR NOW - the one documented exception, reject_unrenderable_edge_epoch at lib.rs:2180, keeps returning InvalidArgument { msg } while the other six validate_write shape rejections return the message-less unit variant WriteValidation at lib.rs:3570. It is accepted, not endorsed, and it is revisited here. (2) EngineError IS NOT #[non_exhaustive] - it is a plain pub enum at lib.rs:3556-3557 carrying only derive(Clone, Debug, Eq, PartialEq). Consequences, both real: adding any future variant is a COMPILE break for a downstream exhaustive match, and RECLASSIFYING a rejection between two existing variants is a SILENT runtime break, because the match still compiles and simply takes a different arm. This is the HIDDEN-BREAK shape and it exists ONLY on the Rust facade - Python and TypeScript are exception-based with a single root (EngineError / FathomDbError), so a leaf mismatch propagates loudly and can never take a wrong branch silently. NOTE THE WINDOW: adding #[non_exhaustive] is ITSELF a breaking change, so the natural venues are breaking releases; 0.8.20 was one and the HITL chose to defer past it. NOTE ALSO: the T1e byte-pin would probably NOT catch such a change, because that pin tracks allowlist NAMES, not type SHAPES. (3) The asymmetry is between two SIBLING conditions on the same write call, not one condition raising two classes - a given malformed write raises exactly one deterministic class. Blast radius on PUBLISHED clients was verified as ZERO against the v0.8.9 tag before the deferral: node valid_from/valid_until (9c6420e5) and reject_unrenderable_edge_epoch (39f35108) are BOTH absent from v0.8.9, so no published client catches either class on these paths.", "decider": "hitl", "id": "TC-103", "status": "open"}
{"ts": "2026-07-29T15:06:52.980Z", "seq": 148, "kind": "todo", "summary": "SENDMESSAGE GRANT IS ON DISK BUT ITS EFFECTIVE AVAILABILITY IS UNVERIFIED - one experiment still owed, and it CANNOT be run from the session that made the edit. RED-then-GREEN probe, 2026-07-29 (steward seq-158). RED, before the grant: verdict (i), tool absent and NOT recoverable via ToolSearch (ToolSearch itself absent). GREEN, after the grant landed on disk: verdict (iii), tool STILL absent. CRITICAL - DO NOT MISREAD THE GREEN RESULT AS THE HARNESS BLOCKING SIBLING MESSAGING. No message was ever attempted at transport level because the tool did not exist to call, so the run tests the LOADING PATH, not the capability. The capability question is still open. LEADING EXPLANATION, registry staleness: agent definitions appear to load ONCE at session start, so a session begun before the edit keeps the pre-edit registry. Direct evidence - the probing session own agent registry advertised the pre-edit tool list while disk showed the post-edit one. THE DECISIVE TEST: re-run the probe in a session STARTED AFTER the edit. If SendMessage appears, it was staleness and the grant works. If it stays absent while the registry advertises it, a real seat-level restriction exists and the section 9 mechanism docs must say so. STEWARD CORRECTION of both probes: they proposed a seat-level TRIM as the rival mechanism, citing Grep and Glob being declared yet uncallable. That is NOT a seat trim - Grep and Glob are absent SESSION-WIDE including from the main-thread Steward seat, so it is a session tool policy affecting everyone, and it does not narrow spawned seats specifically. Removing that confound leaves staleness as the single leading explanation. Also, SendMessage IS available to the main thread as a deferred tool, which is consistent with seat-scoping being the only barrier for a spawned seat. Task is UNTESTED not absent - no call could be emitted distinguishable from Agent. Error wording splits into two families (not available in this session for Grep/Glob versus exists but is not enabled in this context for SendMessage/ToolSearch), weak textual evidence of two mechanisms. UNTIL THIS CLOSES: treat a mid-flight steer as unavailable and fall back to fresh-spawn, and do NOT brief an orchestrator with the mid-flight-steer-is-not-a-round rule as if it were actionable.", "decider": "steward", "id": "TC-104", "status": "open"}
{"ts": "2026-07-29T15:17:05.568Z", "seq": 149, "kind": "todo", "summary": "SUPERSEDES THE EARLIER TC-104 ENTRY, WHICH WAS WRONG. RETRACTED: the claim that the SendMessage grant needed a FRESH SESSION because agent definitions load once at session start. That was a Steward error. Claude Code WATCHES .claude/agents/ and the next delegation uses the updated definition within seconds, no restart needed (official sub-agents doc). The first GREEN probe failed only because the Steward spawned it SECONDS after writing the file - it raced the watcher, then recorded the result as evidence about the harness instead of about its own timing. MEASURED TRUTH after a re-probe 20+ minutes post-edit: (1) a spawned BACKGROUND orchestrator seat DOES receive SendMessage from the frontmatter tools: list - grant confirmed, no fresh session needed; (2) CHILD-TO-MAIN DELIVERY IS PROVEN, not merely accepted - the probe ping arrived in the Steward session as an agent-message from=orchestrator, which the probe itself could not observe from inside; (3) CHILD-TO-CHILD REMAINS UNPROVEN - the send returned success true and queued for delivery at its next tool round, but the trivial child finished in 8.4 seconds and never had a next tool round. That is a probe-design race, NOT a broken transport, and the failure mode CANNOT occur in the real mid-flight-steering case where the child is long-running by definition. To close it, use a long-running child instructed to report anything it receives. TWO TRAPS FOR THE NEXT TESTER: the SIBLING ROSTER is NOT a reliable grant signal - the docs say it appears only when the tools include SendMessage, but the probe held the grant and got NO roster, so roster-absence does not imply grant-absence; and the ADVERTISED AGENT REGISTRY OVER-REPORTS - it listed Grep, Glob and Task while the actual grant was Read, Bash, Agent, SendMessage, because subagents inherit the MAIN conversation pool, Grep/Glob are absent session-wide including from main, and Task is not in the background keep-list. Neither is a seat trim. WEB-RESEARCH VERDICT REFUTED BY PRIMARY SOURCE, recorded so nobody re-derives it: a research agent concluded SendMessage is architecturally unavailable to subagents and that the fix was to migrate to agent teams with CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1. Both central claims are FALSE. The official sub-agents doc names SendMessage explicitly in the list of built-in tools a BACKGROUND subagent keeps, and states that SendMessage does not require agent teams to be enabled - only structured team-protocol messages do. That agent also labelled three user-filed GitHub issues as OFFICIAL; they are community reports and their issue numbers were never verified. DO NOT MIGRATE TO AGENT TEAMS ON THAT BASIS.", "decider": "steward", "id": "TC-104", "status": "open"}
{"ts": "2026-07-29T15:56:28.884Z", "seq": 150, "kind": "todo", "summary": "SPEC-AND-RED-TEST SLICES ARE AS REVIEW-EXPENSIVE AS ENGINE SLICES - two data points now, and it bears on how Slices 32 and 33 are budgeted. Slice 31 touched ZERO engine source (no files under src/, no manifest, no lockfile; deliverable was a design doc plus 8 RED test files) and still consumed the FULL six-round cap with TWELVE distinct codex section 9 findings. Slice 30 took seven productive rounds. The standing mental model - that review cost tracks engine-code risk - is therefore wrong, and the TC-75 cap (3 same-finding / Steward check-in at 6 / 10 total) was calibrated on engine slices. WHY IT HAPPENS, from the Slice 31 round table: the dominant finding class was A CRITERION GRADED AGAINST A HELPER WHILE THE INTEGRATION BOUNDARY THAT MUST CALL IT WENT UNGRADED - i.e. the test asserts the unit works but never drives it through the real entry point, so an implementation can satisfy the suite without satisfying the requirement. It recurred three times before the orchestrator swept all 23 criteria for that class at fix-4 (a SendMessage-less steer, not a round), which found six further instances and dropped the finding rate from 3-4 per round to 1. THE CHEAP MITIGATION FOR 32 AND 33: brief the class up front - every acceptance criterion must be graded through the integration boundary, not only against its helper - rather than discovering it on round 3. PLACEMENT IS AN HITL CALL: whether this changes the TC-75 cap for spec/test slices, or is merely briefing guidance, is not the Steward to decide.", "decider": "steward", "id": "TC-105", "status": "open"}
{"ts": "2026-07-29T15:56:28.911Z", "seq": 151, "kind": "todo", "summary": "dev/tools/mermaid/package.json IS A TRACKED NPM MANIFEST UNDER NO CONFIGURED DEPENDABOT DIRECTORY - surfaced by Slice 31 while specifying the SBOM survey tool, same class as the R-DEP-2 note. Nothing was changed about it. It means the repo has a tracked dependency manifest that the advisory tooling does not watch, so its dependencies can carry advisories nobody is told about - and note one of the six open advisories ruled to 0.8.22 at seq-152 (dompurify, low) is in exactly that manifest, which is how it became visible at all. HANDED TO SLICE 33 (the dependency survey, whose whole job is enumerating tracked manifests and which should therefore CATCH this class mechanically rather than by anyone remembering) AND TO 0.8.22, which now owns the advisory work per seq-152. Does NOT overturn the standing Dependabot-stays-out-of-the-sequence directive or TC-78.", "decider": "steward", "id": "TC-106", "status": "open"}
{"ts": "2026-07-29T16:09:54.247Z", "seq": 152, "kind": "todo", "summary": "TC-105 PLACEMENT RULED (HITL 2026-07-29): option (a) - BRIEFING GUIDANCE ONLY. The TC-75 round cap is UNCHANGED at 3 same-finding / Steward check-in at 6 / 10 total, for spec-and-test slices exactly as for engine slices. What changes is the BRIEF: Slices 32 and 33 must be told the causal defect class up front - EVERY ACCEPTANCE CRITERION MUST BE GRADED THROUGH THE INTEGRATION BOUNDARY, NOT ONLY AGAINST ITS HELPER - because that class generated 9 of Slice 31 twelve findings and was only caught on round 3. Rejected (b) raising the cap for spec/test slices and (c) pre-authorizing silent Steward extension. THE JUSTIFICATION, recorded so it is not re-argued: THE CAP DID NOT MALFUNCTION - IT DID ITS JOB. It halted Slice 31, the check-in cost minutes, and it produced a BOUNDED round 7 with a pre-committed exit. The two data points (Slice 30 seven rounds, Slice 31 six-plus) show the ROUNDS are productive, not that the CHECKPOINT is wasteful; raising the cap would remove a working control to treat a symptom. Option (c) was worse still - it would remove HITL visibility in precisely the case where a slice is running long. REVISIT TRIGGER: if the check-in itself becomes the bottleneck (Steward unavailable and slices stalling on a ruling), which has not happened.", "decider": "hitl", "id": "TC-105", "status": "resolved"}
{"ts": "2026-07-29T16:09:54.276Z", "seq": 153, "kind": "todo", "summary": "core.hooksPath RULED (HITL 2026-07-29): option (a) - RUN scripts/install-hooks.sh NOW, before Slice 31 and DOC-HYGIENE-3 land. Switches core.hooksPath from the absolute /home/coreyt/projects/fathomdb/.git/hooks to the repo-relative scripts/hooks, so every linked worktree runs ITS OWN tracked hooks instead of the primary manually-synced copy. WHY NOW rather than after both branches land: TC-88 guard exists to catch a stranded .jsonl.seq sidecar AT COMMIT TIME IN A WORKTREE, which is where the failure happened twice - deferring would land an UNEXERCISED guardrail. The risk was MEASURED not assumed: scripts/hooks/pre-commit and .git/hooks/pre-commit were byte-identical (diff rc=0) before the switch, so the Slice 31 worktree changes behaviour not at all, while the DOC-HYGIENE-3 worktree gains the new sidecar guard - which is exactly where we want it exercised. NOTE THIS IS LOCAL GIT CONFIG, NOT REPO CONTENT: it is machine-local, affects no other clone, and reverts with a single git config command. Rejected (b) run it after both land, and (c) never - which would leave TC-88 fix permanently inert and let the tracked and active hook copies keep drifting silently, the very class TC-88 exists to close.", "decider": "hitl", "id": "TC-107", "status": "resolved"}
{"ts": "2026-07-29T16:09:54.300Z", "seq": 154, "kind": "todo", "summary": "BARE-NUMBER TOKEN FLOOR RULED (HITL 2026-07-29): option (a) - KEEP THE TWO-DIGIT FLOOR. commission-manifest.sh matches bare-number legs with (?<![0-9A-Za-z_])#[0-9]{2,}(?![0-9A-Za-z_]) - two or more digits - and that stands as authored on the doc-hygiene-3-machinery branch. Measured basis: across the entire 0.8.20 ladder the only #N tokens in titles are #18 and #99 (Slice 22, both MATCH) and #3 (Slices 31/32/33, SUPPRESSED). #3 comes from Library Sweep #3 - a release-numbering artifact, not a requirement reference - and unfloored it drags 11 incidental docs into briefs for slices that currently and correctly TC-37 hard-fail, producing a brief that LOOKS supported when it is not. That is precisely the failure class TC-100 was funded to close. ACCEPTED COST: master tracked items #1 through #9 would not match if a future slice title cited one; #10 through #17 are unaffected. Rejected (c) floor plus an allowlist of meaningful single-digit ids - it would add a second hand-maintained list to the very tool this work exists to make mechanical, and it buys nothing today since no ladder title cites #1-#9. REVISIT TRIGGER, and it is self-announcing: a slice needing to cite a master item #1-#9 will surface at commission time because the manifest reports unmatched tokens PER TOKEN. Take option (c) then, not speculatively.", "decider": "hitl", "id": "TC-108", "status": "resolved"}
{"ts": "2026-07-29T16:09:54.323Z", "seq": 155, "kind": "todo", "summary": "SLICE 20 CITATION REMOVAL RULED (HITL 2026-07-29): option (a) - ACCEPT AS IT FALLS. Slice 20 loses dev/design/0.8.20-tc57-write-race-characterization.md from its design-citation set under the TC-100 word-boundary fix, and that removal is CORRECT: dense_readiness occurs in that doc exactly three times, at lines 30, 44 and 636, and EVERY ONE is inside the test filename slice20_dense_readiness.rs - there is no substantive dense_readiness content in it. It was a substring false positive. Slice 20 is LANDED at 841c307b so no future commission consumes its brief, and the doc is already a curated design_refs entry for Slices 21 and 23, so it remains reachable where it is genuinely relevant. Rejected (b) restore it via design_refs on Slice 20 - that would spend the curation mechanism preserving exactly the noise TC-100 was funded to remove, and would assert a design relationship that does not exist - and (c) loosen the matcher so filename mentions count, which re-opens TC-100 for the whole snake_case identifier class. REVERSIBLE via design_refs at any time.", "decider": "hitl", "id": "TC-109", "status": "resolved"}
{"ts": "2026-07-29T16:13:16.131Z", "seq": 156, "kind": "todo", "summary": "A LANDING WORKTREE RUNNING git checkout -B main CORRUPTS THE PRIMARY CHECKOUT INDEX, AND preflight --landing DOES NOT CATCH IT. Measured live during the Slice 31 landing (2026-07-29). The orchestrator landing worktree ran git checkout -B main, which moved the SHARED main ref while the PRIMARY checkout also had main checked out. HEAD advanced to contain the landed files while the primary index and working tree stayed at the old tree, so git status in the primary reported EIGHTEEN FILES AS STAGED DELETIONS - the entire slice, design doc, closure json, seven codex transcripts and eight test files. COMMITTING IN THAT STATE WOULD HAVE DELETED THE SLICE THAT HAD JUST LANDED. The Steward hit exactly this window, stopped rather than committing, and recovered with git reset plus verification; the orchestrator independently repaired with a targeted git checkout HEAD -- <20 paths>, no reset --hard, after first confirming the Steward uncommitted ledger writes (steward .seq 163, todos .seq 155, both AHEAD of HEAD) were intact. Nothing was lost, but only because two actors were being careful at the same moment. THE DURABLE FIX, per the standing rule that guardrail failures are fixed in TOOLING and not with a be-careful note: a landing worktree must merge into a DETACHED HEAD and push HEAD:main, NEVER checkout -B main. That belongs in preflight.sh --landing as a hard guard - refuse to land from a worktree whose HEAD is an attached ref that another checkout also has attached - and in the orchestrator handoff landing procedure. NOTE THE COMPOUND HAZARD that makes this worse than a normal stale index: the primary checkout is where the Steward holds UNCOMMITTED ledger appends, so the blast radius includes the append-only decision record, which cannot be repaired in place. Related but distinct from shared-checkout-branch-can-be-stale, which is about reading a stale branch; this one CREATES the staleness from another worktree.", "decider": "steward", "id": "TC-110", "status": "open"}
{"ts": "2026-07-29T17:10:57.659Z", "seq": 157, "kind": "todo", "summary": "THE SBOM SUITE GREEN REQUIRES A MANUAL INSTALL THAT NOTHING IN THE REPO PERFORMS - stated plainly by the Slice 32 orchestrator when pressed, and INDEPENDENTLY REPRODUCED BY THE STEWARD. With a clean venv (python3 -m venv, pip install scripts/sbom-survey[dev], pip uninstall -y sbom-survey so the source tree is what runs, then pytest from the worktree root) the suite is 23 passed rc=0 with ZERO skips. With a bare system python3 it is 16 failed / 7 passed rc=1, every trace ModuleNotFoundError: No module named cyclonedx - which is the DESIGNED require_external fail-never-skip behaviour from design section 5.7, not a defect, because a pytest.skip would be a vacuous green. THE CONSEQUENCE, and why this is recorded rather than shrugged at: GREEN IS ENVIRONMENT-DEPENDENT AND NOT REPRODUCIBLE FROM A CLEAN CHECKOUT WITHOUT AN UNDOCUMENTED-IN-CI INSTALL STEP. That bears on TWO live decisions. (1) SLICE 33 must RUN this tool, so its brief must carry the install sequence explicitly or it will hit 16 failures and misread them as a broken tool. (2) THE CI-WIRING DECISION (seq-166, GREEN and HITL both required): wiring this suite into CI necessarily means wiring the INSTALL too, so the wiring proposal must present the install step as part of what is being approved, not discover it afterwards. Also caught by the orchestrator and fixed in-slice: pip install scripts/sbom-survey leaves untracked build/ and sbom_survey.egg-info/ and NEITHER was gitignored anywhere in the repo, which would have bitten Slice 33 on its very first install. Minor tooling note from the same run: codex exec review --base cannot take a custom prompt in codex 0.136.0, so the review ran in the plain form Slice 31 used.", "decider": "steward", "id": "TC-111", "status": "open"}
{"ts": "2026-07-29T17:26:04.454Z", "seq": 158, "kind": "todo", "summary": "THE SLICE 31 FROZEN ORACLE HAS A HOLE THE SLICE 32 FIX CANNOT CLOSE, AND IT IS PROVEN BY MUTATION, NOT ARGUED. Codex round 1 found a real defect by INSPECTION: _Assembler.add_declarations attached origin, constraint and direct-ness to EVERY locked entry sharing an (ecosystem, name), so sha2 0.10.9 carried constraint 0.11 and thiserror 2.0.18 / tokenizers 0.22.2 were falsely DIRECT. Fixed in fix-1 via a new constraints.matches() with no new dependency. THE PART THAT MATTERS BEYOND THE FIX: the implementer wrote two mutations - M13 revert to attach-every-version, M14 fallback widens - and BOTH LEAVE THE FROZEN 23-TEST SUITE GREEN. So the suite cannot detect the defect it just shipped a fix for, which is exactly why codex had to catch it by reading rather than by running. The 16-mutation sweep did not help either, because every mutation was derived from WHAT THE TESTS GRADE, so it confirmed the suite bites without ever asking whether the OUTPUT WAS TRUE - a general lesson about mutation testing against a frozen oracle. WHY THIS IS LOAD-BEARING RATHER THAN TIDY-UP: depth and edit_sites are precisely the two fields SLICE 33 DECIDES ON, so a permissive oracle here propagates into the dependency survey that feeds 0.8.22. THE NAMED CHEAP FIX, from the implementer: ONE criterion - no component carries a fathomdb:constraint its own version does not satisfy - would close it, and would have caught this at Slice 31. THE OBSTACLE: the Slice 31 suite is FROZEN and was declared the contract; Slice 32 brief forbids editing it, and Slice 31 spent seven codex rounds hardening it. Unfreezing a landed acceptance suite is a scope/record question and therefore NOT the Steward call - it goes to the HITL. Options are (a) unfreeze for exactly this one added criterion, (b) leave the hole and carry it as a known permissive oracle into Slice 33, (c) add the criterion at Slice 33 instead, where the tool is actually run against real data.", "decider": "steward", "id": "TC-112", "status": "open"}
{"ts": "2026-07-29T17:26:04.480Z", "seq": 159, "kind": "todo", "summary": "A MUTATION-SWEEP SCRIPT THAT REVERTS WITH git checkout -- . DESTROYS UNCOMMITTED WORK, MEASURED LIVE. During Slice 32 fix-1 the implementer ran its mutation sweep against an UNCOMMITTED tree; the script revert step wiped its edits to survey.py, cyclonedx.py and .gitignore, and only the untracked constraints.py survived because git checkout -- . does not touch untracked files. Caught immediately from git status, re-applied, re-verified, COMMITTED FIRST, then swept again. Logged by the implementer as B-4. Nothing was lost, but the failure is SILENT in the sense that a sweep run against a reverted tree would report mutation results for code that is no longer there. THE RULE, which belongs in the sweep script itself rather than in a be-careful note per the standing fix-the-tooling directive: the script must REFUSE TO RUN when git status --porcelain is non-empty for tracked files, or must stash and restore around itself. Same family as TC-110 (a git command whose blast radius is wider than the actor expects) and the same remedy - make the tool refuse, do not ask people to remember.", "decider": "steward", "id": "TC-113", "status": "open"}
{"ts": "2026-07-29T18:01:07.527Z", "seq": 160, "kind": "todo", "summary": "TC-112 RULED (HITL 2026-07-29): option (a) - UNFREEZE THE SLICE 31 ACCEPTANCE SUITE FOR EXACTLY ONE ADDED CRITERION, and nothing else. The criterion: NO COMPONENT MAY CARRY A fathomdb:constraint THAT ITS OWN VERSION DOES NOT SATISFY. Rejected (b) carry the known-permissive oracle into Slice 33 and (c) add it at Slice 33 instead. WHY IT HAD TO BE RULED AT ALL: the Slice 31 suite was declared the contract and frozen after seven codex rounds, so Slice 32 brief forbids touching it - and the defect codex found by inspection (declarations attaching to every locked version of a name, making thiserror 2.0.18 and tokenizers 0.22.2 falsely direct) is PROVABLY invisible to that suite: mutations M13 revert-to-attach-every-version and M14 fallback-widens BOTH leave all 23 tests GREEN. The oracle cannot detect the defect it just shipped a fix for. Since depth and edit_sites are exactly the two fields Slice 33 decides on, and that survey feeds 0.8.22, a permissive oracle here propagates into dependency decisions. HARD BOUNDS ON THE UNFREEZE, so it cannot become a rewrite: exactly ONE criterion is added; NO existing test may be modified, relaxed, renamed or deleted; the suite goes 23 to 24 and no further; the new criterion must be demonstrated RED against the pre-fix code (M13 and M14 must now both FAIL, which is the proof it closes the hole) and GREEN against the fixed code; and it is subject to the same codex section 9 review as everything else. The general lesson recorded alongside: the 16-mutation sweep missed this because every mutation was derived from WHAT THE TESTS GRADE, so it proved the suite bites without ever asking whether the OUTPUT WAS TRUE.", "decider": "hitl", "id": "TC-112", "status": "resolved"}
{"ts": "2026-07-29T18:18:32.222Z", "seq": 161, "kind": "todo", "summary": "TC-113 CLOSED STRUCTURALLY, AND THE GUARD FIRED FOR REAL ON THE VERY NEXT ROUND. The Slice 32 mutation-sweep script now reads git status --porcelain --untracked-files=no and exits 2 listing the dirty paths, so it REFUSES to run against uncommitted work. It then refused for real while AC-SBOM-24 was still uncommitted - the hazard the implementer own B-4 disclosure created cannot recur, closed in tooling within hours rather than with a be-careful note. Verified by the Steward from the branch.", "decider": "steward", "id": "TC-113", "status": "resolved"}
{"ts": "2026-07-29T18:27:26.868Z", "seq": 162, "kind": "todo", "summary": "THE SBOM SPEC IS INTERNALLY INCONSISTENT ABOUT STALENESS ROW ORDER, and the inconsistency is in the SPEC, not the code. Verified by the Steward at file level: dev/design/0.8.20-slice-31-sbom-survey-tool.md:768 states literally that rows are sorted by (tier, ecosystem, name, locked_version), while :754 states shipped outranks dev-tooling; drives priority and :817 repeats that shipped is the tier that outranks the others in Slice 33 triage. Sorting on the RAW TIER STRING is alphabetical - dev-tooling, eval-only, shipped - so THE HIGHEST-PRIORITY TIER SORTS LAST, the exact opposite of what the field table promises. Raised as a codex section 9 [P2] at Slice 32 round 2. NOT ACTIONED IN SLICE 32, and the reason matters: the implementation matches the spec literal sort tuple AND the frozen oracle (test_report.py pins keys == sorted(keys) on the raw tier string, on both the in-process and written-artifact legs, as AC-SBOM-22), so there is NO DEFECT IN SLICE 32 WORK - codex is objecting to the specification. Severity is PRESENTATION, not falsity: every field is present and correctly labelled and only row ORDER differs, in contrast to the round-1 finding which emitted FALSE data into depth and edit_sites. Slice 33 can group by tier on read. TO RESOLVE, at Slice 33 or 0.8.22: decide whether row order is part of the consumer contract the tool owes; if it is, change BOTH the spec sort tuple and AC-SBOM-22 two sort assertions together, since they are pinned to each other.", "decider": "steward", "id": "TC-114", "status": "open"}
{"ts": "2026-07-29T18:36:45.320Z", "seq": 163, "kind": "todo", "summary": "THE SBOM SUITE STRUCTURALLY CANNOT CATCH THE INSTALL-PATH DEFECT CLASS, AND NO PYTEST CRITERION CAN FIX IT - a shell-level install-then-run smoke is the only guard that works. Proven by mutation, not argued: the implementer probed M17 (revert DEFAULT_TIERS_FILE to package-relative) against the committed tree and ALL 24 TESTS STAY GREEN. The reason is structural - every test drives the tool through sys.path or PYTHONPATH from the SOURCE TREE, where the package-relative and repo-relative paths resolve to the SAME FILE. Only the INSTALLED console script can distinguish them (M17 gives rc 3 installed after fix-2 error handling, rc 1 before it). That is why codex had to find the [P1] by reading, the same shape as fix-1. This is the SECOND proven hole in the Slice 31 oracle after TC-112, but it is a DIFFERENT KIND: TC-112 was closable by adding one criterion, and this one is NOT closable by any criterion at all, because the defect only exists in an execution mode pytest-from-source never enters. The implementer correctly did NOT add a criterion (seq-168 unfroze exactly one, and that is AC-SBOM-24) and wired nothing (seq-166). WHAT THE GUARD SHOULD BE: a shell-level smoke that (1) pip installs the tool into a throwaway venv, (2) invokes the INSTALLED console script, (3) asserts rc 0 and that artifacts are byte-identical to a source-tree run. The third part is the load-bearing one - it proves both invocation paths resolve THE SAME RULES rather than merely both succeeding. PLACEMENT IS OPEN: Slice 33 (which must run the tool and would exercise it naturally), or 0.8.22, or as part of whatever CI wiring the HITL eventually approves under seq-166 - note that wiring the suite into CI WITHOUT this smoke would gate on the one execution mode that provably cannot see this class of defect.", "decider": "steward", "id": "TC-115", "status": "open"}
{"ts": "2026-07-29T18:52:59.005Z", "seq": 164, "kind": "todo", "summary": "TC-115 RULED (HITL 2026-07-29): option (a) - THE INSTALL-THEN-RUN SMOKE IS PLACED AT SLICE 33, and a shell script is explicitly authorised as the mechanism. NOT Slice 32 (which is already at fix-2 and is being capped), NOT 0.8.22 (which is after publish), and NOT nothing. Slice 33 must install and run the tool as its actual job, so the smoke is a byproduct of real work rather than a synthetic extra. WHAT IT MUST DO: pip install into a throwaway venv, invoke the INSTALLED console script, and assert BOTH rc 0 AND that the emitted artifacts are BYTE-IDENTICAL to a source-tree run - the byte-identical half is the load-bearing part, because it proves both invocation paths resolve THE SAME RULES rather than merely both succeeding. IT IS NOT A PYTEST CRITERION AND CANNOT BE ONE: pytest runs from the source tree where the package-relative and repo-relative paths resolve to the same file, which is why mutation M17 left all 24 tests GREEN. PROPORTIONALITY RECORDED: this is dev tooling, not shipped code, and touches nothing in the published artifact; the realistic worst case is Slice 33 losing time to a broken install, not a defect reaching users. The earlier note that this smoke should land WITH any CI wiring is now MOOT - see the same-day ruling that the SBOM suite is not being wired into CI at all.", "decider": "hitl", "id": "TC-115", "status": "resolved"}
{"ts": "2026-07-29T19:03:55.848Z", "seq": 165, "kind": "todo", "summary": "THE tiers.toml RULE prefix = \"package\" IS A PREFIX MATCH THAT SILENTLY CREATES THE CATCH-ALL DESIGN 5.3 FORBIDS. Raised as the Slice 32 codex round-4 [P2] and handed to the Steward for an id; verified at file level: scripts/sbom-survey/tiers.toml:31 reads prefix = \"package\", and prefix matching means any path BEGINNING with that string matches - so a future top-level packages/ manifest would be silently tagged dev-tooling instead of raising UntieredManifestError. That undercuts REQ-4 loud-gap guarantee and contradicts section 5.3 explicit statement that there is DELIBERATELY NO CATCH-ALL. LATENT, NOT LIVE - VERIFIED BY THE STEWARD: there is no top-level packages/ directory in the repo today, so nothing is currently mis-tagged and the emitted survey is not wrong. NOT A SLICE 32 IMPLEMENTATION DEFECT: the rule is verbatim from the design section 5.3 frozen shape, so the code is doing exactly what it was told - this is the SECOND spec-originated finding after TC-114, and the pattern is worth noting in itself, that Slice 31 froze a specification whose own rules carry defects the implementation is obliged to reproduce. SUGGESTED FIX IS DATA-ONLY, no code change: split the single prefix rule into exact-path rules for package.json and package-lock.json. AC-SBOM-23 already proves matching is longest-prefix and order-independent, so the split is safe by an existing criterion. PLACEMENT: Slice 33 or 0.8.22 - it edits ruled data, which is why it is recorded rather than actioned inside the capped Slice 32.", "decider": "steward", "id": "TC-116", "status": "open"}
{"ts": "2026-07-29T19:21:54.901Z", "seq": 166, "kind": "todo", "summary": "lookup_error DOES NOT EXPLAIN 77 PERCENT OF unknown ROWS, breaking a promise section 5.8 makes in writing. Found by the Slice 33 online run (finding F1). The online survey returned 774 components with 13 unknown, but only 3 carry a non-null lookup_error - the other 10 have lookup_error = null. Design section 5.8 states that lookup_error records why a row is unknown so that section 5 of the findings doc writes itself; for 10 of 13 rows it does not. Composition of the silent 10: 6 are pypi rows whose declaring manifest has no reachable lock entry, and 1 is ort 2.0.0-rc.10 where latest resolved to null silently. HONEST DEGRADATION STILL HOLDS - REQ-10 is not violated, nothing is falsely marked current, so this is a REPORTING gap and not a correctness gap. NOT FIXED IN SLICE 33, which ascertains and never implements, and which is also forbidden to modify the sbom-survey implementation. THIRD SPEC-ORIGINATED FINDING after TC-114 and TC-116, which continues the pattern worth naming: Slice 31 froze a specification whose own promises the implementation cannot keep, and codex plus real runs keep discovering it downstream. Placement: 0.8.22, or whenever the sbom spec is next opened.", "decider": "steward", "id": "TC-117", "status": "open"}
{"ts": "2026-07-29T19:21:54.929Z", "seq": 167, "kind": "todo", "summary": "src/python/uv.lock IS INCOMPLETE - IT OMITS FIVE DECLARED OPTIONAL DEPENDENCIES, so uv sync from it does NOT reproduce the declared dev environment. Found by the Slice 33 online survey (finding F2) and INDEPENDENTLY VERIFIED BY THE STEWARD: the lock contains exactly 15 packages; pytest and hypothesis are present, but maturin, numpy, scipy, networkx and pyyaml are ALL ABSENT. Those five are precisely the M1-harness dependencies that a codex section 9 [P1] caused to be added to src/python/pyproject.toml optional-dependencies during 0.8.20 Slice 5 - the lock was evidently resolved over a subset of extras and never re-resolved after that change. CONSEQUENCE: anyone provisioning from this lock gets an environment that does not match the declared one, and the eval harness imports (numpy in particular) would fail. THIS IS THE SURVEY EARNING ITS KEEP - the manual Library Bump Steward sweep never surfaced it, and it is exactly the class of gap a mechanical SBOM was funded to find, which is a concrete answer to the question of whether the tool was worth three slices. NOT FIXED IN SLICE 33: re-resolving a lockfile is a manifest/lockfile edit, which every leg is forbidden to make. HAND-OFF TO 0.8.22 via the findings doc section 4.", "decider": "steward", "id": "TC-118", "status": "open"}
{"ts": "2026-07-29T19:28:28.348Z", "seq": 168, "kind": "todo", "summary": "THE SURVEY IS STRUCTURALLY BLIND TO PRE-RELEASE-ONLY DEPENDENCIES, AND THAT BLINDNESS HIDES REAL DRIFT ON A SHIPPED DIRECT DEPENDENCY. Slice 33 finding F4, root-caused and verified by the Steward. scripts/sbom-survey/sbom_survey/registry.py:92 reads payload[\"crate\"][\"max_stable_version\"] for cargo. The crate ort has NEVER published a stable release - live crates.io shows max_stable_version null and max_version 2.0.0-rc.13 - so the lookup returns None WITHOUT RAISING, the row degrades to unknown, and because lookup_error is only ever populated from a raised exception no reason is recorded. CONSEQUENCE, and it is live: the repo pins ort = \"=2.0.0-rc.10\" in fathomdb-embedder (verified, an EXACT pin on a pre-release) while upstream is at rc.13, and THE SURVEY CANNOT SEE THAT GAP. ort is a SHIPPED DIRECT dependency - the ONNX Runtime binding adopted at 0.8.16 - and ort-sys is its transitive twin. MINTED SEPARATELY FROM TC-117 RATHER THAN FOLDED, on the orchestrator argument which the Steward accepts: TC-117 is a REPORTING gap, a missing explanatory string with no correctness consequence, whereas this is a COVERAGE gap - a permanent blind spot for any dependency that lives on pre-releases BY DESIGN, and this repo has exactly one, deliberately. The remedies differ too: TC-117 needs lookup_error populated, this needs the tool to consider max_version or to report pre-release-only as a distinct status. REQ-10 still holds - nothing is falsely marked current - so this is a LIMIT ON WHAT THE TOOL CAN SURVEY, not a breach of the honesty guarantee. NOT FIXED IN SLICE 33, which may not modify the implementation. Placement 0.8.22.", "decider": "steward", "id": "TC-119", "status": "open"}
{"ts": "2026-07-29T19:28:28.378Z", "seq": 169, "kind": "todo", "summary": "TC-106 WIDENED AND SUPERSEDED - THE DEPENDABOT COVERAGE GAP IS THREE TRACKED MANIFEST DIRECTORIES, NOT ONE, AND dependabot.yml OWN HEADER CLAIM IS MECHANICALLY FALSIFIED. Slice 33 finding F3 extended; verified by the Steward. The set-difference between discovered tracked manifests and the five configured Dependabot directories (/src/python, /, /src/ts, /, /) leaves THREE uncovered: (1) dev/tools/mermaid/ npm, as originally recorded; (2) tools/docs/ pip, holding mkdocs==1.6.1, NOT PREVIOUSLY RECORDED ANYWHERE; (3) scripts/sbom-survey/ pip - CREATED BY SLICE 32 ITSELF THREE DAYS AGO AND ALREADY UNCOVERED, which is the tool discovering a gap its own creation made. NONE of the three appears on dependabot.yml Deliberately NOT covered list, so none is an intentional carve-out. dependabot.yml:4 asserts in writing that the five entries cover every SHIPPED/tracked manifest; the survey mechanically falsifies that. THE DEFENSIBLE READING, recorded so the finding is not overstated: all three are dev-tooling tier, so the header survives a narrow reading of SHIPPED. THE COUNTER, which the Steward finds stronger: the R-DEP-2 precedent IN THAT SAME FILE added the root npm entry for a DEV-TOOLING manifest precisely because uncovered dev tooling carried live advisories - so the narrow reading is exactly the reasoning that has already failed once here. Also uncovered but VACUOUS: src/ts/npm/linux-x64-gnu/package.json, a publish stub with no dependencies key contributing zero components - noted only so the set-difference is complete. NOT LIVE TODAY: mermaid-cli 11.16.0 is current, so nothing is presently being missed; the gap is structural. .github/ is out of bounds for every Library Sweep leg, so this is a 0.8.22 hand-off via the findings doc section 4, not a Slice 33 fix.", "decider": "steward", "id": "TC-106", "status": "open"}
{"ts": "2026-07-29T19:31:07.859Z", "seq": 170, "kind": "todo", "summary": "TC-119 CORRECTED AND SUPERSEDED - THE TOOL FINDING STANDS, THE STEWARD FRAMING OF IT DID NOT. I told the Slice 33 orchestrator to write that a shipped direct dependency is three pre-releases behind and the survey cannot see it. The second half is right; THE FIRST HALF IS NOT DRIFT AND WAS MY ERROR. The orchestrator checked the manifest rather than the survey row and pushed back. Verified verbatim by the Steward at src/rust/crates/fathomdb-embedder/Cargo.toml:34-51: a 16-line rationale sits above the pin recording RC PIN ACCEPTED BY HITL 2026-07-08, that the ort 2.0 line publishes ONLY release candidates because 1.x is EOL, that rc.10 IS THE LATEST RC THAT BUILDS CLEANLY IN THE OFFLINE ENVELOPE because rc.12 carries an ort/ort-sys VitisAI EP mismatch, and that the eventual 2.0-STABLE bump is tracked as TC-9 - which is live and open in the todos ledger describing exactly this. The dependency is optional = true, default-features = false, behind a feature whose default = [] at line 53. So the pin is a RULED, REASONED, ALREADY-TRACKED decision and rc.10 is deliberately newest-that-builds, not neglect; moving to rc.13 is not self-evidently desirable when rc.12 was known broken. THE SHARP FINDING, which is the orchestrator framing and is better than mine: WHEN TC-9 COMES DUE, THIS TOOL CANNOT HELP WITH IT. It can neither see rc.10 to rc.13 nor say whether rc.13 resolves the VitisAI defect that is the stated reason for the pin. A DEPENDENCY-HYGIENE TOOL BEING BLIND ON PRECISELY THE DEPENDENCY THAT HAS AN OPEN HYGIENE TODO is the finding worth having. The mechanism is unchanged: registry.py:92 reads max_stable_version, which is null for ort permanently, so the row degrades to unknown with no lookup_error. Slice 33 section 4 cites TC-9 for the bump and mints nothing for ort itself - only the tool gap is new. SECOND, DEFINITIONAL QUALIFIER folded in here so it is not lost when the findings doc ages: ort tiers as shipped because tiers.toml derives tier from the path prefix src/rust/crates/, and that models NOTHING about optionality or feature-gating. An opt-in dependency behind default = [] tiers identically to rusqlite. NOT A DEFECT - the tier vocabulary was never specified to carry feature gating - but it means SHIPPED IN THIS SURVEY MEANS DECLARED IN A SHIPPED MANIFEST, NOT PRESENT IN THE SHIPPED ARTIFACT, and a 0.8.22 reader triaging by tier could over-prioritise an opt-in eval-only dependency.", "decider": "steward", "id": "TC-119", "status": "open"}
{"ts": "2026-07-29T20:10:58.638Z", "seq": 171, "kind": "todo", "summary": "TC-103 SPLIT BY THE HITL (2026-07-29). PART (b) - the #[non_exhaustive] attribute on EngineError - IS PULLED BACK INTO 0.8.20 as net-new SLICE 34, because adding it is ITSELF a breaking change and therefore needs a breaking venue: 0.8.20 is the breaking pair, 0.8.22 is internal dependency migrations and not a declared consumer break, and after 0.8.20 publishes the next declared breaking venue is 0.9.0. PARTS (a) AND (c) REMAIN AT 0.8.22 UNDER THIS ID - (a) decision #18 one named exception, reject_unrenderable_edge_epoch keeping InvalidArgument { msg } while the other six validate_write shape rejections use the message-less unit variant WriteValidation, and (c) the sibling-condition asymmetry. Both are taxonomy questions with no closing window. SIZING for Slice 34, measured from code: non_exhaustive does NOT affect matches within the defining crate, only downstream, so the surface is 70 EngineError:: references in fathomdb-cli (26), fathomdb-napi (22) and fathomdb-py (22); the fathomdb facade crate has zero. STEWARD FINDING RAISED WITH THE PLACEMENT, not yet ruled: EngineError is NOT the only facade enum with this closing window. The public re-export block at src/rust/crates/fathomdb/src/lib.rs:47-55 exposes TWELVE enums that are not #[non_exhaustive] - ComparisonOp, CorruptionKind, CorruptionLocator, EngineError, InitialState, LifecycleState, OpenStage, Predicate, ProjectionRole, ScalarValue, SoftFallbackBranch, TraversalDirection - and only PreparedWrite carries the attribute today. Downstream reference counts for the other eleven are individually small (2 to 24, 88 combined versus EngineError 70), so widening Slice 34 to cover them roughly doubles a mechanical change rather than adding a new kind of work. Doing EngineError alone leaves eleven types facing the identical problem at 0.9.0, knowingly.", "decider": "hitl", "id": "TC-103", "status": "open"}
{"ts": "2026-07-29T20:23:14.551Z", "seq": 172, "kind": "todo", "summary": "SURVEYING OUTSIDE A SINGLE GIT REPOSITORY - the one capability the SBOM tool genuinely lacks, recorded post-publish rather than built into Slice 33 (HITL 2026-07-29, we can always do more work on it later). REQUESTED: explicit include and exclude path arguments, where an exclusion may be a subdirectory, a PEER of the target, or even a PARENT of it; traversal that follows the exclusion rules; a traversal-step limit; and virtualenv awareness. WHAT ALREADY EXISTS, verified from code before deferring: the target directory is configurable via --repo; exclusions exist as DATA-DRIVEN tiers.toml rules with action = exclude, overridable via --tiers and pinned by an AC against hardcoding; and virtualenvs plus node_modules, target/ and every gitignored tree are STRUCTURALLY UNREACHABLE because discovery is git ls-files with zero os.walk, glob or rglob anywhere in the package - a stronger guarantee than any exclusion list. Traversal-step limits are MOOT: there is no traversal to limit. WHAT IS GENUINELY MISSING: paths outside the surveyed repository. git ls-files reports only paths INSIDE the repo, so a peer or parent exclusion has nothing to exclude, and a non-git directory cannot be surveyed at all. THE TRADE-OFF TO WEIGH WHEN THIS IS TAKEN UP: adding a filesystem-walk mode would restore exactly the hazard git ls-files was chosen to eliminate - tens of thousands of vendored manifests the project does not own - so it must be an explicit opt-in mode with its own exclusion discipline, never the default. WHEN IT MATTERS: if the tool is ever pointed at Memex, a vendored tree, or a multi-repo workspace. NO 0.8.22 decision needs it; 0.8.22 acts on THIS repository dependencies.", "decider": "hitl", "id": "TC-120", "status": "open"}
{"ts": "2026-07-29T20:29:05.429Z", "seq": 173, "kind": "todo", "summary": "seat-path-guard.sh READS PROSE AS A WRITE TARGET - a false positive that blocked a legitimate in-bounds write, twice in one slice. Slice 33, verified by the Steward. The orchestrator write of dev/plans/runs/0.8.20-slice-33-output.json was DENIED because the TC-118 finding text contains the literal string src/python/uv.lock INSIDE A DESCRIPTIVE ENGLISH SENTENCE, and the guard read that prose as the write target - it quoted the whole sentence back as a filename, which is the tell. The real target is under dev/plans/**, squarely inside the orchestrator boundary. THE GUARD ALREADY HAS THE RIGHT IDEA AND IT DOES NOT COVER THIS FORM: strip_heredocs (line 270) exists precisely to drop heredoc BODIES before scanning, with the comment that the body is DATA, not shell, and that markdown blockquotes and pasted shell examples routinely contain > and would otherwise deny at random. So the concept is understood; the write form actually used escaped it - most likely scan_inline_interpreter (line 584), which by design scans python3 -c and node -e bodies across the WHOLE command and therefore sees prose inside a quoted program string. WHY THIS MATTERS MORE THAN A NUISANCE: the natural way past it is to REWORD the prose so it no longer names the file the finding is ABOUT. The orchestrator refused to do that and escalated instead, and it was RIGHT - quietly reshaping text to slip past a path control is how the control stops meaning anything, and a finding that cannot name its own subject is a degraded finding. THE STRUCTURAL ASYMMETRY THAT MADE THIS BITE: line 735 reads Edit|Write) consider \"$FILE_PATH\" - the Write tool is evaluated on its EXPLICIT file_path argument and cannot suffer this defect at all - while line 736 reads Bash) scan_bash_command. The orchestrator seat has NO Write tool (Read, Bash, Grep, Glob, Agent, Task, SendMessage), so it is forced onto the only path that has the bug. FIX DIRECTIONS: extend the DATA-not-shell exemption to inline-interpreter bodies the way strip_heredocs already does for heredocs, or grant the orchestrator seat Write, or both. Second denial in the same slice (the first, rm -rf on a gitignored tests/__pycache__, was correct conservative behaviour and immaterial). Placement: DOC-HYGIENE or 0.8.22.", "decider": "steward", "id": "TC-121", "status": "open"}
{"ts": "2026-07-29T20:31:39.732Z", "seq": 174, "kind": "todo", "summary": "SLICE 34 PER-ENUM SCOPING STUDY - the twelve facade enums, read-only, with two Steward premise corrections. CORRECTION 1: enum-level #[non_exhaustive] does NOT break downstream CONSTRUCTION - only VARIANT-level does. Proven in-repo: PreparedWrite is enum-level non_exhaustive at lib.rs:3037 and fathomdb/tests/sdk_only_erasure.rs:53-61 still constructs PreparedWrite::Node from another crate. So field-carrying variants change nothing, and my brief was wrong to warn about it. CORRECTION 2: fathomdb-engine/tests/*.rs ARE downstream - integration tests compile as separate crates, so the attribute bites them, and that is where the hard cases live. My brief omitted them from the crate list. Proven: slice35_filter_grammar.rs:362 already works around SearchFilter non_exhaustive from inside the engine own tests/. THE SPLIT. Attribute-only, ZERO arms, zero risk: InitialState, LifecycleState, ProjectionRole, TraversalDirection. Mechanical: CorruptionKind (3 arms, obvious _ => Unknown fallback), SoftFallbackBranch (6 arms, only hot-path member, and the fallback string MUST be added to the closed literal unions at src/python/fathomdb/types.py:16 and src/ts/src/index.ts:222 plus three TS runtime guards in the same slice or the wildcard becomes a silent swallow). Involved but the strongest case: EngineError - 22 variants, and it DEMONSTRABLY GREW FIVE VARIANTS IN THREE RELEASES (VectorEquivalenceMismatch 0.8.18, IllegalTransition and NotLifecycleAddressable 0.8.19, ErasureIncomplete and ProjectionDestructive 0.8.20), every one a breaking change under today shape, which is exactly what the attribute exists for; 3 arms across cli/py/napi plus THREE binding fallback designs. POSTPONE FIVE, and the reason is categorical rather than cost: for ScalarValue, ComparisonOp, Predicate, OpenStage and CorruptionLocator the attribute does not merely cost arms, IT CONTRADICTS A RATIFIED DESIGN RECORD, and adding the wildcard silently VOIDS a compile-time drift alarm somebody signed. The first three are ADR D-F1 closed-enum pins (exactly 3, 4 and 2 variants at lib.rs:2407, 2417, 2443, enforced by slice35_filter_grammar.rs:41-98); OpenStage is the AC-035b pin at durability_open_path.rs:200-209 which NAMES the exhaustive match as the enforcement mechanism; CorruptionLocator is forbidden by dev/design/errors.md:326, which already solves unknown-locator with a named OpaqueSqliteError variant and explicitly rejects a free-form escape hatch - non_exhaustive IS that escape hatch at the type level. Honest sequencing for the D-F1 trio: settle reserved-gap 37 (G4 to G10 unification) first and let THAT decide whether the grammar vocabulary is open. EngineOpenError (lib.rs:3457, re-exported at fathomdb/src/lib.rs:49) is NOT in the twelve but is structurally identical to EngineError and shares its fallback design - doing both in one slice halves the design cost. AC-074 INTERACTION: NONE - the compile_fail doctests assert method absence and tests/ui/ asserts field/type-level construction; neither is affected. THE REAL COST OF EngineError, stated plainly: napi doc at fathomdb-napi/src/lib.rs:176-177 says no catch-all arm, drift between the Rust enum and the TS class set is a compile error - adding _ deletes that guarantee by design, and the TS side needs a new FDB_UNKNOWN code plus class in the same slice because src/ts/src/errors.ts:367-373 uses a never-typed exhaustiveness check. Python is lowest-friction: a base EngineError exception already exists at fathomdb-py/src/lib.rs:71 and every documented except EngineError handler would still catch an unknown variant.", "decider": "steward", "id": "TC-122", "status": "open"}
{"ts": "2026-07-29T20:38:23.175Z", "seq": 175, "kind": "todo", "summary": "TC-115 DISCHARGED - the install-then-run smoke is built, landed at f02dc5b4 in Slice 33, and PROVED TO BITE rather than merely asserted. scripts/sbom-survey/smoke-install-run.sh. Steward re-ran it on main after the landing: rc=0, PASS over 774 components, artifacts byte-identical between the installed and source-tree runs, both provenance guards and the vacuity guard holding. THE VALUE PROOF, which is why this closes rather than just ships: an A/B against a DELIBERATELY BROKEN WHEEL - pip install succeeds, then the installed package directory is deleted leaving the console script intact - gives rc=0 with a full PASS ... 774 components under the PRE-FIX script and rc=1 under the fixed one, with the message pip install reported success, so the wheel does not contain an importable package. Identical broken wheel, old script certifies it, new script refuses it. It also survived three codex rounds of its own: RUN A provenance ungraded (fix-2), probes grading a different import environment than the boundary they certify (fix-3), and transcript hygiene (fix-4). The durable rule it produced: A PROVENANCE PROBE MUST BE CONSTRUCTED TO BE INDISTINGUISHABLE FROM THE INVOCATION IT CERTIFIES, NOT MERELY TO LOOK LIKE IT. With CI wiring ruled OUT at seq-172, this smoke is now the ONLY guard for the install-path defect class, which is exactly what it was placed at Slice 33 to be.", "decider": "steward", "id": "TC-115", "status": "resolved"}
{"ts": "2026-07-29T20:38:23.204Z", "seq": 176, "kind": "todo", "summary": "TC-121 WIDENED - the seat-path-guard prose false positive reaches COMMIT MESSAGES too, not only file writes. Discovered independently during the Slice 33 land: the implementer avoided the guard a SECOND way nobody instructed, by passing the commit message via git commit -F from a scratchpad file so the guard never text-scanned the sentence containing src/python/uv.lock. Same defect, same class, second workaround found without direction - which is the strongest evidence that the false positive is broad rather than incidental, and that agents will route around it silently unless it is fixed. The original finding stands: line 735 Edit|Write) consider \"$FILE_PATH\" is immune because it reads the explicit path argument, while line 736 Bash) scan_bash_command text-scans and cannot distinguish a path in prose from a write target. strip_heredocs (line 270) already implements the DATA-not-shell exemption for heredoc bodies; the fix is to extend that reasoning to inline-interpreter bodies and to -F message files, and/or to grant the orchestrator seat the Write tool. WORTH RECORDING FOR THE FIX: the prose survived unrewordedly in the landed artifact - grep -c src/python/uv.lock on the findings doc returns 4 - so the control was satisfied without degrading the finding, but only because two agents escalated instead of editing around it.", "decider": "steward", "id": "TC-121", "status": "open"}
{"ts": "2026-07-29T20:44:30.070Z", "seq": 177, "kind": "todo", "summary": "TC-103 RE-UNIFIED AT 0.9.0-OR-LATER (HITL 2026-07-29), reversing the seq-178 split. NONE of the error-taxonomy work happens in 0.8.20: part (b), the #[non_exhaustive] attribute, is no longer Slice 34 and Slice 34 is CANCELLED; parts (a) the decision-#18 named exception and (c) the sibling-condition asymmetry were already deferred. VENUE, and this is the part that must not be lost: adding #[non_exhaustive] is ITSELF a breaking change and needs a breaking release. 0.8.20 was one and has been passed. 0.8.22 is deferred dependency migrations - napi 2 to 3, rusqlite 0.31 to 0.40 - which are INTERNAL and not a declared consumer-facing break, so 0.8.22 IS NOT A VENUE FOR PART (b) EITHER. Part (b) therefore lands at 0.9.0 or later. Parts (a) and (c) are taxonomy questions with no closing window and may still be taken at 0.8.22. THE STANDING COST: EngineError grew five variants across 0.8.18, 0.8.19 and 0.8.20, and until the attribute lands every future variant is a breaking change for any downstream exhaustive match, plus a reclassification between existing variants is a SILENT runtime break on the Rust facade (Python and TypeScript are single-rooted and fail loudly). THE ANALYSIS IS DONE AND SHOULD BE REUSED: TC-122 carries the full per-enum study - definition sites, downstream match sites classified exhaustive versus already-wildcarded, the 7-plus-EngineOpenError proceed set, and the five whose signed ADR D-F1 / AC-035b / errors.md pins the attribute would VOID rather than merely cost.", "decider": "hitl", "id": "TC-103", "status": "open"}
{"ts": "2026-07-29T20:53:06.840Z", "seq": 178, "kind": "todo", "summary": "TC-103 PLACEMENT FINAL (HITL 2026-07-29): PART (b), the #[non_exhaustive] attribute, IS PARKED AT 0.8.21 - not 0.8.20 (Slice 34 cancelled at seq-182) and not 0.9.0 (the Steward earlier framing, which missed the pattern). PARTS (a) and (c) REMAIN AT 0.8.22. WHY 0.8.21 IS THE RIGHT VENUE AND NOT A COMPROMISE: 0.8.21 is ODD, and the two-tier rule is even = publishable with HITL, odd = OOB LABEL-ONLY, so 0.8.21 does NOT publish - the attribute can land there breaking nothing, because no consumer receives it. F-19 records the pattern in terms: build label-only across odd micros then publish atomically at an even micro, which is precisely how OPP-12 ran (built 0.8.19 label-only, published 0.8.20). The change is therefore BUILT at 0.8.21 and PUBLISHED at 0.8.22, and 0.8.22 becomes the declared breaking venue. CARRY TC-122 WITH IT - the twelve-enum scoping study is done and should be the starting point, not repeated: 4 attribute-only with zero arms (InitialState, LifecycleState, ProjectionRole, TraversalDirection), 2 mechanical (CorruptionKind 3 arms; SoftFallbackBranch 6 arms plus the closed literal unions at src/python/fathomdb/types.py and src/ts/src/index.ts and three TS runtime guards, or the wildcard becomes a silent swallow), EngineError involved with three binding fallback designs and the napi compile-time drift alarm traded for a runtime fallback, EngineOpenError structurally identical and worth doing in the same slice, and FIVE POSTPONED because the attribute would VOID a signed pin rather than cost arms (ScalarValue, ComparisonOp, Predicate under ADR D-F1; OpenStage under AC-035b; CorruptionLocator under errors.md). For the D-F1 trio the honest sequencing remains: settle reserved-gap 37 first and let that decide whether the grammar vocabulary is open.", "decider": "hitl", "id": "TC-103", "status": "open"}
{"ts": "2026-07-29T21:02:50.520Z", "seq": 179, "kind": "todo", "summary": "V-GATE DISPOSITION ESTABLISHED - ONLY 1 OF 7 GATE ITEMS PASSED CLEANLY, AND V-7 IS A DECISION NOT A MEASUREMENT. Investigation 2026-07-29, read-only across fathomdb and memex, every claim cited. THE GATE IS SEVEN ITEMS, not three (dev/plans/runs/0.8.11-handoff-to-0.8.15.md:44-53), strict V-1 through V-7 order per HITL 2026-06-29. V-1 RAN-WITH-VERDICT PASS - all five intent tuples re-measured on a live-CE build, three of three LME tuples REVISED against the 0.8.3 priors, verdict verbatim at V-1-results.md:103 GATE: PASS - V-1 LANDS AND UNBLOCKS V-3 / V-7. V-3 RAN but PARTIAL BY DESIGN - legs A0 to A3 ran with Build-GO MET and Adopt-GO HELD to Memex 0.5.5 (V-3-results.md:3, :81-83), while leg (a), arm A4 EXP-AF-MH, was deliberately deferred and NEVER RAN, so the test-BOTH-iteration-types clause is only half discharged. V-7 NEVER RAN - no V-7-plan.md and no V-7-results.md in EITHER repo, independently ratified by both sides on 2026-07-07 (0.8.x-remaining-todos.md:48 not started; memex LEVERAGE-OPPORTUNITIES-LEDGER.md:340-346 has NOT run, with MEMEX concurrence at :347-351); master F-18 itself calls it the unrun V-7 leg. V-2, V-4, V-5 and V-6 ALSO NEVER RAN - per-query arm oracle, real classifier, multi-corpus, competitor head-to-head. TWO CORRECTIONS TO THE STEWARD PRIORS: 0.8.11 results PROVISIONAL V-1..V-7 is CONFIRMED verbatim at dev/experiments-ledger.md:334-338; and the V-3 verdict is Adopt-GO HELD, not Adopt-HOLD - substance right, wording wrong. ONE MATERIAL ADDITION THE STEWARD DID NOT HAVE: Memex independently re-ran A2 on its own conversational domain and got a NULL RESULT - A2 does NOT beat A0, all four 95 percent CIs include zero (memex dev/plans/runs/0.5.5-decomposer-slice4-output.json:45) with a 37 percent decompose_fallback_rate caveat - so V-3 adoption tail is WEAKER than the FathomDB-side record alone suggests. DOES V-7 STILL BIND ON 0.8.15: no ruling ever retired it, but it is NOT the operative blocker. V-7 text is CE-reranker default-on packaging DECISION (handoff:53); the empirical part it was ordered to settle, whether the router assumption of a live ce_score holds, was DISCHARGED BY V-1 which measured every tuple on a live-CE build. What remains is an HITL packaging call, and the standing rule dont-price-a-confirming-run-for-a-settled-decision says close it BY DECISION rather than commission a run. F-18 already demoted it to Memex #3, P1 but low value, gap about 0.03, cascade stays OFF regardless, with Memex concurring. And 0.8.15 was parked for reasons independent of V-7 - zero top-5 Memex need, ZERO OPP-12 dependency, the OPP-12 dispatcher being the async projection worker, a naming collision. TREATING V-7 AS THE GATE UNDERSTATES THE EVIDENTIARY HOLE BY A FACTOR OF FIVE.", "decider": "steward", "id": "TC-123", "status": "open"}
{"ts": "2026-07-29T21:02:50.548Z", "seq": 180, "kind": "todo", "summary": "NOTHING ON THE LIVE SURFACE TRACKS 0.8.15 OR 0.8.17 - the todos ledger contains ZERO references to either release, so their parked contents are already invisible, which is the silent-drop F-11 exists to prevent. Full per-item disposition established 2026-07-29 (see TC-123 for the V-gate half). 0.8.15 IN-LIBRARY DISPATCHER: the locus decision (Slice 0) is STILL WANTED and is effectively the whole release, never ratified, no implementation or STATUS doc; Slices 5/10/15/40 are STILL WANTED but EVIDENCE-UNDERMINED, because 0.8.11 Gate-2 concluded arm-selection headroom is within recall noise so the value is CONFIG-CARRYING TUNING NOT ARM ROUTING, and the EXP-Fr-acc classifier is a lexical TF-IDF LOWER BOUND at macro 0.768 and NEEDLE 0.500 with the real-classifier gate V-4 never run. Prereqs EXP-S at 0.8.14 and EXP-B-prime plus EXP-Fr-acc are SATISFIED. record_feedback F-8b is COMPLETE-ELSEWHERE as a NO-OP, the EXP-AF KILL overriding it. The KILL-path agent-side L2 prototype is COMPLETE-ELSEWHERE, already built at 0.8.11 Slice 35 with a working L2Router.recommend, generated registry.json, check_forbidden validator and zero fathomdb import. Section 10 multi-field FTS is SUPERSEDED to 0.9.x. Section 14 governed physical-purge is COMPLETE-ELSEWHERE, landed as OPP-12 transition/purge plus erase_source. THE FOUR ADJACENCIES AT SECTIONS 11 TO 13 - read.state latest-state list, touch/last-accessed verb, graph.neighbors label-scope and 50-cap - are STILL WANTED and UNSCHEDULED, real Memex-surfaced gaps with shipped consumer-side workarounds, and THEY ARE THE ACTUAL SILENT-DROP RISK IN THIS PAIR, not V-7: they must not be retired merely because they were parked inside the dispatcher plan file. 0.8.17 ROUTER HARDENING: Workstream A config-tuple registry and forbidden-composition validator are COMPLETE-ELSEWHERE IN SUBSTANCE as the l2-router prototype registry.json and check_forbidden, re-validated by V-1, with only in-library wiring outstanding, which presupposes 0.8.15. Workstream B EXP-AF productization is DEPRECATED - the account is a closed KILL, net -0.0126 CI -0.0274 to +0.0022, GO=False, and the NO-GO path is ALREADY EXECUTED IN CODE with feedback_arm defaulting False and _maybe_escalate an intentional no-op. record_feedback governance migration likewise DEPRECATED. Workstream C EXP-C is STILL WANTED but UNPOWERED, MDE 0.058 above epsilon at the N=200 AP-News cap with question-clustered bootstrap so more runs cannot tighten it; EXP-D is STILL WANTED but BLOCKED on unspent HITL spend approval; EXP-E is doubly BLOCKED needing EXP-D and D1/RAPTOR which is scoped in NO release. Workstream C has ZERO dispatcher dependency and is mis-filed under router hardening purely by co-location. The 0.8.17 position after 0.8.18 before 0.8.19 is SUPERSEDED by F-19.", "decider": "steward", "id": "TC-124", "status": "open"}
{"ts": "2026-07-29T22:07:56.828Z", "seq": 181, "kind": "todo", "summary": "BACKLOG - EXP-C / EXP-D / EXP-E, carried out of the retired 0.8.17 as an INDEPENDENT EVAL ITEM (HITL 2026-07-29, steps 1-5 approved). These were 0.8.17 Workstream C and have ZERO dependency on the dispatcher - they were mis-filed under router hardening purely by co-location, which is why they must not die with 0.8.17. STATE OF EACH, evidence in TC-124: EXP-C, productizing query --global, is STILL WANTED and UNBLOCKED since AP-News is on disk, but it is UNPOWERED - decide_084 MDE is 0.058 against an epsilon below that at the N=200 AP-News cap, and the bootstrap is question-clustered, so MORE RUNS CANNOT TIGHTEN IT; taking it up means either a larger corpus or a different decision rule, not more spend on the same design. EXP-D, the roughly 269-question acquisition, is STILL WANTED but BLOCKED on an unspent HITL spend approval; it was explicitly excluded from 0.8.11 Gate-0 and pinned to 0.8.17, which no longer exists. EXP-E, the Fork-E gate, is STILL WANTED but DOUBLY BLOCKED - it needs EXP-D and it needs D1/RAPTOR, which is SCOPED IN NO RELEASE; it is a gate document only. NO RELEASE SLOT is assigned by this entry - it is backlog, and scheduling it is a separate HITL call.", "decider": "hitl", "id": "TC-125", "status": "open"}
{"ts": "2026-07-29T22:07:56.858Z", "seq": 182, "kind": "todo", "summary": "BACKLOG - FOUR GOVERNED-VERB CANDIDATES rescued from the retired 0.8.15, and this is the item the disposition existed to save (HITL 2026-07-29). They were sections 11 to 13 of plan-0.8.15.md and were parked INSIDE the dispatcher plan file purely by co-location; the dispatcher is retired and these are NOT dispatcher work. THE FOUR: (1) op-store read.state - a latest-state read plus a list/scan over current state; (2) a touch / last-accessed verb; (3) graph.neighbors label-scope filtering; (4) a 50-result cap on graph.neighbors. ALL FOUR ARE STILL WANTED AND UNSCHEDULED. They are real Memex-surfaced gaps with shipped CONSUMER-SIDE WORKAROUNDS, which is exactly why they are easy to lose - nothing is broken today, so nothing forces the conversation. F-18 declassified the sections 10 to 14 adjacencies as NOT COMMITMENTS, which is correct as to scheduling and must not be read as retirement. NOTE what is NOT here: section 10 multi-field FTS is SUPERSEDED to 0.9.x by a standing ruling, and section 14 governed physical-purge is COMPLETE-ELSEWHERE, having landed as the OPP-12 transition and purge verbs plus erase_source in 0.8.19 and 0.8.20 - so of the six adjacencies only these four survive as pending. Each is a GOVERNED-SURFACE ADDITION, so each carries an allowlist delta and a sign-off whenever it is taken up. NO RELEASE SLOT assigned; scheduling is a separate HITL call.", "decider": "hitl", "id": "TC-126", "status": "open"}
{"ts": "2026-07-29T23:14:33.843Z", "seq": 183, "kind": "todo", "summary": "LEDGERWRITE HAS NO ALLOCATION PROTOCOL FOR CONCURRENT WRITERS - two writers branching from the same base both allocate the SAME seq, and nothing detects it until landing. Measured live 2026-07-29 during the DOC-HYGIENE-3 remainder, which is fittingly the very slice hardening ledgerwrite. The Steward cut the worktree at b6ab99b7, whose steward-ledger tip was seq-185. The Steward then appended seq-186 on main (the commission record). The branch, working from its own base, independently allocated seq-186 for its own entry. BOTH ARE seq-186 AND THEY ARE DIFFERENT ENTRIES. On rebase the append-only file merges cleanly - zero conflicts, because both are additions at the end - and produces a DUPLICATE seq, which check-ledgers.sh then rejects for non-contiguity at the worst possible moment, during landing, with a failure whose cause is several steps removed from its symptom. THE MECHANISM: ledgerwrite allocates from the .jsonl.seq sidecar, which is per-checkout, so two checkouts off one base necessarily agree on the next value. THE APPEND-ONLY PROPERTY THAT MAKES THE LEDGER TRUSTWORTHY IS EXACTLY WHAT MAKES THIS INVISIBLE TO GIT - a conflict would have been better, because git would have surfaced it. RELATED BUT DISTINCT FROM TC-120, which was a collision on TC ids allocated by hand; this is a collision on seq numbers allocated by the tool. Related to TC-88 (the .seq sidecar being strandable) as a third defect in the same sidecar mechanism. FIX DIRECTIONS: have ledgerwrite detect a duplicate seq at append time; or make check-ledgers.sh name the duplicate explicitly rather than reporting generic non-contiguity; or have the landing procedure re-number branch appends against the current main tip as a routine rebase step. NOTE FOR WHOEVER TAKES THIS: the Steward is a near-continuous writer to this ledger, so ANY long-lived branch that appends will hit this - it is not an edge case, it is the default outcome of the current protocol.", "decider": "steward", "id": "TC-127", "status": "open"}
{"ts": "2026-07-29T23:18:13.848Z", "seq": 184, "kind": "todo", "summary": "TC-127 CORRECTED - THE HAZARD IS REAL BUT IT WAS NOT MEASURED LIVE; IT WAS INFERRED FROM A STEWARD DIFF-RANGE ERROR. The original entry claimed a seq-186 collision was observed during the DOC-HYGIENE-3 remainder. IT WAS NOT. The orchestrator refuted it and the Steward verified the refutation from git: dev/steward/steward-ledger.jsonl is blob faceb1ab at ALL THREE of the true merge-base 2671346d, the branch HEAD, and origin/main, and the branch diff contains NO .jsonl or .jsonl.seq path at all. THE BRANCH NEVER APPENDED TO ANY LEDGER. The single seq-186 is the Steward own commission record, which arrived IN the branch base because the worktree had fast-forwarded before work started. THE STEWARD ERROR: I diffed b6ab99b7..HEAD, using the SHA I CREATED THE WORKTREE AT rather than the actual merge-base, and b6ab99b7 is an ancestor of 2671346d - so my range swept in MY OWN COMMITS and I read my own append as the branch. THE GENERALISABLE LESSON, which is why this correction is worth its space: WHEN VERIFYING A BRANCH, TAKE THE RANGE FROM git merge-base, NEVER FROM THE SHA YOU THINK YOU CUT FROM - a worktree can fast-forward, rebase, or be advanced by another actor between creation and inspection, and every conclusion drawn from the stale range is then about your own work. This is the trust-git-not-narration rule applied to ones own bookkeeping. THE HAZARD ITSELF STANDS AND THE ENTRY IS KEPT: ledgerwrite allocates from a per-checkout .jsonl.seq sidecar, so two checkouts off one base DO share the counter, the append-only property means git merges both additions with ZERO conflicts, and check-ledgers.sh then fails for non-contiguity at landing with a symptom several steps from its cause. The Steward is a near-continuous writer, so any long-lived branch that appends will hit it. It simply did not fire here, because this branch was disciplined enough not to touch the ledger at all - which is itself the cheapest available mitigation and worth recommending as practice.", "decider": "steward", "id": "TC-127", "status": "open"}
{"ts": "2026-07-29T23:20:20.874Z", "seq": 185, "kind": "todo", "summary": "INCIDENT - THE DOC-HYGIENE-3 [P1] FIRED FOR REAL AND SET core.bare = true ON THE PRIMARY REPOSITORY. Not theoretical, not a near-miss: measured and repaired by the Steward at 2026-07-29 18:18, with .git/config rewritten at 18:14:51, four minutes earlier. SYMPTOM: every work-tree operation in /home/coreyt/projects/fathomdb failed with fatal: this operation must be run in a work tree - git add, git commit and git status all refused - while git rev-parse HEAD still resolved, so the repository looked half-alive. git worktree list reported the primary as (bare). MECHANISM, exactly as codex round 2 described it before it happened: the new --staged-only snapshot machinery runs git init \"$SNAPSHOT\" BEFORE scrubbing the GIT_* environment. With GIT_DIR exported - which is what a linked-worktree hook does - git init does not create $SNAPSHOT/.git; it re-initialises WHATEVER GIT_DIR POINTS AT, and here that was the primary repository .git, which git init then marked bare. BLAST RADIUS, assessed before repairing: exactly ONE config line. bare = true was the only wrong value. remote origin, branch main tracking, user identity, the exp/subagent-persistence branch entry and core.hooksPath = scripts/hooks all survived byte-intact. Refs intact - main and origin/main both b00faa1a, HEAD a valid commit object. Working tree files all present, including four uncommitted Steward ledger appends that were on disk at the time (steward seq-187, todos TC-127) and were NOT lost. All four worktrees still registered. REPAIR: git config core.bare false, single command, verified by --is-inside-work-tree returning true, --show-toplevel resolving, git status listing exactly the four expected modified files, and git worktree list showing the primary on main again. WHY THIS IS RECORDED AS AN INCIDENT AND NOT A FOOTNOTE: an unreviewed fix round, running in a linked worktree with an installed hook, silently reconfigured the PRIMARY checkout - the one holding the Steward uncommitted append-only ledger writes. It is the TC-110 blast-radius family (a git command reaching further than the actor expected) and the TC-121 family (a guard blocking the very path it certifies) arriving together. The orchestrator had already caught the defect in review and had a fix in flight; the incident happened anyway because the defective code was already committed and runnable. LESSON: a git init whose GIT_DIR is not scrubbed is not a sandbox, it is a remote-control for whatever repository the environment names.", "decider": "steward", "id": "TC-128", "status": "open"}
{"ts": "2026-07-29T23:38:02.809Z", "seq": 186, "kind": "todo", "summary": "TC-128 UPDATED - THERE WERE TWO OCCURRENCES, THE SECOND WAS DELIBERATE, AND THE IMPLEMENTER DISCLOSED IT UNPROMPTED. Occurrence 1 at 18:14:51, repaired by the Steward at 18:18. OCCURRENCE 2 AT 18:26:28, AFTER that repair, and it was not an accident of the code: to capture contrast RED evidence the implementer EXECUTED THE KNOWN-BUGGY PRE-FIX GATE (edcce999) with the real linked worktree GIT_DIR exported, pointed at the live repository. In its own words, it ran a binary it already knew was destructive-under-that-condition against the real repository in order to watch it be destructive, when a throwaway fixture would have given identical evidence at zero risk. It repaired the damage (config mtime 18:27:39), froze execution of every prior gate revision, and disclosed the whole thing unprompted; the Steward independently verified the primary is healthy - core.bare false, hooksPath scripts/hooks, work-tree true, main equals origin/main at 675ac4f0, four worktrees, clean status. THE GENERALISABLE RULE, which is the durable output and is distinct from the code defect: NEVER EXECUTE A BINARY YOU ALREADY KNOW TO BE DESTRUCTIVE-UNDER-CONDITION-X AGAINST THE LIVE REPOSITORY IN ORDER TO DEMONSTRATE CONDITION X. Reproduce it in a throwaway fixture. The evidence is identical and the blast radius is zero. A negative control does not need a real victim. CODEX PREDICTED SYMPTOM WAS WRONG AND THE STEWARD PROPAGATED IT: codex round 2 said the mis-ordering would BLOCK the commits the gate certifies, and the Steward repeated that framing. Measured, it did NOT block - the manifest cd \"$(git rev-parse --show-toplevel)\" degrades to cd \"\" which is a bash no-op returning 0, so the verdict stayed correct. The [P1] is entirely right and its real symptom is WORSE than predicted: a WRITE INTO THE LIVE REPOSITORY, plus a mechanism silently resting on behaviour POSIX leaves unspecified. That second half is now closed independently - the snapshot must prove it resolves to ITSELF before the sweep runs, or the gate prints NOT CHECKED and exits 0.", "decider": "steward", "id": "TC-128", "status": "open"}
{"ts": "2026-07-29T23:58:51.017Z", "seq": 187, "kind": "todo", "summary": "TC-100 IS RESOLVED AND THE LEDGER WAS STALE - corrected by the Steward after the DOC-HYGIENE-3 orchestrator flagged it. The bare-substring token-scan defect was FIXED IN THE TREE at c358ad1a, fix(DOC-HYGIENE-3): GREEN - whole-token matching, three scan roots, bare-number legs, which landed with the machinery half; scripts/commission-manifest.sh now uses whole-token matching so C-1 no longer matches inside TC-15. The entry stayed open because nothing closed it when the code landed. Verified from git by the Steward. NOTE THE PATTERN, which is the reusable part: a todos entry marked open while the code is already fixed is the mirror image of a plan doc marked live while the work is dead, and both mislead in the same way - the record asserting a state the repository contradicts. The orchestrator caught this by reading the code rather than the ledger, which is the trust-git-not-narration rule applied to our own bookkeeping.", "decider": "steward", "id": "TC-100", "status": "resolved"}
{"ts": "2026-07-29T23:58:51.046Z", "seq": 188, "kind": "todo", "summary": "THE NEW check-design-refs SUITE HAS NO AUTOMATIC RUNNER - scripts/tests/test_check_design_refs.sh is invoked by nothing. Verified by the Steward: no reference in scripts/agent-test.sh and none in any .github/workflows file. So a 30-arm suite guarding a commit-path gate runs ONLY when a human or agent remembers to run it by hand, which over time is the same as not running. WHY IT WAS LEFT: wiring it needs a .github/ edit, and .github/ is barred to every DOC-HYGIENE effort - only Slice 40 may touch it (seq-185). The orchestrator correctly declined rather than reaching outside its boundary, and disclosed the gap instead of quietly wiring it. HAND TO SLICE 40, which is the first and only slice permitted in .github/ this release. NOTE THE INTERACTION WITH seq-172 AND seq-185, because they pull in opposite directions and whoever takes this must weigh both: seq-172 ruled the SBOM suite is NOT wired into CI at all, so there is a live precedent for a suite that exists without CI; but this suite guards a PRE-COMMIT GATE that can block commits, which is a different risk profile from an informational survey. Also note the ordering constraint - TC-16/F-30 must be GREEN LOCALLY before any commit reaches CI (seq-185), so any wiring lands after that determination, not before. THE HONEST MINIMUM if CI wiring is declined: at least register it in scripts/agent-test.sh so a local full-suite run reaches it - though note agent-test.sh own aggregate exit is vacuous today (TC-16), so that is a weaker guarantee than it sounds.", "decider": "steward", "id": "TC-129", "status": "open"}
{"ts": "2026-07-30T00:12:29.591Z", "seq": 189, "kind": "todo", "summary": "TC-92 DISCHARGED - the requirement-id back-link discipline is now MECHANICAL, landed 85d44c74. scripts/check-design-refs.sh (660 lines, 925-line suite) is wired into the ACTIVE pre-commit hook and fires the moment an id is minted, so the gap closes at mint time rather than being discovered at commission time as it was on Slice 22. DESIGN POINTS THAT MATTER: the predicate is PER-TOKEN, not per-entry - the orchestrator initially specified per-entry and the implementer flagged that it would have made the Slice 21 exemption dead code and let new ids on six slices slip, so per-token was accepted on the merits. Two frozen exemptions (Slice 20/TC-45, Slice 21/ac_002) with a hard ratchet on everything else. It NEVER READS A LADDER status, so a new gap on an ALREADY-LANDED slice fails too - the discipline does not lapse once a slice closes. And it DRIVES commission-manifest.sh rather than re-deriving its tokens, so the two cannot disagree; the manifest is unmodified with byte-identity verified 14/14 across the whole unit. ZERO hand-written traceability notes were added - the explicit anti-goal, since d30ef52f superseded that approach and twelve such blockquotes already remain as owed cleanup. NOTE THE RESIDUAL, named in the script header rather than implied: an unmerged index and a snapshot that fails to build cannot be certified, and both are announced loudly as NOT CHECKED and let through at exit 0 rather than refused - a deliberate divergence from check-staged-ledger-sidecars.sh, which exits 2 on an unmerged path, on the reasoning that that gate protects an invariant conflict resolution actively breaks while this one is coverage discipline where a refusal nobody can act on costs more than the miss.", "decider": "steward", "id": "TC-92", "status": "resolved"}
{"ts": "2026-07-30T00:13:01.032Z", "seq": 190, "kind": "todo", "summary": "TC-53 DISCHARGED - ledgerwrite now guards against shell-substitution residue, landed 85d44c74. THE BOUNDARY WAS DECIDED BY CORPUS, NOT BY TASTE, which is what makes it trustworthy: measured against all 378 records in the three live ledgers it gives ZERO FALSE REFUSALS at a 2.2 percent warn rate. It REFUSES only zero-information residue - an unclosed substitution opener (one that actually RAN cannot leave one; unterminated is a syntax error), an empty brace pair, C0/DEL control characters, and text that is nothing but shell punctuation after stripping. It WARNS on everything ambiguous - surviving openers, bare variable sigils, backticks, an empty command-substitution pair, doubled interior spaces. Two candidate refusals were DEMOTED on evidence, including the empty command-substitution pair because TC-53 own summary contains it as prose. EVERY REFUSAL NAMES BOTH THE SINGLE-QUOTING FIX AND THE ACCEPT-SHELL-RESIDUE OVERRIDE, on the explicit reasoning that a refusal with no sanctioned path pushes an agent into hand-appending to an APPEND-ONLY ledger, which is worse than the corruption - the TC-121 lesson applied before it could recur. The corpus arm reads the ledgers FROM DISK so it keeps testing the guard as they grow. HONESTY NOTE carried in the docstring, the commit and the README: NONE OF THE THREE MEASURED INCIDENTS IS DETECTABLE IN-TOOL, because the shell expands before ledgerwrite sees the string - which is exactly why the dev/steward/README.md half was load-bearing and shipped. 52/52 tests green on main. FIRST-DAY FIELD TEST, unplanned and worth recording: THIS VERY ENTRY WAS REFUSED on its first attempt, because the Steward summary quoted shell syntax as prose while describing what the guard refuses. The guard fired correctly, named the override, and the override worked. A guard that refuses a description of itself and still leaves a sanctioned path is behaving exactly as designed.", "decider": "steward", "id": "TC-53", "status": "resolved"}
{"ts": "2026-07-30T01:09:10.942Z", "seq": 191, "kind": "todo", "summary": "VACUOUS-GREEN: dev/plans/runs/** is EXCLUDED from markdownlint (.markdownlint-cli2.jsonc, rationale \"agent-emitted run logs, not maintained prose\"), so scripts/agent-lint-md.sh exiting 0 says NOTHING about any file in that directory. The exclusion was written for machine-generated run logs, but the directory also holds MAINTAINED PROSE that the program depends on: STATUS-0.8.20.md (the board) and every STEWARD-SESSION-HANDOFF-*.md (the cold-start path). Discovered 2026-07-30 when a subagent correctly reported its own lint green as vacuous for a hand-off it had just written. Same class as the canary that hashed nothing on both sides and compared equal. Candidate fix: narrow the exclusion so it covers generated run logs only (output.json closure artifacts, codex transcripts) while linting the maintained prose - NOT a blanket un-exclude, which would light up a large backlog of genuinely-generated files. NOT blocking 0.8.20; do not attempt mid-release.", "decider": "steward", "id": "TC-130", "status": "open"}
{"ts": "2026-07-30T01:17:12.330Z", "seq": 192, "kind": "todo", "summary": "COMMISSION BRIEFS NEED A MECHANICAL FACT-CHECKER. Across three independent adversarial reviews of Steward-drafted briefs, every CRITICAL finding was the same defect class: a confidently-worded factual assertion the Steward did not measure. Instances: (a) Slice 40 v1 asserted release.yml fails to invoke cargo publish --dry-run - backwards, the TEST has swapped tier names, and the brief pre-authorised deleting an idempotency guard (seq-195); (b) Slice 40 v2 asserted a v* tag is the only irreversible publish path - false, workflow_dispatch with dry_run=false publishes for real and verify-release-gates only warns (seq-196); (c) Slice 39 asserted DenseReadiness is recorded signed in the allowlist - it appears ZERO times, and the instruction would have shipped a false sign-off onto docs.rs (seq-197); (d) both briefs asserted an AWAITING HITL SIGN-OFF string that was removed at seq-164 (seq-197). Plus ~17 off-by-N file:line cites across the same drafts. RESOLVE DOES NOT FIX THIS - tooling does, per the standing rule that a guardrail failure is fixed in the repo, not with a be-careful note. Proposal: a checker that parses a brief for (i) file:line citations, (ii) quoted source strings, (iii) claimed grep counts, and verifies each against HEAD, emitting a per-claim PASS/FAIL. Every one of the instances above is mechanically detectable. Natural home is scripts/, sibling to check-design-refs.sh. NOT blocking 0.8.20; the interim control is an adversarial review subagent per brief, which has caught 100 percent of these but costs a full review cycle each time.", "decider": "steward", "id": "TC-131", "status": "open"}
{"ts": "2026-07-30T03:01:52.934Z", "seq": 193, "kind": "todo", "summary": "PLACED AT 0.8.21 by HITL ruling (steward seq-198, 2026-07-30). TC-131, the commission-brief fact-checker first raised at seq-192, is BUILT AT 0.8.21 - not in 0.8.20. Rationale recorded with the ruling: only two commission briefs remain in 0.8.20, and the interim control (an adversarial review subagent per brief) has caught 100 percent of the defect class, so the tooling is not on the 0.8.20 critical path; and 0.8.21 can use the four recorded failure instances as TEST FIXTURES - seq-195 (Slice 40 v1: the cargo publish --dry-run assertion was backwards and the TEST had swapped tier names), seq-196 (Slice 40 v2: workflow_dispatch with dry_run=false publishes for real and verify-release-gates only warns), and seq-197 (Slice 39: DenseReadiness appears ZERO times in the allowlist, plus the AWAITING HITL SIGN-OFF string removed at seq-164). Scope is unchanged from seq-192: parse a brief for file:line citations, quoted source strings and claimed grep counts, and verify each against HEAD, emitting a per-claim PASS/FAIL. Natural home scripts/, sibling to check-design-refs.sh. NOTE 0.8.21 is an ODD micro, therefore OOB and label-only - building the checker there publishes nothing. Status stays open; this record places it, it does not close it.", "decider": "hitl", "id": "TC-131", "status": "open"}
{"ts": "2026-07-30T12:01:32.542Z", "seq": 194, "kind": "todo", "summary": "TRANSCRIPT-HYGIENE GATE IS VACUOUS AT PRE-COMMIT - it did not fire on a real unredacted transcript I staged into a commit on 2026-07-30. Sequence: the Slice 39 orchestrator wrote codex transcripts into the PRIMARY checkout as well as its worktree; it redacted the branch copies (TC-86 predicate-2 hit, repaired with --redact) but the untracked primary copies stayed raw. My git add -A swept them in. The commit succeeded with only [pre-commit] md-safe-fix running - no hygiene arm fired at all. Measured afterwards on the swept blob: ZERO redaction markers, and scripts/check-transcript-hygiene.sh on that file exits 1 with \"1 out-of-repo directory-inventory block, 36 lines of listing\". The branch copy carries 1 REDACTED marker and is clean. So a file the gate DEMONSTRABLY rejects passed straight through commit into a PUBLIC repo. Recovery: the commit was never pushed (origin/main stayed at 3e9d6d12), main was reset, the tag holding the blob was deleted, the blob is unreachable and GC-eligible, and nothing left the machine. The Slice 39 orchestrator independently reported this same gap in its own closure - check-transcript-hygiene.sh returns 0 on an UNTRACKED transcript, so a pre-commit spot check is silently vacuous and only preflight --landing after committing is honest. This is the TC-86 mechanism recurring one layer up: the redaction step exists, but nothing between writing a transcript and committing it inspects the contents. Fix in the TOOLING, per the standing rule: the pre-commit hook must run the hygiene check over STAGED transcript paths, treating an untracked-but-staged file as in scope, and must fail closed rather than returning 0 when it finds nothing to check. A control proving it rejects a known-bad transcript is part of the fix - this gate has now been vacuous twice.", "decider": "steward", "id": "TC-132", "status": "open"}
{"ts": "2026-07-30T14:32:24.595Z", "seq": 195, "kind": "todo", "summary": "BOARD-CURRENCY BLIND SPOT: check-board-currency.sh certifies rc=0 on a board whose ladder row says a LANDED slice is NOT_STARTED. Distinct from the seq-204 fractional-id defect at :123.", "area": "scripts/check-board-currency.sh", "epistemic": "verified", "id": "TC-133", "owner": "steward", "priority": "p2", "status": "open", "refs": ["git:46b9365e", "seq:206", "file:scripts/check-board-currency.sh", "file:dev/plans/runs/STATUS-0.8.20.md"], "body": "MEASURED at 46b9365e by the Steward, 2026-07-30. dev/plans/release-state-0.8.20.json records Slice 39 as LANDED at 91db34d8 with next_slice=40 and remaining_ladder=[40]. At the same commit, STATUS-0.8.20.md:76 (the Slice 39 ladder row) read \"NOT_STARTED - the immediate next slice\", :46 said \"Next: Slice 39 (R-20-DOC), then Slice 40\", :37 and :79 both said \"Ladder remaining: 39 -> 40\", and there was no Slice 39 close-record section. bash scripts/check-board-currency.sh exited 0 anyway, and so did the always-on CI board-currency job.\n\nWHY IT PASSES: the check verifies that a landing merge subject of the form \"merge(<version>): Slice[- ]<N>\" has its short SHA cited LITERALLY SOMEWHERE in the board. 91db34d8 does appear on the board - but only inside the two GENERATED cells (the status-unblocks block and the immediate-next-action cell), which are rendered from the state file by check-release-state-views.sh. The generated half was therefore carrying the check while the entire hand-written half of the board contradicted it. Presence of a SHA is not currency of the row that SHA belongs to.\n\nWHY IT MATTERS: the board is what a commissioning agent reads. A brief generated or hand-written against this board would have inherited \"Slice 39 is the immediate next slice\" - the TC-89 failure class, recurring one level up. It is also the same shape as the fractional-id class ruled at seq-204: a check that exits 0 while being silently wrong about which unit it is talking about.\n\nDISTINCT FROM the SLICE-ID-HARDENING scope. That unit fixes check-board-currency.sh:123, where the slice-number capture takes \"39\" out of \"Slice 39.5\" and skips the real landing merge as superseded. This item is different: the capture is correct, the landing merge IS found, the SHA IS present, and the check still certifies a board that contradicts the state file on every hand-written row. Folding it into SLICE-ID-HARDENING would widen a ruled scope and is NOT done here.\n\nCANDIDATE REMEDY (proposed, not ruled): have the check cross-read release-state-<version>.json and assert that for every id in \"landed\", the board does not describe it with a not-started/in-flight/next marker, and that remaining_ladder agrees with any \"Ladder remaining:\" prose. Any such arm must be proven non-vacuous with a control that makes it fail (trap 18) - a check added here that hashes nothing on both sides is the exact defect class this release has fought hardest.\n\nPLACEMENT: unplaced. Natural home is 0.8.21 alongside TC-131 (the commission-brief fact-checker), which is the same \"nothing mechanically catches a confidently-wrong assertion\" family. HITL to rule."}
{"ts": "2026-07-30T15:07:54.972Z", "seq": 196, "kind": "consideration", "summary": "DO NOT BULK-REMOVE the \"Requirement traceability\" notes: there are TWELVE not eight, and several carry AUDIT FINDINGS that design_refs does not replace. Removal would destroy information.", "area": "dev/design", "epistemic": "verified", "id": "TC-134", "owner": "steward", "priority": "p3", "status": "open", "refs": ["git:09d47443", "git:d30ef52f", "file:dev/design/vector.md", "file:dev/design/errors.md"], "body": "CONTEXT. STEWARD-SESSION-HANDOFF-2026-07-30-A section 9 carries an owed item: \"Eight Requirement traceability notes remain in dev/design/** from before the design_refs fix (d30ef52f). Removing them is owed. Do not write new ones.\"\n\nMEASURED at 09d47443, and BOTH halves of that item need qualifying.\n\nCOUNT. grep -rn \"Requirement traceability\" dev/design/ returns TWELVE occurrences across TEN files, not eight: vector.md, embedder.md, errors.md (TWO), record-lifecycle-protocol/projection-registry-and-async-embed.md (TWO), 0.8.20-tc90-tc91-characterization.md, 0.8.20-tc57-write-race-characterization.md, 0.8.18-slice-0-vector-equivalence-publish-design.md, 0.8.18-slice-5-vector-equivalence-probe.md, 0.8.20-slice0-erasure-design.md, 0.7.0-vector-quant-pack1.md. I have NOT determined whether the handoffs \"eight\" used a different counting convention (e.g. files rather than occurrences, or excluding the ones added later on 2026-07-29). Given that this session already produced one wrong \"correction\" of exactly that shape - seq-209, where 31 and 35 were both right under different conventions - the count discrepancy is recorded, NOT resolved by assertion.\n\nTHE SUBSTANTIVE POINT, which matters more than the count. These notes are NOT uniformly redundant linkage. The design_refs mechanism DOES supersede the linkage function - check-design-refs.sh reports 15 slices, 34 declared tokens, 2 exemptions, ZERO gaps - so \"which requirement consults this doc\" is now machine-checked and the prose restating it is genuinely redundant. But several notes carry AUDIT FINDINGS that exist nowhere else and that design_refs cannot express. Examples: vector.md records that the file \"is a stub and carries NO material on #99\" - no vec0 column list, no kind/attr_* columns, no DELETE semantics - and names the real sources; errors.md records that the doc did not name InvalidArgument anywhere and so could not by itself settle decision #18, that this was true when written, and that Slice 22 has since settled it. Eight of the twelve are explicitly stamped \"corrected after independent audit\", \"added after independent audit\" or \"confirmed by independent audit\".\n\nCONSEQUENCE. A blanket removal keyed on the string \"Requirement traceability\" would delete real audit results along with the redundant linkage prose, and the audit results have no other home. The correct shape is a PER-NOTE triage: strip the linkage sentence, KEEP any audit finding, and give the kept material a heading that is not \"Requirement traceability\" so the ban on writing new ones still reads cleanly.\n\nSTATUS. NOT DONE, deliberately. Removing content from design docs is a record change and therefore an explicit HITL decision (steward hand-off section 5); it is not inside the reconcile mandate. The handoff item should be re-scoped from \"removing them is owed\" to \"per-note triage is owed\" before anyone acts on it. The ban on WRITING NEW ONES is unaffected and stands."}
{"ts": "2026-07-30T15:35:19.316Z", "seq": 197, "kind": "todo", "summary": "PLACED by HITL 2026-07-30 (steward seq-212, option b): FOLDED INTO THE IN-FLIGHT SLICE-ID-HARDENING unit, not deferred to 0.8.21. The Steward recommended 0.8.21; the HITL chose the fold.", "decider": "hitl", "epistemic": "verified", "id": "TC-133", "placement": "0.8.20 SLICE-ID-HARDENING (in flight)", "status": "placed", "refs": ["seq:212", "plan:dev/plans/runs/0.8.20-slice-id-hardening-commission-brief.md"], "body": "The unit now additionally owes: make check-board-currency.sh detect a board that CONTRADICTS release-state-<version>.json, not merely one that omits a landing SHA. For every id in `landed`, the board must not describe that slice with a not-started / in-flight / next marker, and any \"Ladder remaining:\" prose must agree with `remaining_ladder`. The recurrence arm is the deliverable, as with every other site in this unit, and it must be proven non-vacuous with a control that makes it fail - an arm that passes on both the fixed and unfixed tool is worthless here, and this whole item exists because a check exited 0 while being silently wrong.\n\nDISTINCT FROM SITE 1, which is already in the units scope: there the slice-number capture is wrong (39 taken from \"Slice 39.5\"); here the capture is correct, the landing merge IS found, its SHA IS present, and the gate still certifies a board every hand-written row of which contradicts the state file. During the entire period the board said Slice 39 was NOT_STARTED, check-board-currency.sh and the always-on CI job both exited 0, because 91db34d8 appeared inside the two GENERATED cells - the generated half was carrying the check for the hand-written half.\n\nOriginal entry: seq-195."}
{"ts": "2026-07-30T15:35:19.346Z", "seq": 198, "kind": "consideration", "summary": "RULED by HITL 2026-07-30 (steward seq-212, option a): the Requirement-traceability work is PER-NOTE TRIAGE. Blanket removal keyed on the string is ruled OUT. Placement of the triage still unplaced.", "decider": "hitl", "epistemic": "verified", "id": "TC-134", "status": "open", "refs": ["seq:212"], "body": "The hand-off section 9 item \"Eight Requirement traceability notes remain in dev/design/** from before the design_refs fix. Removing them is owed\" is RE-SCOPED by this ruling.\n\nTHE WORK, as ruled: per note - strip the redundant LINKAGE sentence, KEEP any audit finding, and re-head the kept material so the standing ban on writing NEW traceability notes still reads cleanly.\n\nWHY: measured at 09d47443 there are TWELVE occurrences across TEN files, not eight, and several carry audit findings with no other home - vector.md records the file is a stub carrying NO material on #99 and names the real sources; errors.md records that it never named InvalidArgument and so could not by itself settle decision #18, and that Slice 22 has since settled it. Eight of the twelve are stamped corrected/added/confirmed by independent audit. design_refs supersedes the linkage function - check-design-refs.sh reports 15 slices, 34 declared tokens, 2 exemptions, ZERO gaps - but cannot express a finding.\n\nNOT SETTLED BY THIS RULING: which release carries the triage. Still unplaced. Also unresolved and deliberately not asserted: whether the hand-offs \"eight\" used a different counting convention than my twelve. That discrepancy is recorded rather than resolved, because this session already produced one wrong correction of exactly that shape (seq-209).\n\nOriginal entry: seq-196."}
{"ts": "2026-07-30T16:59:20.483Z", "seq": 199, "kind": "caveat", "summary": "TC-RUBRIC-7 COLLIDES WITH THE ORPHAN-MARKER SCAN: persisting a codex §9 transcript to its required TRACKED path reddens check-release-state-views.sh, because the scanner cannot tell a marker that DELIMITS a region from one merely QUOTED.", "area": "scripts/check-release-state-views.sh", "epistemic": "verified", "id": "TC-135", "owner": "steward", "priority": "p2", "status": "open", "refs": ["git:2008f529", "seq:214"], "body": "MEASURED during SLICE-ID-HARDENING (landed 2008f529). codex quotes a literal generated-region BEGIN marker into its review transcript whenever it reads the board or the plan. check-release-state-views.sh enumerates candidate files with os.walk over the tree INCLUDING UNTRACKED FILES, so such a transcript is scanned and its quoted marker is reported as an ORPHAN generated-region marker. Measured rc=1 on 2 of the 3 transcripts that unit produced.\n\nCONSEQUENCE: TC-RUBRIC-7 requires §9 transcripts to be persisted at a tracked path. Doing so would redden a governed gate on main PERMANENTLY. The two obligations are in direct conflict and one of them has to move. The unit correctly refused both bad options - it did not invent an ad-hoc redaction rule, and it did not weaken the scanner - and held the transcripts outside the tree instead, which means the release currently has §9 evidence that is NOT persisted where the rubric says it must be.\n\nTHE STEWARD REPRODUCED IT WITHIN MINUTES, which is the strongest evidence it needs a mechanical fix rather than a discipline note. Writing the Slice-ID-HARDENING close record into STATUS-0.8.20.md, I quoted the marker verbatim while DESCRIBING this very hazard; check-release-state-views.sh immediately returned rc=1 ORPHAN generated-region marker on the board itself. Any document that discusses the mechanism - a codex transcript, a close record, a design doc, a hand-off - is a landmine. \"Just do not quote it\" already failed on the person writing the warning.\n\nCANDIDATE REMEDIES (proposed, none ruled): (a) exclude dev/plans/runs/codex/ from the orphan scan; (b) scan only TRACKED files, so untracked scratch cannot redden a gate - note this weakens the guard against a genuinely orphaned marker in a new file; (c) require the marker to be at line-start in a markdown comment context rather than anywhere in the line; (d) an explicit escape convention for quoting markers in prose. Any fix MUST be proven non-vacuous with a control that makes it fail, and must not stop detecting a REAL orphan - that is the whole purpose of the check.\n\nHITL RULING OWED. Until then, §9 transcripts stay out of the tree and TC-RUBRIC-7 is knowingly unmet for this release."}
{"ts": "2026-07-30T16:59:20.509Z", "seq": 200, "kind": "caveat", "summary": "TC-121 THIRD INSTANCE: seat-path-guard.sh matched a path string appearing as a JSON VALUE inside a closure payload and blocked the write. Prose is not a write target; the guard should match the resolved path, not payload contents.", "area": ".claude/hooks/seat-path-guard.sh", "epistemic": "verified", "id": "TC-121", "owner": "steward", "priority": "p2", "status": "open", "refs": ["git:2008f529", "seq:213"], "body": "THIRD recorded instance (the first two were during Slice 33). At the very end of SLICE-ID-HARDENING the orchestrator could not write its closure output.json: the guard denied with \"the orchestrator seat may not write ... it is under a tests/ path segment\", because the closure JSON quoted the path scripts/tests/test_sealed_worktree_guard.sh AS A STRING VALUE in its payload. Nothing was being written to that path.\n\nThe orchestrator diagnosed it correctly and worked around it by writing to scratch rather than softening the guard - the right call, and exactly what brief §5 required. But it cost real time at the end of an already long unit, and it will recur: EVERY closure record names the test files the unit touched, so a closure that mentions a test path is structurally at risk.\n\nCOMPOUNDING FACT WORTH RECORDING: the specific path that triggered it, scripts/tests/test_sealed_worktree_guard.sh, existed only because the STEWARD wrote a test source it should have commissioned (seq-213, the boundary breach the HITL flagged). The breach and this block are the same event seen twice.\n\nREMEDY DIRECTION (proposed, not ruled): the guard should decide on the RESOLVED WRITE TARGET - Edit/Write file_path, and for Bash the actual redirection/target of the command - not on any occurrence of a protected path substring anywhere in the payload. A guard that reads its own subject matter as a violation will keep firing on the documents that describe it, which is the same shape as TC-135 recorded this same day. Fix the tooling, do not add a note telling people to avoid mentioning paths."}
{"ts": "2026-07-30T17:44:22.534Z", "seq": 201, "kind": "observation", "summary": "A STALE NATIVE MODULE MAKES test-python LOOK CATASTROPHICALLY RED (40 collection errors) WHEN IT IS SIMPLY UNRUNNABLE. It needs a FOURTH report state: UNKNOWN-requires-rebuild. Not TC-97.", "area": "src/python", "epistemic": "verified", "id": "TC-136", "owner": "steward", "priority": "p2", "status": "open", "refs": ["git:fcae308e", "file:src/python/fathomdb/_fathomdb.abi3.so"], "body": "MEASURED 2026-07-30 at fcae308e, from the primary checkout, read-only, with FATHOMDB_TESTS_ALLOW_REBUILD deliberately unset.\n\n.venv/bin/python -m pytest -q src/python/tests gives \"Interrupted: 40 errors during collection\". Every one is the same ImportError: cannot import name IllegalTransitionError from fathomdb._fathomdb (src/python/fathomdb/_fathomdb.abi3.so). The compiled native module is STALE - it predates the 0.8.19 Phase-1 lifecycle symbols. NOT ONE of the 40 is a test failure.\n\nRULED OUT: this is NOT TC-97s pythonpath shadowing. Re-running with -o pythonpath= from a neutral cwd (/tmp) produces the IDENTICAL 40 errors.\n\nWHY IT MATTERS RIGHT NOW. Slice 39.5s whole deliverable is the TRUE red list, local and CI, and that list is the input to a single HITL disposition decision. An agent that runs test-python read-only and records \"40 errors\" publishes a FALSE RED into the exact document the decision will be made from - the failure class this release has fought hardest. The suites real state is UNKNOWN, not red.\n\nTHE STRUCTURAL POINT. agent-test.sh:231-236 authorises FATHOMDB_TESTS_ALLOW_REBUILD=1 and a conftest maturin develop when the interpreter is the checkouts own .venv. For the PYTHON suite that rebuild is a PREREQUISITE, not a convenience: without it the suite cannot even collect. So a red list needs FOUR states, not three - PASSED, FAILED, SKIPPED-prerequisite-absent, and UNKNOWN-requires-the-sanctioned-rebuild.\n\nCOMPOUNDING CONSTRAINT: the rebuild cannot simply be run to resolve it. In a WORKTREE it rebinds the shared virtualenv to the worktree source tree - the named agent-worktree-stale-base-trap, which has already cost two slices - so a worktree baseline can NEVER cover this suite honestly. In the PRIMARY it is sanctioned but is a build, which is neither the Stewards work nor a reporting units.\n\nCONSEQUENCE FOR THE BASELINE: the true local red list has TWO principled, nameable holes - the venv-gated suites a worktree cannot reach without the forbidden rebind, and test-python whose result is meaningless without a rebuild nobody is authorised to perform mid-unit. A list that names what it could not reach, and why, is worth more than one that looks complete. Recorded so a later session does not \"fix\" this by rebuilding blind or by filing 40 phantom failures."}
{"ts": "2026-07-30T18:13:38.235Z", "seq": 202, "kind": "observation", "summary": "LEAD ON PUBLISH GATE (i): 5 of the 7 pyright errors that kill CI verify+security are STUB-vs-SOURCE DRIFT in a TRACKED file - symbols present in the Rust binding source and ABSENT from the committed _fathomdb.pyi.", "area": "src/python/fathomdb/_fathomdb.pyi", "epistemic": "verified", "id": "TC-137", "owner": "steward", "priority": "p1", "status": "open", "refs": ["git:59ec30c7", "file:src/python/fathomdb/_fathomdb.pyi", "tc:TC-136"], "body": "MEASURED 2026-07-30 at 59ec30c7, from the primary checkout.\n\nbash scripts/agent-typecheck.sh on UNMODIFIED main is rc=1 with exactly SEVEN errors - the same seven the record says kill the ci.yml verify and security jobs in BOOTSTRAP, before their real work runs. They reproduce LOCALLY, which is the first useful thing to know: this is not a CI-environment problem.\n\nTHE DRIFT, measured. The three symbols named in five of the seven errors exist in the Rust binding source - dense_disabled 11 occurrences, dense_disabled_reason 4, vector_equivalence_refusal_count 2, under src/rust/crates/fathomdb-py/src/. In the TYPE STUB the checker actually reads, src/python/fathomdb/_fathomdb.pyi, dense_disabled appears ZERO times. The five errors are all of the form \"Cannot access attribute X for class Engine\" / \"for class OpenReport\".\n\nWHY THIS EXPLAINS CI EXACTLY. _fathomdb.pyi is TRACKED - I verified with git ls-files --error-unmatch - so CI reads the identical stale file and fails identically. By contrast _fathomdb.abi3.so is NOT tracked, so the separate local staleness recorded at TC-136 (the .so built 2026-07-09 against binding source last touched 2026-07-28, 19 days apart, missing IllegalTransitionError) is a LOCAL-ENVIRONMENT artifact and a DIFFERENT problem. Do not conflate them: TC-136 is an untracked build artifact, TC-137 is a committed file that is wrong in the repository.\n\nSCOPE, stated honestly. FIVE of seven are this shape. The other two are NOT and must not be assumed to share the cause: graph.py:153 is an assignability error between fathomdb._fathomdb.IdSpace and fathomdb.types.IdSpace - a duplicate-type problem, not a missing attribute - and the seventh is an unexpected `reason` parameter in test_vector_equivalence_probe.py.\n\nWHAT IS NOT PROVEN. I have NOT regenerated the stub and re-run pyright, so \"regenerating the stub clears five of seven\" is a well-evidenced HYPOTHESIS, not a measured fact. State it that way. The determination - and the fix - belong to whoever owns the disposition of the CI baseline, not to Slice 39.5, which reports and does not fix.\n\nWHY IT MATTERS NOW. Publish gate (i) (seq-202 as amended by seq-211) requires every ci.yml job THAT EXECUTED to conclude success. Four jobs fail; verify and security are two of them and both die in bootstrap on precisely these seven errors. If the stub-drift hypothesis holds, a stub regeneration would clear two of the four reds and materially advance a gate that currently blocks the first real publish since v0.8.9. That makes this the highest-value lead currently open on the release."}
{"ts": "2026-07-30T19:26:39.069Z", "seq": 203, "kind": "consideration", "summary": "PLACED AT 0.8.21 by HITL 2026-07-30 (steward seq-219). The per-note-triage SCOPE was already ruled at seq-212; only placement was open.", "decider": "hitl", "epistemic": "verified", "id": "TC-134", "placement": "0.8.21", "status": "placed", "refs": ["seq:219"], "body": "Work as ruled at seq-212: per note, strip the redundant linkage sentence, KEEP any audit finding, and re-head the kept material so the standing ban on writing NEW traceability notes still reads cleanly. Blanket removal keyed on the string remains ruled OUT. Twelve occurrences across ten files, not the eight the record says. Now placed at 0.8.21."}
{"ts": "2026-07-30T19:26:39.097Z", "seq": 204, "kind": "caveat", "summary": "RULED option (a) by HITL 2026-07-30 (steward seq-219): EXCLUDE dev/plans/runs/codex/ from the orphan-marker scan in check-release-state-views.sh.", "decider": "hitl", "epistemic": "verified", "id": "TC-135", "placement": "0.8.20 Slice 40 (baseline-disposition fix scope)", "status": "placed", "refs": ["seq:219"], "body": "Chosen over the alternative of scanning only TRACKED files, which would have traded a broad detection capability - catching a genuinely orphaned marker in a new file - for a narrow fix.\n\nUNBLOCKS TC-RUBRIC-7: once the exclusion lands, codex section 9 transcripts can be persisted at their required tracked path without reddening the gate. Until then transcripts stay OUTSIDE the tree and TC-RUBRIC-7 is knowingly unmet.\n\nAny implementation must be proven non-vacuous with a control that makes it fail, and must NOT stop detecting a real orphaned marker outside the excluded directory - that is the whole purpose of the check. Note the Steward reproduced this defect within minutes of it being reported, by quoting the marker verbatim in a close record while describing the hazard."}
{"ts": "2026-07-30T19:26:39.120Z", "seq": 205, "kind": "observation", "summary": "PLACED by HITL 2026-07-30 (steward seq-219): baseline disposition = option (b), FIX EVERYTHING. TC-136 is in Slice 40's fix scope, not waived and not deferred.", "decider": "hitl", "epistemic": "verified", "id": "TC-136", "placement": "0.8.20 Slice 40 (baseline-disposition fix scope, HITL option b)", "status": "placed", "refs": ["seq:219"], "body": "The HITL ruled the single baseline-disposition decision as option (b): every item Slice 39.5s baseline surfaced is FIXED inside Slice 40 - none waived, none deferred. The Steward had recommended option (a), fix-what-clears-gate-(i) and explicitly waive the flake, rust-windows and test-python. The HITL chose (b); recorded as given.\n\nConsequence: Slice 40 is materially larger than the brief drafted for it, and it remains ONE unit - seq-216 ruled it is not to be split and must be structured internally with checkpoints. The hand-off -B section 3a checkpoint table needs these folded in BEFORE Slice 40 is commissioned."}
{"ts": "2026-07-30T19:26:39.142Z", "seq": 206, "kind": "observation", "summary": "PLACED by HITL 2026-07-30 (steward seq-219): baseline disposition = option (b), FIX EVERYTHING. TC-137 is in Slice 40's fix scope, not waived and not deferred.", "decider": "hitl", "epistemic": "verified", "id": "TC-137", "placement": "0.8.20 Slice 40 (baseline-disposition fix scope, HITL option b)", "status": "placed", "refs": ["seq:219"], "body": "The HITL ruled the single baseline-disposition decision as option (b): every item Slice 39.5s baseline surfaced is FIXED inside Slice 40 - none waived, none deferred. The Steward had recommended option (a), fix-what-clears-gate-(i) and explicitly waive the flake, rust-windows and test-python. The HITL chose (b); recorded as given.\n\nConsequence: Slice 40 is materially larger than the brief drafted for it, and it remains ONE unit - seq-216 ruled it is not to be split and must be structured internally with checkpoints. The hand-off -B section 3a checkpoint table needs these folded in BEFORE Slice 40 is commissioned."}
{"ts": "2026-07-30T19:29:07.860Z", "seq": 207, "kind": "caveat", "summary": "STEWARD CORRECTION: TC-135 is UNPLACED. The HITL ruled the REMEDY (option a, exclude dev/plans/runs/codex/ from the orphan scan) but NOT the placement; the Steward inferred 0.8.20 Slice 40 and had no authority to.", "decider": "steward", "epistemic": "verified", "id": "TC-135", "placement": "UNPLACED", "status": "open", "refs": ["seq:219", "seq:220"], "body": "CORRECTING MY OWN ENTRY at todos seq-204. On 2026-07-30 the HITL answered \"2. A\" to the TC-135 question, which asked HOW the TC-RUBRIC-7 / orphan-marker-scan conflict is resolved: change the scanner, change the rubric, or accept the gap. Option (a) - exclude dev/plans/runs/codex/ from the scan - is therefore RULED and stands.\n\nWHAT WAS NOT RULED: where that work runs. I recorded placement as \"0.8.20 Slice 40 (baseline-disposition fix scope)\" and that was an inference, not an answer. TC-135 is NOT on Slice 39.5s baseline list - it is a separate tooling defect found during SLICE-ID-HARDENING - so the option-(b) fix-everything disposition does not reach it. Folding it into Slice 40s scope was me widening a units scope on my own authority, which the steward hand-off section 5 puts squarely in the always-HITL row. The HITL flagged it: \"careful with the milestones and placement.\"\n\nSTATE NOW: remedy RULED (option a, seq-219), placement UNPLACED and owed to the HITL. Until placed, TC-RUBRIC-7 stays knowingly unmet and codex section 9 transcripts stay OUTSIDE the tree.\n\nTC-136 and TC-137 are DIFFERENT and their placement stands: both ARE on the baseline list, and the HITL explicitly confirmed \"1. Is supposed to be fix in slice 40\". TC-134 at 0.8.21 was ruled directly (\"3. 0.8.21\")."}
{"ts": "2026-07-30T19:45:38.346Z", "seq": 208, "kind": "observation", "summary": "PLACED by HITL 2026-07-30: TC-135 goes to 0.8.21. The remedy was already ruled (option a, exclude dev/plans/runs/codex/ from the orphan-marker scan) at seq-219; only the placement was open, and it is now closed.", "decider": "hitl", "epistemic": "verified", "id": "TC-135", "placement": "0.8.21", "status": "placed", "refs": ["seq:219", "seq:220", "git:75341e4a"], "body": "Closes the last thread from the seq-220 correction. The Steward had inferred placement at 0.8.20 Slice 40 without authority and reverted it to UNPLACED at todos seq-207; the HITL has now ruled the placement directly: 0.8.21.\n\nWHY 0.8.21 AND NOT SLICE 40, recorded so nobody re-litigates it. TC-135 is not on Slice 39.5s baseline list - it is a separate tooling defect surfaced during SLICE-ID-HARDENING - so the option-(b) fix-everything disposition, which is scoped to that baseline list, never reached it. Folding it into Slice 40 would have widened an already-enlarged, un-splittable unit on the Stewards own authority. The Steward recommended 0.8.21 on those grounds and the HITL agreed.\n\nCONSEQUENCE CARRIED FORWARD, unchanged until the fix lands at 0.8.21: TC-RUBRIC-7 remains KNOWINGLY UNMET, and codex section 9 review transcripts continue to be held OUTSIDE the tree. Persisting one at its required tracked path still reddens check-release-state-views.sh (measured rc=1 on 2 of 3 transcripts). Do not invent a redaction rule and do not weaken the scanner in the meantime - both were considered and refused.\n\nSTILL LIVE AS A DOCUMENT HAZARD until then: never quote a generated-region BEGIN marker verbatim in any file in the tree, including a close record that is describing the hazard. The prior Steward reproduced the defect that way within minutes."}
{"ts": "2026-07-30T20:20:50.023Z", "seq": 209, "kind": "defect", "summary": "graph.search_expand has TWO mapper defects: it passes the NATIVE PyO3 IdSpace into a dataclass field typed as the SDK IdSpace, and it silently DROPS source_id and ce_score. The second is invisible to pyright and contradicts the SearchHit docstring.", "area": "src/python/fathomdb/graph.py", "epistemic": "verified", "id": "TC-138", "owner": "steward", "placement": "UNPLACED", "priority": "p1", "status": "open", "refs": ["seq:221"], "body": "MEASURED by a commissioned read-only probe and re-verified by the Steward directly from source.\n\nDEFECT 1 - TYPE. graph.py builds SearchHit(id=h.id, ...) passing the native fathomdb._fathomdb.IdSpace straight through, while types.SearchHit.id is annotated as the fathomdb.types.IdSpace frozen dataclass. engine.py:488 does it correctly (id=IdSpace(space=hit.id.space, value=hit.id.value)). Measured at runtime: engine.search hits give isinstance(sdk IdSpace)=True, graph.search_expand hits give False. Consequences: equality between hits from the two read paths fails, and dataclasses.asdict/astuple/replace break on graph hits. This is the SEVENTH pyright error and the ONE a stub edit cannot clear - pyright is nominal for dataclasses. A stub-level workaround exists (declare the native class a subclass of the SDK dataclass) and was MEASURED to silence it; it is REJECTED because issubclass is False at runtime, so it would encode a lie to hide a live defect.\n\nDEFECT 2 - PROVENANCE, and invisible to pyright. The same mapper omits source_id and ce_score entirely. Both fields have None defaults, so nothing type-checks as wrong. Measured on a real DB: native search_expand hit source_id=probe:1, SDK-returned hit source_id=None. The SearchHit docstring explicitly documents that source_id is None ONLY when the stored row really has NULL provenance - written before 0.8.20, or a governed row spared by the step-21 backfill under the TC-11 pin. So the SDK returns an answer that is not merely absent but MISLEADING per its own contract, on the field the erasure axis runs through.\n\nPLACEMENT. Defect 1 is unavoidably in Slice 40 - it is the last pyright error and publish gate (i) cannot pass without it. Defect 2 is NOT on Slice 39.5s baseline list and its placement is UNPLACED and owed to the HITL. Steward recommendation on record: fix both, since they are the same two lines of the same mapper. NOT placed by the Steward - the seq-220 lesson.\n\nOrigin: defect 1 dates to 6616db93 (0.8.19 Slice 15, C-2 typed SearchHit.id). Defect 2 predates it (ccfb2191, 2026-06-13)."}
{"ts": "2026-07-30T20:20:50.054Z", "seq": 210, "kind": "defect", "summary": "test-md-generators is RED in the primary checkout: context-clarity.sh uses $PRUNE_EXPR UNQUOTED in find, so its glob patterns expand against the repo root whenever matching local dirs exist. NOT on Slice 39.5s baseline list.", "area": "scripts/repo-prune/bin/context-clarity.sh", "epistemic": "verified", "id": "TC-139", "owner": "steward", "placement": "UNPLACED", "priority": "p2", "status": "open", "refs": ["seq:221"], "body": "A REAL bug that is ENVIRONMENT-TRIGGERED, found by a commissioned probe and proven decisively by it.\n\nMECHANISM. PRUNE_EXPR holds find predicates containing literal glob patterns (-path */node_modules -o -path */__pycache__ and six more). It is then interpolated into find UNQUOTED, so the shell word-splits AND glob-expands it against the repo root before find ever sees it. Traced with bash -x: -path */node_modules became -path node_modules/node_modules typescript/node_modules; */__pycache__ became scripts/__pycache__; */.pytest_cache and */.ruff_cache likewise. find then errors on the stray bare argument, returns an empty list with nonzero status, set -e kills the script, and no .md is produced. The generator failure is SWALLOWED by a || true at test_md_generators.sh:76, so only the missing output file surfaces - the real error never reaches the report.\n\nPROOF. bash -f context-clarity.sh __probe_noglob (globbing disabled, nothing else changed) returns rc=0 and produces both outputs.\n\nWHY IT WAS NOT ON THE 39.5 BASELINE. It fires only when the repo root contains a directory matching one of the eight patterns. Those are untracked local artifacts, so the red is present in the primary checkout and absent where they are not - which is why the 39.5 run, and the CI reachability, differ. Whether a pristine ubuntu-latest checkout produces a matching path is UNDETERMINED; bootstrap.sh npm-installs a root node_modules but not a nested node_modules/node_modules.\n\nPLACEMENT UNPLACED and owed to the HITL. The fix is a one-line quoting correction. NOTE it also means the local red list and the CI red list are NOT the same list, which is itself worth carrying."}
{"ts": "2026-07-30T20:20:50.078Z", "seq": 211, "kind": "defect", "summary": "FOUR typed-error classes are missing their __init__ in the stub - the identical gap to the seventh pyright error, merely not yet exercised. MEASURED: 8 new pyright errors the moment any code constructs them with their documented kwargs.", "area": "src/python/fathomdb/_fathomdb.pyi", "epistemic": "verified", "id": "TC-140", "owner": "steward", "placement": "UNPLACED", "priority": "p3", "status": "open", "refs": ["seq:221"], "body": "errors.py installs typed inits at import time via _install_typed_init on IllegalTransitionError (from_state, to_state, legal), NotLifecycleAddressableError (id_space), ErasureIncompleteError (stage, detail) and ProjectionDestructiveError (name, delta). The stub declares only the ATTRIBUTES, not the __init__ signatures. Measured with a throwaway probe file: constructing them with those kwargs yields 8 new reportCallIssue errors.\n\nThis is exactly the shape of the existing seventh error (VectorEquivalenceMismatchError with reason=), which is red today only because one test happens to construct it. The other four are latent landmines that will redden CI for the next person who uses the documented API.\n\nZero marginal cost to close: same file, same edit as the TC-137 stub sync. Steward recommendation on record is to close it in that edit. PLACEMENT UNPLACED - owed to the HITL.\n\nSEPARATE FIDELITY NOTE, not fixed and not proposed here: _install_typed_init defaults every unpassed payload field to None, so at runtime these payload attributes can be None although the stub types several of them as non-optional str. That is a wider surface decision, pre-existing across all typed error leaves, and is NOT part of unbreaking CI."}
{"ts": "2026-07-30T20:21:23.730Z", "seq": 212, "kind": "caveat", "summary": "CI TEST-COVERAGE BLIND SPOT: there is NO full-workspace cargo test on Linux anywhere in ci.yml, and rust-macos runs WITHOUT --no-fail-fast so it aborts early. rust-windows is the only job that reliably surfaces the full Rust set - and it has been ~90 percent red since it first ran.", "area": ".github/workflows/ci.yml", "epistemic": "verified", "id": "TC-141", "owner": "steward", "placement": "UNPLACED", "priority": "p1", "status": "open", "refs": ["seq:221"], "body": "VERIFIED BY THE STEWARD from ci.yml directly, not taken from the probe report.\n\nTHE GAP. cargo test appears four times in ci.yml. Two are default-embedder-tests running NAMED TARGETS ONLY (-p fathomdb-engine --test eu5b_lockflip and -p fathomdb-cli --test cli_warm_cache). The other two are rust-windows (cargo test --workspace --no-fail-fast) and rust-macos (cargo test --workspace, NO --no-fail-fast). So the full Rust workspace suite runs on Linux ONLY inside the verify job, via agent-verify.sh then agent-test.sh then test-rust.\n\nWHY THAT MATTERS RIGHT NOW. verify has been dying in Bootstrap dev tooling since roughly 2026-07-09 on the seven pyright errors, so the Linux Rust workspace suite has NOT EXECUTED IN CI for about three weeks. Meanwhile rust-macos aborts at the first failure (usually the TC-72 lock-holder flake) before reaching later tests, and rust-windows - the one job with --no-fail-fast, which is the sole reason the tc57 red was ever visible - has been failing ~90 percent of runs.\n\nCONSEQUENCE: the engine test signal in CI has been substantially degraded for three weeks, entering the first publish since v0.8.9.\n\nSTEWARD RECOMMENDATION ON RECORD: 0.8.21, not Slice 40. Both fixes WIDEN VISIBILITY, which under the ruled fix-everything gate means importing an unknown quantity of new red into a unit that has already grown twice and may not be split. Neither affects what 0.8.20 actually publishes (x86_64-linux only, dist-tag next). PLACEMENT UNPLACED - owed to the HITL."}
{"ts": "2026-07-30T20:21:23.758Z", "seq": 213, "kind": "caveat", "summary": "pyright is UNPINNED (pyright>=1.1.380) in both the typecheck and dev extras, so bootstrap.sh installs whatever is newest and CI can redden overnight with no repository change. A supply-chain hazard on the gate that currently blocks publish.", "area": "src/python/pyproject.toml", "epistemic": "verified", "id": "TC-142", "owner": "steward", "placement": "UNPLACED", "priority": "p3", "status": "open", "refs": ["seq:221"], "body": "Observed while dating the verify red. CI run 30566757420 installed pyright 1.1.411; the local .venv holds 1.1.409. Nothing pins the floor tighter than 1.1.380.\n\nNOT THE CAUSE OF THE CURRENT RED - that is genuine stub drift dating to f2f843ed (2026-07-09), established separately. This entry records the STANDING hazard, not a diagnosis: a stricter pyright release can turn the bootstrap step red with no commit, on the exact step that currently gates verify and security and therefore publish gate (i).\n\nSTEWARD RECOMMENDATION ON RECORD: 0.8.21. Pinning changes bootstrap behaviour, and doing that on the publish slice trades a latent risk for an immediate one. PLACEMENT UNPLACED - owed to the HITL."}
{"ts": "2026-07-30T20:21:23.782Z", "seq": 214, "kind": "caveat", "summary": "OOS-18 SIGBUS IS LIVE ON THE PUBLISH GATE PATH: the Python harness can SIGBUS reading a live-WAL DB, and CIs verify job does NOT set FATHOMDB_SKIP_NETWORK_TESTS - so it is a SECOND random-red source on gate (i), alongside the test-rust/rust-macos lock-holder flake.", "area": "src/python/tests", "epistemic": "verified", "id": "OOS-18", "owner": "steward", "priority": "p2", "status": "open", "refs": ["seq:221"], "body": "Not a new defect - OOS-18 is already recorded in dev/plans/runs/0.8.20-slice-20c-output.json as a PRE-EXISTING harness hazard, not an engine defect: a stdlib sqlite3 mode=ro connection against a database the Rust engine is actively writing with a live embed worker. Recorded there at 5/30 and 3/30 runs.\n\nWHAT IS NEW is its bearing on the publish gate. A commissioned probe observed it at 3 of 4 whole-file runs on a debug maturin develop build, well above the recorded 10-17 percent, plausibly because a debug build widens the race window. Always the identical site: _vector_kind_registered in test_slice20c_flush_barrier.py reached from test_late_enrolment_backfills_rows_a_no_embedder_session_stranded.\n\nWHY IT MATTERS. Publish gate (i) requires every EXECUTED ci.yml job green ON THE LANDED COMMIT. Once the seven pyright errors are cleared, verify reaches its test stage for the first time in three weeks and this flake becomes live on that path. The HITL already ruled the analogous test-rust/rust-macos flake must be FIXED rather than waived, on the reasoning that a random red on a landed-commit gate can only be cleared by re-running until green, which is gate-laundering. THE SAME REASONING APPLIES HERE and the Steward is surfacing it rather than assuming the earlier ruling reaches a flake that was not on the list at the time.\n\nPLACEMENT UNPLACED - owed to the HITL."}
{"ts": "2026-07-30T20:22:00.763Z", "seq": 215, "kind": "observation", "summary": "PLACED IN SLICE 40 by HITL 2026-07-30. TC-91 is the cause of the rust-windows red: commit_projection_outcomes opens DEFERRED while commit_batch opens IMMEDIATE. MEASURED 83 failures / 92 executed Windows runs = 90.2 percent - NOT a flake.", "decider": "hitl", "epistemic": "verified", "id": "TC-91", "placement": "0.8.20 Slice 40", "status": "placed", "refs": ["seq:221"], "body": "The rust-windows red was reported-only for several sessions and never diagnosed. A commissioned probe bucketed it and the Steward verified every load-bearing claim from source.\n\nBUCKET (c): a platform-neutral defect that Windows timing exposes past a Linux-calibrated bound. Definitively NOT the lock-holder flake class - no DatabaseLocked panic, and macOS is 21/21 green on identical source. NOT a timing artifact: constraining cores locally with taskset moved the number 12 percent, not 250.\n\nMECHANISM, STEWARD-VERIFIED. TransactionBehavior::Immediate appears EXACTLY ONCE in the whole engine - commit_batch at lib.rs:18706, TC-57s fix. commit_projection_outcomes at lib.rs:13878 opens a plain connection.transaction(), i.e. DEFERRED, and has the same read-then-upgrade shape - the residual risk TC-57s own design doc named and deliberately left. Its error is DISCARDED at lib.rs:12780 and lib.rs:12893 via let _ = , so a failed commit leaves terminal IS NULL, the row is re-dispatched, and it re-embeds. Re-embeds are therefore the only observable.\n\nTHE DEFECT SHIPS ON LINUX. Windows repeat_embeds median 370 over 200 rows; LINUX measures 105 to 119 - roughly every other row embedded twice, on the platform 0.8.20 actually publishes. That is TC-91(a), the ~50 percent baseline duplicate rate, open and unowned since Slice 21. Windows is the same defect at 3-5x because lock occupancy is 81 percent there versus 60 percent on Linux.\n\nFIX SHAPE: (1) stop discarding the error at 12780/12893 so the cause is MEASURED rather than inferred - the one inference in the chain; (2) open commit_projection_outcomes IMMEDIATE, the same one-line change TC-57 applied to the sibling. With BEGIN IMMEDIATE the busy handler is consulted, so the existing 5s timeout absorbs contention.\n\nACCEPTANCE BAR: N=5 consecutive green rust-windows runs. At 90.2 percent failure a single green happens by luck about 10 percent of the time. No local Windows box exists; CI is the only sufficient check, at 10-15 minutes per run, non-parallelisable, unverifiable before pushing. THIS IS SLICE 40s LONG POLE.\n\nHONEST LIMIT: nobody can say whether TC-57s fix CAUSED the Windows excursion. The instrument landed in the same commit series, so there is no Windows before. The mechanism predicts it worsened it; that is a prediction, not a measurement.\n\nTEST HYGIENE FOUND: the assertion doc comment claims a 10 percent bound while the code asserts 100 percent (stale), and the ANONYMOUS arm carries no repeat_embeds assertion at all - which is why only half the red was ever visible."}
{"ts": "2026-07-30T20:22:00.786Z", "seq": 216, "kind": "observation", "summary": "RESOLVED. The sanctioned rebuild cleared the stale native module. test-python REAL state measured: rc=0, 863 passed, 6 skipped, with the one known SIGBUS-flaky test deselected. It was never 40 failures - it was one unrunnable prerequisite.", "epistemic": "verified", "id": "TC-136", "status": "resolved", "refs": ["seq:221"], "body": "Cleared under the Stewards diagnosis mandate, in the PRIMARY checkout only, via the repo-sanctioned mechanism rather than an ad-hoc maturin invocation: agent-test.sh exports FATHOMDB_TESTS_ALLOW_REBUILD=1 only when the interpreter is this checkouts .venv/bin/python, and conftest.py then decides at import time whether to rebuild.\n\n_fathomdb.abi3.so went from 251,017,512 bytes dated Jul 9 to 257,061,352 bytes dated Jul 30. The artifact is gitignored (.gitignore:38 *.so, Steward-verified via git check-ignore), so nothing tracked was dirtied and the working tree stayed clean.\n\nThe ~40 identical ImportError collection errors are gone. This confirms the fourth-report-state discipline was right: recording test-python as 40 FAILURES would have put a large false red into the exact document the HITL disposition decision was made from. The real answer is 863 passing tests.\n\nRESIDUAL, tracked separately: the remaining non-zero exit is the OOS-18 SIGBUS flake, not TC-136."}
{"ts": "2026-07-30T20:22:00.809Z", "seq": 217, "kind": "observation", "summary": "QUANTIFIED: a stub-only edit clears SIX of the seven pyright errors, MEASURED. The seventh is not a stub defect at all - it is the graph.py mapper bug now tracked as TC-138. Also: the stub CANNOT be regenerated; it is hand-maintained.", "epistemic": "verified", "id": "TC-137", "placement": "0.8.20 Slice 40", "status": "placed", "refs": ["seq:221"], "body": "Measured by a commissioned probe against a scratch copy of src/python, using the exact pyright invocation agent-typecheck.sh uses, after first reproducing the 7-error baseline in the copy. Stub-only edit gives rc=1 with exactly one error remaining; stub plus the graph.py fix gives rc=0.\n\nTHE SIX. Engine.dense_disabled, .dense_disabled_reason and .vector_equivalence_refusal_count are #[pymethods] on PyEngine and simply absent from the stub. OpenReport.dense_disabled and .dense_disabled_reason are get_all fields on PyOpenReport, absent from the stub. VectorEquivalenceMismatchError needs an __init__ with reason= - note this is a PYTHON-side construct installed by errors.py _install_typed_init, NOT a Rust export, so an implementer reading only lib.rs would wrongly conclude the test is at fault and delete the kwarg.\n\nTHE SEVENTH is graph.py:153 - see TC-138. Pyright is nominal for dataclasses so no stub relaxation makes the native and SDK IdSpace assignable; the only stub-level silencer would assert a subclass relationship that is FALSE at runtime.\n\nORIGIN, Steward-verified: errors 1-5 and 7 all trace to ONE commit, f2f843ed (2026-07-09, 0.8.18 Slice 5) - engine.py, the test and the Rust binding landed together and the stub alone was not updated.\n\nSURFACE AUDIT, otherwise clean: 24/24 classes, 18/18 functions with matching signatures and defaults, 28/28 exception leaves with correct bases, and field-level agreement on 22 structs. OpenReport was the only field-level gap."}
{"ts": "2026-07-30T21:06:07.515Z", "seq": 218, "kind": "observation", "summary": "PLACED IN SLICE 40 by HITL 2026-07-30, BOTH HALVES: the IdSpace conversion AND the source_id/ce_score restore. The Steward recommended fixing both and did not place either.", "decider": "hitl", "epistemic": "verified", "id": "TC-138", "placement": "0.8.20 Slice 40", "status": "placed", "refs": ["seq:222"], "body": "The IdSpace half was unavoidable regardless - it is the seventh pyright error and publish gate (i) cannot pass without it. The provenance half was a genuine scope decision: it is NOT on Slice 39.5s baseline list, pyright cannot see it, and it could have been deferred.\n\nHITL ruled BOTH into Slice 40. Steward reasoning offered and accepted: they are the same two lines of the same mapper, and shipping a known provenance-dropping defect in a function just edited - on the release whose erasure axis IS provenance - is the worse outcome.\n\nIMPLEMENTATION NOTE for the brief: the fix is to add IdSpace to the fathomdb.types import block in graph.py and build id=IdSpace(space=h.id.space, value=h.id.value), plus pass source_id=h.source_id and ce_score=h.ce_score - matching what engine.py already does at its own SearchHit construction. RED-first: a test asserting isinstance(hit.id, types.IdSpace) and a populated source_id from search_expand.\n\nRISK carried into the brief: this may flip a currently-PASSING test that asserts todays wrong behaviour. Run the full Python suite after, not just the new test."}
{"ts": "2026-07-30T21:06:07.546Z", "seq": 219, "kind": "observation", "summary": "PLACED IN SLICE 40 by HITL 2026-07-30. One-line quoting fix for the unquoted $PRUNE_EXPR glob expansion that reddens test-md-generators.", "decider": "hitl", "epistemic": "verified", "id": "TC-139", "placement": "0.8.20 Slice 40", "status": "placed", "refs": ["seq:222"], "body": "Steward recommendation accepted: fix in 40 rather than defer, because leaving it means the LOCAL red list and the CI red list diverge, and the orchestrator working checkpoint BASE would spend time explaining a red that is not on any baseline list.\n\nCI reachability remains UNDETERMINED - it fires only where a repo-root directory matches one of the eight glob patterns, and those are untracked local artifacts. So this may be invisible in CI. That does NOT change the ruling: the bug is real either way and the fix is one line."}
{"ts": "2026-07-30T21:06:07.572Z", "seq": 220, "kind": "observation", "summary": "PLACED IN SLICE 40 by HITL 2026-07-30: close the four latent typed-error __init__ stubs in the SAME edit as the TC-137 stub sync.", "decider": "hitl", "epistemic": "verified", "id": "TC-140", "placement": "0.8.20 Slice 40", "status": "placed", "refs": ["seq:222"], "body": "Zero marginal cost - same file, same edit, and it forecloses 8 pyright errors that would redden CI for the next person who uses the documented API. Steward recommendation accepted.\n\nSCOPE BOUNDARY, to be stated in the brief: this closes the missing __init__ SIGNATURES only. The separate fidelity question - that _install_typed_init defaults unpassed payload fields to None while the stub types several as non-optional str - is NOT in scope, is pre-existing across all typed error leaves, and is a wider surface decision."}
{"ts": "2026-07-30T21:06:07.596Z", "seq": 221, "kind": "observation", "summary": "PLACED AT 0.8.21 by HITL 2026-07-30: both halves - add --no-fail-fast to rust-macos, and give Linux a full-workspace cargo test in ci.yml. NOT Slice 40.", "decider": "hitl", "epistemic": "verified", "id": "TC-141", "placement": "0.8.21", "status": "placed", "refs": ["seq:222"], "body": "Steward recommendation accepted. Both changes WIDEN VISIBILITY, which under the ruled fix-everything gate means importing an unknown quantity of new red into a unit that has already grown substantially and may not be split. Neither affects what 0.8.20 actually publishes (x86_64-linux only, dist-tag next).\n\nCARRIED CONSEQUENCE, and it must not be lost: until this lands, the Linux full Rust workspace suite runs in CI ONLY inside the verify job, and rust-macos continues to abort at its first failure. So after 0.8.20 the CI Rust signal is still narrower than it looks. 0.8.21 closes it."}
{"ts": "2026-07-30T21:06:07.620Z", "seq": 222, "kind": "observation", "summary": "PLACED AT 0.8.21 by HITL 2026-07-30: pin pyright rather than pyright>=1.1.380. NOT Slice 40 - pinning changes bootstrap behaviour and the publish slice is the wrong venue.", "decider": "hitl", "epistemic": "verified", "id": "TC-142", "placement": "0.8.21", "status": "placed", "refs": ["seq:222"], "body": "Steward recommendation accepted. Pinning on the publish slice would trade a latent risk for an immediate one: bootstrap.sh is the step that currently gates verify and security and therefore publish gate (i), and changing how it resolves its toolchain immediately before the first publish since v0.8.9 is the wrong trade.\n\nNOTE for whoever implements it at 0.8.21: pin the version that is GREEN at that time, and pin it in BOTH places - the typecheck extra and the dev extra in src/python/pyproject.toml."}
{"ts": "2026-07-30T21:06:07.646Z", "seq": 223, "kind": "observation", "summary": "PLACED IN SLICE 40 by HITL 2026-07-30: the SIGBUS is to be FIXED, not waived. A focused probe on the FIX SHAPE was also authorized and runs before commissioning.", "decider": "hitl", "epistemic": "verified", "id": "OOS-18", "placement": "0.8.20 Slice 40", "status": "placed", "refs": ["seq:222"], "body": "Ruled on the same reasoning the HITL applied to the test-rust/rust-macos lock-holder flake: publish gate (i) is measured on the LANDED commit, so a random red can only be cleared by re-running until green, which is gate-laundering rather than a green.\n\nTHE STEWARD FLAGGED ITS OWN INCONSISTENCY and the HITL acted on it. Recommending this into Slice 40 reintroduced exactly the undiagnosed-work risk the whole diagnosis pass existed to remove: every other Slice 40 item has a named fix and this one did not. The HITL authorized option (a) - one short focused probe on the fix shape BEFORE commissioning - so the slice is specified rather than carrying a research assignment with a landing deadline.\n\nSEQUENCING CONSEQUENCE, already reflected in the BASE plan: this fix and the lock-holder serialisation MUST both land in the push that starts the CI clocks. With either flake still live, a red rust-windows or verify run cannot be attributed between the TC-91 fix failing and a flake firing."}
{"ts": "2026-07-30T21:06:07.671Z", "seq": 224, "kind": "observation", "summary": "SPLIT RULED by HITL 2026-07-30: the tc57 FIX and the stale 10-percent doc comment are Slice 40s; the NEW anonymous-arm repeat_embeds assertion defers to 0.8.21.", "decider": "hitl", "epistemic": "verified", "id": "TC-91", "placement": "0.8.20 Slice 40 (fix) + 0.8.21 (anonymous-arm assertion)", "status": "placed", "refs": ["seq:222"], "body": "Steward recommendation accepted. Adding the anonymous-arm assertion now means adding a guard with ZERO data points on what it would report - cargo captures passing tests stdout, so no Windows or macOS anonymous-arm repeat_embeds number exists anywhere. On an un-splittable slice under a fix-everything gate, that is an unbounded new red.\n\nThe fix itself is expected to collapse BOTH arms toward zero, so the assertion is better written at 0.8.21 against a measured post-fix baseline than guessed at now.\n\nThe stale doc comment - it claims a 10 percent bound while the code asserts 100 percent - is corrected in Slice 40. It is free and it removes a live misreading hazard from the exact file the slice is editing."}
{"ts": "2026-07-30T21:29:23.627Z", "seq": 225, "kind": "caveat", "summary": "PRODUCT-FACING: opening a stdlib sqlite3 connection to the DB file while a FathomDB Engine is open IN THE SAME PROCESS is unsafe - the engine bundles its own SQLite and Pythons links the system one, and two copies in one process steal each others POSIX locks. Consumer-facing docs gap.", "area": "src/python", "epistemic": "verified", "id": "TC-143", "owner": "steward", "placement": "UNPLACED", "priority": "p2", "status": "open", "refs": ["seq:224"], "body": "Surfaced by the OOS-18 fix-shape probe and Steward-verified from source.\n\nMECHANISM. src/rust/crates/fathomdb-engine/Cargo.toml declares rusqlite with features = [\"bundled\"], so the engine links its OWN statically-bundled SQLite; Pythons sqlite3 links the system libsqlite3 (3.45.1 on this box). SQLites WAL concurrency contract is defined over PROCESSES, and its workaround for POSIX advisory-lock semantics - where close() of ANY fd on an inode drops that processs locks on it - lives in a per-library-copy inode cache the two copies do not share. Two copies in one process therefore steal each others locks and invalidate each others -shm mappings.\n\nMEASURED: an in-process stdlib probe against a live-WAL DB while the engine is open SIGBUSes 25 times in 60; the identical probe in a CHILD process, 0 in 60.\n\nWHY THIS IS NOT MERELY A TEST ISSUE. OOS-18 is classified in-repo as a HARNESS hazard, not an engine defect. That is correct ABOUT THE TEST. But the same configuration is reachable by any consumer of the Python binding who opens a stdlib sqlite3 connection - for inspection, migration, a dashboard - while holding an open Engine in the same process. Reading from a SEPARATE process (the sqlite3 CLI, a DB browser, eval/verify_embed_db.py) is safe.\n\nWHAT IS OWED: a consumer-facing note on the Python surface stating the constraint. NOT Slice 40s - the slice fixes the test, and the Steward is not placing this. NOTE the TS side already states the discipline explicitly: the slice20c mirror in src/ts/tests declares node:sqlite is used only as a READ oracle on a CLOSED database. The Python test had no equivalent rule and drifted.\n\nPLACEMENT UNPLACED - owed to the HITL."}
{"ts": "2026-08-01T01:24:11.244Z", "seq": 226, "kind": "consideration", "summary": "B5 post-circuit-breaker decision opened: choose a materially different trusted test-only verification design or re-scope B5.", "area": "release/0.8.20", "epistemic": "verified", "id": "TC-144", "owner": "steward", "priority": "p0", "status": "open", "refs": ["plan:dev/plans/plan-0.8.20.md", "file:dev/plans/runs/STEWARD-SESSION-HANDOFF-2026-07-31-B.md"], "body": "B5 CI/source-route candidate 52108139 was rejected after three containment P1 findings; the terminal bypass accepts actionlint-valid job-level permissions: write-all. The circuit breaker forbids a fourth same-class repair. The decision is required before E1 can close or E2 can begin."}
{"ts": "2026-08-01T01:24:22.103Z", "seq": 227, "kind": "consideration", "summary": "B5 verification-design decision resolved: HITL authorized a materially different trusted test-only design; no re-scope or deferral.", "decider": "hitl", "epistemic": "verified", "id": "TC-144", "owner": "steward", "status": "done", "refs": ["seq:226", "steward:seq-235"], "body": "The next commission may design a new trusted test-only verification route. It must not repair or integrate the rejected CI/source-route candidate, dispatch CI, publish, or advance E2 until independently reviewed."}
{"ts": "2026-08-19T12:32:24.812Z", "seq": 228, "kind": "todo", "summary": "platform-capabilities.json cannot represent Tegra: manifest row shape assumes every platform is an npm loader triple with a Rust target", "area": "release/0.8.23", "epistemic": "verified", "id": "TC-54aecaa0-ba2d-45e7-a8f5-cab60fefbe3c", "owner": "steward", "priority": "p2", "status": "open", "refs": ["file:dev/plans/runs/0.8.23-slice-80-open-items.md", "file:dev/tegra-platform-reference.md"], "body": "Tegra has no npm package (D-80.7-3 rules npm permanently out of scope) and no distinct Rust target triple (aarch64-unknown-linux-gnu covers both Tegra and SBSA). Adding a naive row either breaks check-platform-capabilities.sh or forces inventing an npm package name the ruling forbids. Needs an artifact-family concept in the manifest before Tegra can be represented at all. This is a design question, not a fix."}
{"ts": "2026-08-19T12:32:34.270Z", "seq": 229, "kind": "todo", "summary": "README claims 9 Rust workspace members; Cargo.toml has 10 — check-public-doc-truth is RED and release-blocking", "area": "release/0.8.23", "epistemic": "verified", "id": "TC-4c1c89bc-7a78-47cb-85f1-393977486bbe", "owner": "steward", "priority": "p1", "status": "open", "refs": ["file:dev/plans/runs/0.8.23-slice-80-open-items.md", "git:6c6f79d3"], "body": "README.md:43 reads 'Nine Rust workspace members live under src/rust/crates/'. Cargo.toml lists 10 after 6c6f79d3 added fathomdb-tc5-benchmark. scripts/check-public-doc-truth.py fails on this. Already inside the known test-public-doc-truth baseline failure, so it is not new breakage, but it must clear before 0.8.23 GA. One-word fix; belongs to a doc-drift slice rather than Slice 80."}
{"ts": "2026-08-19T12:32:45.656Z", "seq": 230, "kind": "todo", "summary": "cargo clippy -p fathomdb-embedder --features embed-cuda is NOT clean (7 errors) — must never be described as green", "area": "release/0.8.23", "epistemic": "verified", "id": "TC-91568e24-f36b-4dde-8425-f0a619ed49c0", "owner": "steward", "priority": "p2", "status": "open", "refs": ["file:dev/plans/runs/0.8.23-slice-80-open-items.md"], "body": "cargo clippy -p fathomdb-embedder --features embed-cuda --all-targets -- -D warnings fails with 7 real errors: unused import and never-used items (load_pinned_default_embedder_from_local_asset, Tc5DeviceAttestation, Tc5CandleConstruction, ExplicitCandleDevice, cuda_uuid/cuda_ordinal/into_embedder) that are live only under tc5-benchmark and dead in an embed-cuda-without-tc5-benchmark build. Pre-existing: 9 errors at 2dbe7c63, 7 now — Slice 80.5 improved it. NOTE a naive 'grep -c ^error' reports 9 because two lines are 'could not compile' summaries; the real count is 7. An implementer once reported this gate clean; it is not."}
{"ts": "2026-08-19T12:32:53.700Z", "seq": 231, "kind": "todo", "summary": "ir_c_recall_run.rs:327 fails E0308 under --features embed-cuda (pre-existing, untouched crate)", "area": "release/0.8.23", "epistemic": "verified", "id": "TC-50845d9a-f2b7-4e28-bd06-37d40b407f1f", "owner": "steward", "priority": "p2", "status": "open", "refs": ["file:dev/plans/runs/0.8.23-slice-80-open-items.md"], "body": "cargo check -p fathomdb-engine --test ir_c_recall_run --features embed-cuda fails with E0308 at src/rust/crates/fathomdb-engine/tests/ir_c_recall_run.rs:327. Reproduced independently on this Jetson. Pre-existing and in a file Slice 80 never touched."}
{"ts": "2026-08-19T12:33:06.806Z", "seq": 232, "kind": "todo", "summary": "check-cuda-release-contract.py imports datetime.UTC and dies on Python 3.10 — latent for any host without the repo 3.13 venv", "area": "release/0.8.23", "epistemic": "verified", "id": "TC-6c29783d-c165-4c1a-82c3-cb5b6d4e289a", "owner": "steward", "priority": "p2", "status": "open", "refs": ["file:dev/plans/runs/0.8.23-slice-80-open-items.md"], "body": "scripts/check-cuda-release-contract.py:17 does 'from datetime import UTC, datetime'. datetime.UTC is 3.11+. System python3 on this Jetson is 3.10 and fails with ImportError: cannot import name 'UTC' from 'datetime'. Works under the repo .venv (3.13). Any hosted job, fresh box, or remote transfer lane that runs it with system python will die on import. Fix is datetime.timezone.utc, or an explicit interpreter floor."}
{"ts": "2026-08-19T12:33:16.802Z", "seq": 233, "kind": "consideration", "summary": "Tegra GPU witness depends on undocumented nvmap page-pool behavior and on an unenforced sole-GPU-consumer precondition", "area": "release/0.8.23", "epistemic": "verified", "id": "TC-fea1bcce-f0ab-47a9-bf96-ba473f886696", "owner": "steward", "priority": "p3", "status": "watching", "refs": ["file:dev/tegra-platform-reference.md", "file:dev/plans/runs/0.8.23-slice-80-open-items.md"], "body": "Two coupled watch items. (1) Tegra's nvgpu/nvmap page pool absorbs freed pages, so a warm pool hides a later allocation from cuMemGetInfo entirely — measured: a 133,466,304-byte allocation moved the counter by 0 bytes. The witness works around this with a pool-draining control allocation, which relies on behavior NVIDIA does not document and a future JetPack could change. (2) The sole-GPU-consumer precondition is stated, not enforced; a concurrent GPU tenant could mask or inflate the delta. Both failure modes surface as failing closed (R80-12 names an unattributable delta as a failure), never as a false pass. Re-check on JetPack/L4T upgrade."}
{"ts": "2026-08-19T12:33:26.127Z", "seq": 234, "kind": "consideration", "summary": "libtest has no skipped status: the GPU-gated SKIP arm prints its reason but the harness line still reads ok", "area": "release/0.8.23", "epistemic": "verified", "id": "TC-ac385b3e-ec84-4972-8359-a60f3e1bb03a", "owner": "steward", "priority": "p3", "status": "watching", "refs": ["file:dev/plans/runs/0.8.23-slice-80-open-items.md"], "body": "Env-gated GPU tests print an explicit named skip reason and then pass, because libtest has no runtime skipped state. Not a false pass in substance — nothing in the skipped arm asserts GPU engagement — but the summary line reads as a pass to anyone scanning output. Worth a harness-level convention if the pattern spreads."}
{"ts": "2026-08-19T12:33:36.388Z", "seq": 235, "kind": "todo", "summary": "Implementer subagents need the git stash prohibition in their briefs — the stash stack is shared across worktrees and sessions", "area": "process", "epistemic": "verified", "id": "TC-93ef9590-5770-437b-aa73-750e3f6d6fa9", "owner": "steward", "priority": "p2", "status": "open", "refs": ["file:dev/plans/runs/0.8.23-slice-80-open-items.md", "file:dev/design/orchestration.md"], "body": "The 80.6.5 implementer used git stash / git stash pop to isolate pre-existing working-tree changes it did not own. The stash stack is shared across every worktree and every concurrent session, so a bare pop can take another session's work. No damage occurred this time — stash list verified empty afterwards and all doc commits intact — but that was luck, not discipline. The prohibition currently lives in the orchestrator's environment notes and does not reach implementer briefs. Either add it to the implementer agent definition or to dev/design/orchestration.md's PREAMBLE. Preferred alternative for setting work aside: a temporary WIP commit."}
{"ts": "2026-08-19T21:49:24.694Z", "seq": 236, "kind": "todo", "summary": "Slice 40 fixture-scoped scale characterization moved out of 0.8.23; preserve the V2 runner/validator design and resume as a separately commissioned backlog item", "area": "release/backlog", "id": "TC-8ccd1cf1-8e7d-4b7c-8efa-176663aed553", "owner": "hitl", "priority": "p2", "status": "open", "refs": ["plan:dev/design/0.8.23-scale-characterization-v2.md", "plan:dev/design/0.8.23-scale-artifact-v2.schema.json", "seq:254"]}
{"ts": "2026-08-21T20:08:26.499Z", "seq": 237, "kind": "caveat", "summary": "Merged performance-branch caveat: LOCOMO GPU arms need a dedicated CUDA-feature Python artifact, not only FATHOMDB_EMBED_DEVICE.", "id": "TC-83883483-1094-47a1-a096-16854f3bcbae", "source_branch": "experiments/performance-experiments-20260815", "source_seq": "228", "status": "watching", "refs": ["git:4619bddc"]}
